# 流程图模型与节点原语设计

> 讨论如何将标准流程图（ISO 5807 / BPMN 2.0）的图形元素映射到 quantalithos-flow 的工作流模板，
> 使工作流模板能够完整表达流程图，支持可视化渲染。

---

## 一、问题背景

当前 flow 概要设计的工作流模板只有两种节点类型（task / session），只能表达线性流程 + 失败回退。
无法表达项目管理中常见的流程图元素：条件分支、并行分支、人工审批点、循环等。

如果要实现"用户在前端看到完整的项目流程图，并实时看到当前走到哪一步"，
工作流模板必须能完整表达标准流程图的所有图形元素。

---

## 二、标准流程图的图形元素

```
┌─────────────────────────────────────────────────────────────────┐
│  图形              名称              含义                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ╭───╮            起止符            流程的开始和结束              │
│  ╰───╯            (Terminator)                                  │
│                                                                 │
│  ┌───────┐        处理              执行一个动作                 │
│  │       │        (Process)                                     │
│  └───────┘                                                      │
│                                                                 │
│    ╱╲              判断              根据条件选择分支             │
│   ╱  ╲             (Decision)                                   │
│   ╲  ╱                                                          │
│    ╲╱                                                           │
│                                                                 │
│  ╱      ╲          输入/输出         数据的输入或输出             │
│ ╱        ╲         (I/O)                                        │
│ ╲        ╱                                                      │
│  ╲      ╱                                                       │
│                                                                 │
│  ┌──────────┐     文档              产出的文档/产物              │
│  │          │     (Document)                                    │
│  └────╮─────┘                                                   │
│                                                                 │
│  ────────>        流程线            连接各节点，表示执行顺序      │
│  (Arrow)                                                        │
│                                                                 │
│  ┌─ ─ ─ ─ ─┐     子流程            内部包含完整流程              │
│  │    +     │     (Sub-Process)                                 │
│  └─ ─ ─ ─ ─┘                                                   │
│                                                                 │
│  ═══╤═══          并行线            同时执行多个分支              │
│     │             (Parallel Bar)                                │
│  ═══╧═══                                                        │
│                                                                 │
│  ┌─────────┐      人工操作          需要人类介入                 │
│  │ 👤      │      (Manual)                                     │
│  └─────────┘                                                    │
│                                                                 │
│  ○                连接符            跨页/跨区域连接              │
│  (Connector)                                                    │
│                                                                 │
│  ┌╌╌╌╌╌╌╌╌┐      注释              补充说明                    │
│  ╎        ╎      (Annotation)                                  │
│  └╌╌╌╌╌╌╌╌┘                                                    │
└─────────────────────────────────────────────────────────────────┘
```

---

## 三、与 BPMN 2.0 的对应关系

| 流程图图形 | BPMN 2.0 对应 | 说明 |
|-----------|---------------|------|
| 起止符 | Start Event / End Event | 流程入口和出口 |
| 处理 | Task（User Task / Service Task） | 执行一个动作 |
| 子流程 | Sub-Process | 内部包含完整流程 |
| 判断 | Exclusive Gateway (XOR) | 根据条件选择一条分支 |
| 并行线 | Parallel Gateway (AND) | 同时执行多条分支，全部完成后合并 |
| 人工操作 | User Task + Message Event | 需要人类操作确认 |
| 文档 | Data Object | 流程中产出或消费的数据 |
| 流程线 | Sequence Flow | 连接节点，表示执行顺序 |
| 注释 | Text Annotation | 补充说明，不影响执行 |

---

## 四、当前概要设计的差距

| 流程图元素 | 当前概要设计 | 差距 |
|-----------|-------------|------|
| 起止符 | 隐含在 `trigger: stage_entered` 和 `next: null` | ⚠️ 没有显式建模 |
| 处理 | `mode: task` 的 step | ✅ 已有 |
| 子流程 | `mode: session` 的 step + Agent 级 workflow | ✅ 已有 |
| 判断 | `on_fail` 隐含了二选一 | ⚠️ 只能表达成功/失败，不能表达多条件分支 |
| 并行线 | TaskScheduler 的依赖图调度 | ⚠️ 是代码逻辑，不是模板里的显式节点 |
| 人工操作 | pipeline template 的 `gate_type` | ⚠️ 只在阶段边界，不能在 step 之间插入 |
| 文档 | `output_schema` | ✅ 已有 |
| 流程线 | `next` / `on_fail` | ⚠️ 只有两种边，缺 conditional |
| 注释 | 无 | ❌ 缺失 |

---

## 五、推荐方案：step 增加 type 字段

### 5.1 节点类型定义

```python
class StepType(str, Enum):
    """工作流节点类型 — 对应流程图图形"""
    TASK = "task"                 # ┌───────┐  处理：一个 Agent 执行任务
    SESSION = "session"           # ┌─ ─ ─ ─┐  子流程：多人协作讨论
    DECISION = "decision"         #   ╱╲      判断：根据上一步输出选择分支
    GATE = "gate"                 # ┌─👤──┐   人工审批：等待用户确认
    FORK = "fork"                 # ═══╤═══   并行分支：同时启动多个分支
    JOIN = "join"                 # ═══╧═══   并行合并：等待所有分支完成
```

> 注：起止符不需要显式建模 — 第一个 step 就是 start，`next: null` 就是 end。
> 文档/产物通过 `output_schema` 和 `input_refs` 表达，不需要独立节点。
> 注释通过 step 的 `description` 字段表达。

### 5.2 各类型的 YAML 格式

**task — 处理节点（已有，不变）：**

```yaml
- id: implement
  type: task
  actor: developer
  action: "按任务卡要求实现功能"
  allowed_tools: [file_read, file_write, code_execute, git_commit]
  workflow: tdd_workflow
  output_schema: {...}
  next: review_decision
```

**session — 子流程节点（已有，不变）：**

```yaml
- id: brainstorm
  type: session
  session:
    moderator: tech_lead
    participants: [tech_lead, backend_dev, frontend_dev, tester]
    agenda: "讨论需求的可行性、风险和建议"
  allowed_tools: [file_read, search_docs]
  output_schema: {...}
  next: scope_refinement
```

**decision — 判断节点（新增）：**

```yaml
- id: review_decision
  type: decision
  description: "根据代码审查结果决定下一步"
  input_ref: code_review.output       # 读取上一步的输出
  conditions:
    - when: "verdict == 'approved'"
      next: testing
    - when: "verdict == 'needs_revision'"
      next: implement
  default: implement                   # 所有条件都不满足时的默认分支
```

**gate — 人工审批节点（新增）：**

```yaml
- id: user_direction_confirm
  type: gate
  description: "用户确认需求方向"
  gate_config:
    title: "需求方向确认"
    gate_type: hard
    timeout_minutes: 4320
    artifacts_from: [scope_refinement]
    actions:
      - { action_type: approve, label: "方向正确" }
      - { action_type: request_changes, label: "需要调整" }
      - { action_type: reject, label: "方向不对" }
  on_approve: write_prd
  on_request_changes: collect_requirements    # 回到需求收集
  on_reject: null                             # 终止流程
```

**fork — 并行分支节点（新增）：**

```yaml
- id: parallel_dev_start
  type: fork
  description: "多任务并行开发"
  branches:
    - task_001_workflow      # 每个分支是一个独立的工作流实例
    - task_002_workflow
    - task_004_workflow
  join: parallel_dev_end     # 指向对应的 join 节点
```

**join — 并行合并节点（新增）：**

```yaml
- id: parallel_dev_end
  type: join
  description: "等待所有并行任务完成"
  fork: parallel_dev_start   # 指向对应的 fork 节点
  next: integration_test     # 全部完成后进入集成测试
```

### 5.3 边类型

```python
class EdgeType(str, Enum):
    """流程线类型"""
    SEQUENCE = "sequence"           # ────>  顺序：next 字段
    CONDITIONAL = "conditional"     # ---->  条件：decision 的 conditions
    ON_FAIL = "on_fail"             # ╌╌╌>  失败回退：on_fail 字段
    ON_APPROVE = "on_approve"       # ════>  审批通过：gate 的 on_approve
    ON_REJECT = "on_reject"         # ╳╳╳>  审批驳回：gate 的 on_reject
    FORK_BRANCH = "fork_branch"     # ═══>  并行分支：fork 的 branches
    JOIN_BRANCH = "join_branch"     # ═══>  并行合并：join 的 fork
```

---

## 六、改动前后对比

### 6.1 requirement_analysis 工作流

**改动前（只有 task/session + next/on_fail）：**

```
collect ──next──> brainstorm ──next──> write_prd ──next──> review_prd
                                          ↑                    │
                                          └────── on_fail ─────┘

问题：
  - 没有"用户方向确认"中间门禁
  - 没有"范围收敛"步骤
  - review_prd 不通过只能回到 write_prd，不能回到 collect
```

**改动后（完整流程图节点）：**

```
╭───╮
│ ○ │ start
╰─┬─╯
  │
  ▼
┌──────────────────┐
│ collect          │ type: task
│ 收集需求信息      │ actor: tech_lead
└────────┬─────────┘
         │
         ▼
┌─ ─ ─ ─ ─ ─ ─ ─ ┐
│ brainstorm       │ type: session
│ 团队头脑风暴      │ moderator: tech_lead
└─ ─ ─ ─ ┬ ─ ─ ─ ┘
         │
         ▼
┌──────────────────┐
│ scope_refinement │ type: task
│ 版本范围收敛      │ actor: tech_lead
└────────┬─────────┘
         │
         ▼
┌─────────────────┐
│ 👤 user_confirm │ type: gate
│ 用户方向确认     │ gate_type: hard
└────────┬────────┘
    ┌────┼────────────┐
    │    │             │
 approve │        request_changes
    │    │             │
    │  reject          │
    │    │             │
    │    ▼             ▼
    │  ╭───╮     collect（回到收集）
    │  │ ⊕ │
    │  ╰───╯ end
    │
    ▼
┌──────────────────┐
│ write_prd        │ type: task
│ 撰写 PRD         │ actor: tech_lead
└────────┬─────────┘
         │
         ▼
┌─ ─ ─ ─ ─ ─ ─ ─ ┐
│ review_prd       │ type: session
│ PRD 评审          │ moderator: tech_lead
└─ ─ ─ ─ ┬ ─ ─ ─ ┘
         │
         ▼
    ╱╲
   ╱  ╲
  ╱ review╲
  ╲decision╱             type: decision
   ╲  ╱
    ╲╱
    ┌┴────────────┐
    │             │
 approved    needs_revision
    │             │
    ▼             ▼
╭───╮        write_prd（回到撰写）
│ ⊕ │ end
╰───╯
```

### 6.2 iterative_development 工作流（并行任务）

**改动前（并行调度在代码里，模板看不出来）：**

```
implement ──next──> code_review ──next──> testing
    ↑                   │
    └──── on_fail ──────┘

（多任务并行是 TaskScheduler 代码逻辑，模板里看不到）
```

**改动后（并行分支显式表达）：**

```
╭───╮
│ ○ │ start
╰─┬─╯
  │
  ▼
═══╤═══ fork: parallel_dev_start
   │
   ├──────────────────────────────────────┐
   │                │                     │
   ▼                ▼                     ▼
┌────────┐    ┌────────┐           ┌────────┐
│TASK-001│    │TASK-002│           │TASK-004│
│用户认证 │    │文章CRUD│           │前端页面 │
│        │    │        │           │        │
│implement│   │implement│          │implement│
│  ↓     │    │  ↓     │           │  ↓     │
│review  │    │review  │           │review  │
│  ↓     │    │  ↓     │           │  ↓     │
│testing │    │testing │           │testing │
└───┬────┘    └───┬────┘           └───┬────┘
    │             │                     │
    │             │  TASK-002 done      │
    │             │  → TASK-003 ready   │
    │             │  ┌────────┐         │
    │             └─>│TASK-003│         │
    │                │标签系统 │         │
    │                │implement│        │
    │                │  ↓     │         │
    │                │review  │         │
    │                │  ↓     │         │
    │                │testing │         │
    │                └───┬────┘         │
    │                    │              │
═══╧════════════════════╧══════════════╧═══ join: parallel_dev_end
    │
    ▼
┌──────────────────┐
│integration_test  │ type: task
│集成测试           │
└────────┬─────────┘
         │
         ▼
       ╭───╮
       │ ⊕ │ end
       ╰───╯
```

---

## 七、Phase 分期

### Phase 1（当前）

只实现 `task` 和 `session` 两种节点类型，用 `next` / `on_fail` 连接。
gate 仍然定义在 pipeline template 的阶段边界上。
并行调度仍然由 TaskScheduler 代码逻辑处理。

**Phase 1 的模板格式不变**，本文档的设计在 Phase 2 实现。

### Phase 2

引入完整节点类型：

| 节点类型 | 优先级 | 说明 |
|---------|--------|------|
| decision | 高 | 替代 on_fail 的隐含判断，支持多条件分支 |
| gate | 高 | 从 pipeline 下沉到 step 级，支持中间门禁 |
| fork / join | 中 | 将并行调度从代码逻辑提升到模板显式表达 |

引入后的好处：
- 工作流模板可以完整表达流程图
- 前端可以直接从模板渲染流程图，不需要额外的可视化逻辑
- 流程变更只改模板 YAML，不改代码

### Phase 1 → Phase 2 兼容性

Phase 1 的模板在 Phase 2 中仍然有效：
- `type` 默认为 `task`（不写 type 就是 task）
- `next` / `on_fail` 仍然工作
- 阶段边界的 gate_type 仍然工作

Phase 2 是增量扩展，不是破坏性变更。

---

## 八、与 design 侧"流程模型与动态编排抽象"的对应

| design 侧概念 | 本文档对应 | 说明 |
|---------------|-----------|------|
| 节点原语：action | StepType.TASK | 一致 |
| 节点原语：discussion_subflow | StepType.SESSION | 合并为 session |
| 节点原语：gate | StepType.GATE | 一致 |
| 节点原语：event_wait | 暂不实现 | Phase 3 考虑（等待外部事件） |
| 节点原语：fork / join | StepType.FORK / JOIN | 一致 |
| 节点原语：decision | StepType.DECISION | 一致 |
| 节点原语：subflow | StepType.SESSION + Agent 级 workflow | 已有 |
| 边原语：sequence | EdgeType.SEQUENCE | 一致 |
| 边原语：conditional | EdgeType.CONDITIONAL | 一致 |
| 边原语：loop | on_fail 回退到前面的 step | 隐含表达 |
| 四层抽象：流程原语层 | 本文档第五章 | ✅ |
| 四层抽象：规范约束层 | 待补充（硬规则列表） | ⚠️ |
| 四层抽象：流程模板层 | flow 概要设计第四章 | ✅ |
| 四层抽象：项目实例化层 | flow 概要设计 4.7 模板编译 | ✅ |

---

## 九、待讨论

- 规范约束层（硬规则列表）：TL 不能裁剪哪些 step、不能跳过哪些 gate
- TailoringPolicy 的具体实现：五类可变能力（步骤裁剪、步骤合并、角色裁剪、轮次裁剪、门禁强度调整）如何在模板中表达
- decision 节点的条件表达式语法：`"verdict == 'approved'"` 用什么解析？简单字符串匹配还是表达式引擎？
- event_wait 节点（Phase 3）：等待外部事件（如 CI 构建完成、第三方 API 回调）

# 05-测试方案 · L0-bus

> 本文回答: 如何证明 `L0-bus` 的需求、架构、概要设计、详细设计和配置设计被正确实现,哪些证据可以支撑后续 `06-验收标准.md` 裁决。
> 本文不回答: Rust 结构体和函数如何实现、真实 MQ / DB / KMS 产品如何部署、验收是否通过、实施任务如何排期和提交。

---

## 文档元信息

| 项 | 内容 |
|---|---|
| 项目 | `L0-bus` |
| 文档类型 | 测试方案 |
| 当前版本 | v0.2.0 |
| 日期 | 2026-05-29 |
| 作者 | codex |
| 状态 | Draft |
| 前置文档 | `projects/L0-bus/00-需求文档.md` v0.2.0；`01-架构设计.md` v0.2.0；`02-概要设计.md` v0.2.0；`03-详细设计.md` v0.2.0；`04-配置设计.md` v0.1.0 |
| 校准工作台 | `projects/L0-bus/design-calibration/05_test_plan_calibration_flow.md` |
| 目标实现仓 | `/home/aris/Projects/quantalithos-bus` |
| 稳定上游 | `projects/L0-core/00~07`；实现仓 `/home/aris/Projects/quantalithos-core` |

### 读者导航

| 阅读目标 | 建议路径 |
|---|---|
| 确认测试边界和目标 | §1 -> §2 |
| 理解要测哪些对象和层级 | §3 -> §4 |
| 查需求、用例和证据追溯 | §5 -> §6 -> §13 |
| 准备数据、环境、自动化门禁 | §7 -> §8 -> §9 |
| 审查红线、缺陷、退出和风险 | §10 -> §11 -> §12 -> §14 |

### 变更记录

| 版本 | 日期 | 变更 | 作者 |
|---|---|---|---|
| v0.1.0 | 2026-05-17 | 旧版测试方案草案 | Claude |
| v0.2.0 | 2026-05-29 | 按新版测试方案 SOP 删除旧版并重建,收稳 L0-bus P0 测试范围、用例矩阵、自动化门禁、证据归档、缺陷复验和残余风险 | codex |

---

## 1. 与上游文档的关系声明

> 校准来源：
> - `design-calibration/05_test_plan_step_01_input_boundary.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“上游输入映射表”“测试方案不再回答的问题清单”和“测试方案必须回答的问题清单”小节，了解本文如何从新版 `00~04` 收敛。

本文只承接新版 `00-需求文档.md`、`01-架构设计.md`、`02-概要设计.md`、`03-详细设计.md` 和 `04-配置设计.md`。当前旧版 `06-验收标准.md` 只作为后续重校准风险参考,不得反向决定本测试方案事实。

| 来源文档 | 本文承接内容 | 回填章节 |
|---|---|---|
| `00-需求文档.md` | CL-001~CL-006、US-001~US-007、F-001~F-008、BR-001~BR-012、数据归属、非功能目标、验收方向 | §1、§2、§5、§6、§10 |
| `01-架构设计.md` | 职责边界、系统上下文、依赖方向、数据所有权、关键交互、横切关注点 | §1、§2、§4、§8、§10、§14 |
| `02-概要设计.md` | 主要组成部分、关键对象、接口骨架、处理流、状态机、异常边界、配置影响 | §3、§4、§6、§7、§8 |
| `03-详细设计.md` | 模块契约、对象 / trait / API、处理流、状态机、事务、错误、幂等、配置、观测、测试切口、脚本契约 | §3、§4、§6、§9、§10、§13 |
| `04-配置设计.md` | profile、配置项、敏感配置、加载校验、变更回滚、失效模式、reports / artifacts 承接 | §3、§8、§9、§10、§13 |

测试方案不重新定义需求、架构、对象、接口、状态机、配置项或验收裁决。本文只回答“如何验证这些结论”,并为 `06-验收标准.md` 提供可追溯证据。

---

## 2. 本次测试目标与范围

> 校准来源：
> - `design-calibration/05_test_plan_step_02_scope.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“测试目标表”“范围 / 非范围表”“一票否决候选与测试证据映射”小节，了解 P0 / P0-min / P1 / P2 的测试边界。

本次测试的目标是证明 L0-bus 的 P0 事件传递主闭环成立: 基于 `L0-core` 契约的发布材料可以进入 bus,形成统一传递语义,推进 delivery,记录 feedback 和幂等锚点,进入 retry / DLQ / replay preparation 恢复链,并输出只读 audit / tap / transport view / failure material。同时,测试必须证明配置控制面和证据归档路径可被验收引用。

| 测试目标 | 优先级 | 覆盖需求 / 设计 | 预期证据 |
|---|---|---|---|
| 验证契约绑定发布材料接入 | P0 | F-001、BR-001、BR-002 | publication acceptance 正反用例、rejection reason、audit evidence |
| 验证统一传递语义 | P0 | F-002、BR-003、BR-012 | semantic mapping assertions、backend boundary evidence |
| 验证 delivery 推进 | P0 | F-003、F-008 | delivery progression cases、history evidence |
| 验证 feedback 与幂等锚点 | P0 | F-004、BR-004、BR-006、BR-011 | ack / fail / timeout / duplicate feedback cases |
| 验证 retry、DLQ、replay preparation | P0 | F-005、BR-005、BR-008 | recovery cases、replay rejection evidence |
| 验证 audit、tap、transport view、failure material | P0 | F-006、BR-007、BR-009 | read-only query / output cases、no-write assertions |
| 验证 Outbox relay 边界 | P0-min | F-007、BR-010 | committed / uncommitted / duplicate outbox cases |
| 验证默认 backend / store / fixture path | P0-min | F-008、`03` §2.2 | in-memory integration evidence、adapter unsupported evidence |
| 验证配置控制面 | P0 | `04` §2、`04` §12 | config loader / validator / redaction / reload rejection cases |
| 验证报告与证据归档 | P0 | `00` §14、`04` §12 | artifacts and reports archive evidence |

当前不覆盖 production MQ / durable store 全量产品行为、gateway / auth / TLS、业务 payload 正文语义、governance decision truth、observability dashboard / alerting、SDK 高层开发者体验、DLQ Console UI、exactly-once / effectively-once、config center / hot reload / admin override、多后端和多租户矩阵。这些内容在 §14 作为残余风险或后续专项处理。

---

## 3. 测试对象与测试切口

> 校准来源：
> - `design-calibration/05_test_plan_step_03_test_objects_slices.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“测试对象与测试切口总表”“最小验证清单覆盖表”和“P0 对象覆盖状态表”小节，了解测试对象如何从 `02/03/04` 抽取。

测试对象按“业务组成部分 + 技术对象 + 横切行为”抽取,不按所有文件或所有 struct 等量展开。

| 测试对象 | 测试切口 | 推荐测试层级 |
|---|---|---|
| Publication domain objects | material 构造、accept / reject、不变量、payload boundary | domain unit |
| Transport semantic and backend boundary | semantic derive、capability mapping、禁止裸后端参数 | domain unit / service |
| Delivery domain objects | lifecycle、attempt、history、非法迁移 | domain unit / service |
| Feedback and idempotency objects | feedback 终态、duplicate、same / different digest | domain unit / service |
| Recovery objects and policy | retry、DLQ、replay preparation、eligibility | domain unit / service |
| Audit and read-only objects | audit append、projection derive、read-only policy | domain unit / service |
| Config objects and validator | defaults、JSON parse、env override、secret ref、reload rejected | config unit / integration |
| Application services | UoW、idempotency、repository / port 编排、audit / event 副作用 | service test |
| Repository ports | unique、expected version、lock、append-only、projection version | repository integration |
| External adapter ports | source、publisher、transport、clock、id generator | adapter integration |
| API handlers | HTTP JSON、header、validation、error mapping | API / contract |
| Worker consumers | inbound event schema、duplicate、retryable failure | consumer integration |
| Operations jobs | item transaction、cursor、partial success、summary | job runner |
| Outbound events | schema、topic、trace ref、forbidden body absent | contract / publisher |
| Observability and redaction | log、metric、audit、event、projection、evidence redaction | snapshot / redaction check |
| Reports and artifacts | artifact root、report root、redaction report、run summary | script / report check |

横切切口必须单列验证: publication acceptance state、delivery lifecycle、feedback result、retry / DLQ / replay state、projection status、UnitOfWork consistency、idempotency、error recovery、config failure mode、observability / redaction。

---

## 4. 测试策略与分层

> 校准来源：
> - `design-calibration/05_test_plan_step_04_strategy_layers.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“测试分层图”“测试分层表”和“阻断策略表”小节，了解各层测试分别发现哪些问题。

#### 测试分层图: L0-bus 测试金字塔

```text
+---------------------------------------------------------------+
| Release gate / E2E                                            |
| P0 closed loop, recovery chain, redaction, reports            |
+-------------------------------^-------------------------------+
                                |
+-------------------------------+-------------------------------+
| API / Consumer / Job / Contract                               |
| HTTP JSON, event schema, job protocol, error mapping           |
+-------------------------------^-------------------------------+
                                |
+-------------------------------+-------------------------------+
| Integration                                                   |
| repository, UnitOfWork, fake adapters, workers, runtime config |
+-------------------------------^-------------------------------+
                                |
+-------------------------------+-------------------------------+
| Service                                                       |
| application orchestration, idempotency, audit, side effects    |
+-------------------------------^-------------------------------+
                                |
+-------------------------------+-------------------------------+
| Unit                                                          |
| domain invariants, state machines, policies, config values     |
+---------------------------------------------------------------+
```

关键说明:

- 下层测试负责快速发现规则错误,上层测试负责验证接缝和闭环。
- P0 红线不能只依赖 release gate;unit / service / contract 层也必须前置断言。
- 当前 P0 integration 使用 in-memory / fake / fixture,不要求真实 MQ / DB 产品。

| 层级 | 目标 | 典型内容 | 执行时机 | 失败处理 |
|---|---|---|---|---|
| Unit | 验证纯规则、不变量、状态和 policy | domain objects、state enum、policy、config value、projection derive | 本地开发 / PR gate | P0 失败阻断 PR |
| Service | 验证 application 编排、事务顺序、幂等和副作用 | application service + fake repository / fake port / fake UoW | 本地开发 / PR gate | P0 失败阻断 PR |
| Integration | 验证 repository、adapter、worker、job 和 runtime graph 接缝 | in-memory store、fake backend、fixture source、runtime builder | PR gate / main CI | P0 失败阻断 |
| API / Consumer / Job / Contract | 验证外部协议、入口映射和错误响应 | HTTP route、event consumer、outbound event schema、job protocol | PR gate / main CI | P0 协议失败阻断 |
| Release gate / E2E | 验证最小闭环、红线和证据归档 | closed loop、recovery chain、redaction scan、reports generation | release gate / pre-acceptance | 失败阻断 release / acceptance |

---

## 5. 需求追溯与覆盖矩阵

> 校准来源：
> - `design-calibration/05_test_plan_step_05_traceability_matrix.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“核心能力闭环覆盖矩阵”“功能需求覆盖矩阵”“业务规则覆盖矩阵”和“未覆盖项清单”小节，了解需求、用例和证据如何关联。

#### 追溯链路图: 需求到证据

```text
+------------------+
| CL / F / BR      |
+--------+---------+
         |
         v
+--------+---------+
| Design basis     |
| 02 / 03 / 04     |
+--------+---------+
         |
         v
+--------+---------+
| Test scenario    |
| TS-BUS-xxx       |
+--------+---------+
         |
         v
+--------+---------+
| Test case        |
| TC-BUS-AREA-xxx  |
+--------+---------+
         |
         v
+--------+---------+
| Evidence         |
| EV-BUS / RP-BUS  |
+------------------+
```

关键说明:

- `CL / F / BR` 是需求和规则源头。
- `TS-BUS` 是场景分组,`TC-BUS` 是可执行用例。
- `EV-BUS / RP-BUS` 是 §13 的证据和报告编号。

| 需求 / 规则 ID | 设计依据 | 测试场景 | 用例 ID | 自动化 | 证据 ID | 覆盖状态 |
|---|---|---|---|---|---|---|
| CL-001 | `02` §5.4 / §8.3 / §8.4、`03` §8.1 | TS-BUS-001、TS-BUS-007 | TC-BUS-PUB-001、TC-BUS-OBX-001 | 是 | EV-BUS-PUB-001、EV-BUS-OBX-001 | 已覆盖 |
| CL-002 | `02` §6.5 / §6.22、`03` §13 | TS-BUS-002、TS-BUS-008 | TC-BUS-SEM-001、TC-BUS-BND-001 | 是 | EV-BUS-SEM-001、EV-BUS-BND-001 | 已覆盖 |
| CL-003 | `02` §5.5 / §8.5、`03` §9 | TS-BUS-003、TS-BUS-008 | TC-BUS-DLV-001、TC-BUS-BND-002 | 是 | EV-BUS-DLV-001、EV-BUS-BND-002 | 已覆盖 |
| CL-004 | `02` §5.6、`03` §10~§12 | TS-BUS-004 | TC-BUS-FDB-001、TC-BUS-FDB-002 | 是 | EV-BUS-FDB-001 | 已覆盖 |
| CL-005 | `02` §5.7 / §8.9~§8.12、`03` §11 | TS-BUS-005 | TC-BUS-REC-001、TC-BUS-REC-002、TC-BUS-REC-003 | 是 | EV-BUS-REC-001 | 已覆盖 |
| CL-006 | `02` §5.8 / §8.13~§8.17、`03` §14、`04` §12 | TS-BUS-006、TS-BUS-010 | TC-BUS-OUT-001、TC-BUS-RED-001 | 是 | EV-BUS-OUT-001、RP-BUS-RED-001 | 已覆盖 |
| F-001~F-008 | `02` §5~§11、`03` §5~§15、`04` §12 | TS-BUS-001~TS-BUS-010 | `TC-BUS-*` | 是 | `EV-BUS-*`、`RP-BUS-*` | 已覆盖 |
| BR-001~BR-012 | `02` §10 / §11、`03` §9~§15、`04` §8~§12 | TS-BUS-001~TS-BUS-010 | positive / negative / boundary / redaction cases | 是 | `EV-BUS-*`、`RP-BUS-*` | 已覆盖 |

P0 / P0-min 没有覆盖空洞。P1 production MQ / durable store adapter、P2 config center / hot reload / multi-backend、DLQ Console UI、observability dashboard 和 SDK convenience 进入 §14 残余风险或其他仓测试方案。

---

## 6. 测试场景与用例设计

> 校准来源：
> - `design-calibration/05_test_plan_step_06_cases.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“测试场景表”“用例矩阵”和“用例到证据映射表”小节，了解每条用例的前置条件、输入、断言和自动化候选层级。

| 场景 ID | 场景名称 | 优先级 | 必须覆盖的用例族 |
|---|---|---|---|
| TS-BUS-001 | 契约绑定发布材料接入 | P0 | `TC-BUS-PUB-001`~`004` |
| TS-BUS-002 | 平台级传递语义形成 | P0 | `TC-BUS-SEM-001`~`002` |
| TS-BUS-003 | delivery 推进与默认可验证路径 | P0 | `TC-BUS-DLV-001`~`004` |
| TS-BUS-004 | feedback、history 与幂等锚点 | P0 | `TC-BUS-FDB-001`~`004` |
| TS-BUS-005 | retry、DLQ 与 replay preparation | P0 | `TC-BUS-REC-001`~`004` |
| TS-BUS-006 | audit、tap、transport view 与 failure material | P0 | `TC-BUS-OUT-001`~`006` |
| TS-BUS-007 | Outbox relay 边界承接 | P0-min | `TC-BUS-OBX-001`~`002` |
| TS-BUS-008 | backend adapter 接缝与 capability | P0-min | `TC-BUS-BND-001`~`003` |
| TS-BUS-009 | 配置控制面与 runtime graph | P0 | `TC-BUS-CFG-001`~`003` |
| TS-BUS-010 | redaction、reports 与 artifacts 证据 | P0 | `TC-BUS-RED-001`~`002` |

| 用例 ID | 场景 | 优先级 | 输入 / 操作 | 预期结果 | 自动化候选 |
|---|---|---|---|---|---|
| TC-BUS-PUB-001 | TS-BUS-001 | P0 | valid core contract ref + payload ref -> `AcceptPublication` | accepted、audit append、no payload body | service / API |
| TC-BUS-PUB-002 | TS-BUS-001 | P0 | missing core contract ref -> `AcceptPublication` | rejected / validation error,no accepted truth | unit / API |
| TC-BUS-PUB-003 | TS-BUS-001 | P0 | command 含 payload body | boundary violation,forbidden body not persisted | unit / redaction |
| TC-BUS-PUB-004 | TS-BUS-001 | P0 | accepted / rejected 后尝试终态互改 | domain error / conflict | unit / service |
| TC-BUS-SEM-001 | TS-BUS-002 | P0 | accepted material + backend capability ref | platform semantic created,no raw backend param | unit / service |
| TC-BUS-SEM-002 | TS-BUS-002 | P0 | backend private field 注入 semantic | rejected / boundary violation | unit / service |
| TC-BUS-DLV-001 | TS-BUS-003 | P0 | scheduled delivery + fake backend success | dispatched / completed,history append | service / integration |
| TC-BUS-DLV-002 | TS-BUS-003 | P0 | scheduled delivery + backend unavailable | failed or retryable evidence | service / integration |
| TC-BUS-DLV-003 | TS-BUS-003 | P0 | completed delivery 尝试 reopen / skip attempt | conflict / domain error | unit / service |
| TC-BUS-DLV-004 | TS-BUS-003 | P0 | batch has success + failure item | partial success summary,item isolated | job runner |
| TC-BUS-FDB-001 | TS-BUS-004 | P0 | dispatched delivery -> record ack feedback | completed + feedback result,history append | service / API |
| TC-BUS-FDB-002 | TS-BUS-004 | P0 | same idempotency key + same digest | existing result,no duplicate truth | service / API |
| TC-BUS-FDB-003 | TS-BUS-004 | P0 | same idempotency key + different digest | conflict,no mutation | unit / service |
| TC-BUS-FDB-004 | TS-BUS-004 | P0 | unknown delivery or late feedback | not found / conflict,no orphan feedback | service / API |
| TC-BUS-REC-001 | TS-BUS-005 | P0 | failed delivery + retry policy -> request retry | retry plan scheduled,audit append | unit / service |
| TC-BUS-REC-002 | TS-BUS-005 | P0 | failed delivery + failure material -> DLQ | DLQ created,failure material linked | service / API |
| TC-BUS-REC-003 | TS-BUS-005 | P0 | DLQ missing approval / audit chain -> prepare replay | replay rejected,no ready state | unit / service / API |
| TC-BUS-REC-004 | TS-BUS-005 | P0 | DLQ + approval ref + audit chain -> prepare replay | replay preparation ready,event emitted | service / E2E |
| TC-BUS-OUT-001 | TS-BUS-006 | P0 | current transport projection -> query | read-only result,no write UoW | API / service |
| TC-BUS-OUT-002 | TS-BUS-006 | P0 | stale / missing projection -> query | stale / not found marker,no rebuild | API / service |
| TC-BUS-OUT-003 | TS-BUS-006 | P0 | failure material + actor context -> query | authorized read or stable rejection | API / service |
| TC-BUS-OUT-004 | TS-BUS-006 | P0 | read governance-facing output | no governance decision body | contract / API |
| TC-BUS-OUT-005 | TS-BUS-006 | P0 | audit trail with sequence | append-only chain,monotonic sequence | repository / service |
| TC-BUS-OUT-006 | TS-BUS-006 | P0 | publisher retryable failure after truth commit | truth not rolled back,publish evidence saved | service / integration |
| TC-BUS-OBX-001 | TS-BUS-007 | P0-min | committed outbox fact -> consume | publication accepted,source ref unique | consumer / integration |
| TC-BUS-OBX-002 | TS-BUS-007 | P0-min | duplicate fact or source ack failure -> reconsume | existing result,no duplicate acceptance | consumer / integration |
| TC-BUS-BND-001 | TS-BUS-008 | P0-min | backend capability available -> dispatch | capability accepted,no secret leak | integration / job |
| TC-BUS-BND-002 | TS-BUS-008 | P0-min | unsupported semantic or backend unavailable | unsupported / unavailable evidence | integration |
| TC-BUS-BND-003 | TS-BUS-008 | P0-min | commit uncertain / adapter dependency error | manual action / retryable evidence,no unsafe retry | service / integration |
| TC-BUS-CFG-001 | TS-BUS-009 | P0 | valid JSON profile -> build runtime graph | validated runtime graph | config unit / integration |
| TC-BUS-CFG-002 | TS-BUS-009 | P0 | unsupported key / invalid enum / raw secret | fail-fast before runtime | config unit |
| TC-BUS-CFG-003 | TS-BUS-009 | P0 | secret unavailable or reload request | fail-closed / rejected | config integration |
| TC-BUS-RED-001 | TS-BUS-010 | P0 | artifacts with logs/events/projections -> redaction check | forbidden body absent | snapshot / release gate |
| TC-BUS-RED-002 | TS-BUS-010 | P0 | completed test run -> generate reports | `reports/runs/<run_id>` created | script / release gate |

---

## 7. 测试数据设计

> 校准来源：
> - `design-calibration/05_test_plan_step_07_test_data.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“测试数据集表”“Fixture / builder 规则”和“Seed 顺序”小节，了解测试数据如何构造、隔离、复用和清理。

测试数据按 `run_id` 和 deterministic namespace 隔离。所有 source ref、event id、job item key、artifact path、report path 必须绑定当前 `run_id` 或由当前 `run_id` 派生。

| 数据集 | 用途 | 构造方式 | 隔离键 | 关联用例 |
|---|---|---|---|---|
| DS-BUS-RUN-001 | run identity、actor、metadata | `TestRunBuilder` | `run_id` | 全部用例 |
| DS-BUS-PUB-001 | publication accepted 正向 | `PublicationFixtureBuilder.valid_material()` | `run_id + source_ref` | TC-BUS-PUB-001 |
| DS-BUS-PUB-002 | publication negative | missing contract、payload body injected、terminal record | `run_id + negative_case` | TC-BUS-PUB-002~004 |
| DS-BUS-SEM-001 | transport semantic | backend capability available / unsupported / private field sample | `run_id + backend_profile` | TC-BUS-SEM / BND |
| DS-BUS-DLV-001 | delivery lifecycle | scheduled、dispatched、completed、failed、batch mixed items | `run_id + delivery_id` | TC-BUS-DLV |
| DS-BUS-FDB-001 | feedback / idempotency | ack、fail、timeout、same / different digest、late feedback | `run_id + idempotency_key` | TC-BUS-FDB |
| DS-BUS-REC-001 | recovery chain | failed delivery、retry policy、failure material、DLQ、approval ref、audit chain | `run_id + recovery_ref` | TC-BUS-REC |
| DS-BUS-OUT-001 | read-only output | current / stale / missing projection、failure material、audit sequence | `run_id + projection_key` | TC-BUS-OUT |
| DS-BUS-OBX-001 | outbox relay | committed outbox fact、duplicate fact、source ack failure marker | `run_id + source_record_ref` | TC-BUS-OBX |
| DS-BUS-CFG-001 | config valid / invalid | valid profile、unsupported key、invalid enum、raw secret、reload request | `run_id + profile_name` | TC-BUS-CFG |
| DS-BUS-RED-001 | redaction / reports | clean artifacts、forbidden sample artifacts、expected report refs | `run_id` | TC-BUS-RED |

Builder 默认生成合法数据。negative / boundary 数据必须显式调用对应 builder 方法,禁止默认生成 forbidden body。forbidden sample 只能进入 redaction negative fixture。

---

## 8. 测试环境与配置矩阵

> 校准来源：
> - `design-calibration/05_test_plan_step_08_environment_config.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“环境拓扑图”“环境矩阵”“配置矩阵”和“测试依赖类型与协作方式判定表”小节，了解测试环境、依赖和配置 profile 如何对应。

#### 环境拓扑图: L0-bus P0 测试环境

```text
+-----------------------------+
| L0-bus test process         |
| unit/service/api/worker/job |
+--------------+--------------+
               |
               +-- [compile] --> L0-core shared contracts
               |
               +-- [runtime] --> in-memory constrained store
               |
               +-- [runtime] --> fake transport backend
               |
               +-- [runtime] --> fake secret provider
               |
               +-- [event] ----> fake outbox source / event replay
               |
               +-- [event] ----> in-memory outbound publisher sink
               |
               +-- [event] ----> fake SDK / observability / governance consumer
               |
               v
+--------------+--------------+
| artifacts/test/<run_id>     |
| reports/runs/<run_id>       |
+-----------------------------+
```

关键说明:

- 只有 `L0-core shared contracts` 是 `[compile]` 依赖,可使用本地 path dependency。
- store、backend、secret provider 是 `[runtime]` 依赖,当前 P0 使用 fake / in-memory。
- outbox source、outbound publisher 和下游消费者是 `[event]` 协作,当前 P0 使用 fixture、event replay 或 in-memory sink。

| 环境 / profile | 当前测试定位 | 依赖服务 | 是否 P0 阻断 |
|---|---|---|---|
| local-dev | 本地 smoke、unit、少量 service / API 手动验证 | L0-core contracts、in-memory runtime | 否 |
| ci-test | PR gate 的 unit / service / contract / config / redaction smoke | fake store/backend/source/publisher | 是 |
| integration-test | repository / adapter / worker / job / runtime graph 集成 | fake source、fake backend、in-memory store、in-memory sink | 是 |
| operations-recovery | retry、DLQ、replay preparation、projection rebuild、backend capability job | fake audit chain、fake approval ref、fake backend capability | 是 |
| staging-like | 后续 real-like adapter 和 durable store 演练 | durable store、MQ、secret provider | 否,当前 P1 |
| production-like | 后续生产部署验证 | production MQ / DB / KMS / observability | 否,当前不执行 |

P0 测试使用 `store.kind=in_memory`、`outbox_source.kind=in_memory_fixture`、`transport_backend.kind=in_memory`、`publisher.kind=in_memory_sink`、`projection.kind=in_memory`。`security_boundary.*` 在所有测试 profile 中都必须保持 reject / ref_only / required。

---

## 9. 自动化与 CI/CD 门禁

> 校准来源：
> - `design-calibration/05_test_plan_step_09_automation_ci_gates.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“自动化套件表”“脚本表”“artifact 与 report 输出映射表”和“CI/CD 门禁图”小节，了解自动化门禁如何从测试分层、用例矩阵和环境矩阵收敛。

#### 自动化门禁图: PR 到验收报告

```text
+------------------+
| Pull request      |
+--------+---------+
         |
         v
+--------+---------+
| run_pr_gate.sh    |
| fast P0 suites    |
+--------+---------+
         |
         v
+--------+---------+
| Main CI           |
| run_ci_gate.sh    |
+--------+---------+
         |
         v
+--------+---------+
| Nightly           |
| stress / failure  |
+--------+---------+
         |
         v
+--------+---------+
| Release gate      |
| run_release_gate  |
+--------+---------+
         |
         v
+--------+---------+
| reports/runs/id   |
| acceptance index  |
+------------------+
```

关键说明:

- PR gate 必须快,但不能跳过 P0 红线。
- main CI 覆盖完整 in-memory / fake 集成路径。
- nightly 承接压力、故障注入和 P1 smoke,不直接替代 release gate。
- release gate 必须生成 `reports/runs/<run_id>` 和 `reports/acceptance` 索引。

| 阻断范围 | Gate script | 覆盖 suite | 输出 |
|---|---|---|---|
| PR gate | `scripts/gates/run_pr_gate.sh` | `bus-unit`、`bus-service`、`bus-contract`、`bus-config`、`bus-redaction-smoke`、`bus-integration-fast` | `artifacts/test/<run_id>` |
| main CI gate | `scripts/gates/run_ci_gate.sh` | `bus-integration-full`、`bus-worker-consumer`、`bus-job-runner`、`bus-report-smoke` | `artifacts/test/<run_id>`、`reports/runs/<run_id>` |
| nightly | `scripts/gates/run_nightly_gate.sh` | `bus-concurrency-stress`、`bus-failure-injection`、`bus-p1-adapter-smoke` | `artifacts/test/<run_id>`、`reports/runs/<run_id>` |
| release gate | `scripts/gates/run_release_gate.sh` | `bus-release-closed-loop`、`bus-release-recovery`、`bus-release-config-runtime`、`bus-release-redaction`、`bus-release-report` | `reports/runs/<run_id>`、`reports/acceptance` |

Gate script 必须支持 `--run-id`、`--artifact-root`、`--config-profile`。Report script 至少支持 `--run-id`、`--artifact-root`、`--report-root`。正式门禁不得使用 `latest` 或 `artifacts/test/<project>/<run_id>`。

---

## 10. 专项测试与非功能验证

> 校准来源：
> - `design-calibration/05_test_plan_step_10_special_nonfunctional.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“专项测试矩阵”“专项与门禁映射”和“红线型通过条件”小节，了解非功能和横切风险如何被验证。

| 专项 | 指标 / 风险 | 方法 | 环境 | 阈值 / 通过条件 | 证据 |
|---|---|---|---|---|---|
| Performance baseline | publication、delivery、feedback、query、recovery 默认路径耗时 | fixed fixture batch + duration metrics | release gate / nightly | 生成 p50 / p95 / max baseline,不触发 gate timeout | `EV-BUS-PERF-001` |
| Security boundary | payload body、raw secret、backend private body、governance decision 泄漏 | negative fixture + redaction scan | PR / release gate | forbidden body absent,命中即失败 | `RP-BUS-RED-001` |
| Authorization seam | tap、DLQ read、replay preparation、failure material 输出边界 | actor context + privileged ref negative tests | service / API | 未授权或缺 ref 时 stable rejection + access audit | `EV-BUS-SEC-001` |
| Consistency / UoW | truth、audit、history、outbox evidence 顺序错误 | fake UoW call order + failure injection | service / integration | no half-state,no unsafe retry | `EV-BUS-CONS-001` |
| Idempotency / concurrency | duplicate command、duplicate event、并发推进 | deterministic duplicate + concurrent execution | PR / nightly | existing / conflict / version conflict 符合预期 | `EV-BUS-IDEM-001` |
| Recovery fault injection | backend unavailable、retry exhausted、DLQ、replay rejected / ready | recovery fixture + fake backend | main CI / release gate | history / audit / failure material 完整 | `EV-BUS-REC-FAULT-001` |
| Config failure mode | invalid JSON、unsupported key、raw secret、secret unavailable、reload request | config negative suite | PR / release gate | fail-fast / fail-closed / rejected | `EV-BUS-CFG-FAULT-001` |
| Observability / audit | log、metric、audit、trace ref、delivery history 缺失 | snapshot + report checks | main CI / release gate | required evidence present,forbidden body absent | `EV-BUS-OBS-001` |
| Report / artifact integrity | evidence 缺失、跨 run 引用、latest 引用 | `check_artifact_layout.sh` + `check_report_links.sh` | release gate | no latest,no project layer,no missing evidence | `RP-BUS-SUM-001` |

红线型通过条件: forbidden body 不得出现在 log / audit / event / projection / evidence / report;raw secret 或完整连接串不得出现在配置摘要、日志、审计或报告;Query 不得写 truth;replay 缺少 dead-letter / history / audit chain 不得 ready;same idempotency key + different digest 必须 conflict;report 不得引用 `latest` 或跨 run artifact。

---

## 11. 缺陷管理与复验规则

> 校准来源：
> - `design-calibration/05_test_plan_step_11_defects_retest.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“缺陷分级表”“复验规则表”和“缺陷区域到回归映射”小节，了解缺陷管理与复验规则如何支撑验收裁决。

| 级别 | 定义 | 示例 | 处理要求 | 是否阻断 |
|---|---|---|---|---|
| S0 | 一票否决 / 安全红线 / 验收证据不可用 | forbidden body 泄漏、Query 写 truth、replay 绕过审计链、无 reports | 立即修复或回退;必须补自动化;不得风险接受 | 阻断 PR / release / acceptance |
| S1 | P0 主链或 P0-min 支撑边界不可用 | F-001~F-008 用例失败、schema 破坏、幂等 conflict 缺失 | 修复后跑最小回归 + 对应 gate | 阻断 PR / main CI / release |
| S2 | P0 非主链问题或非红线质量缺陷 | performance baseline 波动、非关键报告字段缺失 | 指定 owner、期限和复验计划;可条件风险接受 | 视影响阻断 |
| S3 | 文档、提示、低风险易用性问题 | 报告文字不清、非阻断脚本提示不友好 | 可排期修复;重复出现升级 | 不默认阻断 |
| P1-risk | 当前 P0 非范围,但影响后续生产化 | staging-like adapter 失败、真实 MQ smoke 不稳定 | 记录残余风险,后续 P1 专项处理 | 不阻断当前 P0 |

S0 / S1 不得风险接受。S2 / S3 可以在有 owner、到期时间、影响范围和复验计划时条件接受。S0 / S1 缺陷必须新增或修复自动化防回归,不得只靠人工复验。

| 缺陷区域 | 最小回归范围 |
|---|---|
| publication / core boundary | `TC-BUS-PUB-001`~`004`、`bus-contract` |
| transport semantic / backend boundary | `TC-BUS-SEM-001`~`002`、`TC-BUS-BND-001`~`003` |
| delivery lifecycle | `TC-BUS-DLV-001`~`004`、state machine unit tests |
| feedback / idempotency | `TC-BUS-FDB-001`~`004`、idempotency suite |
| recovery / replay | `TC-BUS-REC-001`~`004`、`bus-release-recovery` |
| read-only output / Query | `TC-BUS-OUT-001`~`006`、no-write Query tests |
| config | `TC-BUS-CFG-001`~`003`、`bus-config` |
| redaction / evidence | `TC-BUS-RED-001`~`002`、`check_redaction.sh`、`generate_reports.sh` |

---

## 12. 进入准则与退出准则

> 校准来源：
> - `design-calibration/05_test_plan_step_12_entry_exit_criteria.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“进入准则”“退出准则”和“准则到证据映射表”小节，了解测试开始、结束和证据交接如何被裁决。

### 12.1 进入准则

- [ ] `00-需求文档.md`、`01-架构设计.md`、`02-概要设计.md`、`03-详细设计.md`、`04-配置设计.md` 已校准,且本轮测试不再改变 P0 范围。
- [ ] `05-测试方案.md` 已按新文件标准重建,且 §1~§12 已承接 Step 1~12 中间产物。
- [ ] `ci-test`、`integration-test`、`operations-recovery` profile 可加载并通过 config validation。
- [ ] `L0-core` shared contracts 可通过本地 path dependency 编译。
- [ ] `TestRunBuilder` 能生成唯一 `run_id`、actor、metadata、fixed clock 和 deterministic id namespace。
- [ ] `DS-BUS-*` fixture 数据集可构造,并能按 `run_id` 隔离。
- [ ] `scripts/gates/run_pr_gate.sh`、`run_ci_gate.sh`、`run_release_gate.sh` 可运行并支持标准参数。
- [ ] `scripts/checks/check_redaction.sh`、`check_artifact_layout.sh`、`check_report_links.sh`、`check_config_summary.sh` 可运行。
- [ ] `scripts/reports/generate_reports.sh` 和 `generate_acceptance_index.sh` 可生成约定目录。
- [ ] `artifacts/test/<run_id>` 和 `reports/runs/<run_id>` 可写,且测试门禁不使用 `latest`。

### 12.2 退出准则

- [ ] PR gate 阻断套件全部通过,没有未关闭 S0 / S1。
- [ ] main CI 阻断套件全部通过,并生成当前 `run_id` 的 summary。
- [ ] release gate 的 P0 / P0-min 用例族 `TC-BUS-PUB-*`、`TC-BUS-SEM-*`、`TC-BUS-DLV-*`、`TC-BUS-FDB-*`、`TC-BUS-REC-*`、`TC-BUS-OUT-*`、`TC-BUS-OBX-*`、`TC-BUS-BND-*`、`TC-BUS-CFG-*`、`TC-BUS-RED-*` 全部通过。
- [ ] redaction、artifact layout、report links、config summary 四类 check 全部通过。
- [ ] `artifacts/test/<run_id>` 存在且不含 `<project>` 层级、不依赖 `latest`。
- [ ] `reports/runs/<run_id>/summary.md`、coverage、redaction、artifact index 可用。
- [ ] `reports/acceptance/<run_id>-index.md` 已生成,并能被 `06-验收标准.md` 引用。
- [ ] S0 / S1 缺陷全部关闭并完成自动化复验。
- [ ] S2 缺陷如未关闭,必须有 owner、影响范围、到期时间、风险接受记录和复验计划。
- [ ] P1-risk 已进入残余风险清单,且不被误声明为当前 P0 已交付能力。

---

## 13. 测试报告与证据归档

> 校准来源：
> - `design-calibration/05_test_plan_step_13_reports_evidence.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“证据归档表”“测试报告结构”“artifacts 目录结构”和“reports 目录结构”小节，了解 L0-bus 测试证据如何支撑后续验收裁决。

机器原始证据统一写入 `artifacts/test/<run_id>`,人类可读报告统一写入 `reports/runs/<run_id>`,验收交接材料统一写入 `reports/acceptance`。正式证据引用必须绑定固定 `run_id`,不得引用 `latest`,不得额外增加 `<project>` 层级。

| 证据 ID | 证据类型 | 来源 | 保存位置 | 关联用例 | 验收引用 |
|---|---|---|---|---|---|
| `EV-BUS-PUB-001`~`004` | publication evidence | service / API / contract suite | `artifacts/test/<run_id>/suites/publication` | `TC-BUS-PUB-001`~`004` | `reports/runs/<run_id>/evidence/EV-BUS-PUB.md` |
| `EV-BUS-SEM-001`~`002` | transport semantic evidence | domain / service suite | `artifacts/test/<run_id>/suites/semantic` | `TC-BUS-SEM-001`~`002` | `reports/runs/<run_id>/evidence/EV-BUS-SEM.md` |
| `EV-BUS-DLV-001`~`004` | delivery lifecycle evidence | integration / job suite | `artifacts/test/<run_id>/suites/delivery` | `TC-BUS-DLV-001`~`004` | `reports/runs/<run_id>/evidence/EV-BUS-DLV.md` |
| `EV-BUS-FDB-001`~`004` | feedback / idempotency evidence | service / API suite | `artifacts/test/<run_id>/suites/feedback` | `TC-BUS-FDB-001`~`004` | `reports/runs/<run_id>/evidence/EV-BUS-FDB.md` |
| `EV-BUS-REC-001`~`004` | recovery evidence | recovery / release suite | `artifacts/test/<run_id>/suites/recovery` | `TC-BUS-REC-001`~`004` | `reports/runs/<run_id>/evidence/EV-BUS-REC.md` |
| `EV-BUS-OUT-001`~`006` | read-only output / audit evidence | API / service / integration suite | `artifacts/test/<run_id>/suites/output` | `TC-BUS-OUT-001`~`006` | `reports/runs/<run_id>/evidence/EV-BUS-OUT.md` |
| `EV-BUS-OBX-001`~`002` | outbox relay evidence | consumer / integration suite | `artifacts/test/<run_id>/suites/outbox` | `TC-BUS-OBX-001`~`002` | `reports/runs/<run_id>/evidence/EV-BUS-OBX.md` |
| `EV-BUS-BND-001`~`003` | backend boundary evidence | integration / job suite | `artifacts/test/<run_id>/suites/backend` | `TC-BUS-BND-001`~`003` | `reports/runs/<run_id>/evidence/EV-BUS-BND.md` |
| `EV-BUS-CFG-001`~`003` | config evidence | config suite | `artifacts/test/<run_id>/suites/config` | `TC-BUS-CFG-001`~`003` | `reports/runs/<run_id>/config-summary.md` |
| `RP-BUS-RED-001` | redaction report | redaction check | `artifacts/test/<run_id>/suites/redaction` | `TC-BUS-RED-001` | `reports/runs/<run_id>/redaction-check.md` |
| `RP-BUS-SUM-001` | run summary / acceptance index | report scripts | `reports/runs/<run_id>`、`reports/acceptance` | `TC-BUS-RED-002` | `reports/acceptance/<run_id>-index.md` |

```text
artifacts/test/<run_id>/
  meta/context.json
  evidence-index.json
  fixtures/fixture-summary.json
  suites/<suite>/report.json
  suites/<suite>/stdout.log
  suites/<suite>/stderr.log

reports/
  README.md
  runs/<run_id>/
    summary.md
    evidence-index.md
    gate-results.md
    coverage-matrix.md
    config-summary.md
    redaction-check.md
    artifact-index.md
    suites/<suite>.md
    evidence/EV-BUS-<AREA>.md
  acceptance/
    <run_id>-index.md
    handoff.md
    veto-checklist.md
    risk-acceptance.md
    open-issues.md
  review/
    reviewer-notes.md
    agent-review.md
```

`scripts/reports/*` 只能生成报告初稿。`reports/acceptance/*` 必须允许人或 Agent 审查补充。失败 suite 仍应产出 `report.json`、stdout/stderr log 和 failure reason。所有 artifact 和 report 必须通过 redaction / boundary scan。

---

## 14. 回归策略与残余风险

> 校准来源：
> - `design-calibration/05_test_plan_step_14_regression_risks.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“回归触发表”“残余风险表”和“风险转验收表”小节，了解 L0-bus 变更后如何选择回归范围以及未覆盖风险如何交给验收标准裁决。

| 变更类型 | 最小回归集 | 全量回归触发条件 | 责任人 |
|---|---|---|---|
| core contract reference handling | `TC-BUS-PUB-001`~`004`、`bus-contract` | `L0-core` 契约语义变化或 contract DTO 变化 | Bus maintainer |
| transport semantic mapping | `TC-BUS-SEM-001`~`002`、`TC-BUS-BND-001`~`003` | backend capability model 或 unsupported / unavailable 语义变化 | Bus maintainer |
| delivery lifecycle | `TC-BUS-DLV-001`~`004`、state machine tests | 状态、terminal、history append 规则变化 | Bus maintainer |
| feedback / idempotency | `TC-BUS-FDB-001`~`004`、idempotency suite | idempotency key / digest / conflict 规则变化 | Bus maintainer |
| recovery / replay | `TC-BUS-REC-001`~`004`、`bus-release-recovery` | DLQ、replay preparation、audit chain 条件变化 | Bus maintainer + governance interface owner |
| read-only output / Query | `TC-BUS-OUT-001`~`006`、no-write tests | Query / projection / failure material 输出契约变化 | Bus maintainer + downstream consumer owner |
| outbox relay | `TC-BUS-OBX-001`~`002`、consumer integration | source uniqueness、ack、duplicate 规则变化 | Bus maintainer + publisher interface owner |
| config control plane | `TC-BUS-CFG-001`~`003`、`bus-config` | RuntimeConfig root、validator、runtime graph 装配变化 | Bus maintainer |
| redaction / security boundary | `TC-BUS-RED-001`、`bus-release-redaction` | redaction policy、forbidden body、secret boundary 变化 | Bus maintainer + security owner |
| artifacts / reports | `TC-BUS-RED-002`、report scripts、link checks | artifact / report 路径、evidence index、acceptance handoff 变化 | Bus maintainer + release owner |

| 风险 | 未覆盖原因 | 影响 | 缓解方式 | 接受人 |
|---|---|---|---|---|
| Production MQ / durable store 全量行为 | 当前 P0 使用 fake / in-memory | 真实 adapter 可能暴露后端差异 | P1 adapter smoke + 后续 production adapter 专项 | Bus maintainer + adapter owner |
| Secret provider / KMS / Vault 产品集成 | 当前只测 secret ref / fake provider | 真实 provider 可用性和权限风险后置 | P1 security / ops 专项 | Security owner + ops owner |
| Gateway / auth / TLS | bus 不实现身份校验入口 | 入口安全缺陷由其他仓覆盖 | bus 保留 actor / access audit 接缝 | Gateway / identity owner |
| 业务 payload 正文语义 | bus 不拥有正文真相 | 业务内容错误不由 bus 发现 | payload 只传 ref / digest / metadata,业务仓自测 | Publisher / subscriber owner |
| Governance decision truth | bus 不做审批和策略裁决 | 治理决策错误后置 | failure material 只读输出,governance 仓自测 | Governance owner |
| Observability dashboard / alerting | bus 不做长期存储和报表 | 告警阈值和看板风险后置 | tap / trace / audit material 输出测试 | Observability owner |
| SDK developer experience | bus 不封装高层 client | client retry、认证封装后置 | transport view / error contract 稳定 | SDK owner |
| Exactly-once / effectively-once | 当前非目标 | 业务方误解 delivery 语义 | 文档和验收说明 at-least-once + subscriber idempotency | Bus maintainer + subscriber owner |
| Config center / hot reload / admin override | P2 非范围 | 运行期配置变更风险后置 | 当前 reload request 明确 rejected | Platform / ops owner |
| Multi-backend / multi-tenant matrix | P2 非范围 | 复杂部署差异未验证 | 后续专项测试矩阵 | Platform / ops owner |

必须转入 `06-验收标准.md` 的内容: S0 一票否决、S1 P0 主链阻断、P0 report / artifact 缺失、redaction / raw secret / payload body 泄漏、replay 缺 audit chain 仍 ready、Query 写 truth、P1 production adapter 未交付的非范围说明、exactly-once 非目标、S2 / P1-risk 条件接受。

---

## 15. 参考

> 校准来源：
> - `design-calibration/05_test_plan_step_15_formal_document_assembly.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“正式文档整理规则”和“自审结果”小节，了解本文如何从 Step 1~Step 14 的中间产物整理为正式测试方案。

| 类型 | 文档 |
|---|---|
| 书写规范 | `standards/document/测试方案书写规范.md` |
| 讨论 SOP | `standards/document/测试方案讨论流程_SOP.md` |
| 中间产物规范 | `standards/document/设计文档讨论中间产物规范.md` |
| 目录与脚本规则 | `standards/document/子项目目录与代码文件组织规范.md` |
| 当前需求文档 | `projects/L0-bus/00-需求文档.md` |
| 当前架构设计 | `projects/L0-bus/01-架构设计.md` |
| 当前概要设计 | `projects/L0-bus/02-概要设计.md` |
| 当前详细设计 | `projects/L0-bus/03-详细设计.md` |
| 当前配置设计 | `projects/L0-bus/04-配置设计.md` |
| 校准工作台 | `projects/L0-bus/design-calibration/05_test_plan_calibration_flow.md` |

# L1-artifact 07-实施计划

> 文档状态: formal draft assembled from `design-calibration/07_implementation_plan_step_01_input_boundary.md` through `07_implementation_plan_step_13_formal_document_assembly.md`
> 目标实现仓: `/home/aris/Projects/quantalithos-artifact`
> 设计仓: `/home/aris/Projects/quantalithos-design`
> 执行约束: 本文不填写真实 `run_id`、implementation commit、config digest、最终验收结论、签署或正式 `EV-ART-*` alias。

本文定义 `L1-artifact` 从当前正式 `00`~`06` 设计基线落地为实现仓代码、测试、门禁、证据和移交台账的执行计划。它不重新定义需求、架构、对象 schema、DTO、port、flow、状态矩阵、配置项、测试用例或验收裁决。

## 1. 与上游文档的关系声明

> 校准来源: `design-calibration/07_implementation_plan_step_01_input_boundary.md`

本实施计划承接以下正式文档:

| 上游文档 | 本计划使用方式 |
|---|---|
| `00-需求文档.md` | 提供五个核心能力、`FR-ART-*`、`BR-ART-*`、NFR 与 `VF-ART-*` |
| `01-架构设计.md` | 提供 Artifact truth ownership、正文排除、依赖方向和跨仓 seam |
| `02-概要设计.md` | 提供组成部分、接口骨架、处理流和状态集合 |
| `03-详细设计.md` | 提供对象、port、protocol、flow、状态、事务、错误、幂等、观测与 implementation handoff |
| `04-配置设计.md` | 提供 P0 profile、config source priority、strict validation、redaction、degraded/no-write 与 replay 口径 |
| `05-测试方案.md` | 提供 `TC-ART-*`、blocking suite、artifact/report root、`EV-CAND-ART-*` 与回归策略 |
| `06-验收标准.md` | 提供 `AC-ART-001~058`、`VETO-ART-001~009`、risk acceptance 和最终裁决口径 |

`03-详细设计.md` 及其 `design-calibration/03_ddd_step_*.md` 是实现契约真相源。若实施计划拆分 phase / commit boundary 时发现字段、DTO、状态、读取面、version 来源、payload source、affected view、job report、证据来源或 phase boundary 无法 1:1 闭合,必须暂停并回写设计真相源,不得要求实现 agent 自行补 schema、port、状态、mapper、gate、evidence 或 boundary。

正式实现移交前必须固定 design baseline,完成按 phase / commit boundary 的可落码闭环审计和经验复核,并预创建项目级 implementation ledger 与全部 planned boundary ledger。

## 2. 实施目标与范围

> 校准来源: `design-calibration/07_implementation_plan_step_02_scope.md`

本轮实施目标是把 `L1-artifact` 落地为可编译、可测试、可送验的 Artifact truth center。P0 范围覆盖五个核心能力:

| 核心能力 | 覆盖需求 | 实施落点 |
|---|---|---|
| 制品事实承载 | `FR-ART-001~004` | fact / intake / review / responsibility accepted flow |
| 制品版本化 | `FR-ART-005~008` | candidate / publish / supersede / current pointer / history retain |
| 制品血缘关联 | `FR-ART-009~012` | formal lineage link / rejection / impact / backref |
| 制品基线冻结 | `FR-ART-013~016` | baseline candidate / freeze / supersede / history audit |
| 制品事实可消费表达 | `FR-ART-017~020` | consumable ref / read surface / backref / handoff |

实施范围包括:

- 七 crate Rust workspace: `contracts`、`domain`、`application`、`infra`、`api`、`worker`、`jobs`。
- 16 Command、13 Query、6 Inbound Event Consumer、8 Outbound Event、6 public Operations Job。
- worker-only `PublishPendingArtifactRelays` relay publication facade,不得计入 6 public jobs。
- P0 config profiles、fake / in-memory / controlled / disabled adapters、gate / report / check scripts、artifact/report roots、candidate evidence index 和 acceptance handoff generators。
- `AC-ART-001~058` 可判定路径和 `VETO-ART-001~009` 未触发证据路径。

本轮不交付真实 DB / bus / search / object storage 产品绑定、真实 upstream / downstream 产品深度集成、production-like / capacity / hard SLO、高级 dashboard / analytics、derived UX、长期 evidence retention 天数、部署运维 runbook、正式 `EV-ART-*` alias、真实 `run_id`、final verdict 或 signoff。

## 3. 实施前置条件与阅读清单

> 校准来源: `design-calibration/07_implementation_plan_step_03_prerequisites_reading.md`

实现 agent 开工前必须满足:

| 前置条件 | 要求 |
|---|---|
| 目标仓 | `/home/aris/Projects/quantalithos-artifact` 存在;若不存在,只能在 `commit-01-a` 范围内创建 |
| git identity | 目标仓项目级 `user.name=quantalithos-labs`;`user.email=quantalithos.ai@gmail.com` |
| 编码规范 | 已读 `standards/coding/rust.md`;Rust 标识符、rustdoc、普通注释和测试名默认英文 |
| 目录规范 | 已读 `standards/document/子项目目录与代码文件组织规范.md` |
| 实施台账 | 先读 `design-calibration/implementation_execution_ledger.md` 和当前 boundary ledger |
| 编译期依赖 | 只允许 `core-contracts = { path = "../quantalithos-core/crates/contracts" }` |
| 证据路径 | raw artifacts 使用 `artifacts/test/<run_id>`;reports 使用 `reports/runs/<run_id>`、`reports/acceptance`、`reports/review`;不得引用 `latest` |

每次继续、换 agent、进入新 boundary、准备提交或设计 blocker 修复后,必须按以下顺序恢复:

```text
1. projects/L1-artifact/design-calibration/implementation_execution_ledger.md
2. projects/L1-artifact/design-calibration/implementation-boundaries/<current_boundary>.md
3. projects/L1-artifact/07-实施计划.md
4. 当前 boundary 的 required_reads
5. 可选 scratch: /home/aris/Projects/quantalithos-artifact/.codex/implementation_ledger.md
```

永久记忆种子必须包含目标仓路径、唯一编译期依赖、Rust / commit / git config 纪律、正式文档优先、不得自行补设计、artifact/report path、交付实现前审计、implementation ledger 读取和设计修复后经验沉淀检查。

## 4. 实施对象与交付物清单

> 校准来源: `design-calibration/07_implementation_plan_step_04_objects_deliverables.md`

| 交付物 | 类型 | 完成判定 |
|---|---|---|
| Artifact contracts crate | code | 16 Command、13 Query、6 Consumer、8 Event、6 Job、relay worker DTO 和 public carriers 编译并通过 contract tests |
| Artifact domain crate | code | fact、version、lineage、baseline、consumption boundary、trace/audit/outbox objects 的合法/非法迁移测试通过 |
| Artifact application crate | code | service facade、repository/UoW/idempotency/port traits、stored result / receipt / report replay tests 通过 |
| Artifact infra crate | code | fake runtime、in-memory store、config loader、adapter failure mapping、dependency boundary tests 通过 |
| Artifact API crate | code | Command / Query entry 调用 application 并映射 protocol error |
| Artifact worker crate | code | inbound consumer、relay publication loop、outbox worker no-current-truth-rebuild tests 通过 |
| Artifact jobs crate | code | 6 public operations jobs 的 duplicate report replay、partial failure 和 disabled target surface 通过 |
| P0 test suite matrix | test | P0 suites / checks 可从 gate scripts 运行并输出 raw artifacts |
| Config and runtime builder | config / code / test | strict validation、source priority、topic completeness、profile isolation 和 fail-fast covered |
| Evidence report generation | script / evidence | evidence index、gate summary、suite reports、handoff、veto、risk、open issues 从 raw artifacts 生成 |
| Implementation handoff ledgers | design handoff | project ledger and all boundary skeletons exist before implementation handoff |

跨仓依赖只允许 `quantalithos-core` 作为编译期 dependency。`quantalithos-bus`、identity、process、work、governance、method-library、conversation、runtime/capability、archive、observability、sync 和 external vendor 均通过 body-free ref、safe summary、event、adapter、handoff、disabled / fake / controlled seam 交付。

## 5. 实施阶段与依赖顺序

> 校准来源: `design-calibration/07_implementation_plan_step_05_phases_dependencies.md`

```text
PH-01 仓初始化、配置与证据骨架
  -> PH-02 制品事实承载最小 accepted 纵切
  -> PH-03 制品版本化与血缘关联纵切
  -> PH-04 制品基线冻结纵切
  -> PH-05 制品事实可消费表达与只读读面
  -> PH-06 Inbound / Outbound Event 与 relay publication
  -> PH-07 Operations Jobs / Reconciliation / Handoff / Export
  -> PH-08 Release Gate / Reports / Acceptance Handoff
```

| Phase | 实施目标 | 阶段门禁 |
|---|---|---|
| PH-01 | 目标仓、workspace、唯一编译期依赖、config profile skeleton、artifact/report roots 和 script shell | `cargo check`;config shell;dependency boundary;script `--help`;path checks |
| PH-02 | formal Artifact fact、intake / review / responsibility anchor 和 accepted mutation 原子性 | `contract-domain-fast` fact slice;`service-flow-fast` fact slice;targeted `infra-runtime-fake` |
| PH-03 | formal version、history retain、supersede、lineage link / reject / impact | version/lineage contract-domain and service-flow slices;conflict/replay tests |
| PH-04 | baseline candidate / freeze / supersede / history audit | baseline contract-domain and service-flow slices |
| PH-05 | 13 Query、consumable ref / backref、stable selector、projection / trace / report read surface | query no-write;projection tests;API query tests |
| PH-06 | 6 Consumer、8 Event、stored payload snapshot、publisher marker、worker-only relay loop | `entry-worker-job`;`operations-replay-core`;topic/redaction checks |
| PH-07 | 6 public jobs、report replay、partial failure、handoff/export seam、no-truth-repair | public job / operations replay / handoff-export tests |
| PH-08 | release smoke、candidate evidence、report audit、acceptance handoff / veto / risk docs | `release-main-smoke`;`report-generation-audit`;VETO / redaction / dependency audits |

每个 phase 完成时都必须停审:是否形成可验证功能增量,是否误依赖后续 phase,是否引入 compile-time sibling 污染,是否触及 `VETO-ART-*` 红线,以及是否需要回写正式 `03/05/06/07`。

## 6. 阶段任务拆分、编写顺序与提交边界

> 校准来源: `design-calibration/07_implementation_plan_step_06_tasks_commit_boundaries.md`

L1-artifact P0 实施按 20 个 commit boundary 推进:

| Commit boundary | Phase | 可验证增量 | Required checks 摘要 |
|---|---|---|---|
| `commit-01-a` | PH-01 | workspace / crate skeleton / naming / only-core dependency | `cargo fmt --check`;`cargo check`;dependency boundary;`git diff --check` |
| `commit-01-b` | PH-01 | config profile shell + scripts + artifact/report roots | config parse smoke;scripts dry-run;`cargo check`;`git diff --check` |
| `commit-02-a` | PH-02 | fact/intake/review/responsibility public contracts + domain truth | `contract-domain-fast` fact slice;`cargo check` |
| `commit-02-b` | PH-02 | accepted fact command service + UoW/idempotency + fake + API handler | `service-flow-fast` fact slice;`infra-runtime-fake`;`cargo check` |
| `commit-03-a` | PH-03 | version contracts + domain state/history | `contract-domain-fast` version slice |
| `commit-03-b` | PH-03 | lineage contracts + relation/impact domain | `contract-domain-fast` lineage slice |
| `commit-03-c` | PH-03 | version + lineage services/runtime/handlers | `service-flow-fast` version/lineage;`infra-runtime-fake` |
| `commit-04-a` | PH-04 | baseline contracts + candidate/freeze/supersede/history domain | `contract-domain-fast` baseline slice |
| `commit-04-b` | PH-04 | baseline services/runtime/audit | `service-flow-fast` baseline slice |
| `commit-05-a` | PH-05 | query/view/projection public contracts | query contract tests;projection identity tests |
| `commit-05-b` | PH-05 | core query services + visibility/degraded/freshness/no-write | query no-write;projection tests;`cargo check` |
| `commit-05-c` | PH-05 | trace/report/history/backref query + API query entry | API query tests;projection targeted |
| `commit-06-a` | PH-06 | inbound consumer/event public carriers | inbound event contract tests |
| `commit-06-b` | PH-06 | consumer services + snapshots/receipts/stale markers + worker entry | `entry-worker-job` consumer slice |
| `commit-06-c` | PH-06 | outbound event snapshot + payload builders + publisher fake + relay loop | `operations-replay-core` relay slice;topic map;redaction targeted |
| `commit-07-a` | PH-07 | public jobs shared schema/report/result carriers | job contract tests |
| `commit-07-b` | PH-07 | maintenance/rebuild/reconcile/report replay services | `operations-replay-core` jobs slice |
| `commit-07-c` | PH-07 | handoff/export services + jobs entry + artifact/report output | handoff/export tests;`entry-worker-job`;redaction targeted |
| `commit-08-a` | PH-08 | gate/check/report generator shell + evidence index shell | release gate dry-run;dependency/redaction/report shell dry-run |
| `commit-08-b` | PH-08 | release smoke + final reports + veto/risk + acceptance handoff | `release-main-smoke`;report-generation-audit;VETO audit |

每个 boundary 必须有独立 ledger: `projects/L1-artifact/design-calibration/implementation-boundaries/<boundary>.md`。初始 current boundary 是 `commit-01-a`;其他 boundary 必须保持 `planned / wait_until_current`,直到项目级 implementation ledger 正式推进。

实现者每个 boundary 开工前必须完成字段、DTO、状态、query/view、projection/stale、ref identity、validation truth、optimistic version、idempotency、history/trace/audit、outbox source identity、config binding、artifact materialization 和 phase boundary 复核。复核失败时,实现 agent 只能记录 blocker 并等待设计闭口。

## 7. 测试与验收门禁嵌入

> 校准来源: `design-calibration/07_implementation_plan_step_07_test_acceptance_gates.md`

P0 blocking suites / checks:

| suite / check | 覆盖范围 | 产物 |
|---|---|---|
| `contract-domain-fast` | DTO / typed ref / metadata / state matrix / invariant / reject surface | raw suite artifact + suite report |
| `service-flow-fast` | 16 Command、13 Query、6 Consumer 的 service orchestration、duplicate、conflict、no-write | raw suite artifact + suite report |
| `config-redline` | strict JSON、source priority、profile isolation、topic completeness、replay root、forbidden override | config suite report |
| `dependency-boundary` | only `L0-core/core-contracts` compile-time upstream | `dependency-boundary.md` |
| `infra-runtime-fake` | repository / UoW / runtime builder / fake parity / idempotency | suite report |
| `entry-worker-job` | API handler、worker consumer disposition、relay loop、public job runner entry | suite report |
| `operations-replay-core` | outbox payload snapshots、relay publication、public jobs、stored report replay、no-truth-repair | suite report |
| `redaction-boundary` | logs / metrics / audit / trace / report / outbox / handoff forbidden body scan | `redaction-check.md` |
| `report-generation-audit` | raw artifact/report pairing、candidate evidence derivation、no-static-evidence | `report-audit.md` |
| `release-main-smoke` | 五个核心能力最小业务闭环 | release smoke report |

当前 authoritative evidence family 是 `EV-CAND-ART-*`。PH-02~PH-07 只生成可被 candidate evidence 回指的 raw artifacts 与 suite reports;PH-08 才生成 `reports/runs/<run_id>`、`evidence-index.md`、`reports/acceptance/handoff.md`、`veto-checklist.md`、`risk-acceptance.md` 和 `open-issues.md`。禁止提前发明真实 `EV-ART-*` alias 或静态写入默认 `passed`。

任一 P0 blocking gate 失败都不得进入下一 phase、不得提交当前 boundary、不得用 residual 或 unavailable 伪装通过。

## 8. 配置、环境与外部依赖准备

> 校准来源: `design-calibration/07_implementation_plan_step_08_config_environment_dependencies.md`

P0 profiles:

| profile | 用途 | 完成条件 |
|---|---|---|
| `local-dev` | 本地 fake / in-memory 开发 | strict parse, no silent fallback |
| `ci-test` | PR / main P0 suite | deterministic clock/id/fake adapters |
| `integration-like` | controlled seam / selected integration structure | no real product requirement |
| `operations-replay` | replay / report / handoff validation | fixed artifact/report roots |

外部依赖规则:

- 编译期只允许 `core-contracts` path dependency。
- `bus`、identity、process、work、governance、method-library、conversation、archive、observability、sync 和 external vendors 均不得成为 Cargo path dependency。
- runtime external seams 必须通过 ref、safe summary、event、adapter、fake、controlled 或 disabled surface 进入。
- P0 不要求真实 DB / bus / search / object storage / vendor 绑定;这些只能作为 P1/P2 residual 或 future ADR。

目标实现仓 `/home/aris/Projects/quantalithos-artifact` 在 Step 3 检查时未发现;`commit-01-a` 必须先确认或创建目标仓,并保护用户已有文件。

## 9. Spike、风险与待确认事项

> 校准来源: `design-calibration/07_implementation_plan_step_09_spikes_risks_open_questions.md`

Spike 只服务于 boundary、gate、evidence 或 design closure。必须完成或显式取消以下 Spike:

| Spike | 截止点 | 输出 |
|---|---|---|
| `SP-ART-001` target repo bootstrap dry-run | `commit-01-a` 开工前 | repo/workspace/dependency check |
| `SP-ART-002` config profile/runtime builder smoke | `commit-01-b` 提交前 | config smoke and negative invalid profile report |
| `SP-ART-003` truth carrier / DTO / state / port closure spot check | PH-02~PH-04 open gates | design closure checklist |
| `SP-ART-004` query visibility/degraded/stale surface dry-run | `commit-05-a` 开工前 | query source matrix |
| `SP-ART-005` consumer affected projection / stale mapping dry-run | `commit-06-b` 开工前 | affected view mapping |
| `SP-ART-006` outbox payload snapshot/source identity dry-run | `commit-06-c` 开工前 | payload source matrix |
| `SP-ART-007` job stored report duplicate replay dry-run | `commit-07-a` / `07-b` 开工前 | stored report demo |
| `SP-ART-008` handoff/export partial failure dry-run | `commit-07-c` 开工前 | partial failure report demo |
| `SP-ART-009` release evidence generator no-static-evidence dry-run | `commit-08-a` 提交前 | report-generation-audit dry-run |

阻塞类风险包括目标实现仓不存在、`core-contracts` 不可用、字段 / DTO / state / port / source surface 不闭口、non-core sibling compile dependency、redaction leak、query / relay / job / handoff 反写 truth、outbox current truth rebuild、static evidence / default VETO passed、planned boundary skeleton 未预创建。

待确认事项必须绑定截止点。目标仓创建、git config、release `run_id`、acceptance report 审查责任、replay root、P1 selected-run、真实产品选型、boundary 经验复核记录和 artifact/report root 创建时机,都不得以“后续确认”带入实现。

## 10. 回退、暂停与变更控制

> 校准来源: `design-calibration/07_implementation_plan_step_10_rollback_pause_change_control.md`

暂停触发:

- 目标仓、core dependency、P0 profile、required fake / replay seam、project ledger 或 boundary ledger 不满足。
- 字段 / DTO / 状态 / typed ref / port / version / source 闭口缺失。
- 当前实现越过 allowed_scope 或引用未来 boundary surface。
- P0 blocking suite、redaction、dependency、report audit 或 VETO 失败。
- 无关用户改动进入 staged diff。

回退默认只允许作用于当前 boundary 未提交改动。不得擅自回退用户改动、已验证历史提交或其他 phase 文件。已提交 boundary 需要修复时,默认新增修复 commit;只有用户明确要求时才 amend / rebase。

设计真相源缺口按目标回写:`03` 承接对象、协议、flow、状态、repository、UoW、idempotency、outbox、job report;`04` 承接配置、profile、adapter、CLI/env、topic、replay root;`05` 承接 suite、artifact、report、evidence;`06` 承接 AC、VETO、risk acceptance、final decision;`07` 承接 phase、boundary、gate、ledger。

恢复实施必须同时满足:新 design baseline 或修复已固定;project ledger 与 boundary ledger 状态一致;当前 boundary Design / Scope Gate 重新通过;工作区只含授权改动;required checks 重跑通过;failed artifact/report 保留且 fixed run 可追溯。

## 11. 提交、评审与交付纪律

> 校准来源: `design-calibration/07_implementation_plan_step_11_commit_review_delivery.md`

实现仓提交规则:

- 一笔 commit 对应 Step 6 的一个 commit boundary。
- title 固定为 `<type>(<scope>): <subject>`,title 和 body 使用英文。
- scope 必须回指 Artifact surface,如 `fact`、`version`、`lineage`、`baseline`、`query`、`consumer`、`outbox`、`job`、`release` 或 `report`。
- body 第一段说明 boundary summary,之后按 Step 6 子功能分组。
- 文件条目只写文件名,标注近似改动量。
- 禁止字面量 `\n`;复杂 message 使用 `git commit -F <message-file>`。
- footer 前空一行,AI 参与时默认使用 `Co-Authored-By: Codex <noreply@openai.com>`。

提交前必须完成 git identity、current boundary、staged scope、Design Gate、required checks、message format、whitespace、artifact/report 和 user-owned changes 复核。Commit Gate 必须记录 staged scope、unrelated changes、commit message、whitespace 和 required checks 证据。Handoff Gate 必须记录 commit hash、gates run、tests not run、remaining blockers、next boundary 和用户改动保护说明。

提交或交付说明只引用 `reports/runs/<run_id>`、suite reports、`reports/acceptance/*` 和 boundary ledger evidence,不得粘贴完整日志。

## 12. 实施完成判定

> 校准来源: `design-calibration/07_implementation_plan_step_12_completion_criteria.md`

实施完成只表示实现可送验,不替代 `06-验收标准.md` 的最终验收裁决。实现可送验必须同时满足:

1. P0 范围全部覆盖。
2. Step 4 交付物全部完成。
3. `commit-01-a` 到 `commit-08-b` 均完成、提交、review、handoff。
4. 所有 P0 blocking gates 通过。
5. `VETO-ART-001~009` 未命中。
6. S 级缺陷为零。
7. evidence index、redaction、dependency、report audit 和 acceptance reports 完整。
8. phase / commit boundary 交付实现前可落码闭环审计无 blocker。

完成判定只允许三种结论:

| 结论 | 条件 |
|---|---|
| 实现完成 / 可送验 | P0 全部通过,VETO 未命中,S=0,证据完整,无未接受 A 级,设计闭环无 blocker |
| 有条件实现完成 / 可送验但带 residual | P0 主线成立,VETO 未命中,S=0,证据完整,存在已接受 A/B/R residual |
| 不完成 / 不可送验 | 任一 P0 blocking gate failed,VETO 命中,S 未关闭,证据不可裁决,设计闭环 blocker 未修 |

禁止使用“基本完成”“原则上完成”“大体完成”等模糊结论。VETO、S 级、P0 blocking gate、redaction/dependency/report audit failure、static evidence、query/job truth repair、设计闭环 blocker 均不得风险接受。

## 13. 参考

| 类别 | 文档 |
|---|---|
| 正式设计 | `00-需求文档.md`;`01-架构设计.md`;`02-概要设计.md`;`03-详细设计.md`;`04-配置设计.md`;`05-测试方案.md`;`06-验收标准.md` |
| 实施计划校准 | `design-calibration/07_implementation_plan_step_01_input_boundary.md` through `07_implementation_plan_step_13_formal_document_assembly.md` |
| 实施门禁 | `design-calibration/implementation_execution_ledger.md`;`design-calibration/implementation-boundaries/*.md` |
| 标准 | `standards/document/实施计划讨论流程_SOP.md`;`standards/document/实施计划书写规范.md`;`standards/document/代码实施台账与门禁规范.md`;`standards/document/设计真相源闭环与可落码性标准.md`;`standards/coding/rust.md`;`standards/document/子项目目录与代码文件组织规范.md` |

正式实现移交时,implementation agent 必须从 `implementation_execution_ledger.md` 和当前 boundary ledger 开始,不得直接从本文 future boundary 表格开工。

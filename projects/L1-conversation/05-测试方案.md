# 05-测试方案 · L1-conversation

## 文档元信息

| 项 | 内容 |
|---|---|
| 子项目 | L1-conversation |
| 文档类型 | 测试方案 |
| 版本 | v1.0 |
| 状态 | 正式草案 |
| 日期 | 2026-06-02 |
| 生成方式 | 按 `测试方案讨论流程_SOP.md` Step 1~15 校准后重建 |

### 变更记录

| 版本 | 日期 | 变更 | 作者 |
|---|---|---|---|
| v1.0 | 2026-06-02 | 删除旧版测试方案,按新版需求、架构、概要、详细和配置设计重建 15 章测试方案 | codex |

---

## 1. 与上游文档的关系声明

> 校准来源：
> - `design-calibration/05_test_plan_step_01_input_boundary.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_01_input_boundary.md` 的“结构化中间产物”“回填草稿”和“待确认事项”小节，了解本测试方案如何确认输入边界、旧稿处理方式和后续验收承接关系。

本测试方案只定义如何验证 `L1-conversation` 的新版需求与设计是否被正确实现。本文不重新定义需求、架构、概要、详细设计或配置项；若测试方案发现设计契约冲突,必须回到对应设计文档重新校准。

| 来源文档 | 测试输入 | 本文如何使用 |
|---|---|---|
| `00-需求文档.md` | 核心能力闭环、`FR-CONV-*`、`BR-CONV-*`、`NFR-CONV-*`、验收方向和一票否决项 | 转成测试目标、范围、覆盖矩阵和红线用例 |
| `01-架构设计.md` | 上下游依赖、职责边界、数据所有权、横切关注点 | 转成环境拓扑、跨仓接缝测试和边界负向测试 |
| `02-概要设计.md` | 主要组成部分、关键对象、接口骨架、处理流、状态机 | 转成测试对象、测试切口和场景分组 |
| `03-详细设计.md` | 模块契约、对象字段、协议、处理流、状态矩阵、错误、事务、幂等、观测、测试切口 | 转成用例断言、状态验证、恢复验证和最小测试门禁 |
| `04-配置设计.md` | profile、配置项、失效模式、reports / artifacts、redaction | 转成配置测试、环境矩阵、path shape 和 redaction gate |
| `06-验收标准.md` | 验收裁决和 AC 追溯 | 后续由验收标准消费本文的 TC、EV 和 reports 入口 |

测试方案与验收标准的边界如下:

```text
05-测试方案
  - 怎么测
  - 测哪些场景
  - 用哪些数据、环境、脚本和门禁
  - 证据如何归档

06-验收标准
  - 什么结果算通过
  - 什么问题一票否决
  - 哪些残余风险可接受
  - 哪些 EV 支撑哪些 AC
```

## 2. 本次测试目标与范围

> 校准来源：
> - `design-calibration/05_test_plan_step_02_scope.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_02_scope.md` 的“测试目标总表”“范围 / 非范围表”“优先级口径”和“回填草稿”小节，了解本章的 P0 / P1 / P2 边界如何从需求、架构、详细设计和配置设计收敛而来。

本轮测试目标是证明 `L1-conversation` 作为 Conversation truth center 的 P0 主链成立: 对话空间与参与范围能够被独立确立,多来源协作事实能够追加沉淀,对话事实能够按授权视野被稳定消费,关键跨域事实只能以引用、safe snapshot 或 manifestation 进入对话视野,对话历史能够被追溯并交接给观测 / 归档接缝。

| 范围项 | 类型 | 优先级 | 验证目标 | 非目标 / 说明 |
|---|---|---|---|---|
| Conversation space / scope | 核心功能 | P0-blocking | 建立、更新、关闭和 visibility guard | 不测试 identity 生命周期 |
| Conversation fact append | 核心功能 | P0-blocking | append-only、receipt、idempotency、forbidden body reject | 不测试 runtime 推理过程 |
| Authorized query / projection | 核心功能 + 派生消费 | P0-blocking | 授权读取、stale / failed marker、query 不写入 | 不测试 Chat UI / Workspace 展示 |
| Cross-domain manifestation | 核心功能 | P0-blocking | ref-only、safe snapshot、unresolved / digest mismatch | 不测试来源仓业务真相 |
| Trace / review / handoff | 核心功能 + 追溯 | P0-blocking | audit、review anchor、handoff retry / failed、payload ref-only | 不测试全局归档恢复 |
| Outbox / events / worker | 事件协作 | P0-blocking | event schema、retry、duplicate、publish failure | 不测试真实 broker |
| Operations jobs | 维护与恢复 | P0-supporting / P0-blocking redline | rebuild、refresh、cleanup、consistency report、rerun | 不自动修写真相 |
| Configuration / reports / redaction | 配置与证据 | P0-blocking | profile、config validation、path shape、redaction failure | 不定义生产 runbook |
| Durable store / real bus / real resolver / real handoff | 外部集成 | P1 | 保留 adapter 接缝和 failure semantics | 当前不要求真实 endpoint |
| Config center / hot reload / auto repair | 远期能力 | P2 | P0 中启用必须 reject / unsupported | 当前不设计、不测试功能通过 |

一票否决范围包括核心闭环缺失、授权视野失效、相邻仓正文或 forbidden body 进入本仓、已成立 fact 被覆盖或 query / projection 反写真相、Conversation 自行推断来源仓真相、配置绕过 redaction / idempotency / audit / state machine,以及测试无法形成固定 artifacts / reports 证据。

## 3. 测试对象与测试切口

> 校准来源：
> - `design-calibration/05_test_plan_step_03_test_objects_slices.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_03_test_objects_slices.md` 的“测试对象总表”“P0 切口矩阵”“风险到切口映射”和“回填草稿”小节，了解测试对象如何从新版详细设计的对象、协议、状态和恢复口径中抽取。

测试对象按 truth center、协议入口、派生消费、跨仓接缝、恢复维护和证据门禁分组。每个 P0 对象必须回指详细设计中的正式对象、协议、状态或错误契约。

| 测试对象 | 来源章节 / 中间产物 | 测试切口 | 风险 | 推荐测试层级 |
|---|---|---|---|---|
| Conversation space / participant scope / visibility scope | `03_ddd_step_06_object_contracts.md`; `03_ddd_step_10_state_matrix.md` | 创建、关闭、scope 更新、sealed visibility、outbox side effect | space / scope 不成立会破坏所有后续事实消费 | unit + service |
| Conversation fact / receipt / trace | `03_ddd_step_06_object_contracts.md`; `03_ddd_step_09_function_flows.md` | append、retract、idempotency、transaction rollback、forbidden body | Conversation 退化为消息缓存或覆盖式存储 | unit + service + gate |
| Command / Query / Consumer / Job 协议 | `03_ddd_step_08_protocol_contracts.md` | metadata、required fields、DTO roundtrip、error mapping | 协议缺字段或字段漂移导致实现无法 1:1 落码 | contract + service |
| Manifestation / reference resolution | `03_ddd_step_09_function_flows.md`; `03_ddd_step_12_error_recovery.md` | safe snapshot、unresolved、digest mismatch、source body absent | 来源 truth 被本仓补造或正文泄露 | service + integration-like |
| Outbox / event collaboration | `03_ddd_step_08_protocol_contracts.md`; `03_ddd_step_13_concurrency_idempotency.md` | publish success、retry、failed、duplicate event id | 下游无法稳定感知变化或重复消费 | worker + job |
| Projection / search / cursor / consistency | `03_ddd_step_09_function_flows.md`; `03_ddd_step_10_state_matrix.md` | derived read-only、stale / failed marker、cursor no regress、no auto repair | 派生层反写真相或隐藏不一致 | query + job + operations-replay |
| Trace / archive handoff | `03_ddd_step_09_function_flows.md`; `03_ddd_step_15_observability_audit.md` | payload ref-only、retry / failed、audit refs、redaction | 追溯不可交接或交接泄露正文 | service + job + redaction |
| Config / reports / redaction scripts | `03_ddd_step_16_test_slices.md`; `04_config_step_11_failure_modes.md` | unsupported profile、path shape、strict redaction、fake-as-production | 证据不可验收或安全下限被绕过 | config + release gate |

## 4. 测试策略与分层

> 校准来源：
> - `design-calibration/05_test_plan_step_04_strategy_layers.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_04_strategy_layers.md` 的“测试分层图”“分层策略表”“风险发现位置矩阵”和“回填草稿”小节，了解不同风险为什么被放在 unit、service、contract、worker、job 或 release gate 中发现。

#### 测试分层图: L1-conversation P0 test layers

```text
[Unit tests]
  - domain object / state / policy / idempotency invariant
        |
        v
[Service tests]
  - command / query service + fake ports + transaction boundary
        |
        v
[Contract tests]
  - DTO / event / job / metadata roundtrip
        |
        v
[Worker / Job tests]
  - consumer / outbox / projection / handoff / replay
        |
        v
[Release gate]
  - P0 smoke + redaction + path shape + evidence package
```

关键说明:

- domain 与 service 层优先发现 truth、状态、事务和幂等错误。
- contract 层发现 DTO / event / metadata 字段漂移。
- worker / job 层发现 outbox、consumer、handoff、projection 和 replay 问题。
- release gate 只验证 P0 smoke、红线和证据包,不替代底层测试。

| 层级 | 目标 | 典型内容 | 执行时机 | 失败处理 |
|---|---|---|---|---|
| Unit | 发现领域不变量、状态机和 policy 错误 | scope、fact、manifestation、outbox、handoff state | PR | 阻断 |
| Service | 验证应用编排、事务、幂等和 fake port failure | command / query service、UnitOfWork、outbox enqueue | PR / main CI | 阻断 |
| Contract | 验证 DTO、metadata、event、job input / output | Command、Query、Consumer、Event、Job schema | PR / main CI | 阻断 |
| Worker / Job | 验证事件消费、发布、重跑、恢复和 derived view | inbound consumer、outbox relay、projection、handoff | main CI / nightly | 阻断或 readiness 阻断 |
| Integration-like | 验证 controlled adapter 接缝和 degraded semantics | resolver、publisher、handoff controlled adapter | nightly | release readiness 阻断 |
| Release gate | 验证 P0 红线、证据路径和 redaction | redline suite、report generation、redaction scan | release candidate | 阻断 |

## 5. 需求追溯与覆盖矩阵

> 校准来源：
> - `design-calibration/05_test_plan_step_05_traceability_matrix.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_05_traceability_matrix.md` 的“FR 覆盖矩阵”“BR 覆盖矩阵”“NFR 覆盖矩阵”“覆盖状态汇总”和“未覆盖项清单”小节，了解需求、规则、非功能要求如何追溯到设计章节、测试切口、自动化层级和证据类别。

本轮测试追溯覆盖 `FR-CONV-001~008`、`BR-CONV-001~021` 和 `NFR-CONV-001~012`。`FR-CONV-001~005` 是核心闭环能力,必须进入 P0-blocking 自动化覆盖；`FR-CONV-006~008` 是外围增强能力,但涉及授权、数据归属、derived read-only、redaction 和证据路径的负向场景必须进入 gate。

| 需求 / 规则 ID | 设计依据 | 测试场景 | 用例 ID | 自动化 | 证据 ID | 覆盖状态 |
|---|---|---|---|---|---|---|
| FR-CONV-001 | `03` Step 6 / 8 / 9 / 10 / 11 / 15 | space、participant scope、visibility scope | TC-CONV-SPACE-*; TC-CONV-SCOPE-* | CI service + API | EV-CONV-TRUTH-001 | 已覆盖 |
| FR-CONV-002 | `03` Step 6 / 8 / 9 / 10 / 13 / 15 | append fact、retract fact、idempotency、forbidden body | TC-CONV-FACT-*; TC-CONV-TX-001 | CI service + gate | EV-CONV-FACT-001; EV-CONV-REDACTION-001 | 已覆盖 |
| FR-CONV-003 | `03` Step 6 / 8 / 9 / 10 / 15 | authorized read、query no-write、cursor、search | TC-CONV-QUERY-*; TC-CONV-SEARCH-001 | CI query + contract | EV-CONV-AUTH-001 | 已覆盖 |
| FR-CONV-004 | `03` Step 6 / 8 / 9 / 12 / 15 | manifestation、safe snapshot、unresolved、digest mismatch | TC-CONV-MAN-*; TC-CONV-CONSUMER-* | CI service + worker + integration-like | EV-CONV-MAN-001; EV-CONV-CONSUMER-001 | 已覆盖 |
| FR-CONV-005 | `03` Step 6 / 8 / 9 / 10 / 12 / 15 | review anchor、trace handoff、archive handoff | TC-CONV-TRACE-001; TC-CONV-HANDOFF-* | CI service + job | EV-CONV-HANDOFF-001 | 已覆盖 |
| FR-CONV-006~008 | `03` Step 8 / 9 / 10 / 11 / 13 | projection、search、cursor、outbox event collaboration | TC-CONV-DERIVED-*; TC-CONV-CURSOR-001; TC-CONV-OUTBOX-* | CI job + operations-replay | EV-CONV-DERIVED-001; EV-CONV-OUTBOX-001 | 已覆盖 |
| BR-CONV-001~021 | `03` Step 6 / 8 / 9 / 10 / 11 / 13 / 15 | truth、边界、授权、source isolation、derived read-only、trace / audit | 对应 P0 用例族 | CI + release gate | EV-CONV-* | 已覆盖 |
| NFR-CONV-001~012 | `03` Step 11 / 12 / 13 / 15; `04` §11 / §12 | baseline、恢复、安全、redaction、证据路径、观测 | 专项和 release gate 用例 | main CI + nightly + release gate | EV-CONV-GATE-001; EV-CONV-REDACTION-001; EV-CONV-ACCEPT-001 | 已覆盖 |

当前没有 P0 需求空洞。真实 DB / broker / resolver / handoff、production-like、生产级量化性能数字、Chat UI / Workspace / Bridges 体验和 Runtime 推理质量属于 P1/P2 或其他子项目专项,不作为本轮 P0 通过条件。

## 6. 测试场景与用例设计

> 校准来源：
> - `design-calibration/05_test_plan_step_06_cases.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_06_cases.md` 的“测试场景总表”“P0 用例矩阵”“契约断言矩阵”和“后续 phase 防提前写入检查”小节，了解每个 P0 场景如何从详细设计协议、处理流、状态矩阵和错误恢复口径落成可执行用例。

P0 用例按 space / scope truth、fact append truth、authorized consumption、cross-domain manifestation、trace / handoff、derived views / search / cursor、outbox / event collaboration、configuration / redaction / evidence 八组组织。所有 P0-blocking 用例必须有明确输入、预期状态、错误或 event 断言。

| 用例 ID | 场景 | 优先级 | 输入 / 操作 | 预期结果与断言点 | 自动化候选 | 证据 ID |
|---|---|---|---|---|---|---|
| TC-CONV-SPACE-001 | 创建对话空间主线 | P0-blocking | `CreateConversationSpace` with `CommandMetadata` + `IdempotencyKey` | `ConversationTruthState::Open`; `ConversationSpaceLifecycleState::Active`; `VisibilityScopeState::Open`; outbox `Pending` | CI service + API | EV-CONV-TRUTH-001 |
| TC-CONV-SPACE-002 | 创建缺少幂等键拒绝 | P0-blocking | `CreateConversationSpace` missing `idempotency_key` | `ProtocolError::MissingRequiredField`; no truth / outbox | CI contract | EV-CONV-TRUTH-001 |
| TC-CONV-SPACE-003 | 关闭后再次打开拒绝 | P0-blocking | 对 `Closed` space 触发 reopen | `DomainError::InvalidStateTransition`; no outbox | CI domain | EV-CONV-TRUTH-001 |
| TC-CONV-SCOPE-001 | visibility 收紧并标记派生陈旧 | P0-blocking | `UpdateVisibilityScope` narrow visibility | `VisibilityScopeState::Restricted`; `ProjectionFreshnessState::Stale`; scope changed outbox | CI service | EV-CONV-TRUTH-001 |
| TC-CONV-SCOPE-002 | sealed visibility 扩张拒绝 | P0-blocking | `UpdateVisibilityScope` expand sealed visibility | `DomainError::InvalidStateTransition`; visibility unchanged | CI domain + service | EV-CONV-TRUTH-001 |
| TC-CONV-FACT-001 | 追加协作事实主线 | P0-blocking | `AppendConversationFact` with `payload_ref` | `ConversationFactState::Accepted`; `FactAppendResult::Accepted`; `ConversationFactAppendedEvent` | CI service + API | EV-CONV-FACT-001 |
| TC-CONV-FACT-002 | 重复 append 返回已有 receipt | P0-blocking | same `IdempotencyKey` + same digest | existing receipt; outbox count unchanged | CI service | EV-CONV-FACT-001 |
| TC-CONV-FACT-003 | 同幂等键不同 digest 冲突 | P0-blocking | same key + different request digest | `IdempotencyError::Conflict`; no new fact | CI service | EV-CONV-FACT-001 |
| TC-CONV-FACT-004 | forbidden body 不得落库 | P0-blocking | command or consumer carries reasoning / platform body | `DomainError::SourceTruthViolation` or `BoundaryViolation`; no truth / log / event body | CI gate | EV-CONV-REDACTION-001 |
| TC-CONV-FACT-005 | 撤回 fact 主线 | P0-blocking | `RetractConversationFact` for accepted fact | `ConversationFactState::Retracted`; trace retained; outbox generated | CI service | EV-CONV-FACT-001 |
| TC-CONV-TX-001 | command outbox enqueue failure 回滚 | P0-blocking | inject outbox enqueue failure | truth、trace、receipt、idempotency complete 同事务回滚 | CI repository + service | EV-CONV-FACT-001 |
| TC-CONV-QUERY-001 | 授权读取 read model | P0-blocking | `GetConversationReadModel` by visible consumer | authorized view only; query no-write | CI query | EV-CONV-AUTH-001 |
| TC-CONV-QUERY-002 | 未授权读取拒绝 | P0-blocking | hidden consumer reads hidden fact | `ApplicationError::NotVisible`; hidden fact absent | CI query + gate | EV-CONV-AUTH-001 |
| TC-CONV-QUERY-003 | stale projection 只暴露 marker | P0-blocking | query when projection is `Stale` or `Failed` | marker visible; query does not rebuild | CI query | EV-CONV-AUTH-001 |
| TC-CONV-QUERY-004 | cursor 不可恢复 | P0-blocking | `PollConversationChanges` with expired / invalid cursor | `ApplicationError::CursorNotResumable`; no cursor advance | CI query | EV-CONV-AUTH-001 |
| TC-CONV-SEARCH-001 | 授权搜索只返回 refs | P0-supporting | `SearchConversationHistory` | visible refs / fragments only; payload body absent | CI query + redaction | EV-CONV-AUTH-001 |
| TC-CONV-MAN-001 | 跨域事实显化主线 | P0-blocking | `ManifestExternalFact` with safe snapshot | `ManifestationState::Manifested`; `ReferenceResolutionState::Fresh` | CI service | EV-CONV-MAN-001 |
| TC-CONV-MAN-002 | 来源不可解析不补造 truth | P0-blocking | resolver returns unresolved | `ReferenceResolutionState::Unresolved`; source body absent | CI integration-like | EV-CONV-MAN-001 |
| TC-CONV-MAN-003 | digest mismatch 记录 evidence | P0-blocking | refresh snapshot with mismatched digest | mismatch marker; old truth not overwritten | CI job | EV-CONV-MAN-001 |
| TC-CONV-CONSUMER-001 | runtime 结果事实入库 | P0-blocking | `ConsumeRuntimeResultCommitted` with result ref only | fact、trace、outbox committed; reasoning body absent | CI worker | EV-CONV-CONSUMER-001 |
| TC-CONV-CONSUMER-002 | inbound envelope 缺字段 quarantine | P0-blocking | consumer receives invalid envelope | `ProtocolError::InvalidEnvelope`; no truth write | CI worker | EV-CONV-CONSUMER-001 |
| TC-CONV-CONSUMER-003 | bridge 平台正文拒绝 | P0-blocking | bridge event contains platform body | reject or quarantine; no body saved | CI worker + gate | EV-CONV-REDACTION-001 |
| TC-CONV-TRACE-001 | 创建 review anchor | P0-blocking | `CreateReviewAnchor` for visible target | anchor saved; trace / audit refs present | CI service | EV-CONV-HANDOFF-001 |
| TC-CONV-HANDOFF-001 | 请求 trace handoff | P0-blocking | `RequestTraceHandoff` | `TraceHandoffState::Pending`; handoff requested event | CI service | EV-CONV-HANDOFF-001 |
| TC-CONV-HANDOFF-002 | trace handoff retry / failed 不改 fact | P0-blocking | fake handoff fails | `RetryPending` or `Failed`; fact state unchanged | CI job | EV-CONV-HANDOFF-001 |
| TC-CONV-HANDOFF-003 | archive handoff 只保存 package ref | P0-blocking | archive handoff success | `ArchiveHandoffState::Archived`; package body absent | CI job + redaction | EV-CONV-HANDOFF-001 |
| TC-CONV-OUTBOX-001 | 发布 outbox 成功 | P0-blocking | `PublishConversationOutbox` success | `ConversationOutboxPublicationState::Published`; stable event id | CI job | EV-CONV-OUTBOX-001 |
| TC-CONV-OUTBOX-002 | 发布失败进入 retry / failed | P0-blocking | fake publisher failure | truth unchanged; outbox `RetryPending` or `Failed` | CI job | EV-CONV-OUTBOX-001 |
| TC-CONV-OUTBOX-003 | publish rerun 不重复事件 | P0-blocking | rerun after state write failure | no duplicate downstream record; stable event id | CI job | EV-CONV-OUTBOX-001 |
| TC-CONV-DERIVED-001 | 重建 read model 不修写真相 | P0-supporting | `RebuildConversationReadModels` | read model fresh; fact count unchanged | CI job | EV-CONV-DERIVED-001 |
| TC-CONV-DERIVED-002 | search rebuild 失败暴露 failed marker | P0-supporting | fake index failure | `ProjectionFreshnessState::Failed`; query exposes failed marker | CI job + query | EV-CONV-DERIVED-001 |
| TC-CONV-CURSOR-001 | cursor 前进且不倒退 | P0-blocking | `MaintainConversationChangeCursors` | no `DomainError::SequenceRegression`; stale on outbox gap | CI job | EV-CONV-DERIVED-001 |
| TC-CONV-CONSISTENCY-001 | consistency validation 只报告不修复 | P0-blocking | read model missing fixture | issue / report ref produced; no truth repair | operations-replay | EV-CONV-DERIVED-001 |
| TC-CONV-CONFIG-001 | unsupported profile fail-fast | P0-blocking | start runtime with unsupported profile | config validation failure; no runtime graph | CI config | EV-CONV-CONFIG-001 |
| TC-CONV-REPORT-001 | evidence path shape 固定 | P0-blocking | run gate / report scripts | output uses `artifacts/test/<run_id>` and `reports/runs/<run_id>`; no `<project>` / `latest` | release gate | EV-CONV-CONFIG-001 |
| TC-CONV-REDACTION-001 | redaction check 命中失败 | P0-blocking | artifact / report contains sentinel raw secret or forbidden body | `scripts/checks/check_redaction.sh` exits non-zero; failure summary retained | release gate | EV-CONV-REDACTION-001 |

## 7. 测试数据设计

> 校准来源：
> - `design-calibration/05_test_plan_step_07_test_data.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_07_test_data.md` 的“测试数据集表”“Fixture / Builder 规则”“Seed 命名规则”“隔离与清理规则”和“外部依赖数据替身表”小节，了解每组 P0 用例的数据前置条件如何稳定生成。

测试数据必须由 deterministic seed、fixture builder 和 fake adapter script 自动生成。所有数据以 `TestRunId` 隔离,业务 ID、event id、idempotency key、job run id、trace ref、artifact path 和 report path 都必须能从 run-scoped seed 重建。

| 数据集 | 用途 | 构造方式 | 隔离键 | 清理方式 | 关联用例 |
|---|---|---|---|---|---|
| `DS-CONV-BASE-SPACE` | space / scope 主线 | `ConversationSpaceFixtureBuilder` | `TestRunId` + `SpaceSeed` | reset in-memory truth store | TC-CONV-SPACE-*; TC-CONV-SCOPE-* |
| `DS-CONV-FACT-HAPPY` | append / retract fact | `ConversationFactFixtureBuilder` | `TestRunId` + fact sequence seed | reset fact / trace / outbox stores | TC-CONV-FACT-001; TC-CONV-FACT-005 |
| `DS-CONV-IDEMPOTENCY` | duplicate / conflict | fixed idempotency key + digest | `TestRunId` namespace | reset idempotency store | TC-CONV-FACT-002~003 |
| `DS-CONV-AUTH-VISIBILITY` | authorized and hidden views | visible / hidden actor + fact fixtures | `TestRunId` + consumer ref | reset read model store | TC-CONV-QUERY-*; TC-CONV-SEARCH-001 |
| `DS-CONV-MANIFESTATION` | safe snapshot / unresolved / mismatch | fake resolver scripted responses | `TestRunId` + external ref | reset resolver script + snapshot store | TC-CONV-MAN-* |
| `DS-CONV-CONSUMER` | inbound event positive / negative | event builder with valid / invalid envelope | `TestRunId` + event id namespace | reset event idempotency + quarantine store | TC-CONV-CONSUMER-* |
| `DS-CONV-HANDOFF` | trace / archive handoff | fake handoff success / transient / permanent failure | `TestRunId` + handoff id | reset handoff store + fake script | TC-CONV-HANDOFF-* |
| `DS-CONV-OUTBOX` | publish / retry / rerun | fake publisher success / failure / state write failure once | `TestRunId` + outbox id | reset outbox store | TC-CONV-OUTBOX-* |
| `DS-CONV-DERIVED` | projection / search / cursor / consistency | projection fixture with fresh / stale / failed states | `TestRunId` + projection scope | reset projection / cursor stores | TC-CONV-DERIVED-*; TC-CONV-CURSOR-001 |
| `DS-CONV-CONFIG` | config / report / redaction negative | JSON fixture with unsupported profile, path and sentinel raw secret | `TestRunId` + config case id | delete current run directory only | TC-CONV-CONFIG-001; TC-CONV-REPORT-001; TC-CONV-REDACTION-001 |

P0 默认使用 in-memory repository、fixed clock、deterministic id generator、fake resolver、fake publisher 和 fake handoff。fake 必须保留 unresolved、retry、failed、quarantine、redaction 和 fake marker 语义,不得被标记为 production success。

## 8. 测试环境与配置矩阵

> 校准来源：
> - `design-calibration/05_test_plan_step_08_environment_config.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_08_environment_config.md` 的“环境拓扑图”“环境矩阵”“配置矩阵”“依赖类型与协作方式判定表”和“环境不可用处理表”小节，了解 P0 环境、依赖类型和配置 profile 如何被测试方案裁剪。

#### 环境拓扑图: L1-conversation P0 test dependencies

```text
[quantalithos-conversation test runtime]
  | [compile] path dependency
  v
[L0-core contracts]

[quantalithos-conversation test runtime]
  | [event] fake publisher / event replay
  v
[L0-bus collaboration boundary]

[quantalithos-conversation test runtime]
  | [runtime] fake / controlled resolver
  v
[L1-identity / L1-work / L1-governance / L1-artifact / L2-runtime / L6-bridges]

[quantalithos-conversation test runtime]
  | [runtime] fake / controlled handoff
  v
[L4-observability / L4-archive]

[quantalithos-conversation test runtime]
  | writes
  v
[artifacts/test/<run_id>] -> [reports/runs/<run_id>]
```

关键说明:

- `[compile]` 只表示本地 path dependency 或已发布契约包。
- `[runtime]` 不允许用 path dependency 伪装服务可用,必须使用 fake / controlled adapter / real-like。
- `[event]` 不要求真实 broker,但必须保留 event id、idempotency、retry、failed 和 quarantine 语义。
- artifacts / reports 路径必须 run-scoped,不得写 `latest` 或 `<project>` 层级。

| 环境 | 用途 | 依赖服务 | 全局依赖类型 | 测试协作方式 | 关键配置 | 数据策略 | 风险 |
|---|---|---|---|---|---|---|---|
| local-dev | 本地最小主链和人工复现 | in-memory store、fake resolver / publisher / handoff | `[compile] L0-core`; `[runtime] fake`; `[event] fake` | defaults + optional JSON | `runtime.profile=local-dev`; `security.redaction_policy=strict` | 基础 happy path 数据 | 不代表验收通过 |
| ci-test | P0 自动化默认环境 | deterministic fake adapters、temp dirs | `[compile] L0-core`; `[runtime] fake`; `[event] fake` | deterministic fixture + fake scripts | `runtime.profile=ci-test`; `reports.artifacts_root=artifacts/test` | 全部 P0-blocking 数据集 | path 或 profile 漂移 fail-fast |
| integration-like | controlled 接缝验证 | controlled resolver / publisher / handoff | `[runtime] controlled`; `[event] controlled` | controlled adapter, credential ref only | `runtime.profile=integration-like` | external / handoff / outbox 数据集 | 不代表 production pass |
| operations-replay | 重跑、恢复、partial failure、diagnostic | replay state、outbox、projection、handoff、report root | `[runtime] replay`; `[event] replay` | replay JSON + fake / historical refs | `runtime.profile=operations-replay`; `jobs.*`; `projection.*` | outbox、derived、handoff、consistency | replay 数据不完整时 fail-fast |
| staging-like | 后续跨仓集成演练 | real-like DB / event bus / resolver / handoff | `[runtime] real-like`; `[event] real-like` | 后续专项 | P1 部署材料定义 | 后续专项数据 | 不阻塞 P0 |
| production-like | 生产运维语境 | real DB / event bus / source adapters / secret provider | `[runtime] production`; `[event] production` | 后续运维专项 | P1/P2 运维材料定义 | 不在当前 P0 中生成 | 不阻塞 P0 |

## 9. 自动化与 CI/CD 门禁

> 校准来源：
> - `design-calibration/05_test_plan_step_09_automation_ci_gates.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_09_automation_ci_gates.md` 的“自动化套件表”“CI/CD 门禁图”“gate / report / check 脚本表”“artifact 与 report 输出映射表”和“flaky / timeout / dependency failure 处理表”小节，了解每个 P0-blocking 用例如何进入自动化门禁。

| 套件 | 覆盖范围 | 执行位置 | 触发条件 | 阻断级别 | 执行脚本 | artifact 输出 | report 输出 |
|---|---|---|---|---|---|---|---|
| `SUITE-CONV-PR-UNIT` | domain object、policy、状态机、forbidden field guard | PR | 任意代码 / 契约变更 | P0-blocking | `scripts/gates/run_ci_gate.sh` | `artifacts/test/<run_id>/pr-unit` | `reports/runs/<run_id>/pr-unit.md` |
| `SUITE-CONV-PR-CONTRACT` | DTO roundtrip、metadata、envelope、payload ref-only | PR | contracts / API / worker 变更 | P0-blocking | `scripts/gates/run_ci_gate.sh` | `artifacts/test/<run_id>/pr-contract` | `reports/runs/<run_id>/pr-contract.md` |
| `SUITE-CONV-MAIN-SERVICE` | command service、transaction、idempotency、manifestation、handoff | main CI | merge / main push | P0-blocking | `scripts/gates/run_ci_gate.sh` | `artifacts/test/<run_id>/main-service` | `reports/runs/<run_id>/main-service.md` |
| `SUITE-CONV-MAIN-QUERY` | Query、visibility、stale / failed marker、cursor、search refs-only | main CI | merge / main push | P0-blocking | `scripts/gates/run_ci_gate.sh` | `artifacts/test/<run_id>/main-query` | `reports/runs/<run_id>/main-query.md` |
| `SUITE-CONV-MAIN-WORKER-JOB` | inbound consumer、outbox、projection、cursor、handoff、consistency jobs | main CI | merge / main push | P0-blocking | `scripts/gates/run_ci_gate.sh` | `artifacts/test/<run_id>/main-worker-job` | `reports/runs/<run_id>/main-worker-job.md` |
| `SUITE-CONV-MAIN-CONFIG` | unsupported profile、strict redaction、path shape、fake marker | main CI | config / scripts / report 变更 | P0-blocking | `scripts/gates/run_ci_gate.sh` | `artifacts/test/<run_id>/main-config` | `reports/runs/<run_id>/main-config.md` |
| `SUITE-CONV-NIGHTLY-INTEGRATION-LIKE` | controlled resolver / publisher / handoff、source unresolved、digest mismatch | nightly | scheduled / release candidate | Release readiness blocking | `scripts/gates/run_ci_gate.sh` | `artifacts/test/<run_id>/nightly-integration-like` | `reports/runs/<run_id>/nightly-integration-like.md` |
| `SUITE-CONV-NIGHTLY-OPS-REPLAY` | projection、search、cursor、consistency、handoff、outbox rerun | nightly | scheduled / replay input change | Release readiness blocking | `scripts/gates/run_ci_gate.sh` | `artifacts/test/<run_id>/nightly-ops-replay` | `reports/runs/<run_id>/nightly-ops-replay.md` |
| `SUITE-CONV-RELEASE-REDLINE` | P0 smoke、authorization、append-only、source isolation、redaction、path shape | release gate | release candidate | P0-blocking | `scripts/gates/run_ci_gate.sh` + `scripts/checks/check_redaction.sh` | `artifacts/test/<run_id>/release-redline` | `reports/runs/<run_id>/release-redline.md` |
| `SUITE-CONV-RELEASE-REPORT` | report generation、run summary、acceptance extract input | release gate | release candidate | P0-blocking | `scripts/reports/generate_reports.sh` | reads `artifacts/test/<run_id>` | `reports/runs/<run_id>` and `reports/acceptance` |

所有 P0 gate 输出必须使用 `artifacts/test/<run_id>` 和 `reports/runs/<run_id>`。正式证据不得使用 `latest` 或项目名子目录。

## 10. 专项测试与非功能验证

> 校准来源：
> - `design-calibration/05_test_plan_step_10_special_nonfunctional.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_10_special_nonfunctional.md` 的“专项测试矩阵”“非功能专项覆盖表”“阈值来源表”“专项测试执行位置表”和“观测材料检查表”小节，了解 P0 非功能、红线、一致性恢复和可观测性如何被验证。

本轮专项测试覆盖性能 baseline、安全与数据归属红线、一致性与恢复、配置 fail-fast、观测和审计材料。性能专项不写未确认的 p95、TPS 或容量数字,只验证核心追加、授权读取和追溯不会因历史规模、相邻仓正文不可解析或外围增强失败而无法成立。

| 专项 | 指标 / 风险 | 方法 | 环境 | 阈值 / 通过条件 | 证据 |
|---|---|---|---|---|---|
| 核心追加 baseline | append fact、trace、outbox 同事务在历史规模增长下仍成立 | 长历史 fixture + TC-CONV-FACT-001 / TC-CONV-TX-001 | `ci-test`; nightly trend | 不因历史 fixture 或外部正文不可解析而无法 append | EV-CONV-FACT-001 |
| 授权读取 baseline | authorized read / query no-write / stale marker | TC-CONV-QUERY-001~004 | `ci-test`; nightly trend | only authorized view; query no-write; marker visible | EV-CONV-AUTH-001 |
| 外围增强降级 | projection / search / cursor failure 不阻断核心 truth | inject rebuild / search / cursor failure | `ci-test`; `operations-replay` | 核心 truth 仍成立;派生状态为 `Stale` / `Failed` | EV-CONV-DERIVED-001 |
| 授权红线 | 未授权消费、sealed visibility 扩张 | negative query / scope tests | `ci-test`; release gate | unauthorized 不返回 hidden fact; sealed 不被打开 | EV-CONV-AUTH-001 |
| 数据归属红线 | runtime reasoning body、bridge platform body、artifact body 泄露 | forbidden body sentinel + redaction check | `ci-test`; release gate | forbidden body 不进 truth、log、event、audit、report | EV-CONV-REDACTION-001 |
| source truth isolation | resolver unresolved、digest mismatch、不补造来源 truth | fake resolver unresolved / mismatch | `integration-like`; `operations-replay` | 只写 marker;不生成 source body | EV-CONV-MAN-001 |
| transaction / idempotency | rollback、duplicate、same key conflict | failure injection + fixed idempotency key | `ci-test` | transaction rollback; same digest existing; different digest conflict | EV-CONV-FACT-001 |
| outbox / handoff recovery | publish / handoff failure, retry, rerun | fake publisher / handoff failure scripts | `ci-test`; nightly | truth 不回滚;outbox / handoff 进入 retry or failed | EV-CONV-OUTBOX-001; EV-CONV-HANDOFF-001 |
| observability / audit | 日志、指标、审计字段存在且安全 | scan generated artifacts / reports | `ci-test`; release gate | required refs present; forbidden fields absent | EV-CONV-GATE-001 |
| report path / evidence shape | artifact / report 层级错误 | path shape negative + report generation | release gate | 固定路径;无 `<project>` / `latest` | EV-CONV-CONFIG-001 |

redaction / boundary scan 失败、授权视野失效、source truth 被补造、query / projection / report 反写真相、fake adapter 被标记为 production success 均不得通过 P0。

## 11. 缺陷管理与复验规则

> 校准来源：
> - `design-calibration/05_test_plan_step_11_defects_retest.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_11_defects_retest.md` 的“缺陷分级表”“缺陷类型到回归范围表”“复验规则表”“关闭证据要求表”和“新增自动化防回归规则表”小节，了解 P0 缺陷如何分级、复验和关闭。

缺陷分为 S0 一票否决、S1 P0-blocking、S2 boundary / readiness 和 S3 非阻断四级。S0 和 S1 不允许风险接受。

| 级别 | 定义 | 示例 | 处理要求 | 是否阻断 |
|---|---|---|---|---|
| S0 一票否决 | 命中需求一票否决或 Step 10 红线,会破坏 truth / authorization / data ownership / traceability | 授权视野失效;相邻仓正文进入 truth / log / report;fact 可被覆盖;fake-as-production | 立即阻断 PR / main / release;必须修复;不得风险接受;必须新增或更新自动化防回归 | 是 |
| S1 P0-blocking | P0 用例、P0 suite 或核心非功能专项失败,但未直接命中 S0 | transaction rollback 失败;idempotency conflict 错误;path shape 错误;P0 suite timeout | 阻断对应 gate;修复后回归直接 TC、同组 TC 和相关 suite | 是 |
| S2 Boundary / readiness | P0/P1 boundary、nightly、operations-replay 或 P0-supporting 失败,不破坏 P0 红线 | staging-like smoke 失败;long history trend 退化;controlled adapter 接缝失败 | 可条件风险接受;必须记录影响、owner、目标修复时间和 P0 不受影响证据 | 视范围 |
| S3 非阻断 | 文档、报告呈现、非关键统计或后续专项缺口 | report 文案不清;P2 production-like 场景未覆盖 | 不阻断 P0;进入 backlog;不得掩盖 S0 / S1 | 否 |

复验规则:

- S0 修复必须重跑命中红线的直接 TC、release redline、redaction check 和相关 main CI suite。
- S1 修复必须重跑直接 TC、同场景组 TC、相关 P0 suite 和失败前同类 negative case。
- S2 修复必须重跑对应 nightly / operations-replay / staging smoke,并说明是否影响 P0。
- flaky、timeout、dependency failure 和 config failure 都必须保留 failure summary、复验 run 和 run-scoped report。
- 任何 S0 / S1 缺陷关闭前必须新增或更新自动化防回归,除非已有 P0 suite 能稳定复现并已补充断言。

## 12. 进入准则与退出准则

> 校准来源：
> - `design-calibration/05_test_plan_step_12_entry_exit_criteria.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_12_entry_exit_criteria.md` 的“进入准则”“退出准则”“进入 / 退出阻断矩阵”和“未满足准则时的处理”小节，了解 P0 测试执行何时可以开始、何时可以形成通过结论。

### 进入准则

- [ ] `05_test_plan_calibration_flow.md` 中 Step 1~14 均为 `[x]`,且没有未解决的 S0 / S1 设计冲突。
- [ ] `00-需求文档.md`、`01-架构设计.md`、`02-概要设计.md`、`03-详细设计.md`、`04-配置设计.md` 已冻结到同一 design baseline。
- [ ] `ci-test` profile 可启动,并使用 in-memory store、fixed clock、deterministic id generator、fake resolver、fake publisher、fake handoff。
- [ ] `DS-CONV-*` 数据集可由 seed / builder / fake script 自动生成,无需人工造数。
- [ ] `TestRunId` / `run_id` 生成规则可用,并能创建 `artifacts/test/<run_id>` 与 `reports/runs/<run_id>`。
- [ ] `scripts/gates/run_ci_gate.sh`、`scripts/reports/generate_reports.sh`、`scripts/checks/check_redaction.sh` 可运行。
- [ ] PR / main CI / release gate 的 P0-blocking suite 清单与 P0 用例矩阵一致。
- [ ] redaction policy、artifact path shape、report path shape 和 fake-as-production negative fixture 已准备。

### 退出准则

- [ ] 所有 P0-blocking 用例在同一测试基线下通过,并能关联到对应 suite result。
- [ ] `SUITE-CONV-MAIN-SERVICE`、`SUITE-CONV-MAIN-QUERY`、`SUITE-CONV-MAIN-WORKER-JOB`、`SUITE-CONV-MAIN-CONFIG` 全部通过。
- [ ] `SUITE-CONV-RELEASE-REDLINE`、`scripts/checks/check_redaction.sh` 和 `SUITE-CONV-RELEASE-REPORT` 全部通过。
- [ ] release readiness 所需 nightly / integration-like / operations-replay suite 已通过,或失败项被正式分级为 S2 并完成风险接受记录。
- [ ] 没有未关闭的 S0 / S1 缺陷;所有 S0 / S1 修复均完成直接 TC、同组 TC、相关 suite、redline 和必要 redaction 复验。
- [ ] 所有未关闭 S2 / S3 均有影响范围、owner、目标修复时间、临时规避和 P0 不受影响说明。
- [ ] `artifacts/test/<run_id>`、`reports/runs/<run_id>` 和 `reports/acceptance` 已生成,且不存在 `<project>` 层级或 `latest` 目录。
- [ ] generated reports、logs、audit materials、diagnostics 中未出现 forbidden body、runtime reasoning body、bridge platform body、raw secret、raw payload。

## 13. 测试报告与证据归档

> 校准来源：
> - `design-calibration/05_test_plan_step_13_reports_evidence.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_13_reports_evidence.md` 的“artifacts 目录结构”“reports 目录结构”“证据归档表”“报告生成映射表”和“evidence-index 最小字段”小节，了解 P0 测试证据如何从机器原始产物追溯到验收交接材料。

原始机器证据固定写入 `artifacts/test/<run_id>`。人类可读运行报告固定写入 `reports/runs/<run_id>`。验收交接报告固定写入 `reports/acceptance`。正式证据引用必须绑定固定 `<run_id>`,不得引用 `latest`,不得增加 `<project>` 层级。

```text
artifacts/test/<run_id>/
  meta/context.json
  meta/design-baseline.json
  evidence-index.json
  suites/<suite>/report.json
  suites/<suite>/stdout.log
  suites/<suite>/stderr.log
  suites/<suite>/failure-summary.md
  suites/<suite>/tc-results.json
  suites/<suite>/redaction-scan.json
  diagnostics/<suite>/*.json

reports/
  README.md
  runs/<run_id>/
    summary.md
    evidence-index.md
    gate-results.md
    redaction-check.md
    suites/<suite>.md
    evidence/EV-CONV-<TYPE>-<NNN>.md
  acceptance/
    handoff.md
    veto-checklist.md
    risk-acceptance.md
    open-issues.md
  review/
    reviewer-notes.md
    agent-review.md
```

| 证据 ID | 证据类型 | 来源 | 保存位置 | 关联用例 | 验收引用 |
|---|---|---|---|---|---|
| EV-CONV-TRUTH-001 | space / scope truth result | PR / main service suite | `reports/runs/<run_id>/evidence/EV-CONV-TRUTH-001.md` | TC-CONV-SPACE-*; TC-CONV-SCOPE-* | 06 待映射 |
| EV-CONV-FACT-001 | fact append / retract / idempotency result | main service suite | `reports/runs/<run_id>/evidence/EV-CONV-FACT-001.md` | TC-CONV-FACT-*; TC-CONV-TX-001 | 06 待映射 |
| EV-CONV-AUTH-001 | authorized query and search result | main query suite | `reports/runs/<run_id>/evidence/EV-CONV-AUTH-001.md` | TC-CONV-QUERY-*; TC-CONV-SEARCH-001 | 06 待映射 |
| EV-CONV-MAN-001 | manifestation and source isolation result | service / integration-like suite | `reports/runs/<run_id>/evidence/EV-CONV-MAN-001.md` | TC-CONV-MAN-* | 06 待映射 |
| EV-CONV-CONSUMER-001 | inbound consumer quarantine / ref-only result | worker suite | `reports/runs/<run_id>/evidence/EV-CONV-CONSUMER-001.md` | TC-CONV-CONSUMER-* | 06 待映射 |
| EV-CONV-HANDOFF-001 | trace / archive handoff result | service / job suite | `reports/runs/<run_id>/evidence/EV-CONV-HANDOFF-001.md` | TC-CONV-TRACE-001; TC-CONV-HANDOFF-* | 06 待映射 |
| EV-CONV-OUTBOX-001 | outbox publish / retry / rerun result | worker / job suite | `reports/runs/<run_id>/evidence/EV-CONV-OUTBOX-001.md` | TC-CONV-OUTBOX-* | 06 待映射 |
| EV-CONV-DERIVED-001 | projection / cursor / consistency result | job / operations-replay suite | `reports/runs/<run_id>/evidence/EV-CONV-DERIVED-001.md` | TC-CONV-DERIVED-*; TC-CONV-CURSOR-001; TC-CONV-CONSISTENCY-001 | 06 待映射 |
| EV-CONV-CONFIG-001 | config and path shape result | main config / release report suite | `reports/runs/<run_id>/evidence/EV-CONV-CONFIG-001.md` | TC-CONV-CONFIG-001; TC-CONV-REPORT-001 | 06 待映射 |
| EV-CONV-REDACTION-001 | redaction / boundary scan result | release redline + `check_redaction.sh` | `reports/runs/<run_id>/redaction-check.md` | TC-CONV-FACT-004; TC-CONV-CONSUMER-003; TC-CONV-REDACTION-001 | 06 待映射 |
| EV-CONV-GATE-001 | gate results and suite status | `run_ci_gate.sh` + report generator | `reports/runs/<run_id>/gate-results.md` | all P0-blocking TC | 06 待映射 |
| EV-CONV-ACCEPT-001 | acceptance handoff and veto checklist | report script + human / Agent review | `reports/acceptance/handoff.md`; `reports/acceptance/veto-checklist.md` | all P0 EV | 06 待映射 |

`scripts/reports/generate_reports.sh` 负责从 `artifacts/test/<run_id>` 生成报告初稿。`reports/acceptance/handoff.md`、`veto-checklist.md`、`risk-acceptance.md` 和 `open-issues.md` 必须由人或 Agent 审查补充后才能作为验收交接依据。

## 14. 回归策略与残余风险

> 校准来源：
> - `design-calibration/05_test_plan_step_14_regression_risks.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_14_regression_risks.md` 的“回归触发表”“回归集定义表”“残余风险表”“必须转入验收标准的风险 / 规则”和“风险接受门禁”小节，了解代码、配置、脚本、设计和缺陷修复如何触发回归，以及哪些风险可以进入验收交接。

| 变更类型 | 最小回归集 | 全量回归触发条件 | 责任人 |
|---|---|---|---|
| Space / scope domain or service | `TC-CONV-SPACE-*`; `TC-CONV-SCOPE-*`; `EV-CONV-TRUTH-001` | 修改状态机、visibility guard、outbox side effect、audit 字段 | domain owner + test owner |
| Fact append / retract / idempotency | `TC-CONV-FACT-*`; `TC-CONV-TX-001`; `EV-CONV-FACT-001` | 修改 transaction、idempotency、append-only、forbidden body guard | domain owner + service owner |
| Query / read model / search | `TC-CONV-QUERY-*`; `TC-CONV-SEARCH-001`; `EV-CONV-AUTH-001` | 修改 authorization、query no-write、projection marker、cursor semantic | query owner + test owner |
| Manifestation / source resolver | `TC-CONV-MAN-*`; `EV-CONV-MAN-001` | 修改 source truth isolation、safe snapshot、digest / unresolved 语义 | integration owner + domain owner |
| Inbound consumer | `TC-CONV-CONSUMER-*`; `EV-CONV-CONSUMER-001` | 修改 event envelope、quarantine、ref-only boundary、duplicate handling | worker owner + contract owner |
| Trace / archive handoff | `TC-CONV-TRACE-001`; `TC-CONV-HANDOFF-*`; `EV-CONV-HANDOFF-001` | 修改 handoff state、payload ref-only、redaction requirement、retry / failed | job owner + observability liaison |
| Outbox / event publish | `TC-CONV-OUTBOX-*`; `EV-CONV-OUTBOX-001` | 修改 event schema、event id、publish retry、state write recovery | worker owner + bus liaison |
| Projection / cursor / consistency | `TC-CONV-DERIVED-*`; `TC-CONV-CURSOR-001`; `TC-CONV-CONSISTENCY-001`; `EV-CONV-DERIVED-001` | 修改 no-auto-repair、cursor sequence、failed / stale marker | job owner + query owner |
| Config / scripts / reports | `TC-CONV-CONFIG-001`; `TC-CONV-REPORT-001`; `TC-CONV-REDACTION-001`; `EV-CONV-CONFIG-001`; `EV-CONV-REDACTION-001` | 修改 path shape、profile、redaction、report generator、evidence index | config owner + release owner |
| Public DTO / event schema / metadata | contract suite + affected scenario TC | 影响两个以上对象族或跨仓 contract | contract owner + affected owners |
| S0 / S1 defect fix | direct TC + same group TC + related suite + release redline | 缺陷根因跨对象族、红线、事务、授权或 redaction | defect owner + test owner |

残余风险只允许记录 P1/P2 或 S2/S3 风险。真实 DB / broker / resolver / handoff 产品行为、真实跨仓端到端、下游 UI / bridge 体验、runtime 推理质量、来源仓 truth lifecycle、生产级容量数字、config center / hot reload / auto repair 和 production-like 运维专项不构成 P0 空洞,但必须进入 `reports/acceptance/risk-acceptance.md` 或 `open-issues.md`。

| 风险 | 未覆盖原因 | 影响 | 缓解方式 | 接受人 |
|---|---|---|---|---|
| 真实 DB / broker / resolver / handoff 产品行为未验证 | P0 使用 in-memory / fake / controlled adapter | P0 通过不代表生产产品集成可用 | P1 integration / staging-like 专项 | integration owner + acceptance owner |
| 真实跨仓端到端联调未完成 | 上游 / 下游仓实现和部署不属于当前 P0 | 跨仓部署可能暴露接缝配置问题 | 后续以已收稳仓库为输入做 staging-like 测试 | project owner + affected repo owner |
| Chat UI / Workspace / Bridges 体验未覆盖 | 属于下游子项目测试范围 | 产品体验完整性不能由本仓 P0 证明 | 下游仓测试方案覆盖 | downstream owner |
| Runtime 推理质量和工具调用正确性未覆盖 | Conversation 只消费结果性事实 ref | 推理质量问题不会在本仓 P0 中裁决 | `L2-runtime` / tools 测试方案覆盖 | runtime owner |
| 来源仓 truth lifecycle 未覆盖 | 本仓不拥有来源 truth | 来源仓数据不一致会影响 manifestation 输入 | 本仓验证 unresolved / digest mismatch;来源仓自行验收 | source repo owner |
| 生产级吞吐、延迟、容量数字未锁定 | 需求未确认量化阈值 | 不能宣称达到生产容量目标 | 保留 baseline / trend evidence;06 若补阈值需追加专项 | performance owner + acceptance owner |

S0 / S1、redaction violation、授权失效、source truth isolation 失败和证据路径错误不得风险接受。

## 15. 参考

> 校准来源：
> - `design-calibration/05_test_plan_step_15_formal_document_assembly.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_15_formal_document_assembly.md` 的“正式文档装配检查”“自审结果”和“后续承接”小节，了解本文如何从 Step 1~14 中间产物整理为正式测试方案。

参考文档:

- `projects/L1-conversation/00-需求文档.md`
- `projects/L1-conversation/01-架构设计.md`
- `projects/L1-conversation/02-概要设计.md`
- `projects/L1-conversation/03-详细设计.md`
- `projects/L1-conversation/04-配置设计.md`
- `projects/L1-conversation/design-calibration/05_test_plan_calibration_flow.md`
- `standards/document/测试方案书写规范.md`
- `standards/document/测试方案讨论流程_SOP.md`
- `standards/document/设计文档讨论中间产物规范.md`
- `standards/document/子项目目录与代码文件组织规范.md`

后续 `06-验收标准.md` 必须消费本文的 P0 用例、EV 编号、reports / artifacts 路径、进入 / 退出准则、一票否决规则和残余风险表。

# L1-conversation 06-验收标准

> 本文遵循 `standards/document/验收标准书写规范.md`。
> 本文是验收裁决文档，不是测试执行报告。
> 本文回答：在什么条件下 `L1-conversation` 交付物算通过、算有条件通过、算不通过。

---

## 文档元信息

| 项 | 内容 |
|---|---|
| 子项目 | `L1-conversation` |
| 文档类型 | 验收标准 |
| 生成方式 | 按 `验收标准讨论流程_SOP.md` Step 1~15 重建 |
| 状态 | Draft / 待送验执行 |
| 日期 | 2026-06-02 |
| 当前说明 | 本文定义验收标准；真实通过结论必须等待固定 design commit、implementation commit、build、`<run_id>` 和 `reports/acceptance/*` |

正式验收结论只能使用 `通过`、`有条件通过`、`不通过`。缺少送验 commit、build、`<run_id>`、report 或 acceptance handoff 时，只能视为裁决前状态，不能写成通过。

---

## 1. 与上游文档的关系声明

> 校准来源：
> - `design-calibration/06_acceptance_step_01_input_boundary.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“验收输入映射表”“验收标准不再回答的问题”和“验收标准必须回答的问题”小节，了解本章验收输入边界如何从新版 `00~05` 收敛而来。

本验收标准承接新版 `00-需求文档.md`、`01-架构设计.md`、`02-概要设计.md`、`03-详细设计.md`、`04-配置设计.md` 和 `05-测试方案.md`。本文不重新定义需求、设计、配置或测试用例，只把这些文档中的 P0 能力、红线、证据和风险规则转成可裁决的 AC。

旧版 `06-验收标准.md` 只作为历史诊断输入。新版正式验收标准围绕 Conversation truth center、space / scope、fact append、authorized consumption、manifestation、handoff、outbox、operations jobs、configuration、reports / artifacts 生成裁决门禁。

| 来源文档 | 验收输入 | 本文如何裁决 |
|---|---|---|
| `00-需求文档.md` | 仓定位、FR / BR / NFR、一票否决方向 | 转成验收范围、功能门禁、非功能门禁和 veto |
| `01-架构设计.md` | 职责边界、依赖方向、数据所有权、架构硬约束 | 转成数据边界、架构红线和跨仓接缝 AC |
| `02-概要设计.md` | 主要组成部分、关键对象、接口骨架、状态和异常边界 | 转成验收范围分组和功能 AC 主语 |
| `03-详细设计.md` | 对象、协议、状态、处理流、事务、幂等、错误、观测 | 转成 AC 设计契约、正式字段 / 状态和失败条件 |
| `04-配置设计.md` | profile、配置项、失效模式、reports / artifacts、redaction | 转成配置、环境、path shape 和 redaction 证据门禁 |
| `05-测试方案.md` | TC、EV、gate、entry / exit、reports / artifacts、缺陷和风险规则 | 转成证据来源、进入 / 退出条件、风险接受和放行规则 |

本文不回答测试执行命令、fixture 生成细节、phase / commit boundary、开发排期、部署步骤或运维 runbook。这些内容分别归属 `05-测试方案.md`、`07-实施计划.md` 和部署与运维文档。

## 2. 验收目标与范围

> 校准来源：
> - `design-calibration/06_acceptance_step_02_scope.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“验收目标”“验收范围表”“下游能力接缝裁决表”“非范围及最终结论影响表”和“一票否决候选范围”小节，了解本章验收范围如何从新版 `00~05` 收敛而来。

本轮验收的核心目标是判断 `L1-conversation` 是否作为 Conversation truth center 成立。P0 必须裁决 space / scope、fact append、authorized consumption、cross-domain manifestation、trace / handoff、outbox / events、operations jobs、configuration / reports / redaction。P1 只裁决 readiness 或 controlled seam 风险；P2 不在本轮裁决范围内。

| 验收范围项 | 类型 | 优先级 | 裁决目标 | 非范围 / 说明 |
|---|---|---|---|---|
| Conversation space / scope | 功能 / 状态 / 授权 | P0 | space、participant scope、visibility scope 可显式建立、更新、关闭 | 不裁决 Identity 生命周期 |
| Conversation fact append | 功能 / 事务 / 幂等 | P0 | fact append-only、receipt、trace、outbox 同事务成立 | 不裁决 Runtime 推理质量 |
| Authorized query / projection | 功能 / 数据边界 | P0 | authorized read、query no-write、stale / failed marker 和 refs-only search 成立 | 不裁决 Chat UI / Workspace 展示体验 |
| Cross-domain manifestation | 功能 / 数据所有权 | P0 | external fact 只以 ref、safe snapshot、manifestation 或 marker 进入 | 不裁决来源仓 truth lifecycle |
| Trace / review / handoff | 功能 / 可追溯 / 恢复 | P0 | review anchor、trace handoff、archive handoff 可追溯且不泄露正文 | 不裁决全局 trace store 或 archive package body |
| Outbox / event collaboration | 事件协作 | P0 | outbound event、publish retry / failed、duplicate prevention 和 replay evidence 成立 | 不裁决真实 broker 产品行为 |
| Operations jobs | 维护 / 一致性 / 诊断 | P0 / P0-supporting | projection、search、cursor、snapshot refresh、consistency validation 可重跑且不自动修写真相 | 不裁决 auto repair |
| Configuration / reports / redaction | 配置 / 证据 / 安全 | P0 | profile、path shape、redaction lower bound、fake-as-production reject 和 reports / artifacts 成立 | 不裁决 production runbook |

真实生产外部服务、真实跨仓端到端、Chat UI / Workspace / Bridges 体验、Runtime 推理质量、来源仓 truth lifecycle、生产级容量数字、config center / hot reload / auto repair 不阻塞 P0 通过，但必须进入风险接受或后续专项。正式 AC 必须使用详细设计和测试方案中的正式对象、协议、状态、错误、TC、EV 和 report 路径。

## 3. 验收基线

> 校准来源：
> - `design-calibration/06_acceptance_step_03_baseline.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“验收基线表”“证据入口基线表”“基线冻结关系图”和“基线变更处理表”小节，了解本章如何固定设计、交付、环境、数据、artifact、report 和 acceptance handoff 基线。

本轮验收必须固定设计基线、实现交付基线、环境配置基线、测试数据基线和证据基线。设计基线必须是包含 `00~06` 的 design repo commit；实现基线必须是 `/home/aris/Projects/quantalithos-conversation` 的具体 commit hash、build id / artifact digest 和必要时的 image digest。

| 基线类型 | 基线内容 | 版本 / 标识 |
|---|---|---|
| 需求基线 | `00-需求文档.md` | `待固定 design commit` |
| 架构基线 | `01-架构设计.md` | `待固定 design commit` |
| 概要设计基线 | `02-概要设计.md` | `待固定 design commit` |
| 详细设计基线 | `03-详细设计.md` | `待固定 design commit` |
| 配置设计基线 | `04-配置设计.md` | `待固定 design commit` |
| 测试方案基线 | `05-测试方案.md` | `待固定 design commit` |
| 验收标准基线 | `06-验收标准.md` | 本文固定后提交 |
| 实现交付基线 | `/home/aris/Projects/quantalithos-conversation` | `待送验固定` |
| 环境基线 | `ci-test`; `integration-like`; `operations-replay` | `待送验固定` |
| 数据基线 | `DS-CONV-*`; `TestRunId`; deterministic fixture | `待送验固定` |

机器原始证据固定为 `artifacts/test/<run_id>`，人类可读报告固定为 `reports/runs/<run_id>`，验收交接固定为 `reports/acceptance`。正式验收不得引用 `latest`，不得使用 `reports/<project>` 或 `artifacts/test/<project>/<run_id>`。

当前若尚未产生送验 commit、build、`<run_id>` 或 `reports/acceptance/*`，只能完成验收标准定义，不能给出最终“通过”或“有条件通过”结论。

## 4. 进入条件与退出条件

> 校准来源：
> - `design-calibration/06_acceptance_step_04_entry_exit.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“验收进入条件”“验收退出条件”“进入 / 退出阻断矩阵”和“结论判定表”小节，了解本章如何把基线、测试证据、缺陷分级和风险接受转成正式验收门禁。

正式验收审查开始前，必须固定 design repo commit、implementation repo commit、build id / artifact digest、`run_id`、profile、fixture seed、artifact root、report root 和 acceptance review version。

| 进入条件 | 判定 |
|---|---|
| design repo commit 已固定，且包含新版 `00~06` | 必须满足 |
| implementation repo commit、build id / artifact digest 和必要 image digest 已固定 | 必须满足 |
| `run_id` 已固定，并与 design / implementation / profile / fixture 对齐 | 必须满足 |
| `artifacts/test/<run_id>` 已生成，无 `<project>` 层级或 `latest` | 必须满足 |
| `reports/runs/<run_id>` 已生成，含 summary、evidence-index、gate-results、redaction-check 和 EV 证据页 | 必须满足 |
| `reports/acceptance/handoff.md` 和 `veto-checklist.md` 已生成并审查 | 必须满足 |
| 如存在 S2 / S3 或 P1 / P2 遗留，`risk-acceptance.md` 或 `open-issues.md` 已生成 | 条件满足 |

退出验收必须形成 `通过`、`有条件通过` 或 `不通过` 三类结论之一。通过要求所有 P0 AC、P0-blocking suite、release redline、redaction、path shape、veto checklist 和 acceptance handoff 均通过，且无未关闭 S0 / S1。有条件通过只允许剩余已接受的 S2 / S3 或 P1 / P2 风险。任一 S0 / S1、redaction violation、授权失效、source truth isolation 失败、append-only 破坏、path shape 错误、fake-as-production 或证据不可审计均不得通过。

## 5. 功能验收门禁

> 校准来源：
> - `design-calibration/06_acceptance_step_05_function_gate.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“功能验收门禁表”“功能门禁到需求追溯表”“P1 / P2 后置边界表”和“功能失败对最终结论的影响”小节，了解 `FR-CONV-001~008` 如何转换为可裁决功能 AC。

本轮功能验收以 `AC-FUNC-001~AC-FUNC-008` 为裁决入口，对应 `FR-CONV-001~FR-CONV-008`。`AC-FUNC-001~AC-FUNC-005` 是 Conversation truth center 核心闭环，任一失败即不通过。`AC-FUNC-006~AC-FUNC-008` 是外围增强与变化感知的最小切口，如果破坏 truth、authorization、derived read-only、outbox idempotency 或 evidence path，同样不通过；如果仅为 P1/P2 外围体验不足，必须进入风险接受。

| 验收项 ID | 功能 / 场景 | 通过条件 | 失败条件 | 证据来源 |
|---|---|---|---|---|
| AC-FUNC-001 | 对话空间与参与范围能力 | space、participant scope、visibility scope 可建立和维护，合法更新生成 scope change 与 outbox | space / scope 缺失，sealed visibility 扩张成功，关闭后重开成功 | `TC-CONV-SPACE-*`;`TC-CONV-SCOPE-*`;`EV-CONV-TRUTH-001` |
| AC-FUNC-002 | 协作事实追加沉淀能力 | append-only fact history、receipt、trace context、outbox 和幂等语义成立 | fact 被覆盖，缺幂等键仍写入，同 key 不同 digest 不冲突 | `TC-CONV-FACT-*`;`TC-CONV-TX-001`;`EV-CONV-FACT-001` |
| AC-FUNC-003 | 授权视野消费能力 | Query 只返回 visibility 允许的 fact / marker，且 query no-write | 未授权读到隐藏 fact，query 追加或修写真相 | `TC-CONV-QUERY-*`;`EV-CONV-AUTH-001` |
| AC-FUNC-004 | 跨域事实引用显化能力 | 外部事实只以 ref、safe snapshot、manifestation、unresolved 或 mismatch marker 承接 | 来源不可解析仍生成来源正文，digest mismatch 覆盖旧 truth | `TC-CONV-MAN-*`;`TC-CONV-CONSUMER-*`;`EV-CONV-MAN-001`;`EV-CONV-CONSUMER-001` |
| AC-FUNC-005 | 对话历史追溯与复盘能力 | review anchor、trace handoff、archive handoff 可追溯，失败进入 retry / failed | handoff 保存正文，handoff 失败回滚 fact，archive success 保存 package body | `TC-CONV-TRACE-001`;`TC-CONV-HANDOFF-*`;`EV-CONV-HANDOFF-001` |
| AC-FUNC-006 | 对话索引 / 投影维护能力 | 派生结果只从既有 truth 重建，失败暴露 stale / failed / issue marker | rebuild 生成新业务 fact，consistency job 自动修写真相 | `TC-CONV-DERIVED-*`;`TC-CONV-CONSISTENCY-001`;`EV-CONV-DERIVED-001` |
| AC-FUNC-007 | 长历史检索与定位能力 | search 只返回授权 refs / fragments，cursor 只前进且不可恢复时明确失败 | search 返回 payload body，未授权结果可见，cursor sequence regression | `TC-CONV-SEARCH-001`;`TC-CONV-CURSOR-001`;`EV-CONV-AUTH-001`;`EV-CONV-DERIVED-001` |
| AC-FUNC-008 | 对话变化及时感知能力 | outbox、change event、poll changes 可基于 ref / marker 感知变化，publish retry / rerun 不重复 | outbox publish duplicate，publish failure 改写 truth，change event 含正文 | `TC-CONV-OUTBOX-*`;`TC-CONV-QUERY-004`;`EV-CONV-OUTBOX-001`;`EV-CONV-AUTH-001` |

所有功能门禁的正式裁决必须同时引用 `reports/runs/<run_id>/evidence-index.md` 和 `EV-CONV-GATE-001`。如果对应 EV 页面缺失，该 AC 不得判定为通过。

## 6. 数据边界与架构红线验收

> 校准来源：
> - `design-calibration/06_acceptance_step_06_data_architecture_redlines.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“数据边界与架构红线表”“数据类别裁决表”和“红线失败对最终结论的影响”小节，了解本章如何把数据所有权、架构边界和禁止事项转成验收红线。

`L1-conversation` 只拥有对话空间、参与范围、可见范围、对话事实历史、对话内跨域显化记录和对话追溯上下文。成员、项目、治理、产物、外部平台、runtime、observability 和 archive 等相邻对象只能以 ref、safe snapshot、manifestation、handoff ref 或 marker 进入本仓。

| 红线 ID | 红线 | 失败条件 | 证据来源 |
|---|---|---|---|
| AC-RED-001 | Conversation 只拥有本仓 truth 数据 | 外部对象正文或生命周期真相进入 Conversation truth | `TC-CONV-FACT-004`;`TC-CONV-MAN-*`;`EV-CONV-MAN-001`;`EV-CONV-REDACTION-001` |
| AC-RED-002 | forbidden body 不得进入 truth / log / event / report | runtime reasoning body、bridge platform body、artifact body、source body 或 raw payload 泄漏 | `TC-CONV-FACT-004`;`TC-CONV-CONSUMER-003`;`EV-CONV-REDACTION-001` |
| AC-RED-003 | raw secret / credential 不得进入配置和证据 | raw secret、raw token、private key 或 raw credential 出现在 config、artifact、report 或 handoff | `TC-CONV-CONFIG-001`;`TC-CONV-REDACTION-001`;`EV-CONV-CONFIG-001`;`EV-CONV-REDACTION-001` |
| AC-RED-004 | 授权视野不可被绕过 | 未授权 actor / consumer 读取隐藏 fact，sealed visibility 被扩张 | `TC-CONV-SCOPE-002`;`TC-CONV-QUERY-*`;`EV-CONV-AUTH-001` |
| AC-RED-005 | source truth isolation 不得被破坏 | resolver unresolved 时补造来源正文，digest mismatch 覆盖 truth | `TC-CONV-MAN-*`;`TC-CONV-CONSUMER-*`;`EV-CONV-MAN-001`;`EV-CONV-CONSUMER-001` |
| AC-RED-006 | 下游消费不得反写 Conversation truth | 下游 UI、projection、runtime context 或 external platform state 覆盖本仓 truth | `TC-CONV-QUERY-*`;`TC-CONV-OUTBOX-*`;`EV-CONV-AUTH-001`;`EV-CONV-OUTBOX-001` |
| AC-RED-007 | projection / search / cursor / report 不得反写真相 | query、projection、report 追加业务 fact、修复 truth 或把 stale 当 fresh | `TC-CONV-DERIVED-*`;`TC-CONV-CURSOR-001`;`EV-CONV-DERIVED-001` |
| AC-RED-008 | 配置不得绕过架构红线 | config 允许 non-strict redaction、raw secret、forbidden body、auto repair truth 或 path shape 错误 | `TC-CONV-CONFIG-001`;`TC-CONV-REPORT-001`;`EV-CONV-CONFIG-001` |
| AC-RED-009 | P1 / P2 能力不得污染 P0 主链 | P1/P2 能力成为 P0 必填配置、P0 DTO 字段、P0 gate 或 P0 通过条件 | scope review;`TC-CONV-CONFIG-001`;`EV-CONV-ACCEPT-001` |
| AC-RED-010 | fake / controlled adapter 不得伪装 production success | fake 被标记为真实生产集成通过，fake failure 被吞掉 | `TC-CONV-CONFIG-001`;`TC-CONV-MAN-002`;`EV-CONV-CONFIG-001`;`EV-CONV-MAN-001` |

任何 forbidden body、raw secret、source truth 补造、授权绕过、下游反写、projection 反写、配置绕过红线、fake-as-production 或 P1/P2 污染 P0 主链，均不得判定通过。核心红线由 §11 汇总为一票否决项。

## 7. 接口、事件与跨仓同步验收

> 校准来源：
> - `design-calibration/06_acceptance_step_07_interface_event_sync.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“接口、事件与跨仓同步验收表”“协议覆盖清单”“跨仓依赖类型与验收方式映射表”和“接口失败对最终结论的影响”小节，了解 Command、Query、Consumer、Event、Job 和跨仓接缝如何形成验收门禁。

本轮接口、事件与跨仓同步验收以 `AC-SYNC-001~AC-SYNC-010` 为裁决入口。Command、Query、Inbound Consumer、Outbound Event 和 Operations Job 必须使用 `03-详细设计.md` §7 / §8 的正式协议名和 flow。每项验收必须绑定 `TC-CONV-*`、`EV-CONV-*` 和 `reports/runs/<run_id>/evidence-index.md`。

| 验收项 ID | 接口 / 事件 / 下游 | 通过条件 | 失败条件 | 证据来源 |
|---|---|---|---|---|
| AC-SYNC-001 | 10 个 Command API | required fields、metadata、actor、idempotency、result / receipt / error 闭合 | Command 缺失、字段漂移、缺 idempotency 仍写 truth | `TC-CONV-SPACE-*`;`TC-CONV-FACT-*`;`EV-CONV-TRUTH-001`;`EV-CONV-FACT-001` |
| AC-SYNC-002 | 11 个 Query API | request、consumer、page、consistency、visibility 和 stale / failed marker 闭合 | Query 写 truth，未授权读到 hidden fact，search 返回 payload body | `TC-CONV-QUERY-*`;`TC-CONV-SEARCH-001`;`EV-CONV-AUTH-001` |
| AC-SYNC-003 | 6 个 Inbound Event Consumer | envelope、event id、source ref、idempotency key、payload ref 闭合，duplicate 不重复 truth | invalid envelope 写 truth，source body / platform body 入库 | `TC-CONV-CONSUMER-*`;`EV-CONV-CONSUMER-001`;`EV-CONV-REDACTION-001` |
| AC-SYNC-004 | 9 个 Outbound Event / Outbox | event 只来自已提交 truth、projection state 或 handoff intent，payload ref / marker-only | 未提交 truth 发布，event 含正文，publish failure 回滚 truth | `TC-CONV-OUTBOX-*`;`EV-CONV-OUTBOX-001` |
| AC-SYNC-005 | 9 个 Operations Job | job_run_id、scope、metadata、idempotency、receipt / report ref 和 failed marker 闭合 | job 无 receipt，partial failure 污染成功项，rebuild 自动修 truth | `TC-CONV-DERIVED-*`;`TC-CONV-CURSOR-001`;`EV-CONV-DERIVED-001` |
| AC-SYNC-006 | `L0-core` 共享契约 | shared ID、ActorRef、TraceContext、Error、metadata 可编译和 roundtrip | 本仓重定义 L0-core shared contract 或 compile 失败 | `contracts_dto_roundtrip`;`EV-CONV-GATE-001` |
| AC-SYNC-007 | `L0-bus` 事件协作接缝 | outbox 可读，publish retry / failed / published 可审计，真实 bus 未就绪不阻塞 P0 | 要求真实 broker 才能 P0，fake publish 被标 production success | `TC-CONV-OUTBOX-*`;`EV-CONV-OUTBOX-001`;`EV-CONV-ACCEPT-001` |
| AC-SYNC-008 | 来源仓输入接缝 | actor、project、governance、artifact、runtime result、bridge mapped fact 只以 ref / safe snapshot / marker 进入 | 来源正文入库，runtime reasoning body 或 bridge platform body 保存 | `TC-CONV-MAN-*`;`TC-CONV-CONSUMER-*`;`EV-CONV-MAN-001`;`EV-CONV-REDACTION-001` |
| AC-SYNC-009 | 下游消费与交接 | 下游只能消费 authorized read model、event ref、cursor、trace / archive handoff ref | 下游 UI / runtime / archive state 反写 Conversation truth | `TC-CONV-QUERY-*`;`TC-CONV-HANDOFF-*`;`EV-CONV-AUTH-001`;`EV-CONV-HANDOFF-001` |
| AC-SYNC-010 | 下游未就绪与 controlled seam | fake / controlled seam 有 marker，未就绪真实下游进入 risk 或 open issues | fake success 伪装真实集成通过，未就绪风险未记录 | `TC-CONV-CONFIG-001`;`EV-CONV-CONFIG-001`;`EV-CONV-ACCEPT-001` |

跨仓验收必须区分编译期依赖、运行期依赖和事件协作依赖。`L0-core` 用 package dependency / contract compile 证明；`L0-bus` 用 outbox / publisher / replay 证明；来源仓和下游仓用 resolver、controlled adapter、fake consumer、safe snapshot、marker、handoff ref 和风险接受证明。不得要求下游仓完整实现，也不得把 fake success 宣称为 production-like success。

## 8. 状态机、事务与一致性验收

> 校准来源：
> - `design-calibration/06_acceptance_step_08_state_tx_consistency.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“状态机、事务与一致性验收表”“状态族覆盖清单”“事务与一致性裁决图”和“失败对最终结论的影响”小节，了解状态迁移、事务原子性、幂等、并发和失败恢复如何进入验收裁决。

本轮状态机、事务与一致性验收以 `AC-STATE-001~AC-STATE-005`、`AC-TX-001~AC-TX-003`、`AC-CONS-001~AC-CONS-002` 和 `AC-IDEM-001~AC-IDEM-002` 为裁决入口。所有状态名必须使用 `03-详细设计.md` §9 与 `design-calibration/03_ddd_step_10_state_matrix.md` 的正式 enum variant。

| 验收项 ID | 主题 | 通过条件 | 失败条件 | 证据来源 |
|---|---|---|---|---|
| AC-STATE-001 | truth / space / scope 状态机 | 合法创建、收紧、关闭和 supersede 迁移通过 | `Closed -> Open` 成功，sealed visibility 被打开 | `TC-CONV-SPACE-*`;`TC-CONV-SCOPE-*`;`EV-CONV-TRUTH-001` |
| AC-STATE-002 | fact / manifestation / reference 状态机 | fact append / retract、duplicate receipt、manifested / unresolved / stale / invalid reference 按正式状态推进 | duplicate 生成新 fact，unresolved 时补造来源 truth | `TC-CONV-FACT-*`;`TC-CONV-MAN-*`;`EV-CONV-FACT-001`;`EV-CONV-MAN-001` |
| AC-STATE-003 | projection / cursor 状态机 | projection 可进入 Fresh / Stale / Rebuilding / Failed / Disabled，cursor 只前进 | rebuild failure 被写成 fresh，cursor 倒退 | `TC-CONV-DERIVED-*`;`TC-CONV-CURSOR-001`;`EV-CONV-DERIVED-001` |
| AC-STATE-004 | outbox / trace / archive handoff 状态机 | outbox、trace handoff、archive handoff 可进入 published / retry / failed / archived 等正式状态 | terminal state 被再次推进，publish failure 回滚 truth | `TC-CONV-OUTBOX-*`;`TC-CONV-HANDOFF-*`;`EV-CONV-OUTBOX-001`;`EV-CONV-HANDOFF-001` |
| AC-STATE-005 | 正式状态命名 | 报告、测试断言和 evidence 使用正式 enum variant | 使用旧 Turn / StreamEvents 主语或无法映射的口语状态 | `domain_state_transitions`;`reports/runs/<run_id>/evidence-index.md` |
| AC-TX-001 | command / consumer truth 事务原子性 | idempotency、business write、trace / receipt、outbox 和 complete 在同一 UnitOfWork 闭合 | 事务部分提交或 outbox enqueue 失败后 truth 仍存在 | `TC-CONV-TX-001`;`EV-CONV-FACT-001`;`EV-CONV-CONSUMER-001` |
| AC-TX-002 | job 短事务与外部副作用边界 | publish、rebuild、cursor、handoff delivery 按短事务推进状态 | 长事务包住外部调用，rerun 重复外部副作用 | `TC-CONV-OUTBOX-*`;`TC-CONV-DERIVED-*`;`EV-CONV-OUTBOX-001` |
| AC-TX-003 | Query no-write | Query 只读 truth / projection / visibility，可返回 marker | query 写 idempotency、cursor、projection 或 truth | `TC-CONV-QUERY-*`;`TC-CONV-CONSISTENCY-001`;`EV-CONV-AUTH-001` |
| AC-CONS-001 | sequence 与 source position 单调 | fact append sequence、outbox sequence、projection source position 和 cursor position 只前进 | fact sequence 重复或倒退，projection source position regression | `TC-CONV-FACT-001`;`TC-CONV-CURSOR-001`;`EV-CONV-FACT-001`;`EV-CONV-DERIVED-001` |
| AC-IDEM-001 | command 幂等 duplicate / conflict | 缺幂等键拒绝，同 key 同 digest existing，同 key 不同 digest conflict | 缺 key 仍写 truth，duplicate 追加新 fact / outbox | `TC-CONV-FACT-002`;`TC-CONV-FACT-003`;`EV-CONV-FACT-001` |
| AC-IDEM-002 | event / outbox / job 幂等和重跑 | inbound duplicate skip，outbox rerun 使用 stable event id，job rerun existing receipt | duplicate event 写新 truth，publish rerun 生成重复 downstream record | `TC-CONV-CONSUMER-*`;`TC-CONV-OUTBOX-003`;`EV-CONV-CONSUMER-001`;`EV-CONV-OUTBOX-001` |
| AC-CONS-002 | 失败恢复一致性 | 失败只写 retry、failed、unresolved、stale、quarantine 或 issue marker | 外部失败回滚 fact，resolver 失败补造 source body，consistency 自动修 truth | `TC-CONV-MAN-002`;`TC-CONV-DERIVED-002`;`TC-CONV-CONSISTENCY-001`;`EV-CONV-MAN-001`;`EV-CONV-DERIVED-001` |

Command 和 inbound consumer 写 truth 时，必须在同一 UnitOfWork 中完成 idempotency、business write、trace / receipt、outbox enqueue 和 idempotency complete。Query API 不得写 truth、projection、cursor 或 idempotency state。outbox publish、projection rebuild、cursor maintenance、trace / archive handoff delivery 只能通过短事务推进传播、派生或交接状态，外部失败不得回滚已提交 truth。

## 9. 非功能验收门禁

> 校准来源：
> - `design-calibration/06_acceptance_step_09_nonfunctional_gate.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“非功能验收表”“NFR 到验收项映射表”“阈值与缺口处理表”和“非功能门禁裁决图”小节，了解性能 baseline、可用性降级、安全红线、幂等一致性、配置 fail-fast 和证据路径如何进入验收裁决。

本轮非功能验收以 `AC-NFR-001~AC-NFR-012` 为裁决入口。当前不写未确认的 p95、TPS、连接数、月度事实容量或重建窗口数字；性能验收只证明核心追加、授权读取和追溯在长历史、相邻依赖不可用或外围增强故障下仍能成立。

| 验收项 ID | 维度 | 通过条件 | 失败影响 | 证据来源 |
|---|---|---|---|---|
| AC-NFR-001 | 性能 baseline | 核心追加、授权读取和追溯在长历史 fixture 与 controlled dependency failure 下不阻塞 | 核心 truth 阻塞则不通过；量化数字缺失只进风险 | `EV-CONV-FACT-001`;`EV-CONV-AUTH-001` |
| AC-NFR-002 | 外围增强降级 | projection、search、cursor、report failure 不阻断核心 truth | 核心被阻断则不通过；外围 degraded 可风险接受 | `EV-CONV-DERIVED-001` |
| AC-NFR-003 | 下游降级 | 下游不可用不得丢失或改写已提交 truth | 下游失败回滚或改写 truth 则不通过 | `EV-CONV-OUTBOX-001`;`EV-CONV-HANDOFF-001` |
| AC-NFR-004 | 来源不可用安全降级 | 来源不可解析时只保存 ref / safe snapshot / unresolved / mismatch marker | 补造 source truth 则一票否决候选 | `EV-CONV-MAN-001`;`EV-CONV-CONSUMER-001` |
| AC-NFR-005 | 授权视野安全 | 任一消费路径只返回授权范围内 fact、snapshot、ref、cursor 或 marker | 授权失效不得风险接受 | `EV-CONV-AUTH-001` |
| AC-NFR-006 | 数据归属 / redaction | forbidden body、raw secret、raw token 不得进入 truth、event、log、audit、report | 命中即不通过 | `EV-CONV-REDACTION-001` |
| AC-NFR-007 | 审计 / 追溯底线 | 关键变化可定位 actor / source / time / state / evidence ref | 关键变化不可追溯则不通过 | `EV-CONV-TRUTH-001`;`EV-CONV-GATE-001` |
| AC-NFR-008 | 幂等 / 一致性 | duplicate / rerun 不形成冲突 truth 或重复副作用 | 冲突 truth 或重复外部副作用则不通过 | `EV-CONV-FACT-001`;`EV-CONV-OUTBOX-001` |
| AC-NFR-009 | 配置 fail-fast / fail-closed | unsupported profile、重复 key、raw secret、non-strict redaction、fake-as-production fail-fast | silent fallback 或 fake-as-production 不允许风险接受 | `EV-CONV-CONFIG-001`;`EV-CONV-REDACTION-001` |
| AC-NFR-010 | 证据路径兼容性 | 输出路径为 `artifacts/test/<run_id>`、`reports/runs/<run_id>`、`reports/acceptance` | 路径错误导致送验不成立 | `EV-CONV-GATE-001`;`EV-CONV-ACCEPT-001` |
| AC-NFR-011 | 可观测性风险可见 | 核心成功 / 异常、依赖降级、边界违规、重复冲突、job partial failure 可观察 | 完全不可观察则不通过 | `reports/runs/<run_id>/gate-results.md` |
| AC-NFR-012 | P1 / P2 缺口隔离 | production-like、真实外部服务、下游体验和容量数字缺口进入风险记录 | 伪装 production-like 或风险未记录则不通过 | `reports/acceptance/risk-acceptance.md`;`EV-CONV-ACCEPT-001` |

授权视野失效、forbidden body / raw secret 泄漏、source truth 被补造、query / projection / report 反写真相、duplicate 形成冲突 truth、配置 silent fallback、fake-as-production 和证据路径错误不得风险接受。

## 10. 可观测性、审计与证据门禁

> 校准来源：
> - `design-calibration/06_acceptance_step_10_observability_evidence.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“证据门禁表”“Report 完整性检查表”“Acceptance Handoff 检查表”“证据复查链路图”和“EV 覆盖清单”小节，了解日志、指标、审计、EV、报告和验收交接如何支撑最终裁决。

本轮可观测性、审计与证据验收以 `AC-OBS-001`、`AC-AUDIT-001~002` 和 `AC-EVID-001~008` 为裁决入口。P0 evidence 必须优先从 `reports/runs/<run_id>` 读取，再回指 `artifacts/test/<run_id>`。正式证据路径固定为 `artifacts/test/<run_id>`、`reports/runs/<run_id>` 和 `reports/acceptance`，不得使用 `latest` 或 `<project>` 层级。

| 验收项 ID | 证据主题 | 通过条件 | 失败条件 |
|---|---|---|---|
| AC-OBS-001 | safe log / metric / trace | command、query、consumer、outbox、resolver、projection、handoff、job、config、boundary 有 safe log / metric / trace ref | 核心能力不可观察或日志含正文 / secret |
| AC-AUDIT-001 | truth / scope / fact / manifestation audit | 能复盘 actor、subject、source、from / to state、result、reason 和 time | 关键变化无 audit / evidence ref |
| AC-AUDIT-002 | outbox / projection / handoff / job audit | publish、handoff、rebuild、validation 结果可复查 | 传播、派生、交接或 job 失败无 evidence |
| AC-EVID-001 | EV 索引 | `reports/runs/<run_id>/evidence-index.md` 覆盖全部 P0 EV | P0 EV 缺失或索引路径断裂 |
| AC-EVID-002 | Gate 结果 | `gate-results.md` 覆盖 P0-blocking suite 和 release gate | P0 suite 缺结果或失败无原因 |
| AC-EVID-003 | Redaction / boundary scan | `redaction-check.md` 证明 artifact 与 report 无 raw secret、raw body、forbidden body | 命中 raw secret / body 或报告复制敏感原文 |
| AC-EVID-004 | Suite / failure evidence | 成功和失败 suite 都有 report、stdout / stderr、tc-results、failure summary | 失败只有 CI 红叉 |
| AC-EVID-005 | Acceptance handoff | `reports/acceptance/handoff.md` 已审查并说明 baseline、scope、run id、commit / build | 自动初稿未审查或缺 baseline |
| AC-EVID-006 | Veto checklist | `reports/acceptance/veto-checklist.md` 覆盖全部 `VETO-CONV-*` | veto 项未覆盖或无结论 |
| AC-EVID-007 | Risk acceptance | 有条件通过时每项风险有 owner、影响、接受理由、动作和截止时间 | 用风险接受覆盖 S0 / S1 或风险缺 owner / deadline |
| AC-EVID-008 | Open issues / review notes | 未关闭问题分级清楚，review 不改写机器证据 | S0 / S1 未关闭仍送验或 review 改写原始结果 |

`reports/acceptance/handoff.md`、`veto-checklist.md`、`risk-acceptance.md` 和 `open-issues.md` 可以由脚本生成初稿，但必须由人或 Agent 审查补充后才能作为验收交接依据。

## 11. 一票否决项

> 校准来源：
> - `design-calibration/06_acceptance_step_11_veto_items.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“一票否决项表”“否决项到前序门禁映射表”“风险接受边界表”和“否决检查流程图”小节，了解哪些失败不可被风险接受覆盖。

本轮一票否决项以 `VETO-CONV-001~014` 为准。任一否决项命中时，最终结论只能是不通过，不得进入风险接受。`reports/acceptance/veto-checklist.md` 必须覆盖全部 `VETO-CONV-*`，并回指 `reports/runs/<run_id>/evidence-index.md`、`gate-results.md`、`redaction-check.md`、EV 明细和 suite / artifact 证据。

| 否决项 ID | 否决项 | 主要证据 |
|---|---|---|
| VETO-CONV-001 | 核心能力闭环任一必要节点缺失 | `AC-FUNC-*`;`EV-CONV-TRUTH-001`;`EV-CONV-FACT-001`;`EV-CONV-AUTH-001`;`EV-CONV-MAN-001`;`EV-CONV-HANDOFF-001` |
| VETO-CONV-002 | Conversation 退化为消息缓存、UI 副本或 runtime memory | `AC-RED-001`;`AC-FUNC-001`;`veto-checklist.md` |
| VETO-CONV-003 | Conversation truth 被覆盖、抹除、反写或自动修复 | `AC-RED-006`;`AC-RED-007`;`AC-CONS-002`;`EV-CONV-DERIVED-001` |
| VETO-CONV-004 | 授权视野失效 | `AC-RED-004`;`AC-NFR-005`;`EV-CONV-AUTH-001` |
| VETO-CONV-005 | 相邻仓正文、forbidden body 或 raw payload 进入 truth / event / log / audit / report | `AC-RED-002`;`AC-NFR-006`;`EV-CONV-REDACTION-001` |
| VETO-CONV-006 | raw secret、raw token、private key 或 raw credential 进入配置、artifact、report 或 handoff | `AC-RED-003`;`AC-NFR-006`;`EV-CONV-CONFIG-001`;`EV-CONV-REDACTION-001` |
| VETO-CONV-007 | source truth isolation 被破坏 | `AC-RED-005`;`AC-NFR-004`;`EV-CONV-MAN-001`;`EV-CONV-CONSUMER-001` |
| VETO-CONV-008 | 下游或外围增强反向定义 Conversation truth | `AC-RED-006`;`AC-NFR-003`;`EV-CONV-AUTH-001`;`EV-CONV-OUTBOX-001` |
| VETO-CONV-009 | command / consumer truth 事务出现 partial commit | `AC-TX-001`;`EV-CONV-FACT-001`;`EV-CONV-CONSUMER-001` |
| VETO-CONV-010 | query、projection、cursor、report 或 consistency validation 写入业务 truth | `AC-TX-003`;`AC-CONS-002`;`EV-CONV-DERIVED-001` |
| VETO-CONV-011 | duplicate / conflict 形成冲突 truth 或重复外部副作用 | `AC-IDEM-001`;`AC-IDEM-002`;`EV-CONV-FACT-001`;`EV-CONV-OUTBOX-001` |
| VETO-CONV-012 | 配置绕过 visibility、redaction、idempotency、audit、state machine 或 path shape | `AC-RED-008`;`AC-NFR-009`;`EV-CONV-CONFIG-001` |
| VETO-CONV-013 | fake / controlled adapter 被标记为 production success | `AC-RED-010`;`AC-NFR-012`;`reports/acceptance/handoff.md` |
| VETO-CONV-014 | P0 证据缺失、redaction 失败、路径错误或 veto checklist 未覆盖 | `AC-EVID-001~008`;`EV-CONV-GATE-001`;`EV-CONV-ACCEPT-001` |

风险接受只允许处理 P1 / P2、S2 / S3 或 readiness 缺口。核心闭环缺失、授权失效、正文或 secret 泄漏、source truth 被补造、truth 反写、partial commit、幂等冲突形成 truth、配置绕过红线、fake-as-production 和 P0 证据缺失均不得风险接受。

## 12. 缺陷分级、复验与放行规则

> 校准来源：
> - `design-calibration/06_acceptance_step_12_defects_release.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“缺陷分级表”“缺陷到结论影响表”“复验规则表”“放行规则表”和“关闭证据要求表”小节，了解缺陷如何影响通过 / 有条件通过 / 不通过。

本轮缺陷分为 S0 一票否决、S1 P0-blocking、S2 boundary / readiness 和 S3 非阻断四级。S0 等价命中 `VETO-CONV-*`，结论只能是不通过。S1 未关闭时不得通过或有条件通过。S2 只有在证明 P0 truth、安全、redaction 和 evidence 不受影响，且完成风险接受记录后，才能支撑有条件通过。S3 不阻断 P0，但必须进入 open issues 或 backlog。

| 缺陷级别 | 定义 | 对结论的影响 | 复验要求 |
|---|---|---|---|
| S0 一票否决 | 命中 `VETO-CONV-*` 或破坏 truth、authorization、data ownership、traceability、P0 evidence | 直接不通过，不得风险接受 | 重跑直接 TC、release redline、redaction check、相关 main CI suite |
| S1 P0-blocking | P0 用例、P0 suite、核心非功能专项或 release gate 失败 | 未关闭时不通过，不得风险接受 | 重跑直接 TC、同场景组 TC、相关 P0 suite、同类 negative case |
| S2 Boundary / readiness | P0/P1 边界、nightly、operations-replay、controlled seam 或 P0-supporting 失败，但不破坏 P0 红线 | 可进入有条件通过候选，未记录风险接受时不得放行 | 重跑对应 nightly / operations-replay / staging smoke，证明 P0 不受影响 |
| S3 非阻断 | 文档、报告呈现、非关键统计、P2 production-like 缺口或后续专项不完整 | 不阻断 P0，可通过或有条件通过 | 进入 backlog / open issues，必要时局部检查 |

任何 S0 / S1 修复都必须完成直接 TC、同组 TC、相关 suite、release redline 或 redaction check 的复验，并保留 run-scoped artifact 与 report。

## 13. 风险接受与遗留项

> 校准来源：
> - `design-calibration/06_acceptance_step_13_risk_acceptance.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“风险接受表”“不可接受风险表”“风险记录字段要求表”“风险到报告位置映射表”和“有条件通过裁决图”小节，了解哪些残余风险可以支撑有条件通过，以及哪些失败必须修复后重验。

风险接受只允许覆盖 P1/P2 非范围风险、S2 boundary / readiness 风险和 S3 非阻断问题。所有可接受风险必须写明影响、接受理由、后续动作、责任人、接受人和截止时间，并进入 `reports/acceptance/risk-acceptance.md` 或 `reports/acceptance/open-issues.md`。

| 风险 / 遗留项 | 影响 | 后续动作 | 责任人 / 接受人 | 截止时间 |
|---|---|---|---|---|
| RISK-CONV-001 真实 DB / broker / resolver / handoff 产品行为未验证 | P0 通过不代表真实产品集成可用 | P1 integration / staging-like 专项 | integration owner + acceptance owner | P1 staging-like 专项 |
| RISK-CONV-002 真实跨仓端到端联调未完成 | 跨仓部署可能暴露配置或契约接缝问题 | 基于已收稳仓库执行后续联调 | project owner + affected repo owner + acceptance owner | P1 staging-like 专项 |
| RISK-CONV-003 Chat UI / Workspace / Bridges 体验未覆盖 | 产品体验完整性不能由本仓 P0 证明 | 转入下游仓测试和验收计划 | downstream owner + acceptance owner | 对应下游仓验收 |
| RISK-CONV-004 Runtime 推理质量和工具调用正确性未覆盖 | 推理质量问题不在本仓裁决 | 转入 `L2-runtime` / tools 测试方案 | runtime owner + acceptance owner | 对应运行时专项 |
| RISK-CONV-005 来源仓 truth lifecycle 未覆盖 | 来源仓数据不一致会影响 manifestation 输入 | 来源仓自行验收，本仓保留 unresolved / digest mismatch | source repo owner + acceptance owner | 对应来源仓验收 |
| RISK-CONV-006 生产级吞吐、延迟、容量数字未锁定 | 不能宣称达到生产容量目标 | 后续容量专项锁定阈值 | performance owner + acceptance owner | P2 capacity 专项 |
| RISK-CONV-007 config center / hot reload / auto repair 未覆盖 | 后续启用可能影响冷更新和审计边界 | 后续配置中心设计，P0 中启用应 fail-fast | config owner + acceptance owner | P2 configuration 专项 |
| RISK-CONV-008 production-like runbook / dashboard 未覆盖 | 运维交接完整性不足 | 转入 production-like 运维专项 | operations owner + acceptance owner | P2 operations 专项 |
| RISK-CONV-009 S2 readiness / boundary 缺陷 | 可能影响进入下一阶段准备度 | 记录 defect、影响、临时规避、复验计划和目标时间 | defect owner + test owner + acceptance owner | 待送验填写 |
| RISK-CONV-010 S3 非阻断问题 | 不影响 P0 裁决，但影响文档、报告或后续体验 | 写入 `open-issues.md` 或 backlog | issue owner + acceptance owner | 待送验填写 |

`VETO-CONV-*`、S0、S1、redaction violation、授权失效、source truth isolation 失败、truth 反写、fake-as-production、证据路径错误和 P0 evidence 缺失不得风险接受。没有接受人、后续动作或截止时间的风险不能作为有条件通过依据。

## 14. 最终结论与签署

> 校准来源：
> - `design-calibration/06_acceptance_step_14_final_conclusion.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“最终结论取值表”“裁决前状态表”“结论判定矩阵”“签署角色表”和“最终裁决流程图”小节，了解最终结论如何从 P0 evidence、veto、缺陷分级和风险接受中收敛。

最终验收结论只允许使用 `通过`、`有条件通过`、`不通过`。`送验不成立`、`暂停裁决` 和 `待送验填写` 只能作为结论前状态，不能替代最终结论。禁止使用“基本通过”“原则上通过”“大体没问题”等模糊表述。

| 维度 | 结论 | 说明 |
|---|---|---|
| 功能验收 | 待送验填写: 通过 / 有条件通过 / 不通过 | 依据 §5~§8 的 P0 AC、TC、EV 和 gate 结果 |
| 非功能验收 | 待送验填写: 通过 / 有条件通过 / 不通过 | 依据 §9~§10 的 redaction、config、observability、evidence 和 report 结果 |
| 发布准备 | 待送验填写: 通过 / 有条件通过 / 不通过 | 依据 release gate、veto checklist、risk acceptance 和 handoff |
| 总体结论 | 待送验填写: 通过 / 有条件通过 / 不通过 | 取决于 P0 主线、VETO、S0 / S1、S2 风险接受和 evidence 完整性 |
| 是否允许进入下一阶段 | 待送验填写: 是 / 否 / 有条件 | `有条件` 必须回指 `reports/acceptance/risk-acceptance.md` |

当全部 P0 AC 可裁决通过、P0 evidence / gate / redaction / handoff 完整、无 `VETO-CONV-*`、无 S0 / S1 且无未接受 S2 时，总体结论为通过。P0 主线成立但存在已接受 S2 / readiness 或 P1/P2 残余风险时，总体结论可为有条件通过，并必须回指 `reports/acceptance/risk-acceptance.md`。任一 VETO、S0 / S1、redaction failure、授权失效、source truth isolation 失败或 P0 evidence 缺失时，不得通过。

| 角色 | 姓名 / 责任 | 结论 | 日期 |
|---|---|---|---|
| Acceptance owner | 待送验填写；确认最终裁决、风险接受和 handoff | 待送验填写 | 待送验填写 |
| Test owner | 待送验填写；确认 P0 suite、EV、gate、缺陷和复验 | 待送验填写 | 待送验填写 |
| Technical owner | 待送验填写；确认设计契约、实现边界和 P0 主线 | 待送验填写 | 待送验填写 |
| Security / redaction reviewer | 待送验填写；确认 redaction、forbidden body 和 secret 边界 | 待送验填写 | 待送验填写 |
| Release / operations owner | 待送验填写；确认 reports、artifacts、handoff 和发布准备范围 | 待送验填写 | 待送验填写 |
| Affected repo owner | 如存在跨仓风险则填写；确认被接受风险和后续动作 | 待送验填写 | 待送验填写 |

签署不等于自动接受全部风险。对于 `有条件通过`，签署只代表接受 `risk-acceptance.md` 中明确列出的风险、后续动作和截止时间。没有列入风险接受表的风险，不得因签署而自动被接受。

## 15. 参考

> 校准来源：
> - `design-calibration/06_acceptance_step_15_formal_document_assembly.md`
>
> 延伸阅读：
> - 建议继续阅读上述中间产物的“正式文档组装清单”“自审结果”和“追溯引用检查”小节，了解本文如何由 Step 1~14 中间产物汇总而来。

| 类型 | 文档 |
|---|---|
| 验收标准书写规范 | `standards/document/验收标准书写规范.md` |
| 验收标准讨论 SOP | `standards/document/验收标准讨论流程_SOP.md` |
| 中间产物规范 | `standards/document/设计文档讨论中间产物规范.md` |
| 需求文档 | `projects/L1-conversation/00-需求文档.md` |
| 架构设计 | `projects/L1-conversation/01-架构设计.md` |
| 概要设计 | `projects/L1-conversation/02-概要设计.md` |
| 详细设计 | `projects/L1-conversation/03-详细设计.md` |
| 配置设计 | `projects/L1-conversation/04-配置设计.md` |
| 测试方案 | `projects/L1-conversation/05-测试方案.md` |
| 验收校准流程 | `projects/L1-conversation/design-calibration/06_acceptance_calibration_flow.md` |
| Step 1~14 中间产物 | `projects/L1-conversation/design-calibration/06_acceptance_step_*.md` |

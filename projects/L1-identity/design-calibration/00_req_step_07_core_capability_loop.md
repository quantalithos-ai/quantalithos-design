# Step 7. 核心能力闭环

> 对应 SOP: `standards/document/需求文档讨论流程_SOP.md` Step 7
> 回填章节: `00-需求文档.md` §7 核心能力闭环
> 生成日期: 2026-06-10

---

## 1. Step 状态 + Step 内计划

- 状态: 已完成
- 本步目标: 把 `L1-identity` 的需求主线拆成可逐个停审的核心能力节点,作为 Step 8~14 的组织轴。
- 复杂度判断: 核心能力节点为五个,可在单文件完成;后续故事、功能、规则、数据、接口、验收必须逐能力节点承接。

| 子步骤 | 产物 | 状态 |
|---|---|---|
| 读取 Step 2 / Step 4 / Step 6 | 输入表 | 已完成 |
| 回答核心能力闭环问题 | SOP 问题回答表 | 已完成 |
| 诊断旧文档平铺清单问题 | 当前文档问题诊断表 | 已完成 |
| 比较能力闭环前后组织方式 | 改动前后对比表 | 已完成 |
| 记录能力节点取舍 | 设计取舍表 | 已完成 |
| 输出闭环定义、能力图、能力层级和停审清单 | 结构化中间产物 | 已完成 |
| 形成正式 §7 回填草稿 | 回填草稿 | 已完成 |
| 列出待确认事项和下一步门禁 | 待确认事项、进入下一步条件 | 已完成 |

---

## 2. 本步输入

| 输入 | 使用方式 |
|---|---|
| Step 2 本仓定位与边界 | 确认能力不能越过 identity truth |
| Step 4 目标与非目标 | 将 `G-ID-001`~`G-ID-005` 转成能力节点 |
| Step 6 使用方与依赖 | 确认哪些能力需要外部来源或下游消费 |
| 产品叙事中 AI 员工身份个体承诺 | 提供身份、生命周期、职业能力、记忆、生涯和可观察线索 |

---

## 3. SOP 问题回答

| 问题 | 回答 |
|---|---|
| 如果没有这个仓,系统会缺什么 | 会缺少跨项目长期稳定的 AI 员工身份锚点和身份变化追溯。 |
| 本仓成立必须共同具备哪些能力 | 身份锚定、生命周期可用性、角色能力摘要、生涯与记忆引用、身份事实消费与追溯。 |
| 哪些能力缺一个就不成立 | 缺身份锚定则没有主语;缺生命周期则无法判断可用性;缺角色能力则无法解释职业与能力;缺生涯记忆则无法体现持久个体;缺消费追溯则无法被平台其他仓使用和审计。 |
| 哪些是外围增强 | 高级绩效分析、自动能力评级、复杂 HR 流程、外部身份供应商深度集成、图形化员工主页、全量 event sourcing。 |
| 哪些根本不属于本仓 | 认证登录、项目成员分配、RoleDefinition 正文、runtime 执行、对话正文、artifact / memory 正文。 |
| 核心能力节点顺序是什么 | `C-ID-1` 身份锚定 -> `C-ID-2` 生命周期可用性 -> `C-ID-3` 角色能力摘要 -> `C-ID-4` 生涯与记忆引用 -> `C-ID-5` 消费与追溯。 |

---

## 4. 当前文档问题诊断

| 旧表现 | 问题 | 新处理 |
|---|---|---|
| lifecycle、Role、Capability、career、memory、事件和审计平铺 | 难以判断哪些能力共同构成闭环 | 抽象为五个能力节点 |
| 能力和对象一一混写 | 容易把对象名当需求能力 | 先写能力成立条件,对象后移 |
| 维护对账、event、projection 可能单独成主线 | 容易把派生机制凌驾于 identity truth | 归入 C-ID-5 消费与追溯 |
| 外围增强和核心能力混在一起 | 后续实施优先级不清 | 明确外围增强和边界外能力 |
| 能力缺失后果未说明 | 难以评审是否必须做 | 每个节点说明缺失后果 |

---

## 5. 改动前后对比

| 项 | 改动前 | 改动后 | 原因 |
|---|---|---|---|
| 组织方式 | 功能 / 对象平铺 | 五个核心能力节点 | 支撑能力小循环 |
| 身份锚定 | 只是 GlobalMember 对象 | `C-ID-1` 稳定身份主语 | 先讲能力,再推对象 |
| 生命周期 | 状态机细节 | `C-ID-2` 全局可用性边界 | 状态 enum 后移 |
| 角色能力 | Role / Capability 对象 | `C-ID-3` 身份侧摘要和来源 | 防止定义正文入仓 |
| 生涯记忆 | career / memory 字段 | `C-ID-4` append-only 经验和 ref-only | 防止外部正文入仓 |
| 事件追溯 | event / audit 机制 | `C-ID-5` 消费与追溯能力 | 机制后移设计 |

---

## 6. 设计取舍

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| 方案 A: 按旧对象聚合组织能力 | 与历史材料接近 | 会把对象形状提前固化 | 不采用 |
| 方案 B: 按五个能力节点组织需求后半段 | 可逐能力停审,追溯清楚 | 后续需要把旧对象重新映射 | 采用 |
| 方案 C: 把维护对账单独列为第六核心能力 | 强调运维可靠性 | 会让派生维护与身份 truth 并列,主线过宽 | 暂不采用,归入 C-ID-5 |

---

## 7. 结构化中间产物

### 7.1 闭环定义

`L1-identity` 的核心闭环是:先建立可长期引用的 AI 员工身份锚点,再管理该身份的全局可用性,再说明该身份的职业 / 能力语义,再沉淀其生涯与记忆引用,最后让相邻仓能消费这些身份事实并追溯变化。五类能力必须共同成立,否则 identity 要么只是通讯录,要么只是运行实例缓存,要么只是角色索引,都不能成为平台级 AI 员工身份真相仓。

### 7.2 核心能力闭环图

```text
C-ID-1 Identity Anchor
  平台存在稳定、唯一、可长期引用的 AI 员工身份主语
        |
        v
C-ID-2 Lifecycle Availability
  身份主语具备全局生命周期和可用性边界
        |
        v
C-ID-3 Role Capability Summary
  身份主语具备职业、能力和证据引用语义
        |
        v
C-ID-4 Career Memory References
  身份主语沉淀项目经历和记忆引用,但不拥有外部正文
        |
        v
C-ID-5 Consumption Traceability
  相邻仓可消费身份事实,身份变化可审计和追溯
```

### 7.3 能力层级划分表

| 能力编号 | 能力节点 | 类型 | 成立条件 | 不包含 |
|---|---|---|---|---|
| C-ID-1 | 身份锚定 | 核心闭环 | 平台可以创建、引用和查询稳定 AI 员工身份主语 | 登录认证、token、ProjectMember |
| C-ID-2 | 生命周期可用性 | 核心闭环 | 平台可以判断成员是否可用、暂停、退役或墓碑化 | 运行容器启停、项目内状态 |
| C-ID-3 | 角色能力摘要 | 核心闭环 | 成员身份具备角色 / 能力来源、摘要和证据引用语义 | RoleDefinition 正文、能力评估算法 |
| C-ID-4 | 生涯与记忆引用 | 核心闭环 | 成员项目经历可追加追溯,memory refs 可保存和迁移 | 项目 truth、memory 原文、向量 |
| C-ID-5 | 消费与追溯 | 核心闭环 | 相邻仓可消费身份事实,变化可审计、投影和对账 | UI 展示状态、observability 存储正文 |

### 7.4 能力节点说明

| 能力节点 | 需求说明 | 缺失后果 |
|---|---|---|
| C-ID-1 身份锚定 | 平台必须先有一个可长期引用的成员身份主语,它不是账号、token、进程、项目成员或显示名称。 | 下游只能用临时名称、runtime id 或项目内成员来指代 AI 员工,无法跨项目追溯。 |
| C-ID-2 生命周期可用性 | 身份主语必须有全局可用性边界,用于判断成员是否可继续被选择、调用、展示或归档。 | 暂停、退役、墓碑化等动作会散落在项目、runtime 或 UI 中,无法统一约束。 |
| C-ID-3 角色能力摘要 | 成员身份需要表达职业 / 能力语义,但只能保存身份侧摘要、来源和证据引用。 | 平台无法解释成员“适合做什么”,或把方法定义正文和评估算法错误塞入 identity。 |
| C-ID-4 生涯与记忆引用 | 长期身份需要保留经历和记忆引用,但历史必须追加,正文必须外部归属。 | AI 员工无法体现跨项目成长;若保存正文则会破坏隐私、归属和归档边界。 |
| C-ID-5 消费与追溯 | 身份事实必须能被 work、process、conversation、governance、workspace、runtime 等消费,并能追溯变化。 | identity 会变成孤立目录;下游可能各自维护成员副本并产生漂移。 |

### 7.5 外围增强与边界外能力

| 类型 | 能力 | 当前处理 |
|---|---|---|
| 外围增强 | 自动能力评级 / 绩效评分 | 后续分析能力,不进入核心闭环 |
| 外围增强 | 高级员工主页 / 社交资料 | UI / workspace 增强,不定义 identity truth |
| 外围增强 | 外部 HR / IdP 深度集成 | 外部集成增强,不作为当前需求主链 |
| 外围增强 | 复杂继任 / 组织架构 / 团队编制 | 后续组织管理能力 |
| 边界外 | ProjectMember 分配、项目内角色和任务承担 | `L1-work` |
| 边界外 | Gate / approval / policy 裁决 truth | `L1-governance` |
| 边界外 | RoleDefinition / Method Content 正文 | `L3-method-library` |
| 边界外 | 容器、工具执行和 runtime context | `L2-member-service` / `L2-runtime` |
| 边界外 | 对话正文、消息和参与展示 | `L1-conversation` / `L1-workspace` |
| 边界外 | memory / archive / artifact 正文 | 相邻仓或外部承载方 |

---

## 8. 回填草稿

```md
## 7. 核心能力闭环

> 校准来源:
> - `design-calibration/00_req_step_07_core_capability_loop.md`

`L1-identity` 的核心闭环是:先建立可长期引用的 AI 员工身份锚点,再管理该身份的全局可用性,再说明该身份的职业 / 能力语义,再沉淀其生涯与记忆引用,最后让相邻仓能消费这些身份事实并追溯变化。

五个能力节点分别是 `C-ID-1` 身份锚定、`C-ID-2` 生命周期可用性、`C-ID-3` 角色能力摘要、`C-ID-4` 生涯与记忆引用、`C-ID-5` 消费与追溯。后续用户故事、功能需求、业务规则、数据归属、接口依赖和验收标准均按这五个能力节点组织。
```

---

## 9. 待确认事项

| 编号 | 待确认事项 | 当前处理 |
|---|---|---|
| OQ-ID-S7-001 | 是否需要第六个“维护对账”核心节点 | 暂归入 C-ID-5 消费与追溯,后续若 job / reconciliation 成为主需求再拆分 |
| OQ-ID-S7-002 | 是否把 capability-hub 放入角色能力闭环 | 当前只作为外部能力背景,不进入当前核心依赖 |
| OQ-ID-S7-003 | 外部 HR / IdP 是否进入核心闭环 | 当前作为外围增强,不进入本轮核心闭环 |

---

## 10. 进入下一步条件

核心能力闭环和能力节点顺序已确认,且每个节点都有成立条件、缺失后果、外围增强和边界外说明。可以进入 Step 8,按能力节点展开用户故事。

# Step 11. 备选方案与取舍

> 对应 SOP: `standards/document/架构设计讨论流程_SOP.md` Step 11
> 回填章节: `01-架构设计.md` §12 备选方案与取舍
> 生成日期: 2026-06-05

---

## 1. 本步目标

把 `L1-process` 当前主线架构方案与主要相邻替代路径放到同一判断框架下比较,说明为什么选择当前方案,为什么放弃其它路径,以及当前方案牺牲了什么、换来了什么。本步只比较架构层路径级替代关系,不写产品横评、局部实现对比、愿望池、未来可能性大全、API / event / job / schema 或部署细节。

---

## 2. 本步输入

| 输入 | 当前状态 | 本步使用方式 |
|---|---|---|
| `design-calibration/01_arch_step_02_goals_constraints.md` | Step 2 已完成 | 承接架构目标、不可变约束和当前阶段取舍 |
| `design-calibration/01_arch_step_03_responsibility_boundary.md` | Step 3 已完成 | 排除职责边界外事项,防止重新包装为备选方案 |
| `design-calibration/01_arch_step_07_dependency_direction.md` | Step 7 已完成 | 承接唯一编译期依赖、依赖倒置和层间约束 |
| `design-calibration/01_arch_step_08_data_ownership_consistency.md` | Step 8 已完成 | 承接数据归属和一致性取舍 |
| `design-calibration/01_arch_step_09_interactions_communication.md` | Step 9 已完成 | 承接同步 / 异步 / 后台路径分离 |
| `design-calibration/01_arch_step_10_technology_choices.md` | Step 10 已完成 | 承接关键技术机制和不采用口径 |
| 旧 `projects/L1-process/01-架构设计.md` | 旧 Draft | 仅作为旧架构风格和旧技术假设的替代路径诊断来源 |

---

## 3. SOP 问题回答

### 3.1 这个仓有哪些主要可选架构方案?

当前主线方案是:

> 以 Process truth 为核心,通过正式承接边界、运行期接缝、ref / snapshot、本地运行时索引、持久化状态、checkpoint / recovery 连续性、同步 / 异步 / 后台分离、projection 隔离、幂等顺序保护、可靠传播 / handoff 和 traceability / evidence 机制构成独立过程执行事实架构。

值得比较的相邻替代路径是:

| 替代路径 | 是否进入本章比较 | 判断 |
|---|---|---|
| 单体流程引擎中心路径 | 是 | 与当前主线构成结构性替代,旧 Draft 有相关线索。 |
| 直接依赖相邻仓源码路径 | 是 | 与依赖倒置主线构成结构性替代。 |
| 完整 BPMN engine 优先路径 | 是 | 与基础过程推进主线构成表达力优先替代。 |
| 全同步闭环路径 | 是 | 与同步 / 异步 / 后台分离构成一致性承接替代。 |
| 全事件化最终一致路径 | 是 | 与核心主真相强一致判断构成替代。 |
| 直接暴露核心写模型给查询和下游路径 | 是 | 与 projection / read model 隔离构成替代。 |
| 外部正文 / 快照入仓路径 | 是 | 与 ref / snapshot / 显式状态标记主线构成替代。 |
| 具体数据库、消息中间件、语言或框架产品路径 | 否 | 属于产品级或实现级选择,后续文档收敛。 |
| method-library、work、governance、runtime 等职责入 Process 路径 | 否 | 已被 Step 2 / Step 3 的不可变约束和非目标排除,不再作为有效备选方案。 |

### 3.2 为什么当前选择这一种?

当前主线方案能同时满足四个硬目标:

1. Process truth 独立成立,不被 method-library、work、governance、runtime、conversation、workspace、observability 或 archive 替代。
2. 跨仓协作可发生,但只通过 ref、snapshot、事件、adapter、handoff 或共享 core contract 承接。
3. 核心推进、等待和恢复保持同一事实连续性,查询、投影、报告和维护不得反写真相。
4. 下游消费和外部反馈允许最终一致,但重复、乱序、stale、unresolved、failed 和 retryable 状态必须可解释。

其它路径通常只能优化某一面:更快接入、更少中间层、更强即时感或更完整表达力,但会牺牲边界、依赖裁剪、主真相连续性或未来演进空间。

### 3.3 被放弃方案的主要优点是什么?

| 被放弃方案 | 主要优点 |
|---|---|
| 单体流程引擎中心路径 | 初期概念统一,流程推进能力集中,可能更快形成端到端演示。 |
| 直接依赖相邻仓源码路径 | 开发短期更直接,减少 adapter / snapshot / ref 转换成本。 |
| 完整 BPMN engine 优先路径 | 表达力强,复杂网关、嵌套过程和建模生态更完整。 |
| 全同步闭环路径 | 调用方即时感强,短链路结果更直观。 |
| 全事件化最终一致路径 | 运行单元更解耦,入口压力小,吞吐扩展空间更明显。 |
| 直接暴露核心写模型给查询和下游路径 | 初期查询接入快,少一层 projection / read model。 |
| 外部正文 / 快照入仓路径 | 本地判断更方便,短期减少跨仓读取或解析失败。 |

### 3.4 为什么即便有这些优点,当前仍不采用?

这些优点大多是短期实现便利、即时体验或表达力收益,但 `L1-process` 的首要问题不是“更快接入全部流程能力”,而是“守住过程执行事实的独立真相”。一旦选择单体中心、直接源码依赖、外部正文入仓或读模型反写,后续会很难再恢复仓际边界。完整 BPMN、产品级存储、消息中间件和运行部署属于后续演进或实现承载问题,不能在当前阶段反向定义 Process truth。

### 3.5 当前选择牺牲了什么,换来了什么?

| 当前方案牺牲 | 当前方案换来 |
|---|---|
| 初期接入路径更长 | 外部输入不会直接污染核心语义 |
| adapter / ref / snapshot / marker 设计成本更高 | 非 core sibling 仓不会变成编译期依赖或第二真相 |
| projection 和 handoff 会引入延迟 | 主真相成立不被下游消费阻塞 |
| 幂等、顺序和 recovery 证据更复杂 | 重复、乱序和恢复重放不会产生多份 Process truth |
| 暂不硬化完整 BPMN 表达力 | 当前核心闭环更可控,复杂表达力有清晰演进空间 |
| 暂不锁定具体产品 | 后续技术选择不会推翻已经收稳的边界机制 |

---

## 4. 当前文档问题诊断

| 位置 | 当前表现 | 问题 | 本步处理 |
|---|---|---|---|
| 旧 `01-架构设计.md` | “流程引擎 + persisted state machine”偏单体流程中心表达 | 未经过新版边界和依赖裁剪重新论证 | 作为替代路径比较,不直接继承 |
| 旧 `01-架构设计.md` | Python、PostgreSQL、BPMN engine 等具体技术出现较早 | 容易把产品 / 技术栈当成架构备选方案 | 明确不进入本章主比较 |
| Step 2 / Step 3 | 已排除相邻仓职责入 Process | 不能把已排除事项重新包装成方案 | 本章只比较仍在边界内的相邻替代路径 |
| Step 10 | 已列单项关键技术机制 | 本步需要路径级取舍,不能重写每项机制理由 | 主表按方案路径比较 |

---

## 5. 改动前后对比

| 项 | 改动前 | 改动后 | 原因 |
|---|---|---|---|
| 方案取舍表达 | 分散在各 Step 的局部设计取舍中 | 汇总为架构路径级比较 | 对齐架构规范 4.12 |
| 旧流程引擎假设 | 可能被默认视为主线 | 明确作为不采用路径比较 | 避免旧 Draft 污染新版架构 |
| 产品级差异 | 容易混入方案对比 | 明确排除出本章 | 产品横评不属于架构路径取舍 |
| 边界外职责 | 可能再次进入讨论 | 明确不作为有效备选方案 | 尊重 Step 2 / Step 3 的不可变约束 |

---

## 6. 设计取舍

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| 方案 A: 只保留 Step 10 的单项机制说明 | 简短 | 缺路径级替代方案比较 | 不采用 |
| 方案 B: 按结构性替代路径做方案比较 | 能说明当前主线为什么成立 | 表格较长 | 采用 |
| 方案 C: 加入数据库、消息中间件、语言等产品比较 | 接近实现讨论 | 违反本章边界 | 不采用 |
| 方案 D: 把所有未来增强都列为备选方案 | 看似完整 | 会把愿望池和正式取舍混在一起 | 不采用 |

### 6.1 待确认问题的方案选择

#### 完整 BPMN engine 是否作为当前主线?

| 方案 | 内容 | 影响 |
|---|---|---|
| 方案 A | 完整 BPMN engine 优先 | 表达力强,但会扩大当前核心闭环和引擎语义约束 |
| 方案 B | 基础过程推进主线,完整 BPMN 保留演进 | 与当前 C-1~C-5 核心闭环更匹配 |

推荐方案 B。

#### 跨仓协作是否允许源码依赖?

| 方案 | 内容 | 影响 |
|---|---|---|
| 方案 A | 允许直接依赖相邻仓源码 | 初期开发快,但破坏唯一编译期依赖 |
| 方案 B | 仅 `L0-core` 编译期依赖,其它仓运行期倒置 | 边界稳定,但 adapter / snapshot 成本更高 |

推荐方案 B。

#### 查询消费是否直接读核心写模型?

| 方案 | 内容 | 影响 |
|---|---|---|
| 方案 A | 直接暴露核心写模型 | 接入快,但下游绑定核心结构 |
| 方案 B | 通过 read model / projection 隔离 | 消费稳定,但需要处理最终一致 |

推荐方案 B。

---

## 7. 结构化中间产物

### 7.1 当前主线方案

`L1-process` 当前主线方案是独立 Process truth 架构:核心过程执行事实由本仓正式拥有,外部定义、工作、治理、运行、对话、工作台、观测和归档都通过正式边界协作。主线依赖正式承接边界、运行期接缝、ref / snapshot、本地运行时索引、持久化状态、checkpoint / recovery 连续性、同步 / 异步 / 后台分离、projection 隔离、幂等顺序保护、可靠传播 / handoff 和 traceability / evidence 机制共同成立。

### 7.2 方案路径比较表

| 方案路径 | 解决的问题 | 主要收益 | 主要代价 / 约束 | 当前结论 | 说明 |
|---|---|---|---|---|---|
| 独立 Process truth + 正式边界协作主线 | 在多仓协作中保持过程执行事实独立、可推进、可等待、可恢复、可消费 | 同时保护真相边界、依赖裁剪、恢复连续性和下游消费 | 增加承接、adapter、snapshot、projection、handoff 和 evidence 成本 | 采用 | 这是当前主线方案,最符合 Step 2~10 已收敛约束。 |
| 单体流程引擎中心路径 | 用一个流程中心统一定义、执行、等待、恢复和消费 | 初期概念集中,推进路径直观 | 容易吸收 method-library、work、governance、runtime 和 projection 职责,形成多真相中心 | 不采用 | 该路径与仓级真相拆分和依赖裁剪冲突。 |
| 直接依赖相邻仓源码路径 | 降低跨仓上下文解析和适配成本 | 开发路径短,类型和调用复用直接 | 破坏 `L0-core` 唯一编译期依赖,相邻仓模型会反向塑造 Process | 不采用 | 该路径是有效替代,但违反已收稳依赖边界。 |
| 运行期接缝 / ref / snapshot / event / handoff 路径 | 在不依赖相邻仓源码的前提下完成协作 | 仓际边界稳定,外部 truth 不入仓 | 需要处理解析失败、stale、unresolved、幂等和转换 | 采用 | 这是主线协作路径,支撑跨仓依赖倒置。 |
| 完整 BPMN engine 优先路径 | 快速获得复杂流程表达力和建模生态 | 复杂网关、嵌套过程和标准表达更完整 | 当前核心闭环被扩大,引擎语义可能反向定义 Process truth | 不采用 | 当前只需要基础 Activity、Token / Gateway、waiting gate 和 recovery 主线;完整 BPMN 作为演进观察。 |
| 基础过程推进主线路径 | 先保证当前核心过程事实闭环成立 | 范围可控,更容易保护 Activity、Token、waiting gate 和 recovery 语义 | 表达力不如完整 BPMN,后续复杂流程需要演进 | 采用 | 该路径对齐当前阶段取舍。 |
| 全同步闭环路径 | 让所有跨边界变化在同步入口即时完成 | 调用方即时感强,结果直观 | 会把外部结果、下游消费和长时维护压入主路径,形成高耦合和伪一致 | 不采用 | 只适合主真相即时判断,不适合作为全局交互主线。 |
| 同步判断 + 异步结果 + 后台维护分离路径 | 按不同一致性需求承接不同交互 | 主真相清晰,外部结果和派生维护不阻塞核心 | 增加延迟、状态解释和运行承载复杂度 | 采用 | 该路径承接 Step 8 / Step 9 的一致性和通信结论。 |
| 全事件化最终一致路径 | 最大化运行解耦和吞吐弹性 | 异步能力强,入口压力小 | ProcessInstance、Activity、Token、waiting gate 和 recovery 的强一致判断会变弱 | 不采用 | 核心推进和恢复连续性不能完全最终一致化。 |
| 直接暴露核心写模型给查询和下游路径 | 降低查询和消费接入成本 | 初期简单,少一层派生模型 | 下游会绑定核心写模型,查询或报告容易反写真相 | 不采用 | 该路径破坏写 / 读边界和演进空间。 |
| read model / projection 隔离路径 | 让消费面稳定读取过程事实摘要 | 保护写模型,支持最终一致消费 | 需要 stale / rebuilding / unavailable 等状态和重建机制 | 采用 | 这是当前消费主线,但不得反写业务真相。 |
| 外部正文 / 快照入仓路径 | 降低跨仓读取和解析失败成本 | 本地判断更方便,查询材料更完整 | Process 会接管 method-library、work、runtime、artifact、conversation 或 archive 正文 | 不采用 | 该路径违反数据所有权和正文禁止边界。 |
| 外部 ref / snapshot + 显式状态标记路径 | 使用外部语境但不拥有外部 truth | 边界清晰,缺失和过期可解释 | 调用方必须处理 stale / unresolved / waiting / invalid | 采用 | 这是处理外部 truth 的当前主线。 |
| 下游确认作为主真相成立前置路径 | 让调用方看到端到端消费完成 | 表面完整性强 | conversation、workspace、observability、archive 滞后会阻塞或回滚 Process 主事实 | 不采用 | 下游消费不能反向决定 Process truth 成立。 |
| Process fact 可靠传播 / handoff 路径 | 保证已成立事实可消费和可追溯交接 | 主真相不被下游阻塞,交接状态可解释 | 需要 pending / failed / retryable / delivered 等状态承接 | 采用 | 该路径符合最终一致消费和追溯交接需求。 |

### 7.3 轻量取舍对照表

| 当前方案得到 | 当前方案失去 |
|---|---|
| 独立 Process truth 和清晰职责边界 | 单体流程中心的短期简单性 |
| 非 core sibling 仓不进入编译期依赖 | 直接复用相邻仓模型的开发便利 |
| 主真相推进和 recovery 连续性 | 全事件化带来的极致解耦 |
| 下游消费和派生视图最终一致 | 全同步端到端即时完成感 |
| read model / projection 隔离 | 直接暴露核心模型的低成本接入 |
| 外部 truth 不入仓 | 本地保存完整外部材料的便利 |
| 复杂表达力可演进 | 当前立即覆盖完整 BPMN 的表达力 |

### 7.4 方案边界说明短文

本章只比较会改变 `L1-process` 主线结构的相邻替代路径,不比较产品、框架、语言、数据库、消息中间件或部署方式。method-library 定义管理、work truth、governance decision、runtime 执行正文、conversation truth、workspace dashboard、observability 正文和 archive package 等已被 Step 2 / Step 3 明确排除的事项,不再包装成有效备选方案。完整 BPMN、复杂网关、嵌套过程和高级视图是后续演进方向,只有当它们改变当前主线结构时才进入本章取舍。当前方案的核心取舍是牺牲短期直接性和表达力,换取 Process truth、仓际边界、一致性和演进空间。

---

## 8. 回填草稿

以下内容供 Step 16 重建正式 `01-架构设计.md` 时回填。正式文档可摘录本文件 §7 的结构化结论。

```md
## 12. 备选方案与取舍

> 校准来源:
> - `design-calibration/01_arch_step_11_alternatives_tradeoffs.md`
>
> 延伸阅读:
> - 建议继续阅读上述中间产物的“SOP 问题回答”“当前主线方案”“方案路径比较表”“轻量取舍对照表”和“方案边界说明短文”小节,了解本章如何从架构目标、职责边界、依赖、一致性和关键技术机制中收敛主线方案。

正式章节应摘录:

- `design-calibration/01_arch_step_11_alternatives_tradeoffs.md` §7.1 当前主线方案。
- `design-calibration/01_arch_step_11_alternatives_tradeoffs.md` §7.2 方案路径比较表。
- `design-calibration/01_arch_step_11_alternatives_tradeoffs.md` §7.3 轻量取舍对照表。
- `design-calibration/01_arch_step_11_alternatives_tradeoffs.md` §7.4 方案边界说明短文。
```

---

## 9. 待确认事项

| 编号 | 待确认事项 | 当前状态 |
|---|---|---|
| Q-001 | 完整 BPMN engine 是否在后续版本引入 | 演进路线和后续 ADR 收敛 |
| Q-002 | 基础过程推进机制如何映射到详细状态机和协议 | 后续详细设计收敛 |
| Q-003 | projection / handoff / recovery 的具体可靠性机制 | 后续详细设计、配置设计和测试计划收敛 |
| Q-004 | 产品级技术栈比较 | 后续概要设计、配置设计或实施计划收敛 |

---

## 10. 进入下一步条件

- 已明确当前架构主线方案。
- 已明确哪些相邻替代路径值得比较。
- 已说明每条路径解决的问题、主要收益和主要代价 / 约束。
- 已给出采用 / 不采用的正式结论。
- 未滑入产品横评、局部实现对比、愿望池或前文已排除事项的重新包装。

结论:可以进入 Step 12 `横切关注点`。

# 05-测试方案 · L1-work

> 本文定义 `L1-work` 如何把新版 `00/01/02/03/04` 中的需求、架构、概要、详细设计和配置结论转化为可执行、可追溯、可留证的测试方案。
> 本文不重新定义需求、对象字段、协议 schema、配置项、实现计划或验收裁决。

---

## 文档元信息

- 作者: codex
- 日期: 2026-06-04
- 状态: Draft
- 版本: v0.2.0

### 变更记录

| 版本 | 日期 | 变更 | 作者 |
|---|---|---|---|
| v0.2.0 | 2026-06-04 | 按测试方案 SOP 和 Step 1~14 中间产物重建正式测试方案 | codex |
| v0.1.0 | 2026-05-15 | 旧版草案 | Claude |

---

## 1. 与上游文档的关系声明

> 校准来源：
> - `design-calibration/05_test_plan_step_01_input_boundary.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_01_input_boundary.md` 的“结构化中间产物”“回填草稿”和“待确认事项”小节，了解测试输入边界如何收敛。

### 1.1 测试输入边界

本轮 `05-测试方案.md` 以新版 `00-需求文档.md`、`01-架构设计.md`、`02-概要设计.md`、`03-详细设计.md`、`04-配置设计.md` 为主输入。

旧版 `05-测试方案.md` 和旧版 `06-验收标准.md` 只作为诊断输入和验收方向参考，不作为新版测试事实源。

测试方案只回答：

- 测什么。
- 怎么测。
- 在哪个层级测。
- 用什么数据、环境和门禁测。
- 如何形成 `EV-WORK-*` 证据并交给新版 `06-验收标准.md` 裁决。

### 1.2 上游输入映射

| 来源文档 | 测试输入 | 本文承接章节 |
|---|---|---|
| `00-需求文档.md` | C-1~C-5、`FR-WORK-*`、`BR-WORK-*`、`AC-WORK-*`、数据归属和非功能要求 | §2 / §5 / §6 / §10 / §14 |
| `01-架构设计.md` | 职责边界、依赖方向、数据所有权、一致性策略和横切关注点 | §3 / §4 / §8 / §10 / §14 |
| `02-概要设计.md` | 主要组成部分、关键对象、API 骨架、处理流、状态和异常边界 | §3 / §5 / §6 |
| `03-详细设计.md` | 模块契约、对象契约、协议、函数流、状态矩阵、事务、错误、幂等、配置、观测和最小验证清单 | §3 / §4 / §6 / §9 / §10 / §11 / §13 |
| `04-配置设计.md` | profile、配置项、敏感配置、加载校验、生效机制、失效策略和证据边界 | §7 / §8 / §9 / §10 / §12 / §13 / §14 |
| `06-验收标准.md` | 验收方向和后续裁决入口 | §5 / §12 / §13 / §14 |

### 1.3 不在本文重新定义的问题

| 问题 | 归属文档 |
|---|---|
| 本仓为什么存在、做什么、不做什么 | `00-需求文档.md` |
| 系统边界、依赖方向和架构红线 | `01-架构设计.md` |
| 主要组成部分、接口骨架和概要状态 | `02-概要设计.md` |
| Rust 对象字段、DTO schema、状态矩阵、错误模型和函数流 | `03-详细设计.md` |
| 配置项、配置来源、profile 和敏感配置 | `04-配置设计.md` |
| 通过 / 不通过、一票否决和风险接受裁决 | `06-验收标准.md` |
| 开发批次、提交边界、编码规范和提交规范 | `07-实施计划.md` |
| 真实部署、密钥挂载、告警和值班 | `09-部署与运维手册.md` 或后续运维材料 |

### 1.4 对上游设计的影响判定

| 测试结论 | 是否影响上游设计 | 回写位置 | 处理状态 |
|---|---|---|---|
| 新版 `00/01/02/03/04` 为测试主输入 | 否 | 无 | 无回写 |
| 旧 `05/06` 不能作为新版测试事实源 | 否 | 无 | 无回写 |
| 测试方案不得自行新增字段、状态、接口、错误或配置项 | 否 | 无 | 无回写 |
| `AC-WORK-001`~`029` 需要由 `EV-WORK-*` 承接证据 | 否 | 无 | 无回写 |

---

## 2. 本次测试目标与范围

> 校准来源：
> - `design-calibration/05_test_plan_step_02_scope.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_02_scope.md` 的“结构化中间产物”“回填草稿”和“待确认事项”小节，了解 P0 / P1 / P2 范围如何裁剪。

### 2.1 测试目标

| 测试目标 | 说明 | 下游输出 |
|---|---|---|
| 证明核心能力闭环成立 | C-1~C-5 均有正向和关键负向测试证据 | 新版 `06` 可据此裁决核心闭环 |
| 证明正式工作事实不被污染 | 边界外输入、外部正文、runtime step、conversation suggestion、query / projection / maintenance 反写被拒绝 | 新版 `06` 可据此裁决一票否决项 |
| 证明详细设计契约可实现 | 七模块、协议、状态、事务、幂等、错误、配置和观测均有测试切口 | `07` 可据此安排门禁 |
| 证明配置 P0 profile 可验证 | `local-dev`、`ci-test`、`integration-like`、`operations-replay` 的成功和失败路径可测试 | 本文 §8 / §9 承接环境和自动化 |
| 证明证据可交给验收 | 每个 P0 范围项都有 `TC-WORK-*` 和 `EV-WORK-*` | 新版 `06` 可据此做裁决 |

### 2.2 范围 / 非范围

| 范围项 | 类型 | 优先级 | 验证目标 | 非目标 / 说明 |
|---|---|---|---|---|
| Project / ProjectMember truth | 核心对象 | P0 | 项目主语和项目内承担成立 | 不测 identity 生命周期 |
| Backlog / WorkItem / child WorkItem truth | 核心对象 | P0 | 正式工作全集、拆分、升级和拒绝边界成立 | 不接收个人步骤、对话建议或 runtime step 直写 |
| Work dependency / blocker / done evidence | 核心关系 | P0 | 依赖、阻塞、解除依据和完成依据可解释 | 不保存 artifact / evidence 正文 |
| Iteration commitment | 核心对象 | P0 | 承诺子集从正式工作全集中形成并保持边界 | 不测 process planning 内部实现 |
| Promote boundary | 跨仓接缝 | P0 | plan item 符合条件时显式升级，不符合时拒绝 | 不拥有 ImplementationPlan 正文 |
| Command / Query / Event / Job protocol | 协议 | P0 | success、reject、duplicate、conflict、no-write、rerun 成立 | 不新增未定义协议 |
| 状态机 / 事务 / 幂等 / 并发 / 恢复 | 一致性 | P0 | 合法 / 非法转换、UoW、outbox、projection、commit unknown 成立 | 不测真实 DB 产品行为 |
| Projection / query / maintenance no-write | 派生 / 维护 | P0 | 查询、投影、对账、报告不反写真相 | 不测 workspace UI |
| 配置加载 / 失败 / 敏感输出 | 配置 | P0 | P0 profile、strict JSON、env、sensitive、adapter ref、outbox / handoff / replay 可验证 | 不测 config center / hot reload |
| Observability / audit / evidence | 证据 | P0 | safe log、metric、audit / trace / outbox / report 证据成立 | 不测真实 observability dashboard |
| Controlled integration seams | 集成接缝 | P1 | controlled resolver / publisher / handoff / staging-like dry-run | 不要求真实生产 endpoint |
| Production-like deployment | 生产化 | P2 | 后续专项验证 | 当前不进入 P0 |

### 2.3 P0 / P1 / P2 口径

| 优先级 | 必须形成什么 | 不得写成什么 |
|---|---|---|
| P0 | 可执行用例、断言点、测试数据、环境配置、自动化候选和证据 ID | 不得依赖真实生产 DB / MQ / KMS / config center |
| P1 | 接缝验证、dry-run、contract / compatibility test、风险记录 | 不得阻塞 P0 正式测试方案定稿 |
| P2 | 生产化专项、容量模型、remote config、hot reload、full runbook | 不得进入当前 P0 用例矩阵或验收硬门禁 |

### 2.4 对上游设计的影响判定

| 测试结论 | 是否影响上游设计 | 回写位置 | 处理状态 |
|---|---|---|---|
| P0 聚焦核心能力闭环、详细设计最小验证清单和配置 P0 profile | 否 | 无 | 无回写 |
| P1/P2 生产化、remote config、hot reload、真实依赖和容量模型只作后续专项 | 否 | 无 | 无回写 |
| 旧性能数字只作为候选专项目标，不在本文升级为硬阈值 | 否 | 无 | 无回写 |
| 下游 / 相邻仓只测接缝和边界，不测对方完整实现 | 否 | 无 | 无回写 |

---

## 3. 测试对象与测试切口

> 校准来源：
> - `design-calibration/05_test_plan_step_03_test_objects_cuts.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_03_test_objects_cuts.md` 的“结构化中间产物”“回填草稿”和“待确认事项”小节，了解测试对象和切口如何从详细设计抽取。

### 3.1 测试对象抽取原则

P0 测试对象必须能回指 `03-详细设计.md` 中的正式对象、协议、状态、错误、事务、配置或观测契约。DTO 构造失败、字段缺失、引用混同、forbidden body 和 no-write 都是负向测试切口。

正式状态名必须使用详细设计中的 enum variant。测试不得继续使用旧口语状态名或实现者临时字段名。

### 3.2 测试对象与测试切口总表

| 测试对象 | 测试切口 | 主要风险 | 推荐测试层级 |
|---|---|---|---|
| `Project` / project lifecycle | create、read-only、close、archive、implicit create reject | 项目主语不成立或被 workspace / process 吸收 | domain unit + command service |
| `ProjectMember` / responsibility | GlobalMemberRef、capability snapshot、Proposed / Active / Paused / Released | Work 接管 identity truth | domain unit + service |
| `Backlog` | empty backlog、open / maintenance lock / archive、formal work membership | Backlog 混入个人步骤或外部建议 | domain unit + integration |
| `WorkItem` / `ChildWorkItem` | formalize、child boundary、completion evidence、terminal reject | 正式工作全集被 runtime / conversation 污染 | domain unit + API / service |
| `PromoteResult` / `PromoteDecisionRecord` | request、accept、reject、supersede、created work ref | promote 绕过显式审查或保存 runtime 正文 | domain unit + service |
| `WorkDependency` / `WorkBlocker` | dependency edge、cycle reject、blocker open / resolve、evidence required | 依赖 / 阻塞不可解释或形成错误图 | domain unit + service |
| `Iteration` / `IterationCommitment` | timebox ref、candidate scope、commit、change、close / cancel | Iteration 变成 Backlog 全集或 process truth | domain unit + service |
| `WorkTraceRecord` / `WorkAuditTrail` | accepted change 写 trace / audit;invalid request 不写 business audit | 关键变化不可追溯或泄露正文 | domain + observability |
| `WorkOutboxRecord` | enqueue same UoW、publish state、failed marker、no rollback truth | 已成立事实不可传播或被错误回滚 | repository + worker |
| reference snapshots | resolved / unresolved / stale / failed、last good snapshot、no body | 相邻仓正文或假 truth 进入 Work | consumer + job |
| derived views / projections | stale、rebuilding、failed、query surface、rebuild no-write | 派生视图成为第二 truth | projection + query |
| contracts DTO / typed refs | roundtrip、必填字段、public DTO 传递类型、domain-only type absent | placeholder 或双真相 | contract unit |
| command services | UoW、idempotency、version conflict、outbox rollback、error mapping | 写路径半提交或重复写 truth | service integration |
| query services | hit、missing、not visible、stale / failed、no-write | 读路径修复或隐式创建 truth | service + handler |
| inbound consumers | envelope、dedup、dead-letter、snapshot / marker only | 外部事件直接创建 Work truth | worker integration |
| outbound events | payload schema、refs only、publish failure marker | 外部正文泄露或传播伪成功 | contract + publisher |
| operations jobs | batch、partial failure、rerun、report、no repair truth | 维护任务反写真相 | job runner |
| state matrix | 12 组合法 / 非法转换和正式 variant | 测试使用旧状态名或漏非法转换 | domain unit |
| transaction / consistency | truth + trace + outbox + projection stale + idempotency 同 UoW | accepted truth 与副作用不一致 | service + repository |
| idempotency / concurrency | duplicate、conflict、reserved in-flight、version conflict、commit unknown | 重复正式事实或盲重试 | service + fake UoW |
| configuration | defaults、strict JSON、env override、sensitive ref、forbidden boundary | 配置绕过核心边界或泄露 secret | config + integration |
| observability / redaction | low-cardinality metric、safe log、audit refs only、raw body absent | 证据不可复核或敏感泄露 | observability check |

### 3.3 协议测试对象检查表

```text
18 Command
  -> contract DTO + handler validation + service flow + idempotency
8 Query
  -> contract DTO + authorization + projection surface + no-write
7 Inbound Event Consumer
  -> envelope + dedup + source mapping + dead-letter / marker
9 Outbound Event
  -> outbox source + schema + forbidden field absent + publish failure
6 Operations Job
  -> job input + batch / retry + partial failure + report / rerun
```

### 3.4 状态 / 一致性 / 恢复切口

```text
Domain object method
  -> legal transition
  -> save truth in UoW
  -> append trace / audit
  -> enqueue outbox
  -> mark projection stale
  -> complete idempotency

Domain reject / metadata reject / version conflict
  -> no accepted truth
  -> no business trace
  -> no outbox
  -> stable error surface

External / publisher / handoff / projection failure
  -> explicit unresolved / failed / stale / report marker
  -> no fake success
  -> no rollback of accepted truth
```

### 3.5 对上游设计的影响判定

| 测试结论 | 是否影响上游设计 | 回写位置 | 处理状态 |
|---|---|---|---|
| P0 覆盖七模块、18 Command、8 Query、7 Consumer、10 Event、6 Job、12 状态机和配置 / 观测边界 | 否 | 无 | 无回写 |
| DTO 构造失败、字段缺失、引用混同、forbidden body 和 no-write 被列为负向切口 | 否 | 无 | 无回写 |
| 正式状态名必须使用 `03` enum variant | 否 | 无 | 无回写 |
| P1/P2 真实生产依赖、remote config、hot reload、容量模型不进入 P0 对象清单 | 否 | 无 | 无回写 |

---

## 4. 测试策略与分层

> 校准来源：
> - `design-calibration/05_test_plan_step_04_strategy_layers.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_04_strategy_layers.md` 的“结构化中间产物”“回填草稿”和“待确认事项”小节，了解测试层级选择依据。

### 4.1 分层原则

P0 风险优先在最低可验证层级发现。schema、状态、policy、digest 和 forbidden field 错误不得推给 E2E；E2E / release gate 只保留最小业务闭环、跨模块高风险链路和可归档证据。

### 4.2 测试分层图

图类型: 测试分层图

图标题: L1-work 测试金字塔

```text
[Unit tests]
  - contracts DTO / value object / domain object / policy / state machine
        |
        v
[Service tests]
  - application service + fake ports + UoW + idempotency + error mapping
        |
        v
[Integration tests]
  - repository / runtime builder / config / adapter / worker / job runner
        |
        v
[API / Worker contract tests]
  - HTTP / RPC / event envelope / outbox event / job contract
        |
        v
[E2E / Release gate]
  - minimal business closure + cross-module smoke + evidence archive
```

关键说明:

- Unit 层发现局部不变量、schema、状态和 policy 错误。
- Service 层验证编排、事务、幂等和错误映射。
- Integration 层验证 fake / in-memory / controlled adapter 的技术语义和配置装配。
- Release gate 证明最小主链和证据包可复核。

### 4.3 测试分层表

| 层级 | 目标 | 典型内容 | 执行时机 | 失败处理 |
|---|---|---|---|---|
| Unit | 发现局部不变量、schema、状态和 policy 错误 | DTO roundtrip、typed ref、domain object、policy、state transition、digest、forbidden field | PR / CI | 阻断 PR / CI |
| Service | 验证 application 编排、UoW、幂等、错误映射和 no-write | 18 Command service、8 Query service、consumer orchestration、job orchestration | PR / CI | 阻断 PR / CI |
| Integration | 验证 repository、adapter、runtime builder、config、worker、job runner 技术边界 | fake / in-memory repository、UnitOfWork、config loader、resolver / publisher / handoff、outbox worker、redaction check | CI / pre-release | 阻断 CI / release |
| API / Worker contract | 验证公开协议、metadata、envelope、receipt、event / job schema 和 error surface | command / query handler、event consumer envelope、outbound event schema、job input / report | CI / pre-release | 阻断 CI / release |
| E2E / Release gate | 证明最小业务闭环、跨模块高风险链路和证据归档可复核 | project -> member -> work -> iteration -> query / trace;promote;outbox / projection;config redline smoke | release gate / staging-like dry-run | 阻断 release |

### 4.4 层级阻断策略

| 风险类别 | 首选发现层级 | 是否 P0 阻断 | 说明 |
|---|---|---|---|
| schema / DTO / typed ref 缺失或不一致 | Unit / API contract | 是 | public surface 字段不闭合即阻断 |
| domain invariant / state transition 错误 | Unit | 是 | 不得靠 service / E2E 间接发现 |
| UoW / idempotency / error mapping 错误 | Service | 是 | 写路径核心一致性 |
| repository / adapter / config 装配错误 | Integration | 是 | P0 fake / in-memory / controlled adapter 必须可靠 |
| worker / job duplicate、partial failure、rerun 错误 | Integration + contract | 是 | 影响派生、outbox 和维护链路 |
| raw secret / raw body / high-cardinality leakage | Unit + integration + release gate | 是 | 安全和证据红线 |
| production DB / MQ / KMS / endpoint 真实联调失败 | P1/P2 专项 | 当前不阻断 P0 | 未进入 P0 配置字段全集 |
| performance candidate 未达旧草案数字 | Step 10 专项 | 当前不阻断 | 旧数字未升级为正式阈值 |

### 4.5 对上游设计的影响判定

| 测试结论 | 是否影响上游设计 | 回写位置 | 处理状态 |
|---|---|---|---|
| 测试策略按 unit / service / integration / API-worker contract / E2E-release gate 分层 | 否 | 无 | 无回写 |
| P0 风险必须在最低可验证层级优先发现 | 否 | 无 | 无回写 |
| 配置 loader / validator / sensitive output 进入 integration / release gate,但不新增配置字段 | 否 | 无 | 无回写 |
| production-like 真实依赖、remote config、hot reload 和旧性能数字不进入当前 P0 gate | 否 | 无 | 无回写 |

---

## 5. 需求追溯与覆盖矩阵

> 校准来源：
> - `design-calibration/05_test_plan_step_05_traceability_coverage.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_05_traceability_coverage.md` 的“结构化中间产物”“回填草稿”和“待确认事项”小节，了解 `FR / BR / AC -> TC / EV` 覆盖如何收敛。

### 5.1 覆盖原则与编号族

测试用例编号使用 `TC-WORK-<族>-<三位序号>`，测试证据编号使用 `EV-WORK-<族>-<三位序号>`。

当前 P0 编号族：

```text
CORE / MEMBER / FORMAL / PROMOTE / DEP / ITER / QUERY / OPS / CFG / NFR
```

### 5.2 P0 功能需求覆盖矩阵

| 需求 / 规则 ID | 设计依据 | 测试场景 | 用例 ID | 自动化 | 证据 ID | 覆盖状态 |
|---|---|---|---|---|---|---|
| `FR-WORK-001`;`BR-WORK-001/012/025/026`;`AC-WORK-001/006/014/016/018/019` | `02` §5~§9;`03` §6~§10 / §15 | Project 显式创建、引用、生命周期、trace / audit / outbox;隐式创建拒绝 | `TC-WORK-CORE-001`~`004` | 是 | `EV-WORK-CORE-001`~`004` | 已覆盖 |
| `FR-WORK-002`;`BR-WORK-002/013/017/025/026`;`AC-WORK-002/007/014/016/017/018` | `02` §5~§9;`03` §6~§9 / §11 / §15 | ProjectMember 显式承担、capability snapshot、identity 边界、责任状态变化 | `TC-WORK-MEMBER-001`~`004` | 是 | `EV-WORK-MEMBER-001`~`004` | 已覆盖 |
| `FR-WORK-003`;`BR-WORK-003/007/008/009/010/014/026`;`AC-WORK-003/008/014/015/016/020` | `02` §5~§10;`03` §6~§11 / §15 | Backlog / WorkItem 正式全集、边界外输入拒绝、maintenance lock、forbidden body absent | `TC-WORK-FORMAL-001`~`005` | 是 | `EV-WORK-FORMAL-001`~`005` | 已覆盖 |
| `FR-WORK-004`;`BR-WORK-004/008/015/022/023/025/026`;`AC-WORK-003/009/015/016/017/020` | `02` §6~§10;`03` §6~§9 / §12 / §15 | child WorkItem、runtime promote intake、review accept / reject、ImplementationPlan / runtime 正文拒绝 | `TC-WORK-PROMOTE-001`~`005` | 是 | `EV-WORK-PROMOTE-001`~`005` | 已覆盖 |
| `FR-WORK-005`;`BR-WORK-003/014/026/027`;`AC-WORK-010/019/020/027/028` | `02` §5~§9;`03` §6~§10 / §15 | dependency link、cycle reject、blocker open / resolve、evidence refs、audit / trace | `TC-WORK-DEP-001`~`005` | 是 | `EV-WORK-DEP-001`~`005` | 已覆盖 |
| `FR-WORK-006`;`BR-WORK-005/016/020/026`;`AC-WORK-004/011/014/016/017/020` | `02` §5~§9;`03` §6~§10 / §15 | Iteration open、commit formal scope、update commitment、close / cancel、process ref boundary | `TC-WORK-ITER-001`~`005` | 是 | `EV-WORK-ITER-001`~`005` | 已覆盖 |
| `FR-WORK-007`;`BR-WORK-006/011/017~024/026`;`AC-WORK-005/012/017/019/021/022/023/027` | `02` §5~§10;`03` §6~§8 / §11 / §14 / §15 | 8 Query、authorized read、not visible / degraded、trace page、projection stale / failed、query no-write | `TC-WORK-QUERY-001`~`008` | 是 | `EV-WORK-QUERY-001`~`008` | 已覆盖 |
| `FR-WORK-008`;`BR-WORK-006/011/027`;`AC-WORK-013/015/019/021/025/029` | `02` §7 / §8 / §10 / §11;`03` §7 / §8 / §10 / §11 / §14 / §15;`04` §11 / §12 | projection rebuild no-write、reference refresh marker、reconciliation read-only report、outbox / handoff failed marker | `TC-WORK-OPS-001`~`006` | 是 | `EV-WORK-OPS-001`~`006` | 已覆盖 |

### 5.3 规则族覆盖矩阵

| 规则族 | 包含规则 | 覆盖场景族 | 首选层级 | 自动化 | 覆盖状态 |
|---|---|---|---|---|---|
| 不变量 | `BR-WORK-001`~`006` | Project、ProjectMember、Backlog、WorkItem、Iteration、projection no-write | Unit + Service | 是 | 已覆盖 |
| 禁止行为 | `BR-WORK-007`~`011` | conversation / runtime / process / adjacent repo direct write reject;query / projection / report no-write | Unit + Service + Contract | 是 | 已覆盖 |
| 显式变化 | `BR-WORK-012`~`016` | create / assign / formalize / promote / commit scope 必须走 command 或 consumer intake + review | Service + API contract | 是 | 已覆盖 |
| 相邻仓边界 | `BR-WORK-017`~`024` | identity / conversation / method / process / governance / artifact / runtime / workspace ref-only 和 no body | Contract + Integration + E2E smoke | 是 | 已覆盖 |
| 治理约束 | `BR-WORK-025` | high-risk lifecycle / split / promote / tool capability 只引用正式约束，不绕过 policy | Unit + Service | 是 | 已覆盖 |
| 审计约束 | `BR-WORK-026`~`027` | trace / audit / outbox / report / evidence refs | Service + Integration + Release gate | 是 | 已覆盖 |

### 5.4 验收项覆盖矩阵

| 验收 ID | 覆盖来源 | 测试场景族 | 证据 ID | 覆盖状态 |
|---|---|---|---|---|
| `AC-WORK-001`~`005` | 核心能力闭环 C-1~C-5 | `CORE`、`MEMBER`、`FORMAL`、`ITER`、`QUERY` smoke 和负向 | `EV-WORK-CORE-*`、`EV-WORK-MEMBER-*`、`EV-WORK-FORMAL-*`、`EV-WORK-ITER-*`、`EV-WORK-QUERY-*` | 已覆盖 |
| `AC-WORK-006`~`013` | `FR-WORK-001`~`008` | P0 功能需求覆盖矩阵全部场景族 | `EV-WORK-CORE-*`~`EV-WORK-OPS-*` | 已覆盖 |
| `AC-WORK-014`~`019` | `BR-WORK-001`~`027` | 不变量、禁止行为、显式变化、边界、治理、审计规则族 | `EV-WORK-CORE-*`、`EV-WORK-FORMAL-*`、`EV-WORK-PROMOTE-*`、`EV-WORK-QUERY-*`、`EV-WORK-OPS-*` | 已覆盖 |
| `AC-WORK-020`~`023` | 数据归属和外部正文禁止 | Work truth ownership、external snapshot / ref-only、forbidden body absent | `EV-WORK-FORMAL-*`、`EV-WORK-PROMOTE-*`、`EV-WORK-QUERY-*`、`EV-WORK-CFG-*` | 已覆盖 |
| `AC-WORK-024` | 性能判断口径 | 旧性能候选专项评估，当前不设硬阈值 | `EV-WORK-NFR-001` | 专项承接 |
| `AC-WORK-025` | 可用性判断口径 | external unavailable marker、projection stale / failed、core closure unaffected | `EV-WORK-OPS-*`、`EV-WORK-NFR-002` | 已覆盖 |
| `AC-WORK-026` | 安全判断口径 | authorization、forbidden body、sensitive output、high-risk policy | `EV-WORK-CFG-*`、`EV-WORK-NFR-003` | 已覆盖 |
| `AC-WORK-027` | 审计 / 可追溯判断口径 | trace / audit / evidence refs、blocker / completion / reconciliation explainability | `EV-WORK-DEP-*`、`EV-WORK-QUERY-*`、`EV-WORK-OPS-*` | 已覆盖 |
| `AC-WORK-028` | 幂等 / 一致性判断口径 | duplicate / conflict、version conflict、commit unknown、projection lag explainable | `EV-WORK-NFR-004` | 已覆盖 |
| `AC-WORK-029` | 可观测性判断口径 | low-cardinality metric、safe log、maintenance status visible | `EV-WORK-NFR-005` | 已覆盖 |

### 5.5 未覆盖项清单

| 需求 / 规则 ID | 未覆盖原因 | 处理方式 | 风险状态 |
|---|---|---|---|
| `FR-WORK-E01` | 外围增强，不属于当前 P0 核心闭环 | §14 记录后续专项 | 非阻塞 |
| `FR-WORK-E02` | 外围增强，自动建议不得替代正式规则 | §14 记录后续专项 | 非阻塞 |
| `FR-WORK-E03` | 容量趋势和旧性能数字需专项评估 | §10 / §14 承接 | 非阻塞 |
| `FR-WORK-E04` | 依赖治理 / 方法定义后续闭环 | §14 记录后续专项 | 非阻塞 |
| `FR-WORK-E05` | 跨项目依赖为外围分析能力 | §14 记录后续专项 | 非阻塞 |

当前没有未覆盖的 P0 功能需求、P0 业务规则或 P0 验收方向。

### 5.6 对上游设计的影响判定

| 测试结论 | 是否影响上游设计 | 回写位置 | 处理状态 |
|---|---|---|---|
| `FR-WORK-001`~`008`、`BR-WORK-001`~`027`、`AC-WORK-001`~`029` 均有测试覆盖或专项承接 | 否 | 无 | 无回写 |
| 外围增强 `FR-WORK-E01`~`E05` 不进入 P0 硬覆盖，进入后续专项和风险 | 否 | 无 | 无回写 |
| `AC-WORK-024` 性能判断口径进入 `NFR` 专项，不在本文升级硬阈值 | 否 | 无 | 无回写 |
| 配置 profile、loader、validator、sensitive output、unsupported hot reload 进入 `CFG` 覆盖族 | 否 | 无 | 无回写 |

---

## 6. 测试场景与用例设计

> 校准来源：
> - `design-calibration/05_test_plan_step_06_cases_matrix.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_06_cases_matrix.md` 的“结构化中间产物”“回填草稿”和“待确认事项”小节，了解 P0 用例矩阵和设计契约断言。

### 6.1 用例族总览

| 用例族 | 覆盖范围 | 用例 ID | 证据 ID |
|---|---|---|---|
| `CORE` | Project 显式创建、生命周期、隐式创建拒绝、幂等 duplicate | `TC-WORK-CORE-001`~`004` | `EV-WORK-CORE-001`~`004` |
| `MEMBER` | ProjectMember 承担、identity boundary、责任状态 | `TC-WORK-MEMBER-001`~`004` | `EV-WORK-MEMBER-001`~`004` |
| `FORMAL` | Backlog、WorkItem、child WorkItem、forbidden body | `TC-WORK-FORMAL-001`~`005` | `EV-WORK-FORMAL-001`~`005` |
| `PROMOTE` | Request / Review Work promotion、runtime body reject、version conflict | `TC-WORK-PROMOTE-001`~`005` | `EV-WORK-PROMOTE-001`~`005` |
| `DEP` | dependency、cycle reject、blocker、evidence | `TC-WORK-DEP-001`~`005` | `EV-WORK-DEP-001`~`005` |
| `ITER` | Iteration open、commit、change、close / cancel | `TC-WORK-ITER-001`~`005` | `EV-WORK-ITER-001`~`005` |
| `QUERY` | 8 Query hit / missing / not visible / stale / failed / no-write | `TC-WORK-QUERY-001`~`008` | `EV-WORK-QUERY-001`~`008` |
| `OPS` | outbox、projection rebuild、reference refresh、reconciliation、handoff | `TC-WORK-OPS-001`~`006` | `EV-WORK-OPS-001`~`006` |
| `CFG` | profile、loader、validation、sensitive、adapter ref、unsupported hot reload | `TC-WORK-CFG-001`~`017` | `EV-WORK-CFG-001`~`017` |
| `NFR` | 性能观察、可用性、安全、幂等、可观测性 | `TC-WORK-NFR-001`~`005` | `EV-WORK-NFR-001`~`005` |

### 6.2 Command 主线用例

| 用例 ID | 场景 | 预期结果 | 关键断言 |
|---|---|---|---|
| `TC-WORK-CORE-001` | 显式创建 Project 和 Backlog | Project Active、Backlog Open、enqueue `ProjectChanged` | Project、Backlog、trace、audit、outbox、projection stale、command result save、idempotency complete 同 UoW |
| `TC-WORK-CORE-002` | 隐式创建 Project 被拒绝 | 返回 `Missing` 或 `NotFound` | 无 Project / Backlog / outbox / trace |
| `TC-WORK-CORE-003` | Project lifecycle 关闭和归档 | Project Closed / Archived，Backlog Archived | version 匹配，projection stale |
| `TC-WORK-CORE-004` | CreateProject 幂等 duplicate | 通过 `CommandResultRepository.get_result` 返回既有 result | 无新 Project、Backlog、trace、audit、outbox;receipt 标记 duplicate |
| `TC-WORK-MEMBER-001` | AssignProjectMember 成功 | 保存 ProjectMember，enqueue `ProjectMemberChanged` | snapshot ref 保存，member / project views stale |
| `TC-WORK-MEMBER-002` | identity resolver unavailable / unresolved | retry surface 或 unresolved | 不保存 accepted member truth |
| `TC-WORK-MEMBER-003` | 拒绝接管 identity truth / body | `WorkTruthPolicy` reject | 不保存 identity body，report 无 raw body |
| `TC-WORK-MEMBER-004` | Released 后恢复责任被拒绝 | `DomainRejected` | member version 不变，无 outbox |
| `TC-WORK-FORMAL-001` | 创建正式 WorkItem | WorkItem Formalized，加入 Backlog | source ref、trace、audit、outbox、views stale |
| `TC-WORK-FORMAL-002` | 外部事件直写 Work truth 被拒绝 | 不创建 WorkItem / ChildWorkItem | 只允许 reference state、pending intake 或 marker |
| `TC-WORK-FORMAL-003` | Backlog maintenance lock 拒绝新增 WorkItem | `DomainRejected` | 无 WorkItem、membership、trace、outbox |
| `TC-WORK-FORMAL-004` | forbidden body absent | 只保存 ref / digest / summary | repository、event、audit、report 无 raw payload / ImplementationPlan body |
| `TC-WORK-FORMAL-005` | ChildWorkItem 创建与非法 parent | 合法 root parent 成功，非法 parent reject | 非法路径无 truth / outbox |
| `TC-WORK-PROMOTE-001` | 请求 Work promotion | `PromoteResultState::PendingReview` | 不创建 Work truth，trace / audit / outbox 成立 |
| `TC-WORK-PROMOTE-002` | ReviewWorkPromotion accept | `Accepted`，创建或绑定 Formalized WorkItem | promote decision、WorkItem、outbox 同 UoW |
| `TC-WORK-PROMOTE-003` | ReviewWorkPromotion reject | `Rejected`，不创建 WorkItem | decision record 与 outbox 成立 |
| `TC-WORK-PROMOTE-004` | runtime / ImplementationPlan 正文拒绝 | reject / unresolved / pending intake | 不保存 raw runtime body |
| `TC-WORK-PROMOTE-005` | 并发 review 版本冲突 | single-winner，loser `VersionConflict` | losing path 无 trace / outbox / WorkItem |
| `TC-WORK-DEP-001` | LinkWorkDependency 成功 | dependency Proposed 或 Active | edge、history、trace、audit、outbox;graph scope 由 downstream `FormalWorkScope` 提供;project-board + resolvable member-work stale |
| `TC-WORK-DEP-002` | dependency cycle reject | `DomainRejected` | 无 dependency、history、trace、outbox |
| `TC-WORK-DEP-003` | UpdateWorkDependencyState | `Proposed -> Active` 和 `Active -> Satisfied / Waived / Cancelled` 合法推进 | Active 要求 `Activated` reason;Satisfied 要求 verified evidence;reason kind mismatch reject;terminal 后不能 reopen;downstream relation views stale |
| `TC-WORK-DEP-004` | OpenWorkBlocker 成功 | `BlockerState::Open` | blocker ref、cause ref、history、trace、outbox;blocked work scope resolved;blocked project-board + resolvable member-work stale |
| `TC-WORK-DEP-005` | ResolveWorkBlocker 与 evidence reject | verified -> Resolved 且写入 `resolved_evidence_ref`;missing / rejected -> reject | success event evidence 来自 blocker truth;failure 无 truth / outbox，无 evidence body;success marks blocked relation views stale |
| `TC-WORK-ITER-001` | OpenIteration 成功 | `IterationState::Planning` | timebox ref 不改 process truth |
| `TC-WORK-ITER-002` | CommitIterationScope 成功 | Iteration Committed，Commitment Committed，work Committed | iteration、commitment、work marks、trace、outbox 同 UoW |
| `TC-WORK-ITER-003` | 非 formal candidate 拒绝 commit | `DomainRejected` 或 `InvalidRequest` | Iteration 仍 Planning，无 commitment / outbox |
| `TC-WORK-ITER-004` | UpdateIterationCommitment 成功 | `CommitmentState::Changed` | change record、projection stale、version 匹配 |
| `TC-WORK-ITER-005` | Iteration close / cancel 与非法 reopen | close / cancel 成功，非法 reopen reject | 非法路径无 truth / outbox / stale marker |

### 6.3 Query 用例

| 用例 ID | 场景 | 预期结果 | 关键断言 |
|---|---|---|---|
| `TC-WORK-QUERY-001` | GetProjectWorkFacts hit / missing / not visible | visible view、`Missing` 或 `NotVisible` | Query no-write |
| `TC-WORK-QUERY-002` | GetBacklog page / empty | `BacklogView` 或 visible empty surface | page token / item refs 正确 |
| `TC-WORK-QUERY-003` | GetWorkItem visible / terminal surfaced | 返回 `WorkItemView` | terminal state 正常可见，无 external body |
| `TC-WORK-QUERY-004` | ListMemberWork projection stale / failed | stale / rebuilding / failed surface | 不触发 rebuild |
| `TC-WORK-QUERY-005` | GetIterationSummary projection surface | present、missing 或 stale | 不写 projection state |
| `TC-WORK-QUERY-006` | SearchWork criteria / failed projection / no body copy | result 或 failed surface | 结果只含 Work refs / safe summary |
| `TC-WORK-QUERY-007` | GetWorkTrace page / empty / not visible | records、`Empty` 或 `NotVisible` | page token、trace subject、no-write |
| `TC-WORK-QUERY-008` | GetProjectBoardView board / rebuilding | board 或 rebuilding / missing | 不 enqueue `DerivedWorkViewChanged` |

### 6.4 Consumer / Outbound Event 覆盖

Consumer 和 outbound event 不新增独立编号族；它们由既有 `TC-WORK-*` 用例覆盖并留同族证据。

| 协议对象 | 覆盖用例 | 预期结果 | 关键断言 |
|---|---|---|---|
| `ConsumeIdentityMemberChanged` | `MEMBER-001/002` | 保存 capability snapshot / reference state 或 dead-letter;通过正式 affected-view 读取面标记既有 public views stale | duplicate 不重复 snapshot;no affected public views 不生成 stale marker |
| `ConsumeMethodDefinitionChanged` | `FORMAL-002` / `QUERY-006` | 保存 method snapshot / reference state;通过正式 affected-view 读取面 mark views stale | 不改 Work truth;no affected public views 不生成 stale marker |
| `ConsumeConversationWorkContextChanged` | `FORMAL-002` / `PROMOTE-001` | 保存 source reference / pending source marker | digest mismatch -> unresolved |
| `ConsumeProcessTimingChanged` | `ITER-001` / `QUERY-005` | 保存 timebox reference state | 不 open / close Iteration |
| `ConsumeGovernanceDecisionChanged` | `DEP-003/005` | 保存 source / evidence reference state | missing -> dead-letter，不生成 governance truth |
| `ConsumeArtifactEvidenceChanged` | `DEP-005` / `FORMAL-004` | verified -> resolved，rejected -> stale / failed marker | 不直接 complete work |
| `ConsumeRuntimePromoteRequested` | `PROMOTE-001/004` | 保存 source reference 和 pending promote intake | 不直接 evaluate promote |
| outbound events | 相关 `CORE` / `MEMBER` / `FORMAL` / `PROMOTE` / `DEP` / `ITER` / `OPS` 用例 | payload 来自 Work truth 或 marker | 只含 refs / state / safe summary，无正文 |

### 6.5 Operations Job 用例

| 用例 ID | 场景 | 预期结果 | 关键断言 |
|---|---|---|---|
| `TC-WORK-OPS-001` | PublishWorkOutbox success / partial failure | success -> Published，failure -> Failed | 不改业务 truth |
| `TC-WORK-OPS-002` | RebuildWorkProjections | Rebuilding -> Fresh | projection 来自 committed truth，不反推 truth |
| `TC-WORK-OPS-003` | RefreshExternalReferenceSnapshots | success 更新 snapshot，failure 写 marker | last good snapshot preserved |
| `TC-WORK-OPS-004` | RunWorkReconciliation read-only report | 生成 report | 不自动修复 truth / projection / outbox |
| `TC-WORK-OPS-005` | PrepareWorkTraceHandoff | success 写 handoff marker，failure 写 failed refs | 无 observability raw body |
| `TC-WORK-OPS-006` | PrepareArchiveHandoff | success 写 archive marker，failure 进入 report | 不写 archive long-term body，rerun 幂等 |

### 6.6 配置 / NFR 用例

| 用例族 | 覆盖内容 | 用例 ID |
|---|---|---|
| P0 profile | `local-dev`、`ci-test`、`integration-like`、`operations-replay` | `TC-WORK-CFG-001`~`004` |
| loader / validation | defaults、JSON、env、malformed、unknown key、typed validation、cross-field validation | `TC-WORK-CFG-005`~`009` |
| sensitive / forbidden output | raw secret、raw token、raw payload、source body、redacted output | `TC-WORK-CFG-010`~`012` |
| adapter / failure marker | configured adapter missing ref、resolver unavailable、publisher failure、handoff failure | `TC-WORK-CFG-013`~`016` |
| unsupported feature | config center、admin override、hot reload、last-known-good 自动吞错 | `TC-WORK-CFG-017` |
| NFR | performance observation、availability、security、idempotency、observability | `TC-WORK-NFR-001`~`005` |

### 6.7 状态 / 事务 / 幂等 / 恢复断言

| 测试切口 | 覆盖用例 | 关键断言 |
|---|---|---|
| 非法状态迁移 | `CORE-003`、`MEMBER-004`、`FORMAL-003/005`、`PROMOTE-005`、`DEP-003/005`、`ITER-005` | `DomainError::InvalidStateTransition` 或对应 reject surface，业务 truth 和 outbox 不变 |
| 幂等 duplicate | `CORE-004` | same key same digest 读取 stored result surface，无新 truth / trace / outbox |
| 幂等 conflict | `NFR-004` | same key different digest -> `IdempotencyConflict`，无业务副作用 |
| version conflict | `PROMOTE-005` | single-winner，losing path 无 WorkItem / outbox |
| same UoW | `ITER-002` | iteration、commitment、work marks、outbox、trace、idempotency complete 一起提交 |
| rollback | `DEP-002` | cycle detected 后 no dependency history、no outbox |
| recovery | `OPS-001`~`006` | failed marker / rerun / last good snapshot / no truth repair |

### 6.8 对上游设计的影响判定

| 测试结论 | 是否影响上游设计 | 回写位置 | 处理状态 |
|---|---|---|---|
| P0 用例矩阵承接 Step 5 覆盖场景族，不新增协议或字段 | 否 | 无 | 无回写 |
| Consumer / outbound event 不新增独立编号族，由同族用例承接 | 否 | 无 | 无回写 |
| 当前不把未开放 public 写入口的 domain state 写成 public command 用例 | 否 | 无 | 无回写 |
| `TC-WORK-NFR-001` 不把旧性能数字写成硬门槛 | 否 | 无 | 无回写 |

---

## 7. 测试数据设计

> 校准来源：
> - `design-calibration/05_test_plan_step_07_test_data.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_07_test_data.md` 的“结构化中间产物”“回填草稿”和“待确认事项”小节，了解 fixture、builder、seed 和清理规则。

### 7.1 命名与隔离规则

| 规则 | 口径 |
|---|---|
| 测试运行隔离键 | `test_run_id`，只用于测试命名空间和临时目录，不进入 Work domain / DTO / config |
| deterministic id prefix | `work-test-{test_run_id}-{dataset}-{sequence}` |
| deterministic time | 每个用例从固定 `Timestamp` 起步，按操作顺序递增 |
| deterministic metadata | Command / Query / Job metadata 使用 run-scoped request id、actor ref、trace context ref 和 idempotency key |
| fake adapter seed | ref -> outcome map 注入 success / unresolved / unavailable / body-leak / failure |
| event replay seed | inbound event 使用 run-scoped source_event_id、topic、source_ref、payload digest |
| forbidden body sentinel | 用明显 sentinel 验证 repository、event、log、report 均不存在 forbidden body |
| cleanup scope | 每个用例默认独立 store；共享 fixture 只允许只读 seed |

### 7.2 测试数据集

| 数据集 | 用途 | 关联用例 |
|---|---|---|
| `DS-WORK-RUN-BASE` | deterministic clock / id / metadata / actor 基线 | 全部 P0 |
| `DS-WORK-PROJECT-BASE` | Project + Backlog 正向主线 | `CORE-001`、`FORMAL-*`、`ITER-*`、`QUERY-*` |
| `DS-WORK-PROJECT-ARCHIVE` | lifecycle close / archive 和非法 reopen | `CORE-003`、状态非法迁移 |
| `DS-WORK-MEMBER-CAPABILITY` | 成员承担成功 | `MEMBER-001`、`FORMAL-001` |
| `DS-WORK-MEMBER-UNRESOLVED` | identity unresolved / unavailable / body leak | `MEMBER-002/003`、`NFR-003` |
| `DS-WORK-BACKLOG-WORK` | WorkItem / ChildWorkItem 正向主线 | `FORMAL-001/005`、`QUERY-002/003` |
| `DS-WORK-WORK-BOUNDARY` | locked backlog、terminal work、invalid parent、body 禁止 | `FORMAL-002`~`005` |
| `DS-WORK-PROMOTE` | promote request / review accept / reject | `PROMOTE-001`~`003` |
| `DS-WORK-PROMOTE-BOUNDARY` | runtime body reject、duplicate source、concurrent review | `PROMOTE-004/005`、`NFR-004` |
| `DS-WORK-DEPENDENCY-GRAPH` | dependency link、cycle、state update | `DEP-001`~`003` |
| `DS-WORK-BLOCKER-EVIDENCE` | blocker open / resolve / evidence reject | `DEP-004/005` |
| `DS-WORK-ITERATION` | open / commit / update / close iteration | `ITER-001`~`005` |
| `DS-WORK-QUERY-VIEWS` | 8 Query hit / missing / stale / failed / no-write | `QUERY-001`~`008` |
| `DS-WORK-REFERENCE-STATES` | reference resolved / unresolved / stale / failed | `OPS-003`、`CFG-014`、`NFR-002` |
| `DS-WORK-OUTBOX` | outbox publish success / failure / concurrency | `OPS-001` |
| `DS-WORK-PROJECTION-REBUILD` | projection rebuild success / failure / query surface | `OPS-002`、`QUERY-004/005/008` |
| `DS-WORK-TRACE-HANDOFF` | trace handoff / archive handoff | `OPS-005/006`、`QUERY-007` |
| `DS-WORK-RECONCILIATION` | reconciliation clean / gap report | `OPS-004` |
| `DS-WORK-CONSUMER-EVENTS` | 7 inbound consumer success / dead-letter / duplicate | Consumer coverage matrix |
| `DS-WORK-IDEMPOTENCY` | duplicate / conflict / in-flight / commit unknown | `CORE-004`、`NFR-004` |
| `DS-WORK-CONCURRENCY` | version conflict and single-winner race | `PROMOTE-005`、`ITER-005`、`OPS-001` |
| `DS-WORK-CONFIG-PROFILE` | P0 profile loading | `CFG-001`~`004` |
| `DS-WORK-CONFIG-INVALID` | malformed / unknown / invalid / cross-field config | `CFG-005`~`009`、`CFG-017` |
| `DS-WORK-SENSITIVE-OUTPUT` | raw secret / payload / redaction gate | `CFG-010`~`012`、`NFR-003/005` |
| `DS-WORK-OPS-RECOVERY` | replay / failed marker / rerun | `OPS-*`、`CFG-004/015/016`、`NFR-002/004` |
| `DS-WORK-NFR-OBS` | metric / log / audit / trace boundary | `NFR-002`~`005` |

### 7.3 fixture / builder / seed 规则

| 规则 | 要求 | 禁止事项 |
|---|---|---|
| builder 输入 | 只能接收正式 DTO / ref / state / metadata 字段，或测试隔离参数 `test_run_id` | 不得添加 domain-only 隐式字段 |
| builder 输出 | 输出正式对象、request / event / job input，或 fake adapter seed | 不得输出 production-only 配置或真实 secret |
| deterministic id / time | 使用 fixed generator 和 fixed clock | 不得用随机数或真实系统时间影响 P0 断言 |
| version seed | 并发测试显式 seed version pair | 不得靠真实线程竞态制造唯一证据 |
| request digest | duplicate / conflict 数据显式区分 stable payload 和 volatile metadata | 不得把 request_id / trace / requested_at 写入 digest 断言 |
| fake resolver outcome | 每个 ref 明确 success / unresolved / unavailable / rejected / body-leak | fake success 不得伪装 configured success |
| event seed | inbound event 有 topic、source_event_id、source_ref、payload digest 和 trace context | 不得省略 dedup 所需字段 |
| forbidden output | sentinel 后必须扫描 repository、event、log、audit、report、artifact | 不得只断言 command reject |
| shared seed | 只读基础 seed 可复用；可变状态每个用例独立实例 | 不得跨用例共享 mutable store |

### 7.4 外部依赖测试替身

| 外部依赖 | P0 协作方式 | seed outcome |
|---|---|---|
| identity member | `FakeMemberReferencePort` | capability success、unresolved、unavailable、body-leak |
| method definition | `FakeMethodDefinitionResolverPort` | definition success、missing、unavailable |
| source work / conversation / runtime | `FakeSourceWorkResolverPort` | summary success、unresolved、rejected、body-leak |
| artifact / governance evidence | `FakeEvidenceResolverPort` | verified、rejected、unverified、missing |
| process timebox | `FakeProcessTimeboxResolverPort` | resolved、missing、unavailable |
| outbox publisher | `FakeWorkOutboxPublisher` | publication ref success、publish failure、timeout |
| trace handoff | `FakeTraceHandoffAdapter` | handoff ref success、failure / timeout |
| archive handoff | `FakeArchiveHandoffAdapter` | archive ref success、failure / timeout |
| store / UoW | `InMemoryWorkStores` + `InMemoryUnitOfWork` | commit、rollback、version conflict、commit unknown |

### 7.5 数据隔离与清理

| 数据类型 | 隔离方式 | 清理方式 |
|---|---|---|
| in-memory truth / projection / reference / outbox / idempotency store | 每个用例独立 store instance 或 run-scoped namespace | drop store instance |
| temp config / report / replay root | `test_run_id` 子目录 | 删除 temp dir |
| fake resolver / publisher / handoff queues | 每个用例独立 outcome map | reset fake map / captured calls |
| captured log / metric / audit output | run-scoped sink | 清空 sink |
| event dedup records | event key prefix = topic + source_event_id + source_ref | drop idempotency / dedup store |
| env overrides | key prefix 或 test process scoped env | 用例后 clear env |
| controlled configured adapter refs | integration-like profile 下 ref-only sample | reset configured local adapter state |
| operations-replay historical seed | 脱敏 replay bundle + run id | 删除 replay temp root |

### 7.6 不提前写入的数据范围

| 不提前写入项 | 原因 | 后续承接 |
|---|---|---|
| production-like durable DB / MQ seed | P1/P2，不属于 P0 测试数据事实源 | §8 / §14 / 运维材料 |
| real secret material / KMS / Vault sample | `04` 只允许 ref-only sensitive | 安全运维专项 |
| config center / admin override / hot reload 数据 | P0 unsupported | 范围变更先回写 `04` |
| 外围增强 `FR-WORK-E01`~`E05` 数据 | 不进入 P0 硬覆盖 | §14 |
| 验收 pass / fail evidence bundle | `06-验收标准.md` 职责 | §13 和新版 `06` |
| 具体测试函数名 / CI job name | `07-实施计划.md` 和实现仓职责 | §9 / `07` |

### 7.7 对上游设计的影响判定

| 测试结论 | 是否影响上游设计 | 回写位置 | 处理状态 |
|---|---|---|---|
| P0 用例前置条件可由 run-scoped fixture / builder / fake seed 满足 | 否 | 无 | 无回写 |
| `test_run_id` 仅作为测试隔离键，不进入 Work domain / DTO / config | 否 | 无 | 无回写 |
| P0 使用 in-memory store、deterministic fake 和 controlled adapter ref，不要求真实生产 endpoint | 否 | 无 | 无回写 |
| forbidden body / sensitive output 通过 sentinel + scan 数据集验证 | 否 | 无 | 无回写 |

---

## 8. 测试环境与配置矩阵

> 校准来源：
> - `design-calibration/05_test_plan_step_08_environment_config.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_08_environment_config.md` 的“结构化中间产物”“回填草稿”和“待确认事项”小节，了解 profile、依赖类型和环境不可用口径。

### 8.1 环境矩阵

| 环境 | 用途 | 依赖服务 | 协作方式 | 数据策略 | 风险 |
|---|---|---|---|---|---|
| `local-dev` | 本地 API / worker / job smoke，验证默认可运行路径 | 本地进程、in-memory store、fake resolver / publisher / handoff | fake adapter + deterministic clock / id | 最小 `CORE` / `FORMAL` / `QUERY` / `OPS` smoke | fake success 不代表验收 |
| `ci-test` | 自动化 P0 门禁 | temp dir、in-memory stores、deterministic fake adapters、captured logs / reports | unit / service / contract / integration fake | 全部 P0 `DS-WORK-*` | 并行隔离和 env cleanup 必须通过 |
| `integration-like` | configured adapter ref、external unavailable 和 fake marker 区分 | controlled local / configured resolver、publisher、handoff、store refs | configured adapter dry-run，不 silent fallback | config profile / invalid / recovery seed | configured adapter 不可用必须 explicit marker 或 fail-fast |
| `operations-replay` | outbox、projection、reference refresh、handoff、reconciliation、cleanup 和 replay 幂等 | 脱敏历史状态、snapshot、本地 report root、replay job args | replay bundle + event / job replay | recovery、reconciliation、outbox、projection、reference、handoff seed | raw secret / body 禁止，baseline mismatch fail-fast |
| `staging-like` | P1 跨仓集成和部署配置 dry-run | real-like DB、event bus、resolver、handoff、secret provider | 后续 controlled real-like / dry-run | 后续专项数据 | 不阻塞 P0 |
| `production-like` | P1/P2 生产运行和运维语境 | real durable store、event bus、source adapters、handoff、secret provider | 后续部署与运维手册承接 | 后续生产化专项 | 不能预支 DB / MQ / KMS / endpoint 字段全集 |

### 8.2 依赖类型与测试协作方式

| 依赖 / 协作对象 | 类型 | 是否允许 path dependency | P0 测试协作方式 |
|---|---|---|---|
| `core-contracts` | `[compile]` | 是 | path dependency |
| L1-identity | `[runtime]` / `[event]` | 否 | fake member resolver、controlled adapter、identity event replay |
| L1-conversation | `[runtime]` / `[event]` | 否 | fake source resolver、conversation source ref、event replay |
| L3-method-library | `[runtime]` / `[event]` | 否 | fake method definition resolver、method event replay |
| L1-process | `[runtime]` / `[event]` | 否 | fake timebox resolver、process event replay |
| L1-governance | `[runtime]` / `[event]` | 否 | fake evidence resolver、governance event replay |
| L1-artifact | `[runtime]` / `[event]` | 否 | fake artifact evidence resolver、artifact event replay |
| L2-runtime | `[runtime]` / `[event]` | 否 | fake runtime result resolver、runtime event replay |
| L0-bus | `[event]` | 否 | fake publisher、event envelope fixture、event replay |
| L4-observability | `[runtime]` | 否 | fake trace handoff adapter、controlled handoff ref |
| L4-archive | `[runtime]` | 否 | fake archive handoff adapter、controlled archive ref |
| Work store / projection / idempotency / outbox | `[runtime]` | 不适用 | in-memory store、temp dir、controlled failure injection |
| clock / id generator | `[runtime]` | 不适用 | deterministic fake |

### 8.3 配置矩阵

| 配置 section | 影响的测试结果 | P0 口径 |
|---|---|---|
| `store.*` | truth / projection / idempotency / outbox 持久化、rollback、version conflict | local / CI 使用 in-memory + isolated temp |
| `boundary.*` | command body limit、query timeout、page limit、entry guard | strict boundary + invalid config cases |
| `idempotency.*` | duplicate / conflict / in-flight / retention | deterministic key / digest matrix |
| `projection.*` | stale / failed / rebuild / page | derived views enabled，rebuild failure injection |
| `jobs.*` | batch、retry、timeout、manual job args | deterministic job fixtures |
| `external.*` | identity、method、source、evidence、timebox resolution | fake success / unresolved / unavailable / body-leak;configured refs 条件必填 |
| `outbox.*` | publish success / failed marker / retry | fake publisher + failure injection |
| `handoff.*` | trace / archive handoff success、failed、redaction | fake handoff + failure injection |
| `features.*` | derived views、advanced search、truth path 不可关闭 | derived views enabled;advanced search default false;核心边界不可关闭 |

### 8.4 环境拓扑图

图类型: 环境拓扑图

图标题: L1-work P0 测试环境依赖类型

```text
L1-work test runtime
  |
  +--[compile] core-contracts path dependency
  |
  +--[runtime] in-memory store / projection / idempotency / outbox
  |
  +--[runtime] fake or controlled resolver ports
  |     +-- identity / method / source work / process / evidence / runtime
  |
  +--[event] inbound event replay / fake event envelopes
  |
  +--[event] fake outbox publisher / controlled bus seam
  |
  +--[runtime] fake trace / archive handoff adapters
  |
  +--[runtime] operations replay bundle / sanitized report root
```

关键说明:

- 只有 `core-contracts` 是 `[compile]` 依赖。
- 非 core sibling 仓一律通过 `[runtime]` port / adapter / handoff 或 `[event]` replay 协作。
- P0 不要求真实 DB、MQ、event bus、secret provider 或 production endpoint。
- fake / configured / replay 的证据必须可区分。

### 8.5 环境不可用处理

| 环境 / 依赖 | 不可用场景 | 处理口径 | 是否阻塞 P0 |
|---|---|---|---|
| local-dev fake adapter | fake seed 或 fake marker 缺失 | startup / test fail-fast | 是 |
| ci-test temp dir / isolated store | temp dir 创建失败、env override 泄漏 | fail-fast，不复用上一 run | 是 |
| ci-test external fake | resolver outcome 未注册 | explicit unresolved / test failure，按用例预期区分 | 是 |
| integration-like configured adapter | endpoint / credential ref / target ref 缺失 | startup fail-fast 或 fail-closed，不 fallback fake success | 是 |
| integration-like controlled external | provider unavailable | explicit unavailable / failed marker 或配置 fail-fast | 是，限 integration-like 用例 |
| operations-replay bundle | bundle 缺失、baseline digest mismatch | replay job fail-fast，不修 truth | 是，限 replay 用例 |
| operations-replay historical sensitive | bundle 含 raw secret / raw source body | fail-fast，不写报告 | 是 |
| staging-like | real-like 服务不可用 | 记录为 P1 环境风险 | 否 |
| production-like | real 服务 / secret provider 不可用 | 不属于当前 P0 测试执行 | 否 |

### 8.6 对上游设计的影响判定

| 测试结论 | 是否影响上游设计 | 回写位置 | 处理状态 |
|---|---|---|---|
| P0 环境由 local-dev、ci-test、integration-like、operations-replay 覆盖 | 否 | 无 | 无回写 |
| 只有 `core-contracts` 是编译期 path dependency | 否 | 无 | 无回写 |
| 非 core sibling repo 通过 runtime / event 协作 | 否 | 无 | 无回写 |
| staging-like / production-like 不阻塞 P0，不写真实字段全集 | 否 | 无 | 无回写 |
| 环境不可用使用 fail-fast / fail-closed / explicit marker / replay mismatch | 否 | 无 | 无回写 |

---

## 9. 自动化与 CI/CD 门禁

> 校准来源：
> - `design-calibration/05_test_plan_step_09_automation_gates.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_09_automation_gates.md` 的“结构化中间产物”“回填草稿”和“待确认事项”小节，了解 suite、gate、脚本和 artifact / report 路径。

### 9.1 自动化套件表

| 套件 | 覆盖范围 | 执行位置 | 阻断级别 | artifact 输出 | report 输出 |
|---|---|---|---|---|---|
| `unit-contract-domain` | contracts DTO / refs、domain objects、policy、state machine、digest pure function | PR、main CI、release gate | 阻断 PR / CI / release | `artifacts/test/<run_id>/unit-contract-domain` | `reports/runs/<run_id>/unit-contract-domain.md` |
| `service-core` | command / query service、UoW、idempotency、query no-write、error mapping | PR、main CI、release gate | 阻断 PR / CI / release | `artifacts/test/<run_id>/service-core` | `reports/runs/<run_id>/service-core.md` |
| `api-contract-fast` | command / query handler DTO roundtrip、metadata、protocol error surface | PR、main CI | 阻断 PR / CI | `artifacts/test/<run_id>/api-contract-fast` | `reports/runs/<run_id>/api-contract-fast.md` |
| `config-fast` | defaults、strict JSON、env override、invalid value、unsupported hot update reject | PR、main CI、release gate | 阻断 PR / CI / release | `artifacts/test/<run_id>/config-fast` | `reports/runs/<run_id>/config-fast.md` |
| `service-all` | 18 Command、8 Query、7 Consumer、6 Job service 编排 P0 | main CI、release gate | 阻断 CI / release | `artifacts/test/<run_id>/service-all` | `reports/runs/<run_id>/service-all.md` |
| `integration-p0` | repository、UoW、runtime builder、config loader、fake / in-memory adapter | main CI、release gate | 阻断 CI / release | `artifacts/test/<run_id>/integration-p0` | `reports/runs/<run_id>/integration-p0.md` |
| `worker-job-contract` | inbound consumer envelope、outbox publisher、job input / receipt / partial failure report | main CI、release gate | 阻断 CI / release | `artifacts/test/<run_id>/worker-job-contract` | `reports/runs/<run_id>/worker-job-contract.md` |
| `consumer-outbox` | 7 Consumer、10 Outbound Event、outbox failure / retry / duplicate | main CI、release gate | 阻断 CI / release | `artifacts/test/<run_id>/consumer-outbox` | `reports/runs/<run_id>/consumer-outbox.md` |
| `config-redaction` | raw secret / token / payload / source body scan、fake marker、config source summary | main CI、release gate | 阻断 CI / release | `artifacts/test/<run_id>/config-redaction` | `reports/runs/<run_id>/config-redaction.md` |
| `integration-like-seam` | controlled configured adapter refs、external unavailable、no fake fallback | main CI optional、release selected | selected 阻断 | `artifacts/test/<run_id>/integration-like-seam` | `reports/runs/<run_id>/integration-like-seam.md` |
| `operations-replay` | replay bundle、outbox / projection / reference / handoff / reconciliation rerun | nightly、release selected | nightly 记录;release selected 阻断 | `artifacts/test/<run_id>/operations-replay` | `reports/runs/<run_id>/operations-replay.md` |
| `concurrency-idempotency-stress` | version conflict、single-winner、duplicate / conflict / commit unknown 重复运行 | nightly | 非 release 默认不阻断 | `artifacts/test/<run_id>/concurrency-idempotency-stress` | `reports/runs/<run_id>/concurrency-idempotency-stress.md` |
| `release-main-smoke` | core closure smoke、promote、outbox smoke | release gate | 阻断 release | `artifacts/test/<run_id>/release-main-smoke` | `reports/runs/<run_id>/release-main-smoke.md` |
| `release-config-redline` | invalid config、redaction、fake-as-production、report path、no `latest` | release gate | 阻断 release | `artifacts/test/<run_id>/release-config-redline` | `reports/runs/<run_id>/release-config-redline.md` |
| `release-evidence-pack` | evidence index、suite summary、redaction report、acceptance handoff package | release gate | 阻断 release | `artifacts/test/<run_id>/release-evidence-pack` | `reports/runs/<run_id>/release-evidence-pack.md` |

### 9.2 CI/CD 门禁图

图类型: 自动化门禁图

图标题: L1-work 自动化门禁流

```text
[Pull Request]
  | run_pr_gate.sh
  | unit-contract-domain + service-core + api-contract-fast + config-fast
  v
[Main CI]
  | run_main_gate.sh
  | service-all + integration-p0 + worker-job-contract + consumer-outbox + config-redaction
  v
[Nightly]                    [Release candidate]
  | operations / stress        | release-main-smoke
  | extended redaction         | release-config-redline
  v                            | release-evidence-pack
[Risk queue]                  v
                            [Release allowed?]
```

关键说明:

- PR 覆盖核心 schema、domain、service、API contract 和快速配置失败。
- Main CI 是 P0 全量自动化主门禁。
- Nightly 负责 replay、stress 和长耗时专项，失败进入风险 / 缺陷队列。
- Release gate 不引用 `latest`，只引用显式 `run_id`、artifact 和 report。

### 9.3 脚本分类与路径

| 脚本类型 | 目录 | 职责 | 输出 |
|---|---|---|---|
| gate | `scripts/gates/*.sh` | 执行 PR / main / nightly / release suite | `artifacts/test/<run_id>/...` |
| check | `scripts/checks/*.sh` | 检查 forbidden output、report path、config source summary、fake marker、evidence index | check artifact 和 gate result |
| report | `scripts/reports/*.sh` | 汇总 gate artifact 并生成人类可读报告 | `reports/runs/<run_id>/...` 和 `reports/acceptance/...` |

所有 gate 默认支持 `--run-id`、`--artifact-root`、`--config-profile`；replay gate 额外支持 `--replay-bundle-ref`。

### 9.4 用例族到 suite 映射

| 用例族 | PR | Main CI | Nightly | Release gate |
|---|---|---|---|---|
| `CORE` | `service-core` | `service-all` / `integration-p0` | concurrency selected | `release-main-smoke` |
| `MEMBER` | `service-core` selected | `service-all` / `integration-p0` | external unavailable replay | `release-main-smoke` |
| `FORMAL` | `service-core` selected | `service-all` / `integration-p0` | forbidden body extended | `release-main-smoke` |
| `PROMOTE` | unit / service selected | `service-all` / `worker-job-contract` | runtime source replay | selected release smoke |
| `DEP` | unit policy selected | `service-all` | graph stress selected | main CI evidence |
| `ITER` | service selected | `service-all` / `integration-p0` | concurrency selected | `release-main-smoke` |
| `QUERY` | `api-contract-fast` selected | `service-all` / `integration-p0` | projection replay | `release-main-smoke` |
| `OPS` | unit / config selected | `worker-job-contract` / `consumer-outbox` | `operations-replay` | selected release smoke |
| `CFG` | `config-fast` | `config-redaction` | extended redaction scan | `release-config-redline` |
| `NFR` | forbidden field unit selected | `config-redaction` | stress / redaction / replay | `release-config-redline` / `release-evidence-pack` |

### 9.5 对上游设计的影响判定

| 测试结论 | 是否影响上游设计 | 回写位置 | 处理状态 |
|---|---|---|---|
| P0 自动化可由 PR / main CI / nightly / release gate 分层覆盖 | 否 | 无 | 无回写 |
| gate / check / report 只定义目录、参数和输出契约，不创建脚本实现 | 否 | 无 | 无回写 |
| artifact / report 路径统一为 `artifacts/test/<run_id>`、`reports/runs/<run_id>`、`reports/acceptance` | 否 | 无 | 无回写 |
| `integration-like` / `operations-replay` 只使用 controlled adapter / replay bundle | 否 | 无 | 无回写 |

---

## 10. 专项测试与非功能验证

> 校准来源：
> - `design-calibration/05_test_plan_step_10_special_non_functional.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_10_special_non_functional.md` 的“结构化中间产物”“回填草稿”和“待确认事项”小节，了解非功能和专项测试口径。

### 10.1 专项测试矩阵

| 专项 | 指标 / 风险 | 方法 | 环境 | 阈值 / 通过条件 | 证据 |
|---|---|---|---|---|---|
| 性能观察 | 核心 command / query / job 不成为明显瓶颈；旧数字只作候选 | 采集 `duration_ms`、item count、batch size、page size | `ci-test`;nightly 可扩展 | 报告必须包含样本、P50 / P95、fixture 规模和 profile；不以旧 `100ms / 300ms` 作为 P0 fail 条件 | `EV-WORK-NFR-001` |
| 运行边界性能 | timeout、batch、page、body limit 生效 | 构造超时、超 page、超 body、非法 batch / retry | `ci-test` | 触发正式阈值时 fail-fast、reject 或 timeout surface | `EV-WORK-CFG-005`~`009`;`EV-WORK-NFR-001` |
| 可用性 / 降级 | 外部依赖失败不得造 Work truth | 注入 resolver unavailable、publisher failure、projection failed、handoff failure | `ci-test`;`integration-like`;`operations-replay` | core truth closure 不被破坏；返回 unavailable / unresolved / failed / stale marker | `EV-WORK-NFR-002`;`EV-WORK-OPS-*` |
| 安全 / 授权 | unauthorized、forbidden body、相邻仓正文和敏感材料泄露 | unauthorized negative、forbidden output scan、DTO / event / report dump scan | `ci-test`;release gate | unauthorized -> `NotVisible` / reject；raw secret / token / payload / body 零命中 | `EV-WORK-NFR-003`;`EV-WORK-CFG-010`~`012` |
| 幂等 / 一致性 | duplicate、conflict、version conflict、commit unknown 不产生重复 truth | same key same digest / different digest、并发 review、commit unknown retry | `ci-test`;nightly stress | duplicate 读取 stored result surface；conflict reject；single-winner；无重复 truth | `EV-WORK-NFR-004` |
| 可恢复性 | publish / projection / reference / handoff / replay failure 可恢复 | failure injection、rerun job、operations replay、baseline mismatch | `ci-test`;`operations-replay`;release selected | failed marker 可见；retry 按正式状态推进；reconciliation read-only | `EV-WORK-OPS-*`;`EV-WORK-NFR-002` |
| 可观测性 | 核心变化、边界越界、依赖延迟和维护状态可发现 | structured log、metric、trace / audit / outbox / job report | `ci-test`;main CI;release gate | safe fields 存在；metric label low-cardinality；无 raw body / secret | `EV-WORK-NFR-005` |
| 审计 / 可追溯 | accepted / rejected / maintenance 可解释 | command、reject、job report、trace page cross-check | `ci-test`;release smoke | accepted truth change 有 trace / audit / outbox 或 report | `EV-WORK-CORE-*`;`EV-WORK-DEP-*`;`EV-WORK-NFR-005` |

### 10.2 性能专项口径

| 项 | P0 口径 | 是否 release 硬阈值 | 来源 |
|---|---|---|---|
| `CreateWorkItem P95 < 100ms` | 观察候选 | 否 | `00-需求文档.md` §13 旧候选目标 |
| `GetProjectBoard P95 < 300ms` | 观察候选 | 否 | `00-需求文档.md` §13 旧候选目标 |
| `boundary.query_read_timeout = 2s` | 必须验证 timeout 生效 | 是，按配置契约 | `04-配置设计.md` §7 |
| `store.transaction_timeout = 5s` | 必须验证配置可解析、非正数 fail-fast | 是，按配置契约 | `04-配置设计.md` §7 / §9 |
| `jobs.job_timeout = 300s` | 必须验证 job-run-start 参数和非法值处理 | 是，按配置契约 | `04-配置设计.md` §7 / §9 |
| `boundary.max_page_limit = 100` | 必须验证 page limit | 是，按配置契约 | `04-配置设计.md` §7 |
| `boundary.max_command_body_bytes = 1048576` | 必须验证 body limit | 是，按配置契约 | `04-配置设计.md` §7 |

### 10.3 安全与边界红线

| 红线 | 测试方法 | 阻断条件 | 证据 |
|---|---|---|---|
| raw secret / raw token 不得出现 | config、env capture、log、audit、report、artifact scan | 任一命中 | `EV-WORK-CFG-010`~`012`;redaction report |
| raw payload / source body 不得出现 | DTO / event / repository dump / report scan | source body、provider response body、runtime progress body、ImplementationPlan body 命中 | `EV-WORK-FORMAL-004`;`EV-WORK-PROMOTE-004`;`EV-WORK-NFR-003` |
| 未授权访问不得返回 truth | command / query unauthorized negative | 返回 visible truth 或写入 truth | `EV-WORK-QUERY-001`;`EV-WORK-NFR-003` |
| 配置不得关闭核心边界 | 关闭 truth / metadata / idempotency / visibility / audit / outbox / redaction 的配置 | runtime build 成功或 silent ignore | `EV-WORK-CFG-017` |
| configured adapter 不得 fallback fake success | 缺 endpoint / credential ref 或 provider unavailable | 自动使用 fake 并返回 success | `EV-WORK-CFG-013/014` |
| query / projection / reconciliation 不得反写真相 | query、projection rebuild、reconciliation read-only 用例 | 写入 truth | `EV-WORK-QUERY-*`;`EV-WORK-OPS-004` |
| report / artifact 路径不得用 `latest` | path check | 出现 `latest` 或错误 root | `EV-WORK-NFR-005` |

### 10.4 对上游设计的影响判定

| 测试结论 | 是否影响上游设计 | 回写位置 | 处理状态 |
|---|---|---|---|
| 旧 `100ms / 300ms` 不升级为 P0 release 硬阈值 | 否 | 无 | 无回写 |
| P0 性能专项验证正式 timeout、batch、page、body limit 和样本报告 presence | 否 | 无 | 无回写 |
| raw secret / token / payload / source body / external body 泄露一票阻断 | 否 | 无 | 无回写 |
| duplicate / conflict / version conflict / commit unknown / job rerun 进入 P0 一致性专项 | 否 | 无 | 无回写 |

---

## 11. 缺陷管理与复验规则

> 校准来源：
> - `design-calibration/05_test_plan_step_11_defects_retest.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_11_defects_retest.md` 的“结构化中间产物”“回填草稿”和“待确认事项”小节，了解 S / A / B / C 分级和复验规则。

### 11.1 缺陷分级

| 级别 | 定义 | 示例 | 处理要求 | 是否阻断 |
|---|---|---|---|---|
| S | 一票否决、P0 核心闭环断裂、truth / security / evidence redline、release gate redline 或无法复核证据 | Work 保存相邻仓正文、raw secret 泄露、duplicate 产生第二 WorkItem、`latest` 进入正式证据 | 立即阻断；必须修复；必须新增自动化防回归 | 是 |
| A | P0 主线能力、P0 service / API / job 编排、状态机、事务、恢复或配置失败，但未触发一票否决 | `CreateWorkItem` happy path 失败、projection rebuild failed 后不能恢复 | 阻断受影响 gate；可进入 §14 / 新版 `06` 评审但本节不批准接受 | 是，对相关 gate |
| B | P1/P2、非阻断专项、报告表达、低风险 fixture 或局部稳定性问题 | staging-like smoke 失败、性能观察报告缺非必填字段 | 建缺陷并排期；修复后精准复验 | 否，除升级场景 |
| C | 文档错字、非事实源说明、测试描述清晰度或不影响执行的整理项 | 中间产物措辞不统一 | 可随文档批次修复 | 否 |

### 11.2 风险接受边界

| 缺陷类型 | 是否可风险接受 | 条件 | 记录位置 |
|---|---|---|---|
| S 级 | 否 | 不适用 | 缺陷记录保留阻断状态 |
| A 级 P0 主线失败 | 原则上否 | 只有上游正式降级 P0 范围后才可改变 | §14 + 新版 `06` |
| A 级非主线但影响 release gate | 仅可评审，不得自动接受 | root cause、绕行说明、影响范围、补测计划、无 S 级触发证明 | §14 |
| B 级 | 可排期 | 不影响 P0 gate、证据完整、无一票否决 | 缺陷清单 / §14 |
| C 级 | 可随批处理 | 不影响事实源、编号、执行和证据 | 文档修复记录 |

### 11.3 修复后回归矩阵

| 缺陷影响面 | 必回归 | 新增自动化要求 |
|---|---|---|
| Contract / DTO / refs | direct contract test、`api-contract-fast`、同对象 roundtrip | S / A 必须新增 roundtrip 或 negative test |
| Domain state / policy | direct domain transition test、同状态机非法迁移、`unit-contract-domain` | S / A 必须新增状态或 policy 断言 |
| Application service / UoW | 直接 `TC-WORK-*`、同族 family、`service-core` / `service-all` | S / A 必须新增 rollback / no side effect 断言 |
| Repository / projection | direct repository / integration test、`integration-p0`、相关 `QUERY` / `OPS` | S / A 必须新增 source truth / no-write 断言 |
| Idempotency / concurrency | `CORE-004`、`NFR-004`、same key conflict、version conflict | S / A 必须新增 duplicate / conflict 防回归 |
| Config / redaction | `CFG-*`、`NFR-003`、`config-fast`、`config-redaction` | S / A 必须新增 config 或 scan case |
| Handoff / outbox / job recovery | `OPS-*`、`worker-job-contract`、`consumer-outbox` | S / A 必须新增 failure injection / rerun 断言 |
| Report / evidence path | `check_report_paths.sh`、`check_evidence_index.sh`、`release-evidence-pack` | S 必须新增 path / evidence check fixture |

### 11.4 缺陷记录最小字段

| 字段 | 必填 | 说明 |
|---|---|---|
| `defect_id` | 是 | 稳定缺陷编号 |
| `severity` | 是 | S / A / B / C |
| `source_gate` | 是 | PR / main / nightly / release / manual review |
| `suite` | 是 | 失败 suite 或 review source |
| `test_case_ids` | 是 | 一个或多个 `TC-WORK-*` |
| `evidence_ids` | 是 | 一个或多个 `EV-WORK-*` 或 redline report |
| `affected_requirements` | 是 | `FR-WORK-*` / `BR-WORK-*` / `AC-WORK-*` |
| `run_id` | 是 | 失败 run id，不得写 `latest` |
| `artifact_refs` | 是 | `artifacts/test/<run_id>/...` |
| `report_refs` | 是 | `reports/runs/<run_id>/...` |
| `root_cause` | S / A 必填 | 设计缺口、实现缺陷、fixture、环境、脚本、文档等 |
| `fix_ref` | 修复后必填 | commit / patch / design baseline |
| `retest_run_id` | 关闭前必填 | 复验 run id |
| `regression_scope` | 关闭前必填 | direct、family、suite、redline、evidence check |
| `residual_risk` | A / B 必填 | 若未全量修复，进入 §14 |

### 11.5 对上游设计的影响判定

| 测试结论 | 是否影响上游设计 | 回写位置 | 处理状态 |
|---|---|---|---|
| 缺陷分级按 S / A / B / C 定义，S 级覆盖一票否决和 release redline | 否 | 无 | 无回写 |
| 风险接受不在本文 §11 批准，S 级不得风险接受，A 级只可进入 §14 / 新版 `06` 评审 | 否 | 无 | 无回写 |
| 修复后回归矩阵使用既有 `TC-WORK-*`、`EV-WORK-*` 和 suite | 否 | 无 | 无回写 |
| S / A 缺陷必须新增或更新自动化防回归 | 否 | 无 | 无回写 |

---

## 12. 进入准则与退出准则

> 校准来源：
> - `design-calibration/05_test_plan_step_12_entry_exit.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_12_entry_exit.md` 的“结构化中间产物”“回填草稿”和“待确认事项”小节，了解进入 / 退出门禁如何收敛。

### 12.1 进入准则

- [ ] `00-需求文档.md`、`01-架构设计.md`、`02-概要设计.md`、`03-详细设计.md`、`04-配置设计.md` 已有可引用 baseline。
- [ ] 正式 `05-测试方案.md` 已由 Step 15 装配完成，且不再引用旧版 `05/06` 作为事实源。
- [ ] `FR-WORK-001`~`008`、`BR-WORK-001`~`027`、`AC-WORK-001`~`029` 均已映射到 `TC-WORK-*` 和 `EV-WORK-*`。
- [ ] P0 用例矩阵已覆盖 Command、Query、Consumer、Outbound Event、Operations Job、状态机、事务、幂等、配置、观测和 NFR 红线。
- [ ] P0 `DS-WORK-*` 测试数据集、fixture builder、fake adapter seed、run-scoped isolation 和 cleanup 规则已可执行。
- [ ] `ci-test` profile 可启动，strict config、env override、redaction fixture、fake marker 和 temp root 可用。
- [ ] PR / main / release 阻断 suite 对应 gate / check / report 脚本契约已实现或被当前实施计划列为本 run 前置任务。
- [ ] `artifacts/test/<run_id>`、`reports/runs/<run_id>`、`reports/acceptance` 输出路径可创建，且不会使用 `latest`。
- [ ] 缺陷分级、复验、自动化防回归和关闭证据规则已确认。
- [ ] 当前没有阻塞测试开始的上游设计缺口、字段缺失、状态冲突、配置缺失或环境不可用项。

### 12.2 退出准则

- [ ] `unit-contract-domain`、`service-core`、`api-contract-fast`、`config-fast` 阻断 suite 通过。
- [ ] `service-all`、`integration-p0`、`worker-job-contract`、`consumer-outbox`、`config-redaction` 主线 suite 通过。
- [ ] `release-main-smoke`、`release-config-redline`、`release-evidence-pack` 通过。
- [ ] P0 `TC-WORK-*` 阻断用例全部通过，包括 CORE、MEMBER、FORMAL、PROMOTE、DEP、ITER、QUERY、OPS、CFG、NFR。
- [ ] 一票否决项和 S 级触发项全部为通过状态。
- [ ] S 级缺陷为 0。
- [ ] 影响 P0 gate、release gate 或 P0 evidence 的 A 级缺陷为 0。
- [ ] B / C 级缺陷已记录影响范围、处理计划和是否进入 §14 残余风险。
- [ ] redaction、forbidden output、fake marker、report path、evidence index 检查全部通过。
- [ ] duplicate / conflict / version conflict / commit unknown、query no-write、projection no-write、reconciliation read-only 和 outbox / handoff failure recovery 关键断言全部通过。
- [ ] `EV-WORK-*` 证据已生成并能从 `reports/runs/<run_id>/evidence-index.md` 追溯到 `TC-WORK-*` 和 `AC-WORK-*`。
- [ ] 残余风险、P1/P2 未覆盖项和非阻断 nightly / staging-like 结果已进入 §14。

### 12.3 退出阻断项

| 阻断项 | 来源 | 处理 |
|---|---|---|
| 任一 S 级缺陷未关闭 | §11 | 阻断退出，必须修复并复验 |
| P0 A 级缺陷未关闭 | §11 | 阻断退出，除非上游正式降级 P0 范围 |
| release redline 失败 | §9 / §10 | 阻断退出，不得风险接受 |
| raw secret / token / payload / source body 命中 | §10 | 阻断退出，修复后重跑 redaction |
| Work truth 被 query / projection / report / adjacent input 反写 | §6 / §10 / §11 | 阻断退出，修复并补自动化 |
| duplicate 产生重复 truth | §6 / §10 | 阻断退出，修复幂等并回归 |
| evidence index 缺 P0 `EV-WORK-*` | §9 / §13 | 阻断退出，补证据生成和索引 |
| `latest` 或错误 artifact / report 路径 | §9 / §13 | 阻断退出，修正路径后重跑 |
| configured adapter 自动 fake success | §8 / §10 | 阻断退出，修复 fake marker 和 unavailable 处理 |

### 12.4 文档与风险边界

| 项 | 进入时口径 | 退出时口径 |
|---|---|---|
| 新版 `05-测试方案.md` | Step 15 完成后才能作为执行事实源 | 退出报告必须引用正式 `05` baseline |
| 新版 `06-验收标准.md` | 不是测试进入硬前置；旧版只作方向参考 | 正式验收裁决由新版 `06` 承接 |
| P1/P2 staging-like / production-like | 不阻塞 P0 进入 | 未完成不阻塞 P0 退出，但必须进入 §14 |
| 旧性能数字 | 不作为进入条件 | 不作为 P0 release 硬退出条件，只作为观察报告 |
| 残余风险 | 不允许隐藏 S / P0 A | 只能记录非阻断 B / C 或 P1/P2 风险 |

### 12.5 对上游设计的影响判定

| 测试结论 | 是否影响上游设计 | 回写位置 | 处理状态 |
|---|---|---|---|
| 进入准则要求新版 `00/01/02/03/04` 和正式 `05` baseline 可引用 | 否 | 无 | 无回写 |
| 退出准则要求 P0 suite、release redline 和 evidence pack 通过 | 否 | 无 | 无回写 |
| S 级和影响 P0 的 A 级缺陷阻断退出 | 否 | 无 | 无回写 |
| P1/P2 未覆盖项不阻断 P0 退出，但必须进入 §14 | 否 | 无 | 无回写 |

---

## 13. 测试报告与证据归档

> 校准来源：
> - `design-calibration/05_test_plan_step_13_reports_evidence.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_13_reports_evidence.md` 的“结构化中间产物”“回填草稿”和“待确认事项”小节，了解报告和证据归档结构。

### 13.1 证据归档表

| 证据 ID | 证据类型 | 来源 | 保存位置 | 关联用例 | 验收引用 |
|---|---|---|---|---|---|
| `EV-WORK-CORE-001`~`004` | command result、UoW snapshot digest、outbox / trace / idempotency report | `service-core` / `service-all` | `artifacts/test/<run_id>/suites/service-core/`;`reports/runs/<run_id>/evidence/EV-WORK-CORE-*.md` | `TC-WORK-CORE-001`~`004` | `AC-WORK-001/006/014/016/018/019` |
| `EV-WORK-MEMBER-001`~`004` | member snapshot ref、resolver outcome、safe log、negative report | `service-all` / `integration-p0` | `artifacts/test/<run_id>/suites/service-all/` | `TC-WORK-MEMBER-001`~`004` | `AC-WORK-002/007/014/016/017/018` |
| `EV-WORK-FORMAL-001`~`005` | Work truth snapshot digest、forbidden body scan、event dump scan | `service-all` / `config-redaction` | `artifacts/test/<run_id>/suites/service-all/`;`artifacts/test/<run_id>/redaction-scan/` | `TC-WORK-FORMAL-001`~`005` | `AC-WORK-003/008/014/015/016/020` |
| `EV-WORK-PROMOTE-001`~`005` | promote decision report、runtime body rejection evidence、version conflict report | `service-all` / `worker-job-contract` | `artifacts/test/<run_id>/suites/worker-job-contract/` | `TC-WORK-PROMOTE-001`~`005` | `AC-WORK-003/009/015/016/017/020` |
| `EV-WORK-DEP-001`~`005` | dependency graph assertion、blocker evidence ref report、cycle reject report | `service-all` | `artifacts/test/<run_id>/suites/service-all/` | `TC-WORK-DEP-001`~`005` | `AC-WORK-010/019/020/027/028` |
| `EV-WORK-ITER-001`~`005` | iteration / commitment state snapshot、UoW report、state reject report | `service-all` / `integration-p0` | `artifacts/test/<run_id>/suites/integration-p0/` | `TC-WORK-ITER-001`~`005` | `AC-WORK-004/011/014/016/017/020` |
| `EV-WORK-QUERY-001`~`008` | query response report、projection freshness report、no-write assertion | `api-contract-fast` / `integration-p0` | `artifacts/test/<run_id>/suites/api-contract-fast/` | `TC-WORK-QUERY-001`~`008` | `AC-WORK-005/012/017/019/021/022/023/027` |
| `EV-WORK-OPS-001`~`006` | job report、outbox state report、rebuild / refresh / reconciliation / handoff report | `worker-job-contract` / `consumer-outbox` / `operations-replay` | `artifacts/test/<run_id>/suites/worker-job-contract/`;`artifacts/test/<run_id>/suites/operations-replay/` | `TC-WORK-OPS-001`~`006` | `AC-WORK-013/015/019/021/025/029` |
| `EV-WORK-CFG-001`~`017` | config loader report、redaction scan、fake marker check、path check | `config-fast` / `config-redaction` / `release-config-redline` | `artifacts/test/<run_id>/suites/config-fast/`;`artifacts/test/<run_id>/redaction-scan/` | `TC-WORK-CFG-001`~`017` | `AC-WORK-020/023/026/029` |
| `EV-WORK-NFR-001`~`005` | performance observation、availability、security、idempotency、observability report | `service-all` / `config-redaction` / `release-evidence-pack` | `reports/runs/<run_id>/nfr-*.md`;`reports/runs/<run_id>/observability-audit.md` | `TC-WORK-NFR-001`~`005` | `AC-WORK-024`~`029` |

### 13.2 报告生成表

| 报告 | 来源 artifact | 生成脚本 | 输出位置 | 人 / Agent 审查要求 |
|---|---|---|---|---|
| suite summary | `artifacts/test/<run_id>/suites/<suite>/report.json` | `scripts/reports/collect_gate_reports.sh` | `reports/runs/<run_id>/suites/<suite>.md` | 检查失败解释、设计契约引用和 suite 范围 |
| gate results | `artifacts/test/<run_id>/suites/*/report.json` | `scripts/reports/collect_gate_reports.sh` | `reports/runs/<run_id>/gate-results.md` | 确认阻断 / 非阻断分类 |
| evidence index | `artifacts/test/<run_id>/evidence-index.json` 和 suite reports | `scripts/reports/build_evidence_index.sh` | `reports/runs/<run_id>/evidence-index.md` | 检查 `EV / TC / AC / artifact` 可追溯 |
| redaction report | redaction scan artifacts、path check、fake marker check | `scripts/reports/build_redaction_report.sh` | `reports/runs/<run_id>/redaction-check.md` | 确认只写 sanitized location |
| release summary | gate results、evidence index、redaction report、defect status | `scripts/reports/build_release_summary.sh` | `reports/runs/<run_id>/release-summary.md` | 确认 redline 和 evidence pack 均有结论 |
| acceptance handoff | `reports/runs/<run_id>/*` | `scripts/reports/build_release_summary.sh` | `reports/acceptance/handoff.md` | 人 / Agent 补充交接说明、开放问题、残余风险入口；不写验收裁决 |

### 13.3 artifacts 目录结构

```text
artifacts/test/<run_id>/
  meta/context.json
  evidence-index.json
  suites/<suite>/report.json
  suites/<suite>/stdout.log
  suites/<suite>/stderr.log
  suites/<suite>/failure-reason.json
  suites/<suite>/safe-logs.jsonl
  suites/<suite>/snapshots/*.digest
  redaction-scan/report.json
  checks/report-paths.json
  checks/fake-marker.json
  checks/evidence-index.json
```

失败 suite 也必须写 `report.json`、stdout / stderr 和 failure reason。snapshot 只允许保存 digest、ref、summary 或 redacted marker，不得保存 raw body。

### 13.4 reports 目录结构

```text
reports/
  README.md
  runs/<run_id>/
    summary.md
    gate-results.md
    evidence-index.md
    redaction-check.md
    nfr-summary.md
    release-summary.md
    suites/<suite>.md
    evidence/EV-WORK-<TYPE>-<NNN>.md
  acceptance/handoff.md
  acceptance/veto-checklist.md
  acceptance/risk-acceptance.md
  acceptance/open-issues.md
  review/reviewer-notes.md
  review/agent-review.md
```

`reports/acceptance/handoff.md` 是给新版 `06-验收标准.md` 消费的交接摘要；`reports/acceptance/risk-acceptance.md` 只记录待评审风险，不在本文批准风险接受。

### 13.5 evidence index 最小字段

| 字段 | 必填 | 说明 |
|---|---|---|
| `evidence_id` | 是 | `EV-WORK-*` |
| `test_case_ids` | 是 | 一个或多个 `TC-WORK-*` |
| `acceptance_ids` | 是 | 一个或多个 `AC-WORK-*` |
| `suite` | 是 | 产生该证据的 suite |
| `run_id` | 是 | 固定 run id，不得写 `latest` |
| `artifact_refs` | 是 | `artifacts/test/<run_id>/...` |
| `report_refs` | 是 | `reports/runs/<run_id>/...` |
| `design_contract_refs` | P0 必填 | 对应 `03-详细设计.md` 或 `04-配置设计.md` 的契约入口 |
| `redaction_status` | 是 | passed / failed / not_applicable |
| `defect_refs` | 失败时必填 | 关联 S / A / B / C 缺陷 |
| `review_status` | 验收交接前必填 | pending / reviewed / needs_followup |

### 13.6 报告审查清单

| 审查项 | 通过条件 | 失败处理 |
|---|---|---|
| `EV / TC / AC` 可追溯 | 每个 P0 `EV-WORK-*` 都能回指 `TC-WORK-*` 和 `AC-WORK-*` | 补 evidence index 或回到 §5 / §6 修正 |
| artifact 路径稳定 | 所有引用都在 `artifacts/test/<run_id>` | 修正报告并重跑 path check |
| 无 `latest` | 正式报告和验收交接无 `latest` | 阻断 release evidence pack |
| 无 forbidden output | 报告、artifact、review notes 无 raw secret / token / payload / source body | 阻断退出，修复后重跑 redaction |
| 失败证据保留 | 失败 suite 的原始 artifact 未被成功 run 覆盖 | 恢复或重跑并记录新缺陷 |
| 缺陷闭环 | S / P0 A 为 0，B / C 有处理计划 | S / P0 A 阻断退出，B / C 进入 §14 |
| 验收交接边界 | `reports/acceptance` 只提供事实和风险入口，不写最终裁决 | 移除验收裁决文字，交给新版 `06` |

### 13.7 对上游设计的影响判定

| 测试结论 | 是否影响上游设计 | 回写位置 | 处理状态 |
|---|---|---|---|
| 证据归档使用既有 `EV-WORK-*`、`TC-WORK-*`、`AC-WORK-*` 编号族 | 否 | 无 | 无回写 |
| 原始证据、可读报告、验收交接报告分层归档 | 否 | 无 | 无回写 |
| `reports/acceptance` 只提供交接事实，不做验收裁决 | 否 | 无 | 无回写 |
| P0 证据保留周期未在本文定硬规则 | 否 | 无 | 无回写 |

---

## 14. 回归策略与残余风险

> 校准来源：
> - `design-calibration/05_test_plan_step_14_regression_risks.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_14_regression_risks.md` 的“结构化中间产物”“回填草稿”和“待确认事项”小节，了解回归触发和残余风险边界。

### 14.1 回归触发表

| 变更类型 | 最小回归集 | 全量回归触发条件 | 责任人 |
|---|---|---|---|
| contracts DTO / refs / command / query / event schema | direct roundtrip / negative test、`api-contract-fast`、evidence index check | public schema、字段必填 / 可选、状态 enum、error surface 或 event payload 变化 | 合约负责人、测试负责人 |
| domain object / state / policy | direct domain transition test、同状态机非法迁移、`unit-contract-domain` | 状态机、policy 默认值、terminal state、forbidden transition 或 source truth guard 变化 | 领域负责人、测试负责人 |
| application service / UoW / idempotency | direct service test、同族 `service-core`、rollback / no side effect assertion | multi-object UoW、idempotency digest、duplicate / conflict、commit unknown 或 version conflict 变化 | 应用负责人、测试负责人 |
| repository / projection / persistence | direct repository test、`integration-p0`、相关 `QUERY` / `OPS` | persistence schema、query no-write、projection freshness、rebuild source 或 snapshot digest 变化 | 基础设施负责人、测试负责人 |
| consumer / job / outbox / handoff | direct worker / job test、`worker-job-contract`、`consumer-outbox`、`OPS` | event envelope、dedup、retry、outbox publication、handoff success / failure 或 recovery 变化 | worker 负责人、测试负责人 |
| configuration profile / loader / validation | direct config test、`config-fast`、`CFG` | P0 profile、cross-field validation、unsupported source、adapter ref、secret boundary 或 fail-fast / fail-closed 变化 | 配置负责人、测试负责人 |
| redaction / forbidden output / fake marker | direct scanner fixture、`config-redaction`、`release-config-redline` | raw secret / payload / body 边界、report dump、fake / configured marker 或 redaction check 变化 | 安全负责人、测试负责人 |
| report / artifact / evidence scripts | report script dry-run、path check、evidence index check、`release-evidence-pack` | artifact root、report root、acceptance handoff、evidence index 字段或 no `latest` 规则变化 | 测试负责人、实施负责人 |
| runtime builder / adapter wiring | direct runtime fixture、`integration-p0`、`integration-like-seam` selected | adapter kind、configured / fake fallback、runtime dependency graph 或 unavailable marker 变化 | 基础设施负责人、架构负责人 |
| release candidate baseline | `release-main-smoke`、`release-config-redline`、`release-evidence-pack` | 任一 release candidate、P0 A 修复后、S 级修复后、redline / evidence path 变更后 | 发布负责人、测试负责人 |

### 14.2 全量回归触发条件

| 触发条件 | 必跑范围 |
|---|---|
| public protocol / schema 变化 | `unit-contract-domain`、`api-contract-fast`、相关 `service-*`、evidence index |
| source truth / ownership guard 变化 | `service-all`、`integration-p0`、`config-redaction` |
| UoW / transaction / idempotency 变化 | `service-core`、`service-all`、`consumer-outbox`、selected stress |
| redaction / forbidden output 变化 | `config-redaction`、`release-config-redline`、forbidden output scan |
| config profile / validation 变化 | `config-fast`、`config-redaction`、`integration-p0` |
| query / projection / report path 变化 | `api-contract-fast`、`integration-p0`、path check、evidence index check |
| S 级或 P0 A 缺陷修复 | direct failed test、family regression、impacted P0 suite、release gate selected |
| release candidate | `release-main-smoke`、`release-config-redline`、`release-evidence-pack` |

### 14.3 残余风险表

| 风险 | 未覆盖原因 | 影响 | 缓解方式 | 接受人 |
|---|---|---|---|---|
| staging-like / production-like 真实依赖未进入 P0 阻断测试 | P0 只定义 local-dev、ci-test、integration-like、operations-replay | 真实部署前仍需专项验证 | P1/P2 前补生产化详细设计、配置设计、部署运维和 staging-like 测试 | 架构负责人、运维负责人、验收负责人 |
| secret provider / KMS / Vault 未定义 | P0 只允许 ref-only sensitive 和 fake / configured ref | 真实 secret 读取、轮换、吊销和 fail-closed 不能按 P0 执行 | 安全运维专项补 provider、轮换、审计、redaction 和 failure tests | 安全负责人、运维负责人、验收负责人 |
| config center / admin override / hot reload 未定义 | P0 明确 unsupported fail-fast | 未来应急 override 和热更新不能直接落地 | P1/P2 先补权限、审计、reload、回滚、一致性和测试门禁 | 架构负责人、运维负责人 |
| 旧 `100ms / 300ms` 性能候选未升级为硬阈值 | 缺固定环境、容量模型、数据规模和验收来源 | 不能声称满足生产性能 SLA | 当前只做样本观察；若升级，先回写 `00 / 05 / 06` 并补性能专项 | 产品 / 交付负责人、架构负责人、测试负责人 |
| production-like durable store / event bus / endpoint 产品绑定未验证 | P0 使用 in-memory / fake / controlled adapter | 生产事务、重试、超时和部署风险未完全覆盖 | 生产化前补 adapter contract、配置字段、部署手册和 staging evidence | 架构负责人、运维负责人 |
| report / artifact 保留周期未定 | §13 只定义路径和 `<run_id>` 绑定 | 长期审计和追溯保存策略不完整 | `07` / `09` 或运维规范补 retention、清理、归档和访问控制 | 测试负责人、实施负责人、运维负责人 |
| 非阻断 nightly extended stress 未作为 P0 退出硬条件 | 当前 P0 只阻断核心 suite 和 release gate | 罕见并发缺陷可能在 P0 退出后暴露 | 失败建缺陷；进入 release 前相关 blocker 必须关闭 | 测试负责人、架构负责人 |
| 外围增强能力未进入 P0 用例全集 | 容量趋势、跨项目依赖、组合风险等属于外围增强 | 当前 P0 只证明核心 Work truth 闭环 | 后续需求 / 设计 / 测试专项单独补 FR / TC / EV | 产品 / 交付负责人、架构负责人 |

### 14.4 不允许进入残余风险的项目

| 项目 | 原因 | 处理 |
|---|---|---|
| 任一 S 级缺陷 | 一票否决或 release redline | 必须修复并复验 |
| 影响 P0 gate / release gate / P0 evidence 的 A 级缺陷 | 直接破坏 P0 退出条件 | 必须修复，除非上游正式降级 P0 范围 |
| raw secret / token / payload / source body 泄露 | 安全红线 | 阻断退出，修复后重跑 redaction |
| Work truth 被 query / projection / report / adjacent input 反写 | source truth 红线 | 阻断退出，修复并补自动化 |
| duplicate 产生重复 truth | 幂等一致性红线 | 阻断退出，修复并回归 |
| evidence index 缺 P0 `EV-WORK-*` 或引用 `latest` | 证据不可复核 | 阻断 release evidence pack |
| release redline 失败 | release gate 硬条件 | 不得进入残余风险 |

### 14.5 风险移交

| 移交目标 | 输入 | 输出 |
|---|---|---|
| `reports/acceptance/handoff.md` | 残余风险表、B / C 缺陷、非阻断 nightly / staging-like 结果 | 验收交接摘要，不写裁决 |
| 新版 `06-验收标准.md` | §14 风险、§13 evidence index、§12 退出准则 | 风险接受或拒绝的正式裁决 |
| `07-实施计划.md` | 回归触发表、P0 gate、报告脚本和实现前置项 | 实施阶段批次、提交前测试和回归纪律 |
| `09-部署与运维手册.md` | production-like、secret provider、retention、config center 风险 | P1/P2 部署、运维、保留和真实依赖专项 |

### 14.6 对上游设计的影响判定

| 测试结论 | 是否影响上游设计 | 回写位置 | 处理状态 |
|---|---|---|---|
| 回归触发规则承接既有 `TC-WORK-*`、suite、gate 和 `EV-WORK-*` | 否 | 无 | 无回写 |
| P1/P2 production-like、secret provider、config center、durable adapter 等进入残余风险 | 否 | 无 | 无回写 |
| 旧 `100ms / 300ms` 不作为 P0 release 硬阈值，继续作为风险移交项 | 否 | 无 | 无回写 |
| S 级和影响 P0 的 A 级不得进入残余风险 | 否 | 无 | 无回写 |

---

## 15. 参考

> 校准来源：
> - `design-calibration/05_test_plan_step_15_formal_document_assembly.md`
>
> 延伸阅读：
> - 建议继续阅读 `design-calibration/05_test_plan_step_15_formal_document_assembly.md` 的“结构化中间产物”“回填草稿”和“待确认事项”小节，了解正式文档装配和自审结果。

### 15.1 上游 / 下游文档

| 类型 | 文档 |
|---|---|
| 需求文档 | `projects/L1-work/00-需求文档.md` |
| 架构设计 | `projects/L1-work/01-架构设计.md` |
| 概要设计 | `projects/L1-work/02-概要设计.md` |
| 详细设计 | `projects/L1-work/03-详细设计.md` |
| 配置设计 | `projects/L1-work/04-配置设计.md` |
| 验收标准 | `projects/L1-work/06-验收标准.md` |
| 实施计划 | `projects/L1-work/07-实施计划.md` |

### 15.2 规范与 SOP

| 类型 | 文档 |
|---|---|
| 测试方案书写规范 | `standards/document/测试方案书写规范.md` |
| 测试方案讨论 SOP | `standards/document/测试方案讨论流程_SOP.md` |
| 中间产物规范 | `standards/document/设计文档讨论中间产物规范.md` |
| 目录组织规范 | `standards/document/子项目目录与代码文件组织规范.md` |

### 15.3 校准中间产物

| Step | 中间产物 |
|---|---|
| Step 1 | `design-calibration/05_test_plan_step_01_input_boundary.md` |
| Step 2 | `design-calibration/05_test_plan_step_02_scope.md` |
| Step 3 | `design-calibration/05_test_plan_step_03_test_objects_cuts.md` |
| Step 4 | `design-calibration/05_test_plan_step_04_strategy_layers.md` |
| Step 5 | `design-calibration/05_test_plan_step_05_traceability_coverage.md` |
| Step 6 | `design-calibration/05_test_plan_step_06_cases_matrix.md` |
| Step 7 | `design-calibration/05_test_plan_step_07_test_data.md` |
| Step 8 | `design-calibration/05_test_plan_step_08_environment_config.md` |
| Step 9 | `design-calibration/05_test_plan_step_09_automation_gates.md` |
| Step 10 | `design-calibration/05_test_plan_step_10_special_non_functional.md` |
| Step 11 | `design-calibration/05_test_plan_step_11_defects_retest.md` |
| Step 12 | `design-calibration/05_test_plan_step_12_entry_exit.md` |
| Step 13 | `design-calibration/05_test_plan_step_13_reports_evidence.md` |
| Step 14 | `design-calibration/05_test_plan_step_14_regression_risks.md` |
| Step 15 | `design-calibration/05_test_plan_step_15_formal_document_assembly.md` |

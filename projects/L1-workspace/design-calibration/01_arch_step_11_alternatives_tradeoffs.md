# 01 架构 Step 11：备选方案与取舍

## 1. Step 状态

状态：completed / stop_review；模式：full-restart；对应 SOP：架构 SOP Step 11。
开工确认：项目台账、flow、前序 Step 已读；三层门禁允许当前 Step。formal_01_write_allowed 仍关闭。

### Step 内计划

- [x] 读取输入和前序结论：见 §2。
- [x] SOP 问题回答：见 §3。
- [x] 当前材料诊断：见 §4。
- [x] 设计取舍：见 §6。
- [x] 结构化中间产物。
- [x] 复杂度判断与按单元停审。
- [x] 回填草稿。
- [x] 自检与进入下一步条件。

模块骨架：not_applicable。未来 Step 不创建。

## 2. 本步输入

Step 2/10 与正式 00 的核心能力闭环；共同输入：正式 00、项目台账、架构 flow 的来源复核表、架构 SOP 对应 Step、书写规范对应章节、中间产物规范和真相源闭环标准。前序产物的未闭合 seam 仍保留。

## 3. SOP 问题回答

1. 可用替代路径有哪些？纯实时聚合、统一物化视图、每消费方各自聚合；同一仓内又可统一 local state 与投影世代或独立 overlay。
2. 为什么选当前？统一 read model 可提供稳定 provenance/revision；事件增量配 owner baseline 能重建；局部 overlay 保留用户意图。
3. 被放弃方案优点？实时聚合少存储、消费方自建适配快、整体重建容易清空。
4. 为什么不选？分别失去稳定分页/恢复、造成多套解释、丢失局部动作。
5. 牺牲什么？接受数据延迟和维护成本换取一致的读边界；权限无法证明时牺牲可用性。

## 4. 当前文档问题诊断

README 的“首页/任务板”可由 UI 拼装，但正式 00 已将 read model/local state 定为独立职责。共享业务数据库和统一授权中心属于禁区，不是可选择的备选方案。

## 5. 改动前后对比

| 项 | 改动前 | 改动后 | 原因 |
|---|---|---|---|
| 当前架构主题 | 正式 01 未创建，draft 只作候选 | 由本步输入独立收束后进入 §7 | 阻止历史草稿升级为真相 |
| 外部依赖 | 00 已登记 pending/blocker | 保留 owner 和解锁条件 | 不以架构补外部合同 |

## 6. 设计取舍

| 方案 | 优点 | 代价 | 结论 |
|---|---|---|---|
| 同一判断框架比较合法架构路径 | 取舍可审查 | 需说明牺牲项 | 采用 |
| 将所有禁止项当备选充数 | 表格容易填写 | 弱化 owner 红线 | 不采用 |

## 7. 结构化中间产物

### 7.1 方案路径比较

| 路径 | 解决的问题 | 主要收益 | 主要代价 / 约束 | 当前结论 | 说明 |
|---|---|---|---|---|---|
| 纯实时 fanout + 独立 local state | 组合最新可读结果 | 少投影存储 | 来源可用性放大；无稳定持久 revision/replay 进度 | 不作主线；保留 no-write 聚合能力 | 临时响应不能声称 durable view。 |
| 统一物化 + 事件增量 + owner baseline + local overlay | 稳定读取与可恢复性 | 一套 provenance/coverage/版本解释 | 最终一致、维护与安全检查成本 | 采用 | 无正式 event/baseline seam 的来源 blocked。 |
| SDK/产品各自维护投影 | 入口快速适配 | 每消费方可定制 | 重复语义和游标、安全降级漂移 | 不采用 | 与独立 workspace 需求矛盾。 |
| 投影/局部动作同世代全量替换 | 简化重建保存 | 整体替换直观 | pin/read 等用户动作随重建丢失 | 不采用 | 本地动作不是来源事件派生。 |
| 独立 local overlay 与投影关联 | 保留用户意图 | 重建不改变局部 truth | stable ref 与局部版本需协调 | 采用 | 不能将 old ReadCursor 直接当新世代 source cursor。 |
| 同部署三逻辑运行角色 | 限制部署耦合成本 | 本地一致性与故障边界清楚 | 需资源隔离 | 当前允许 | 不是已有部署事实。 |
| 立即拆成多服务、多存储 | 独立扩展 | 可按吞吐分配资源 | 分布式一致性与运维成本增加 | 当前不采用 | 真正负载/隔离要求出现后重新评估。 |

### 7.2 不作为备选的边界外方向

共享 L1 业务表、workspace 授权中心、上游 command facade、runtime/tool executor、archive restore owner 和镜像 seed owner 都不是合法等价路径，不能因实现便利获得权限。

### 7.3 取舍结论

当前用维护成本与可解释延迟换取稳定 read model，保留 no-write 聚合而不混淆临时响应与持久投影。用现时授权检查造成的不可用换取安全确定性；绝不以配置、TTL 或 stale fallback 放宽 owner 决定。部署可演进，所有权与 no-write 不能作为阶段性债务。

## 8. 回填草稿

正式 §12 承接 §7 已收束的表、图和结论；图注随图一起回填。不复制 §3~6 的讨论过程。

复杂度判断：按主线架构路径、局部状态边界和部署选择比较；禁区不混进合法备选。

## 9. 待确认事项

WS-UP-001~008 仍保持 open；exact query/event/ref/visibility/cursor/export 和共享 schema 必须由 owner 关闭，不由本 Step 补全。

## 10. 进入下一步条件

至少两条合法方案逐维度比较；采用与未采用有明确代价；没有把后续待办或越权方案伪作合理替代。

写入前检查：模块思考已完成，结构化结论仅整理 §3/6；正式回填草稿无新增协议。
思考记录 = done；写入记录 = done；自检状态 = done；gate_status = pass；gate_reason = 本步架构结论可引用，未闭合外部能力已隔离为 blocker；next_allowed_action = 进入 Step 12。
本 pass 只指设计静态自检，不是测试、集成、证据、用户 signoff 或 readiness。

# L2-member 需求文档

> 文档状态: `Draft / formal-00-complete / review-pending`
> 版本: `v1.0.0-restart`
> 日期: `2026-08-22`
> 设计模式: `full-restart`
> 本文回答: 要做什么、为什么做、做到什么程度算完成
> 本文不声明实现、测试结果、artifact、report、evidence、签署或真实 readiness。

## 文档元信息

| 项目 | 内容 |
|---|---|
| 项目 | `L2-member` |
| 正式顺序 | `L2-runtime` 完成 `00~07` 后开启的 Layer 3 并行窗口成员之一 |
| 当前 authority | `standards/document/全局项目依赖关系与裁剪规则.md` 与已完成上游正式文档 |
| 校准状态 | 需求 Step 1~17 已完成修复、装配与静态审计;等待用户审阅确认 |
| 实施状态 | `not_started`;本任务不实现代码 |
| 上游开放项 | `L2M-UP-001~008`,见第 15 章与项目台账 |

---

## 1. 与上游文档的关系声明

> 校准来源:
> - `design-calibration/00_req_step_01_upstream_relation.md`
>
> 延伸阅读:
> - 建议继续阅读上述中间产物的"本步输入与来源分层""结构化中间产物"和"当前文档问题诊断"小节,了解 authority、上游输入、sibling pending、historical material 与 blocker 如何分层。

| 来源文档 | 上游章节 / 模块 | 承接内容 |
|---|---|---|
| `standards/document/全局项目依赖关系与裁剪规则.md` | `§4 总依赖矩阵` | `L2-member` 的全局依赖位置与三类依赖纪律。 |
| `standards/document/全局项目依赖关系与裁剪规则.md` | `§4.1 系统讨论顺序与并行窗口` | Layer 3 并行窗口的进入条件与并行纪律。 |
| `projects/L2-runtime/00-需求文档.md` | `§2 本仓定位与边界` | Runtime 拥有 run / decision / checkpoint / outcome truth,member host lifecycle 被排除在外。 |
| `projects/L2-runtime/00-需求文档.md` | `§5 用户与角色`;`§12 接口与依赖` | member 作为运行触发方 / 运行材料消费方经 entry 合同协作的边界。 |
| `projects/L2-runtime/01-架构设计.md` | `§4.2 边界红线`;`§5 系统边界与上下文` | member 不得成为第二 run truth owner;Entry consumers 是下游消费边界。 |
| `projects/L2-tools/00-需求文档.md` | `§2 本仓定位与边界`;`§12 接口与依赖` | 工具契约 / normalized outcome truth 归 Tools;member 只以受控 ref / safe view 引用。 |
| `projects/L1-identity/00-需求文档.md` | `§2 本仓定位与边界` | 平台级成员身份真相归 identity;member 只持运行态身份锚点消费视图。 |
| `projects/L1-work/00-需求文档.md`;`architecture/adr/0004-global-vs-project-member.md` | ProjectMember / GlobalMember 边界 | 项目型运行实例以 `ProjectMemberRef` 为执行主语并关联 `GlobalMemberRef` 身份锚;两者不可替代。 |
| `projects/L1-conversation/00-需求文档.md` | `§2 本仓定位与边界` | 对话真相归 conversation;member 出站只形成 body-free handoff material。 |
| `projects/L1-governance/00-需求文档.md` | `§2`;`§10` | Policy effective / Decision truth 归 governance;入站筛选只消费正式结果。 |
| `projects/L0-core/00-需求文档.md`;`01-架构设计.md` | shared contract / 标准对齐边界 | 共享 ID、ref、metadata、error、CloudEvents envelope 与 W3C Trace Context authority。 |
| `projects/L0-bus/00-需求文档.md` | `§2`;`§7` | 已提交事实的事件传递主干;delivery truth 归 bus。 |
| `projects/L0-sdk/00-需求文档.md` | `§2`;`§12` | 下游 SDK 封装边界,不反向进入 member package 依赖。 |

本文承接当前全局依赖规则和已停审的 `L2-runtime`、`L2-tools` 正式设计链,在 Layer 3 并行窗口内细化 AI Member 容器内成员门面的交互语义。本文不重新定义运行循环、工具契约、成员身份、对话真相、治理裁决、事件传递或共享契约,只收束 member 自身的运行态主体、入站筛选与受控投递、出站交互与发布、交互追溯与成员可见摘要需求。

旧 `README.md`、旧 `00/01/02/03/05/06` 均为 `historical_material`,只用于污染审计。CloudEvents / W3C Trace Context 只经 Core 当前 authority 承接;旧 member-specific type / source / subject / payload / route、AG-UI 事件集、UDS、launch_token 形态、B1-B6 模块、P95 / SLA 数字、Rust / supervisord 不直接继承。同窗口兄弟 `L2-member-service` 已完成需求 Step 9 并停审、Step 10 尚未开始,`L2-member-images` 已完成需求 Step 1~8 且正在 Step 9;两者正式 00 均未停审,只能把已通过门禁的明确边界作为 sibling pending 输入,不能引用进行中结论或升格为稳定合同。

## 2. 本仓定位与边界

> 校准来源:
> - `design-calibration/00_req_step_02_position_boundary.md`
>
> 延伸阅读:
> - 建议继续阅读上述中间产物的"边界判定口径""设计取舍"和"当前文档问题诊断"小节,了解定位如何从 Runtime 边界红线与旧口径差异中收敛。

| 字段 | 内容 |
|---|---|
| 一句话定义 | `L2-member` 是 AI Member 容器内的成员门面真相仓,拥有可归属运行态成员实例的本地在场与对外交互边界语义。 |
| 本仓不是什么 | 它不是推理 / 计划 / 记忆仓,不是工具契约或工具执行仓,不是 capability registry 或外部 adapter,不是容器编排仓,不是镜像构建仓,不是隔离执行仓,不是治理裁决仓,不是对话真相仓,不是观测后端,也不是身份生命周期仓。 |
| 边界对象列表 | 仓:`L2-runtime`;仓:`L2-member-service`;仓:`L2-member-images`;仓:`L0-bus`;仓:`L1-governance`;仓:`L1-conversation`;仓:`L1-identity`;仓:`L2-tools`;概念:交互边界事实 vs 运行决策事实;概念:筛选结论 vs 裁决真相 |
| 单独成仓原因 | 平台需要一个独立于运行决策、宿主编排和传递主干的成员交互边界真相,让入站筛选、受控投递、出站发布和在场语义可解释、可追溯且不打穿相邻 owner。 |

`L2-member` 需要单独存在,因为 AI Member 容器内"什么进入运行视野、什么以何种形态离开容器、成员是否在场"必须有唯一且可审计的 owner,而 Runtime 正式链已把这些排除在自身边界之外。它最容易与 `L2-runtime` 混淆在"交互边界事实"与"运行决策事实"的分界上,与 `L2-member-service` 混淆在"容器内在场语义"与"容器外编排裁决"的分界上,与 `L0-bus` / `L1-governance` / `L1-conversation` 分别混淆在传递、裁决与对话真相上;这些边界不先分开,后续需求、设计和验收都会串线。

### 2.1 边界判定口径

| 判定问题 | 落入 member 的情况 | 不落入 member 的情况 |
|---|---|---|
| 它是否决定"什么进入 Runtime 视野" | 订阅范围决定、入站筛选结论、投递决定与记录 | Runtime 对输入的受理 / 处置、bus delivery、policy 裁决 |
| 它是否决定"成员输出以何种形态离开容器" | 出站交互决定、body-free 材料准备、发布尝试 / gap | Runtime outcome truth、delivery / observed、对话 / 制品真相 |
| 它是否表达"这个成员实例当前在场吗" | 本地 presence 状态、启动语境受理、注册请求 / 存活信号 / 状态报告及本地尝试记录 | 容器启停、注册接受、endpoint registry、host session、健康裁决、凭据签发 |
| 它是否说明"谁在执行" | 项目型实例消费 `ProjectMemberRef` 执行主语并关联 `GlobalMemberRef` 身份锚 | ProjectMember / GlobalMember 真相生命周期;以 GlobalMember 或 Workspace view 代替执行主语 |
| 它是否解释"这个成员能承接什么" | 从上游 ref 派生的能力出口安全视图 | ToolDefinition / capability registry / method body truth |
| 它是否属于容器内两进程的交互语境 | member 侧受理 / 拒绝 / 关联 / 背压语义与安全边界 | Runtime run / decision truth;IPC 物理载体选择 |

## 3. 背景与问题定义

> 校准来源:
> - `design-calibration/00_req_step_03_problem_context.md`
>
> 延伸阅读:
> - 建议继续阅读上述中间产物的"结构化中间产物"和"当前文档问题诊断"小节,了解问题如何从旧痛点表的伪量化与方案化中重新收敛。

### 3.1 业务背景

Quantalithos 的 AI 成员以容器内运行态实例参与协作。`L2-runtime` 与 `L2-tools` 已完成正式设计链,分别收束了受控运行循环和工具行动契约,并把协议接入面、宿主生命周期和成员对外交互排除在各自边界外。Layer 3 并行窗口由此开启,平台需要把"成员容器内的对外交互边界"独立收束为 `L2-member` 的仓级需求,为 Runtime 提供受控的入站语境与出站承接,为宿主与下游提供可解释的在场语义与安全摘要。

### 3.2 现状与问题

| 问题 | 当前表现 | 影响范围 / 后果 |
|---|---|---|
| 成员交互边界缺少单一需求真相 | 入站筛选结论、受控投递决定、出站发布决定尚无按当前上游收束的 owner;旧文档只有管道式模块清单 | Runtime、宿主或 bus 适配层可能各自定义筛选与发布语义,形成多真相;注入类风险的预筛与审计断链 |
| 容器内 member↔runtime 分界只有单侧收束 | Runtime 正式链已锁定 entry 合同与边界红线,member 侧对应的投递、承接、背压语义未成文;`Q-L2R-001` 开放 | 容器内交互靠隐式约定推进;出站的 outcome→发布→delivery→accepted 各层可能被压平 |
| 执行主语、兄弟合同与历史输入尚未分层稳定 | ADR / Work 已支持项目型 `ProjectMemberRef` + `GlobalMemberRef`;member-service 已完成 Step 9 并停审,member-images 已完成 Step 1~8 且正在 Step 9,两者正式 00 均未停审;非项目型主语仍开放。旧材料又混合了 Core 已承接的 CloudEvents / W3C 与无当前 authority 的私有事件细节、载体和指标 | 项目型路径可按双锚成文;宿主字段、打包边界和非项目型路径只能 pending / fail closed;共享标准经 Core 承接,其余逐项 historical / pending |

当前没有可信实现、workload 或测量 authority,因此无法提出有依据的发生率、覆盖率、P95 或 SLA;本章以可核查的 owner 缺口、契约缺口和后果为依据。

### 3.3 业务问题 vs 技术问题

| 类型 | 内容 |
|---|---|
| 业务问题 | 平台缺少对"AI 成员如何安全、可解释、可审计地接入协作体系"的正式需求收束,导致成员在场、听、说三类行为无法被宿主、下游和审查者稳定理解与追溯。 |
| 技术问题 | presence / 入站筛选 / 受控投递 / 出站发布 / 摘要与能力出口的 owner 边界,与 Runtime entry、bus 传递、governance 裁决、identity 锚点、宿主编排之间的消费与失败语义,若不在需求层先讲清,后续设计与实现会在容器内外反复串线,并让旧协议假设回流。 |

## 4. 目标与非目标

> 校准来源:
> - `design-calibration/00_req_step_04_goals_non_goals.md`
>
> 延伸阅读:
> - 建议继续阅读上述中间产物的"目标表""非目标表"和"范围收束"小节,了解旧数字目标如何被废弃并重写为状态 / 边界成立目标。

### 4.1 目标

| ID | 目标 | 完成方向 |
|---|---|---|
| `G-L2M-001` | 成员交互边界真相成立 | 一个运行态成员实例的在场、入站筛选结论、投递决定、出站决定与发布尝试均有 member 单一 owner,可解释、可追溯;后续章节不再出现第二 owner。 |
| `G-L2M-002` | 入站筛选与受控投递语义成立 | 订阅范围、筛选结论(通过 / 降级 / 阻断 / 待定)与向 Runtime 的投递决定可区分;规则来源回链正式 owner,unknown 保守处置,不自建 allowlist。 |
| `G-L2M-003` | 出站承接与发布分层成立 | Runtime committed outcome、member 出站决定、发布尝试 / gap、delivery、下游 accepted 五层可区分;外部失败不回滚本地决定,不声明 delivered / observed / accepted。 |
| `G-L2M-004` | 在场语义、执行主语与宿主协作边界成立 | 项目型实例以 `ProjectMemberRef` 为执行主语并关联 `GlobalMemberRef`;状态可区分;member 只拥有注册请求 / 存活信号 / 状态报告与本地尝试,宿主拥有接受 / registry / session / 健康判定;凭据或主语不可验证时 fail closed。 |
| `G-L2M-005` | 摘要与能力出口的派生边界成立 | 成员可见摘要与能力出口只从本地已提交事实和上游 ref / safe view 派生,body-free、可重建、不反写任何 truth。 |

### 4.2 非目标

| ID | 非目标 | 不做原因 / Owner |
|---|---|---|
| `NG-L2M-001` | LLM 推理、goal / plan、context composition、memory、checkpoint / recovery | `L2-runtime` |
| `NG-L2M-002` | Tool execution、ToolDefinition、normalized outcome truth | `L2-tools` |
| `NG-L2M-003` | Capability registry、外部 MCP / A2A / API adapter、外呼 | `L3-capability-hub` 及 Runtime→Tools 链 |
| `NG-L2M-004` | 容器启停、注册表 truth、健康裁决、policy 下发通道 | `L2-member-service` |
| `NG-L2M-005` | 镜像构建与装配 | `L2-member-images` |
| `NG-L2M-006` | 隔离执行 truth | `L4-sandbox` |
| `NG-L2M-007` | approval / policy truth、本地 allowlist 裁决 | `L1-governance` |
| `NG-L2M-008` | 对话真相、消息展示 | `L1-conversation` / `L5-chat` |
| `NG-L2M-009` | observability backend / observed truth | `L4-observability` |
| `NG-L2M-010` | 身份生命周期、凭据签发 | `L1-identity` / 宿主凭据 owner(`L2M-UP-006`) |
| `NG-L2M-011` | 固定语言、框架、IPC 载体、事件协议、进程管理器、SLA 数字 | 无当前 authority;后移 01~05 并以证据为前提 |

本轮只把 `L2-member` 收束为容器内成员门面真相仓:它拥有在场、入站筛选与投递、出站决定与发布、追溯与摘要出口的交互边界语义,通过正式 seam 消费 Runtime / work / identity / governance / tools 输入;所有正向 seam 在对应 blocker(`L2M-UP-001~008`)闭口前只能是 pending / blocked / waiting / degraded / fail-closed。

## 5. 用户与角色

> 校准来源:
> - `design-calibration/00_req_step_05_users_roles.md`
>
> 延伸阅读:
> - 建议继续阅读上述中间产物的"结构化中间产物"和"设计取舍"小节,了解旧权限矩阵为何废弃。

### 5.1 人类角色

| 角色 | 使用场景 |
|---|---|
| member 维护 / 运维者 | 维护成员门面的订阅范围、出站边界与在场诊断入口;处理 degraded / blocked 状态,不修改相邻仓 truth。 |
| 安全 / 边界审查者 | 审查入站筛选结论的来源回链、forbidden body 是否越界、出站材料是否 body-free、是否存在自建 allowlist 或 fail-open。 |
| 交互问题调查者 | 沿入站记录、投递决定、Runtime 受理 ref、出站尝试 / gap 关联定位丢失、误拦截、重复或未送达问题,区分 member / Runtime / bus / 下游各层责任。 |

### 5.2 系统角色

| 角色 | 使用场景 |
|---|---|
| 运行决策协作方 | 容器内消费 member 投递的入站语境,返回受理 / 拒绝 / 状态,并交回 committed safe material 供出站承接。 |
| 宿主编排协作方 | 提供项目型执行主语与启动语境,接收 member 的注册请求 / 存活信号 / 状态报告;注册接受、endpoint registry、host session、健康判定与编排 truth 留在其自身。 |
| 入站事实提供方 | 经事件主干向成员送达已提交事实;delivery truth 归其自身。 |
| 出站材料消费方 | 消费 member 发布的 body-free 交互材料与观测材料,不据此反写 member truth。 |
| 摘要 / 能力出口消费方 | 只读消费成员状态摘要与能力出口安全视图,理解成员当前可承接范围。 |

角色表只描述职责与接触场景,不授予 authorization。具体 principal、scope、allow / deny 必须来自 Governance / Identity 等正式 owner;任何角色都不得声明 delivered、observed、accepted 或健康裁决结论。本章不设权限矩阵:member 的角色差异体现为接触能力面的不同,且 effective authorization owner 未闭口,矩阵在需求层无法成立。

## 6. 使用方与依赖

> 校准来源:
> - `design-calibration/00_req_step_06_consumers_dependencies.md`
>
> 延伸阅读:
> - 建议继续阅读上述中间产物的"SOP 问题回答""内部仓依赖表""依赖裁剪图"和"path dependency 判定结论"小节。

本章从 `standards/document/全局项目依赖关系与裁剪规则.md` 裁剪 `L2-member` 相关子图。只有类型为"编译期依赖"的关系可以成为 package dependency;运行期与事件协作关系均不得落成 sibling path dependency;pending 只表示关系状态。`ref` 是 runtime / event 关系的受控交互形态,不是第四类依赖;外部 adapter 是禁止直边,fake 仅可作为后续测试替身。

### 6.1 内部仓依赖

| 方向 | 对方 | 提供 / 依赖内容 | 是否闭环前置 | 失效影响 |
|---|---|---|---|---|
| 输入 | `L0-core` | 共享 ID / ref / actor / metadata / error、CloudEvents envelope 与 W3C Trace Context 标准及跨仓契约 authority | 是,但 member-specific contract 未闭口(`L2M-UP-005`) | 跨仓身份、关联和错误语义无法稳定;member 不得本地复制第二套 schema。 |
| 输入 / 输出 | `L0-bus` | 入站事件订阅与出站发布的事件传递主干 | 是 | 入站进入 waiting / degraded,出站形成 attempt / gap;本地筛选与决定记录保留,不回滚。 |
| 输入 / 输出 | `L2-runtime` | entry 合同(受控触发 / 控制 / safe query)与 committed outcome / safe material | 是 | 入站投递 waiting / degraded,不代答;出站无材料可承接;presence 可独立成立。 |
| 输入 / 输出 | `L2-member-service` | 启动语境、凭据 / 注册接受 / host session ref;member 向其提供注册请求、存活信号、状态报告与本地尝试语境 | 在场能力场景前置(契约 pending,`L2M-UP-001`) | 启动语境不可验证时不进入在场;宿主不可达进入 degraded;member 不声明注册已接受、session 已建立或健康已判定。 |
| 输入 | `L1-work` | 项目型实例的 `ProjectMemberRef` 执行主语 | 项目型在场建立的来源前置 | ref 不可解析、scope 冲突或非项目型主语未定义时拒绝入场。 |
| 输入 | `L1-identity` | 与执行主语关联的 `GlobalMemberRef` 身份锚 / 安全摘要 | 在场建立的来源前置 | 锚点不可解析或与 ProjectMember 不一致时拒绝入场;不猜测身份。 |
| 输入 | `L1-governance` | Policy effective 结果 / safe snapshot(入站筛选与出站约束) | 入站筛选分级的场景前置(`L2M-UP-007`) | 规则来源 unknown / stale / conflict 时筛选保守处置(降级或阻断),不自建 allowlist。 |
| 输入 | `L2-tools`、`L3-method-library` | 工具契约 ref / 定义 safe view(能力出口派生) | 否(能力出口可裁剪) | 能力出口视图 stale / gap,核心闭环不受影响。 |
| 输出 | `L1-conversation`、`L4-observability` 等下游 | body-free 交互 / 观测材料(经事件主干) | 否 | 下游消费缺口独立显式;不反写 member truth。 |
| 输出 | `L0-sdk`、产品入口 | 未来下游消费面 | 否,当前不进入主链 | 不影响本仓 truth;不反向进入 package 依赖。 |

### 6.2 外部系统依赖

当前阶段,`L2-member` 无需要纳入主链的正式外部系统依赖。容器运行时、进程管理器等属于承载载体,不构成需求层外部系统依赖;外部平台协作一律经平台内正式仓边界。

### 6.3 本仓依赖裁剪表

| 关联项目 | 全局关系 | 本仓角色 | 依赖类型 | 是否进入当前文档主链 | 裁剪理由 |
|---|---|---|---|---|---|
| `L0-core` | L2-member 编译期依赖栏 | 依赖方 | 编译期依赖 | 是 | 唯一 compile 候选;member-specific shared schema 未闭口时保持 pending,不本地 shadow。 |
| `L0-bus` | 订阅 / 发布成员运行事件 | 协作方 | 事件协作依赖 | 是 | 入站 / 出站事件主干;delivery truth 归 bus。 |
| `L2-runtime` | member-service / runtime IPC 边界;Runtime 侧把 member 列为 Entry consumers | 协作方(容器内 peer) | 运行期依赖 | 是 | member 向 Runtime entry 提交入站语境、消费 safe material;正向 mapping 受 `L2M-UP-003` 约束。 |
| `L2-member-service` | member-service / runtime IPC 边界 | 被编排方 | 运行期依赖 | 是(契约 pending) | 对方需求 Step 9 已通过并停审、Step 10 尚未开始;注册、接入 / session、健康 owner 分层可作为 pending 输入,正式 00 与字段合同仍未闭口(`L2M-UP-001`)。 |
| `L1-work` | "consumes L1 according to role" 裁剪 | 依赖方 | 运行期依赖(ref) | 是 | 消费项目型 `ProjectMemberRef` 执行主语;不拥有 ProjectMember truth。 |
| `L1-governance` | "consumes L1 according to role" 裁剪 | 依赖方 | 运行期依赖 + 事件协作依赖 | 是 | 消费 Policy effective 结果 / safe snapshot 与变化事件;unknown fail closed(`L2M-UP-007`)。 |
| `L1-identity` | "consumes L1 according to role" 裁剪 | 依赖方 | 运行期依赖 | 是 | 只消费身份锚点 ref / 安全摘要;身份生命周期不归本仓。 |
| `L2-tools` | "consumes L3 according to role" 经 Tools 合同裁剪 | 依赖方 | 运行期依赖(弱) | 是(弱,能力出口) | 只以 ref / safe view 派生能力出口;不复制 ToolDefinition。 |
| `L3-method-library` | "consumes L3 according to role" 裁剪 | 依赖方 | 运行期依赖(弱) | 是(弱,能力出口) | role / method 定义 ref / safe view;不保存正文。 |
| `L1-conversation` | 出站材料的下游消费方 | 被依赖方 | 事件协作依赖 | 是 | member 只交 body-free 材料;对话真相归 conversation。 |
| `L4-observability` | 横切观测材料消费 | 协作方 | 事件协作依赖 | 是 | 安全材料附着事件 carrier;producer / route 未闭口(`L2M-UP-004`)。 |
| `L2-member-images` | 打包 member 运行部件 | 被打包方 | 不适用 | 否 | 对方需求 Step 1~8 已通过且 Step 9 进行中;静态镜像资产 / pinned 入口方向仍属 sibling pending,构建资产边界不进入运行主链(`L2M-UP-002`)。 |
| `L0-sdk` | 未来下游封装 | 被依赖方 | 不适用 | 否 | 仅 future 边界记录;不反向进入 package。 |
| `L4-sandbox`、`L3-capability-hub`、L5/L6 | 无直接边 | — | 不适用 | 否 | 分别经 Runtime→Tools / SDK 间接协作;不建立直边。 |

### 6.4 本仓依赖类型分类表

| 依赖类型 | 关联项目 | 本仓如何使用 / 提供能力 | 后续文档落点 |
|---|---|---|---|
| 编译期依赖 | `L0-core` | 正式 shared context 类别;member-specific contract 闭口后才能定稿。 | `01~03`;`07` |
| 运行期依赖 | `L2-runtime`;`L2-member-service`;`L1-work`;`L1-governance`;`L1-identity`;(弱)`L2-tools` / `L3-method-library` | entry seam;宿主请求 / 信号 / 报告与 acceptance / session ref 协作;执行主语、Policy 结果与身份锚消费;定义 ref 派生能力出口。 | `01~05`;`07` |
| 事件协作依赖 | `L0-bus`;`L1-governance`(变化输入);`L1-conversation` / `L4-observability`(输出) | 入站订阅、出站发布、policy 变化承接与安全材料交接。 | `00~05`;`07` |

### 6.5 本仓禁止依赖表

| 禁止依赖 | 禁止原因 | 正确协作方式 |
|---|---|---|
| `L2-member -> L2-runtime` 源码 / package 依赖 | 会吸收 run / decision truth,破坏容器内双 owner 分层 | 运行期 entry seam;各自保留 truth。 |
| `L2-member -> L2-member-service` 源码依赖 | 会把编排 / 注册表 / 健康裁决 truth 绑入门面 | 运行期宿主协作面 + 事件;契约 pending 时 fail closed。 |
| `L2-member -> L1-governance` 本地 policy 缓存作裁决 | 形成第二 policy truth,fail-open 风险 | 只消费正式结果 / safe snapshot;unknown 保守处置。 |
| `L2-member -> L1-identity` 源码依赖或身份生命周期写入 | 越过身份 truth owner | 运行期 ref / 安全摘要消费。 |
| `L2-member -> provider / MCP / A2A / 任意外部 API` 直连或自建通用外部监听面 | 越过 capability-hub / tools / sandbox owner并扩大攻击面 | 外部能力经 Runtime→Tools 正式合同;Bus、宿主与 Runtime 只经正式 runtime / event seam;member 不做外部 adapter。 |
| `L2-member -> L1-conversation / L1-artifact` 正文写入 | 反写下游对话 / 制品真相 | 只发布 body-free handoff material。 |
| `L2-member -> L0-bus` 作为业务裁决链 | Bus 只拥有传递 truth | 本地 truth 提交后按事件协作发布。 |
| `L2-member -> L0-sdk` 源码 / package 依赖 | SDK 是下游,反向依赖成环 | member 暴露正式边界,SDK 下游封装。 |
| 任一 sibling(除 Core)成为 path dependency | 违反全局裁剪与层级 | runtime / event / ref / adapter seam。 |

### 6.6 依赖裁剪图

#### 依赖裁剪图: L2-member

```text
L2-member --[compile]--> L0-core
L2-member --[event]----> L0-bus
L2-member <-[runtime]--> L2-runtime          (容器内 entry seam)
L2-member <-[runtime]--> L2-member-service   (宿主协作面, 契约 pending)
L2-member --[runtime]--> L1-work             (ProjectMemberRef 执行主语)
L2-member --[runtime]--> L1-governance       (policy 结果消费; 变化经 [event])
L2-member --[runtime]--> L1-identity         (GlobalMemberRef 身份锚)
L2-member --[runtime]--> L2-tools / L3-method-library  (弱: 能力出口 ref)

L1-conversation / L4-observability --[event]--> L0-bus  (消费 member 出站材料)
```

图示说明:
- 本图只展示 `L2-member` 相关裁剪边,不展示全 27 仓。
- `[compile]` 仅 `L0-core`;`[runtime]` 与 `[event]` 不得写成 package dependency。
- 双向 `[runtime]` 表示协作 seam 的两个消费方向,不表示调用时序或 IPC 载体。
- member-service 边契约未闭口,仅表达能力级 seam;Tools / Method 为可裁剪弱边。
- `ref` 不改变依赖类型;外部 adapter 与 fake 均不属于当前正向依赖。
- 箭头表达依赖 / 消费 / 协作方向,不表达事件传播时序或实施顺序。

### 6.7 本章结论

当前阶段主依赖集中在 `L0-core`、`L0-bus`、`L2-runtime` 三条闭环前置边;`L2-member-service`、`L1-work`、`L1-identity` 与 `L1-governance` 是场景 / 来源前置且部分 pending;`L2-tools` / `L3-method-library` 为可裁剪弱边;member-images、SDK、sandbox、capability-hub 均不进入主链。只有 `L0-core` 是编译期候选;runtime / event 是依赖类型,ref 是交互形态,adapter 直边被禁止,fake 不构成真实集成。

## 7. 核心能力闭环

> 校准来源:
> - `design-calibration/00_req_step_07_core_capability_loop.md`
>
> 延伸阅读:
> - 建议继续阅读上述中间产物的"当前材料诊断""核心能力节点表""进入 / 退出条件与禁止误写"和"能力节点执行顺序与停审清单"小节。

`L2-member` 的核心能力闭环不是订阅、过滤、发布等动作的功能串联,而是:一个项目型成员实例必须以 `ProjectMemberRef` 执行主语、关联的 `GlobalMemberRef` 身份锚与可验证启动语境进入在场状态;在场成员必须能对入站事实形成受控筛选与投递决定;必须能对运行结果形成受控出站决定与发布尝试;上述事实必须可安全追溯;成员状态与可承接范围必须能被外部安全理解。非项目型实例在正式执行主语闭口前不进入正向闭环。

```text
项目型运行态成员能够以可验证执行主语与启动语境进入在场状态
  -> 入站事实能够形成受控的筛选结论与投递决定
  -> 运行结果能够形成受控的出站决定与安全发布
  -> 成员交互事实能够被安全追溯
  -> 成员状态与可承接范围能够被外部安全理解
```

本图只表达能力成立的逻辑依赖关系,不是每次交互的运行时路径(入站与出站在运行中并发存在),也不是接口时序、事件传播顺序或实施步骤。

### 7.1 核心能力节点

| 顺序 | 节点 | 能力成立描述 | member owner | 外部 owner 边界 |
|---:|---|---|---|---|
| 1 | `C-L2M-1` 运行态在场与宿主协作 | 项目型实例以 `ProjectMemberRef` 执行主语、关联 `GlobalMemberRef` 身份锚与可验证启动语境进入在场,并向宿主提供注册请求 / 存活信号 / 状态报告 | 本地 presence、启动语境受理、请求 / 信号 / 报告及尝试记录 | ProjectMember / GlobalMember truth、凭据签发、注册接受 / endpoint registry / host session / 健康判定 / 编排、容器生命周期 |
| 2 | `C-L2M-2` 入站筛选与受控投递 | 订阅范围决定、入站筛选结论(通过 / 降级 / 阻断 / 待定)与向 Runtime 的投递决定成立且可解释 | 订阅决定、筛选结论、投递决定与记录 | bus delivery truth、policy 裁决、Runtime 受理处置 |
| 3 | `C-L2M-3` 出站承接与安全发布 | 从 Runtime committed safe material 形成出站决定、body-free 材料与发布尝试 / gap | 出站决定、材料准备、attempt / gap | Runtime outcome truth、bus delivery、对话 / 制品 / observed truth |
| 4 | `C-L2M-4` 交互追溯与安全材料 | 在场变化、筛选结论、投递决定、出站尝试可按同一关联语境安全追溯,并形成 body-free 低敏观测材料 | 本地交互事实追溯、安全材料准备与交接尝试 | observability backend / observed truth、evidence verdict |
| 5 | `C-L2M-5` 成员摘要与能力出口 | body-free 成员状态摘要与(可裁剪的)能力出口安全视图可从已提交事实与上游 ref 派生、可重建 | 摘要投影、能力出口派生视图 | Runtime safe view source、ToolDefinition / method body truth |

### 7.2 进入 / 退出条件

| 节点 | 进入条件 | 退出条件 | 禁止误写 |
|---|---|---|---|
| `C-L2M-1` | 项目型 `ProjectMemberRef`、关联 `GlobalMemberRef` 与可验证启动语境存在 | 本地在场状态可区分,注册请求 / 存活信号 / 状态报告可形成;任一主语、关联或凭据不可验证时 fail closed | 以 GlobalMember / Workspace view 代替执行主语、凭据形态、注册接受 / session truth、健康阈值、容器编排动作 |
| `C-L2M-2` | C1 在场成立;订阅范围有来源 | 筛选结论与投递决定可区分、可回链规则来源;Runtime 不可用只 waiting 不代答 | 事件协议、过滤算法、本地 allowlist、Runtime 受理语义 |
| `C-L2M-3` | C1 在场成立;存在 Runtime committed safe material | 出站决定与发布尝试可区分;材料不合格拒绝出站;delivery 失败不回滚决定 | delivered / observed / accepted 声明、对话事实写入、协议 / topic |
| `C-L2M-4` | C2 / C3 已形成本地已提交事实 | 交互事实按关联语境可追溯;安全材料 body-free;route 未闭口只 attempt / gap | observed truth、完整日志、hidden reasoning、evidence verdict |
| `C-L2M-5` | C1~C4 有已提交事实;上游 ref 可用(能力出口路径) | 摘要 / 出口视图可派生、可重建、不反写;ref 失效显式 stale / gap | 定义正文复制、第二 registry、写源语义 |

### 7.3 能力层级划分

| 分类 | 内容 |
|---|---|
| 核心能力闭环 | 运行态在场与宿主协作;入站筛选与受控投递;出站承接与安全发布;交互追溯与安全材料;成员摘要与能力出口(出口子项可裁剪)。 |
| 外围增强能力 | 筛选统计与误拦截诊断摘要;成员交互历史只读浏览;订阅 / 出站配置的可解释视图;能力出口检索 / 比较体验。 |
| 边界外能力 | 运行决策链(loop / plan / memory / checkpoint);工具契约与执行;capability registry / 外呼 adapter;容器编排 / 健康裁决 / policy 下发通道;镜像构建;隔离执行;approval / policy truth;对话真相;observability backend;身份生命周期与凭据签发。 |

## 8. 用户故事

> 校准来源:
> - `design-calibration/00_req_step_08_user_stories.md`
>
> 延伸阅读:
> - 建议继续阅读上述中间产物的"按能力节点的核心故事""外围增强故事"和"跨能力故事审计"小节。

本章围绕 `C-L2M-1~5` 组织 16 条核心故事,3 条外围增强故事单列。正向故事只覆盖项目型实例;非项目型 / personal 实例受 `Q-L2M-009` 约束并 fail closed。容器编排、policy 下发、对话展示等边界外目标不进入正式故事表。

### 8.1 核心闭环故事

| 编号 | 用户故事 | 目标类型 | 业务价值 | 与核心能力闭环的关系 |
|---|---|---|---|---|
| `US-L2M-001` | 作为宿主编排协作方,我希望以可验证的 `ProjectMemberRef` 执行主语、关联 `GlobalMemberRef` 身份锚与启动语境让项目型成员实例进入在场,以便平台确知哪个项目成员正在该宿主中工作。 | 核心闭环 | 在场从一开始就是项目可归属的正式状态,不是进程存活或全局身份存在的副产品。 | 支撑 C-L2M-1 |
| `US-L2M-002` | 作为 member 维护 / 运维者,我希望区分成员的就绪、降级、退出与未知状态,以便诊断和处置不依赖猜测。 | 核心闭环 | 在场语义可判别,避免"进程在但成员不可用"的模糊态。 | 支撑 C-L2M-1 |
| `US-L2M-003` | 作为宿主编排协作方,我希望持续获得成员的注册请求、存活信号与状态报告,以便我自己的注册接受、会话、健康裁决与编排决定有可靠输入。 | 核心闭环 | member 不越权声明接受、session 或健康结论。 | 支撑 C-L2M-1 |
| `US-L2M-004` | 作为安全 / 边界审查者,我希望不可验证、关联冲突或缺少项目执行主语的启动语境被拒绝进入在场,以便匿名、伪造或错项目实例无法接入协作体系。 | 核心闭环 | fail-closed 的在场门槛。 | 支撑 C-L2M-1 |
| `US-L2M-005` | 作为运行决策协作方,我希望只接收经过筛选、带来源与安全语境的入站输入,以便运行决策不直接暴露于事件主干与注入类风险。 | 核心闭环 | Runtime 的输入面受控,预筛在"进入决策前"发生。 | 支撑 C-L2M-2 |
| `US-L2M-006` | 作为 member 维护 / 运维者,我希望成员的订阅范围由其身份 / 角色语境决定并可解释,以便成员不多听、不漏听。 | 核心闭环 | 订阅边界可归责。 | 支撑 C-L2M-2 |
| `US-L2M-007` | 作为安全 / 边界审查者,我希望每条入站事实的筛选结论(通过 / 降级 / 阻断 / 待定)回链正式规则来源,以便预筛不是黑盒也不是自建裁决。 | 核心闭环 | 筛选可审计;规则 truth 留在 owner。 | 支撑 C-L2M-2 |
| `US-L2M-008` | 作为运行决策协作方,我希望在我不可用或拒绝受理时,入站语境保持等待或降级而不是被代答或丢弃,以便恢复后交互语境仍可继续。 | 核心闭环 | Runtime 不可用不产生伪投递结果。 | 支撑 C-L2M-2 |
| `US-L2M-009` | 作为运行决策协作方,我希望把已提交的运行结果安全材料交给成员门面出站,以便我不直接接触事件主干、协议与下游。 | 核心闭环 | Runtime 的输出面受控;出站有唯一决定层。 | 支撑 C-L2M-3 |
| `US-L2M-010` | 作为出站材料消费方,我希望成员发布的材料是 body-free、已脱敏且可关联的,以便消费不泄漏隐藏推理、secret 或原始正文。 | 核心闭环 | 出站安全门禁。 | 支撑 C-L2M-3 |
| `US-L2M-011` | 作为交互问题调查者,我希望区分"成员已决定出站""发布已尝试""传递结果如何""下游是否接受"四层状态,以便未送达问题能定位到正确责任层。 | 核心闭环 | 分层不压平;外部失败不改写本地决定。 | 支撑 C-L2M-3 |
| `US-L2M-012` | 作为交互问题调查者,我希望沿同一关联语境追溯在场变化、筛选结论、投递决定与出站尝试,以便丢失、误拦截、重复或未送达可以被解释。 | 核心闭环 | 交互事实可归责,门面不是黑盒。 | 支撑 C-L2M-4 |
| `US-L2M-013` | 作为安全 / 边界审查者,我希望追溯与观测材料不含事件正文、隐藏推理或 secret,以便审计能力不以泄漏为代价。 | 核心闭环 | 追溯与最小暴露同时成立。 | 支撑 C-L2M-4 |
| `US-L2M-014` | 作为出站材料消费方(横切观测),我希望获得低敏、低基数、可关联的成员交互观测材料,即使观测通道未就绪也能看到本地尝试与缺口,以便平台观测不依赖伪造 readiness。 | 核心闭环 | 观测材料 local-truth-first;attempt / gap 可判断。 | 支撑 C-L2M-4 |
| `US-L2M-015` | 作为摘要 / 能力出口消费方,我希望获得 body-free 的成员状态与交互摘要,以便无需进入容器即可理解成员当前情况。 | 核心闭环 | 成员可被外部安全理解。 | 支撑 C-L2M-5 |
| `US-L2M-016` | 作为摘要 / 能力出口消费方,我希望通过安全视图了解成员当前可承接的能力范围,且视图来源失效时显式 stale / gap,以便协作分派不依赖过期或猜测信息。 | 核心闭环(可裁剪子项) | 能力出口可信且不复制定义正文。 | 支撑 C-L2M-5 |

### 8.2 外围增强故事

| 编号 | 用户故事 | 目标类型 | 业务价值 | 与核心能力闭环的关系 |
|---|---|---|---|---|
| `US-L2M-E01` | 作为 member 维护 / 运维者,我希望查看筛选统计与误拦截诊断摘要,以便调整订阅与筛选边界的申请有依据。 | 外围增强 | 提升维护效率,不改筛选 truth。 | 支撑外围增强:筛选统计 / 诊断 |
| `US-L2M-E02` | 作为交互问题调查者,我希望只读浏览成员交互历史摘要,以便缩短定位时间。 | 外围增强 | 改善诊断体验,不替代 conversation / observability。 | 支撑外围增强:历史浏览 |
| `US-L2M-E03` | 作为 member 维护 / 运维者,我希望看到订阅与出站边界当前生效配置的可解释视图,以便变更评审有依据。 | 外围增强 | 配置可解释,不成为配置 truth。 | 支撑外围增强:配置可解释视图 |

### 8.3 故事与闭环映射

| 核心能力节点 | 对应核心故事 |
|---|---|
| `C-L2M-1` | `US-L2M-001~004` |
| `C-L2M-2` | `US-L2M-005~008` |
| `C-L2M-3` | `US-L2M-009~011` |
| `C-L2M-4` | `US-L2M-012~014` |
| `C-L2M-5` | `US-L2M-015~016` |
| 外围增强 | `US-L2M-E01~E03` |

## 9. 功能需求

> 校准来源:
> - `design-calibration/00_req_step_09_functional_requirements.md`
>
> 延伸阅读:
> - 建议继续阅读上述中间产物的"当前材料诊断""按能力节点的核心功能需求"和"跨能力功能审计"小节。

### 9.1 核心功能需求

| ID | 功能 | 能力类型 | 说明 | 支撑节点 | 对应故事 |
|---|---|---|---|---|---|
| `FR-L2M-001` | 启动语境受理与在场建立 | 核心闭环能力 | 系统必须以可验证的项目型 `ProjectMemberRef` 执行主语、关联 `GlobalMemberRef` 身份锚与启动语境建立本地在场;来源不完整、关联冲突或不可验证时拒绝,非项目型主语未闭口时 fail closed。 | C-L2M-1 | US-L2M-001、004 |
| `FR-L2M-002` | 在场状态与退出语义 | 核心闭环能力 | 系统必须区分 starting / ready / degraded / draining / terminated / unknown 等在场状态;宿主或运行侧状态不得覆盖本地在场记录,退出与异常变化显式可追溯。 | C-L2M-1 | US-L2M-002 |
| `FR-L2M-003` | 宿主注册与存活协作面 | 核心闭环能力 | 系统必须向宿主形成注册请求、存活信号、状态报告及本地尝试记录,并关联宿主 acceptance / session ref;不得声明注册已接受、session 已建立或健康已判定。 | C-L2M-1 | US-L2M-003 |
| `FR-L2M-004` | 订阅范围管理 | 核心闭环能力 | 系统必须依据成员身份 / 角色语境形成可解释的订阅范围决定;来源不可验证的订阅变化拒绝。 | C-L2M-2 | US-L2M-006 |
| `FR-L2M-005` | 入站筛选 | 核心闭环能力 | 系统必须在正式授权范围内对入站事实及必要正文进行瞬时受控检查,形成通过 / 降级 / 阻断 / 待定结论;规则来源回链正式 owner,unknown / stale / conflict 时保守处置,不自建 allowlist,不把正文保存为 member truth。 | C-L2M-2 | US-L2M-005、007 |
| `FR-L2M-006` | 受控投递 | 核心闭环能力 | 系统必须以 typed ref、safe snapshot 或受控安全语境提交通过筛选的输入,携带来源、scope 与筛选结论,不得透明转发 raw body;对方拒绝或不可用时 waiting / degraded,不代答、不伪造受理、不盲目重放。 | C-L2M-2 | US-L2M-005、008 |
| `FR-L2M-007` | 出站承接与交互决定 | 核心闭环能力 | 系统必须承接运行决策协作方的 committed safe material,形成"发布什么、向哪个正式边界、以何种安全形态"的出站决定;材料含正文 / secret / 隐藏推理时拒绝出站。 | C-L2M-3 | US-L2M-009、010 |
| `FR-L2M-008` | 发布尝试与分层状态 | 核心闭环能力 | 系统必须向事件主干提交发布尝试并记录 attempt / gap;delivery 失败不回滚出站决定,不声明 delivered / observed / accepted;迟到 / 重复反馈形成新关联事实。 | C-L2M-3 | US-L2M-011 |
| `FR-L2M-009` | 交互事实追溯 | 核心闭环能力 | 系统必须让在场变化、订阅决定、筛选结论、投递决定、出站决定与发布尝试按同一关联语境可追溯;追溯材料 body-free。 | C-L2M-4 | US-L2M-012、013 |
| `FR-L2M-010` | 安全观测材料形成与交接 | 核心闭环能力 | 系统必须从已提交本地事实形成最小必要、body-free、已脱敏、可关联的观测 / 审计材料并尝试交接;观测通道未就绪时只保留本地尝试 / 缺口,不声明 observed。 | C-L2M-4 | US-L2M-013、014 |
| `FR-L2M-011` | 成员状态摘要投影 | 核心闭环能力 | 系统必须从本地已提交事实与运行侧 safe view 派生 body-free 成员状态 / 交互摘要;可延迟、可重建,不成为写源。 | C-L2M-5 | US-L2M-015 |
| `FR-L2M-012` | 能力出口安全视图 | 核心闭环能力(可裁剪子项) | 系统必须能从工具契约 / 定义 ref 派生成员可承接能力的安全出口视图;ref 失效显式 stale / gap,不复制定义正文,不形成第二 registry。 | C-L2M-5 | US-L2M-016 |

### 9.2 能力级输入、输出、触发与失败语境

| FR | 输入 | 输出 | 触发 | 必须显式的失败语境 |
|---|---|---|---|---|
| `FR-L2M-001` | `ProjectMemberRef`、关联 `GlobalMemberRef`、启动语境 / 凭据 ref | 本地在场建立或拒绝结论 | 项目型实例启动 | subject invalid / unsupported、identity unresolved、association conflict、credential unverifiable |
| `FR-L2M-002` | 在场变化来源 | 显式在场状态与变化记录 | 状态变化 | unknown 不压平为 ready / terminated;非法迁移拒绝 |
| `FR-L2M-003` | 本地在场状态、宿主语境 | 注册请求 / 存活信号 / 状态报告、本地尝试与 acceptance / session ref | 在场建立、周期或状态变化 | host unreachable → degraded;acceptance / session / health unknown 不压平为成功 |
| `FR-L2M-004` | 身份 / 角色语境 ref、订阅变化请求 | 订阅范围决定与记录 | 在场建立、角色语境变化 | source unverifiable、scope conflict |
| `FR-L2M-005` | 入站事实 ref、授权瞬时检查材料、规则来源 safe snapshot | 筛选结论 + body-free 处置记录 | 入站事实到达 | rule source unknown / stale / conflict → 保守处置;inspection unauthorized / unsafe;正文不得留存 |
| `FR-L2M-006` | typed ref、safe snapshot 或受控安全语境 | 投递决定 + Runtime 受理结果 ref | 筛选通过 | raw body transparent forwarding 拒绝;runtime rejected / unavailable / timeout / unknown;duplicate 不重复投递 |
| `FR-L2M-007` | Runtime committed safe material ref | 出站决定 + body-free 发布材料 | 运行结果交接 | material unsafe → 拒绝;source mismatch |
| `FR-L2M-008` | 出站决定与材料 | 发布尝试记录、attempt / gap、反馈 ref | 出站决定成立 | route pending、delivery failure、duplicate、late feedback 不逆写 |
| `FR-L2M-009` | 本地已提交交互事实 | 关联追溯语境 | 交互事实形成 | correlation 不完整 → 显式缺口,不猜测补齐 |
| `FR-L2M-010` | 已提交事实、安全材料约束 | body-free 材料 + attempt / gap | 事实提交、周期交接 | route / producer pending;交接失败不回滚本地事实 |
| `FR-L2M-011` | 本地事实、Runtime safe view | 摘要投影 | 事实变化、重建请求 | source stale → 显式 freshness;投影失败独立,不写源 |
| `FR-L2M-012` | tools / method 定义 ref / safe view | 能力出口视图 | ref 变化、消费请求 | ref unresolved / stale → stale / gap 显式 |

### 9.3 外围增强功能

| ID | 功能 | 边界 |
|---|---|---|
| `FR-L2M-E01` | 筛选统计与误拦截诊断摘要 | 只读派生统计与诊断摘要,不改筛选 truth,不自动调规则。 |
| `FR-L2M-E02` | 交互历史只读浏览 | body-free 历史摘要浏览,不替代 conversation / observability truth。 |
| `FR-L2M-E03` | 生效配置可解释视图 | 展示订阅 / 出站边界当前生效配置及来源,不成为配置 truth。 |

能力级失败语义必须区分 invalid、unverifiable、missing source、conflict、stale、unavailable、waiting、blocked、degraded、duplicate、late、unknown、delivery gap;这些状态不得互相压平或由旧框架名称替代。

## 10. 业务规则与边界约束

> 校准来源:
> - `design-calibration/00_req_step_10_business_rules_boundaries.md`
>
> 延伸阅读:
> - 建议继续阅读上述中间产物的"当前文档问题诊断""结构化中间产物"和"跨能力规则审计"小节,了解旧 BR-001~005 如何被逐项审计与重写。

| ID | 类型 | 正式约束 | 约束对象 | 功能映射 |
|---|---|---|---|---|
| `BR-L2M-001` | 不变量 | 项目型成员在场必须以可验证 `ProjectMemberRef` 为执行主语,关联 `GlobalMemberRef` 身份锚和启动语境;GlobalMember、Workspace view、匿名或不可归属进程不得替代执行主语。 | 在场主体 | FR-L2M-001 |
| `BR-L2M-002` | 禁止行为 | 启动语境或凭据引用缺失、冲突或不可验证时,不得进入在场,不得降级放行。 | 入场门槛 | FR-L2M-001 |
| `BR-L2M-003` | 显式变化 | 在场状态变化(含退出与异常)必须显式发生并可追溯,不得由心跳缺失、进程信号或宿主状态隐式改写。 | 在场生命周期 | FR-L2M-002 |
| `BR-L2M-004` | 边界约束 | starting / ready / degraded / draining / terminated / unknown 不得互相压平;unknown 不得自动升级为 ready 或 terminated。 | 在场状态语义 | FR-L2M-002 |
| `BR-L2M-005` | 边界约束 | member 只拥有注册请求、存活信号、状态报告及本地尝试记录;注册接受、endpoint registry、host session、健康判定、恢复 / 重启决定、容器编排与凭据签发不归本仓。 | member / member-service 边界 | FR-L2M-003 |
| `BR-L2M-006` | 禁止行为 | 宿主不可达或反馈 unknown 时不得伪装注册已接受、session 已建立或健康已判定,也不得仅凭宿主缺失自行改写为 terminated;本地只进入可解释 degraded / unknown 并保留记录。 | 宿主协作失效 | FR-L2M-003 |
| `BR-L2M-007` | 不变量 | 订阅范围必须回指成员身份 / 角色语境的可验证来源;不得由字符串、显示名或私有配置猜测订阅边界。 | 订阅决定 | FR-L2M-004 |
| `BR-L2M-008` | 不变量 | 每条入站事实必须形成显式筛选结论(通过 / 降级 / 阻断 / 待定),结论必须回链规则来源。 | 筛选结论 | FR-L2M-005 |
| `BR-L2M-009` | 禁止行为 | 不得自建 allowlist / denylist 作为裁决真相;规则来源 unknown / stale / conflict 时必须保守处置(降级或阻断),不得默认放行。 | 筛选规则来源 | FR-L2M-005 |
| `BR-L2M-010` | 边界约束 | 筛选结论是预筛处置事实,不是 policy 裁决;policy truth 归 governance,bus delivery truth 归 bus。 | member / governance / bus 边界 | FR-L2M-005 |
| `BR-L2M-011` | 不变量 | 正式授权允许 member 为筛选进行必要的瞬时受控正文检查,但不得持久化正文;投递必须采用 typed ref、safe snapshot 或受控安全语境并带来源、scope 与筛选结论,不得透明转发 raw body。 | 筛选与投递语境 | FR-L2M-005/006 |
| `BR-L2M-012` | 禁止行为 | 运行侧拒绝、不可用或超时时,不得代答、不得伪造受理结果、不得对 unknown 结果盲目重放。 | 投递失效 | FR-L2M-006 |
| `BR-L2M-013` | 显式变化 | 投递决定与运行侧受理结果必须显式关联;duplicate / late 输入形成新关联事实,不逆写既有记录。 | 投递记录 | FR-L2M-006 |
| `BR-L2M-014` | 不变量 | 出站决定必须锚定运行侧 committed safe material 的正式引用;member 不得自创运行结果或修改其语义。 | 出站承接 | FR-L2M-007 |
| `BR-L2M-015` | 禁止行为 | 事件正文、secret、隐藏推理、raw provider body 不得进入出站材料;材料不满足 body-free / 脱敏 / 可关联门禁时拒绝出站。 | 出站安全门禁 | FR-L2M-007 |
| `BR-L2M-016` | 边界约束 | Runtime outcome、member 出站决定、发布尝试、bus delivery、下游 accepted 五层必须保持可区分;任何一层不得替代或压平其他层。 | 出站分层 | FR-L2M-007/008 |
| `BR-L2M-017` | 禁止行为 | delivery 失败、receipt、下游摘要或 observed 状态不得回滚、改写或重新裁决本地出站决定。 | 外部反馈边界 | FR-L2M-008 |
| `BR-L2M-018` | 显式变化 | 发布尝试、降级与缺口必须显式记录;后续尝试形成新事实,不覆盖既有 attempt / gap。 | 发布记录 | FR-L2M-008 |
| `BR-L2M-019` | 边界约束 | member 不得暴露通用外部网络监听面,不得任意直连 provider、MCP / A2A 或外部 API;Bus、宿主与 Runtime 的正式 event / runtime seam 不在禁止范围内,外部能力经 Runtime→Tools 链。 | 对外边界 | FR-L2M-007/008 |
| `BR-L2M-020` | 审计约束 | 在场变化、订阅决定、筛选结论、投递决定、出站决定与发布尝试必须能按同一关联语境回链来源、目的与结果分类。 | 交互追溯 | FR-L2M-009 |
| `BR-L2M-021` | 禁止行为 | 追溯与观测材料不得包含事件正文、隐藏推理、secret 或高基数用户内容;追溯不等于完整日志。 | 追溯最小暴露 | FR-L2M-009/010 |
| `BR-L2M-022` | 边界约束 | 观测材料交接的 attempt / delivery / observed 分层独立;observed truth 归 observability,不得由 member 声明。 | member / observability 边界 | FR-L2M-010 |
| `BR-L2M-023` | 显式变化 | 观测交接失败、不可用或重复必须显式可见,但不得解释为 member 本地交互失败。 | 观测交接 | FR-L2M-010 |
| `BR-L2M-024` | 不变量 | 摘要与能力出口只能从本地已提交事实和上游正式 ref / safe view 派生;可延迟、可重建。 | 派生视图 | FR-L2M-011/012 |
| `BR-L2M-025` | 禁止行为 | 摘要 / 出口视图不得反写任何 truth,不得复制工具 / 方法定义正文,不得形成第二 registry。 | 派生边界 | FR-L2M-011/012 |
| `BR-L2M-026` | 显式变化 | 视图来源失效、过期或冲突必须显式呈现为 stale / gap,不得以旧值冒充当前状态。 | 视图新鲜度 | FR-L2M-011/012 |
| `BR-L2M-027` | 边界约束 | member 不拥有运行决策、工具契约与执行、capability registry、容器编排、镜像构建、隔离执行、approval / policy truth、对话真相、observability backend 或身份生命周期。 | 全仓 owner 边界 | 全部 FR |
| `BR-L2M-028` | 边界约束 | 只有 `L0-core` 可成为编译期依赖候选;运行期与事件协作关系不得写成 package dependency;sibling pending 契约不得被当作已闭口。 | 依赖裁剪 | 全部 FR |
| `BR-L2M-029` | 不变量 | 任一正向 seam(宿主合同、entry mapping、member-specific Core schema / event route、凭据 owner、筛选规则来源、观测 route、非项目型执行主语)未闭口时,受影响能力只能 blocked / waiting / degraded / fail-closed,不得声明 ready。 | 开放 seam | 全部 FR |
| `BR-L2M-030` | 审计约束 | member 域关键事实的记录不得保存外部正文或隐藏推理正文;授权瞬时检查不转化为持久化 truth,正确记录形态是 typed ref、safe snapshot、结论分类或 redacted marker。 | 记录形态 | FR-L2M-005~012 |

规则类型分布:不变量 `BR-L2M-001/007/008/011/014/024/029`;禁止行为 `BR-L2M-002/006/009/012/015/017/021/025`;显式变化 `BR-L2M-003/013/018/023/026`;边界约束 `BR-L2M-004/005/010/016/019/022/027/028`;审计约束 `BR-L2M-020/030`。治理约束类当前为空:member 的治理关系是消费 Policy effective 结果用于筛选分级,已由不变量与禁止行为覆盖;若后续出现"出站需治理前置"场景(`Q-L2M-008`),回开对应校准 Step 补充。

## 11. 数据需求与数据归属

> 校准来源:
> - `design-calibration/00_req_step_11_data_ownership.md`
>
> 延伸阅读:
> - 建议继续阅读上述中间产物的"数据归属总表""Snapshot / ref 约束"和"跨能力数据审计"小节。

| 能力 | 数据项 | 数据类型 | 归属说明 | 生命周期口径 | 来源 |
|---|---|---|---|---|---|
| C1 | 成员本地在场记录 | 真相数据 | 本地 presence identity、状态与变化历史归 member。 | 从在场建立到终止 / 退出,历史可追溯。 | FR-001/002;BR-001~004 |
| C1 | 启动语境受理结论 | 真相数据 | 入场受理 / 拒绝归 member;凭据与签发真相外置。 | 每次受理显式形成,不被后续覆盖。 | FR-001;BR-002 |
| C1 | 项目执行主语引用 | 引用数据 | 只保存 `ProjectMemberRef`;ProjectMember truth 归 Work,不得由 GlobalMember 或 Workspace view 替代。 | 随本地在场语境建立;解析失败、scope 冲突或主语未定义时不入场。 | FR-001;BR-001;L2M-UP-008 |
| C1 | 全局身份锚点引用 | 引用数据 | 只保存与执行主语关联的 `GlobalMemberRef`;身份正文与生命周期归 Identity。 | 引用变化或失效时显式;关联冲突时拒绝。 | FR-001;BR-001 |
| C1 | 宿主协作本地记录 | 真相数据 | 注册请求、存活信号、状态报告及本地尝试归 member;注册接受、registry、session、健康判定归宿主。 | 随在场运行期追加;宿主不可达或反馈 unknown 形成 degraded / unknown。 | FR-003;BR-005/006 |
| C1 | 启动凭据正文 | 禁止保存正文 | 只允许可验证引用与受理结论,不得保存凭据正文。 | 不进入 member 生命周期。 | BR-002;L2M-UP-006 |
| C2 | 订阅范围决定 | 真相数据 | 成员订阅边界决定归 member。 | 随身份 / 角色语境正式变化而显式变化。 | FR-004;BR-007 |
| C2 | 入站筛选结论与处置记录 | 真相数据 | 通过 / 降级 / 阻断 / 待定结论归 member。 | 每条入站事实形成,不被后续结论逆写。 | FR-005;BR-008~010 |
| C2 | 投递决定与运行侧受理关联 | 真相数据 | 投递决定和关联归 member;Runtime 受理 / 处置 truth 外置。 | 随投递形成;duplicate / late 形成新关联事实。 | FR-006;BR-011~013 |
| C2 | 筛选规则来源 safe snapshot | 快照数据 | 规则 truth 外置;member 只保留消费时点安全快照。 | stale / conflict 显式,不逆写既有结论依据。 | FR-005;BR-009/010;L2M-UP-007 |
| C2 | 入站事实引用 | 引用数据 | 只保存来源引用与关联,不拥有事实正文。 | 随关联建立;正文生命周期归 owner。 | FR-005/006;BR-011 |
| C2 | 入站正文持久化 / raw 转发副本 | 禁止保存正文 | 正式授权可允许瞬时受控检查;不得保存为 member truth或透明转发 raw body。 | 只存在于授权检查的瞬时边界,不进入持久化生命周期。 | FR-005/006;BR-011/030 |
| C3 | 出站决定 | 真相数据 | 发布内容、正式边界与安全形态的决定归 member。 | 随运行材料承接形成;外部反馈不逆写。 | FR-007;BR-014~016 |
| C3 | 出站材料准备记录 | 真相数据 | body-free 材料准备与安全门禁判断归 member。 | 随决定形成;拒绝记录保留。 | FR-007;BR-015 |
| C3 | 发布尝试 / 缺口记录 | 真相数据 | attempt / gap 与本地降级归 member;delivery truth 归 Bus。 | 新尝试形成新事实,既有记录不覆盖。 | FR-008;BR-017/018 |
| C3 | Runtime safe material 引用 | 引用数据 | 只引用 committed safe material,不拥有 Runtime outcome truth。 | 随承接关系建立;解析失败显式。 | FR-007;BR-014 |
| C3 | delivery 反馈状态摘要 | 快照数据 | delivery / receipt truth 外置;仅保留与 attempt 关联的时点摘要。 | 后续变化形成新摘要,不改写本地决定。 | FR-008;BR-017 |
| C3 | 运行结果正文 / 隐藏推理 / secret | 禁止保存正文 | 不得保存或转发正文、private reasoning、secret 或 raw provider body。 | 不进入 member 生命周期。 | BR-015/030 |
| C4 | 交互关联追溯记录 | 真相数据 | 在场、订阅、筛选、投递、出站之间的关联语境归 member。 | 随事实持续追加,不可原地改写。 | FR-009;BR-020 |
| C4 | 安全观测材料与交接尝试 | 真相数据 | body-free 材料形成判断与 attempt / gap 归 member;observed truth 外置。 | 随事实形成;交接失败独立显式。 | FR-010;BR-022/023 |
| C4 | 观测交接状态摘要 | 快照数据 | 外部消费状态只作时点摘要。 | 变化形成新摘要。 | FR-010;BR-023 |
| C4 | 完整日志 / 高基数原始内容 | 禁止保存正文 | 不得以追溯名义保存完整正文日志。 | 不进入 member 生命周期。 | BR-021 |
| C5 | 成员状态 / 交互摘要视图 | 快照数据 | 从已提交事实派生,不形成独立写源。 | 可延迟、可更新、可重建,freshness 显式。 | FR-011;BR-024~026 |
| C5 | 能力出口安全视图 | 快照数据(可裁剪) | 从工具 / 方法 ref 派生,不形成第二 registry。 | ref 变化时更新;失效显式 stale / gap。 | FR-012;BR-024~026 |
| C5 | 工具契约 / 方法定义引用 | 引用数据 | 只保存引用关系,不拥有定义正文。 | 随引用建立、变化或失效而变化。 | FR-012;BR-025 |
| C5 | Runtime safe view 来源摘要 | 快照数据 | Runtime truth 外置;member 只保留派生所需安全摘要。 | 随上游变化更新;stale 显式。 | FR-011;BR-026 |
| C5 | 工具 / 方法定义正文 | 禁止保存正文 | 不得保存,防止形成第二 registry。 | 不进入 member 生命周期。 | BR-025 |

所有快照必须带来源、freshness 与 scope;所有引用由对应 owner 解析。解析失败、stale、conflict 或 owner unknown 必须显式,不得由 member 补造第二真相。入站正文只可在正式授权范围内瞬时受控检查;跨边界应使用 typed ref、safe snapshot、受控安全语境、结论分类、redacted marker 或 body-free 材料。

## 12. 接口与依赖

> 校准来源:
> - `design-calibration/00_req_step_12_interfaces_dependencies.md`
>
> 延伸阅读:
> - 建议继续阅读上述中间产物的"对外能力接口边界表""外部依赖边界表"和"跨能力接口审计"小节。

### 12.1 对外能力接口

| 接口类型 | 名称 | 说明 | 所属层级 |
|---|---|---|---|
| 变更接口 | `IB-L2M-001` 启动语境受理与在场建立 | 以项目型执行主语、身份锚与启动语境建立本地在场;不可验证即拒绝。 | 核心 C1 |
| 变更接口 | `IB-L2M-002` 在场状态变化与退出 | 显式表达 starting / ready / degraded / draining / terminated / unknown 变化。 | 核心 C1 |
| 变更接口 | `IB-L2M-003` 注册请求、存活信号与状态报告面 | 向宿主提供请求 / 信号 / 报告与本地尝试语境;不包含宿主接受 / registry / session / health truth。 | 核心 C1 |
| 变更接口 | `IB-L2M-004` 订阅范围决定 | 按身份 / 角色语境建立或调整订阅范围。 | 核心 C2 |
| 事件输入 | `IB-L2M-005` 入站事实承接 | 承接事实 ref 与授权范围内必要瞬时检查材料;正文不得落为 member truth。 | 核心 C2 |
| 事件输入 | `IB-L2M-006` 筛选规则来源变化承接 | 承接 Policy effective / safe snapshot 变化,不生成治理裁决。 | 核心 C2 |
| 变更接口 | `IB-L2M-007` 入站筛选结论形成 | 形成通过 / 降级 / 阻断 / 待定结论。 | 核心 C2 |
| 变更接口 | `IB-L2M-008` 受控投递提交 | 提交 typed ref、safe snapshot 或受控安全语境并关联受理结果,不透明转发 raw body。 | 核心 C2 |
| 变更接口 | `IB-L2M-009` 运行结果出站承接 | 承接 Runtime committed safe material 并形成出站决定。 | 核心 C3 |
| 事件输出 | `IB-L2M-010` 安全发布输出 | 向事件主干提交 body-free 材料,不承诺 delivery。 | 核心 C3 |
| 事件输入 | `IB-L2M-011` 发布反馈承接 | 承接 delivery / 下游状态摘要,不逆写本地决定。 | 核心 C3 |
| 查询接口 | `IB-L2M-012` 交互追溯读取 | 按关联语境只读追溯本地交互事实,保持 body-free。 | 核心 C4 |
| 事件输出 | `IB-L2M-013` 安全观测材料输出 | 输出最小必要、body-free、可关联材料;route 未闭口只保留 attempt / gap。 | 核心 C4 |
| 查询接口 | `IB-L2M-014` 成员摘要与能力出口读取 | 只读消费安全视图,失效显式 stale / gap。 | 核心 C5;出口可裁剪 |
| 后台任务接口(按需) | `IB-L2M-015` 派生视图重建与对账 | 重建摘要 / 出口视图并对账关联,不创造业务事实。 | 核心 C4/C5 支撑 |
| 查询接口 | `IB-L2M-E01` 筛选统计与诊断摘要 | 只读外围统计与诊断。 | 外围增强 |
| 查询接口 | `IB-L2M-E02` 交互历史浏览 | body-free 历史摘要只读浏览。 | 外围增强 |
| 查询接口 | `IB-L2M-E03` 生效配置可解释视图 | 只读展示生效边界及来源。 | 外围增强 |

### 12.2 外部依赖边界

| ID | 方向 | 关联方 | 全局依赖类型 | 依赖边界 |
|---|---|---|---|---|
| `DB-L2M-001` | 输入 | `L0-core` | 编译期依赖 | 消费共享 ID / ref / metadata / error、CloudEvents envelope / W3C trace 标准;member-specific 合同 pending。 |
| `DB-L2M-002` | 输入 / 输出 | `L0-bus` | 事件协作依赖 | 入站事实与出站 / 观测材料主干;delivery truth 归 Bus。 |
| `DB-L2M-003` | 输入 / 输出 | `L2-runtime` | 运行期依赖 | 提交入站语境,消费 committed safe material / safe view;mapping 受 L2M-UP-003 约束。 |
| `DB-L2M-004` | 输入 / 输出 | `L2-member-service` | 运行期依赖 | 消费启动语境、凭据 / acceptance / session ref,提供请求 / 信号 / 报告;宿主 truth 外置。 |
| `DB-L2M-005` | 输入 | `L1-work` | 运行期依赖(ref) | 消费项目型 `ProjectMemberRef` 执行主语,不拥有 ProjectMember truth。 |
| `DB-L2M-006` | 输入 | `L1-governance` | 运行期 + 事件协作依赖 | 消费 Policy effective / safe snapshot;unknown 保守处置。 |
| `DB-L2M-007` | 输入 | `L2-tools`、`L3-method-library` | 运行期依赖(ref,弱) | 派生能力出口,失效显式 stale / gap。 |
| `DB-L2M-008` | 输出 | `L1-conversation`、`L4-observability` 等 | 事件协作依赖 | 输出 body-free 材料;route pending,不反写真相。 |
| `DB-L2M-009` | 输入 | `L1-identity` | 运行期依赖(ref) | 消费关联的 `GlobalMemberRef` 身份锚 / 安全摘要,不拥有 GlobalMember truth。 |

具体 API path、DTO、event topic、IPC 载体、repository 与事务不在需求层定义。只有 `L0-core` 为编译期候选;其余保持 runtime / event seam,ref 只是交互形态;外部 adapter 是禁止直边,fake 只可作为后续测试替身,不得声明真实集成。

## 13. 非功能需求

> 校准来源:
> - `design-calibration/00_req_step_13_non_functional_requirements.md`
>
> 延伸阅读:
> - 建议继续阅读上述中间产物的"六类适用性判断""历史指标审计"和"非功能需求表"小节。

当前优先锁定安全、可追溯、幂等、一致性与失效分层;性能只定义可测维度,不填无来源数字。

| ID | 类别 | 要求 | 判断口径 / 目标值 | 挂载 |
|---|---|---|---|---|
| `NFR-L2M-001` | 性能 | 入站本地处理不成为不可解释瓶颈,派生路径不无限放大。 | 固定 workload / dependency profile 下分解筛选、投递与外部等待;无数值目标。 | C2;全局 |
| `NFR-L2M-002` | 性能 | 出站本地处理与外部 delivery 等待可区分。 | 按 stage / dependency / outcome 分解,不用总延迟掩盖外部等待。 | C3 |
| `NFR-L2M-003` | 性能 | 追溯与观测材料形成不成为核心提交的同步前置放大点。 | 核心决定提交不以观测 route ready 为前提。 | C4 |
| `NFR-L2M-004` | 可用性 | 宿主、Runtime、Bus、policy 来源失效时,受影响能力可判别且本地历史不丢失。 | unavailable / timeout / late 下保持 waiting / degraded / blocked 与历史。 | C1~C3;全局 |
| `NFR-L2M-005` | 可用性 | 派生能力失效不拖垮核心在场 / 入站 / 出站 / 追溯。 | C5 与外围可独立降级或排除。 | C5;外围 |
| `NFR-L2M-006` | 安全 | 执行主语、身份关联、启动语境、订阅 / 规则来源或出站材料不可验证时 fail closed / 保守处置。 | invalid / conflict / unsupported subject 不得默认放行、降级入场或由 GlobalMember 代主语。 | C1/C2 |
| `NFR-L2M-007` | 安全 | 入站正文只可授权瞬时检查,不得持久化或透明转发 raw body;其他 forbidden body 不进入记录 / 出站 / 观测。 | 覆盖检查、记录、投递、发布与观测面的 persistence / forwarding / redaction 检查。 | 全局 |
| `NFR-L2M-008` | 安全 | 不暴露通用外部监听面或任意直连 provider / MCP / A2A / API;正式 Bus、宿主、Runtime seam 与违规外联可区分。 | 允许 seam 不被误禁;通用监听 / 任意外呼即阻塞。 | C1~C3 |
| `NFR-L2M-009` | 审计 / 可追溯 | 关键交互事实按同一语境追溯,每条结论有来源 / 目的 / 结果分类。 | 任一事实可回链规则来源、上游 ref 与结果分类,不要求正文。 | C4;全局 |
| `NFR-L2M-010` | 审计 / 可追溯 | 本地决定、attempt、delivery、observed、accepted 分层追溯,外部状态不改本地历史。 | 故障后本地记录保持,外部状态独立标注。 | C3/C4 |
| `NFR-L2M-011` | 幂等 / 一致性 | duplicate 输入 / 投递 / 发布 / 反馈不产生 truth 分叉或重复不可逆副作用。 | 重复可判定;unknown 不自动重放。 | C2/C3 |
| `NFR-L2M-012` | 幂等 / 一致性 | late / out-of-order 结果、反馈或快照不覆盖新决定或既有记录。 | 只形成新关联事实。 | C2/C3 |
| `NFR-L2M-013` | 幂等 / 一致性 | 同一项目型实例的本地在场锚定同一 ProjectMember / GlobalMember 关联;宿主视角冲突不静默合并。 | subject / presence / host-view 冲突显式 conflict / unknown。 | C1 |
| `NFR-L2M-014` | 可观测性 | 状态、处置分布、失败、依赖缺口与降级形成低敏低基数材料。 | 不依赖后端 ready;本地 attempt / gap 可判断。 | C4;全局 |
| `NFR-L2M-015` | 可观测性 / 安全 | 观测材料不泄漏正文、隐藏推理、secret 或高基数用户内容。 | body-free、redaction 与 cardinality 可判断。 | C4 |
| `NFR-L2M-016` | 全局 | 依赖边界、fake / controlled seam 与真实 readiness 可区分。 | planned / blocked / not_run 不得写成 pass。 | 全局 |

## 14. 验收标准

> 校准来源:
> - `design-calibration/00_req_step_14_acceptance_criteria.md`
>
> 延伸阅读:
> - 建议继续阅读上述中间产物的"核心能力闭环验收""规则 / 边界与数据归属验收"和"跨能力验收审计"小节。

本章只定义验收合同,不填写执行结果、run、artifact、report、evidence、verdict 或 signoff。

### 14.1 核心能力闭环验收

| ID | 验收项 | 验收条件 |
|---|---|---|
| `AC-L2M-001` | C1 在场成立 | 项目型实例以 ProjectMember 执行主语、关联 GlobalMember 身份锚和可验证语境进入本地在场;状态可区分;member 未吸收宿主 acceptance / session / health truth。 |
| `AC-L2M-002` | C2 入站成立 | 授权瞬时检查、四态筛选、规则回链和 typed ref / safe snapshot / 受控安全语境投递成立;无正文持久化或 raw 转发。 |
| `AC-L2M-003` | C3 出站成立 | 出站决定锚定 committed safe material;body-free 门禁成立;outcome / 决定 / attempt / delivery / accepted 分层。 |
| `AC-L2M-004` | C4 追溯成立 | 事实按关联语境追溯;材料 body-free;观测只声明 attempt / gap。 |
| `AC-L2M-005` | C5 摘要成立 | 摘要 / 出口从已提交事实与 ref 派生,可重建、不反写,来源失效显式。 |

### 14.2 功能能力验收

| ID | 验收条件 | 功能 |
|---|---|---|
| `AC-L2M-006` | 项目型在场具备可验证 ProjectMemberRef、关联 GlobalMemberRef 与启动语境;匿名、错项目、关联冲突、身份锚代主语或未定义非项目型主语均不入场。 | FR-001 |
| `AC-L2M-007` | 在场状态显式变化且不互相压平;unknown 不自动升级。 | FR-002 |
| `AC-L2M-008` | 宿主持续获得注册请求 / 存活信号 / 状态报告;member 不声明接受、session 或健康成功。 | FR-003 |
| `AC-L2M-009` | 订阅范围可解释且来源可验证;不可验证变化被拒绝。 | FR-004 |
| `AC-L2M-010` | 授权瞬时检查与持久化分开;筛选四态可区分并回链规则;unknown / stale / conflict 保守处置;无本地 allowlist或正文副本。 | FR-005 |
| `AC-L2M-011` | 投递采用 typed ref、safe snapshot 或受控安全语境且不透明转发 raw body;决定与受理结果可关联。 | FR-006 |
| `AC-L2M-012` | 出站决定锚定正式引用;不合格材料被拒绝。 | FR-007 |
| `AC-L2M-013` | 发布 attempt / gap 显式;delivery 失败不逆写;迟到反馈形成新事实。 | FR-008 |
| `AC-L2M-014` | 交互事实按关联语境可追溯且 body-free。 | FR-009 |
| `AC-L2M-015` | 观测材料满足最小必要 / body-free / 脱敏 / 可关联;route 未闭口只 attempt / gap。 | FR-010 |
| `AC-L2M-016` | 摘要投影可派生、可重建、不反写,freshness 显式。 | FR-011 |
| `AC-L2M-017` | 能力出口不复制定义正文;ref 失效显式 stale / gap;若裁剪则显式记不适用。 | FR-012 |
| `AC-L2M-018` | 外围增强缺失不影响核心;提供时只读不反写。 | FR-E01~E03 |

### 14.3 规则、边界与数据归属验收

| ID | 验收条件 |
|---|---|
| `AC-L2M-019` | Step 10 不变量成立:双锚在场、订阅来源、筛选结论、受控投递、出站锚定、派生只读、开放 seam fail closed。 |
| `AC-L2M-020` | 禁止行为未发生:错主语 / 降级入场、自建 allowlist、代答、盲重放、正文持久化 / raw 转发、正文出站、外部反馈逆写、通用监听 / 任意外呼、派生反写。 |
| `AC-L2M-021` | 在场、订阅、筛选、投递关联、发布 attempt 与视图失效均显式,不静默覆盖。 |
| `AC-L2M-022` | 运行决策、工具、registry、编排 / 健康、镜像、隔离、治理、对话、观测后端、identity / work 生命周期均保持外部。 |
| `AC-L2M-023` | member truth 只包含本地在场、宿主协作本地记录、筛选 / 投递 / 出站 / 追溯 / attempt;宿主 acceptance / registry / session / health 外置。 |
| `AC-L2M-024` | 快照 / 引用不成为第二 truth;来源时点、freshness、scope 与解析失败显式。 |
| `AC-L2M-025` | 入站正文只授权瞬时检查,不持久化或 raw 交接;运行结果正文、隐藏推理、secret、定义正文、完整日志不进入记录 / 出站 / 观测。 |
| `AC-L2M-026` | 仅 Core 为编译期候选;runtime / event 未写成 package;ref / adapter / fake 分类未伪装真实集成;sibling pending 未升格。 |

### 14.4 非功能验收

| ID | 验收条件 |
|---|---|
| `AC-L2M-027` | 本地处理与外部等待可分解,派生不阻塞核心,无无来源性能数字。 |
| `AC-L2M-028` | 依赖失效可判别且历史保留,派生失效不拖垮核心。 |
| `AC-L2M-029` | 主语 / 入场 / 筛选 fail closed,forbidden persistence / raw forwarding 可判断,正式 seam 与违规外联可区分。 |
| `AC-L2M-030` | 关联追溯和状态分层成立,外部状态不改本地历史。 |
| `AC-L2M-031` | duplicate / late / out-of-order 不分叉不逆写,项目型 presence 关联唯一。 |
| `AC-L2M-032` | 低敏低基数材料可判断,不依赖后端 ready,不泄漏正文。 |
| `AC-L2M-033` | planned / blocked / not_run 未被写成 pass。 |

### 14.5 一票否决项

| ID | 否决条件 |
|---|---|
| `VF-L2M-001` | 任一核心节点 C1~C4 不成立,或 member 退化为透明转发管道 / 第二运行决策 owner。 |
| `VF-L2M-002` | 无效 / 冲突主语或语境被放行,以 GlobalMember / Workspace view 代执行主语,未定义非项目型主语被放行,或规则 unknown 时 fail open。 |
| `VF-L2M-003` | member 自建裁决 truth,或反写 Runtime / governance / conversation / identity / work / bus / observability truth。 |
| `VF-L2M-004` | 入站正文被持久化或 raw 投递,或运行结果正文、隐藏推理、secret、定义正文进入记录 / 出站 / 观测;授权瞬时检查本身不构成否决。 |
| `VF-L2M-005` | delivery / observed / accepted、宿主 acceptance / session / health 被写成本地 truth,或外部失败回滚本地事实。 |
| `VF-L2M-006` | member 暴露通用外部监听面或任意直连 provider / MCP / A2A / API;正式 Bus / 宿主 / Runtime seam 不属于该条件。 |
| `VF-L2M-007` | 非 Core sibling 进入 package,或 pending host / entry / member-specific schema / route / rule / non-project subject seam 被伪装闭口。 |
| `VF-L2M-008` | fake / planned / blocked / not_run 被伪装成 positive integration、evidence、readiness 或验收通过。 |
| `VF-L2M-009` | 交互事实来源、结论或变化不可回链正式需求 / owner。 |

## 15. 风险与待确认事项

> 校准来源:
> - `design-calibration/00_req_step_15_risks_open_questions.md`
> - `design-calibration/project_execution_ledger.md`
>
> 延伸阅读:
> - 建议继续阅读上述中间产物的"上游 blocker 承接""风险清单"和"待确认事项"小节。

### 15.1 上游 blocker / pending

| ID | 当前状态 | 影响 | 当前约束 |
|---|---|---|---|
| `L2M-UP-001` | sibling_step_09_pass_contract_pending | member-service Step 9 pass / Step 10 pending;宿主合同未正式停审 | 采用当前已通过材料一致的 owner 分层,字段合同未闭口时 fail closed。 |
| `L2M-UP-002` | sibling_step_09_in_progress_contract_pending | member-images Step 1~8 pass / Step 9 in progress;正式 00 未停审 | 只记录静态镜像资产、pinned 入口与被打包方向,不引用进行中结论,不声明 manifest / version / readiness。 |
| `L2M-UP-003` | open_upstream_contract | member 投递物与 Runtime formal trigger mapping | 只保留 entry seam,mapping blocked。 |
| `L2M-UP-004` | open_integration_boundary | Runtime handoff source family 与观测 / 下游 route | 只保留 attempt / gap,不声明 delivered / observed。 |
| `L2M-UP-005` | schema_and_route_pending | member-specific Core schema / event family | Core CloudEvents / W3C 基线有效;member-specific type / source / subject / payload / route pending。 |
| `L2M-UP-006` | owner_contract_pending | 启动凭据签发 / 撤销与身份锚合同 | 凭据形态 pending,校验 fail closed。 |
| `L2M-UP-007` | open_upstream_contract | 筛选规则来源与 taxonomy | 只消费正式结果 / safe snapshot,unknown 保守处置。 |
| `L2M-UP-008` | execution_subject_scope_pending | 项目型双锚与非项目型 / personal 执行主语 | 项目型以 ProjectMemberRef 为主语并关联 GlobalMemberRef;非项目型 fail closed。 |

### 15.2 风险清单

| ID | 风险 | 影响范围 | 当前处理口径 |
|---|---|---|---|
| `R-L2M-001` | member / Runtime 分界被混写 | §2、§7、§9、§10 | 由 entry / outcome owner 与 BR-011/012/014 收口。 |
| `R-L2M-002` | 筛选膨胀为本地裁决 | FR-005、§10、§14 | 规则 truth 外置,unknown 保守处置,禁止本地 allowlist。 |
| `R-L2M-003` | 出站状态被压平 | FR-007/008、§13、§14 | outcome / 决定 / attempt / delivery / accepted 分层进入验收。 |
| `R-L2M-004` | member-service 后续结论与注册 / session / health 分界冲突 | §6、FR-003、§12 | 只冻结双方当前一致 owner 方向;字段合同 pending,冲突时回开 Step。 |
| `R-L2M-005` | 旧协议、凭据与指标整包回流 | 全文、后续 01~07 | Core 标准单独承接;旧 member-specific event detail、AG-UI、UDS、launch_token、P95 逐项 historical / pending。 |
| `R-L2M-006` | 能力出口被当 registry / 调用资格 | FR-012、§11、§14 | 只读派生,失效显式,子项可裁剪。 |
| `R-L2M-007` | 追溯 / 观测扩张为正文日志 | FR-009/010、§13 | body-free / forbidden persistence / veto 收口。 |
| `R-L2M-008` | Core 合同未闭口时私造共享类型 | §6、§11、§12 | 只引用 Core authority,member-specific schema pending。 |
| `R-L2M-009` | 身份锚 / Workspace view 被误作执行主语 | §2、§7、FR-001、§14 | 项目型双锚;非项目型随 L2M-UP-008 / Q-L2M-009 fail closed。 |
| `R-L2M-010` | 入站正文边界被误解为完全不可读取或可持久化 / raw 转发 | FR-005/006、§10、§11、§14 | 只允许授权瞬时检查,禁止持久化与透明 raw 转发。 |

### 15.3 待确认事项

| ID | 待确认事项 | 影响章节 | 当前状态 |
|---|---|---|---|
| `Q-L2M-001` | 启动语境 / 凭据 owner 合同与形态 | §6、FR-001/003、§12 | fail closed,不指定形态。 |
| `Q-L2M-002` | member 投递物与 Runtime trigger mapping | FR-006、§12 | 只保留 entry seam,不单方面定义。 |
| `Q-L2M-003` | 筛选规则来源矩阵与风险 taxonomy | FR-005、§10、§13 | 保守处置,不自建标准。 |
| `Q-L2M-004` | 出站材料与 conversation / observability source family / route | FR-008/010、§12 | 只形成本地 attempt / gap。 |
| `Q-L2M-005` | member-specific shared types、CloudEvent type / source / subject / payload 与 route 最小集合 | §6、§11、§12 | Core 基线已承接,member-specific 集合等待正式合同。 |
| `Q-L2M-006` | 能力出口进入首批核心还是外围 | §7、FR-012、§14 | 保留 C5 可裁剪子项,01 阶段复核。 |
| `Q-L2M-007` | 追溯 / 观测记录 retention / archive | §11、§13 | 只锁可追溯与不可原地改写,后移 03/04。 |
| `Q-L2M-008` | 出站是否存在治理前置场景 | §10 | 当前无此正式场景;出现时回开 Step 10。 |
| `Q-L2M-009` | 当前版是否只支持项目型实例;未来非项目型 / personal 实例的第三种执行主语与生命周期 | §2、§7~§9、§14 | 正向需求仅覆盖 ProjectMemberRef;非项目型 fail closed,不得以 GlobalMemberRef / PersonalWorkspace view 替代。 |

这些事项不阻塞本需求对 owner、能力、失败与 fail-closed 边界成文,但阻塞受影响 schema、配置激活、测试执行、evidence、联调和 readiness 声明。

## 16. 需求追溯矩阵

> 校准来源:
> - `design-calibration/00_req_step_16_traceability_matrix.md`
>
> 延伸阅读:
> - 建议继续阅读上述中间产物的"主追溯矩阵""跨能力追溯审计"和"漏项检查表"小节。

### 16.1 能力覆盖

| 能力 | 核心功能 | 核心故事 | 核心验收 |
|---|---|---|---|
| `C-L2M-1` | FR-L2M-001~003 | US-L2M-001~004 | AC-L2M-001 |
| `C-L2M-2` | FR-L2M-004~006 | US-L2M-005~008 | AC-L2M-002 |
| `C-L2M-3` | FR-L2M-007~008 | US-L2M-009~011 | AC-L2M-003 |
| `C-L2M-4` | FR-L2M-009~010 | US-L2M-012~014 | AC-L2M-004 |
| `C-L2M-5` | FR-L2M-011~012 | US-L2M-015~016 | AC-L2M-005 |

### 16.2 主追溯矩阵

| 功能需求 | 核心能力 | 用户故事 | 业务规则 | 数据归属要求 | 验收标准 |
|---|---|---|---|---|---|
| `FR-L2M-001` 启动与在场 | C-L2M-1 | US-L2M-001、004 | BR-L2M-001~002、029 | presence;受理结论;ProjectMemberRef;GlobalMemberRef;凭据正文禁止 | AC-L2M-001、006、029 |
| `FR-L2M-002` 在场状态 | C-L2M-1 | US-L2M-002 | BR-L2M-003~004 | 本地在场记录 | AC-L2M-001、007、031 |
| `FR-L2M-003` 宿主协作 | C-L2M-1 | US-L2M-003 | BR-L2M-005~006 | 请求 / 信号 / 报告、本地尝试、acceptance / session ref | AC-L2M-001、008 |
| `FR-L2M-004` 订阅范围 | C-L2M-2 | US-L2M-006 | BR-L2M-007 | 订阅范围决定 | AC-L2M-002、009 |
| `FR-L2M-005` 入站筛选 | C-L2M-2 | US-L2M-005、007 | BR-L2M-008~011、030 | 结论;规则快照;事实 ref;授权瞬时检查;持久化副本禁止 | AC-L2M-002、010、025、029 |
| `FR-L2M-006` 受控投递 | C-L2M-2 | US-L2M-005、008 | BR-L2M-011~013 | typed ref / safe snapshot / 安全语境;投递关联;raw 转发禁止 | AC-L2M-002、011、025、031 |
| `FR-L2M-007` 出站决定 | C-L2M-3 | US-L2M-009、010 | BR-L2M-014~016、019 | 出站决定;材料记录;safe material ref;运行正文禁止 | AC-L2M-003、012、025 |
| `FR-L2M-008` 发布尝试 | C-L2M-3 | US-L2M-011 | BR-L2M-016~018 | attempt / gap;delivery 摘要 | AC-L2M-003、013、030 |
| `FR-L2M-009` 交互追溯 | C-L2M-4 | US-L2M-012、013 | BR-L2M-020~021、030 | 关联追溯;完整日志禁止 | AC-L2M-004、014、030 |
| `FR-L2M-010` 观测材料 | C-L2M-4 | US-L2M-013、014 | BR-L2M-021~023 | 观测材料 / 尝试;状态摘要 | AC-L2M-004、015、032 |
| `FR-L2M-011` 状态摘要 | C-L2M-5 | US-L2M-015 | BR-L2M-024~026 | 摘要视图;safe view 摘要 | AC-L2M-005、016 |
| `FR-L2M-012` 能力出口(可裁剪) | C-L2M-5 | US-L2M-016 | BR-L2M-024~026 | 出口视图;定义 ref;定义正文禁止 | AC-L2M-005、017 |
| `FR-L2M-E01` 筛选诊断 | 外围 | US-L2M-E01 | BR-L2M-025、030 类推 | 只读派生 | AC-L2M-018 |
| `FR-L2M-E02` 历史浏览 | 外围 | US-L2M-E02 | BR-L2M-021、025 类推 | 只读派生 | AC-L2M-018 |
| `FR-L2M-E03` 配置解释 | 外围 | US-L2M-E03 | BR-L2M-025 类推 | 只读派生 | AC-L2M-018 |

### 16.3 附属覆盖审计

| 范围 | 接口承接 | 主要依赖 | 结论 |
|---|---|---|---|
| FR-001~003 | IB-001~003 | DB-001、004、005、009 | 双锚与宿主 owner 分层完整;非项目型 / 凭据 / 字段 pending 显式。 |
| FR-004~006 | IB-004~008 | DB-002、003、006 | 入站正文检查 / 保存 / 投递分层完整;mapping / taxonomy pending。 |
| FR-007~008 | IB-009~011 | DB-002、003 | 出站状态分层完整;route pending。 |
| FR-009~010 | IB-012~013、015 | DB-002、008 | 追溯与观测面完整。 |
| FR-011~012 | IB-014~015 | DB-003、007 | 摘要 / 出口完整;裁剪条件显式。 |
| FR-E01~E03 | IB-E01~E03 | 只读消费核心 truth | 外围不反写、不阻塞。 |

| 审计项 | 结果 | 说明 |
|---|---|---|
| 孤儿故事 | 无 | US-001~016、E01~E03 均有 FR。 |
| 孤儿功能 | 无 | 12 核心 + 3 外围均入矩阵。 |
| 孤儿规则 | 无 | BR-001~030 保护功能或全局边界。 |
| 孤儿数据 | 无 | 第 11 章各项有 FR / BR 来源。 |
| 孤儿接口 / 依赖 | 无 | IB-001~015 / E01~E03、DB-001~009 有来源。 |
| 孤儿 NFR / AC / VF | 无 | 均有来源与承接。 |
| 重复定义 | 无 | 同一 truth 未在多节点重复拥有。 |
| 边界串仓 | 未发现 | Work / Identity 双 ref、Runtime / host / Bus / Governance / Conversation / Tools / Observability owner 保持外部。 |
| 依赖口径冲突 | 无 | 仅 Core 为编译期候选;runtime / event / ref / adapter / fake 分类一致。 |
| 矩阵新增未确认项 | 无 | 只引用前文既有 ID。 |

### 16.4 漏项检查

| 检查项 | 结果 |
|---|---|
| 是否存在没有故事来源的功能需求 | 否 |
| 是否存在没有闭环映射的功能需求 | 否;外围显式标注 |
| 是否存在没有规则保护的核心功能 | 否 |
| 是否存在没有数据归属的功能需求 | 否 |
| 是否存在没有接口承接的功能需求 | 否 |
| 是否存在没有验收标准的功能需求 | 否 |
| 是否存在未进入前文结构却出现在矩阵中的新项 | 否 |

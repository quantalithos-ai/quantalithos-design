# L3-method-library 00 需求 Step 4: 目标与非目标

> 创建日期: 2026-06-15
> 状态: completed
> 当前模式: full-restart
> 本轮口径: 全量重新讨论,旧 L3-method-library 文档只作差异审计。
> 回填位置: `00-需求文档.md` 第 4 章“目标与非目标”
> 项目级台账: `design-calibration/project_execution_ledger.md`
> 文档级 flow: `design-calibration/00_requirements_calibration_flow.md`

---

## 0. Step 开工确认

| 项目 | 记录 |
|---|---|
| Step | Step 4 目标与非目标 |
| 输出文件 | `design-calibration/00_req_step_04_goals_non_goals.md` |
| 已读取通用规范 | yes:`设计文档讨论中间产物规范.md`;`设计真相源闭环与可落码性标准.md` |
| 已读取 SOP / 书写规范 | yes:`需求文档讨论流程_SOP.md`;`需求文档书写规范.md` §4.4 |
| 已读取前序输入 | yes:`00_req_step_02_position_boundary.md`;`00_req_step_03_problem_context.md` |
| 已读取项目输入 | yes:`product/六域模型.md` method-library 横切段;`architecture/标准对齐全景图.md`;`architecture/开发路线图与优先级.md`;`standards/子项目遵循规范清单.md`;`domain/method-library/README.md`;旧 `00_req_step_04_goals_non_goals.md` |
| 当前模式 | full-restart |
| 进入条件 | pass |
| next_allowed_action | Step 4 已完成,允许文档级 flow 进入 Step 5。 |

---

## 1. Step 内计划

| 模块 | 状态 | 产物 | gate_status | next_allowed_action |
|---|---|---|---|---|
| 必读文档读取 | done | 必读文档摘要 | pass | 进入整体模块骨架。 |
| 整体模块搭建 | done | 本 Step 模块骨架 | pass | 进入目标思考。 |
| 模块 1 目标:先思考 | done | 问题回答 / 诊断 / 取舍 | pass | 进入模块 1 写入。 |
| 模块 1 目标:再写入 | done | 目标表 | pass | 进入模块 2 思考。 |
| 模块 2 非目标:先思考 | done | 排除范围判断 | pass | 进入模块 2 写入。 |
| 模块 2 非目标:再写入 | done | 非目标表 | pass | 进入模块 3 思考。 |
| 模块 3 范围收束:先思考 | done | 当前范围边界 | pass | 进入模块 3 写入。 |
| 模块 3 范围收束:再写入 | done | 范围收束结论 | pass | 进入旧材料差异审计。 |
| 旧材料差异审计 | done | 冲突 / 可保留事实 / 废弃项 | pass | 进入回填草稿。 |
| 回填草稿 | done | 正式第 4 章草稿 | pass | 进入自检与停审。 |
| 自检与停审 | done | 自检表 / 待确认事项 | pass | 允许进入 Step 5。 |

---

## 2. 必读文档

| 文档 | 读取结论 | 对 Step 4 的影响 |
|---|---|---|
| `需求文档书写规范.md` §4.4 | 本章固定为目标表和非目标表;目标写成立的状态、边界或能力范围,不是功能名、接口名、口号或实现路径。 | 目标必须可验证,非目标必须有边界作用。 |
| `00_req_step_02_position_boundary.md` | 本仓是方法资产定义、版本发布与分发语义的真相仓。 | 目标围绕定义真相、Definition vs Use、版本发布和相邻仓边界收束。 |
| `00_req_step_03_problem_context.md` | 核心问题是定义源不统一、定义与使用混写、版本 / 分发语义缺少统一背景。 | 目标必须回应这些问题,不能只列旧功能。 |
| `product/六域模型.md` method-library 横切段 | method-library 承载 ProcessTemplate 完整定义、Role/Task/WorkProduct 定义、ViewProfile、AIPolicy、MethodPlugin 和 MethodConfiguration 等横切主题。 | 这些提供目标背景,但 P0 覆盖范围需要后续 Step 7/9/11 裁剪。 |
| `architecture/标准对齐全景图.md` | 本仓主要承接 SPEM、24748-2、42001 和 ViewProfile 等方法资产主题。 | 目标应保持方法资产主题边界,不转成运行时职责。 |
| `architecture/开发路线图与优先级.md` | N3 方法能力层产出“怎么做”和“用什么”两套资产。 | 可说明当前目标需要支撑方法资产定义,但不把 exit criteria 原文写成目标。 |
| `domain/method-library/README.md` | 旧 domain 文档详细列出 MethodContent 子类、Plugin、Configuration、RPC、事件、表和场景。 | 只作差异审计;对象 / RPC / 事件 / 表不得进入目标层。 |

---

## 3. 整体模块骨架

| 模块 | 要回答的问题 | 输出 | 不输出 |
|---|---|---|---|
| 模块 1:目标 | 本轮需求完成后应成立哪些状态、边界或能力范围? | 目标表。 | 不写功能名、接口名、事件名或实现路径。 |
| 模块 2:非目标 | 哪些相关事项不纳入本仓当前需求范围? | 非目标表。 | 不写空泛“不做所有扩展”。 |
| 模块 3:范围收束 | 当前目标与非目标如何约束后续 Step? | 范围收束结论。 | 不写核心能力闭环步骤或数据归属矩阵。 |

---

## 4. 模块思考记录

### 4.1 模块 1:目标

问题回答:

- 目标必须回应 Step 3 的三个问题:定义源不统一、定义与使用混写、版本 / 分发语义缺少统一背景。
- 当前阶段目标应先钉住“方法资产定义真相边界”和“相邻仓只消费 / 执行 / 引用”的原则。
- 目标可以要求后续章节能裁剪方法资产主题范围,但不能在本 Step 直接固化完整对象清单。

诊断:

- 旧稿中“CRUD、发布、事件、同步、ResolveViewProfile”等表述属于功能或接口,不适合作为 Step 4 目标。
- 路线图中的“8 种模板、9 种 Role”是阶段交付或验收候选,不能直接当目标层正文。
- domain README 的 MethodContent 子类、Plugin、Configuration、RPC 和事件足够详细,但属于后续需求 / 设计审计输入。

取舍:

- 目标写成可验证的需求状态:定义真相边界成立、Definition vs Use 分离成立、当前方法资产主题可裁剪、版本发布分发语义可承接、相邻仓不反向侵入。
- 验证方式用后续章节检查口径表达,不写具体测试动作。

### 4.2 模块 2:非目标

问题回答:

- 非目标必须把相邻仓运行职责排除出去:流程执行、成员状态、治理裁决、外部能力注册、marketplace 交易、UI 渲染、artifact 正文和认证鉴权。
- 非目标不是否定这些主题,而是说明本仓只提供方法资产定义源或边界语义。

诊断:

- ViewProfile 和 AIPolicy 容易被误判为 UI / governance 的全部职责,实际应区分“定义”与“执行/消费”。
- Plugin 和 MethodConfiguration 容易被误判为 marketplace 或 process 运行方案,实际是否进入 P0 要后续裁剪。

取舍:

- 非目标按职责归属写,不按旧对象模型写。
- 对 AIPolicy、ViewProfile、Plugin 等争议主题,只排除执行、渲染、交易等行为,不在 Step 4 排除定义主题。

### 4.3 模块 3:范围收束

问题回答:

- Step 4 之后,后续章节必须围绕“方法资产定义源”展开。
- 目标为 Step 7 核心能力闭环提供边界,但本 Step 不画闭环。
- 非目标将成为 Step 6、Step 10、Step 11、Step 14 的边界检查输入。

诊断:

- 如果不在 Step 4 收束范围,后续用户角色和依赖章节会把 process、identity、governance、marketplace 和 UI 的运行职责拉回本仓。

取舍:

- 范围收束只写后续承接原则。
- 不写角色、故事、功能、数据或接口细节。

---

## 5. 旧材料差异审计

| 旧材料 | 审计结论 |
|---|---|
| 旧 `00_req_step_04_goals_non_goals.md` | 旧目标方向大体可作为差异线索,但旧完成状态不继承;本轮重新裁剪目标和非目标。 |
| `projects/L3-method-library/00-需求文档.md` §3 | 可保留“只定义不执行”“不管成员状态”“不做 Gate 决策”“不做 marketplace 交易”等非目标方向;不能继承旧 P0 闭环、功能化目标或下游同步口径。 |
| `domain/method-library/README.md` | 可保留 Definition vs Use、只定义不执行、相邻仓消费边界等线索;不能继承 MethodContent 字段、RPC、事件、表结构、QPS 或场景作为需求目标。 |

| 旧口径 | 为什么不能在 Step 4 继承 | 后续处理 |
|---|---|---|
| MethodContent 6/7 类可 CRUD + 发布 | CRUD 是功能需求,对象清单需要在能力、功能和数据层重审。 | Step 7 / Step 9 / Step 11。 |
| 8 种 Template 家族和 9 种 Role 作为种子数据 | 种子数据和数量验收不是目标层表达。 | Step 9 / Step 14。 |
| ResolveViewProfile 成功 | 这是接口或功能验收候选。 | Step 9 / Step 12 / Step 14。 |
| method_library.* 事件 | 这是接口协作方案。 | Step 12。 |
| Plugin Marketplace 分发路径 | 可能涉及 marketplace 边界和外部供应商治理,不能在目标层直接纳入。 | Step 6 / Step 7 / Step 12 / Step 15。 |

---

## 6. 结构化中间产物

### 6.1 目标表

| 目标 | 说明 | 验证方式 |
|---|---|---|
| 建立方法资产定义真相边界 | 明确本仓承载方法资产定义、版本发布与分发语义,相邻仓只按边界引用、消费或执行。 | 后续章节不把流程实例、成员状态、治理裁决、外部能力注册、交易流程或 UI 渲染写成本仓真相。 |
| 收束 Definition vs Use 分离 | 明确本仓讨论“定义”,不讨论相邻仓的运行时“使用”状态。 | Step 6/10/11 中所有相邻仓关系都能保持定义源与使用方分离。 |
| 收束当前方法资产主题范围 | 明确本轮需求围绕 SPEM Method Content、生命周期模型、AI 方针、视图策略和方法资产分发语义展开,不从旧对象模型反推范围。 | Step 7/9/11 的能力、功能和数据项均能回指当前方法资产主题,无法回指的项进入待确认或排除。 |
| 支撑方法资产版本与发布语义 | 为后续讨论版本稳定性、发布后不可随意修改、可追溯分发和下游消费建立目标边界。 | Step 10/11/14 能分别承接版本 / 发布规则、数据归属和验收口径,且不需要由消费仓补定义。 |
| 明确相邻仓职责不反向侵入 | 将 process、identity、governance、capability-hub、marketplace、UI/console 等职责排出本仓目标范围。 | 后续功能、接口、数据和验收中若出现相邻仓运行职责,必须被标为外部依赖、边界背景、外围增强或非目标。 |

### 6.2 非目标表

| 非目标 | 不做原因 |
|---|---|
| 流程执行、ProcessInstance 状态和运行时编排 | 属于 `L1-process` / L2 runtime 的运行职责;本仓只收束方法资产定义。 |
| 成员身份、成员生命周期、成员具备哪些角色或资格的状态 | 属于 `L1-identity` 或相关成员仓;本仓不拥有成员状态真相。 |
| Gate 决策、治理裁决、策略 enforce 和运行时合规执行 | 属于 `L1-governance` / runtime;本仓只讨论方法资产中的定义性方针或被引用语义。 |
| MCP/A2A/provider/tool 外部能力注册、绑定和访问裁决 | 属于 `L3-capability-hub` 及治理协作边界;本仓不管理外部能力接入。 |
| marketplace 定价、购买、结算、订单和商业分发流程 | 属于 `L6-marketplace`;本仓最多作为方法资产定义源或包来源被后续讨论。 |
| UI 页面渲染、字段过滤执行和前端交互体验 | 属于 `L5-chat`、`L5-console`、mobile 或其他体验仓;本仓只收束视图策略作为方法资产的定义边界。 |
| Artifact 正文、外部文件、证据正文和制品生命周期状态 | 属于 `L1-artifact` / archive 等相邻仓;本仓不保存或管理外部正文生命周期。 |
| 认证登录、权限系统和操作主体鉴权实现 | 属于身份、治理或平台安全边界;本仓需求只讨论方法资产的仓级职责。 |

### 6.3 范围收束结论

本轮 `L3-method-library` 需求的目标,不是把所有与方法论相关的系统行为都放进本仓,而是把方法资产定义源、版本发布语义、分发语义和相邻仓消费边界先收束清楚。后续用户、依赖、核心能力、功能、规则、数据、接口和验收都必须围绕这些目标展开;凡是落到运行时执行、成员状态、治理裁决、外部能力注册、交易流程、UI 执行或外部正文管理的内容,都不能作为本仓目标进入当前主线。

---

## 7. 回填草稿

### 4. 目标与非目标

> 校准来源:
> - `design-calibration/00_req_step_04_goals_non_goals.md`
>
> 延伸阅读:
> - 建议继续阅读 `design-calibration/00_req_step_04_goals_non_goals.md` 的“结构化中间产物”“旧材料差异审计”和“自检与停审”小节,了解本章目标与非目标如何收敛。

#### 4.1 目标

| 目标 | 说明 | 验证方式 |
|---|---|---|
| 建立方法资产定义真相边界 | 明确本仓承载方法资产定义、版本发布与分发语义,相邻仓只按边界引用、消费或执行。 | 后续章节不把流程实例、成员状态、治理裁决、外部能力注册、交易流程或 UI 渲染写成本仓真相。 |
| 收束 Definition vs Use 分离 | 明确本仓讨论“定义”,不讨论相邻仓的运行时“使用”状态。 | Step 6/10/11 中所有相邻仓关系都能保持定义源与使用方分离。 |
| 收束当前方法资产主题范围 | 明确本轮需求围绕 SPEM Method Content、生命周期模型、AI 方针、视图策略和方法资产分发语义展开,不从旧对象模型反推范围。 | Step 7/9/11 的能力、功能和数据项均能回指当前方法资产主题,无法回指的项进入待确认或排除。 |
| 支撑方法资产版本与发布语义 | 为后续讨论版本稳定性、发布后不可随意修改、可追溯分发和下游消费建立目标边界。 | Step 10/11/14 能分别承接版本 / 发布规则、数据归属和验收口径,且不需要由消费仓补定义。 |
| 明确相邻仓职责不反向侵入 | 将 process、identity、governance、capability-hub、marketplace、UI/console 等职责排出本仓目标范围。 | 后续功能、接口、数据和验收中若出现相邻仓运行职责,必须被标为外部依赖、边界背景、外围增强或非目标。 |

#### 4.2 非目标

| 非目标 | 不做原因 |
|---|---|
| 流程执行、ProcessInstance 状态和运行时编排 | 属于 `L1-process` / L2 runtime 的运行职责;本仓只收束方法资产定义。 |
| 成员身份、成员生命周期、成员具备哪些角色或资格的状态 | 属于 `L1-identity` 或相关成员仓;本仓不拥有成员状态真相。 |
| Gate 决策、治理裁决、策略 enforce 和运行时合规执行 | 属于 `L1-governance` / runtime;本仓只讨论方法资产中的定义性方针或被引用语义。 |
| MCP/A2A/provider/tool 外部能力注册、绑定和访问裁决 | 属于 `L3-capability-hub` 及治理协作边界;本仓不管理外部能力接入。 |
| marketplace 定价、购买、结算、订单和商业分发流程 | 属于 `L6-marketplace`;本仓最多作为方法资产定义源或包来源被后续讨论。 |
| UI 页面渲染、字段过滤执行和前端交互体验 | 属于 `L5-chat`、`L5-console`、mobile 或其他体验仓;本仓只收束视图策略作为方法资产的定义边界。 |
| Artifact 正文、外部文件、证据正文和制品生命周期状态 | 属于 `L1-artifact` / archive 等相邻仓;本仓不保存或管理外部正文生命周期。 |
| 认证登录、权限系统和操作主体鉴权实现 | 属于身份、治理或平台安全边界;本仓需求只讨论方法资产的仓级职责。 |

---

## 8. 自检与停审

| 检查项 | 结论 | 说明 |
|---|---|---|
| 每个目标是否可验证 | 通过 | 验证方式均落到后续章节是否保持边界和可追溯。 |
| 每个非目标是否具体 | 通过 | 每个非目标都有相邻仓或责任边界。 |
| 是否避免功能需求 | 通过 | 未写 CRUD、查询、事件、同步或用户故事。 |
| 是否避免实现方案 | 通过 | 未写表结构、DTO、repository、port、事务、代码目录或技术栈。 |
| 是否避免空洞口号 | 通过 | 目标均指向方法资产真相边界、Definition vs Use、版本发布语义或相邻仓边界。 |

### 8.1 待确认事项

| 编号 | 事项 | 当前状态 | 后续落点 |
|---|---|---|---|
| REQ-S4-OPEN-001 | ViewProfile、AIPolicy、Plugin、MethodConfiguration 是否全部进入当前 P0。 | 本 Step 不裁定。 | Step 7 / Step 9 / Step 11。 |
| REQ-S4-OPEN-002 | Marketplace 相关分发是否属于本轮核心闭环、外围增强或边界外。 | 本 Step 不裁定。 | Step 6 / Step 7 / Step 12 / Step 15。 |

### 8.2 进入下一步条件

已满足进入 Step 5 的条件:

- 每个目标都可验证。
- 每个非目标足够具体。
- 未把功能、方案或空洞口号写进目标。

# L3-method-library 00 需求 Step 10: 业务规则与边界约束

> 状态: completed
> 创建日期: 2026-06-14
> 本轮口径: 全量重新讨论,旧 L3-method-library 文档只作差异审计。
> 回填位置: `00-需求文档.md` 第 10 章“业务规则与边界约束”

---

## 0. Step 内计划

| 模块 | 状态 | 产物 | 完成门禁 |
|---|---|---|---|
| 必读文档读取 | done | 必读文档摘要 | 已读取本 Step 必读输入。 |
| 整体模块搭建 | done | 本 Step 模块骨架 | 已先建骨架。 |
| 不变量规则思考 | done | 定义真相 / 正式版本 / Definition vs Use 规则候选 | 未写字段、状态机或数据库约束。 |
| 不变量规则写入 | done | 不变量规则 | 规则可回指功能需求。 |
| 禁止行为规则思考 | done | 串仓、越界和隐式拥有风险 | 未写接口或实现校验。 |
| 禁止行为规则写入 | done | 禁止行为规则 | 明确约束对象。 |
| 显式变化规则思考 | done | 正式化、版本变化、消费影响显式化 | 未写状态机编码。 |
| 显式变化规则写入 | done | 显式变化规则 | 明确不能隐式发生的变化。 |
| 边界 / 治理 / 审计规则思考 | done | 相邻仓边界和追溯要求 | 未把数据归属表写成规则。 |
| 边界 / 治理 / 审计规则写入 | done | 边界约束、治理约束、审计约束 | 边界不能被打穿。 |
| 规则与功能映射 | done | FR -> BR 映射表 | 无孤儿规则。 |
| 能力级规则停审 | done | 每个核心能力节点停审结论 | 可进入 Step 11。 |
| 旧材料差异审计 | done | 冲突 / 可保留事实 / 废弃项 | 旧材料未直接继承。 |
| 自检与停审 | done | 自检表 / 待确认事项 | 达到本 Step 门禁。 |

---

## 1. 必读文档

### 1.1 公共规范

| 文档 | 读取结论 |
|---|---|
| `standards/document/需求文档讨论流程_SOP.md` | Step 10 要把能力结构和仓边界固化为需求层硬规则;规则必须挂到能力节点和功能需求,不能写成实现校验、接口协议或对象字段。 |
| `standards/document/需求文档书写规范.md` | 4.10 固定使用业务规则表,列为`规则编号 / 规则类型 / 规则内容 / 约束对象`;规则类型先按不变量、禁止行为、显式变化、边界约束收敛,治理约束和审计约束按需补充。 |
| `projects/L3-method-library/design-calibration/00_req_step_02_position_boundary.md` | 本仓是方法资产定义、版本发布与分发语义真相仓,不承担相邻仓执行职责。 |
| `projects/L3-method-library/design-calibration/00_req_step_04_goals_non_goals.md` | 非目标已排除流程执行、成员状态、治理裁决、外部能力注册、交易、UI 执行、外部正文和认证鉴权。 |
| `projects/L3-method-library/design-calibration/00_req_step_07_core_capability_loop.md` | 四个核心能力节点是规则组织锚点:统一定义与识别、稳定版本正式化、下游按边界消费、变化追溯和一致性保护。 |
| `projects/L3-method-library/design-calibration/00_req_step_09_functional_requirements.md` | FR-ML-001~007 是核心功能映射来源,FR-ML-E-* 是外围增强来源。 |

### 1.2 本 Step 后置审计输入

| 旧材料 | 读取结论 |
|---|---|
| `projects/L3-method-library/00-需求文档.md` | 旧稿有发布后不可原地修改、下游不反向改写定义、marketplace 不拥有正文等规则线索;但混入 fingerprint、event、outbox、接口名、验收脚本和对象字段。 |
| `projects/L3-method-library/README.md` | 可作为边界线索,不能继承技术栈、目录、接口或运行流程。 |
| `domain/method-library/README.md` | Definition vs Use、Published 不可修改、下游只持引用/快照等方向可保留为需求规则;INV 编号、RPC、事件和数据结构不能继承。 |
| `projects/L3-method-library/design-calibration/03_ddd_*` | 旧详细设计只用于识别不该进入需求层的状态机、port、repository、event schema 和事务规则。 |

---

## 2. 整体模块骨架

Step 10 按规则类型与能力节点交叉收敛:

| 模块 | 输出 | 不输出 |
|---|---|---|
| 不变量 | 必须始终成立的定义真相、正式版本和 Definition vs Use 规则。 | 不写字段唯一约束、数据库约束或状态机代码。 |
| 禁止行为 | 明确禁止串仓、越界、隐式拥有和消费未正式资产。 | 不写 handler/service 校验逻辑。 |
| 显式变化 | 正式化、版本变化和消费影响必须显式发生。 | 不写具体状态枚举、Command 名或事件名。 |
| 边界约束 | 与 process、identity、governance、capability-hub、marketplace、UI、artifact 的边界红线。 | 不写接口签名、包依赖或数据归属矩阵。 |
| 治理 / 审计约束 | 方法资产正式化和影响性变化需要可追溯、可审计,治理结论只作为边界条件。 | 不把 governance 的裁决流程搬进本仓。 |

---

## 3. 模块思考记录

### 3.1 不变量规则

问题回答:

- 本仓存在的硬前提是:方法资产定义真相归 `L3-method-library`,不能由下游消费仓各自拥有。
- 正式消费必须围绕稳定、可识别的方法资产版本展开。
- Definition vs Use 必须持续成立:本仓定义,相邻仓使用、执行、索引或展示。

诊断:

- 旧材料中“Published 不可修改”“fingerprint 贯穿下游”方向有价值,但 fingerprint 是实现/设计层稳定性机制,不能作为需求规则原文。
- 如果把不变量写成某个字段必须存在,会提前进入 Step 11 或 03 详细设计。

取舍:

- 不变量写为需求层业务条件:定义真相归属、正式资产可消费、版本语义稳定、Definition vs Use 分离。

### 3.2 禁止行为规则

问题回答:

- 最危险的失败模式是消费仓反向创建或修改方法资产定义,或者把运行时状态当成定义真相。
- 未正式化或不稳定的方法资产不能作为正式消费依据。
- 运行期消费、事件协作和生态分发都不能被写成源码依赖或定义迁移。

诊断:

- 旧稿把下游同步、snapshot、event、query 混在规则附近,容易让规则滑向接口协作。

取舍:

- 禁止行为只写“不得发生什么”,不写如何校验、如何报错、通过哪个接口阻止。

### 3.3 显式变化规则

问题回答:

- 方法资产从非正式到正式必须是显式变化,不能因为被读取、引用或消费而隐式正式化。
- 影响版本语义或下游消费的一类变化必须被显式识别,不能静默覆盖既有正式语义。
- 消费影响也必须可被识别,否则追溯和一致性保护无法成立。

诊断:

- 旧稿中的 publish、supersede、retire、fingerprint_changed 等可以提示变化类型,但具体生命周期状态和事件名属于后续设计。

取舍:

- 显式变化规则只表达“必须显式发生”的业务边界,不规定具体状态机和事件。

### 3.4 边界 / 治理 / 审计规则

问题回答:

- 相邻仓边界需要在规则层写成红线,否则 Step 11/12 容易重新把执行状态、成员状态、治理裁决、工具注册、交易、UI 渲染和 artifact 正文拉回本仓。
- 正式化和影响性变化必须可审计,否则 Step 14 验收无法证明方法资产变化有来源。
- 若正式化需要治理结论,该结论只能作为前置或引用边界,不能让本仓承担治理执行。

诊断:

- 旧稿中 Gate、outbox、audit record、snapshot ref 等实现/设计语义较多,不能直接写入需求规则。

取舍:

- 用治理约束和审计约束保留需求层硬约束,具体裁决、审计材料、事件和存储在后续文档再收束。

---

## 4. 结构化中间产物

### 4.1 业务规则表

| 规则编号 | 规则类型 | 规则内容 | 约束对象 |
|---|---|---|---|
| BR-ML-001 | 不变量 | 方法资产定义真相必须归属 `L3-method-library`,不能退化为下游消费仓的私有定义或散落文档。 | 方法资产定义真相 |
| BR-ML-002 | 不变量 | 方法资产必须具备可被人类和系统稳定识别的定义身份与适用语境,否则不能支撑正式版本、消费和追溯。 | 方法资产身份与目录语义 |
| BR-ML-003 | 不变量 | Definition vs Use 必须始终成立:本仓负责定义,相邻仓只能按边界使用、执行、索引或展示。 | 本仓与消费仓关系 |
| BR-ML-004 | 不变量 | 已进入正式使用语境的方法资产必须保持版本语义稳定,不得让既有正式引用在未显式变化的情况下改变含义。 | 正式方法资产版本 |
| BR-ML-005 | 禁止行为 | 下游消费仓不得创建、修改或替代方法资产定义作为自身真相。 | 下游消费行为 |
| BR-ML-006 | 禁止行为 | 不得把流程执行状态、成员状态、治理裁决、外部能力注册、交易流程、UI 渲染或 artifact 正文作为方法资产定义成立条件。 | 边界外运行职责 |
| BR-ML-007 | 禁止行为 | 未正式化或仍处于调整语境的方法资产不得作为正式消费依据。 | 正式消费依据 |
| BR-ML-008 | 禁止行为 | 不得把运行期消费关系、事件协作关系或生态分发关系写成消费仓对本仓业务实现的源码级拥有关系。 | 仓际依赖边界 |
| BR-ML-009 | 显式变化 | 方法资产从非正式语境进入正式使用语境必须通过显式正式化发生,不得由读取、引用、同步或运行时使用隐式触发。 | 方法资产正式化 |
| BR-ML-010 | 显式变化 | 会改变正式方法资产版本语义的修改必须显式形成新的版本或等价正式变化口径,不得静默覆盖既有正式语义。 | 版本语义变化 |
| BR-ML-011 | 显式变化 | 可能影响既有下游消费的一类变化必须能被显式识别,不得让消费影响只存在于实现细节或人工约定中。 | 消费影响变化 |
| BR-ML-012 | 边界约束 | `L1-process` 负责流程执行和过程运行状态;`L3-method-library` 只定义过程模板、任务和方法语义。 | method-library / process 边界 |
| BR-ML-013 | 边界约束 | `L1-identity` 负责成员身份、成员生命周期和成员实际具备的角色状态;`L3-method-library` 只定义角色等方法语义。 | method-library / identity 边界 |
| BR-ML-014 | 边界约束 | `L1-governance` 负责治理裁决和策略执行;`L3-method-library` 只定义可被引用的方法方针或 AI 方针语义。 | method-library / governance 边界 |
| BR-ML-015 | 边界约束 | `L3-capability-hub` 负责外部工具、MCP、A2A、provider 等能力注册和访问语义;`L3-method-library` 不接管外部能力接入。 | method-library / capability-hub 边界 |
| BR-ML-016 | 边界约束 | `L6-marketplace` 负责定价、购买、订单、结算和商业履约;`L3-method-library` 只作为方法资产定义或资产包来源被消费。 | method-library / marketplace 边界 |
| BR-ML-017 | 边界约束 | UI/console 负责渲染和交互执行;`L3-method-library` 可以定义视图策略语义,但不执行前端渲染。 | method-library / UI 边界 |
| BR-ML-018 | 边界约束 | artifact/archive 负责正文、证据文件和制品生命周期;`L3-method-library` 可以定义 work product 语义,但不拥有 artifact 正文真相。 | method-library / artifact 边界 |
| BR-ML-019 | 治理约束 | 若方法资产正式化需要治理结论,该治理结论只能作为正式化前置或引用依据,不得把治理裁决执行迁入本仓。 | 正式化治理边界 |
| BR-ML-020 | 审计约束 | 方法资产正式化、版本语义变化和影响既有消费的变化必须具备可追溯依据,以支撑后续验收和审计。 | 方法资产追溯 |
| BR-ML-021 | 审计约束 | 正式消费应能回溯到被消费的方法资产定义来源和版本语境,避免下游只留下不可解释的本地约定。 | 正式消费追溯 |
| BR-ML-E-001 | 边界约束 | MethodPlugin、MethodConfiguration、marketplace 生态分发和高级策略变体属于外围增强时,不得改变核心方法资产定义真相边界。 | 外围增强能力 |

### 4.2 规则与功能需求映射

| 功能需求 | 对应规则 | 映射说明 |
|---|---|---|
| FR-ML-001 方法资产定义建模能力 | BR-ML-001, BR-ML-003, BR-ML-005, BR-ML-006 | 保护方法资产定义真相不散落、不串仓、不被边界外运行职责污染。 |
| FR-ML-002 方法资产身份与目录识别能力 | BR-ML-002, BR-ML-021 | 确保方法资产身份和适用语境可被识别,正式消费能回溯定义来源。 |
| FR-ML-003 方法资产正式化能力 | BR-ML-007, BR-ML-009, BR-ML-019, BR-ML-020 | 确保只有显式正式化后的资产进入正式使用语境,治理和审计边界清楚。 |
| FR-ML-004 方法资产版本稳定边界能力 | BR-ML-004, BR-ML-010, BR-ML-020 | 确保正式版本语义稳定,影响语义的变化必须显式。 |
| FR-ML-005 正式方法资产受控消费支撑能力 | BR-ML-003, BR-ML-005, BR-ML-007, BR-ML-008, BR-ML-012~018, BR-ML-021 | 确保下游按边界消费,不把定义真相或运行职责迁移。 |
| FR-ML-006 方法资产追溯能力 | BR-ML-020, BR-ML-021 | 确保正式化、版本变化和消费引用具备可追溯依据。 |
| FR-ML-007 方法资产消费一致性保护能力 | BR-ML-004, BR-ML-010, BR-ML-011, BR-ML-020 | 确保影响下游消费的变化显式可识别,避免静默破坏引用稳定性。 |
| FR-ML-E-001 方法资产包与方法集组织能力 | BR-ML-E-001 | 外围增强不能改变核心定义真相边界。 |
| FR-ML-E-002 方法资产生态发现能力 | BR-ML-016, BR-ML-E-001 | marketplace 生态发现不能变成交易职责或定义迁移。 |
| FR-ML-E-003 高级策略变体表达能力 | BR-ML-014, BR-ML-017, BR-ML-E-001 | 高级策略变体不得让治理执行或 UI 渲染进入本仓。 |

### 4.3 能力级规则停审结论

| 核心能力节点 | 规则承接 | 停审结论 |
|---|---|---|
| 方法资产能够被统一定义和识别 | BR-ML-001, BR-ML-002, BR-ML-003, BR-ML-005, BR-ML-006 | 已钉住定义真相、身份语境和 Definition vs Use,可进入数据归属讨论。 |
| 方法资产能够以稳定版本进入正式使用语境 | BR-ML-004, BR-ML-007, BR-ML-009, BR-ML-010, BR-ML-019, BR-ML-020 | 已钉住正式化、版本稳定、治理前置和审计追溯,未写状态机。 |
| 正式方法资产能够被下游按边界消费 | BR-ML-003, BR-ML-005, BR-ML-007, BR-ML-008, BR-ML-012~018, BR-ML-021 | 已钉住消费边界和相邻仓红线,未写接口或事件。 |
| 方法资产变化能够被追溯并保护消费一致性 | BR-ML-004, BR-ML-010, BR-ML-011, BR-ML-020, BR-ML-021 | 已钉住显式变化、消费影响识别和追溯要求,可交给 Step 11/12/14 承接。 |

### 4.4 跨能力规则审计

| 检查项 | 结论 | 说明 |
|---|---|---|
| 是否存在无法回指功能需求的规则 | 无 | 每条核心规则均映射到 FR-ML-001~007 或外围增强功能。 |
| 是否存在规则重复 | 无 | 相近规则分别承担不变量、禁止行为、显式变化或边界约束。 |
| 是否存在规则冲突 | 无 | 治理约束只保留前置/引用,不冲突于本仓不执行治理裁决的边界。 |
| 是否遗漏主要相邻仓边界 | 未发现 | process、identity、governance、capability-hub、marketplace、UI、artifact 均有边界规则。 |
| 是否把实现机制写成规则 | 未发现 | 未写事件 schema、fingerprint 字段、port、repository、handler、事务或异常码。 |

---

## 5. 旧材料差异审计

### 5.1 可保留为需求规则的旧方向

| 旧方向 | 本轮吸收方式 |
|---|---|
| Published 内容不可被随意原地修改 | 抽象为 BR-ML-004 和 BR-ML-010:正式版本语义稳定,影响语义变化必须显式。 |
| 下游只消费定义语义,不反向改写定义 | 抽象为 BR-ML-003、BR-ML-005 和 BR-ML-012~018。 |
| marketplace 不拥有 MethodContent 正文 | 抽象为 BR-ML-016 和 BR-ML-E-001。 |
| audit / trace / evidence 支撑发布和变化追溯 | 抽象为 BR-ML-020 和 BR-ML-021。 |
| P1 能力不得改变 P0 定义真相边界 | 抽象为 BR-ML-E-001。 |

### 5.2 不能继承的旧口径

| 旧口径 | 为什么不能在 Step 10 继承 | 后续处理 |
|---|---|---|
| `fingerprint`、`snapshot_ref`、`outbox event` 作为规则正文 | 这是后续设计/接口/数据或实现机制,不是需求层业务规则。 | Step 11/12/13/14 或 03 详细设计。 |
| `PublishMethodContent`、`ResolveViewProfile`、`ExportDefinitionSnapshot` 等接口名 | 接口动作不属于业务规则表。 | Step 12。 |
| 生命周期状态机和状态迁移细节 | Step 10 只说明变化必须显式,不定义状态机编码。 | 后续 01/02/03。 |
| `Gate approved/rejected` 事件或治理调用流程 | 本仓不执行治理裁决,只能保留治理前置/引用边界。 | Step 12/15 后续协作讨论。 |
| 下游事件 payload 字段和幂等处理字段 | 属于接口协作或测试设计,不是本 Step 规则。 | Step 12/14。 |
| Qualification/Profile/Binding 等旧对象边界细节 | 数据归属和对象范围尚未在本轮 Step 11 收束。 | Step 11/15。 |

---

## 6. 回填草稿

### 10. 业务规则与边界约束

> 校准来源:
> - `design-calibration/00_req_step_10_business_rules_boundaries.md`
>
> 延伸阅读:
> - 建议继续阅读 `design-calibration/00_req_step_10_business_rules_boundaries.md` 的“结构化中间产物”“旧材料差异审计”和“自检与停审”小节,了解本章规则如何由功能需求和仓边界收敛。

| 规则编号 | 规则类型 | 规则内容 | 约束对象 |
|---|---|---|---|
| BR-ML-001 | 不变量 | 方法资产定义真相必须归属 `L3-method-library`,不能退化为下游消费仓的私有定义或散落文档。 | 方法资产定义真相 |
| BR-ML-002 | 不变量 | 方法资产必须具备可被人类和系统稳定识别的定义身份与适用语境,否则不能支撑正式版本、消费和追溯。 | 方法资产身份与目录语义 |
| BR-ML-003 | 不变量 | Definition vs Use 必须始终成立:本仓负责定义,相邻仓只能按边界使用、执行、索引或展示。 | 本仓与消费仓关系 |
| BR-ML-004 | 不变量 | 已进入正式使用语境的方法资产必须保持版本语义稳定,不得让既有正式引用在未显式变化的情况下改变含义。 | 正式方法资产版本 |
| BR-ML-005 | 禁止行为 | 下游消费仓不得创建、修改或替代方法资产定义作为自身真相。 | 下游消费行为 |
| BR-ML-006 | 禁止行为 | 不得把流程执行状态、成员状态、治理裁决、外部能力注册、交易流程、UI 渲染或 artifact 正文作为方法资产定义成立条件。 | 边界外运行职责 |
| BR-ML-007 | 禁止行为 | 未正式化或仍处于调整语境的方法资产不得作为正式消费依据。 | 正式消费依据 |
| BR-ML-008 | 禁止行为 | 不得把运行期消费关系、事件协作关系或生态分发关系写成消费仓对本仓业务实现的源码级拥有关系。 | 仓际依赖边界 |
| BR-ML-009 | 显式变化 | 方法资产从非正式语境进入正式使用语境必须通过显式正式化发生,不得由读取、引用、同步或运行时使用隐式触发。 | 方法资产正式化 |
| BR-ML-010 | 显式变化 | 会改变正式方法资产版本语义的修改必须显式形成新的版本或等价正式变化口径,不得静默覆盖既有正式语义。 | 版本语义变化 |
| BR-ML-011 | 显式变化 | 可能影响既有下游消费的一类变化必须能被显式识别,不得让消费影响只存在于实现细节或人工约定中。 | 消费影响变化 |
| BR-ML-012 | 边界约束 | `L1-process` 负责流程执行和过程运行状态;`L3-method-library` 只定义过程模板、任务和方法语义。 | method-library / process 边界 |
| BR-ML-013 | 边界约束 | `L1-identity` 负责成员身份、成员生命周期和成员实际具备的角色状态;`L3-method-library` 只定义角色等方法语义。 | method-library / identity 边界 |
| BR-ML-014 | 边界约束 | `L1-governance` 负责治理裁决和策略执行;`L3-method-library` 只定义可被引用的方法方针或 AI 方针语义。 | method-library / governance 边界 |
| BR-ML-015 | 边界约束 | `L3-capability-hub` 负责外部工具、MCP、A2A、provider 等能力注册和访问语义;`L3-method-library` 不接管外部能力接入。 | method-library / capability-hub 边界 |
| BR-ML-016 | 边界约束 | `L6-marketplace` 负责定价、购买、订单、结算和商业履约;`L3-method-library` 只作为方法资产定义或资产包来源被消费。 | method-library / marketplace 边界 |
| BR-ML-017 | 边界约束 | UI/console 负责渲染和交互执行;`L3-method-library` 可以定义视图策略语义,但不执行前端渲染。 | method-library / UI 边界 |
| BR-ML-018 | 边界约束 | artifact/archive 负责正文、证据文件和制品生命周期;`L3-method-library` 可以定义 work product 语义,但不拥有 artifact 正文真相。 | method-library / artifact 边界 |
| BR-ML-019 | 治理约束 | 若方法资产正式化需要治理结论,该治理结论只能作为正式化前置或引用依据,不得把治理裁决执行迁入本仓。 | 正式化治理边界 |
| BR-ML-020 | 审计约束 | 方法资产正式化、版本语义变化和影响既有消费的变化必须具备可追溯依据,以支撑后续验收和审计。 | 方法资产追溯 |
| BR-ML-021 | 审计约束 | 正式消费应能回溯到被消费的方法资产定义来源和版本语境,避免下游只留下不可解释的本地约定。 | 正式消费追溯 |
| BR-ML-E-001 | 边界约束 | MethodPlugin、MethodConfiguration、marketplace 生态分发和高级策略变体属于外围增强时,不得改变核心方法资产定义真相边界。 | 外围增强能力 |

---

## 7. 自检与停审

### 7.1 自检表

| 检查项 | 结论 | 说明 |
|---|---|---|
| 每条规则是否有编号、类型、内容和约束对象 | 通过 | 业务规则表符合 4.10 固定结构。 |
| 是否区分不变量、禁止行为、显式变化和边界约束 | 通过 | 核心四类均已覆盖,并补充治理约束和审计约束。 |
| 是否回指能力节点和功能需求 | 通过 | 已提供规则与功能需求映射、能力级规则停审结论。 |
| 是否避免实现校验、接口协议和对象字段 | 通过 | 未写 repository、port、handler、事务、事件 schema、字段唯一约束或异常码。 |
| 是否覆盖主要相邻仓边界 | 通过 | process、identity、governance、capability-hub、marketplace、UI、artifact 均有规则承接。 |
| 是否可进入 Step 11 | 通过 | 规则已经能支撑数据真相、快照、引用和禁止保存正文的归属讨论。 |

### 7.2 待确认事项

| 编号 | 事项 | 当前状态 | 后续落点 |
|---|---|---|---|
| ML-REQ-OPEN-010-01 | 当前方法资产主题的具体数据范围需要继续收束,尤其是旧材料中的 Qualification/Profile/Binding、ViewProfile、AIPolicy、Plugin/Configuration 等。 | 不在 Step 10 裁定具体数据项。 | Step 11 / Step 15。 |
| ML-REQ-OPEN-010-02 | 方法资产正式化是否一定需要外部治理结论,或只在高风险资产上需要。 | 当前仅写成治理前置/引用边界,不写执行流程。 | Step 12 / Step 15。 |
| ML-REQ-OPEN-010-03 | 外围增强中的 MethodPlugin、MethodConfiguration 和 marketplace 分发是否进入本轮正式 P0 验收。 | 当前只保留边界约束,不改变核心闭环。 | Step 14 / Step 15。 |

### 7.3 进入下一步条件

Step 10 已满足进入 Step 11 的条件:

- 核心功能需求均有业务规则承接。
- 方法资产定义、正式化、受控消费、追溯一致性均有规则保护。
- 相邻仓边界已经写成需求层红线。
- 未把数据归属、接口协议、状态机、事件 schema 或实现校验写进规则表。

下一步 Step 11 将只处理:

- 哪些数据由本仓拥有真相。
- 哪些数据只是快照或引用。
- 哪些正文绝不能保存在本仓。
- 数据生命周期在需求层面的口径。

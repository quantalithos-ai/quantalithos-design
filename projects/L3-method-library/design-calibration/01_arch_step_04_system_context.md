# L3-method-library 01 架构 Step 4: 系统边界与上下文

> 状态: completed
> 创建日期: 2026-06-14
> 本轮口径: 基于 Step 1~3 的需求基线、目标约束和职责边界,收束本仓在全局系统中的正式上下文对象与输入/输出面。
> 回填位置: `01-架构设计.md` 的系统边界与上下文章节。

---

## 0. Step 内计划

| 模块 | 状态 | 产物 | 完成门禁 |
|---|---|---|---|
| 必读文档读取 | done | 必读文档摘要 | 已读取架构 SOP/规范、Step 1~3、00 使用方与依赖、全局仓库拆分。 |
| 整体模块搭建 | done | 本 Step 模块骨架 | 已先建骨架,未直接写上下文图。 |
| 上游上下文思考 | done | 问题回答 / 诊断 / 取舍 | 已区分编译期来源、事件协作和实现依赖。 |
| 上游上下文写入 | done | 输入/依赖对象表项 | 未写接口名或协议对象。 |
| 下游上下文思考 | done | 问题回答 / 诊断 / 取舍 | 已区分核心下游和候选/外围下游。 |
| 下游上下文写入 | done | 输出对象表项 | 未把候选关系升级为核心前置。 |
| 输入输出面思考 | done | 问题回答 / 诊断 / 取舍 | 已收束系统上下文图和边界说明。 |
| 输入输出面写入 | done | 系统上下文图、上下游表、边界说明 | 图不表达接口、事件、实现组件或运行时顺序。 |
| 架构单元停审 | done | 不适用说明 | Step 4 尚未划分架构单元。 |
| 旧材料差异审计 | done | 可保留方向 / 废弃旧口径 | 旧材料未直接继承。 |
| 自检与停审 | done | 自检表 / 待确认事项 | 达到本 Step 门禁,可进入 Step 5。 |

---

## 1. 必读文档

### 1.1 公共规范

| 文档 | 读取结论 |
|---|---|
| `standards/document/架构设计讨论流程_SOP.md` | Step 4 输出系统上下文图、上下游与输入/输出面表、边界说明,不展开内部职责、数据所有权、容器部署或接口协议。 |
| `standards/document/架构设计书写规范.md` | 系统上下文图只允许本仓、内部仓、外部系统、外部能力、入口系统;图关系只表达输入、输出、依赖。 |
| `standards/document/设计文档讨论中间产物规范.md` | 本 Step 仍需按问题回答、诊断、取舍、结构化中间产物、回填草稿、自检推进。 |

### 1.2 本仓输入

| 文档 | 读取结论 |
|---|---|
| `projects/L3-method-library/design-calibration/01_arch_step_01_requirement_baseline.md` | 核心上下文关系必须服务于定义真相、正式版本、受控消费和追溯一致性。 |
| `projects/L3-method-library/design-calibration/01_arch_step_02_goals_constraints.md` | 核心闭环与外围增强必须隔离,条件型协作不得写成核心前置。 |
| `projects/L3-method-library/design-calibration/01_arch_step_03_responsibility_boundary.md` | 本仓只承担方法资产定义源职责,不承接相邻仓运行职责。 |
| `projects/L3-method-library/design-calibration/00_req_step_06_consumers_dependencies.md` | 主链输入为 `L0-core`、`L0-bus`;主链输出为 `L1-process`、`L1-identity`、`L2-runtime`、`L2-member-images`。 |
| `projects/L3-method-library/design-calibration/00_req_step_12_interfaces_dependencies.md` | 对外关系只能表达能力面,不写 API、DTO、event schema、port 或 adapter。 |
| `projects/README.md` | 设计仓与实现仓分离;`L3-method-library` 是 27 个正式子项目之一。 |
| `architecture/仓库拆分方案.md` | `method-library` 位于 L3 方法能力层,同层兄弟是 `capability-hub`;L0 为共享契约层,L1/L2/L5/L6 为不同消费或入口层。 |

### 1.3 后置审计输入

| 文档 | 审计用途 |
|---|---|
| `projects/L3-method-library/README.md` | 审计旧上下游和技术依赖是否把 PG、对象存储、RPC、marketplace 交易写成上下文前置。 |
| `projects/L3-method-library/01-架构设计.md` | 审计旧上下文图、P0 下游同步表、event/snapshot/fingerprint 链是否越过当前架构层。 |

---

## 2. 整体模块骨架

Step 4 按上游、下游、输入输出面三类收束上下文。图只展示关键上下文对象,表补足候选和外围关系的当前口径。

| 模块 | 本 Step 回答的问题 | 本 Step 不回答的问题 |
|---|---|---|
| 上游上下文 | 本仓依赖哪些正式输入或平台基础。 | 不写 package、crate、SDK 调用、协议对象或存储组件。 |
| 下游上下文 | 哪些仓按边界消费本仓方法资产语义。 | 不写具体查询、同步、事件、缓存或快照机制。 |
| 输入输出面 | 本仓和上下文对象之间有哪些能力面。 | 不写 API 路径、事件名、DTO、port 或 handler。 |

---

## 3. 模块思考记录

### 3.1 上游上下文

#### 问题回答

本仓的关键输入/依赖对象是 `L0-core` 和 `L0-bus`。`L0-core` 提供共享契约、基础引用和跨仓一致性基线;`L0-bus` 提供方法资产变化被平台感知的事件协作通道。

#### 诊断

旧 README 把 PostgreSQL、对象存储和 RPC 等实现组件写得很早,容易被误认为系统上下文对象。但架构 Step 4 的上下文对象应是仓或系统边界,不是实现组件。

#### 取舍

主图只放 `L0-core` 和 `L0-bus` 作为关键输入/依赖对象。外部数据库、对象存储、校验脚本、package 存储等旧技术假设不进入本 Step。

### 3.2 下游上下文

#### 问题回答

当前主链下游是 `L1-process`、`L1-identity`、`L2-runtime`、`L2-member-images`。它们分别消费过程模板/任务/方法定义、角色等身份语义、执行语境所需方法语义、Role 到 image variant 的定义来源。

#### 诊断

`L1-governance`、`L1-artifact`、`L6-marketplace`、`L5-console` / `L0-sdk` 均与本仓相关,但新版 00 把它们收束为条件型、候选或外围关系。若将它们全部放入主图,会把候选上下文误写成核心上下文。

#### 取舍

主图只放核心闭环下游。表中保留 governance、artifact、marketplace、console/SDK 的非主图口径,用于后续 Step 7/8/9/12/14 继续裁剪。

### 3.3 输入输出面

#### 问题回答

本仓的输入面是共享契约/基础引用、事件协作通道、条件型治理结论或外部依据引用。本仓的输出面是正式方法资产定义语义、目录/身份语义、版本/追溯语义、变化可感知语义和分发语义来源。

#### 诊断

如果把输入输出面写成 API、事件名或 snapshot 格式,会越过系统上下文进入接口/协议或详细设计。

#### 取舍

本 Step 只写能力面和边界说明。具体同步、事件、查询、快照、失败重试和数据结构后续再讨论。

---

## 4. 架构单元小循环记录

本 Step 不适用架构单元小循环。

原因: Step 4 只收束本仓与外部上下文对象之间的系统边界,尚未进入本仓内部限界上下文或子域划分。架构单元小循环从 Step 5 开始适用。

---

## 5. 旧材料差异审计

### 5.1 可保留为上下文方向的旧材料

| 旧方向 | 本轮吸收方式 |
|---|---|
| process 消费模板 / 方法定义 | 作为 `L1-process` 核心输出上下文保留。 |
| identity 消费角色等定义语义 | 作为 `L1-identity` 核心输出上下文保留。 |
| runtime 消费方法、角色、模板语义 | 作为 `L2-runtime` 核心输出上下文保留。 |
| member-images 消费 Role -> image variant 来源 | 作为 `L2-member-images` 核心输出上下文保留。 |
| method-library 通过 bus 发出变化信号 | 作为 `L0-bus` 事件协作边界保留,不继承事件名。 |
| governance、artifact、marketplace、console/SDK 与本仓相关 | 作为条件型、候选或外围上下文保留,不进入主图核心前置。 |

### 5.2 必须废弃的旧口径

| 旧口径 | 为什么不能在 Step 4 继承 | 后续处理 |
|---|---|---|
| PostgreSQL、对象存储作为系统上下文对象 | 这些是实现基础设施或技术选型,不是仓级上下文对象。 | Step 10 或后续配置/实施讨论。 |
| RPC、API、event 名称作为输入/输出面 | Step 4 只能写能力面,不能写协议对象。 | Step 9/12 后续讨论。 |
| P0 下游同步成功率和 E2E 链路 | 这是测试/验收或运行指标,不是系统上下文。 | 05/06 后续承接。 |
| governance / artifact / UI / marketplace 全量入主图 | 新版需求将这些关系收缩为条件型、候选或外围关系。 | 后续 Step 分别裁剪。 |
| capability-hub 作为直接上下文 | 当前职责边界明确 method-library 与 capability-hub 是相邻 L3 仓,本 Step 不建立直接上下文依赖。 | 如 Qualification/CapabilityDefinition 重新纳入,需回写。 |

---

## 6. 结构化中间产物

### 6.1 系统上下文图

```text
+----------------------+
| L0-core             |
| shared contracts    |
+----------+-----------+
           |
           | input / dependency
           v
+------------------------------+
| L3-method-library            |
| method asset definition src  |
+-----+----------+--------+----+
      |          |        |
      | output   | output | output
      v          v        v
 +---------+ +----------+ +------------------+
 | L1-     | | L1-      | | L2-runtime      |
 | process | | identity | | method context  |
 +---------+ +----------+ +------------------+
      |
      | output
      v
 +------------------+
 | L2-member-images |
 | role image src   |
 +------------------+

+----------------------+
| L0-bus              |
| event collaboration |
+----------+-----------+
           ^
           | output / dependency
           |
+----------+-----------+
| L3-method-library   |
+----------------------+
```

该图仅表达本仓与正式上下文对象之间的边界关系与输入/输出方向,不表达接口、事件、实现组件或运行时顺序。

图示说明:

- `L0-core` 是本仓表达方法资产身份、引用和跨仓一致性所需的共享契约来源。
- `L0-bus` 是方法资产变化被平台感知的事件协作边界,不是业务仓源码依赖。
- `L1-process`、`L1-identity`、`L2-runtime`、`L2-member-images` 是当前核心闭环的主链消费对象。
- governance、artifact、marketplace、console/SDK 等相关关系不放入主图,避免把候选或外围关系升级成核心前置。

### 6.2 上下游与输入/输出面表

| 对象 | 关系方向 | 关系类型 | 输入/输出面 | 说明 |
|---|---|---|---|---|
| `L0-core` | 输入 | 来源 | 共享契约、基础引用、跨仓一致性基线 | 本仓需要稳定表达可被下游长期引用的方法资产身份。 |
| `L0-bus` | 输出 | 来源 | 方法资产变化可感知协作面 | 本仓通过事件协作边界让变化成为平台级信号,但不写事件 schema。 |
| `L1-process` | 输出 | 消费 | 过程模板、任务和方法定义语义 | process 执行流程,但 Template 和方法定义来源归本仓。 |
| `L1-identity` | 输出 | 消费 | 角色等身份相关方法语义 | identity 可消费角色语义,但成员身份和实际角色状态不归本仓。 |
| `L2-runtime` | 输出 | 消费 | 运行时所需方法、角色、模板语义 | runtime 使用方法语义形成执行上下文,但不拥有定义真相。 |
| `L2-member-images` | 输出 | 消费 | Role 到 image variant 的定义来源 | member-images 不应 hardcode 角色镜像映射。 |
| `L1-governance` | 输入 | 治理依赖 | 条件型正式化结论或治理依据引用 | 当前只作为条件型协作,不把治理执行写成本仓前置。 |
| `L1-artifact` | 输出 | 消费 | WorkProductDefinition 等定义语义候选 | 当前作为候选消费关系,artifact 正文和生命周期不归本仓。 |
| `L6-marketplace` | 输出 | 消费 | 方法资产包或分发语义来源 | 当前只保留外围生态发现和分发语义,交易履约不入仓。 |
| `L5-console` / `L0-sdk` | 输出 | 入口 | 方法资产管理与读取入口候选 | 当前作为体验/封装入口候选,不成为核心闭环前置。 |

### 6.3 边界说明

当前系统上下文以方法资产定义真相和核心下游受控消费为中心,因此主图只纳入 `L0-core`、`L0-bus`、`L1-process`、`L1-identity`、`L2-runtime` 和 `L2-member-images`。`L1-governance`、`L1-artifact`、`L6-marketplace`、`L5-console` / `L0-sdk` 与本仓相关,但当前只按条件型、候选或外围关系记录,不得升级为核心前置。数据库、对象存储、缓存、RPC 和事件名不是系统上下文对象,不能在本章替代仓级边界。依赖失效时的架构口径是:外围或条件型对象不可用不得改写已成立的方法资产定义真相,核心输出消费可降级为不可感知或待恢复,但不能把定义真相迁移给下游。

---

## 7. 回填草稿

### 7.1 可回填到 `01-架构设计.md` 的系统上下文图

> 校准来源:
> - `design-calibration/01_arch_step_04_system_context.md`
>
> 延伸阅读:
> - 建议继续阅读 `design-calibration/01_arch_step_04_system_context.md` 的“结构化中间产物”“旧材料差异审计”和“自检与停审”小节,了解系统上下文如何从需求依赖和职责边界收敛。

```text
                 +----------------------+
                 | L0-core             |
                 | shared contracts    |
                 +----------+-----------+
                            |
                            | input / dependency
                            v
                 +----------------------+
                 | L3-method-library   |
                 | method asset source |
                 +----+------+----+----+
                      |      |    |
            output    |      |    | output
                      v      v    v
               +---------+ +----------+ +-------------+
               | L1-     | | L1-      | | L2-runtime |
               | process | | identity | |             |
               +---------+ +----------+ +-------------+
                      |
                      | output
                      v
               +------------------+
               | L2-member-images |
               +------------------+

                 +----------------------+
                 | L0-bus              |
                 | event collaboration |
                 +----------+-----------+
                            ^
                            | output / dependency
                            |
                 +----------+-----------+
                 | L3-method-library   |
                 +----------------------+
```

该图仅表达本仓与正式上下文对象之间的边界关系与输入/输出方向,不表达接口、事件、实现组件或运行时顺序。

### 7.2 可回填的上下游与输入/输出面表

| 对象 | 关系方向 | 关系类型 | 输入/输出面 | 说明 |
|---|---|---|---|---|
| `L0-core` | 输入 | 来源 | 共享契约、基础引用、跨仓一致性基线 | 本仓需要稳定表达可被下游长期引用的方法资产身份。 |
| `L0-bus` | 输出 | 来源 | 方法资产变化可感知协作面 | 本仓通过事件协作边界让变化成为平台级信号,但不写事件 schema。 |
| `L1-process` | 输出 | 消费 | 过程模板、任务和方法定义语义 | process 执行流程,但 Template 和方法定义来源归本仓。 |
| `L1-identity` | 输出 | 消费 | 角色等身份相关方法语义 | identity 可消费角色语义,但成员身份和实际角色状态不归本仓。 |
| `L2-runtime` | 输出 | 消费 | 运行时所需方法、角色、模板语义 | runtime 使用方法语义形成执行上下文,但不拥有定义真相。 |
| `L2-member-images` | 输出 | 消费 | Role 到 image variant 的定义来源 | member-images 不应 hardcode 角色镜像映射。 |
| `L1-governance` | 输入 | 治理依赖 | 条件型正式化结论或治理依据引用 | 当前只作为条件型协作,不把治理执行写成本仓前置。 |
| `L1-artifact` | 输出 | 消费 | WorkProductDefinition 等定义语义候选 | 当前作为候选消费关系,artifact 正文和生命周期不归本仓。 |
| `L6-marketplace` | 输出 | 消费 | 方法资产包或分发语义来源 | 当前只保留外围生态发现和分发语义,交易履约不入仓。 |

---

## 8. 自检与停审

### 8.1 自检表

| 检查项 | 结果 | 说明 |
|---|---|---|
| 是否列出本 Step 必读文档 | 通过 | 已列规范、Step 1~3、00 依赖、全局仓库拆分和后置审计输入。 |
| 是否形成系统上下文图 | 通过 | 已输出 ASCII 图,并补充图示说明。 |
| 是否形成上下游与输入/输出面表 | 通过 | 已区分核心主链和条件/候选/外围关系。 |
| 是否写明边界说明 | 通过 | 已说明主图收缩原因、非主图关系和依赖失效口径。 |
| 是否把角色或文档来源画进图 | 通过 | 图中只包含仓级对象。 |
| 是否把实现组件当成上下文对象 | 通过 | 未把 PG、对象存储、缓存、RPC、event schema 写成上下文对象。 |
| 是否提前写接口协议 | 通过 | 未写 API、DTO、event name、port、handler。 |
| 是否可进入 Step 5 | 通过 | 系统上下文已能支撑限界上下文和子域划分。 |

### 8.2 待确认事项

| 待确认事项 | 当前处理 |
|---|---|
| governance 是否成为正式化强依赖 | 当前只作为条件型治理依赖,不进入主图核心前置。 |
| artifact 是否进入核心消费 | 当前作为候选消费关系,正文和生命周期禁止入仓。 |
| marketplace 是否进入当前阶段 | 当前只保留分发语义来源,交易履约不入仓。 |
| console / SDK 是否作为 P0 管理入口 | 当前只作为入口候选,不阻塞核心闭环。 |

### 8.3 进入下一步条件

可以进入 Step 5。

进入 Step 5 前提:

- 系统上下文图已收缩到关键对象。
- 上下游与输入/输出面表已区分核心、条件、候选和外围关系。
- 未把接口、事件、实现组件、数据所有权或容器部署写入本 Step。

# Step 13. 设计风险与待确认事项

## 1. Step 状态

- 状态：[x] 已确认
- 对应 SOP：`standards/document/概要设计讨论流程_SOP.md` Step 13
- 回填章节：`projects/L3-method-library/02-概要设计.md` §13 设计风险与待确认事项
- 当前模块：Step 13 已完成,等待进入 Step 14 整理正式概要设计文档

historical material:

- 当前正式 `projects/L3-method-library/02-概要设计.md` `§13`
- 旧风险 / 待确认正文中的 `MethodContent`、publish、snapshot、fingerprint、outbox、delivery、P1 plugin / configuration 主线
- `projects/L1-governance/design-calibration/02_hld_step_13_risks_open_questions.md`

---

### R1.1 开工与必读文档:先思考

#### R1.1.1 本模块问题

本模块只回答四类问题:

1. Step 13 本轮应读取哪些正式文档、中间产物和规范。
2. 哪些旧风险 / 待确认内容只能作为 historical material,不能作为当前结论来源。
3. 本轮 Step 13 应如何区分“设计风险”“待确认事项”“任务 / backlog / 后续实施细节”。
4. 下一批 `开工与必读文档:再写入` 应写入哪些表格和门禁。

本模块不写正式风险清单,不写待确认事项清单,不回写正式 `02-概要设计.md`,不进入 Step 14。

#### R1.1.2 必读文档候选

| 文档 | 必读原因 | 当前判断 |
|---|---|---|
| `standards/document/概要设计讨论流程_SOP.md` | 固定 Step 13 的问题、产物、禁止事项和进入下一步条件。 | required |
| `standards/document/概要设计书写规范.md` | 固定正式 `§13` 的风险表、待确认表、字段语义和禁止写法。 | required |
| `standards/document/设计文档讨论中间产物规范.md` | 固定 full-restart 下旧材料后置审计、Step 内计划和三层台账纪律。 | required |
| `projects/L3-method-library/design-calibration/project_execution_ledger.md` | 确认当前恢复点、三层门禁和 Step 13 开工许可。 | required |
| `projects/L3-method-library/design-calibration/02_hld_calibration_flow.md` | 确认 Step 12 已正式回填、Step 13 当前 gate 和 Step 14 阻断条件。 | required |
| `projects/L3-method-library/02-概要设计.md` `§5`~`§12` | 提供当前已回填的概要主线和 Step 13 的稳定输入。 | required |
| `projects/L3-method-library/02-概要设计.md` `§13` | 作为旧风险 / 待确认污染样本,用于后置差异审计。 | historical_only |
| Step 5~12 中间产物 | 提供组成部分、对象、接口、处理流、状态、异常、配置和承接清单的稳定结论。 | required |
| `projects/L1-governance/design-calibration/02_hld_step_13_risks_open_questions.md` | 只参考框架深度、章节顺序和风险 / 待确认拆分方式。 | framework_only |
| `projects/L3-method-library/00-需求文档.md` 与 `01-架构设计.md` | 用于确认仍与需求 / 架构相关的风险,但不能直接复制上游风险全文。 | required |

#### R1.1.3 Step 13 当前边界

| 边界 | 本轮口径 |
|---|---|
| 第一来源 | 当前正式 `§5`~`§12`、Step 5~12 中间产物、00 / 01 当前正式结论。 |
| 旧正式 `§13` | 只作 historical material,不得直接继承旧风险表。 |
| L1-governance Step 13 | 只作结构框架参考,不得复制 governance 领域风险。 |
| 已进入 `§12` 的详细设计承接项 | 不再重复写成待确认事项。 |
| 任务 / backlog / 实施动作 | 不进入 Step 13;后续归 `07-实施计划.md` 或项目管理。 |
| schema / trait / DDL / port / key / test case | 不进入 Step 13;缺口只可作为“详细设计未闭合会阻塞实现”的风险口径。 |

#### R1.1.4 旧正式 `§13` 初判

当前正式 `§13` 仍包含旧主线:

| 旧内容 | 初判 | 原因 |
|---|---|---|
| `MethodContent` 元模型、7 类 P0 subtype、kind-specific 字段 | historical_pollution | 当前 Step 5 / Step 6 已改用方法资产定义、正式化、受控消费、追溯等对象族。 |
| publish / published / `PublishMethodContent` | historical_pollution | 当前不再以 publish 作为正式化主线。 |
| snapshot / `DefinitionSnapshot` | historical_pollution | 当前只保留 body-free ref / summary / material 承接,不恢复 snapshot 机制。 |
| fingerprint / drift | historical_pollution | 当前不把 fingerprint 作为版本或一致性主线。 |
| outbox / `OutboxEvent` / delivery | historical_pollution | 当前概要层只保留 Outbound Event candidate 边界,不写 outbox / delivery 机制。 |
| P1 plugin / configuration 反向污染 P0 | may_keep_after_rewrite | 可以转写为“外围包与方法集组织不得阻塞 core”的当前风险,但不能沿用旧 P1 plugin/configuration 语义。 |

#### R1.1.5 风险 / 待确认判定口径

| 类型 | 判定标准 | Step 13 写法 |
|---|---|---|
| 设计风险 | 已识别出会影响概要主线、组成部分、接缝、对象、处理流、状态或配置边界的风险,但当前只能保守处理。 | 写入风险表,必须包含影响和当前处理口径。 |
| 待确认事项 | 尚未形成确定结论,但会影响后续详细设计或概要主线判断。 | 写入待确认表,必须说明影响范围和当前挂起口径。 |
| 任务 / backlog | 只是开发动作、文档动作、测试补全、排期或提交拆分。 | 不写入 Step 13。 |
| 已收稳结论 | 已由 Step 4~12 收稳或进入详细设计承接清单。 | 不重新包装成待确认。 |
| 实现级细节 | schema、字段全集、函数签名、DDL、topic、worker、配置 key、测试 case。 | 不直接写;只在“设计未闭合不得由实现补口”的风险层表达。 |

#### R1.1.6 下一写入批次边界

下一批 `R1.2 开工与必读文档:再写入` 只允许写:

1. Step 13 必读文档表和读取状态。
2. Step 13 Step 内计划。
3. 当前输入基线和旧材料处理规则。
4. 本 Step 的模块推进顺序。
5. `next_allowed_action`。

下一批不得写:

1. 设计风险清单正文。
2. 待确认事项清单正文。
3. 正式 `02-概要设计.md` `§13`。
4. Step 14 装配正文。

#### R1.1.7 自检

| 检查项 | 结论 | 说明 |
|---|---|---|
| 是否只做先思考 | pass | 本模块只固定开工输入、边界和下一写入范围。 |
| 是否继承旧正式 `§13` | no | 旧正式 `§13` 只作为 historical material。 |
| 是否复制 L1-governance 领域内容 | no | 只参考框架,不复制领域语义。 |
| 是否写风险 / 待确认正文 | no | 风险表与待确认表留给后续模块。 |
| 是否修改正式 `02-概要设计.md` | no | 正式文档未修改。 |
| 是否进入 Step 14 | no | 当前仍停在 Step 13 开工。 |

next_allowed_action: 等待用户确认后进入 Step 13 `开工与必读文档:再写入`;只写 Step 13 必读文档表、Step 内计划、输入基线、旧材料处理规则、模块推进顺序和门禁,不得写风险清单正文,不得修改正式 `02-概要设计.md`,不得进入 Step 14。

### R1.2 开工与必读文档:再写入

#### R1.2.1 必读文档表

| 文档 | 必读原因 | 当前读取状态 | 本轮使用方式 |
|---|---|---|---|
| `standards/document/概要设计讨论流程_SOP.md` | 固定 Step 13 目标、输入、输出、应问问题、执行约束和完成条件。 | read | 作为 Step 13 执行边界。 |
| `standards/document/概要设计书写规范.md` | 固定正式 `§13` 风险表、待确认表、允许 / 禁止内容和正反例。 | read | 作为正式 `§13` 草稿格式和判定规则。 |
| `standards/document/设计文档讨论中间产物规范.md` | 固定 full-restart 旧材料后置审计、Step 内计划、台账和单批写入纪律。 | read | 作为中间产物结构和恢复门禁。 |
| `projects/L3-method-library/design-calibration/project_execution_ledger.md` | 确认项目级恢复点、当前 gate 和下一动作许可。 | read | 作为恢复顺序第一入口。 |
| `projects/L3-method-library/design-calibration/02_hld_calibration_flow.md` | 确认 Step 12 已完成正式回填、Step 13 当前状态和 Step 14 阻断条件。 | read | 作为文档级恢复点。 |
| `projects/L3-method-library/02-概要设计.md` `§5`~`§12` | 提供已回填的组成部分、对象、接口、处理流、状态、异常、配置和详细设计承接边界。 | read | 作为 Step 13 第一来源。 |
| `projects/L3-method-library/02-概要设计.md` `§13` | 暂存旧风险 / 待确认正文。 | read | 只作 historical material 和污染审计样本。 |
| `projects/L3-method-library/design-calibration/02_hld_step_05_components_boundary.md` | 提供主要组成部分与边界。 | read | 用于判断风险影响范围。 |
| `projects/L3-method-library/design-calibration/02_hld_step_06_key_objects.md` | 提供关键对象轮廓。 | read | 用于判断对象级风险和待确认是否已被承接。 |
| `projects/L3-method-library/design-calibration/02_hld_step_07_api_interface_skeleton.md` | 提供接口骨架和边界红线。 | read | 用于判断接口 / consumer / event / job 风险。 |
| `projects/L3-method-library/design-calibration/02_hld_step_08_processing_flows.md` | 提供关键处理流和副作用边界。 | read | 用于判断处理流误导风险。 |
| `projects/L3-method-library/design-calibration/02_hld_step_09_state_machine.md` | 提供状态 owner、状态传播和禁止迁移红线。 | read | 用于判断状态风险。 |
| `projects/L3-method-library/design-calibration/02_hld_step_10_exceptions_boundaries.md` | 提供异常族、safe failure 和边界拒绝口径。 | read | 用于判断异常 / 降级风险。 |
| `projects/L3-method-library/design-calibration/02_hld_step_11_configuration_impact.md` | 提供配置影响和禁止配置化边界。 | read | 用于判断配置绕界风险。 |
| `projects/L3-method-library/design-calibration/02_hld_step_12_detailed_design_handoff.md` | 提供详细设计承接清单、回退规则与排除项。 | read | 用于避免把已承接内容重复写成待确认。 |
| `projects/L3-method-library/00-需求文档.md` | 提供当前需求边界、功能需求、业务规则和验收口径。 | read | 用于确认仍影响概要主线的风险来源。 |
| `projects/L3-method-library/01-架构设计.md` | 提供当前架构边界、依赖方向、数据所有权和横切关注点。 | read | 用于确认架构相关但仍属于本仓概要层的风险。 |
| `projects/L1-governance/design-calibration/02_hld_step_13_risks_open_questions.md` | 提供 Step 13 框架深度和风险 / 待确认拆分方式。 | read | 只参考结构,不得复制领域语义。 |

#### R1.2.2 Step 内计划

| 序号 | 模块 | 状态 | 产出边界 | 下一动作 |
|---:|---|---|---|---|
| 1 | 开工与必读文档:先思考 / 再写入 | done | `R1.1` / `R1.2` 固定输入、计划、边界和旧材料规则。 | 进入 L1-governance 框架对齐。 |
| 2 | L1-governance 框架对齐:先思考 / 再写入 | done | `R1.3` / `R1.4` 已抽取 Step 13 的框架深度、表格分层和模块顺序。 | 进入风险 / 待确认来源池。 |
| 3 | 风险 / 待确认来源池:先思考 / 再写入 | pending | 从 Step 5~12、00 / 01 和旧 `§13` 中整理候选来源,不下最终结论。 | 进入已收稳结论排除。 |
| 4 | 已收稳结论排除与非风险过滤:先思考 / 再写入 | pending | 排除已进入 `§12`、任务、backlog、实现细节和普通优化项。 | 进入设计风险筛选。 |
| 5 | 设计风险筛选:先思考 / 再写入 | pending | 形成风险候选表,写清影响、当前处理口径和是否阻塞。 | 进入待确认事项筛选。 |
| 6 | 待确认事项筛选:先思考 / 再写入 | pending | 形成待确认候选表,写清影响范围、缺失确认和挂起口径。 | 进入旧材料差异审计。 |
| 7 | 旧材料差异审计:先思考 / 再写入 | pending | 对正式旧 `§13` 逐项裁决保留、改写、废弃或后移。 | 进入正式 `§13` 回填草稿。 |
| 8 | 正式 `§13` 回填草稿:先思考 / 再写入 | pending | 形成正式 `§13` 风险表和待确认表草稿。 | 进入自检与停审。 |
| 9 | 自检与停审:先思考 / 再写入 | pending | 检查风险 / 待确认是否可回填、是否越界、是否保留 Step 14 门禁。 | 等待正式回填记录。 |
| 10 | 正式 `§13` 回填记录:再写入 | pending | 用户确认后替换正式 `§13`,记录回填后检查并放行 Step 14。 | 进入 Step 14。 |

#### R1.2.3 输入基线

| 输入组 | 当前口径 | 使用规则 |
|---|---|---|
| 当前正式 `§5`~`§12` | 已完成本轮回填,是 Step 13 第一来源。 | 风险和待确认必须能回指这些章节或其缺口。 |
| Step 5~12 中间产物 | 已记录详细推导、审计和回填依据。 | 用于判断风险影响范围和旧污染是否已被排除。 |
| 正式旧 `§13` | 仍是旧风险 / 待确认清单。 | 只在旧材料差异审计中使用,不得直接继承。 |
| `00-需求文档.md` / `01-架构设计.md` | 当前正式需求 / 架构基线。 | 只抽取仍影响概要设计成立性的风险,不复制上游风险全文。 |
| L1-governance Step 13 | 结构参考。 | 只学习框架,不引入 governance 领域主语。 |

#### R1.2.4 旧材料处理规则

| 旧材料 | 当前处理 | 说明 |
|---|---|---|
| `MethodContent` / 7 类 P0 subtype 风险 | 废弃或改写 | 不再作为当前对象主线;若有可保留风险,必须改写为当前对象族风险。 |
| publish / published / `PublishMethodContent` 风险 | 废弃 | 当前正式化与版本主线不以 publish 表达。 |
| snapshot / `DefinitionSnapshot` / export 风险 | 废弃或后移 | 当前只保留 body-free ref / summary / material 口径。 |
| fingerprint / drift 风险 | 废弃 | 当前不以 fingerprint 作为版本或一致性基础。 |
| outbox / delivery / `OutboxEvent` 风险 | 废弃或后移 | 当前概要只保留 Outbound Event candidate,不写 outbox delivery 机制。 |
| P1 plugin / configuration 反向污染 | 改写 | 可改写为外围包与方法集组织不得阻塞 core,不得沿用旧 plugin/configuration 主线。 |
| process / governance / work / UI 等下游消费待确认 | 按当前依赖重审 | 只能保留真正影响 method-library 当前概要主线的事项。 |

#### R1.2.5 模块推进门禁

| 门禁 | 要求 |
|---|---|
| 先框架后正文 | 必须先完成 L1-governance 框架对齐,再筛风险正文。 |
| 先来源池后裁决 | 必须先列候选来源,再判断风险 / 待确认。 |
| 已承接不重复挂起 | 已进入正式 `§12` 的详细设计承接内容不得重新作为待确认。 |
| 旧污染后置审计 | 旧正式 `§13` 只能在当前候选形成后做差异审计。 |
| 正式文档后置 | 正式 `02-概要设计.md` `§13` 只能在草稿、自检和用户确认后回填。 |

#### R1.2.6 本模块停审记录

| 检查项 | 结论 | 说明 |
|---|---|---|
| 是否写出必读文档表 | pass | 已列出规范、flow、台账、正式章节、中间产物、00 / 01 和 L1-governance 框架输入。 |
| 是否写出 Step 内计划 | pass | 已固定 10 个模块,且未来模块未提前写正文。 |
| 是否明确输入基线 | pass | 当前正式 `§5`~`§12` 与 Step 5~12 中间产物为第一来源。 |
| 是否明确旧材料规则 | pass | 旧 `MethodContent` / publish / snapshot / fingerprint / outbox 主线只作历史审计。 |
| 是否写风险 / 待确认正文 | no | 当前未写风险清单或待确认清单正文。 |
| 是否修改正式文档 | no | 未修改正式 `02-概要设计.md`。 |
| 是否进入 Step 14 | no | 当前仍停在 Step 13。 |

next_allowed_action: 等待用户确认后进入 Step 13 `L1-governance 框架对齐:先思考`;只抽取 L1-governance Step 13 的框架深度、表格分层、模块顺序和可借鉴门禁,不得复制 governance 领域内容,不得写 L3-method-library 风险 / 待确认正文,不得修改正式 `02-概要设计.md`,不得进入 Step 14。

### R1.3 L1-governance 框架对齐:先思考

#### R1.3.1 本模块问题

本模块只回答三类问题:

1. `L1-governance` Step 13 的哪些结构可以作为本仓 Step 13 的框架参考。
2. 哪些 governance 领域风险、待确认事项和主语必须排除,不能进入 `L3-method-library`。
3. 下一批 `L1-governance 框架对齐:再写入` 应形成怎样的框架对齐记录。

本模块不筛选 `L3-method-library` 的具体风险,不写正式 `§13` 草稿,不修改正式 `02-概要设计.md`,不进入 Step 14。

#### R1.3.2 可借鉴框架

`L1-governance` Step 13 可借鉴的是结构,不是内容:

| governance 框架段 | 可借鉴点 | 在本仓的转译方式 |
|---|---|---|
| 本步目标 | 明确 Step 13 只收纳设计层未闭环项,不写 backlog / TODO / 实施方案。 | 保留同样边界,但主语改为 method-library 的当前 `§5`~`§12`。 |
| 本步输入 | 从 Step 4~12、00 / 01 风险和 Step 12 排除项取输入。 | 本仓以正式 `§5`~`§12`、Step 5~12 中间产物、00 / 01 为第一来源。 |
| SOP 问题回答 | 先回答五个判定问题,再写表格。 | 本仓也先做来源池和过滤,不直接生成风险表。 |
| 设计风险清单 | 风险表包含“风险 / 影响 / 当前处理口径”。 | 正式 `§13` 保持规范三列表;中间产物可先写候选和裁决依据。 |
| 设计待确认事项清单 | 待确认表包含“待确认 / 影响范围 / 当前挂起口径”。 | 正式 `§13` 保持规范三列表,不写任务或愿望。 |
| 当前设计层未闭环项说明 | 区分“不阻塞进入下一步”和“会阻塞后续实现”。 | 本仓可用同样方式说明哪些事项不阻塞 Step 14,哪些会阻塞后续 `03`~`07` 或实现。 |
| 不作为风险 / 待确认事项 | 明确普通任务、测试细节、代码目录、配置 key 不进入本步。 | 本仓需保留此过滤层,防止把实现工作包装成概要风险。 |
| 当前文档问题诊断 | 对旧正式文档的风险表达进行诊断。 | 本仓要诊断旧 `§13` 的 `MethodContent` / publish / snapshot / outbox 污染。 |
| 设计取舍 | 解释为什么不复制上游风险、不重复挂起已承接内容。 | 本仓沿用该取舍,但改成 method-library 的 00 / 01 和 Step 12 承接口径。 |
| 回填草稿与进入下一步条件 | 先规定正式 `§13` 如何引用中间产物,再给 Step 14 条件。 | 本仓后续草稿阶段再写,本模块只固定框架。 |

#### R1.3.3 必须排除的 governance 内容

以下 governance 领域内容不得进入本仓:

| governance 内容 | 排除原因 |
|---|---|
| Gate / Decision / Request 主线 | 属于 governance 仓领域主语,不能替代 method-library 的方法资产定义、正式化、受控消费和追溯主线。 |
| GovernanceDecision、Policy effective fact、shared rules、Control / AIIA / SoA conclusion | 属于治理决策和控制结论,不是 method-library 的 truth owner。 |
| Nonconformity corrective loop | 属于 governance 闭环,不能变成 method-library 的风险主线。 |
| external GRC、archive handoff、governance report health | 可作为结构案例,但具体风险不属于本仓概要第一来源。 |
| governance 的 outbox / handoff / publication 细节 | 不能回流为 method-library 的 outbox / delivery 机制。 |

#### R1.3.4 本仓 Step 13 推荐框架

后续本仓 Step 13 宜采用以下模块顺序:

1. 来源池:从正式 `§5`~`§12`、Step 5~12 中间产物、00 / 01 和旧 `§13` 收集候选。
2. 非风险过滤:剔除已进入 `§12` 的承接项、任务 / backlog、实现级细节和普通优化。
3. 风险筛选:只保留已经识别、会影响概要主线成立且当前需保守处理的问题。
4. 待确认筛选:只保留尚未形成定论、会影响后续详细设计或概要主线判断的问题。
5. 未闭环说明:区分不阻塞 Step 14 的事项与会阻塞后续详细设计 / 实现的事项。
6. 旧材料差异审计:最后再裁决旧 `§13` 中哪些废弃、改写、后移或保留。
7. 正式回填草稿:压缩成规范要求的风险表和待确认表。

#### R1.3.5 表格分层建议

| 阶段 | 表格 | 用途 | 是否进入正式 `§13` |
|---|---|---|---|
| 来源池 | 风险 / 待确认候选来源表 | 先收集,不下最终结论。 | no |
| 过滤 | 非风险 / 非待确认排除表 | 防止任务、实现细节和已承接项进入正文。 | no |
| 风险筛选 | 风险候选裁决表 | 写清影响范围、当前处理口径和是否阻塞。 | compressed |
| 待确认筛选 | 待确认候选裁决表 | 写清影响范围、缺失确认和挂起口径。 | compressed |
| 差异审计 | 旧 `§13` 差异审计表 | 裁决旧污染和可改写项。 | no |
| 回填草稿 | 正式风险表 / 待确认表 | 作为正式 `§13` 可回填正文。 | yes |

#### R1.3.6 下一写入批次边界

下一批 `R1.4 L1-governance 框架对齐:再写入` 只允许写:

1. governance 框架对齐记录。
2. 可借鉴框架表。
3. 必须排除内容表。
4. 本仓 Step 13 推荐模块顺序和表格分层。
5. 后续来源池模块的进入门禁。

下一批不得写:

1. `L3-method-library` 风险清单正文。
2. `L3-method-library` 待确认事项正文。
3. 正式 `02-概要设计.md` `§13`。
4. Step 14 装配正文。

#### R1.3.7 自检

| 检查项 | 结论 | 说明 |
|---|---|---|
| 是否只做框架对齐先思考 | pass | 本模块只抽取结构和门禁。 |
| 是否复制 governance 领域内容 | no | governance 主语已列为排除项。 |
| 是否写本仓风险正文 | no | 本仓风险筛选留给后续模块。 |
| 是否修改正式文档 | no | 未修改正式 `02-概要设计.md`。 |
| 是否进入 Step 14 | no | 当前仍停在 Step 13。 |

next_allowed_action: 等待用户确认后进入 Step 13 `L1-governance 框架对齐:再写入`;只写 governance 框架对齐记录、可借鉴框架、排除项、本仓模块顺序和表格分层,不得写 L3-method-library 风险 / 待确认正文,不得修改正式 `02-概要设计.md`,不得进入 Step 14。

### R1.4 L1-governance 框架对齐:再写入

#### R1.4.1 框架对齐记录

| 项 | 记录 |
|---|---|
| 参考对象 | `projects/L1-governance/design-calibration/02_hld_step_13_risks_open_questions.md`。 |
| 参考范围 | Step 13 的章节结构、风险 / 待确认拆分方式、过滤层、旧文档诊断、取舍和进入下一步条件。 |
| 不参考范围 | governance 领域主语、治理风险正文、待确认事项正文、产品 / GRC / policy / decision 语义。 |
| 本仓转译目标 | 为 `L3-method-library` Step 13 固定“先来源池、再过滤、再风险 / 待确认裁决、最后旧材料审计和回填草稿”的执行顺序。 |
| 当前写入边界 | 本模块只写框架对齐记录,不写本仓风险清单正文,不修改正式 `02-概要设计.md`。 |

#### R1.4.2 可借鉴框架表

| governance 框架做法 | 本仓采用方式 | 使用位置 |
|---|---|---|
| 先明确“本步只收纳设计层未闭环项” | 采用 | Step 13 总边界和后续正式 `§13` 开头说明。 |
| 先列输入,再回答 SOP 问题 | 采用 | 来源池模块先读取 Step 5~12、00 / 01、旧 `§13`。 |
| 风险和待确认拆成两张表 | 采用 | 正式 `§13` 只保留风险表和待确认表两类主表。 |
| 说明“不阻塞下一步”和“会阻塞后续实现”的不同层级 | 采用 | 未闭环项说明模块。 |
| 单独列“不作为风险或待确认事项”的内容 | 采用 | 非风险过滤模块。 |
| 对旧正式文档做问题诊断 | 采用 | 旧材料差异审计模块。 |
| 用设计取舍解释为什么不复制上游风险和不重复挂起已承接内容 | 采用 | 自检与停审前的取舍说明。 |
| 回填草稿只摘正式正文,过程表留在中间产物 | 采用 | 正式 `§13` 回填草稿模块。 |

#### R1.4.3 必须排除的 governance 内容

| governance 内容 | 本仓处理 |
|---|---|
| Gate / Decision / Request | 不进入本仓风险正文;本仓不以治理决策主线组织风险。 |
| GovernanceDecision / Policy effective fact / shared rules | 不进入本仓风险正文;只可作为相邻仓消费或引用边界背景。 |
| Control / AIIA / SoA conclusion | 不进入本仓风险正文;本仓只管理方法资产相关定义、正式化、消费和追溯。 |
| Nonconformity corrective loop | 不进入本仓风险正文;不转写为 method-library 的闭环主线。 |
| external GRC / archive handoff / governance report health | 不进入本仓风险正文;若后续涉及 external summary / artifact ref,必须按本仓当前对象族重审。 |
| governance outbox / handoff / publication | 不进入本仓机制;不得恢复旧 outbox / delivery 主线。 |

#### R1.4.4 本仓 Step 13 模块顺序

| 顺序 | 模块 | 目标 | 关键门禁 |
|---:|---|---|---|
| 1 | 风险 / 待确认来源池 | 收集候选来源,不做最终裁决。 | 来源必须能回指当前正式 `§5`~`§12`、Step 5~12、00 / 01 或旧 `§13`。 |
| 2 | 已收稳结论排除与非风险过滤 | 排除已承接、任务、实现细节和普通优化。 | 已进入正式 `§12` 的内容不得重复挂起。 |
| 3 | 设计风险筛选 | 从候选中筛出已识别且会影响概要主线的风险。 | 每项必须有影响范围和当前处理口径。 |
| 4 | 待确认事项筛选 | 从候选中筛出尚未形成定论但影响后续判断的问题。 | 每项必须有影响范围、缺失确认和挂起口径。 |
| 5 | 未闭环项说明 | 区分不阻塞 Step 14 与会阻塞后续设计 / 实现。 | 不写实施方案。 |
| 6 | 旧材料差异审计 | 裁决旧正式 `§13` 的保留、改写、废弃或后移。 | 旧污染不得作为第一来源。 |
| 7 | 正式 `§13` 回填草稿 | 压缩为正式风险表和待确认表。 | 不搬运过程表和审计表。 |
| 8 | 自检与正式回填记录 | 检查可回填性并等待用户确认回填。 | 未正式回填前不得进入 Step 14。 |

#### R1.4.5 表格分层

| 表格层 | 用途 | 正式 `§13` 处理 |
|---|---|---|
| 来源池表 | 保留候选来源、输入依据和初步类型。 | 不进入正式文档。 |
| 排除表 | 说明哪些内容不是风险 / 待确认。 | 不进入正式文档,只在需要时摘要为说明。 |
| 风险候选裁决表 | 记录风险候选、影响、当前处理口径、是否阻塞和裁决。 | 压缩进入正式风险表。 |
| 待确认候选裁决表 | 记录待确认候选、缺失确认、挂起口径和裁决。 | 压缩进入正式待确认表。 |
| 旧材料审计表 | 说明旧 `§13` 的保留 / 改写 / 废弃 / 后移。 | 不进入正式文档。 |
| 正式回填表 | 只保留规范要求的风险表和待确认表。 | 进入正式 `§13`。 |

#### R1.4.6 后续来源池入口

下一模块 `风险 / 待确认来源池:先思考` 需要先建立候选来源范围:

1. 正式 `§5`~`§12` 中已经写明但仍可能误导后续详细设计的边界。
2. Step 5~12 中间产物中的 blocker、排除项、后续风险保留和回退规则。
3. `00-需求文档.md` / `01-架构设计.md` 中仍影响当前概要主线的风险或待确认事项。
4. 旧正式 `§13` 中可作为污染样本或可改写样本的内容。

来源池阶段只允许收集候选,不得直接裁决风险表正文。

#### R1.4.7 本模块停审记录

| 检查项 | 结论 | 说明 |
|---|---|---|
| 是否完成 governance 框架对齐记录 | pass | 已写明参考范围、不参考范围和本仓转译目标。 |
| 是否明确可借鉴框架 | pass | 已保留目标、输入、拆表、过滤、诊断、取舍和回填草稿结构。 |
| 是否排除 governance 领域内容 | pass | 已明确 Gate / Decision / Policy / Control / GRC 等内容不得进入本仓风险正文。 |
| 是否写出本仓模块顺序和表格分层 | pass | 已固定来源池、过滤、风险筛选、待确认筛选、未闭环说明、旧材料审计和回填草稿顺序。 |
| 是否写本仓风险 / 待确认正文 | no | 当前只做框架对齐。 |
| 是否修改正式 `02-概要设计.md` | no | 未修改正式文档。 |
| 是否进入 Step 14 | no | 当前仍停在 Step 13。 |

next_allowed_action: 等待用户确认后进入 Step 13 `风险 / 待确认来源池:先思考`;只建立候选来源范围、来源分类和后续写入边界,不得裁决最终风险 / 待确认清单,不得修改正式 `02-概要设计.md`,不得进入 Step 14。

### R1.5 风险 / 待确认来源池:先思考

#### R1.5.1 本模块问题

本模块只回答四类问题:

1. 来源池阶段应该扫描哪些输入范围。
2. 每类输入只允许提取什么候选信息。
3. 候选来源如何先做初步分类,避免提前裁决为风险或待确认事项。
4. 下一批 `风险 / 待确认来源池:再写入` 应写入哪些来源池表格和边界。

本模块不裁决最终风险清单,不裁决最终待确认事项,不修改正式 `02-概要设计.md`,不进入 Step 14。

#### R1.5.2 候选来源范围

来源池必须覆盖四类输入,但每类输入的使用方式不同:

| 来源组 | 读取范围 | 只允许提取什么 |
|---|---|---|
| 当前正式概要主线 | 正式 `§5`~`§12` | 已写明的边界、红线、回退规则和可能误导后续详细设计的保守口径。 |
| Step 5~12 中间产物 | `02_hld_step_05_*` ~ `02_hld_step_12_*` | blocker 记录、排除项、后续风险保留、详细设计承接和旧污染审计入口。 |
| 当前 00 / 01 基线 | `00-需求文档.md`;`01-架构设计.md` | 仍会影响当前概要主线成立性的范围、依赖、数据所有权和风险背景。 |
| 历史污染样本 | 旧正式 `§13`;旧 `MethodContent` / publish / snapshot / fingerprint / outbox / P1 plugin 主线 | 只作为污染或可改写样本,不得直接继承为当前风险。 |

#### R1.5.3 来源分类方法

来源池先按“来源类型”分类,不提前写成最终结论:

| 来源类型 | 判定线索 | 后续可能进入 |
|---|---|---|
| 当前边界红线 | Definition vs Use、core / support / operation / peripheral、body-free、Query no-write、Job 不修 truth、外围不阻塞 core。 | 非风险过滤或设计风险筛选。 |
| 详细设计承接紧张点 | `§12` 已要求 03 / 04 继续展开,但若被实现端提前补口会打穿概要边界。 | 已收稳排除或设计风险筛选。 |
| 外部 / 下游依赖不确定 | 外部摘要、治理依据、artifact / archive ref、下游消费影响摘要、marketplace context 等只允许 summary/ref。 | 待确认筛选或设计风险筛选。 |
| 配置绕界可能性 | `§11` 禁止配置化边界、adapter / job / consumer / publisher 装配点。 | 设计风险筛选。 |
| 旧材料污染 | `MethodContent`、publish、snapshot、fingerprint、outbox、delivery、旧 P1 plugin / configuration。 | 旧材料差异审计。 |
| 纯任务或实现细节 | schema、trait、DDL、topic、worker、test case、commit boundary、文档润色、开发任务。 | 非风险过滤。 |

#### R1.5.4 当前已知必须进入来源池的线索族

本阶段只标记“需要进入来源池”,不判断最终去留:

| 线索族 | 来源依据 | 进入来源池原因 |
|---|---|---|
| 旧 `§13` 风险表整表 | 正式 `§13` | 当前仍含旧 `MethodContent`、publish、snapshot、fingerprint、outbox 和 P1 plugin / configuration 主线,必须作为污染审计样本。 |
| `§12` 回退规则 | 正式 `§12.3`;Step 12 `R1.23.3` | 后续风险讨论不得削弱回退规则,也不得重新分配 `03 / 04` 职责。 |
| core truth / read material / peripheral 分层边界 | 正式 `§5`~`§8`;Step 5~8 中间产物 | 若后续详细设计误把 view、material、operation 或 peripheral 写成 truth,会误导主线。 |
| 状态 owner 与非法迁移红线 | 正式 `§9`;Step 9 中间产物 | 状态语义不得恢复旧 lifecycle / outbox status,也不得被配置或 job 改写。 |
| 异常传播和 safe failure 红线 | 正式 `§10`;Step 10 中间产物 | Query / Job / Consumer / Publisher / external failure 的传播边界需要作为风险筛选输入。 |
| 配置影响和禁止配置化边界 | 正式 `§11`;Step 11 中间产物 | 配置只能影响装配和运行承载,不能改变 domain invariant、状态机或 truth owner。 |
| 00 / 01 的 P0 / P1 与依赖裁剪背景 | `00-需求文档.md`;`01-架构设计.md` | 外围增强、下游协作和依赖方向需要与当前概要主线保持一致。 |

#### R1.5.5 下一写入批次边界

下一批 `R1.6 风险 / 待确认来源池:再写入` 只允许写:

1. 来源池总表,记录来源组、输入依据、候选线索和初步分类。
2. 候选线索族表,把正式 `§5`~`§12`、Step 5~12、00 / 01 和旧 `§13` 的来源入口列清楚。
3. 来源池使用规则,说明后续如何进入“已收稳结论排除与非风险过滤”。
4. 本模块停审记录和 `next_allowed_action`。

下一批不得写:

1. 最终设计风险表。
2. 最终待确认事项表。
3. 旧 `§13` 条目的最终保留 / 废弃裁决。
4. 正式 `02-概要设计.md` `§13`。

#### R1.5.6 自检

| 检查项 | 结论 | 说明 |
|---|---|---|
| 是否只做来源池先思考 | pass | 当前只定义来源范围、分类方法和下一批写入边界。 |
| 是否裁决最终风险 / 待确认 | no | 风险与待确认筛选留给后续模块。 |
| 是否直接继承旧 `§13` | no | 旧 `§13` 只进入污染样本和差异审计。 |
| 是否修改正式文档 | no | 未修改正式 `02-概要设计.md`。 |
| 是否进入 Step 14 | no | 当前仍停在 Step 13。 |

next_allowed_action: 等待用户确认后进入 Step 13 `风险 / 待确认来源池:再写入`;只写来源池总表、候选线索族、来源池使用规则和停审记录,不得裁决最终风险 / 待确认清单,不得修改正式 `02-概要设计.md`,不得进入 Step 14。

### R1.6 风险 / 待确认来源池:再写入

#### R1.6.1 来源池总表

本表只登记候选来源和初步类型,不裁决最终风险 / 待确认事项。

| 来源组 | 输入依据 | 候选线索 | 初步分类 | 后续处理 |
|---|---|---|---|---|
| 正式 `§5` 主要组成部分 | 当前正式 `§5`;Step 5 中间产物 | 8 个组成部分、core / support / operation / peripheral 分层、旧 `MethodContent` / publish / snapshot / fingerprint / outbox 禁入。 | 当前边界红线;旧材料污染 | 先进入非风险过滤,再判断是否仍构成设计风险。 |
| 正式 `§6` 关键对象 | 当前正式 `§6`;Step 6 主控和对象附录 | truth、summary、view/material、typed ref、history、operation、peripheral 的对象分层。 | 当前边界红线;详细设计承接紧张点 | 已收稳对象不重复挂起;只保留对象 owner 被误改的风险线索。 |
| 正式 `§7` 接口骨架 | 当前正式 `§7`;Step 7 中间产物 | Command / Query / Inbound Consumer / Outbound Event / Operations Job 的接口族边界。 | 当前边界红线;配置 / transport 绕界可能性 | 过滤普通 contract 展开项,保留 Query no-write、Consumer / Job 不写 core truth 等线索。 |
| 正式 `§8` 处理流 | 当前正式 `§8`;Step 8 中间产物 | 定义、正式化、消费、追溯、关系分发、外部摘要、维护、外围组织处理流。 | 当前边界红线;外部 / 下游依赖不确定 | 过滤已承接 flow 细节,保留 flow 类型被翻转或旧 publish 主线回流线索。 |
| 正式 `§9` 状态 | 当前正式 `§9`;Step 9 中间产物 | 状态 owner、允许 / 禁止迁移、状态传播和旧 lifecycle / outbox status 禁入。 | 当前边界红线 | 过滤状态矩阵展开任务,保留状态 owner 被配置、job 或旧机制改写线索。 |
| 正式 `§10` 异常边界 | 当前正式 `§10`;Step 10 中间产物 | Query、Job、Consumer、Publisher、external、peripheral 的 safe failure 与传播红线。 | 当前边界红线;详细设计承接紧张点 | 过滤错误枚举细节,保留异常传播打穿 truth / body-free 边界的线索。 |
| 正式 `§11` 配置影响 | 当前正式 `§11`;Step 11 中间产物 | 配置影响轮廓、禁止配置化边界、03 / 04 分工。 | 配置绕界可能性 | 保留“配置不得改变 invariant / state / truth owner / P0/P1”的风险线索。 |
| 正式 `§12` 承接清单 | 当前正式 `§12`;Step 12 `R1.23` | 详细设计展开方向、回退规则与排除项、旧污染禁入、03 / 04 分工。 | 详细设计承接紧张点 | 先排除已承接项;只保留回退规则被削弱或实现端私补口径线索。 |
| 当前 00 / 01 基线 | `00-需求文档.md`;`01-架构设计.md` | P0 / P1 范围、依赖裁剪、数据所有权、外部正文边界、下游协作边界。 | 外部 / 下游依赖不确定 | 只抽取仍影响当前概要主线的背景,不复制上游风险全文。 |
| 旧正式 `§13` | 当前正式 `§13` | Definition / Use、P1 plugin、event / snapshot / fingerprint、published outbox、ViewProfile、7 类 P0 kind、process / governance 消费等旧条目。 | 旧材料污染 | 后续旧材料差异审计逐项裁决废弃、改写、后移或保留。 |

#### R1.6.2 候选线索族

| 线索族 | 来源入口 | 当前来源池记录 | 禁止提前做什么 |
|---|---|---|---|
| 旧对象 / 旧生命周期污染 | 正式 `§5`~`§7`;旧 `§13` | `MethodContent`、7 类 subtype、draft / review / published、`DefinitionSnapshot`、fingerprint、`OutboxEvent` 只作为污染样本。 | 不得直接转成正式风险表条目;必须等旧材料审计。 |
| core truth 被 view/material/operation/peripheral 替代 | 正式 `§5`~`§8`;Step 5~8 | view、read material、maintenance progress、package / method set 不得成为 core truth。 | 不得把已收稳的分层结论重复挂起。 |
| 外部正文和下游运行 truth 入仓 | 正式 `§5`~`§11`;00 / 01 | external summary/ref、artifact/archive ref、governance basis ref、consumption impact summary 必须 body-free。 | 不得写 schema、payload、文件格式或 adapter 细节。 |
| Query / Consumer / Job / Publisher 边界被翻转 | 正式 `§7`~`§10`;Step 7~10 | Query no-write、Consumer 不生成 core truth、Job 不修 truth、publisher failure 不回滚 truth。 | 不得提前定义 topic、worker、retry、receipt 或 test case。 |
| 配置绕过领域规则 | 正式 `§11`;Step 11 | 配置不得改写 Definition vs Use、truth owner、状态机、审计链、事务一致性和 P0 / P1。 | 不得写具体 config key、默认值、环境变量或 profile 实现。 |
| 详细设计承接被误读为待确认 | 正式 `§12`;Step 12 | 字段、DTO、port、repository、状态矩阵、config contract 已进入 03 / 04 展开。 | 不得在 Step 13 重复挂成待确认。 |
| 上游风险直接搬运 | 00 / 01 | 只保留仍影响当前概要主线成立性的范围和依赖背景。 | 不得复制需求 / 架构风险全文。 |

#### R1.6.3 来源池使用规则

| 规则 | 说明 |
|---|---|
| source_pool_only | 上述条目只是候选来源,不是正式风险或待确认事项。 |
| 先排除再裁决 | 下一模块必须先排除已收稳结论、已进入 `§12` 的承接项、任务、backlog 和实现细节。 |
| 旧材料后置 | 旧正式 `§13` 只能在当前来源池和过滤完成后进入差异审计。 |
| 已承接不重复挂起 | 字段、DTO、状态矩阵、port、repository、config key、测试用例等已由 `§12` 后移的内容不得重新挂起。 |
| 概要层粒度 | Step 13 只表达设计风险、待确认和当前处理 / 挂起口径,不写解决方案或实施动作。 |

#### R1.6.4 本模块停审记录

| 检查项 | 结论 | 说明 |
|---|---|---|
| 是否写出来源池总表 | pass | 已覆盖正式 `§5`~`§12`、Step 5~12、00 / 01 和旧 `§13`。 |
| 是否写出候选线索族 | pass | 已按旧污染、truth 分层、外部正文、接口边界、配置绕界和承接误读分组。 |
| 是否裁决最终风险 / 待确认 | no | 当前只登记来源和初步分类。 |
| 是否直接继承旧 `§13` | no | 旧 `§13` 只作为污染样本,后续再审计。 |
| 是否修改正式文档 | no | 未修改正式 `02-概要设计.md`。 |
| 是否进入 Step 14 | no | 当前仍停在 Step 13。 |

next_allowed_action: 等待用户确认后进入 Step 13 `已收稳结论排除与非风险过滤:先思考`;只定义排除标准和过滤范围,不得裁决最终风险 / 待确认清单,不得修改正式 `02-概要设计.md`,不得进入 Step 14。

### R1.7 已收稳结论排除与非风险过滤:先思考

#### R1.7.1 本模块问题

本模块只回答四类问题:

1. 来源池中的哪些候选应先排除,不得进入风险 / 待确认筛选。
2. 哪些候选只是已收稳结论或已进入 `§12` 的详细设计承接项。
3. 哪些候选属于任务、backlog、实现细节、测试细节、配置细节或旧污染样本。
4. 下一批 `已收稳结论排除与非风险过滤:再写入` 应形成怎样的排除表。

本模块不裁决最终风险表,不裁决最终待确认表,不修改正式 `02-概要设计.md`,不进入 Step 14。

#### R1.7.2 排除标准

| 排除类型 | 判定标准 | 后续归属 |
|---|---|---|
| 已收稳概要结论 | 已在正式 `§5`~`§12` 中形成稳定边界、对象、接口、流程、状态、异常、配置或承接规则。 | 不进入 Step 13;作为后续风险筛选的对照基线。 |
| 已进入 `§12` 承接项 | 字段、DTO、port、repository、状态矩阵、config contract、error mapping、test evidence 等已明确交给 `03 / 04 / 05 / 07` 展开。 | 不重复挂为待确认;按 `§12` 承接。 |
| 纯任务 / backlog | 文档润色、开发任务、提交拆分、排期、报告样式、CI、证据路径、代码生成。 | 后移到实施计划、测试计划或项目管理。 |
| 实现级细节 | schema、trait、DDL、topic、worker、retry、queue、配置 key、默认值、环境变量、测试 case。 | 交给 `03`~`07`;概要层只保留必要边界风险。 |
| 上游风险直接搬运 | 仅存在于 00 / 01,但不再影响当前概要 `§5`~`§12` 主线成立性。 | 不进入 Step 13;必要时只作背景。 |
| 旧污染样本 | 旧 `MethodContent`、publish、snapshot、fingerprint、outbox、delivery、旧 P1 plugin / configuration。 | 不在本模块裁决;后续进入旧材料差异审计。 |
| 普通优化项 | “更好”“更完整”“后续优化”但不影响概要主线判断。 | 不进入 Step 13。 |

#### R1.7.3 不能排除的候选边界

以下候选不能在过滤阶段直接丢弃,必须留给后续风险或待确认筛选:

| 候选边界 | 保留原因 | 后续进入 |
|---|---|---|
| 会误导 03 / 04 自行补对象、接口、状态或配置口径的缺口 | 可能导致实现端私补 schema / port / state / config key。 | 设计风险筛选。 |
| 会削弱 `§12` 回退规则或重分配 `03 / 04` 职责的线索 | 可能推翻当前承接边界。 | 设计风险筛选。 |
| 外部 / 下游依赖是否进入当前概要主线仍缺确认的事项 | 当前不能写成已知风险,但可能影响详细设计展开。 | 待确认事项筛选。 |
| 配置是否可能绕过 domain invariant、truth owner 或状态机的线索 | 若不保留,后续配置设计可能误读为可配置。 | 设计风险筛选。 |
| 旧 `§13` 中可能可改写为当前主线风险的条目 | 需要在旧材料差异审计后决定。 | 旧材料差异审计;必要时进入风险筛选。 |

#### R1.7.4 过滤顺序

后续 `再写入` 应按以下顺序过滤:

1. 先排除已进入 `§12` 的详细设计承接项,避免重复挂起。
2. 再排除任务、backlog、实现细节、测试细节和配置细节。
3. 再排除已由正式 `§5`~`§12` 收稳的稳定结论。
4. 再标记旧污染样本,但不在本模块裁决保留 / 废弃。
5. 最后把剩余候选交给风险筛选或待确认筛选。

#### R1.7.5 下一写入批次边界

下一批 `R1.8 已收稳结论排除与非风险过滤:再写入` 只允许写:

1. 排除标准表。
2. 来源池候选过滤表。
3. 暂不排除候选表。
4. 过滤后进入后续模块的入口说明。
5. 本模块停审记录和 `next_allowed_action`。

下一批不得写:

1. 最终设计风险表。
2. 最终待确认事项表。
3. 旧材料最终差异审计结论。
4. 正式 `02-概要设计.md` `§13`。

#### R1.7.6 自检

| 检查项 | 结论 | 说明 |
|---|---|---|
| 是否只做过滤先思考 | pass | 当前只定义排除标准、不可排除边界和下一写入范围。 |
| 是否裁决最终风险 / 待确认 | no | 仍未裁决最终风险表或待确认表。 |
| 是否提前裁决旧材料 | no | 旧污染样本留给后续差异审计。 |
| 是否修改正式文档 | no | 未修改正式 `02-概要设计.md`。 |
| 是否进入 Step 14 | no | 当前仍停在 Step 13。 |

next_allowed_action: 等待用户确认后进入 Step 13 `已收稳结论排除与非风险过滤:再写入`;只写排除标准表、来源池候选过滤表、暂不排除候选表和后续入口说明,不得裁决最终风险 / 待确认清单,不得修改正式 `02-概要设计.md`,不得进入 Step 14。

### R1.8 已收稳结论排除与非风险过滤:再写入

#### R1.8.1 排除标准表

| 排除标准 | 排除判断 | 后续归属 |
|---|---|---|
| 已由正式 `§5`~`§12` 收稳 | 只是在重复当前组成部分、对象、接口、处理流、状态、异常、配置或承接结论。 | 不进入 Step 13 正文。 |
| 已进入 `§12` 承接清单 | 需要由 `03 / 04 / 05 / 07` 展开,但不改变概要主线。 | 按 `§12` 承接,不得重复挂起。 |
| 纯任务 / backlog | 文档、开发、测试、提交、CI、报告、证据路径或项目管理任务。 | 后移到对应后续文档或项目管理。 |
| 实现级细节 | schema、trait、DTO 字段全集、DDL、topic、worker、retry、配置 key、测试 case。 | 交给 `03`~`07`。 |
| 上游风险直接搬运 | 只属于 00 / 01,未影响当前 `§5`~`§12` 主线。 | 不进入本步;必要时只作背景。 |
| 旧污染样本 | 旧 `MethodContent`、publish、snapshot、fingerprint、outbox、delivery 和旧 P1 plugin / configuration。 | 不在过滤模块裁决;后续旧材料差异审计。 |

#### R1.8.2 来源池候选过滤表

| 来源池候选 | 当前过滤判断 | 理由 | 后续入口 |
|---|---|---|---|
| 正式 `§5` 的 8 个组成部分与分层 | 排除已收稳部分 | 组成部分、职责与边界已正式回填,不能重复挂成风险。 | 仅保留“分层被误改”线索进入风险筛选。 |
| 正式 `§6` 的关键对象分布 | 排除已收稳部分 | 对象家族、owner 和禁入对象已收稳;字段展开已交给 `§12`。 | 仅保留“对象 owner 被详细设计私补”线索进入风险筛选。 |
| 正式 `§7` 的接口分类 | 排除普通 contract 展开项 | request / response / port / adapter 等细节已属 03 承接。 | 保留 Query no-write、Consumer / Job 不写 core truth 等边界线索。 |
| 正式 `§8` 的处理流家族 | 排除已收稳 flow 主体 | flow 主体已回填;service 编排和事务边界交给 03。 | 保留 flow 类型被翻转、旧 publish 主线回流线索。 |
| 正式 `§9` 的状态 owner / 状态传播 | 排除状态矩阵展开任务 | 状态 owner 和红线已收稳;矩阵细节交给 03。 | 保留状态 owner 被配置、job 或旧机制改写线索。 |
| 正式 `§10` 的异常分类 | 排除错误枚举细节 | 错误 enum、映射、测试 case 属 03 / 05。 | 保留 safe failure 打穿 truth / body-free 边界线索。 |
| 正式 `§11` 配置影响轮廓 | 排除具体配置项 | key、默认值、环境变量、profile 实现属 04。 | 保留配置绕过 invariant / truth owner / 状态机线索。 |
| 正式 `§12` 承接清单 | 排除已承接展开项 | 字段、DTO、port、repository、config contract、test evidence 已明确后移。 | 保留 `§12` 回退规则被削弱或 `03 / 04` 职责被重分配线索。 |
| 当前 00 / 01 背景 | 排除上游风险全文搬运 | Step 13 不复制需求 / 架构风险全文。 | 仅保留影响当前概要主线的依赖 / 数据所有权 / P0/P1 背景。 |
| 旧正式 `§13` 条目 | 暂不在本模块裁决 | 旧条目需要后续差异审计逐项处理。 | 进入旧材料差异审计;可改写项再回流风险筛选。 |

#### R1.8.3 暂不排除候选表

| 暂不排除候选 | 保留理由 | 后续处理 |
|---|---|---|
| `§12` 回退规则被后续讨论削弱 | 会影响概要主线与详细设计承接边界。 | 设计风险筛选。 |
| `03 / 04` 职责被 Step 13 重新分配 | 会打破 `§12` 已固定的详细设计 / 配置设计分工。 | 设计风险筛选。 |
| 详细设计未闭合时实现端私补对象、接口、状态或配置口径 | 会导致 schema / port / state / config key 由实现端发明。 | 设计风险筛选。 |
| Query / Consumer / Job / Publisher 边界被翻转 | 会让读取、消费、维护或发布路径改写 core truth。 | 设计风险筛选。 |
| 配置绕过 domain invariant、truth owner 或状态机 | 会让配置成为业务规则后门。 | 设计风险筛选。 |
| 外部 / 下游依赖是否进入当前概要主线仍缺确认 | 可能影响 03 的 contract 展开,但当前未必是已知风险。 | 待确认事项筛选。 |
| 旧 `§13` 中可能可改写为当前主线风险的条目 | 需要先做旧材料差异审计。 | 旧材料差异审计后再决定是否进入风险表。 |

#### R1.8.4 过滤后入口说明

后续只从 `R1.8.3` 暂不排除候选继续筛选。已排除内容不得在后续模块中改名为风险或待确认事项。

处理顺序固定为:

1. `设计风险筛选:先思考 / 再写入`:只处理已经识别、会影响概要主线成立且需要保守处理的风险候选。
2. `待确认事项筛选:先思考 / 再写入`:只处理尚未形成定论、但会影响后续详细设计或概要判断的问题。
3. `旧材料差异审计:先思考 / 再写入`:最后裁决旧正式 `§13` 的保留、改写、废弃或后移。

#### R1.8.5 本模块停审记录

| 检查项 | 结论 | 说明 |
|---|---|---|
| 是否写出排除标准表 | pass | 已区分已收稳结论、承接项、任务、实现细节、上游风险和旧污染样本。 |
| 是否写出来源池候选过滤表 | pass | 已对 R1.6 来源池候选逐组过滤。 |
| 是否写出暂不排除候选表 | pass | 已保留后续风险筛选、待确认筛选和旧材料审计入口。 |
| 是否裁决最终风险 / 待确认 | no | 当前只做过滤,未生成正式风险表或待确认表。 |
| 是否修改正式文档 | no | 未修改正式 `02-概要设计.md`。 |
| 是否进入 Step 14 | no | 当前仍停在 Step 13。 |

next_allowed_action: 等待用户确认后进入 Step 13 `设计风险筛选:先思考`;只定义风险候选判定口径、风险候选范围和下一批风险筛选写入边界,不得写最终正式 `§13`,不得修改正式 `02-概要设计.md`,不得进入 Step 14。

### R1.9 设计风险筛选:先思考

#### R1.9.1 本模块问题

本模块只回答四类问题:

1. `R1.8.3` 暂不排除候选中哪些具备“设计风险”判定条件。
2. 哪些候选只能留给待确认事项筛选或旧材料差异审计,不能提前升级成风险。
3. 风险候选应如何写影响范围、当前处理口径和阻塞性。
4. 下一批 `设计风险筛选:再写入` 应形成怎样的风险候选裁决表。

本模块不写正式 `§13` 风险表,不写待确认事项表,不修改正式 `02-概要设计.md`,不进入 Step 14。

#### R1.9.2 风险判定口径

候选只有同时满足以下条件,才能进入风险候选裁决表:

| 判定条件 | 说明 |
|---|---|
| 已识别 | 问题已经由当前 `§5`~`§12`、Step 5~12 或 R1.8 暂不排除候选明确暴露。 |
| 影响概要主线 | 会影响组成部分边界、对象 owner、接口分类、处理流类型、状态 owner、异常传播、配置禁区或 `§12` 承接边界。 |
| 尚未关闭 | 当前不能在 Step 13 内直接闭合,只能保守约束或继续标红。 |
| 有当前处理口径 | 能写出“当前按什么边界保守处理”,而不是空泛写“后续优化”。 |
| 不是纯待确认 | 若关键事实仍未形成定论,且不能证明已经构成风险,应进入待确认事项筛选。 |

#### R1.9.3 风险候选范围

| 风险候选 | 来源 | 风险化理由 | 当前方向 |
|---|---|---|---|
| `§12` 回退规则被后续讨论削弱 | R1.8.3;正式 `§12.3`;Step 12 `R1.23.3` | 若后续风险讨论反向改写回退位置,会让 03 直接暗改概要主语。 | 风险候选。 |
| `03 / 04` 职责被 Step 13 重新分配 | R1.8.3;正式 `§12.1`~`§12.3` | 会破坏详细设计与配置设计分工,造成配置或 contract 由错误文档承接。 | 风险候选。 |
| 详细设计未闭合时实现端私补对象、接口、状态或配置口径 | R1.8.3;Step 12 承接;概要规范 | 会导致实现侧发明 schema / port / state / config key,破坏设计真相源。 | 风险候选。 |
| Query / Consumer / Job / Publisher 边界被翻转 | R1.8.3;正式 `§7`~`§10` | 会把读取、消费、维护或发布路径变成 core truth 写源或回滚源。 | 风险候选。 |
| 配置绕过 domain invariant、truth owner 或状态机 | R1.8.3;正式 `§11` | 会让配置成为业务规则后门,推翻 Definition vs Use、P0/P1 和状态红线。 | 风险候选。 |

#### R1.9.4 不应提前升级为风险的候选

| 候选 | 不升级原因 | 后续入口 |
|---|---|---|
| 外部 / 下游依赖是否进入当前概要主线仍缺确认 | 当前缺确认,还不能证明已经构成已识别风险。 | 待确认事项筛选。 |
| 旧 `§13` 中可能可改写为当前主线风险的条目 | 必须先完成旧材料差异审计,不能直接继承旧风险。 | 旧材料差异审计。 |
| 已被 `§12` 后移的字段、DTO、port、repository、状态矩阵和 config contract | 已明确是详细设计 / 配置设计承接项。 | 排除,不得回流。 |

#### R1.9.5 风险表写法边界

下一批风险候选裁决表可以多于正式风险表,但必须保持以下字段:

| 字段 | 写法 |
|---|---|
| 风险候选 | 写已识别且会影响概要主线的风险,不用旧 `§13` 旧主语命名。 |
| 影响范围 | 指向 `§5`~`§12` 的主线、组成部分、对象、接口、处理流、状态、异常、配置或承接边界。 |
| 当前处理口径 | 写当前保守约束,不写实施方案。 |
| 是否阻塞 | 只判断是否阻塞 Step 14、03/04 或实现,不写排期。 |
| 裁决 | 仅标记 `进入风险表候选` 或 `后移 / 排除`,正式回填还要等待后续草稿模块。 |

#### R1.9.6 下一写入批次边界

下一批 `R1.10 设计风险筛选:再写入` 只允许写:

1. 风险候选裁决表。
2. 不升级为风险的候选表。
3. 风险筛选后的后续入口说明。
4. 本模块停审记录和 `next_allowed_action`。

下一批不得写:

1. 正式 `§13` 最终风险表。
2. 待确认事项最终表。
3. 旧材料差异审计最终结论。
4. 正式 `02-概要设计.md`。

#### R1.9.7 自检

| 检查项 | 结论 | 说明 |
|---|---|---|
| 是否只做风险筛选先思考 | pass | 当前只定义判定口径、候选范围和下一写入边界。 |
| 是否写正式风险表 | no | 正式 `§13` 风险表留给回填草稿。 |
| 是否把待确认提前升级为风险 | no | 外部 / 下游依赖缺确认事项留给待确认筛选。 |
| 是否提前裁决旧材料 | no | 旧 `§13` 条目留给旧材料差异审计。 |
| 是否修改正式文档 | no | 未修改正式 `02-概要设计.md`。 |
| 是否进入 Step 14 | no | 当前仍停在 Step 13。 |

next_allowed_action: 等待用户确认后进入 Step 13 `设计风险筛选:再写入`;只写风险候选裁决表、不升级为风险的候选表和风险筛选后续入口说明,不得写最终正式 `§13`,不得修改正式 `02-概要设计.md`,不得进入 Step 14。

### R1.10 设计风险筛选:再写入

#### R1.10.1 风险候选裁决表

| 风险候选 | 影响范围 | 当前处理口径 | 是否阻塞 | 裁决 |
|---|---|---|---|---|
| `§12` 回退规则被后续讨论削弱 | `§12` 承接边界、`03-详细设计.md` 回退入口、Step 5~11 已收稳主语 | 当前保留 `§12.3` 回退规则为硬边界;后续若需要改主语、对象 owner、接口族或流程类型,必须回退对应概要 Step,不得在 03 暗改。 | 不阻塞 Step 14;会阻塞 03 中越界补口 | 进入风险表候选 |
| `03 / 04` 职责被 Step 13 重新分配 | `§11` 配置影响、`§12` 详细设计承接、03 / 04 文档分工 | 当前保持 `03` 负责正式 contract / runtime boundary,`04` 负责配置说明 / 填写方式 / 校验口径;Step 13 不重新分配。 | 不阻塞 Step 14;会阻塞后续 03 / 04 分工混写 | 进入风险表候选 |
| 详细设计未闭合时实现端私补对象、接口、状态或配置口径 | `§6` 对象、`§7` 接口、`§9` 状态、`§11` 配置、`§12` 承接 | 当前要求缺 schema / port / state / config key / test evidence schema 时必须回设计闭口,不能由实现端自行发明。 | 不阻塞 Step 14;会阻塞实现 | 进入风险表候选 |
| Query / Consumer / Job / Publisher 边界被翻转 | `§7` 接口分类、`§8` 处理流、`§9` 状态传播、`§10` 异常边界 | 当前保持 Query no-write、Consumer 不生成 core truth、Job 不修 core truth、Publisher failure 不回滚 truth。 | 不阻塞 Step 14;会阻塞 03 flow / error recovery 越界设计 | 进入风险表候选 |
| 配置绕过 domain invariant、truth owner 或状态机 | `§5` core / support / operation / peripheral 分层、`§9` 状态、`§11` 禁止配置化边界 | 当前保持配置只能影响装配、adapter、job、transport、profile 和降级语义,不得改变 Definition vs Use、truth owner、状态机或 P0 / P1。 | 不阻塞 Step 14;会阻塞 04 配置设计越界 | 进入风险表候选 |

#### R1.10.2 不升级为风险的候选表

| 候选 | 当前裁决 | 理由 | 后续入口 |
|---|---|---|---|
| 外部 / 下游依赖是否进入当前概要主线仍缺确认 | 不升级为风险 | 当前缺少最终确认,更符合“尚未形成定论”的待确认事项。 | 待确认事项筛选。 |
| 旧 `§13` 中可能可改写为当前主线风险的条目 | 不直接升级 | 旧条目仍需先审计旧污染和当前可改写关系,不能直接继承。 | 旧材料差异审计。 |
| 已被 `§12` 后移的字段、DTO、port、repository、状态矩阵和 config contract | 排除 | 这些已是详细设计 / 配置设计承接项,重复挂风险会削弱 `§12`。 | 不再进入 Step 13 后续筛选。 |

#### R1.10.3 风险筛选后续入口

后续模块必须遵守:

1. `R1.10.1` 只是风险表候选,正式 `§13` 回填前还需经过未闭环项说明、旧材料差异审计、自检和回填草稿压缩。
2. `R1.10.2` 中外部 / 下游依赖缺确认事项进入待确认事项筛选,不得在风险表中提前定性。
3. 旧正式 `§13` 条目仍不得直接进入风险表,必须先做旧材料差异审计。

#### R1.10.4 本模块停审记录

| 检查项 | 结论 | 说明 |
|---|---|---|
| 是否写出风险候选裁决表 | pass | 已形成 5 个风险表候选,均有影响范围、当前处理口径和阻塞性。 |
| 是否写出不升级为风险候选 | pass | 外部 / 下游缺确认、旧材料可改写项和已承接 contract 均未提前升级。 |
| 是否写正式 `§13` 最终风险表 | no | 当前只是中间产物候选。 |
| 是否写待确认事项表 | no | 待确认事项留给后续模块。 |
| 是否修改正式文档 | no | 未修改正式 `02-概要设计.md`。 |
| 是否进入 Step 14 | no | 当前仍停在 Step 13。 |

next_allowed_action: 等待用户确认后进入 Step 13 `待确认事项筛选:先思考`;只定义待确认事项判定口径、候选范围和下一批写入边界,不得写最终正式 `§13`,不得修改正式 `02-概要设计.md`,不得进入 Step 14。

### R1.11 待确认事项筛选:先思考

#### R1.11.1 本模块问题

本模块只回答四类问题:

1. 哪些候选属于“尚未形成定论,但会影响后续详细设计或概要判断”的待确认事项。
2. 哪些候选已经是风险、已排除项或旧材料审计对象,不能混入待确认表。
3. 待确认事项应如何写影响范围、缺失确认和当前挂起口径。
4. 下一批 `待确认事项筛选:再写入` 应形成怎样的待确认候选裁决表。

本模块不写正式 `§13` 待确认表,不修改正式 `02-概要设计.md`,不进入 Step 14。

#### R1.11.2 待确认判定口径

候选只有满足以下条件,才能进入待确认候选表:

| 判定条件 | 说明 |
|---|---|
| 尚未形成定论 | 当前缺最终确认,不能写成已知风险或稳定结论。 |
| 影响后续展开 | 会影响 `03` contract、`04` 配置说明、`05` 测试边界、`07` 实施边界或概要主线解释。 |
| 有明确缺失确认 | 能说清缺什么确认,不能写“后续再看”。 |
| 有当前挂起口径 | 在定论前有保守处理方式,不会阻断 Step 14。 |
| 不是任务或实现细节 | 不能把字段、DTO、port、测试 case、配置 key 或开发任务写成待确认事项。 |

#### R1.11.3 待确认候选范围

| 待确认候选 | 来源 | 缺失确认方向 | 当前方向 |
|---|---|---|---|
| 外部 / 下游依赖是否进入当前概要主线仍缺确认 | R1.8.3;R1.10.2;00 / 01 背景 | 需要确认哪些外部 / 下游协作只作为 summary/ref 背景,哪些会影响 03 contract 展开。 | 待确认候选。 |
| 下游消费影响摘要的正式来源与承接责任 | 正式 `§5`~`§8`;`§12` 承接;00 / 01 下游协作 | 需要确认消费影响摘要由哪些相邻仓提供,以及本仓只承接 summary/ref 的边界是否足够。 | 待确认候选。 |
| 外部依据、artifact / archive ref 与治理依据的可用性口径 | 正式 `§5`~`§11`;`§12` 承接 | 需要确认外部依据不可用、引用失效或摘要缺失时,03 / 04 应采用何种 formal degraded / pending 口径。 | 待确认候选。 |
| 外围 package / method set 与 marketplace context 的演进范围 | 正式 `§5`~`§11`;00 / 01 P0 / P1 范围 | 需要确认外围增强在当前 detailed design 中展开到什么深度,以及哪些内容留到后续版本。 | 待确认候选。 |

#### R1.11.4 不应进入待确认表的候选

| 候选 | 不进入原因 | 后续入口 |
|---|---|---|
| R1.10 已裁决的 5 个风险表候选 | 已经是已识别风险,不能重复写成待确认。 | 风险候选保留到回填草稿。 |
| 已进入 `§12` 的字段、DTO、port、repository、状态矩阵、config contract | 已有承接位置,不是待确认事项。 | 排除。 |
| 旧正式 `§13` 条目 | 尚未完成旧材料差异审计,不能直接转为待确认。 | 旧材料差异审计。 |
| 具体配置 key、默认值、环境变量、worker、topic、retry、test case | 实现 / 配置 / 测试细节。 | `03`~`07`。 |

#### R1.11.5 待确认表写法边界

下一批待确认候选裁决表必须保持以下字段:

| 字段 | 写法 |
|---|---|
| 待确认候选 | 写尚未定论且会影响后续设计判断的问题。 |
| 影响范围 | 指向具体概要章节或后续文档,不得写“整体相关”。 |
| 缺失确认 | 写当前缺哪类外部 / 下游 / 范围 / 口径确认。 |
| 当前挂起口径 | 写在定论前如何保守处理,不得写“以后再说”。 |
| 裁决 | 标记 `进入待确认表候选` 或 `排除 / 后移`,正式回填仍等草稿模块。 |

#### R1.11.6 下一写入批次边界

下一批 `R1.12 待确认事项筛选:再写入` 只允许写:

1. 待确认候选裁决表。
2. 不进入待确认表的候选表。
3. 待确认筛选后的后续入口说明。
4. 本模块停审记录和 `next_allowed_action`。

下一批不得写:

1. 正式 `§13` 最终待确认表。
2. 旧材料差异审计最终结论。
3. 正式 `02-概要设计.md`。
4. Step 14 装配正文。

#### R1.11.7 自检

| 检查项 | 结论 | 说明 |
|---|---|---|
| 是否只做待确认筛选先思考 | pass | 当前只定义判定口径、候选范围和下一写入边界。 |
| 是否把风险重复写成待确认 | no | R1.10 风险候选不进入待确认表。 |
| 是否把实现细节写成待确认 | no | schema、port、config key、worker、topic、test case 已排除。 |
| 是否裁决旧材料 | no | 旧 `§13` 条目留给旧材料差异审计。 |
| 是否修改正式文档 | no | 未修改正式 `02-概要设计.md`。 |
| 是否进入 Step 14 | no | 当前仍停在 Step 13。 |

next_allowed_action: 等待用户确认后进入 Step 13 `待确认事项筛选:再写入`;只写待确认候选裁决表、不进入待确认表候选表和待确认筛选后续入口说明,不得写最终正式 `§13`,不得修改正式 `02-概要设计.md`,不得进入 Step 14。

### R1.12 待确认事项筛选:再写入

#### R1.12.1 待确认候选裁决表

| 待确认候选 | 影响范围 | 缺失确认 | 当前挂起口径 | 裁决 |
|---|---|---|---|---|
| 外部 / 下游依赖是否进入当前概要主线仍缺确认 | `§7` 接口骨架、`§8` 处理流、`§12` 详细设计承接、03 contract 展开 | 缺哪些外部 / 下游协作只作为 summary/ref 背景、哪些需要进入 03 formal contract 的最终确认。 | 当前只按 summary/ref / body-free 背景承接;不得把外部正文、下游运行 truth 或未确认协作写成核心主线。 | 进入待确认表候选 |
| 下游消费影响摘要的正式来源与承接责任 | `§5` 追溯与一致性保护、`§6` `ConsumptionImpactSummary`、`§8` 追溯处理流、`§12` 对象 / 接口承接 | 缺相邻仓提供消费影响摘要的责任边界、触发条件和本仓承接深度确认。 | 当前本仓只保留消费影响摘要 / 线索 / ref,不扫描或复制下游运行状态。 | 进入待确认表候选 |
| 外部依据、artifact / archive ref 与治理依据的可用性口径 | `§5` 外部摘要与引用、`§6` external / governance ref、`§10` external safe failure、`§11` 配置影响、03 / 04 承接 | 缺外部依据不可用、引用失效或摘要缺失时应采用 pending、unavailable、degraded 或 rejection 的正式口径确认。 | 当前按外部正文禁止入仓和 summary/ref-only 保守处理;不可用只能阻断新承接或形成安全挂起,不得回滚 truth。 | 进入待确认表候选 |
| 外围 package / method set 与 marketplace context 的演进范围 | `§5` 外围包与方法集组织、`§6` peripheral 对象、`§8` 外围组织处理流、`§11` P0 / P1 配置边界、00 / 01 P0 / P1 范围 | 缺当前 detailed design 是否展开外围 package / method set 到正式 contract,以及哪些 marketplace context 内容后移的范围确认。 | 当前按外围增强处理,不作为 core 闭环前置,不进入 marketplace listing / install / fulfillment truth。 | 进入待确认表候选 |

#### R1.12.2 不进入待确认表的候选表

| 候选 | 当前裁决 | 理由 | 后续入口 |
|---|---|---|---|
| R1.10 的 5 个风险表候选 | 不进入待确认表 | 已经是已识别设计风险候选,重复写成待确认会混淆风险 / 待确认边界。 | 保留到正式 `§13` 回填草稿压缩。 |
| 已进入 `§12` 的字段、DTO、port、repository、状态矩阵、config contract、test evidence | 排除 | 这些已经有详细设计、配置设计、测试方案或实施计划承接位置。 | 不再进入 Step 13 后续筛选。 |
| 旧正式 `§13` 的旧条目 | 暂不进入待确认表 | 旧条目仍含旧 `MethodContent` / publish / snapshot / fingerprint / outbox 等污染,必须先审计。 | 旧材料差异审计。 |
| 具体配置 key、默认值、环境变量、worker、topic、retry、test case | 排除 | 属于 03 / 04 / 05 / 07 细化或实施内容,不是概要待确认事项。 | `03`~`07`。 |

#### R1.12.3 待确认筛选后续入口

后续模块必须遵守:

1. `R1.12.1` 只是待确认表候选,正式 `§13` 回填前还需经过未闭环项说明、旧材料差异审计、自检和回填草稿压缩。
2. `R1.10.1` 风险候选和 `R1.12.1` 待确认候选必须继续分开,不得在正式草稿中混写。
3. 旧正式 `§13` 条目仍不得直接进入待确认表,必须先做旧材料差异审计。

#### R1.12.4 本模块停审记录

| 检查项 | 结论 | 说明 |
|---|---|---|
| 是否写出待确认候选裁决表 | pass | 已形成 4 个待确认表候选,均有影响范围、缺失确认和当前挂起口径。 |
| 是否写出不进入待确认表候选 | pass | 风险候选、已承接项、旧材料条目和实现细节均未混入待确认表。 |
| 是否写正式 `§13` 最终待确认表 | no | 当前只是中间产物候选。 |
| 是否修改正式文档 | no | 未修改正式 `02-概要设计.md`。 |
| 是否进入 Step 14 | no | 当前仍停在 Step 13。 |

next_allowed_action: 等待用户确认后进入 Step 13 `未闭环项说明:先思考`;只定义不阻塞 Step 14 与会阻塞后续详细设计 / 实现的分层口径,不得写最终正式 `§13`,不得修改正式 `02-概要设计.md`,不得进入 Step 14。

### R1.13 未闭环项说明:先思考

#### R1.13.1 本模块问题

本模块只回答四类问题:

1. 当前风险候选和待确认候选是否阻塞 Step 14。
2. 哪些项会阻塞后续 `03 / 04` 设计或实现。
3. 这些未闭环项在正式 `§13` 中应以什么层级说明。
4. 下一批 `未闭环项说明:再写入` 应形成怎样的分层说明表。

本模块不写正式 `§13`,不修改正式 `02-概要设计.md`,不进入 Step 14。

#### R1.13.2 分层口径

| 层级 | 判定口径 | 当前处理 |
|---|---|---|
| 不阻塞 Step 14 | 风险 / 待确认已有保守处理口径,不会推翻正式 `§5`~`§12` 已收稳概要主线。 | 可进入后续正式概要整理,但必须在 `§13` 保留风险 / 待确认。 |
| 会阻塞 03 / 04 越界设计 | 若后续详细设计或配置设计试图暗改概要主语、分工、对象 owner、接口边界、状态 owner 或配置禁区。 | 必须回退对应概要 Step 或按 `§12` 承接闭口。 |
| 会阻塞实现 | 若实现前仍缺正式 schema、port、state、config key、test evidence schema 或 artifact schema。 | 实现端不得自行补口,必须回设计闭口。 |
| 不进入未闭环说明 | 已排除项、任务、普通优化、具体实现细节和旧材料污染样本。 | 继续留在排除表或旧材料差异审计中。 |

#### R1.13.3 当前风险候选分层

| 风险候选组 | Step 14 判断 | 后续阻塞方向 |
|---|---|---|
| `§12` 回退规则被削弱 / `03 / 04` 职责被重分配 | 不阻塞 Step 14,因为当前处理口径已固定为不得削弱。 | 会阻塞 03 / 04 越界设计。 |
| 实现端私补对象、接口、状态或配置口径 | 不阻塞 Step 14,因为概要层已明确“不得私补”。 | 会阻塞实现。 |
| Query / Consumer / Job / Publisher 边界被翻转 | 不阻塞 Step 14,因为当前 `§7`~`§10` 已有红线。 | 会阻塞 03 flow / error recovery 越界设计。 |
| 配置绕过 invariant、truth owner 或状态机 | 不阻塞 Step 14,因为 `§11` 已有禁止配置化边界。 | 会阻塞 04 配置设计越界。 |

#### R1.13.4 当前待确认候选分层

| 待确认候选组 | Step 14 判断 | 后续阻塞方向 |
|---|---|---|
| 外部 / 下游依赖是否进入当前概要主线 | 不阻塞 Step 14,因为当前按 summary/ref 背景保守承接。 | 若 03 要把协作写入 formal contract,需先确认。 |
| 下游消费影响摘要的正式来源与承接责任 | 不阻塞 Step 14,因为当前只承接 summary/ref。 | 会影响 03 对 impact summary / trace material 的正式 contract。 |
| 外部依据、artifact / archive ref 与治理依据的可用性口径 | 不阻塞 Step 14,因为当前有 body-free 和 safe pending / unavailable 的保守边界。 | 会影响 03 error / state / external adapter contract 与 04 配置说明。 |
| 外围 package / method set 与 marketplace context 的演进范围 | 不阻塞 Step 14,因为当前作为 peripheral 增强处理。 | 会影响 03 是否展开 peripheral contract 与后续版本范围。 |

#### R1.13.5 下一写入批次边界

下一批 `R1.14 未闭环项说明:再写入` 只允许写:

1. 未闭环项分层说明表。
2. 不阻塞 Step 14 的说明。
3. 会阻塞后续 03 / 04 / 实现的说明。
4. 不进入未闭环说明的排除表。
5. 本模块停审记录和 `next_allowed_action`。

下一批不得写:

1. 正式 `§13` 最终正文。
2. 旧材料差异审计最终结论。
3. 正式 `02-概要设计.md`。
4. Step 14 装配正文。

#### R1.13.6 自检

| 检查项 | 结论 | 说明 |
|---|---|---|
| 是否只做未闭环说明先思考 | pass | 当前只定义分层口径和下一写入边界。 |
| 是否宣称风险 / 待确认已关闭 | no | 当前只是分层,未把候选润色成定论。 |
| 是否阻塞 Step 14 | no | 当前判断为可进入后续草稿和自检,但正式回填前仍需保留 `§13`。 |
| 是否修改正式文档 | no | 未修改正式 `02-概要设计.md`。 |
| 是否进入 Step 14 | no | 当前仍停在 Step 13。 |

next_allowed_action: 等待用户确认后进入 Step 13 `未闭环项说明:再写入`;只写未闭环项分层说明、不阻塞 Step 14 说明、后续阻塞说明和排除说明,不得写最终正式 `§13`,不得修改正式 `02-概要设计.md`,不得进入 Step 14。

### R1.14 未闭环项说明:再写入

#### R1.14.1 未闭环项分层说明表

| 未闭环项层级 | 覆盖对象 | 不阻塞 Step 14 的理由 | 后续阻塞条件 | 当前处理口径 |
|---|---|---|---|---|
| 概要主线保护类风险 | `§12` 回退规则被削弱、`03 / 04` 职责被重新分配 | 当前 `§5`~`§12` 已固定主语、职责和承接关系,Step 14 只需按已收稳主线整理。 | 后续 03 / 04 若试图弱化概要回退规则、重分配对象 / 接口 / 配置职责。 | 作为正式 `§13` 风险候选保留;后续设计越界时必须回退到对应概要 Step。 |
| 实现落码闭口类风险 | 实现端私补对象、接口、状态、配置口径或 evidence / artifact schema | 概要层已明确“不得私补”,不会推翻当前概要正文。 | 实现前仍缺正式 schema、port、state、config key、test evidence schema 或 artifact schema。 | 作为实现前置门禁风险;缺口未闭时实现端必须暂停并回设计。 |
| 运行边界翻转类风险 | Query / Consumer / Job / Publisher owner 或 read / write 边界被翻转 | `§7`~`§10` 已有接口、处理流、异常和状态红线。 | 03 flow、error recovery 或 adapter 设计中出现 owner 反转、跨层改写 truth、越权消费。 | 作为 03 详细设计红线风险;不得用实现便利性覆盖概要边界。 |
| 配置绕行类风险 | 配置绕过 domain invariant、truth owner、状态机或 body-free 规则 | `§11` 已把可配置和不可配置边界分开。 | 04 配置设计把 invariant、状态 owner、truth owner 或 forbidden content 做成配置项。 | 作为 04 配置设计红线风险;配置只能调节策略,不能改写职责与状态真相。 |
| 外部 / 下游依赖范围待确认 | 哪些外部 / 下游协作进入当前概要主线 | 当前只按 summary/ref 背景承接,未把外部正文或下游运行 truth 写入核心主线。 | 03 需要把某类协作写成 formal contract、port、event 或 handoff。 | 保留为待确认候选;确认前只能用 summary/ref / body-free 方式描述。 |
| 下游消费影响来源待确认 | `ConsumptionImpactSummary` 的正式来源、触发条件和承接责任 | 当前本仓不扫描下游状态,只承接影响摘要与线索。 | 03 需要定义 impact summary / trace material 的正式生产与校验契约。 | 保留为待确认候选;责任边界未确认前不得扩大成本仓 truth。 |
| 外部依据可用性待确认 | external basis、artifact / archive ref、governance basis 不可用时的口径 | 当前已有 body-free、safe pending / unavailable 和不可回滚 truth 的保守边界。 | 03 error / state / external adapter 或 04 配置需要规定不可用状态和值域。 | 保留为待确认候选;不可用只能阻断新承接或安全挂起,不得补写外部正文。 |
| 外围演进范围待确认 | peripheral package、method set、marketplace context 的展开深度 | 当前按外围增强处理,不影响 core method-library 概要闭环。 | 03 要展开 peripheral contract,或后续版本要纳入 marketplace listing / install / fulfillment。 | 保留为待确认候选;当前不把 marketplace 运行 truth 写入核心设计。 |

#### R1.14.2 不阻塞 Step 14 的说明

当前 Step 13 的风险和待确认候选不阻塞 Step 14,理由如下:

1. `§5`~`§12` 的核心主线已经收稳,风险 / 待确认只约束后续设计不得越界,不要求重写已完成概要章节。
2. 每个风险候选都有保守处理口径,不会要求 Step 14 新增对象、接口、状态、配置或实现 schema。
3. 每个待确认候选都有挂起口径,确认前只保留 summary/ref、body-free、pending / unavailable 或后续版本范围,不扩大当前概要主线。
4. Step 14 只负责整理正式概要文档和校准来源,不得把 Step 13 候选升级成详细设计承诺。

#### R1.14.3 会阻塞后续 03 / 04 / 实现的说明

以下情况必须阻塞后续动作:

| 后续阶段 | 阻塞条件 | 必须动作 |
|---|---|---|
| `03-详细设计.md` | 试图改变概要主语、对象 owner、接口 owner、read / write 边界、处理流职责或状态 owner。 | 回到对应概要 Step 或在 03 中显式回指 `§12` 承接项,不得私自改写。 |
| `03-详细设计.md` | 需要正式 schema、DTO、port、repository、mapper、state matrix、adapter contract,但概要或 03 未闭口。 | 暂停详细设计推进,补齐正式真相源后再继续。 |
| `04-配置设计.md` | 配置项试图绕过 invariant、状态机、truth owner、body-free 禁区、外部正文禁入或下游 truth 禁入。 | 删除或改写该配置项;必要时回到 `§11` / `§12` 重新裁决。 |
| 实现阶段 | 缺正式 schema、port、state、mapper、config key、test evidence schema、run artifact schema 或 boundary 台账。 | 实现端不得自行补口,必须回设计仓闭口并更新台账 / 门禁。 |

#### R1.14.4 不进入未闭环说明的排除表

| 排除项 | 排除理由 | 后续入口 |
|---|---|---|
| 已进入 `§5`~`§12` 且已有承接位置的结论 | 已是当前概要真相源,不是未闭环项。 | 正式 §5~§12 与 Step 14 校准来源。 |
| 普通优化、文案精简、表格排版和命名润色 | 不影响后续设计或实现闭口。 | Step 14 编辑整理。 |
| 具体字段全集、函数签名、DDL、worker、topic、retry、test case | 属于详细设计、配置设计、测试方案或实施计划。 | `03`~`07`。 |
| 旧正式 `§13` 污染样本 | 尚未完成旧材料差异审计,不能直接作为当前未闭环项。 | 下一模块旧材料差异审计。 |
| 已裁决排除的旧 `MethodContent` / publish / snapshot / fingerprint / outbox / delivery 主线 | 旧主线与当前方法库定位不一致,不得回流当前 Step 13。 | 旧材料差异审计只记录废弃或后移。 |

#### R1.14.5 本模块停审记录

| 检查项 | 结论 | 说明 |
|---|---|---|
| 是否完成未闭环项分层说明 | pass | 已区分不阻塞 Step 14、阻塞 03 / 04 / 实现和排除项。 |
| 是否写正式 `§13` 最终正文 | no | 当前仍是中间产物,正式回填留给后续草稿和确认。 |
| 是否修改正式文档 | no | 未修改正式 `02-概要设计.md`。 |
| 是否裁决旧材料 | no | 旧正式 `§13` 留给下一模块差异审计。 |
| 是否进入 Step 14 | no | 当前仍停在 Step 13。 |

next_allowed_action: 等待用户确认后进入 Step 13 `旧材料差异审计:先思考`;只审计旧正式 `§13` 条目与旧污染样本的处理框架,不得直接继承旧风险 / 待确认,不得写最终正式 `§13`,不得修改正式 `02-概要设计.md`,不得进入 Step 14。

### R1.15 旧材料差异审计:先思考

#### R1.15.1 本模块问题

本模块只回答四类问题:

1. 旧正式 `§13` 中有哪些条目需要进入逐项审计池。
2. 每个旧条目应按什么维度判断保留、改写、废弃或后移。
3. 哪些旧条目明显含旧主线污染,不得直接继承。
4. 下一批 `旧材料差异审计:再写入` 应输出怎样的逐项审计表。

本模块不裁决旧条目的最终去留,不写正式 `§13`,不修改正式 `02-概要设计.md`,不进入 Step 14。

#### R1.15.2 旧材料审计输入池

旧正式 `§13` 当前包含两类输入:

| 输入类型 | 条目数量 | 读取范围 | 当前审计用途 |
|---|---:|---|---|
| 旧设计风险表 | 8 | 正式 `§13.1` | 逐项判断是否仍对应当前 `§5`~`§12` 风险,或只是旧主线污染。 |
| 旧待确认事项表 | 7 | 正式 `§13.2` | 逐项判断是否可改写为当前待确认候选,或已由新概要收稳 / 后移。 |

旧材料只能作为差异审计输入,不能直接覆盖 `R1.10` 风险候选和 `R1.12` 待确认候选。

#### R1.15.3 旧风险条目审计维度

| 审计维度 | 判定问题 | 可能裁决 |
|---|---|---|
| 当前主线匹配 | 旧风险是否仍落在当前 `§5`~`§12` 的 core / support / operation / peripheral 主线内。 | 保留为风险候选 / 改写为当前风险 / 废弃。 |
| 旧污染程度 | 是否依赖旧 `MethodContent`、publish、snapshot、fingerprint、outbox、delivery、P1 plugin / configuration 主线。 | 废弃 / 后移 / 只保留抽象红线。 |
| 与已收稳结论关系 | 是否已经被当前 `§5`~`§12` 写成正式边界、红线或承接项。 | 不再进入风险表 / 改写为后续阻塞说明。 |
| 与当前风险候选关系 | 是否可归并到 `R1.10` 已形成的 5 个风险候选。 | 归并 / 不归并。 |
| 后续设计影响 | 是否会阻塞 03 / 04 或实现,而不是阻塞 Step 14。 | 标记后续阻塞方向。 |

#### R1.15.4 旧待确认条目审计维度

| 审计维度 | 判定问题 | 可能裁决 |
|---|---|---|
| 当前范围匹配 | 旧待确认是否仍属于当前 P0 / 当前概要主线。 | 保留为待确认候选 / 后移 / 废弃。 |
| 外部确认需求 | 是否仍需要 L0 / L1 下游 / UI / 运维 / marketplace 等外部确认。 | 改写为当前待确认 / 后移到后续版本。 |
| 已收稳程度 | 是否已被当前 `§5`~`§12` 明确收稳。 | 排除。 |
| 细节下沉程度 | 是否属于字段、schema、权限、运维参数、测试或实施细节。 | 后移到 03~07。 |
| 与当前待确认候选关系 | 是否可归并到 `R1.12` 已形成的 4 个待确认候选。 | 归并 / 不归并。 |

#### R1.15.5 明显旧污染线索

下一批逐项审计时,遇到以下线索必须先按旧污染处理,不得直接继承:

| 旧污染线索 | 为什么不能直接继承 |
|---|---|
| `MethodContent` 作为元模型中心 | 当前主线已重建为 method asset / catalog / formalization / consumption / trace / external / peripheral 分层,不再以旧 `MethodContent` 单体建模。 |
| publish / snapshot / fingerprint / outbox 作为核心风险中心 | 当前概要已改为 body-free、summary/ref、handoff、publisher 边界和 detailed design 承接,不能恢复旧发布主线。 |
| `TaskDefinition` / `ProcessTemplateDef` 直接映射 runtime | 当前只允许下游消费影响摘要和线索,不把下游 runtime truth 写入本仓。 |
| `ViewProfile` UI / console 产品语义 | 当前概要只保留 query / material / visibility 边界,具体 UI 体验不进入 Step 13 风险。 |
| P1 Plugin / Configuration / dependency DAG | 当前外围增强不阻塞 core,marketplace / install / fulfillment 不进入当前核心主线。 |
| Operations Job 权限、批量参数、审批门禁 | 属于 03 / 04 / 07 或运维设计细化,不是概要 Step 13 当前风险主体。 |

#### R1.15.6 下一写入批次边界

下一批 `R1.16 旧材料差异审计:再写入` 只允许写:

1. 旧风险表逐项审计表。
2. 旧待确认表逐项审计表。
3. 可归并到 `R1.10` / `R1.12` 的映射说明。
4. 废弃 / 后移 / 排除的旧污染说明。
5. 本模块停审记录和 `next_allowed_action`。

下一批不得写:

1. 正式 `§13` 最终风险表或待确认表。
2. 正式 `02-概要设计.md`。
3. Step 14 装配正文。
4. 旧条目未审计就直接继承。
5. 新增超出当前 `R1.10` / `R1.12` / `R1.14` 的全新风险主线。

#### R1.15.7 自检

| 检查项 | 结论 | 说明 |
|---|---|---|
| 是否只做旧材料审计先思考 | pass | 当前只定义输入池、审计维度、污染线索和下一写入边界。 |
| 是否逐项裁决旧条目 | no | 逐项裁决留给 `R1.16`。 |
| 是否直接继承旧风险 / 待确认 | no | 明确旧材料只能作为差异审计输入。 |
| 是否修改正式文档 | no | 未修改正式 `02-概要设计.md`。 |
| 是否进入 Step 14 | no | 当前仍停在 Step 13。 |

next_allowed_action: 等待用户确认后进入 Step 13 `旧材料差异审计:再写入`;只写旧风险表和旧待确认表逐项审计裁决、归并关系和旧污染处置,不得写最终正式 `§13`,不得修改正式 `02-概要设计.md`,不得进入 Step 14。

### R1.16 旧材料差异审计:再写入

#### R1.16.1 旧风险表逐项审计裁决

| 旧风险条目 | 旧主线污染判断 | 当前裁决 | 归并 / 后续入口 |
|---|---|---|---|
| Definition / Use 边界污染 | 含旧 `MethodContent`、consumer、BoundaryViolation / contract test 表述。 | 改写归并,不得直接继承旧对象名。 | 归并到 `Query / Consumer / Job / Publisher 边界被翻转` 风险;保留“下游 Use truth 不进入本仓”的抽象红线。 |
| P1 Plugin / Configuration 反向污染 P0 | 含旧 PublishMethodPlugin / ActivateMethodConfiguration 和 P1 plugin 主线。 | 改写归并并后移细节。 | 归并到 `配置绕过 invariant / truth owner / 状态机` 风险;P1 plugin / configuration 细节后移到 peripheral / marketplace 范围确认。 |
| event / snapshot / fingerprint 不一致 | 以旧 event / snapshot / fingerprint 同步模型为中心。 | 不直接进入风险表。 | 旧同步模型废弃;可抽象支持 `外部依据、artifact / archive ref 与治理依据可用性口径` 待确认。 |
| published 缺 audit / fingerprint / outbox | 以 PublishMethodContent、AuditRecord、OutboxEvent 同一 unit_of_work 为中心。 | 废弃旧风险表述。 | publisher / handoff / evidence 仅在 03 / 05 / 07 细化,不得恢复旧 publish / outbox 主线。 |
| ViewProfile 解析误配或高频读性能不足 | 以旧 ViewProfile、UI / console、cache 性能为中心。 | 排除当前风险表。 | UI / console 产品语义和性能策略后移;当前只保留 query / read material / visibility 边界。 |
| MethodContent 元模型过宽或 kind-specific 字段边界不清 | 旧 `MethodContent` 单体元模型污染明显。 | 废弃旧对象名,保留抽象风险。 | 抽象归并到 `实现端私补对象、接口、状态或配置口径` 风险;具体字段边界进入 03 详细设计。 |
| ProcessTemplateDef / TaskDefinition 到 process runtime 映射不匹配 | 含旧下游 runtime 映射和模板执行索引口径。 | 改写为待确认线索,不作为当前风险。 | 归并到 `外部 / 下游依赖是否进入当前概要主线` 与 `下游消费影响摘要来源` 待确认。 |
| AIPolicyDef 与 governance policy source 语义分叉 | 含旧 policy source / fingerprint 对齐口径。 | 改写为待确认线索,不作为当前风险。 | 归并到 `外部依据、artifact / archive ref 与治理依据可用性口径` 待确认。 |

#### R1.16.2 旧待确认表逐项审计裁决

| 旧待确认条目 | 旧主线污染判断 | 当前裁决 | 归并 / 后续入口 |
|---|---|---|---|
| TaskDefinition 是否直接供 L1-work 作为 WorkItem 模板来源 | 含旧 TaskDefinition / ExportDefinitionSnapshot / event 主线。 | 改写归并。 | 归并到 `外部 / 下游依赖是否进入当前概要主线` 和 `下游消费影响摘要来源`。 |
| ViewProfile 默认 deny / 空视图的产品语义 | 以 UI / console 产品语义为中心。 | 后移。 | 不进入正式 `§13`;后续 UI / console 或 query 体验细化时处理。 |
| ViewProfile 复杂优先级、preset / override 是否进入 P0 | 以旧 ViewProfileProjection / cache 策略为中心。 | 后移并排除当前主线。 | 后续 query / material / UI 设计可处理;不作为当前概要待确认。 |
| snapshot schema 是否由 L0-core 统一定义 | 旧 snapshot schema 表述污染,但有跨仓 artifact schema 确认价值。 | 改写归并。 | 归并到 `外部依据、artifact / archive ref 与治理依据可用性口径`;不得恢复 snapshot 作为核心承载。 |
| P1 MethodPlugin / MethodConfiguration 何时进入正式详细设计 | 旧 P1 plugin / configuration 主线。 | 改写归并并后移范围。 | 归并到 `外围 package / method set 与 marketplace context 的演进范围`。 |
| Plugin dependency DAG / Variability 算法边界 | 旧 plugin dependency / variability 算法细节。 | 后移。 | 仅作为 peripheral 后续版本线索,不进入当前正式 `§13`。 |
| Operations Job 的执行门禁和运维权限边界 | 含 Seed / Replay / Rebuild / Recalculate、AuditRecord、OutboxEvent 运维细节。 | 后移到 03 / 04 / 07。 | 当前只保留“Job 不修 truth / 不绕过状态机”的红线;权限、批量参数、审批门禁不进当前 `§13`。 |

#### R1.16.3 当前候选归并结果

| 当前候选 | 旧材料支持来源 | 归并结论 |
|---|---|---|
| `§12` 回退规则被削弱 / `03 / 04` 职责被重分配 | 无需旧材料支持。 | 当前主线已独立成立,旧材料不增强。 |
| 实现端私补对象、接口、状态或配置口径 | MethodContent 元模型过宽。 | 只保留“不得私补对象 / 字段 / schema”的抽象风险,废弃旧对象名。 |
| Query / Consumer / Job / Publisher 边界被翻转 | Definition / Use 边界污染、Operations Job 旧门禁。 | 保留抽象边界风险,不继承旧 event / outbox / job 参数。 |
| 配置绕过 invariant、truth owner 或状态机 | P1 Plugin / Configuration 反向污染 P0。 | 保留配置绕界风险,不恢复旧 P1 plugin command。 |
| 外部 / 下游依赖是否进入当前概要主线 | TaskDefinition / ProcessTemplateDef / L1-work / process runtime 旧条目。 | 改写为 summary/ref 与下游协作范围确认。 |
| 下游消费影响摘要的正式来源与承接责任 | TaskDefinition / ProcessTemplateDef 旧消费条目。 | 改写为 impact summary 来源确认,不写 runtime 映射。 |
| 外部依据、artifact / archive ref 与治理依据的可用性口径 | snapshot schema、AIPolicyDef / governance source 旧条目。 | 改写为 artifact / governance basis 可用性确认。 |
| 外围 package / method set 与 marketplace context 的演进范围 | P1 MethodPlugin / MethodConfiguration、Plugin dependency DAG。 | 改写为 peripheral / marketplace 演进范围确认。 |

#### R1.16.4 旧污染处置说明

| 处置类型 | 覆盖旧条目 | 说明 |
|---|---|---|
| 废弃旧主线 | `MethodContent` 单体、PublishMethodContent、snapshot / fingerprint / outbox 作为核心发布模型。 | 与当前 method asset / summary-ref / body-free / handoff 主线冲突,不得进入正式 `§13`。 |
| 改写为抽象风险 | Definition / Use、P1 配置反向污染、MethodContent 字段边界。 | 只保留边界翻转、配置绕界、实现私补等抽象风险。 |
| 改写为待确认 | TaskDefinition / process runtime、snapshot schema、AIPolicy / governance source、P1 plugin 进入时机。 | 归并为外部 / 下游 / artifact / peripheral 范围确认。 |
| 后移到后续设计 | ViewProfile UI 语义、复杂优先级 / cache、plugin DAG、job 权限和审批。 | 属于 UI、03、04、07 或后续版本,不进入当前正式 `§13`。 |

#### R1.16.5 本模块停审记录

| 检查项 | 结论 | 说明 |
|---|---|---|
| 是否逐项审计旧风险表 | pass | 旧 8 项风险已全部裁决。 |
| 是否逐项审计旧待确认表 | pass | 旧 7 项待确认已全部裁决。 |
| 是否直接继承旧风险 / 待确认 | no | 所有保留内容均以当前候选归并或抽象改写方式承接。 |
| 是否新增超出当前候选的新主线 | no | 未新增 `R1.10` / `R1.12` 之外的新风险主线。 |
| 是否修改正式文档 | no | 未修改正式 `02-概要设计.md`。 |
| 是否进入 Step 14 | no | 当前仍停在 Step 13。 |

next_allowed_action: 等待用户确认后进入 Step 13 `正式 §13 回填草稿:先思考`;只设计正式 `§13` 草稿结构、压缩原则和风险 / 待确认表来源,不得修改正式 `02-概要设计.md`,不得进入 Step 14。

### R1.17 正式 §13 回填草稿:先思考

#### R1.17.1 本模块问题

本模块只回答四类问题:

1. 正式 `§13` 应采用怎样的章节结构和表格字段。
2. 哪些中间产物可以进入正式风险表和待确认表。
3. 如何把 `R1.10`、`R1.12`、`R1.14`、`R1.16` 压缩成正式草稿。
4. 下一批 `正式 §13 回填草稿:再写入` 应输出哪些草稿内容。

本模块不写正式 `02-概要设计.md`,不替换旧 `§13`,不进入 Step 14。

#### R1.17.2 正式 §13 建议结构

正式 `§13` 保留两段主结构:

| 正式小节 | 内容来源 | 表格字段 | 写法边界 |
|---|---|---|---|
| `13.1 设计风险` | `R1.10` 风险候选、`R1.14` 后续阻塞说明、`R1.16` 旧材料归并 | 风险、影响、当前处理口径 | 只写概要层风险和后续设计 / 实现阻塞口径,不写 schema、trait、DDL、test case。 |
| `13.2 待确认事项` | `R1.12` 待确认候选、`R1.14` 挂起口径、`R1.16` 旧材料归并 | 待确认、影响范围、当前挂起口径 | 只写仍需外部 / 下游 / 范围确认的事项,不写普通任务和已收稳结论。 |

正式 `§13` 不新增第三段,旧污染处置不进入正式正文,只保留在本 Step 中间产物。

#### R1.17.3 风险表压缩原则

`R1.10` 的 5 个风险候选进入正式草稿时按以下原则压缩:

| 风险候选来源 | 草稿处理 | 压缩原则 |
|---|---|---|
| `§12` 回退规则被后续讨论削弱 | 保留为正式风险。 | 强调不得削弱 `§12` 回退规则和不得重分配 03 / 04 职责。 |
| `03 / 04` 职责被 Step 13 重新分配 | 并入上一条或作为同一风险影响。 | 避免重复风险;写成“详细设计 / 配置设计越界”。 |
| 详细设计未闭合时实现端私补对象、接口、状态或配置口径 | 保留为正式风险。 | 写成实现前置门禁,缺 schema / port / state / config / artifact schema 必须回设计。 |
| Query / Consumer / Job / Publisher 边界被翻转 | 保留为正式风险。 | 写成 owner / read-write / side effect 边界风险。 |
| 配置绕过 domain invariant、truth owner 或状态机 | 保留为正式风险。 | 写成配置设计红线风险。 |

正式风险表预计 4 条:把 `§12` 回退规则和 `03 / 04` 职责越界合并为 1 条。

#### R1.17.4 待确认表压缩原则

`R1.12` 的 4 个待确认候选进入正式草稿时全部保留:

| 待确认候选来源 | 草稿处理 | 压缩原则 |
|---|---|---|
| 外部 / 下游依赖是否进入当前概要主线仍缺确认 | 保留。 | 写清当前只按 summary/ref 背景承接。 |
| 下游消费影响摘要的正式来源与承接责任 | 保留。 | 写清本仓不扫描下游运行状态。 |
| 外部依据、artifact / archive ref 与治理依据的可用性口径 | 保留。 | 写清 body-free 与 safe pending / unavailable 口径。 |
| 外围 package / method set 与 marketplace context 的演进范围 | 保留。 | 写清 peripheral / marketplace 不阻塞 core。 |

正式待确认表预计 4 条,不得加入旧 ViewProfile、plugin DAG、job 权限等已后移事项。

#### R1.17.5 旧材料回填限制

`R1.16` 只影响正式草稿的措辞和排除,不直接贡献旧条目:

| 旧材料类型 | 正式草稿处理 |
|---|---|
| `MethodContent`、PublishMethodContent、snapshot、fingerprint、outbox | 不写入正式 `§13`。 |
| Definition / Use、P1 配置反向污染 | 只抽象为边界翻转和配置绕界风险。 |
| TaskDefinition / ProcessTemplateDef / AIPolicyDef 旧条目 | 只抽象为外部 / 下游 / governance basis 待确认。 |
| ViewProfile、plugin DAG、job 权限 / 审批 | 不进入正式 `§13`;后移到 UI、03、04、07 或后续版本。 |

#### R1.17.6 下一写入批次边界

下一批 `R1.18 正式 §13 回填草稿:再写入` 只允许写:

1. 正式 `13.1 设计风险` 草稿表。
2. 正式 `13.2 待确认事项` 草稿表。
3. 草稿来源映射表。
4. 本模块停审记录和 `next_allowed_action`。

下一批不得写:

1. 直接修改正式 `02-概要设计.md`。
2. Step 14 装配正文。
3. 旧污染条目原文。
4. 新增未经过 `R1.10` / `R1.12` / `R1.14` / `R1.16` 的风险或待确认。
5. 03 / 04 / 05 / 07 细节。

#### R1.17.7 自检

| 检查项 | 结论 | 说明 |
|---|---|---|
| 是否只做回填草稿先思考 | pass | 当前只定义正式结构、压缩原则和下一写入边界。 |
| 是否修改正式文档 | no | 未修改正式 `02-概要设计.md`。 |
| 是否写出最终正式 `§13` | no | 草稿内容留给 `R1.18`。 |
| 是否继承旧污染条目 | no | 明确旧材料只影响措辞和排除。 |
| 是否进入 Step 14 | no | 当前仍停在 Step 13。 |

next_allowed_action: 等待用户确认后进入 Step 13 `正式 §13 回填草稿:再写入`;只写正式 `13.1` / `13.2` 草稿表和来源映射,不得修改正式 `02-概要设计.md`,不得进入 Step 14。

### R1.18 正式 §13 回填草稿:再写入

#### R1.18.1 正式 `13.1 设计风险` 草稿表

| 风险 | 影响 | 当前处理口径 |
|---|---|---|
| 详细设计 / 配置设计削弱概要回退规则或重分配职责 | 影响 `§12` 承接边界、`03-详细设计.md` 回退入口、`04-配置设计.md` 分工和 Step 5~11 已收稳主语。 | `§12.3` 回退规则保持硬边界;后续若需要改主语、对象 owner、接口族、流程类型或配置职责,必须回退对应概要 Step,不得在 03 / 04 暗改。 |
| 实现端在正式设计未闭合时私补对象、接口、状态或配置口径 | 影响 `§6` 对象、`§7` 接口、`§9` 状态、`§11` 配置、`§12` 承接和后续实施门禁。 | 缺 schema、port、state、mapper、config key、test evidence schema 或 run artifact schema 时必须回设计闭口,实现端不得自行发明占位规则。 |
| Query / Consumer / Job / Publisher 边界被翻转 | 影响 `§7` 接口分类、`§8` 处理流、`§9` 状态传播、`§10` 异常边界和 03 flow / error recovery。 | 保持 Query no-write、Consumer 不生成 core truth、Job 不修 core truth、Publisher / handoff failure 不回滚 truth;后续设计不得用实现便利性改写 owner 和副作用边界。 |
| 配置绕过 domain invariant、truth owner 或状态机 | 影响 `§5` 分层、`§9` 状态、`§11` 禁止配置化边界和 04 配置设计。 | 配置只能影响装配、adapter、job、transport、profile 和降级语义,不得改变 Definition vs Use、truth owner、状态机、body-free 禁区或 P0 / P1 范围。 |

#### R1.18.2 正式 `13.2 待确认事项` 草稿表

| 待确认 | 影响范围 | 当前挂起口径 |
|---|---|---|
| 外部 / 下游依赖是否进入当前概要主线 | `§7` 接口骨架、`§8` 处理流、`§12` 详细设计承接和 03 contract 展开。 | 当前只按 summary/ref / body-free 背景承接;确认前不得把外部正文、下游运行 truth 或未确认协作写成核心主线。 |
| 下游消费影响摘要的正式来源与承接责任 | `§5` 追溯与一致性保护、`§6` `ConsumptionImpactSummary`、`§8` 追溯处理流和 `§12` 对象 / 接口承接。 | 当前本仓只保留消费影响摘要、线索和 typed ref,不扫描、复制或推断下游运行状态。 |
| 外部依据、artifact / archive ref 与治理依据的可用性口径 | `§5` 外部摘要与引用、`§6` external / governance ref、`§10` external safe failure、`§11` 配置影响和 03 / 04 承接。 | 当前按外部正文禁止入仓和 summary/ref-only 保守处理;不可用只能阻断新承接或形成安全挂起,不得回滚已成立 truth。 |
| 外围 package / method set 与 marketplace context 的演进范围 | `§5` 外围包与方法集组织、`§6` peripheral 对象、`§8` 外围组织处理流、`§11` P0 / P1 配置边界和 00 / 01 P0 / P1 范围。 | 当前按外围增强处理,不作为 core 闭环前置;marketplace listing / install / fulfillment truth 不进入当前概要主线。 |

#### R1.18.3 草稿来源映射表

| 草稿条目 | 主要来源 | 旧材料处理 |
|---|---|---|
| 详细设计 / 配置设计削弱概要回退规则或重分配职责 | `R1.10.1` 前两项;`R1.14.3` | 不依赖旧材料。 |
| 实现端私补对象、接口、状态或配置口径 | `R1.10.1`;`R1.14.3`;`R1.16.3` | 只抽象吸收旧 MethodContent 字段边界风险,不继承旧对象名。 |
| Query / Consumer / Job / Publisher 边界被翻转 | `R1.10.1`;`R1.14.3`;`R1.16.3` | 只抽象吸收 Definition / Use 和 job 旧门禁,不继承旧 event / outbox / job 参数。 |
| 配置绕过 invariant、truth owner 或状态机 | `R1.10.1`;`R1.14.3`;`R1.16.3` | 只抽象吸收 P1 配置反向污染,不恢复旧 plugin command。 |
| 外部 / 下游依赖是否进入当前概要主线 | `R1.12.1`;`R1.16.3` | 旧 TaskDefinition / process runtime 条目改写为 summary/ref 范围确认。 |
| 下游消费影响摘要的正式来源与承接责任 | `R1.12.1`;`R1.16.3` | 旧下游消费条目改写为 impact summary 来源确认。 |
| 外部依据、artifact / archive ref 与治理依据的可用性口径 | `R1.12.1`;`R1.16.3` | 旧 snapshot / AIPolicy / governance source 条目改写为 artifact / basis 可用性确认。 |
| 外围 package / method set 与 marketplace context 的演进范围 | `R1.12.1`;`R1.16.3` | 旧 P1 plugin / DAG 条目只保留 peripheral 演进范围确认。 |

#### R1.18.4 本模块停审记录

| 检查项 | 结论 | 说明 |
|---|---|---|
| 是否写出正式 `13.1` 草稿表 | pass | 已形成 4 条设计风险草稿。 |
| 是否写出正式 `13.2` 草稿表 | pass | 已形成 4 条待确认事项草稿。 |
| 是否写出来源映射 | pass | 每条草稿均回指 `R1.10` / `R1.12` / `R1.14` / `R1.16`。 |
| 是否直接修改正式文档 | no | 未修改正式 `02-概要设计.md`。 |
| 是否继承旧污染条目 | no | 旧材料仅抽象归并,未继承旧对象 / publish / snapshot / outbox 主线。 |
| 是否进入 Step 14 | no | 当前仍停在 Step 13。 |

next_allowed_action: 等待用户确认后进入 Step 13 `自检与停审:先思考`;只设计草稿回填前自检项、阻塞条件和放行条件,不得修改正式 `02-概要设计.md`,不得进入 Step 14。

### R1.19 自检与停审:先思考

#### R1.19.1 本模块问题

本模块只回答四类问题:

1. `R1.18` 草稿能否进入正式 `§13` 回填前检查。
2. 回填前必须检查哪些来源、边界和旧污染条件。
3. 哪些问题会阻塞正式 `§13` 回填。
4. 下一批 `自检与停审:再写入` 应形成怎样的检查结论。

本模块不修改正式 `02-概要设计.md`,不替换正式 `§13`,不进入 Step 14。

#### R1.19.2 回填前自检维度

| 自检维度 | 检查问题 | 预期结论 |
|---|---|---|
| 来源可追溯 | `R1.18` 每条风险 / 待确认是否能回指 `R1.10`、`R1.12`、`R1.14` 或 `R1.16`。 | 必须可追溯。 |
| 风险 / 待确认分离 | 风险表是否只写已识别设计风险,待确认表是否只写尚未定论事项。 | 必须分离。 |
| 旧污染清理 | 是否仍直接继承 `MethodContent`、PublishMethodContent、snapshot、fingerprint、outbox、ViewProfile、plugin DAG、job 权限等旧主线。 | 必须无直接继承。 |
| 概要深度 | 是否写入 schema、trait、DDL、字段全集、test case、worker、topic、commit boundary 等下游细节。 | 必须无下沉。 |
| 阻塞层级 | 是否清楚区分不阻塞 Step 14、会阻塞 03 / 04、会阻塞实现。 | 必须清楚。 |
| 表格字段 | 正式风险表是否使用“风险 / 影响 / 当前处理口径”,待确认表是否使用“待确认 / 影响范围 / 当前挂起口径”。 | 必须符合。 |

#### R1.19.3 回填阻塞条件

下一批检查若发现以下任一问题,不得进入正式 `§13` 回填记录:

| 阻塞条件 | 处理方式 |
|---|---|
| 草稿条目无法回指本 Step 中间产物。 | 回退到 `正式 §13 回填草稿` 模块补来源映射。 |
| 草稿重新引入旧 `MethodContent` / publish / snapshot / fingerprint / outbox 主线。 | 回退到旧材料差异审计或草稿压缩模块重写。 |
| 草稿新增未经 `R1.10` / `R1.12` / `R1.14` / `R1.16` 裁决的新风险或待确认。 | 回退到风险 / 待确认筛选模块。 |
| 草稿把 schema、port、config key、test evidence schema、artifact schema 写成概要正文细节。 | 删除或后移到 `03`~`07`。 |
| 草稿把已收稳 `§5`~`§12` 结论重新写成待确认。 | 删除该待确认或改写为后续承接说明。 |
| 草稿无法说明当前处理口径或当前挂起口径。 | 不得回填,必须补齐口径。 |

#### R1.19.4 回填放行条件

下一批检查只有同时满足以下条件,才能进入正式 `§13` 回填记录:

1. 风险草稿为 4 条,且均来自 `R1.10` 压缩和 `R1.14` 分层说明。
2. 待确认草稿为 4 条,且均来自 `R1.12` 待确认候选。
3. `R1.16` 旧材料只作为抽象归并和排除依据,没有直接继承旧对象 / 发布 / snapshot / outbox 主线。
4. 草稿没有写入 03 / 04 / 05 / 07 的 schema、port、config、test、implementation 细节。
5. 正式文档尚未被本模块修改,回填动作必须等用户确认后在 `正式 §13 回填记录:再写入` 执行。

#### R1.19.5 下一写入批次边界

下一批 `R1.20 自检与停审:再写入` 只允许写:

1. 回填前自检表。
2. 阻塞条件检查结果。
3. 放行条件检查结果。
4. 本模块停审记录和 `next_allowed_action`。

下一批不得写:

1. 修改正式 `02-概要设计.md`。
2. 正式 `§13` 回填记录。
3. Step 14 装配正文。
4. 新增风险或待确认。

#### R1.19.6 自检

| 检查项 | 结论 | 说明 |
|---|---|---|
| 是否只做自检先思考 | pass | 当前只定义检查维度、阻塞条件、放行条件和下一批边界。 |
| 是否修改正式文档 | no | 未修改正式 `02-概要设计.md`。 |
| 是否执行正式回填 | no | 正式回填仍需用户确认后进入回填记录模块。 |
| 是否进入 Step 14 | no | 当前仍停在 Step 13。 |

next_allowed_action: 等待用户确认后进入 Step 13 `自检与停审:再写入`;只写回填前自检结论、阻塞条件检查和放行条件检查,不得修改正式 `02-概要设计.md`,不得进入 Step 14。

### R1.20 自检与停审:再写入

#### R1.20.1 回填前自检表

| 自检维度 | 检查结果 | 结论 | 说明 |
|---|---|---|---|
| 来源可追溯 | `R1.18.3` 已为每条草稿回指 `R1.10` / `R1.12` / `R1.14` / `R1.16`。 | pass | 草稿没有无来源条目。 |
| 风险 / 待确认分离 | `R1.18.1` 只写已识别风险,`R1.18.2` 只写尚未定论事项。 | pass | 风险与待确认未混写。 |
| 旧污染清理 | 草稿未直接继承 `MethodContent`、PublishMethodContent、snapshot、fingerprint、outbox、ViewProfile、plugin DAG 或 job 权限旧主线。 | pass | 旧材料只作为抽象归并和排除依据。 |
| 概要深度 | 草稿未写 schema、trait、DDL、字段全集、test case、worker、topic、commit boundary。 | pass | 仍保持概要层。 |
| 阻塞层级 | 风险表写清后续 03 / 04 / 实现阻塞口径,待确认表写清当前挂起口径。 | pass | 不阻塞 Step 14,但保留后续门禁。 |
| 表格字段 | 风险表使用“风险 / 影响 / 当前处理口径”,待确认表使用“待确认 / 影响范围 / 当前挂起口径”。 | pass | 符合正式 `§13` 表格字段。 |

#### R1.20.2 阻塞条件检查结果

| 阻塞条件 | 检查结果 | 结论 |
|---|---|---|
| 草稿条目无法回指本 Step 中间产物。 | 未发现。 | pass |
| 草稿重新引入旧 `MethodContent` / publish / snapshot / fingerprint / outbox 主线。 | 未发现。 | pass |
| 草稿新增未经 `R1.10` / `R1.12` / `R1.14` / `R1.16` 裁决的新风险或待确认。 | 未发现。 | pass |
| 草稿把 schema、port、config key、test evidence schema、artifact schema 写成概要正文细节。 | 未发现。 | pass |
| 草稿把已收稳 `§5`~`§12` 结论重新写成待确认。 | 未发现。 | pass |
| 草稿无法说明当前处理口径或当前挂起口径。 | 未发现。 | pass |

#### R1.20.3 放行条件检查结果

| 放行条件 | 检查结果 | 结论 |
|---|---|---|
| 风险草稿为 4 条,且均来自 `R1.10` 压缩和 `R1.14` 分层说明。 | 满足。 | pass |
| 待确认草稿为 4 条,且均来自 `R1.12` 待确认候选。 | 满足。 | pass |
| `R1.16` 旧材料只作为抽象归并和排除依据。 | 满足。 | pass |
| 草稿没有写入 03 / 04 / 05 / 07 细节。 | 满足。 | pass |
| 正式文档尚未被本模块修改,回填动作等用户确认后执行。 | 满足。 | pass |

#### R1.20.4 本模块停审记录

| 检查项 | 结论 | 说明 |
|---|---|---|
| 是否完成回填前自检 | pass | 已检查来源、分离、旧污染、深度、阻塞层级和表格字段。 |
| 是否存在回填阻塞条件 | no | 未发现阻塞条件。 |
| 是否满足回填放行条件 | pass | 可在用户确认后进入正式 `§13` 回填记录。 |
| 是否修改正式文档 | no | 未修改正式 `02-概要设计.md`。 |
| 是否进入 Step 14 | no | 当前仍停在 Step 13。 |

next_allowed_action: 等待用户确认后进入 Step 13 `正式 §13 回填记录:再写入`;允许将 `R1.18` 草稿回填到正式 `02-概要设计.md` 的 `§13`,并记录回填后检查;不得进入 Step 14,Step 14 需在回填记录完成后另行确认。

### R1.21 正式 `§13` 回填记录:再写入

#### R1.21.1 回填动作记录

| 项 | 记录 |
|---|---|
| 用户确认 | 已确认执行正式 `§13` 回填。 |
| 回填来源 | `R1.18 正式 §13 回填草稿:再写入`;`R1.20 自检与停审:再写入`。 |
| 回填目标 | `projects/L3-method-library/02-概要设计.md` 的 `## 13. 设计风险与待确认事项`。 |
| 回填范围 | 整体替换正式 `## 13` 到 `## 14` 之前的内容;同步更新文档版本与变更记录。 |
| 未修改范围 | 未修改正式 `§1`~`§12` 和 `§14` 正文。 |
| 回填方式 | 摘要化回填:保留 `13.1 设计风险` 和 `13.2 待确认事项` 两段结构;完整来源池、旧材料审计、取舍和停审过程仍以本文件 `R1.1`~`R1.20` 为准。 |

#### R1.21.2 回填后检查

| 检查项 | 结论 | 说明 |
|---|---|---|
| 正式 `§13` 是否已回填 | pass | 正式 `02-概要设计.md` 的 `§13` 已按 `R1.18` / `R1.20` 整体替换。 |
| 是否只替换 `§13` 正文 | pass | 本次正文回填目标限定在 `## 13` 到 `## 14` 之前;另同步文档元信息版本和变更记录。 |
| 是否移除旧 `MethodContent` / publish / snapshot / fingerprint / outbox 主线 | pass | 正式 `§13` 已移除旧风险和待确认表中的旧对象与旧发布主线。 |
| 是否保持 `13.1` / `13.2` 结构 | pass | 正式 `§13` 保持设计风险和待确认事项两段结构。 |
| 是否保持概要层粒度 | pass | 正式 `§13` 未写入 schema、trait、DDL、worker、topic、配置 key、test case 或实施边界。 |
| 是否保留后续 03 / 04 / 实现门禁 | pass | 正式风险表保留详细设计、配置设计和实现端私补的阻塞口径。 |
| 是否提前进入 Step 14 | pass | 本次只完成正式 `§13` 回填,未创建或写入 Step 14。 |

#### R1.21.3 后续风险保留

| 风险 | 当前状态 | 后续要求 |
|---|---|---|
| Step 14 装配时新增未确认结论 | active_gate | Step 14 只能整理正式 `§1`~`§14`、校准来源和一致性,不得新增风险 / 待确认或改写 `§13`。 |
| 旧 `MethodContent` / publish / snapshot / outbox 主线借 Step 14 回流 | blocked_by_rule | Step 14 只能沿用当前正式 `§13`,不得重新吸收旧正式 `§13`。 |
| 详细设计或配置设计削弱 `§12` / `§13` 红线 | active_gate | 后续 `03` / `04` 若需要改主语、owner、接口、流程或配置职责,必须回退概要对应 Step。 |
| 实现端缺 schema / port / state / config / evidence / artifact schema 时私补 | active_gate | 后续实施必须回设计闭口,不得自行发明占位规则。 |

#### R1.21.4 本模块最终裁决

| 裁决项 | 结论 | 说明 |
|---|---|---|
| Step 13 中间产物 | completed | `R1.1`~`R1.20` 已闭合来源基线、风险筛选、待确认筛选、旧材料审计、正式草稿和停审裁决。 |
| 正式 `§13` | backfilled | 正式 `§13` 已按 `R1.18` / `R1.20` 回填。 |
| Step 13 blocker | none | 当前 Step 13 无遗留 blocker。 |
| 下一步 | ready_for_step14_opening | 下一步只能等待用户确认后进入 Step 14 `开工与必读文档:先思考`,不得直接装配正式文档。 |

next_allowed_action: 等待用户确认后进入 Step 14 `开工与必读文档:先思考`;Step 14 必须以正式 `§1`~`§13` 回填后文本、Step 13 `R1.1`~`R1.21`、Step 12 `R1.23`、Step 11 `R1.25`、Step 10 `R1.27`、Step 9 `R1.31`、Step 8 `R1.33`、Step 7 `R1.45`、Step 5 / Step 6 当前结论为输入基线,不得沿用旧 formal assembly 或 historical Step 13 作为第一来源。

# Step 14. 整理正式概要设计文档

> 对应 SOP: `standards/document/概要设计讨论流程_SOP.md` Step 14
> 回填章节: `projects/L3-method-library/02-概要设计.md` 全文
> 创建日期: 2026-06-21
> 状态: completed
> 当前模块: `R1.17 正式全文回填记录:再写入`
> 当前门禁: Step 14 completed;正式 `02-概要设计.md` completed;等待后续文档启动门禁

---

## R1.1 开工与必读文档:先思考

### 1. 本模块要回答的问题

Step 14 不是重新讨论概要设计内容,而是把 Step 1~Step 13 已确认并已回填到正式 `02-概要设计.md` 的结论重新装配、核对和收束。

本模块只回答以下问题:

| 问题 | 本轮判断 |
|---|---|
| Step 14 的工作对象是什么? | 正式 `02-概要设计.md` 全文,尤其是 §1~§13 已回填内容、§14 参考、校准来源、术语一致性、旧材料残留和跨章节连贯性。 |
| Step 14 是否可以新增概要结论? | 不可以。只能重组、压缩、统一术语、补校准来源、补参考、修正文档级不一致。 |
| Step 14 是否可以直接改正式文档? | 不能从本模块直接开始。必须先列必读文档、装配边界、模块顺序和门禁,再经用户确认进入 `再写入`。 |
| Step 14 是否沿用旧 `02_hld_step_13_formal_document_assembly.md`? | 不沿用。旧文件只作历史差异审计对象,本轮 Step 14 以当前 `02_hld_step_14_formal_document_assembly.md` 为唯一中间产物。 |
| Step 14 的完成标准是什么? | 正式 02 全文可追溯、章节主链完整、§1~§13 不互相冲突、§14 只列实际使用材料,且没有把旧材料或详细设计内容伪装成概要结论。 |

### 2. 必读文档

#### 2.1 流程与规范

| 文档 | 读取目的 | 使用边界 |
|---|---|---|
| `projects/L3-method-library/design-calibration/project_execution_ledger.md` | 确认项目级恢复点、当前文档和下一动作。 | 只作为恢复门禁,不产生概要结论。 |
| `projects/L3-method-library/design-calibration/02_hld_calibration_flow.md` | 确认 Step 1~13 完成状态和 Step 14 当前门禁。 | 作为文档级 flow 真相源。 |
| `standards/document/概要设计讨论流程_SOP.md` | 确认 Step 14 的生成流程和不得跳步规则。 | 只采用流程规则。 |
| `standards/document/概要设计书写规范.md` | 确认正式 14 章主链、校准来源、延伸阅读、图表和禁止下沉内容。 | 作为正式正文装配规则。 |
| `standards/document/设计文档讨论中间产物规范.md` | 确认三层台账、先思考后写入、单批写入规模和未来 Step 不提前落盘规则。 | 作为本文件写入与回填门禁。 |
| `standards/document/设计文档编写通则.md` | 确认通用文风、层级和引用纪律。 | 只用于最终文档级润色自检。 |

#### 2.2 本仓正式输入

| 文档 | 读取目的 | 使用边界 |
|---|---|---|
| `projects/L3-method-library/00-需求文档.md` | 复核概要 §1~§3 是否仍承接新版需求边界。 | 不重新讨论需求。 |
| `projects/L3-method-library/01-架构设计.md` | 复核概要 §1~§5 是否仍承接新版架构边界。 | 不重新讨论架构。 |
| `projects/L3-method-library/02-概要设计.md` | Step 14 正式装配目标。 | 只装配已确认结论,不得新增未讨论判断。 |

#### 2.3 Step 1~13 中间产物

| 中间产物 | 读取目的 | Step 14 关注点 |
|---|---|---|
| `02_hld_step_01_upstream_boundary.md` | §1 来源。 | 上游承接、历史材料降级、非范围是否一致。 |
| `02_hld_step_02_scope.md` | §2 来源。 | 本次概要目标、范围、非范围是否未漂移。 |
| `02_hld_step_03_constraints.md` | §3 来源。 | 约束是否仍能约束对象、接口、流程和状态。 |
| `02_hld_step_04_code_subject_framework.md` | §4 来源。 | 代码主体框架是否与后续 §5~§9 一致。 |
| `02_hld_step_05_components_boundary.md` | §5 来源。 | 主要组成部分、职责、边界、对象候选入口是否仍是主轴。 |
| `02_hld_step_06_key_objects.md` 与 Step 6 附录 | §6 来源。 | 关键对象是否来自 §5,对象细节是否未下沉到详细 schema。 |
| `02_hld_step_07_api_interface_skeleton.md#R1.45` | §7 来源。 | API / 接口骨架是否与对象和处理流一致。 |
| `02_hld_step_08_processing_flows.md#R1.33` | §8 来源。 | 处理流族是否覆盖主要接口和状态触发。 |
| `02_hld_step_09_state_machine.md#R1.31` | §9 来源。 | 状态 owner、状态传播和禁止迁移是否与 §8 对齐。 |
| `02_hld_step_10_exceptions_boundaries.md#R1.27` | §10 来源。 | 异常边界是否不反写 truth、不私补 schema。 |
| `02_hld_step_11_configuration_impact.md#R1.25` | §11 来源。 | 配置影响是否只停留在概要轮廓,不写配置项 schema。 |
| `02_hld_step_12_detailed_design_handoff.md#R1.23` | §12 来源。 | 详细设计承接清单是否完整承接 §5~§11。 |
| `02_hld_step_13_risks_open_questions.md#R1.21` | §13 来源。 | 风险和待确认事项是否仍为未闭口事项,没有被改写成结论。 |

#### 2.4 参考框架

| 文档 | 读取目的 | 使用边界 |
|---|---|---|
| `projects/L1-governance/design-calibration/02_hld_step_14_formal_document_assembly.md` | 学习 Step 14 的组织框架:目标、章节重组、术语统一、交叉引用、参考材料和进入下一步条件。 | 只参考结构和深度,不得复制 governance 领域语义。 |
| `projects/L3-method-library/design-calibration/02_hld_step_13_formal_document_assembly.md` | 旧历史装配材料。 | 只作旧材料差异审计,不得作为当前 Step 14 真相源。 |

### 3. 历史材料处理口径

| 材料 | 当前定位 | Step 14 使用方式 |
|---|---|---|
| 旧 `02_hld_step_13_formal_document_assembly.md` | historical_material | 仅用于审计旧 §14 或旧装配口径是否仍残留在正式文档。 |
| 旧 `02-概要设计.md` 中未被 Step 1~13 覆盖的段落 | historical_residue_candidate | 逐章检查,能追溯到当前 Step 1~13 的保留,不能追溯的标记为旧残留并修正。 |
| 历史 `03_ddd_*` | historical_material | 不反推概要,仅在识别详细设计下沉风险时作为污染检查对象。 |
| 历史 `04~07` | historical_material | 不作为概要正式结论来源。 |

### 4. Step 14 模块顺序

Step 14 采用与 `L1-governance` 相同的装配类框架,但必须按 `L3-method-library` 当前中间产物重新收敛。

| 顺序 | 模块 | 产物 | 门禁 |
|---:|---|---|---|
| R1.1 | 开工与必读文档:先思考 | 本节 | 只列输入、边界和模块顺序。 |
| R1.2 | 开工与必读文档:再写入 | 开工记录、Step 内计划、读取状态表 | 用户确认后写入,不得改正式正文。 |
| R1.3 | L1-governance 框架对齐:先思考 | 可借鉴框架与不得借鉴内容 | 只抽象结构,不复制领域内容。 |
| R1.4 | L1-governance 框架对齐:再写入 | Step 14 框架对齐记录 | 确定本仓 Step 14 输出章节。 |
| R1.5 | 正式章节来源矩阵:先思考 | §1~§14 来源映射草案 | 每章必须定位到当前中间产物。 |
| R1.6 | 正式章节来源矩阵:再写入 | 正式章节来源矩阵 | 形成正式装配依据。 |
| R1.7 | 全文一致性扫描:先思考 | 术语、主语、边界、旧残留扫描计划 | 不直接大改正文。 |
| R1.8 | 全文一致性扫描:再写入 | 扫描结果与修正清单 | 标明哪些进入正式修正批次。 |
| R1.9 | 正式全文装配草稿:先思考 | 装配策略与批次计划 | 明确每批修改范围不超过审查规模。 |
| R1.10 | 正式全文装配草稿:再写入 | 正式 02 修正批次 1 或装配草稿 | 经确认后才改正式正文。 |
| R1.11 | 正式 §14 参考与交叉引用:先思考 | 参考材料和校准来源修正策略 | 只列实际使用材料。 |
| R1.12 | 正式 §14 参考与交叉引用:再写入 | §14 和校准来源修正 | 补齐追溯入口。 |
| R1.13 | 旧材料差异审计:先思考 | 历史装配文件与旧术语审计计划 | 不让旧材料反推当前结论。 |
| R1.14 | 旧材料差异审计:再写入 | 差异审计结论 | 明确保留、删除、替换。 |
| R1.15 | 自检与停审:先思考 | 自检清单与停审条件 | 检查未新增结论、未下沉详细设计。 |
| R1.16 | 自检与停审:再写入 | Step 14 停审记录 | 允许正式关闭 Step 14。 |
| R1.17 | 正式全文回填记录:再写入 | 正式 02 装配完成记录 | 更新 flow / 台账到后续 `03-详细设计` 等待状态。 |

### 5. Step 14 装配边界

#### 5.1 允许做

- 统一正式 `02-概要设计.md` 的章节主链和校准来源。
- 清理旧术语、旧主语和旧材料残留。
- 调整同一结论在不同章节中的表达顺序和轻重。
- 补齐 §14 参考材料及其用途。
- 把 Step 1~13 已确认结论压缩为正式正文可读结构。
- 把过程性诊断、取舍和待确认保留在中间产物,不搬入正式正文。

#### 5.2 禁止做

- 新增未在 Step 1~13 讨论确认的概要结论。
- 用历史 `03_ddd_*` 反推新增对象、接口、流程或状态。
- 把详细字段全集、完整函数签名、协议 schema、DDL、配置项 JSON、实施计划或测试证据写入正式概要。
- 为了压缩正式文档而删除必要的主要组成部分、对象、接口、流程、状态、异常、配置影响或承接清单。
- 把 §13 未闭口事项改写成已确认设计结论。

### 6. 下一写入边界

下一步只允许进入:

```text
R1.2 开工与必读文档:再写入
```

`R1.2` 应写入:

- Step 14 开工记录。
- 必读文档读取状态表。
- Step 内计划确认表。
- 当前不修改正式正文的门禁说明。
- 下一模块 `R1.3 L1-governance 框架对齐:先思考`。

`R1.2` 不得写入:

- 正式 `02-概要设计.md` 正文修改。
- §14 参考正式改写。
- 旧材料差异审计结论。
- Step 14 停审结论。

### 7. 自检

| 检查项 | 结果 |
|---|---|
| 是否只进入 Step 14 第一个模块? | 是。 |
| 是否未修改正式 `02-概要设计.md` 正文? | 是。 |
| 是否明确旧 Step 13 formal assembly 只是历史材料? | 是。 |
| 是否列出 Step 14 必读文档? | 是。 |
| 是否给出 Step 14 模块顺序? | 是。 |
| 是否明确下一步只允许 `R1.2`? | 是。 |

---

## R1.2 开工与必读文档:再写入

### 1. 开工记录

| 项目 | 记录 |
|---|---|
| 当前 Step | Step 14 整理正式概要设计文档 |
| 当前模块 | `R1.2 开工与必读文档:再写入` |
| 上一模块 | `R1.1 开工与必读文档:先思考` |
| 用户确认 | 已确认进入 `R1.2` |
| 本模块允许写入 | Step 14 开工记录、读取状态表、Step 内计划、门禁说明 |
| 本模块禁止写入 | 正式 `02-概要设计.md` 正文、正式 §14 参考、旧材料差异审计结论、Step 14 停审结论 |
| 下一模块 | `R1.3 L1-governance 框架对齐:先思考` |

本模块确认 Step 14 已正式开工,但当前仍处于装配前准备阶段。正式 `02-概要设计.md` 的正文修改必须等到后续“正式全文装配草稿”和“正式 §14 参考与交叉引用”模块到达后,再按用户确认分批执行。

### 2. 必读文档读取状态表

#### 2.1 已完成读取

| 文档 | 状态 | 本模块使用结论 |
|---|---|---|
| `project_execution_ledger.md` | read | 项目级恢复点允许进入 Step 14 `R1.2`,且禁止跳过当前模块。 |
| `02_hld_calibration_flow.md` | read | 文档级 flow 已指向 Step 14 `R1.2`,Step 1~13 均已完成正式回填。 |
| `02_hld_step_14_formal_document_assembly.md#R1.1` | read | `R1.1` 已固定 Step 14 目标、必读文档、历史材料处理、模块顺序和下一写入边界。 |
| `standards/document/概要设计讨论流程_SOP.md` | read | Step 14 只做正式文档整理,不得跳过模块或新增未经确认的概要结论。 |
| `standards/document/概要设计书写规范.md` | read | 正式概要必须保持 14 章主链、校准来源、延伸阅读和禁止下沉边界。 |
| `standards/document/设计文档讨论中间产物规范.md` | read | 本轮继续采用三层门禁、模块级先思考后写入、单批写入规模控制。 |
| `projects/L1-governance/design-calibration/02_hld_step_14_formal_document_assembly.md` | read | 只参考 Step 14 的装配框架,不复制 governance 领域语义。 |

#### 2.2 后续模块到达时读取

| 文档 | 读取时机 | 用途 |
|---|---|---|
| `projects/L3-method-library/02-概要设计.md` 全文 | `R1.5` 起逐章读取;`R1.7` 起全文扫描 | 正式章节来源矩阵、全文一致性扫描、正式装配草稿。 |
| `02_hld_step_01_upstream_boundary.md` ~ `02_hld_step_04_code_subject_framework.md` | `R1.5` 章节来源矩阵 | 校验 §1~§4 是否仍回指当前中间产物。 |
| `02_hld_step_05_components_boundary.md` | `R1.5` 与 `R1.7` | 校验 §5 是否仍是主要组成部分主轴,并支撑 §6~§9。 |
| `02_hld_step_06_key_objects.md` 与 Step 6 附录 | `R1.5` 与 `R1.7` | 校验 §6 对象正式化、对象候选池和延伸阅读关系。 |
| `02_hld_step_07_api_interface_skeleton.md#R1.45` | `R1.5` 与 `R1.7` | 校验 §7 接口骨架是否与对象、处理流一致。 |
| `02_hld_step_08_processing_flows.md#R1.33` | `R1.5` 与 `R1.7` | 校验 §8 处理流族与接口、状态触发关系。 |
| `02_hld_step_09_state_machine.md#R1.31` | `R1.5` 与 `R1.7` | 校验 §9 状态 owner、状态传播和禁止迁移。 |
| `02_hld_step_10_exceptions_boundaries.md#R1.27` | `R1.5` 与 `R1.7` | 校验 §10 异常边界和 degraded / fallback 口径。 |
| `02_hld_step_11_configuration_impact.md#R1.25` | `R1.5` 与 `R1.7` | 校验 §11 是否只保留配置影响轮廓。 |
| `02_hld_step_12_detailed_design_handoff.md#R1.23` | `R1.5` 与 `R1.7` | 校验 §12 承接清单是否覆盖 §5~§11。 |
| `02_hld_step_13_risks_open_questions.md#R1.21` | `R1.5` 与 `R1.7` | 校验 §13 风险和待确认事项未被改写成结论。 |
| `02_hld_step_13_formal_document_assembly.md` | `R1.13` | 旧材料差异审计,只查残留,不作真相源。 |
| `standards/document/设计文档编写通则.md` | `R1.7` 和 `R1.15` | 文风、层级、引用和正式文档一致性自检。 |

### 3. Step 内计划确认表

| 顺序 | 模块 | 状态 | 完成门禁 |
|---:|---|---|---|
| R1.1 | 开工与必读文档:先思考 | completed | 已列出 Step 14 的输入、边界、模块顺序和下一写入边界。 |
| R1.2 | 开工与必读文档:再写入 | completed | 已记录开工、读取状态、Step 内计划和下一模块。 |
| R1.3 | L1-governance 框架对齐:先思考 | next | 只分析可借鉴框架和不得借鉴内容,不得改正式正文。 |
| R1.4 | L1-governance 框架对齐:再写入 | pending | 固化本仓 Step 14 装配框架。 |
| R1.5 | 正式章节来源矩阵:先思考 | pending | 形成 §1~§14 来源映射草案。 |
| R1.6 | 正式章节来源矩阵:再写入 | pending | 写入正式章节来源矩阵。 |
| R1.7 | 全文一致性扫描:先思考 | pending | 设计术语、主语、边界、旧残留扫描计划。 |
| R1.8 | 全文一致性扫描:再写入 | pending | 写入扫描结果和修正清单。 |
| R1.9 | 正式全文装配草稿:先思考 | pending | 制定正式正文修改批次计划。 |
| R1.10 | 正式全文装配草稿:再写入 | pending | 按确认批次修改正式正文。 |
| R1.11 | 正式 §14 参考与交叉引用:先思考 | pending | 制定 §14 与校准来源修正策略。 |
| R1.12 | 正式 §14 参考与交叉引用:再写入 | pending | 补齐 §14 与必要交叉引用。 |
| R1.13 | 旧材料差异审计:先思考 | pending | 制定旧装配文件和旧术语审计计划。 |
| R1.14 | 旧材料差异审计:再写入 | pending | 写入差异审计结论。 |
| R1.15 | 自检与停审:先思考 | pending | 制定最终自检与停审条件。 |
| R1.16 | 自检与停审:再写入 | pending | 写入 Step 14 停审记录。 |
| R1.17 | 正式全文回填记录:再写入 | pending | 记录正式 02 装配完成,更新 flow / 台账到后续文档入口。 |

### 4. 当前门禁说明

本模块完成后,三层门禁应更新为:

```text
项目级台账: 等待用户确认 Step 14 R1.3 L1-governance 框架对齐:先思考
文档级 flow: 等待用户确认 Step 14 R1.3 L1-governance 框架对齐:先思考
Step 文件: R1.2 completed,下一模块 R1.3
```

下一模块只允许分析 `L1-governance` Step 14 的结构借鉴点。不得在 `R1.3` 修改正式 `02-概要设计.md`,也不得开始章节来源矩阵或全文扫描。

### 5. 自检

| 检查项 | 结果 |
|---|---|
| 是否按用户确认进入 `R1.2`? | 是。 |
| 是否只写入开工记录、读取状态和 Step 内计划? | 是。 |
| 是否未修改正式 `02-概要设计.md` 正文? | 是。 |
| 是否区分已读文档和后续模块读取文档? | 是。 |
| 是否把下一步限制为 `R1.3`? | 是。 |

---

## R1.3 L1-governance 框架对齐:先思考

### 1. 本模块要回答的问题

本模块只分析 `L1-governance` Step 14 的结构价值,不迁移其领域内容。目标是确认 `L3-method-library` Step 14 应该借鉴怎样的装配框架,以及哪些内容必须按本仓当前 Step 1~13 重新生成。

| 问题 | 判断 |
|---|---|
| `L1-governance` Step 14 可借鉴什么? | 可借鉴“目标 -> 正式章节重组 -> 章节回填 -> 术语统一 -> 交叉引用 -> 参考材料 -> 进入下一步条件”的装配框架。 |
| 哪些内容不能借鉴? | 不能借鉴 governance 的对象、术语、风险、上下游材料、参考列表和正式章节结论。 |
| method-library 是否需要完全照搬 L1-governance 的简短 Step 14? | 不应照搬。method-library Step 5 以后曾被裁决为重点内容,Step 14 必须增加全文一致性扫描、旧残留审计和正式装配批次控制。 |
| 当前模块是否可以开始章节来源矩阵? | 不可以。章节来源矩阵属于 `R1.5/R1.6`,当前只确定框架取舍。 |
| 当前模块是否可以修改正式 `02-概要设计.md`? | 不可以。正式正文修改最早到 `R1.10` 才允许按确认批次执行。 |

### 2. L1-governance Step 14 框架拆解

`L1-governance` Step 14 的结构可以拆成七个层次:

| 层次 | L1-governance 表达 | 对 method-library 的意义 |
|---|---|---|
| 本步目标 | 把 Step 1~13 已确认结论按 14 章正式结构重组。 | method-library 也必须只整理已确认结论,不得新增概要判断。 |
| 正式概要设计文档重组结论 | 列出 §1~§14 各章来源和重组方式。 | method-library 需要形成自己的 §1~§14 来源矩阵,但来源必须是本仓 Step 1~13。 |
| 章节回填结论 | 列出每个中间产物回填到哪个正式章节。 | method-library 需要把 Step 6 附录、Step 7~13 回填记录纳入映射,避免正式文档只看主控文件。 |
| 术语统一结论 | 汇总治理领域主术语。 | method-library 需要独立汇总本仓术语,例如 method asset、method definition、catalog、consumer、trace、projection、package / method set 等,不得复用治理术语。 |
| 交叉引用结论 | 每章保留校准来源,§14 只列实际使用材料,不机械复制。 | method-library 也必须补齐校准来源和延伸阅读,同时检查旧材料残留。 |
| 参考材料表 | 列出规范、上游正式文档、校准文件和上下游仓材料。 | method-library 的 §14 必须只列当前实际使用材料,不能保留旧参考或未读取材料。 |
| 进入下一步条件 | 正式概要已完成,可进入 03 详细设计。 | method-library 必须在 Step 14 停审后才允许进入 03,且必须先关闭旧残留和未追溯章节问题。 |

### 3. 可借鉴框架

| 可借鉴项 | 采用方式 | method-library 适配 |
|---|---|---|
| 装配目标先行 | 保留 | Step 14 先说明只做重组、压缩、润色、术语统一、交叉引用补齐。 |
| §1~§14 来源表 | 保留并增强 | 不只列 Step 编号,还要列当前文件、关键回填记录和是否需要附录延伸阅读。 |
| 中间产物到正式章节映射 | 保留并增强 | Step 6 附录和 Step 5~9 小循环产物必须进入映射,避免重点章节被压缩。 |
| 术语统一表 | 保留 | 术语必须来自 method-library 当前 Step 5~13,重点检查旧术语和正式主语漂移。 |
| 交叉引用结论 | 保留 | 每个正式章节必须有具体校准来源,§14 只列实际使用材料。 |
| 参考材料表 | 保留 | 必须区分规范、上游正式输入、本仓中间产物、历史审计材料。 |
| 进入下一步条件 | 保留并增强 | 除正式 02 完成外,还必须记录旧材料差异审计和三层门禁状态。 |

### 4. 不得借鉴内容

| 不得借鉴项 | 原因 | method-library 替代口径 |
|---|---|---|
| Governance truth / Gate / Decision 等领域术语 | 属于治理仓领域模型。 | 使用 method-library 当前术语,例如方法资产、目录、正式化、受控消费、追溯、分发、外围包等。 |
| L1-governance 的 10 个组成部分数量和名称 | method-library 的组成部分由当前 Step 5 决定。 | 回读 Step 5 当前结论。 |
| L1-governance 的对象类别和状态组 | 对象 / 状态必须从 method-library Step 6 / Step 9 得出。 | 回读 Step 6 主控与附录、Step 9 当前结论。 |
| L1-governance 的上下游参考材料 | 上下游依赖不同。 | 以 method-library `00/01` 和当前概要 Step 1~13 为第一来源。 |
| L1-governance 的“已完成”状态 | method-library Step 14 尚未完成。 | 当前只到框架对齐阶段,不得声明正式装配完成。 |

### 5. method-library Step 14 应增强的点

`L1-governance` Step 14 是完成态记录,比较精简。method-library 当前上下文不同,需要增强以下检查:

| 增强点 | 原因 | 对应后续模块 |
|---|---|---|
| 章节来源矩阵必须更细 | Step 5 以后内容被裁决为重点,且 Step 6 有多个附录。 | `R1.5/R1.6` |
| 全文一致性扫描必须独立 | 正式 02 中仍可能保留旧术语、旧对象和旧参考。 | `R1.7/R1.8` |
| 正式全文装配必须分批 | 正式文档较长,不能一次性重写导致审查困难。 | `R1.9/R1.10` |
| §14 参考与交叉引用必须独立 | 参考章节容易保留旧材料或列入未实际使用材料。 | `R1.11/R1.12` |
| 旧材料差异审计必须独立 | 本轮是 full-restart,旧 Step 13 formal assembly 不能作为真相源。 | `R1.13/R1.14` |
| 自检与停审必须保留 | 必须证明没有新增结论、没有下沉详细设计、没有跳过三层门禁。 | `R1.15/R1.16` |

### 6. 当前取舍

本轮 Step 14 不采用“一次性完成态记录”的写法,而采用“装配前框架 -> 来源矩阵 -> 一致性扫描 -> 正式装配 -> §14 / 引用 -> 旧材料审计 -> 自检停审”的分段流程。

取舍理由:

- method-library 当前正式文档经历了 Step 5 以后多次重写和回填,需要先查来源再装配。
- Step 6 附录和 Step 7~13 回填记录较多,不能只用一张简表带过。
- 当前仍有旧材料残留风险,必须在装配后独立审计。
- 用户要求一步步确认,所以每个装配模块都必须先思考、再写入。

### 7. 下一写入边界

下一步只允许进入:

```text
R1.4 L1-governance 框架对齐:再写入
```

`R1.4` 应写入:

- `L1-governance` 可借鉴框架确认表。
- method-library Step 14 本轮采用的输出章节。
- 不得借鉴内容和防污染规则。
- 下一模块 `R1.5 正式章节来源矩阵:先思考` 的进入条件。

`R1.4` 不得写入:

- 正式章节来源矩阵正文。
- 全文一致性扫描结果。
- 正式 `02-概要设计.md` 修改。
- 旧材料差异审计结论。

### 8. 自检

| 检查项 | 结果 |
|---|---|
| 是否只参考 L1-governance 框架而非领域内容? | 是。 |
| 是否未修改正式 `02-概要设计.md`? | 是。 |
| 是否未提前进入章节来源矩阵? | 是。 |
| 是否明确 method-library 需要增强的 Step 14 检查点? | 是。 |
| 是否把下一步限制为 `R1.4`? | 是。 |

---

## R1.4 L1-governance 框架对齐:再写入

### 1. 框架确认记录

| 项目 | 结论 |
|---|---|
| 当前模块 | `R1.4 L1-governance 框架对齐:再写入` |
| 上一模块 | `R1.3 L1-governance 框架对齐:先思考` |
| 用户确认 | 已确认进入 `R1.4` |
| 本模块性质 | 固化 Step 14 装配框架,不生成正式正文修改。 |
| 下一模块 | `R1.5 正式章节来源矩阵:先思考` |

本模块确认: `L1-governance` Step 14 只作为装配结构参考。`L3-method-library` 的正式章节来源、术语、参考材料和进入下一步条件必须从本仓当前 Step 1~13 与正式 `00/01/02` 重新生成。

### 2. 可借鉴框架确认表

| 可借鉴框架 | 采用状态 | 本仓落点 |
|---|---|---|
| 本步目标 | adopt | Step 14 只做正式文档重组、压缩、术语统一、交叉引用补齐和旧残留清理。 |
| 正式章节重组结论 | adopt_and_extend | 后续 `R1.5/R1.6` 形成 §1~§14 来源矩阵,并标明关键回填记录。 |
| 章节回填结论 | adopt_and_extend | 后续必须覆盖 Step 6 附录、Step 7~13 回填记录,不能只列主控文件。 |
| 术语统一结论 | adopt_structure_only | 后续 `R1.7/R1.8` 独立扫描 method-library 当前术语和旧术语残留。 |
| 交叉引用结论 | adopt | 正式每章必须保留具体校准来源和延伸阅读入口。 |
| 参考材料表 | adopt | 后续 `R1.11/R1.12` 只列实际使用材料,并说明用途。 |
| 进入下一步条件 | adopt_and_extend | Step 14 停审后才允许进入 `03-详细设计`,并必须关闭旧残留审计与三层门禁。 |

### 3. 本仓 Step 14 输出结构

| 输出区块 | 对应模块 | 输出内容 | 是否修改正式 02 |
|---|---|---|---|
| 开工与必读文档 | `R1.1/R1.2` | Step 14 输入、边界、读取状态、Step 内计划。 | 否 |
| 框架对齐 | `R1.3/R1.4` | 借鉴框架、不得借鉴内容、防污染规则、本仓输出结构。 | 否 |
| 正式章节来源矩阵 | `R1.5/R1.6` | §1~§14 对应中间产物、回填记录和延伸阅读来源。 | 否 |
| 全文一致性扫描 | `R1.7/R1.8` | 术语、主语、边界、旧残留、详细设计下沉风险扫描结果。 | 否 |
| 正式全文装配草稿 | `R1.9/R1.10` | 分批修正文档正文,只改已确认的装配问题。 | 是 |
| §14 参考与交叉引用 | `R1.11/R1.12` | 参考材料、用途、校准来源、延伸阅读入口。 | 是 |
| 旧材料差异审计 | `R1.13/R1.14` | 历史 formal assembly 和旧术语残留审计结论。 | 视审计结果 |
| 自检与停审 | `R1.15/R1.16` | 未新增结论、未下沉详细设计、三层门禁闭合。 | 否 |
| 正式全文回填记录 | `R1.17` | 记录正式 02 装配完成,推进 flow / 台账到后续文档入口。 | 否 |

### 4. 防污染规则

| 污染来源 | 禁止行为 | 允许行为 |
|---|---|---|
| `L1-governance` 领域内容 | 复制 governance 对象、状态、术语、上下游材料或风险事项。 | 只复制装配类章节组织方式。 |
| 旧 `02_hld_step_13_formal_document_assembly.md` | 把旧完成态记录当成本轮 Step 14 真相源。 | 在 `R1.13/R1.14` 作为历史差异审计对象。 |
| 历史 `03_ddd_*` | 反推新增对象、接口、状态、配置或详细 schema。 | 在识别详细设计下沉风险时作为污染检查对象。 |
| 正式 `02-概要设计.md` 旧残留 | 未追溯就保留旧术语和旧结论。 | 通过来源矩阵和一致性扫描确认保留、替换或删除。 |
| Step 13 待确认事项 | 整理时改写成已确认结论。 | 保持为风险 / 待确认事项,直至后续文档闭口。 |

### 5. 后续模块进入条件

进入 `R1.5 正式章节来源矩阵:先思考` 前,必须满足:

| 条件 | 状态 |
|---|---|
| `R1.4` 已确认本仓 Step 14 输出结构。 | pass |
| 已明确 `L1-governance` 只可作框架参考。 | pass |
| 已明确正式正文修改最早到 `R1.10`。 | pass |
| 已明确来源矩阵必须覆盖 §1~§14、Step 6 附录和 Step 7~13 回填记录。 | pass |
| 已明确下一步只做来源矩阵先思考,不扫描全文、不改正式正文。 | pass |

### 6. 下一写入边界

下一步只允许进入:

```text
R1.5 正式章节来源矩阵:先思考
```

`R1.5` 应思考:

- §1~§14 每章需要从哪些当前中间产物追溯。
- 哪些章节必须读取回填记录而不只读主控文件。
- Step 6 附录如何进入正式 §6 的延伸阅读关系。
- §14 参考材料如何避免旧材料和未实际使用材料混入。

`R1.5` 不得写入:

- 正式章节来源矩阵定稿。
- 全文一致性扫描结果。
- 正式 `02-概要设计.md` 修改。
- 旧材料差异审计结论。

### 7. 自检

| 检查项 | 结果 |
|---|---|
| 是否按用户确认进入 `R1.4`? | 是。 |
| 是否只固化框架确认,未进入来源矩阵? | 是。 |
| 是否未修改正式 `02-概要设计.md`? | 是。 |
| 是否明确防污染规则? | 是。 |
| 是否把下一步限制为 `R1.5`? | 是。 |

---

## R1.5 正式章节来源矩阵:先思考

### 1. 本模块要回答的问题

本模块只设计“正式章节来源矩阵”如何生成,不写定稿矩阵,不修改正式 `02-概要设计.md`。

| 问题 | 判断 |
|---|---|
| 来源矩阵要解决什么问题? | 证明正式 `02-概要设计.md` §1~§14 每章都能回到当前 Step 1~13 中间产物、正式 `00/01` 或 Step 14 装配记录。 |
| 来源矩阵是否等于全文一致性扫描? | 不是。来源矩阵只回答“本章从哪里来”;旧术语、旧残留和正文冲突扫描留到 `R1.7/R1.8`。 |
| 来源矩阵是否可以直接改正式正文? | 不可以。正文修改最早到 `R1.10`。 |
| 来源矩阵是否只列 Step 文件名? | 不够。需要列正式章节、主来源、关键回填记录、延伸阅读、特殊审查点和是否需要后续扫描。 |
| Step 6 附录如何处理? | 不能只写 `02_hld_step_06_key_objects.md`;必须列出对象附录作为 §6 延伸阅读和追溯入口。 |
| §14 参考如何处理? | §14 的来源是 Step 14 装配过程和实际读取材料,不能沿用旧参考表或列入未使用材料。 |

### 2. 矩阵字段设计

`R1.6` 的正式章节来源矩阵应使用以下字段:

| 字段 | 用途 | 约束 |
|---|---|---|
| 正式章节 | 标明 `§1`~`§14`。 | 必须覆盖 14 章,不得漏章。 |
| 当前章节主题 | 摘要本章在正式概要中的职责。 | 只写章节主题,不新增设计结论。 |
| 第一来源 | 本章最主要的当前中间产物或正式输入。 | 必须是当前轮次材料,不得用 historical 文件做第一来源。 |
| 关键回填记录 | 标明本章是否已有回填记录或完成记录。 | Step 5~13 必须尽量定位到对应 `0R` / `R1` 记录。 |
| 延伸阅读 | 读者需要继续看的中间产物小节或附录。 | Step 6 必须包含对象附录;Step 5~9 应保留重点中间产物入口。 |
| 特殊审查点 | 本章在 Step 14 后续扫描中需要重点检查的风险。 | 用于 `R1.7/R1.8`,不在本模块裁决。 |
| R1.6 处理方式 | `keep_source` / `needs_source_patch` / `needs_reference_patch` / `scan_later`。 | 只标处理类型,不直接执行正文修改。 |

### 3. 读取顺序策略

`R1.6` 写矩阵前应按以下顺序读取,避免旧材料反推:

| 顺序 | 材料 | 目的 |
|---:|---|---|
| 1 | 正式 `02-概要设计.md` 目录和 §1~§14 标题 | 确认当前正式章节主链。 |
| 2 | `02_hld_calibration_flow.md` 与 `project_execution_ledger.md` | 确认 Step 1~13 当前完成状态和 Step 14 当前门禁。 |
| 3 | `02_hld_step_01_upstream_boundary.md` ~ `02_hld_step_04_code_subject_framework.md` | 给 §1~§4 提供来源。 |
| 4 | `02_hld_step_05_components_boundary.md` | 给 §5 以及 §6~§9 的后续主轴提供来源。 |
| 5 | `02_hld_step_06_key_objects.md` 与 Step 6 五个对象附录 | 给 §6 提供主控和延伸阅读来源。 |
| 6 | `02_hld_step_07_api_interface_skeleton.md#R1.45` ~ `02_hld_step_13_risks_open_questions.md#R1.21` | 给 §7~§13 提供已回填来源。 |
| 7 | 当前 Step 14 `R1.1`~`R1.4` | 给 §14 与全文装配过程提供来源。 |
| 8 | 旧 `02_hld_step_13_formal_document_assembly.md` | 只在后续旧材料差异审计使用,不进入第一来源。 |

### 4. 逐章追溯策略草案

以下只定义 `R1.6` 的追溯策略,不是正式矩阵定稿。

| 正式章节 | 追溯策略 | 特殊处理 |
|---|---|---|
| §1 与上游文档的关系声明 | 回到 Step 1 与正式 `00/01`。 | 检查是否仍说明历史材料降级。 |
| §2 本次设计目标与范围 | 回到 Step 2 与正式 `00/01`。 | 检查非范围是否没有被后续章节突破。 |
| §3 约束条件 | 回到 Step 3。 | 检查约束是否仍支撑 §4~§12。 |
| §4 代码主体框架总览 | 回到 Step 4。 | 检查代码主体和实现分层是否与 §5 主轴一致。 |
| §5 主要组成部分、职责与边界 | 回到 Step 5 `0R.36` / `0R.37` / `0R.39`。 | Step 5 是后续 §6~§9 的主轴,必须保留完整追溯。 |
| §6 关键对象轮廓 | 回到 Step 6 主控和五个对象附录。 | 不能把附录压缩成一个文件名;要标明对象卡片留在 calibration。 |
| §7 API / 接口骨架 | 回到 Step 7 `R1.45`。 | 检查接口族是否仍与 §6 对象和 §8 流程一致。 |
| §8 关键处理流 / 重要函数数据流 | 回到 Step 8 `R1.33`。 | 检查处理流族是否覆盖 §7 接口和 §9 状态触发。 |
| §9 状态定义与状态流转 | 回到 Step 9 `R1.31`。 | 检查状态 owner、传播和禁止迁移是否保留。 |
| §10 异常与边界场景轮廓 | 回到 Step 10 `R1.27`。 | 检查 safe failure 与边界传播不反写 truth。 |
| §11 配置影响轮廓 | 回到 Step 11 `R1.25`。 | 检查仍停在配置影响轮廓,不写配置 key / schema。 |
| §12 详细设计承接清单 | 回到 Step 12 `R1.23`。 | 检查承接清单没有变成待确认事项或实施计划。 |
| §13 设计风险与待确认事项 | 回到 Step 13 `R1.21`。 | 检查未闭口事项没有被整理成已确认结论。 |
| §14 参考 | 回到 Step 14 当前装配过程。 | 后续 `R1.11/R1.12` 只列实际使用材料。 |

### 5. Step 6 附录追溯策略

Step 6 的正式 §6 不能只追溯主控文件,因为对象轮廓曾被拆成主控加附录。`R1.6` 应至少保留以下延伸阅读入口:

| 附录 | 用途 |
|---|---|
| `02_hld_step_06_key_objects_core_truth.md` | core truth 对象来源。 |
| `02_hld_step_06_key_objects_views_materials.md` | read model / material / view 对象来源。 |
| `02_hld_step_06_key_objects_refs_trace_audit.md` | typed ref / trace / audit 对象来源。 |
| `02_hld_step_06_key_objects_operations_peripheral.md` | operation / peripheral 对象来源。 |
| `02_hld_step_06_key_objects_policies_guards.md` | policy / guard / invariant 对象来源。 |

### 6. R1.6 写入边界

下一步只允许进入:

```text
R1.6 正式章节来源矩阵:再写入
```

`R1.6` 应写入:

- §1~§14 正式章节来源矩阵。
- Step 6 附录追溯入口。
- 哪些章节需要后续 `R1.7/R1.8` 一致性扫描。
- 哪些章节需要后续 `R1.11/R1.12` 补 §14 或校准来源。

`R1.6` 不得写入:

- 正式 `02-概要设计.md` 正文修改。
- 全文一致性扫描结论。
- 旧材料差异审计结论。
- Step 14 停审结论。

### 7. 自检

| 检查项 | 结果 |
|---|---|
| 是否按用户确认进入 `R1.5`? | 是。 |
| 是否只设计来源矩阵生成策略? | 是。 |
| 是否未写定稿矩阵? | 是。 |
| 是否未修改正式 `02-概要设计.md`? | 是。 |
| 是否明确 Step 6 附录必须进入追溯? | 是。 |
| 是否把下一步限制为 `R1.6`? | 是。 |

---

## R1.6 正式章节来源矩阵:再写入

### 1. 写入记录

| 项目 | 记录 |
|---|---|
| 当前模块 | `R1.6 正式章节来源矩阵:再写入` |
| 上一模块 | `R1.5 正式章节来源矩阵:先思考` |
| 用户确认 | 已确认进入 `R1.6` |
| 本模块写入范围 | §1~§14 来源矩阵、Step 6 附录追溯入口、后续扫描标记。 |
| 本模块禁止范围 | 正式 `02-概要设计.md` 正文修改、全文一致性扫描结论、旧材料差异审计结论、Step 14 停审结论。 |
| 下一模块 | `R1.7 全文一致性扫描:先思考` |

### 2. 正式章节来源矩阵

| 正式章节 | 当前章节主题 | 第一来源 | 关键回填记录 | 延伸阅读 | 特殊审查点 | R1.6 处理方式 |
|---|---|---|---|---|---|---|
| §1 与上游文档的关系声明 | 概要设计承接新版 00 / 01,并声明历史材料降级。 | `02_hld_step_01_upstream_boundary.md`;正式 `00-需求文档.md`;正式 `01-架构设计.md`。 | Step 1 状态 `completed`;Step 1 `回填草稿`。 | Step 1 “结构化中间产物”“回填草稿”“旧材料差异审计”。 | 当前正式 §1 缺少显式校准来源块,后续需检查是否补齐。 | `needs_source_patch` |
| §2 本次设计目标与范围 | 明确本次概要目标、非范围和设计深度。 | `02_hld_step_02_scope.md`;正式 `00-需求文档.md`;正式 `01-架构设计.md`。 | Step 2 状态 `completed`;Step 2 `回填草稿`。 | Step 2 “设计目标表”“非范围表”“当前阶段设计深度口径”。 | 检查非范围是否被 §5~§12 后续内容突破。 | `needs_source_patch` |
| §3 约束条件 | 约束后续代码主体、对象、接口、流程和状态判断。 | `02_hld_step_03_constraints.md`。 | Step 3 状态 `completed`;Step 3 `回填草稿`。 | Step 3 “约束清单表”“来源与影响追踪表”。 | 检查约束是否仍覆盖外部摘要、配置边界和恢复收敛红线。 | `needs_source_patch` |
| §4 代码主体框架总览 | 将架构模块映射到概要层代码主体和实现分层。 | `02_hld_step_04_code_subject_framework.md`。 | Step 4 状态 `completed`;Step 4 `回填草稿`。 | Step 4 “架构模块到代码主体映射图”“实现分层视图”。 | 检查 §4 的代码主体是否与 §5 八个主要组成部分一致。 | `needs_source_patch` |
| §5 主要组成部分、职责与边界 | 固定八个主要组成部分、职责边界、对象发现入口和 Step 6~9 承接门禁。 | `02_hld_step_05_components_boundary.md`。 | `0R.37 正式 §5 回填记录`;`0R.39 flow / 台账更新:再写入`。 | Step 5 `0R.8`;`0R.9`~`0R.24`;`0R.26`;`0R.28`;`0R.30`;`0R.34`;`0R.36`;`0R.39`。 | 检查正式 §5 是否仍为当前八部分主线,且未残留旧 MethodContent / snapshot / outbox 主语。 | `scan_later` |
| §6 关键对象轮廓 | 汇总关键对象家族、owner、对象类型和禁止下沉边界。 | `02_hld_step_06_key_objects.md`;Step 6 五个对象附录。 | Step 6 状态 `completed`;`8.45` 关闭 Step 6;正式 §6 已回填。 | `02_hld_step_06_key_objects_core_truth.md`;`views_materials.md`;`refs_trace_audit.md`;`operations_peripheral.md`;`policies_guards.md`。 | 检查正式 §6 是否只汇总关键对象,不搬入完整 schema;检查旧对象名残留。 | `scan_later` |
| §7 API / 接口骨架 | 汇总 Command / Query / Consumer / Event / Operations Job 接口族。 | `02_hld_step_07_api_interface_skeleton.md`。 | `R1.45` 正式 §7 回填记录。 | Step 7 回填草稿、接口分类、边界红线和旧材料差异审计。 | 检查接口族是否回指 §6 对象和 §8 处理流,不含 transport schema。 | `scan_later` |
| §8 关键处理流 / 重要函数数据流 | 汇总通用处理流骨架和八个组成部分处理流族。 | `02_hld_step_08_processing_flows.md`。 | `R1.33` 正式 §8 回填记录。 | Step 8 处理流族、跨处理流一致性审计、正式 §8 草稿。 | 检查 flow 是否仍符合 Query no-write、Consumer / Job 不写 core truth、publisher failure 不回滚 truth。 | `scan_later` |
| §9 状态定义与状态流转 | 汇总状态 owner、状态组、传播方向和禁止迁移红线。 | `02_hld_step_09_state_machine.md`。 | `R1.31` 正式 §9 回填记录。 | Step 9 状态 owner 候选池、各组成部分状态、跨状态一致性审计。 | 检查状态名和状态 owner 是否未回流旧 lifecycle / outbox status。 | `scan_later` |
| §10 异常与边界场景轮廓 | 汇总阻断、降级、外部边界、传播、维护和外围异常族。 | `02_hld_step_10_exceptions_boundaries.md`。 | `R1.27` 正式 §10 回填记录。 | Step 10 六个异常族、跨异常一致性审计、正式 §10 草稿。 | 检查异常文案是否没有暗示反写 truth、私补 schema 或跨边界恢复。 | `scan_later` |
| §11 配置影响轮廓 | 汇总配置会影响的承载面和禁止配置化边界。 | `02_hld_step_11_configuration_impact.md`。 | `R1.25` 正式 §11 回填记录。 | Step 11 配置影响来源池、禁止配置化边界、03 / 04 承接边界。 | 检查正式 §11 是否没有下沉到 config key、默认值、环境变量或 JSON schema。 | `scan_later` |
| §12 详细设计承接清单 | 汇总 03 / 04 应继续展开和必须回退概要的事项。 | `02_hld_step_12_detailed_design_handoff.md`。 | `R1.23` 正式 §12 回填记录。 | Step 12 承接来源池、对象与接口承接、处理流 / 状态 / 异常承接、配置承接。 | 检查承接项没有被改写为风险或待确认,也没有变成实施计划。 | `scan_later` |
| §13 设计风险与待确认事项 | 汇总当前仍需保守处理的风险和待确认事项。 | `02_hld_step_13_risks_open_questions.md`。 | `R1.21` 正式 §13 回填记录。 | Step 13 风险 / 待确认来源池、筛选、旧材料差异审计、正式 §13 草稿。 | 检查未闭口事项没有被改写为已确认设计结论。 | `scan_later` |
| §14 参考 | 列出实际使用材料及用途。 | 当前 Step 14 装配过程;规范文档;本仓正式 `00/01/02`;当前 Step 1~13 中间产物。 | Step 14 尚未完成;待 `R1.11/R1.12` 正式修正。 | Step 14 `R1.1`~`R1.6`;后续 `R1.11/R1.12`。 | 当前正式 §14 需要单独检查是否列入旧材料或未实际使用材料。 | `needs_reference_patch` |

### 3. Step 6 附录追溯入口

| §6 延伸阅读入口 | 追溯对象范围 | 在正式 §6 中的作用 |
|---|---|---|
| `02_hld_step_06_key_objects.md` | Step 6 主控、对象分组、正式 §6 回填和完成门禁。 | 作为 §6 第一来源。 |
| `02_hld_step_06_key_objects_core_truth.md` | 方法资产定义、正式化、版本、核心 truth 对象。 | 支撑 core truth 对象 owner 和禁入旧对象。 |
| `02_hld_step_06_key_objects_views_materials.md` | read model、view、material 和派生读取对象。 | 支撑读取材料不得成为第二 truth。 |
| `02_hld_step_06_key_objects_refs_trace_audit.md` | typed ref、trace、audit、history 和 lineage 对象。 | 支撑追溯、一致性保护和审计链轮廓。 |
| `02_hld_step_06_key_objects_operations_peripheral.md` | operation、maintenance、package、method set 和外围对象。 | 支撑 operation / peripheral 不污染 core truth。 |
| `02_hld_step_06_key_objects_policies_guards.md` | policy、guard、invariant 和判定类对象。 | 支撑正式化、受控消费和配置禁入边界。 |

### 4. 后续扫描标记

| 后续模块 | 必须扫描的章节 | 扫描原因 |
|---|---|---|
| `R1.7/R1.8 全文一致性扫描` | §1~§14 全文 | 当前来源矩阵只证明来源关系,尚未检查旧术语、旧对象、旧参考和跨章冲突。 |
| `R1.7/R1.8 全文一致性扫描` | §5~§10 | 这些章节承载 Step 5~10 的重点主线,需要重点查 MethodContent / snapshot / fingerprint / outbox / lifecycle 等旧残留。 |
| `R1.7/R1.8 全文一致性扫描` | §11~§13 | 需要确认配置、承接、风险没有被改写成 schema、实施计划或已确认结论。 |
| `R1.11/R1.12 §14 参考与交叉引用` | §1~§14 的校准来源块和 §14 | 当前正式文档部分章节可能缺少显式校准来源,§14 也需要只列实际使用材料。 |
| `R1.13/R1.14 旧材料差异审计` | 旧 `02_hld_step_13_formal_document_assembly.md`;旧正式 §14;全文旧术语命中 | 历史装配记录只能作为审计对象,不得作为当前来源。 |

### 5. 当前不执行的修正

| 修正类型 | 当前处理 | 原因 |
|---|---|---|
| 正式正文校准来源补丁 | 不执行 | 需等 `R1.9/R1.10` 或 `R1.11/R1.12`。 |
| 正式 §14 参考修正 | 不执行 | 需等 `R1.11/R1.12`。 |
| 旧术语删除或替换 | 不执行 | 需等 `R1.7/R1.8` 扫描后裁决。 |
| 旧 formal assembly 差异裁决 | 不执行 | 需等 `R1.13/R1.14`。 |

### 6. 下一写入边界

下一步只允许进入:

```text
R1.7 全文一致性扫描:先思考
```

`R1.7` 应思考:

- 全文一致性扫描的维度。
- 旧术语 / 旧对象 / 旧流程 / 旧状态 / 旧参考的搜索策略。
- 校准来源缺失和参考章节问题如何分流到后续模块。
- 哪些扫描结果只能记录为修正清单,不能立即改正式正文。

`R1.7` 不得写入:

- 全文扫描结果定稿。
- 正式 `02-概要设计.md` 修改。
- §14 参考正式修正。
- 旧材料差异审计结论。

### 7. 自检

| 检查项 | 结果 |
|---|---|
| 是否按用户确认进入 `R1.6`? | 是。 |
| 是否写入 §1~§14 来源矩阵? | 是。 |
| 是否保留 Step 6 附录追溯入口? | 是。 |
| 是否只标记后续扫描点,未执行扫描裁决? | 是。 |
| 是否未修改正式 `02-概要设计.md`? | 是。 |
| 是否把下一步限制为 `R1.7`? | 是。 |

---

## R1.7 全文一致性扫描:先思考

### 1. 本模块要回答的问题

本模块只定义全文一致性扫描的维度、搜索策略和分流规则。实际扫描结果、命中表和修正清单留到 `R1.8`。

| 问题 | 判断 |
|---|---|
| 为什么需要全文一致性扫描? | `R1.6` 只证明章节来源关系,还没有检查旧术语残留、跨章主语冲突、详细设计下沉和 §14 参考污染。 |
| 扫描对象是什么? | 正式 `02-概要设计.md` 全文,并用 Step 5~13 当前中间产物、Step 6 附录和旧 formal assembly 作对照。 |
| 当前是否记录实际命中结果? | 不记录。`R1.7` 只写扫描计划,`R1.8` 才执行扫描并写结果。 |
| 当前是否修改正式正文? | 不修改。正式正文修改最早到 `R1.10` 或 `R1.12`。 |
| 扫描结果如何处理? | 分流为 `source_patch_candidate`、`formal_text_patch_candidate`、`reference_patch_candidate`、`historical_audit_candidate`、`no_action`。 |

### 2. 扫描维度

| 扫描维度 | 目标 | 重点章节 | 输出去向 |
|---|---|---|---|
| 章节主链完整性 | 检查 §1~§14 是否存在、顺序是否符合书写规范。 | 全文标题。 | `R1.8` 主链检查表。 |
| 校准来源完整性 | 检查每个正式章节是否有具体 `design-calibration/...` 来源和延伸阅读。 | §1~§14 开头。 | `R1.11/R1.12` 交叉引用修正。 |
| 当前主语一致性 | 检查正式正文是否统一围绕 method asset definition、formalization、controlled consumption、trace、distribution、external reference、maintenance、peripheral package / method set。 | §4~§10。 | `R1.8` 修正清单。 |
| 旧术语残留 | 查旧 `MethodContent`、snapshot、fingerprint、outbox、ViewProfile、P1 plugin、configuration 等旧主线。 | §5~§14。 | `R1.8` 命中表;必要时 `R1.13/R1.14` 审计。 |
| 对象 / 接口 / flow / 状态一致性 | 检查 §6 对象、§7 接口、§8 flow、§9 状态是否互相支撑。 | §6~§9。 | `R1.8` 一致性表。 |
| 详细设计下沉 | 查完整 schema、字段全集、函数实现、DDL、transport schema、config key、测试证据、commit boundary。 | §6~§12。 | `R1.8` 越界表。 |
| 风险 / 待确认边界 | 检查 §13 是否仍是风险和待确认,没有变成已确认设计或任务计划。 | §13。 | `R1.8` 风险边界检查。 |
| §14 参考污染 | 检查参考是否只列实际使用材料,是否残留旧材料或未读取材料。 | §14。 | `R1.11/R1.12` 参考修正。 |

### 3. 搜索策略

`R1.8` 应采用“结构扫描 + 术语扫描 + 对照扫描”的组合。

#### 3.1 结构扫描

| 检查项 | 建议命令 / 方法 | 产物 |
|---|---|---|
| 标题主链 | `rg -n '^##|^###|^####' projects/L3-method-library/02-概要设计.md` | 标题表。 |
| 校准来源块 | `rg -n '校准来源|延伸阅读' projects/L3-method-library/02-概要设计.md` | 校准来源缺失候选。 |
| 参考章节 | 读取 §14 全文。 | 参考材料候选表。 |

#### 3.2 旧术语扫描

| 术语组 | 搜索词 | 处理口径 |
|---|---|---|
| 旧内容对象 | `MethodContent`, `MethodContentKind`, `MethodContentRef` | 原则上不应留在当前正式主线。 |
| 旧发布 / 快照 | `snapshot`, `Snapshot`, `DefinitionSnapshot`, `fingerprint`, `Fingerprint` | 若出现,判断是否只是历史污染说明;否则列修正候选。 |
| 旧事件 / outbox | `outbox`, `Outbox`, `publish`, `published`, `Published` | 不能恢复旧 publish / outbox 主线。 |
| 旧 view / profile | `ViewProfile`, `view profile`, `profile` | 检查是否误替代当前 read material / visibility / material 口径。 |
| 旧外围 | `P1 plugin`, `plugin`, `Plugin`, `configuration`, `Configuration` | 检查是否把外围增强写成核心闭环前置。 |
| 旧状态 | `draft`, `review`, `lifecycle`, `Lifecycle` | 检查是否回流旧 lifecycle 状态机。 |

#### 3.3 当前主语对照扫描

| 当前主语 | 对照来源 | 检查目的 |
|---|---|---|
| 八个主要组成部分 | Step 5 `0R.8`;正式 §5。 | 检查 §6~§10 是否仍回指八部分主轴。 |
| 关键对象家族 | Step 6 主控和五个附录。 | 检查对象名和 owner 是否一致。 |
| API 接口族 | Step 7 `R1.45`。 | 检查 Command / Query / Consumer / Event / Job 分类未漂移。 |
| 处理流族 | Step 8 `R1.33`。 | 检查 flow 没有恢复旧 publish / snapshot / outbox。 |
| 状态组 | Step 9 `R1.31`。 | 检查状态 owner 和传播红线一致。 |
| 异常族 | Step 10 `R1.27`。 | 检查异常没有打穿 truth / body-free / no-write 边界。 |
| 配置边界 | Step 11 `R1.25`。 | 检查没有提前写 config key / schema。 |
| 承接清单 | Step 12 `R1.23`。 | 检查承接项没有混成实施计划。 |
| 风险 / 待确认 | Step 13 `R1.21`。 | 检查未闭口事项没有被定论化。 |

### 4. 分流规则

| 扫描发现 | 分流标签 | 后续处理 |
|---|---|---|
| 缺少章节校准来源或延伸阅读 | `source_patch_candidate` | `R1.11/R1.12` 或正式装配批次处理。 |
| 正文存在旧主语 / 旧对象 / 旧状态残留 | `formal_text_patch_candidate` | `R1.9/R1.10` 前先列修正清单。 |
| §14 参考材料旧残留或未实际使用 | `reference_patch_candidate` | `R1.11/R1.12` 处理。 |
| 旧 formal assembly 或旧章节可作为污染样本 | `historical_audit_candidate` | `R1.13/R1.14` 处理。 |
| 命中词只出现在历史污染说明或禁入说明中 | `no_action_or_keep_with_note` | `R1.8` 说明保留理由。 |
| 存在详细设计下沉内容 | `formal_text_patch_candidate` | 后续正式装配时删除或转为 §12 承接。 |

### 5. R1.8 输出格式

`R1.8` 应写入以下表格:

| 表格 | 内容 |
|---|---|
| 主链与校准来源检查表 | 标题、校准来源、延伸阅读是否完整。 |
| 旧术语命中表 | 旧术语、位置、上下文、分流标签。 |
| 当前主语一致性检查表 | 八个主要组成部分、对象、接口、flow、状态是否一致。 |
| 详细设计下沉检查表 | 越界内容、章节、建议分流。 |
| §14 参考污染检查表 | 旧材料、未实际使用材料、应保留材料。 |
| 后续修正清单 | 哪些进入 `R1.9/R1.10`;哪些进入 `R1.11/R1.12`;哪些进入 `R1.13/R1.14`。 |

### 6. 下一写入边界

下一步只允许进入:

```text
R1.8 全文一致性扫描:再写入
```

`R1.8` 可以执行扫描并写入结果,但仍不得修改正式 `02-概要设计.md` 正文。

`R1.8` 不得写入:

- 正式正文修正 patch。
- §14 参考正式修正。
- 旧材料差异审计裁决。
- Step 14 停审结论。

### 7. 自检

| 检查项 | 结果 |
|---|---|
| 是否按用户确认进入 `R1.7`? | 是。 |
| 是否只定义扫描计划,未写扫描结果? | 是。 |
| 是否未修改正式 `02-概要设计.md`? | 是。 |
| 是否明确 `R1.8` 的输出表格? | 是。 |
| 是否把下一步限制为 `R1.8`? | 是。 |

---

## R1.8 全文一致性扫描:再写入

### 1. 写入记录

| 项目 | 记录 |
|---|---|
| 当前模块 | `R1.8 全文一致性扫描:再写入` |
| 上一模块 | `R1.7 全文一致性扫描:先思考` |
| 用户确认 | 已确认进入 `R1.8` |
| 本模块允许 | 执行扫描并记录主链、校准来源、旧术语、下沉内容和修正分流。 |
| 本模块禁止 | 修改正式 `02-概要设计.md`;正式修正 §14;裁决旧 formal assembly;关闭 Step 14。 |
| 下一模块 | `R1.9 正式全文装配草稿:先思考` |

### 2. 扫描执行摘要

| 扫描项 | 执行方式 | 结果摘要 |
|---|---|---|
| 标题主链与校准来源 | `rg -n '^##|^###|^####|校准来源|延伸阅读' projects/L3-method-library/02-概要设计.md` | §1~§14 主链完整;只有 §6 有完整校准来源 / 延伸阅读块,§11 有单行校准来源,其余章节缺显式来源块。 |
| 旧术语 / 旧主线 | `rg -n -i 'MethodContent|snapshot|fingerprint|outbox|publish|ViewProfile|P1 plugin|configuration|draft|review|lifecycle' ...` | §1~§4 存在旧主线正文;§5 以后多为禁入说明、边界红线或旧材料说明。 |
| 下沉内容 | `rg -n -i 'schema|DDL|function|config key|test evidence|commit-|实施|测试|验收|JSON|YAML|TOML' ...` | 未发现 DDL / 代码实现正文;schema / config / test 多数作为“不得下沉”或后续文档承接说明出现。 |
| §14 参考 | 读取 §14 全文。 | §14 仍有旧参考口径,引用范围写到 `02_hld_step_13_formal_document_assembly.md`,需要后续修正为当前 Step 14 和实际材料。 |

### 3. 主链与校准来源检查表

| 检查项 | 结果 | 分流 |
|---|---|---|
| §1~§14 章节顺序 | pass:正式文档已有 14 章主链。 | `no_action` |
| §1~§5 校准来源块 | gap:未在章节开头显式列出当前 `design-calibration` 来源。 | `source_patch_candidate` |
| §6 校准来源块 | pass:已有校准来源和延伸阅读,但后续需核对是否列齐五个对象附录。 | `scan_later` |
| §7~§10 校准来源块 | gap:未在章节开头显式列出当前 Step 7~10 来源。 | `source_patch_candidate` |
| §11 校准来源块 | partial:有单行校准来源,缺延伸阅读和统一块格式。 | `source_patch_candidate` |
| §12~§13 校准来源块 | gap:未在章节开头显式列出当前 Step 12~13 来源。 | `source_patch_candidate` |
| §14 参考 | gap:仍像旧装配参考表,需要按本轮实际读取材料重写。 | `reference_patch_candidate` |

### 4. 旧术语命中表

| 命中范围 | 典型命中 | 上下文判断 | 分流 |
|---|---|---|---|
| §1 | `7 类 MethodContent`;`ViewProfile`;`outbox + snapshot + fingerprint`;`MethodPlugin / MethodConfiguration` | 这些内容出现在“上游关系 / 不再回答 / 必须回答”正文中,不是禁入说明,与当前 Step 1 主线冲突。 | `formal_text_patch_candidate` |
| §2 | `MethodContent`;`P0 发布同步`;`ResolveViewProfile`;`Outbox 传播状态`;`P1 组合对象` | 旧设计目标与范围残留,应由 Step 2 当前目标 / 非范围替换。 | `formal_text_patch_candidate` |
| §3 | `P0 / P1`;`7 类 MethodContent`;`publish`;`fingerprint`;`audit`;`outbox`;`snapshot` | 旧约束清单仍把旧发布同步主线当正向约束,与 Step 3 当前约束冲突。 | `formal_text_patch_candidate` |
| §4 | `MethodContentCommandService`;`PublishGovernanceService`;`OutboxRelayWorker`;`DefinitionSnapshot`;`MethodPluginService`;`MethodConfigurationService` | 旧代码主体框架残留,需要由 Step 4 当前代码主体框架替换。 | `formal_text_patch_candidate` |
| §5 | `MethodContent`;`publish`;`snapshot`;`fingerprint`;`outbox`;`P1 plugin` | 多数出现在“当前设计不再采用”“旧材料禁入”语境中。 | `no_action_or_keep_with_note` |
| §6 | `MethodContent`;`DefinitionSnapshot`;`fingerprint`;`OutboxEvent` | 出现在关键对象禁入说明中,用于防污染。 | `no_action_or_keep_with_note` |
| §7~§10 | `publish`;`snapshot`;`fingerprint`;`outbox`;`lifecycle` | 多数作为接口 / flow / 状态 / 异常的禁止恢复项出现。 | `no_action_or_keep_with_note` |
| §11~§13 | `config key`;`test evidence schema`;`outbox`;`snapshot`;`fingerprint` | 多数作为禁止配置化、回退规则或风险说明出现。 | `no_action_or_keep_with_note` |
| §14 | `02_hld_step_13_formal_document_assembly.md` | 旧装配文件作为已确认回填材料范围的一部分出现,不符合当前 Step 14 真相源。 | `reference_patch_candidate` |

### 5. 当前主语一致性检查表

| 检查范围 | 结果 | 分流 |
|---|---|---|
| §1~§4 对当前 Step 1~4 | fail:仍以旧 P0 / P1、MethodContent、publish / snapshot / outbox 主线组织。 | `formal_text_patch_candidate` |
| §5 对当前 Step 5 | pass_with_guard:八个主要组成部分主线存在,旧词多为禁入说明。 | `scan_later` |
| §6 对当前 Step 6 | pass_with_guard:关键对象轮廓已转向当前对象家族,但需核对校准来源和附录入口。 | `source_patch_candidate` |
| §7 对当前 Step 7 | pass_with_guard:接口族为 Command / Query / Consumer / Event / Job,旧词多为禁入说明。 | `scan_later` |
| §8 对当前 Step 8 | pass_with_guard:处理流使用当前八部分主线,旧 flow 多被列为禁入。 | `scan_later` |
| §9 对当前 Step 9 | pass_with_guard:状态组已按当前 owner 展开,旧 lifecycle / outbox 状态作为禁入出现。 | `scan_later` |
| §10 对当前 Step 10 | pass_with_guard:异常族与当前边界一致,旧主线作为禁入出现。 | `scan_later` |
| §11~§13 对当前 Step 11~13 | pass_with_guard:配置、承接、风险边界基本对齐,后续补校准来源和 §14 即可。 | `source_patch_candidate` |

### 6. 详细设计下沉检查表

| 命中范围 | 判断 | 分流 |
|---|---|---|
| §2 非范围 | 明确把 HTTP / RPC / Event / Job schema、测试、验收、实施计划排除到后续文档。 | `no_action` |
| §7 Inbound / Outbound 表 | 出现 `schema / version`、`source envelope` 等概要接口骨架字段,未展开完整协议 schema。 | `no_action_or_keep_with_note` |
| §8 处理流 | 多处声明不定义 topic、payload schema、outbox、relay、retry、DDL、worker。 | `no_action` |
| §11 配置影响 | 明确不定义配置项清单、JSON 示例、RuntimeConfig 字段或 ConfigError。 | `no_action` |
| §12 / §13 | schema、trait、DDL、test evidence 等作为后续承接或风险红线出现。 | `no_action_or_keep_with_note` |

### 7. §14 参考污染检查表

| 当前条目 | 判断 | 分流 |
|---|---|---|
| `00-需求文档.md`;`01-架构设计.md` | 应保留,但用途需要改为当前新版 00 / 01。 | `reference_patch_candidate` |
| `02-概要设计.md` | 可保留为被装配对象。 | `reference_patch_candidate` |
| 三份 standards 文档 | 应保留,并补充 `设计文档编写通则.md` 是否实际参与最终自检。 | `reference_patch_candidate` |
| `design-calibration/02_hld_step_01_upstream_boundary.md` ~ `02_hld_step_13_formal_document_assembly.md` | 范围错误。当前 Step 14 文件是 `02_hld_step_14_formal_document_assembly.md`,旧 `02_hld_step_13_formal_document_assembly.md` 只能作历史审计。 | `reference_patch_candidate` |

### 8. 后续修正清单

| 后续模块 | 修正 / 审计内容 |
|---|---|
| `R1.9/R1.10 正式全文装配草稿` | 计划并执行正式 §1~§4 的重装配,用当前 Step 1~4 回填草稿替换旧 P0 / P1 / MethodContent 主线;同时决定是否批量补各章校准来源。 |
| `R1.11/R1.12 §14 参考与交叉引用` | 重写 §14 参考表;补齐校准来源 / 延伸阅读块;确保旧 formal assembly 只作为历史审计材料出现。 |
| `R1.13/R1.14 旧材料差异审计` | 对旧 `02_hld_step_13_formal_document_assembly.md`、旧 §14 范围和全文旧词保留点做最终差异裁决。 |
| `R1.15/R1.16 自检与停审` | 检查正式正文是否未新增未讨论结论、未下沉详细设计、未保留旧主线。 |

### 9. 下一写入边界

下一步只允许进入:

```text
R1.9 正式全文装配草稿:先思考
```

`R1.9` 应思考:

- 正式 §1~§4 如何按当前 Step 1~4 草稿重装配。
- 校准来源块和延伸阅读应在哪个批次补。
- 哪些改动属于 `R1.10` 正式正文 patch,哪些必须留给 `R1.11/R1.12` 或 `R1.13/R1.14`。
- 单批正式正文 patch 如何控制在可审查规模内。

`R1.9` 不得写入:

- 正式 `02-概要设计.md` patch。
- §14 参考正式修正。
- 旧材料差异审计裁决。
- Step 14 停审结论。

### 10. 自检

| 检查项 | 结果 |
|---|---|
| 是否按用户确认进入 `R1.8`? | 是。 |
| 是否执行并记录扫描结果? | 是。 |
| 是否未修改正式 `02-概要设计.md`? | 是。 |
| 是否把 §1~§4 旧主线标为正式正文修正候选? | 是。 |
| 是否把 §14 标为参考修正候选? | 是。 |
| 是否把下一步限制为 `R1.9`? | 是。 |

---

## R1.9 正式全文装配草稿:先思考

### 1. 本模块要回答的问题

`R1.9` 只制定正式正文装配策略,不修改正式 `02-概要设计.md`。

| 问题 | 本轮判断 |
|---|---|
| 正式全文装配的第一批应修哪里? | 优先修正式 §1~§4。`R1.8` 已确认这些章节仍保留旧 P0 / P1、MethodContent、publish、snapshot、outbox、ViewProfile 等正向主线。 |
| 第一批是否应同时补齐 §5~§13 的校准来源块? | 不同时做。§5~§13 需要统一来源块和延伸阅读,但与 §14 参考、交叉引用强相关,应留给 `R1.11/R1.12`。 |
| 第一批是否应修改 §14? | 不修改。§14 参考污染已识别,但正式修正属于 `R1.11/R1.12`。 |
| 第一批是否能新增概要结论? | 不能。只能把 Step 1~4 已确认的回填草稿装配进正式 §1~§4。 |
| 第一批是否允许保留旧词? | 只允许在“旧机制不继承 / 历史污染说明 / 禁入说明”语境保留,不得作为正向设计主线保留。 |

### 2. 装配来源与目标

| 正式章节 | 当前问题 | 装配来源 | `R1.10` 目标 |
|---|---|---|---|
| §1 与上游文档的关系声明 | 仍把旧 P0 / P1、7 类 MethodContent、outbox + snapshot + fingerprint、MethodPlugin / MethodConfiguration 写成正向承接。 | `02_hld_step_01_upstream_boundary.md` 的“回填草稿”。 | 替换为新版 00 / 01 承接关系、本轮不再回答、本轮必须回答。 |
| §2 本次设计目标与范围 | 仍以旧 MethodContent、P0 发布同步、ResolveViewProfile、Outbox 传播状态组织目标。 | `02_hld_step_02_scope.md` 的“回填草稿”。 | 替换为当前概要目标、非范围和设计深度口径。 |
| §3 约束条件 | 仍把 P0/P1、publish、fingerprint、audit、outbox、snapshot 当作正向硬约束。 | `02_hld_step_03_constraints.md` 的“回填草稿”。 | 替换为当前方法资产定义 truth、Definition vs Use、正式版本、读取材料、后台维护和概要深度约束。 |
| §4 代码主体框架总览 | 仍使用 MethodContentCommandService、PublishGovernanceService、OutboxRelayWorker、DefinitionSnapshot、MethodPluginService 等旧主体。 | `02_hld_step_04_code_subject_framework.md` 的“回填草稿”。 | 替换为当前八组代码主体主语和实现分层视图。 |

### 3. `R1.10` 正式正文修改批次

`R1.10` 可执行正式 `02-概要设计.md` 修正,但只限第一批装配范围。

| 批次 | 修改范围 | 允许动作 | 禁止动作 |
|---|---|---|---|
| Batch 1-A | 文档元信息 / 变更记录 / §1 | 增加本轮 Step 14 装配变更记录;用 Step 1 回填草稿替换 §1。 | 不改 §5~§14;不新增上游结论。 |
| Batch 1-B | §2 | 用 Step 2 回填草稿替换 §2。 | 不展开 03 / 04 / 05 / 06 / 07 的内容。 |
| Batch 1-C | §3 | 用 Step 3 回填草稿替换 §3。 | 不把旧 fingerprint / snapshot / outbox 恢复为正式机制。 |
| Batch 1-D | §4 | 用 Step 4 回填草稿替换 §4。 | 不写目录树、完整模块文件、trait、函数签名或实现细节。 |

每个实际写入批次应控制在可审查规模内。若 §4 替换内容较长,可以在 `R1.10` 内拆成多个 `apply_patch` 批次,但仍属于同一个已确认模块。

### 4. 校准来源块处理策略

| 范围 | 策略 | 原因 |
|---|---|---|
| §1~§4 | 随对应回填草稿一起进入 `R1.10`。 | Step 1~4 回填草稿已经包含校准来源块,可直接装配。 |
| §5~§13 | 暂缓到 `R1.11/R1.12`。 | 这些章节需要和 §14 参考、延伸阅读、旧材料定位统一处理,避免在第一批扩大修改面。 |
| §14 | 暂缓到 `R1.11/R1.12`。 | `R1.8` 已确认 §14 引用旧 Step 13 formal assembly 的范围错误,需要单独修正。 |
| Step 14 停审记录 | 暂缓到 `R1.15/R1.16` 和 `R1.17`。 | 只有全文修正、参考修正、旧材料审计都完成后才能关闭 Step 14。 |

### 5. 旧术语处置策略

| 旧术语 / 旧主线 | §1~§4 处置 | 后续处置 |
|---|---|---|
| `MethodContent` / 旧七类定义 | 从 §1~§4 正向主线删除。 | 若在 §5 以后作为旧材料禁入说明出现,先保留到 `R1.13/R1.14` 审计。 |
| `P0 / P1` 旧阶段主线 | 从 §1~§4 设计目标和约束中删除。 | 外围能力只按当前“外围包与方法集组织”边界表达。 |
| `publish` / `published` / `outbox` | 从 §1~§4 正向机制删除。 | §5 以后若是禁止恢复旧发布 / outbox 主线的说明,暂不处理。 |
| `snapshot` / `fingerprint` | 从 §1~§4 正向机制删除。 | §3 可保留“旧 fingerprint / snapshot 不直接继承”的否定约束。 |
| `ViewProfile` / `ResolveViewProfile` | 从 §1~§4 正向目标删除。 | 若后续作为历史差异命中,由 `R1.13/R1.14` 裁决。 |
| `MethodPlugin` / `MethodConfiguration` | 从旧 P1 主线删除。 | 当前只允许以外围包 / 方法集组织的概要边界出现。 |

### 6. `R1.10` 验收口径

`R1.10` 完成后应满足:

| 检查项 | 通过标准 |
|---|---|
| §1~§4 来源 | 每章都能回指当前 Step 1~4 回填草稿。 |
| 旧主线删除 | §1~§4 不再把旧 P0 / P1、MethodContent、publish、snapshot、outbox 作为正向设计。 |
| 概要深度 | 未写完整 schema、字段全集、trait、函数签名、DDL、配置项、测试证据或实施计划。 |
| 修改面控制 | 正式正文只改元信息 / 变更记录 / §1~§4,不碰 §5~§14。 |
| 后续分流 | §5~§13 校准来源、§14 参考、旧材料差异审计仍留给后续模块。 |

### 7. 下一写入边界

下一步只允许进入:

```text
R1.10 正式全文装配草稿:再写入
```

`R1.10` 允许在用户确认后修改正式 `02-概要设计.md`,但只允许执行本模块定义的第一批装配范围。

`R1.10` 不得写入:

- §5~§13 的大规模重写。
- §14 参考正式修正。
- 旧材料差异审计最终裁决。
- Step 14 停审结论。

### 8. 自检

| 检查项 | 结果 |
|---|---|
| 是否按用户确认进入 `R1.9`? | 是。 |
| 是否只制定装配策略,未修改正式 `02-概要设计.md`? | 是。 |
| 是否把第一批正式修正限定为元信息 / §1~§4? | 是。 |
| 是否明确 §5~§13 校准来源和 §14 修正后移? | 是。 |
| 是否明确下一步限制为 `R1.10`? | 是。 |

---

## R1.10 正式全文装配草稿:再写入

### 1. 写入记录

| 项目 | 记录 |
|---|---|
| 当前模块 | `R1.10 正式全文装配草稿:再写入` |
| 上一模块 | `R1.9 正式全文装配草稿:先思考` |
| 用户确认 | 已确认进入 `R1.10` |
| 本模块允许 | 修改正式 `02-概要设计.md` 的元信息、变更记录和 §1~§4。 |
| 本模块禁止 | 修改 §5~§14;修正 §14 参考;裁决旧材料差异;关闭 Step 14。 |
| 下一模块 | `R1.11 正式 §14 参考与交叉引用:先思考` |

### 2. 正式正文写入结果

| 批次 | 写入范围 | 结果 |
|---|---|---|
| Batch 1-A | 文档元信息 / 变更记录 / §1 | 正式文档日期与版本更新到 `v0.2.3`;新增变更记录;§1 已按 Step 1 回填草稿重装配。 |
| Batch 1-B | §2 | §2 已按 Step 2 回填草稿重装配,设计目标、非范围和设计深度口径改为当前主线。 |
| Batch 1-C | §3 | §3 已按 Step 3 回填草稿重装配,约束清单改为当前 definition truth、Definition vs Use、读取材料、后台维护和概要深度约束。 |
| Batch 1-D | §4 | §4 已按 Step 4 回填草稿重装配,代码主体框架改为当前八组代码主体主语和实现分层视图。 |

### 3. 写入边界核对

| 检查项 | 结果 |
|---|---|
| 正式文档是否只改元信息 / §1~§4 | pass。 |
| 是否误改 §5~§14 | 未在本模块修改 §5~§14;§5 起仍留给后续模块处理校准来源、参考和旧材料审计。 |
| §1~§4 是否回指 Step 1~4 当前回填草稿 | pass;§1~§4 已补对应校准来源。 |
| §1~§4 是否仍把旧 P0 / P1 / MethodContent / publish / snapshot / outbox 作为正向主线 | no;旧词只保留在变更记录或否定约束语境。 |
| 是否新增未讨论概要结论 | no;内容来自 Step 1~4 回填草稿。 |
| 是否下沉详细设计 | no;未写完整字段、函数签名、协议 schema、DDL、配置项、测试证据或实施 commit。 |

### 4. 后续分流

| 后续模块 | 继续处理内容 |
|---|---|
| `R1.11/R1.12 正式 §14 参考与交叉引用` | 思考并修正 §14 参考材料、各章校准来源 / 延伸阅读的统一补齐策略。 |
| `R1.13/R1.14 旧材料差异审计` | 审计旧 `02_hld_step_13_formal_document_assembly.md`、旧术语保留点和历史材料定位。 |
| `R1.15/R1.16 自检与停审` | 全文检查、停审条件和 Step 14 关闭。 |
| `R1.17 正式全文回填记录` | 记录正式 02 装配完成并更新 flow / 台账到后续文档入口。 |

### 5. 下一写入边界

下一步只允许进入:

```text
R1.11 正式 §14 参考与交叉引用:先思考
```

`R1.11` 只允许思考 §14 参考、校准来源和交叉引用修正策略,不得直接修改正式 `02-概要设计.md`。

### 6. 自检

| 检查项 | 结果 |
|---|---|
| 是否按用户确认进入 `R1.10`? | 是。 |
| 是否完成正式 §1~§4 第一批装配? | 是。 |
| 是否未修改 §5~§14? | 是。 |
| 是否把下一步限制为 `R1.11`? | 是。 |

---

## R1.11 正式 §14 参考与交叉引用:先思考

### 1. 本模块要回答的问题

`R1.11` 只制定 §14 参考、校准来源和交叉引用修正策略,不修改正式 `02-概要设计.md`。

| 问题 | 本轮判断 |
|---|---|
| §14 当前最大问题是什么? | 当前 §14 仍把 `02_hld_step_01_upstream_boundary.md ~ 02_hld_step_13_formal_document_assembly.md` 作为“已确认回填材料”,这会把旧 formal assembly 混入当前真相源。 |
| 是否要在本模块直接修正式 §14? | 不修。正式修正留给 `R1.12`。 |
| §14 是否应列出旧 `02_hld_step_13_formal_document_assembly.md`? | 不应作为当前正式参考。它只属于后续 `R1.13/R1.14` 旧材料差异审计对象。 |
| 是否只修 §14 即可? | 不够。正式章节还缺统一校准来源入口,尤其 §5、§7~§10、§12~§13,§11 也需要从单行改成统一块。 |
| `R1.12` 是否可以新增概要结论? | 不能。只能补来源、延伸阅读、参考用途和交叉引用,不得新增对象、接口、流程、状态或风险结论。 |

### 2. 正式正文当前问题表

| 范围 | 当前问题 | `R1.12` 策略 |
|---|---|---|
| §1~§4 | 已在 `R1.10` 按 Step 1~4 补校准来源。 | 不再改动,除非发现格式硬错误。 |
| §5 | 当前缺正式校准来源块。 | 在 §5 标题下补 `02_hld_step_05_components_boundary.md` 来源和延伸阅读。 |
| §6 | 已有校准来源和延伸阅读。 | 保持;如需补 Step 6 附录入口,只做小幅格式补充。 |
| §7 | 当前缺正式校准来源块。 | 补 `02_hld_step_07_api_interface_skeleton.md` 来源和 `R1.45` / 接口分类 / 边界红线延伸阅读。 |
| §8 | 当前缺正式校准来源块。 | 补 `02_hld_step_08_processing_flows.md` 来源和 `R1.33` / 处理流族 / 跨流一致性审计延伸阅读。 |
| §9 | 当前缺正式校准来源块。 | 补 `02_hld_step_09_state_machine.md` 来源和 `R1.31` / 状态 owner / 状态传播红线延伸阅读。 |
| §10 | 当前缺正式校准来源块。 | 补 `02_hld_step_10_exceptions_boundaries.md` 来源和 `R1.27` / 异常族 / 边界红线延伸阅读。 |
| §11 | 当前只有单行校准来源,缺统一延伸阅读块。 | 改成统一 `校准来源 / 延伸阅读` 块,不改 §11 正文结论。 |
| §12 | 当前缺正式校准来源块。 | 补 `02_hld_step_12_detailed_design_handoff.md` 来源和 `R1.23` / 承接来源池 / 回退规则延伸阅读。 |
| §13 | 当前缺正式校准来源块。 | 补 `02_hld_step_13_risks_open_questions.md` 来源和 `R1.21` / 风险筛选 / 待确认筛选延伸阅读。 |
| §14 | 当前参考表范围错误,且缺 §14 自身校准来源块。 | 补 `02_hld_step_14_formal_document_assembly.md` 来源;重写参考表为实际使用材料。 |

### 3. §14 参考材料重写策略

`R1.12` 中 §14 应只列当前概要设计实际依赖或用于校验的材料,并说明用途。

| 材料类别 | 拟列材料 | 用途 |
|---|---|---|
| 上游正式材料 | `projects/L3-method-library/00-需求文档.md`;`projects/L3-method-library/01-架构设计.md` | 当前概要设计的需求和架构基线。 |
| 正式装配对象 | `projects/L3-method-library/02-概要设计.md` | Step 14 的装配、扫描和修正对象。 |
| 流程与书写规范 | `standards/document/概要设计书写规范.md`;`standards/document/概要设计讨论流程_SOP.md`;`standards/document/设计文档讨论中间产物规范.md`;`standards/document/设计文档编写通则.md` | 约束章节主链、校准来源、参考材料、三层门禁、先思考后写入和正式正文表达边界。 |
| 当前概要 flow / 台账 | `design-calibration/02_hld_calibration_flow.md`;`design-calibration/project_execution_ledger.md` | 确认 Step 1~14 状态、恢复点、门禁和禁止跳步规则。 |
| Step 1~4 当前来源 | `02_hld_step_01_upstream_boundary.md`;`02_hld_step_02_scope.md`;`02_hld_step_03_constraints.md`;`02_hld_step_04_code_subject_framework.md` | 支撑正式 §1~§4。 |
| Step 5~10 当前来源 | `02_hld_step_05_components_boundary.md`;`02_hld_step_06_key_objects.md` 及 Step 6 附录;`02_hld_step_07_api_interface_skeleton.md`;`02_hld_step_08_processing_flows.md`;`02_hld_step_09_state_machine.md`;`02_hld_step_10_exceptions_boundaries.md` | 支撑主要组成部分、关键对象、接口、处理流、状态和异常边界。 |
| Step 11~13 当前来源 | `02_hld_step_11_configuration_impact.md`;`02_hld_step_12_detailed_design_handoff.md`;`02_hld_step_13_risks_open_questions.md` | 支撑配置影响、详细设计承接、风险与待确认事项。 |
| Step 14 当前来源 | `02_hld_step_14_formal_document_assembly.md` | 支撑正式全文装配、扫描、§1~§4 修正、§14 参考和后续停审。 |

### 4. §14 禁止列入清单

| 材料 | 禁止原因 | 后续位置 |
|---|---|---|
| 旧 `02_hld_step_13_formal_document_assembly.md` 作为“已确认回填材料” | 这是历史装配文件,会混淆当前 Step 14 真相源。 | `R1.13/R1.14` 旧材料差异审计。 |
| 历史 `03_ddd_*` | 不得反推概要对象、接口、流程或状态。 | 只可在污染审计中按需提及。 |
| 历史 `04~07` | 当前 02 尚未关闭,后续文档仍 blocked。 | 不进入正式 §14。 |
| `/tmp` 讨论计划 | 临时计划不是正式概要参考材料。 | 不进入正式 §14。 |
| 只读过但未支撑正式结论的材料 | 参考章节不能变成资料仓。 | 不进入正式 §14。 |

### 5. `R1.12` 写入批次计划

| 批次 | 修改范围 | 允许动作 | 禁止动作 |
|---|---|---|---|
| Batch 2-A | §5、§7、§8、§9、§10 校准来源块 | 在章节标题下补 `校准来源 / 延伸阅读` 块。 | 不改章节正文结论。 |
| Batch 2-B | §11、§12、§13 校准来源块 | 统一 §11 格式;补 §12、§13 来源和延伸阅读。 | 不新增风险或承接项。 |
| Batch 2-C | §14 | 补 §14 自身校准来源块;重写参考材料表。 | 不把旧 formal assembly 写成当前正式参考;不关闭 Step 14。 |

### 6. `R1.12` 验收口径

| 检查项 | 通过标准 |
|---|---|
| 每章校准来源 | §1~§14 均有具体 `design-calibration/...` 来源入口。 |
| 延伸阅读 | 每章延伸阅读说明读者应看哪些中间产物小节或回填记录。 |
| §14 参考 | 只列实际使用材料,且说明用途。 |
| 旧材料定位 | 旧 `02_hld_step_13_formal_document_assembly.md` 不作为当前正式参考。 |
| 修改深度 | 不新增概要结论,不下沉详细设计,不修改 §5~§13 正文结论。 |

### 7. 下一写入边界

下一步只允许进入:

```text
R1.12 正式 §14 参考与交叉引用:再写入
```

`R1.12` 允许按本模块计划修改正式 `02-概要设计.md` 的校准来源块和 §14 参考表。

`R1.12` 不得写入:

- 新的对象、接口、流程、状态、异常、配置或风险结论。
- 旧材料差异审计最终裁决。
- Step 14 停审结论。

### 8. 自检

| 检查项 | 结果 |
|---|---|
| 是否按用户确认进入 `R1.11`? | 是。 |
| 是否未修改正式 `02-概要设计.md`? | 是。 |
| 是否明确 §14 参考重写策略? | 是。 |
| 是否明确校准来源补齐范围? | 是。 |
| 是否把下一步限制为 `R1.12`? | 是。 |

---

## R1.12 正式 §14 参考与交叉引用:再写入

### 1. 写入记录

| 项目 | 记录 |
|---|---|
| 当前模块 | `R1.12 正式 §14 参考与交叉引用:再写入` |
| 上一模块 | `R1.11 正式 §14 参考与交叉引用:先思考` |
| 用户确认 | 已确认进入 `R1.12` |
| 本模块允许 | 修改正式 `02-概要设计.md` 的校准来源块和 §14 参考表。 |
| 本模块禁止 | 新增概要结论;裁决旧材料差异;关闭 Step 14。 |
| 下一模块 | `R1.13 旧材料差异审计:先思考` |

### 2. 正式正文写入结果

| 批次 | 写入范围 | 结果 |
|---|---|---|
| Batch 2-A | §5、§7、§8、§9、§10 校准来源块 | 已补 `校准来源 / 延伸阅读` 块,只增加追溯入口,未修改章节正文结论。 |
| Batch 2-B | §11、§12、§13 校准来源块 | §11 已从单行来源改为统一来源块;§12、§13 已补来源和延伸阅读。 |
| Batch 2-C | §14 | 已补 §14 自身校准来源块;参考表已重写为当前实际使用材料,不再把旧 `02_hld_step_13_formal_document_assembly.md` 当作当前正式参考。 |

### 3. 写入边界核对

| 检查项 | 结果 |
|---|---|
| §1~§14 是否都有具体校准来源 | pass。 |
| §1~§14 是否都有延伸阅读入口 | pass。 |
| §14 参考是否只列实际使用材料 | pass。 |
| 旧 `02_hld_step_13_formal_document_assembly.md` 是否仍作为当前正式参考出现 | no。 |
| 是否新增对象、接口、流程、状态、异常、配置或风险结论 | no。 |
| 是否关闭 Step 14 | no;后续仍需旧材料差异审计、自检与正式回填记录。 |

### 4. 后续分流

| 后续模块 | 继续处理内容 |
|---|---|
| `R1.13/R1.14 旧材料差异审计` | 审计旧 `02_hld_step_13_formal_document_assembly.md`、历史 §14 口径和旧术语保留点,裁决哪些仅为禁入说明、哪些需后续清理。 |
| `R1.15/R1.16 自检与停审` | 检查正式全文是否无旧主线污染、无详细设计下沉、无未讨论结论。 |
| `R1.17 正式全文回填记录` | 记录正式 02 装配完成,并更新 flow / 台账到后续文档入口。 |

### 5. 下一写入边界

下一步只允许进入:

```text
R1.13 旧材料差异审计:先思考
```

`R1.13` 只允许制定旧材料差异审计策略,不得直接修改正式 `02-概要设计.md`。

### 6. 自检

| 检查项 | 结果 |
|---|---|
| 是否按用户确认进入 `R1.12`? | 是。 |
| 是否只修改校准来源块和 §14 参考表? | 是。 |
| 是否未新增概要结论? | 是。 |
| 是否把下一步限制为 `R1.13`? | 是。 |

---

## R1.13 旧材料差异审计:先思考

### 1. 本模块要回答的问题

`R1.13` 只制定旧材料差异审计策略,不修改正式 `02-概要设计.md`。

| 问题 | 本轮判断 |
|---|---|
| 本轮旧材料差异审计的对象是什么? | 旧 `02_hld_step_13_formal_document_assembly.md`、旧正式 §14 参考口径、正式全文中的旧术语保留点。 |
| 旧 Step 13 是否仍可作为当前 Step 14 真相源? | 不可。旧 Step 13 属于 historical material,只用于污染审计。 |
| 旧术语命中是否都要删除? | 不一定。作为禁入说明、历史污染说明、否定约束出现的旧术语可以保留;作为正向设计主线出现的才需要清理。 |
| 本模块是否可以直接裁决并修正文档? | 不可以。裁决和记录留给 `R1.14`;本模块只定义审计维度、分类标准和输出表。 |
| `R1.14` 是否允许大改正式正文? | 不应大改。若发现正向旧主线残留,先记录为正式修正候选;只有明显小修才可在用户确认后执行。 |

### 2. 旧材料差异来源

| 审计对象 | 当前定位 | 主要风险 |
|---|---|---|
| 旧 `02_hld_step_13_formal_document_assembly.md` | historical_material | 它使用旧 13 章主链,并把 MethodContent、P0 / P1、outbox、snapshot、fingerprint、ViewProfile 等写成正向概要主线。 |
| 旧正式 §14 参考口径 | 已由 `R1.12` 修正 | 需确认旧 formal assembly 不再作为当前正式参考。 |
| 正式 `02-概要设计.md` 全文旧词命中 | 当前文档审计对象 | 多数旧词应处于禁入、否定约束或历史污染说明语境;需防止仍有正向主线残留。 |
| 历史 `03_ddd_*`、`04~07` | historical_material | 不参与当前概要真相源,只在必要时作为污染来源线索。 |

### 3. 审计维度

| 维度 | 检查内容 | 输出 |
|---|---|---|
| 章节结构差异 | 旧 Step 13 的 13 章主链 vs 当前 14 章主链。 | 旧结构是否已完全退出正式参考。 |
| 主语差异 | 旧 MethodContent / 7 subtype / P0 publish 主线 vs 当前 method asset definition / formalization / consumption / trace / relation / external / maintenance / peripheral 主线。 | 旧主语命中分类表。 |
| 机制差异 | 旧 outbox / snapshot / fingerprint / ViewProfile / lifecycle / plugin / configuration 是否仍被正向使用。 | 正向残留或禁入说明分类。 |
| 参考差异 | §14 是否只列当前实际使用材料。 | 参考污染检查表。 |
| 详细设计下沉差异 | 旧 material 是否诱导 schema、trait、DDL、topic、worker、retry、测试或实施内容进入正式 02。 | 下沉风险表。 |
| 待确认差异 | 旧文把待确认事项写成定论的部分是否回流。 | 待确认边界检查表。 |

### 4. 旧术语分类标准

| 分类 | 判断标准 | 处理 |
|---|---|---|
| `positive_legacy_residue` | 旧术语作为当前设计正向对象、接口、流程、状态或机制出现。 | `R1.14` 标为必须修正;必要时小范围正式修正。 |
| `negative_guard_allowed` | 旧术语出现在“不得恢复 / 不继承 / 禁入 / 旧主线冲突”语境中。 | 可保留,并记录保留理由。 |
| `historical_reference_allowed` | 旧术语出现在变更记录、历史材料定位或差异审计语境中。 | 可保留,但不得扩展成正式参考。 |
| `handoff_risk_marker` | 旧术语出现在 03 / 04 / 后续承接警示中,用于防止后续回流。 | 可保留,但需说明不是当前概要主线。 |
| `unclear_context` | 命中上下文不足以判断。 | `R1.14` 记录为待人工复核,不直接改正文。 |

### 5. `R1.14` 输出表设计

| 输出表 | 内容 |
|---|---|
| 旧 Step 13 差异审计表 | 旧 Step 13 中哪些结论已被当前 Step 1~14 替换、哪些只作污染审计、哪些不得再引用。 |
| 旧术语命中分类表 | 按 `positive_legacy_residue`、`negative_guard_allowed`、`historical_reference_allowed`、`handoff_risk_marker`、`unclear_context` 分类。 |
| 正式修正候选表 | 若仍有正向旧主线残留,列位置、问题、建议处理。 |
| 保留理由表 | 对保留的旧词说明其处于禁入或历史说明语境。 |
| 后续自检输入表 | 把需要 `R1.15/R1.16` 最终检查的点列成清单。 |

### 6. `R1.14` 允许与禁止

| 类型 | 规则 |
|---|---|
| 允许 | 写入审计结论;记录旧材料定位;记录旧术语保留理由;必要时提出正式修正候选。 |
| 有条件允许 | 对明显且小范围的正向旧主线残留做正式修正,但必须先在审计表中说明。 |
| 禁止 | 大规模重写正式正文;恢复旧 Step 13 作为参考;新增对象、接口、流程、状态、异常、配置或风险结论;关闭 Step 14。 |

### 7. 下一写入边界

下一步只允许进入:

```text
R1.14 旧材料差异审计:再写入
```

`R1.14` 应执行旧材料差异审计并写入结论。若发现需要正式正文小修,必须先列入审计表,再做最小范围处理。

### 8. 自检

| 检查项 | 结果 |
|---|---|
| 是否按用户确认进入 `R1.13`? | 是。 |
| 是否只制定旧材料差异审计策略? | 是。 |
| 是否未修改正式 `02-概要设计.md`? | 是。 |
| 是否明确旧术语分类标准? | 是。 |
| 是否把下一步限制为 `R1.14`? | 是。 |

---

## R1.14 旧材料差异审计:再写入

### 1. 写入记录

| 项目 | 记录 |
|---|---|
| 当前模块 | `R1.14 旧材料差异审计:再写入` |
| 上一模块 | `R1.13 旧材料差异审计:先思考` |
| 用户确认 | 已确认进入 `R1.14` |
| 本模块允许 | 写入旧材料差异审计结论、旧术语分类、保留理由和后续自检输入。 |
| 本模块禁止 | 大规模重写正式正文;恢复旧 Step 13 作为参考;新增概要结论;关闭 Step 14。 |
| 下一模块 | `R1.15 自检与停审:先思考` |

### 2. 旧 Step 13 差异审计表

| 旧材料项 | 旧口径 | 当前裁决 |
|---|---|---|
| 旧 Step 13 文档身份 | 旧 `Step 13. 整理正式概要设计文档`,状态为已确认。 | 当前已降级为 `historical_material`;本轮正式装配真相源是当前 `02_hld_step_14_formal_document_assembly.md`。 |
| 章节主链 | 旧文要求正式 02 按 13 章主链重组。 | 已被当前 14 章主链替换;§14 参考为独立章节。 |
| §3 / §6 / §8 / §9 来源 | 旧文把 P0 / P1、MethodContent、outbox、snapshot、fingerprint、ViewProfile、MethodContentLifecycle、OutboxEventStatus 写成正向主线。 | 已被当前 method asset definition、formalization、controlled consumption、trace、relation、external summary、maintenance、peripheral 主线替换。 |
| 旧参考范围 | 旧文把 Step 1~12 和旧 formal assembly 作为当前回填来源。 | 当前 §14 已改为只列本轮实际使用材料;旧 Step 13 不进入正式参考。 |
| 旧术语统一表 | 旧文要求统一使用 `MethodContent`、`DefinitionSnapshot`、`OutboxEvent`、`Fingerprint` 等。 | 当前正式正文只允许这些词以禁入、历史或回退警示语境出现。 |
| 旧待确认事项 | 旧文待确认围绕 ViewProfile、snapshot schema、P1 plugin / configuration 等。 | 当前 §13 已改为外部 / 下游依赖、消费影响摘要、外部依据可用性、外围 package / method set 范围。 |

### 3. 正式全文旧术语命中分类表

| 命中范围 | 代表命中 | 分类 | 处理 |
|---|---|---|---|
| 文档元信息 / 变更记录 | `MethodContent`;`publish`;`snapshot`;`outbox`;`P0 / P1` | `historical_reference_allowed` | 保留。用于说明 v0.2.2 / v0.2.3 已清理旧主线。 |
| §3 约束条件 | `MethodPlugin / MethodConfiguration`;`fingerprint`;`snapshot`;`outbox` | `negative_guard_allowed` | 保留。语境是外围增强不得成为核心前置、旧实现机制不得直接继承。 |
| §5 主要组成部分 | `MethodContent`;`publish`;`snapshot`;`fingerprint`;`outbox`;`P1 plugin` | `negative_guard_allowed` | 保留。语境是当前设计不再采用旧主线、旧对象禁入、旧机制需重新闭口。 |
| §6 关键对象 | `MethodContent`;`MethodContentLifecycle`;`DefinitionSnapshot`;`Fingerprint`;`OutboxEvent`;`P0`;`P1` | `negative_guard_allowed` | 保留。语境是旧对象不得作为关键对象展开。 |
| §7 API / 接口 | `MethodContent`;`publish`;`snapshot`;`fingerprint`;`outbox` | `negative_guard_allowed` | 保留。语境是旧接口、旧 snapshot / fingerprint / outbox API 禁入。 |
| §8 处理流 | `MethodContent`;`DefinitionSnapshot`;`fingerprint`;`OutboxEvent`;`publish`;`snapshot`;`outbox` | `negative_guard_allowed` | 保留。语境是旧处理流不作为当前来源、event candidate 不等于 outbox。 |
| §9 状态 | `MethodContentLifecycle`;`OutboxEventStatus`;`snapshot`;`fingerprint`;`P1 plugin / configuration` | `negative_guard_allowed` | 保留。语境是当前状态机不得继承旧 lifecycle / outbox status。 |
| §10 异常 | `MethodContent`;`outbox`;`snapshot`;`fingerprint`;`MethodPlugin`;`MethodConfiguration` | `negative_guard_allowed` | 保留。语境是异常边界不再采用旧主线。 |
| §11 配置影响 | `P0 / P1`;`publisher`;`outbound`;`external`;`delivery` | `handoff_risk_marker` | 保留。语境是配置不得翻转边界、不得改变 P0 / P1 范围隔离。 |
| §12 承接 | `publish`;`snapshot`;`outbox`;`MethodContent` | `handoff_risk_marker` | 保留。语境是详细设计若要恢复旧机制必须回退概要。 |
| §13 风险 / 待确认 | `P0 / P1`;`Publisher`;`handoff`;`package / method set` | `handoff_risk_marker` | 保留。语境是后续设计风险和外围范围边界。 |
| §14 参考 | 旧 `02_hld_step_13_formal_document_assembly.md` | none | 未命中;已不作为当前正式参考。 |

### 4. 正式修正候选表

| 候选 | 位置 | 结论 |
|---|---|---|
| 正向旧 MethodContent 主线残留 | 正式全文扫描 | 未发现。 |
| 正向 outbox / snapshot / fingerprint 机制残留 | 正式全文扫描 | 未发现。 |
| 旧 Step 13 作为当前正式参考残留 | §14 参考表 | 未发现。 |
| 旧 ViewProfile / ResolveViewProfile 正向目标残留 | 正式全文扫描 | 未发现。 |
| 旧 P0 / P1 作为当前核心范围主线残留 | 正式全文扫描 | 未发现;仅作为范围隔离或历史清理说明出现。 |

本模块不执行正式正文修正。

### 5. 保留理由表

| 保留类型 | 保留理由 |
|---|---|
| 变更记录中的旧词 | 用于说明当前版本清理了哪些旧主线,不构成设计结论。 |
| 禁入说明中的旧词 | 用于防止后续 Step、03 / 04 或实现阶段恢复旧对象、旧接口、旧流程或旧状态。 |
| 回退规则中的旧词 | 用于说明若后续确需恢复类似机制,必须回退概要重新闭口。 |
| 配置 / 风险章节中的 P0 / P1 | 用于维持范围隔离和外围能力不阻塞 core 的边界。 |
| outbox / publisher / delivery 相关词 | 当前只作为“event candidate 不等于 delivery / outbox”的边界说明出现。 |

### 6. 后续自检输入表

| 后续模块 | 自检输入 |
|---|---|
| `R1.15 自检与停审:先思考` | 检查旧术语是否均处于历史、禁入、回退或风险语境;检查 §14 未重新引入旧 Step 13 参考。 |
| `R1.16 自检与停审:再写入` | 若自检仍无正向旧主线残留,可关闭 Step 14 内容层工作。 |
| `R1.17 正式全文回填记录:再写入` | 记录正式 02 装配完成,并更新 flow / 台账到后续文档等待状态。 |

### 7. 下一写入边界

下一步只允许进入:

```text
R1.15 自检与停审:先思考
```

`R1.15` 只允许制定最终自检与停审策略,不得直接修改正式 `02-概要设计.md`。

### 8. 自检

| 检查项 | 结果 |
|---|---|
| 是否按用户确认进入 `R1.14`? | 是。 |
| 是否完成旧材料差异审计结论? | 是。 |
| 是否未修改正式 `02-概要设计.md`? | 是。 |
| 是否未发现需要本模块立刻修正的正向旧主线残留? | 是。 |
| 是否把下一步限制为 `R1.15`? | 是。 |

---

## R1.15 自检与停审:先思考

### 1. 本模块要回答的问题

`R1.15` 只制定 Step 14 的最终自检和停审策略,不关闭 Step 14,也不修改正式 `02-概要设计.md`。

| 问题 | 本轮判断 |
|---|---|
| Step 14 是否已经具备停审候选条件? | 具备候选条件。§1~§14 已完成装配、校准来源、延伸阅读和旧材料差异审计,但仍需 `R1.16` 写入正式停审记录。 |
| 本模块是否可以直接宣布 02 概要设计完成? | 不可以。完成声明必须放在 `R1.16` 停审记录和 `R1.17` 正式全文回填记录中。 |
| 自检对象是什么? | 正式 `02-概要设计.md` 全文、当前 Step 14 主控、文档级 flow、项目级台账。 |
| 自检重点是什么? | 14 章主链、章节来源、旧材料降级、详细设计下沉、未讨论结论、三层门禁和后续文档入口。 |
| 如果发现问题怎么办? | `R1.16` 不得关闭 Step 14;必须记录为 blocker 或回退到对应模块修正。 |

### 2. 最终自检清单

| 检查组 | 检查项 | 通过标准 | 不通过处理 |
|---|---|---|---|
| 章节主链 | 正式文档是否保持 14 章主链。 | §1~§14 均存在,章节角色与概要规范一致。 | 回退到 `R1.10/R1.12` 修正装配或参考。 |
| 章节来源 | 每章是否有校准来源。 | §1~§14 均能追溯到当前 `02_hld_step_*` 或当前 Step 14 记录。 | 补来源;不得用历史 Step 13 替代。 |
| 延伸阅读 | 每章是否给出延伸阅读入口。 | 读者能从正式正文回到对应中间产物。 | 补延伸阅读入口。 |
| §14 参考 | §14 是否只列实际使用材料。 | 不列旧 Step 13 为当前正式参考;不把临时 `/tmp` 计划写入正式参考。 | 回退 `R1.12`。 |
| 旧材料 | 旧 Step 13 是否已降级。 | 只作为 historical material / 差异审计对象存在。 | 回退 `R1.13/R1.14`。 |
| 旧术语 | 旧术语是否均处于允许语境。 | 只出现在变更记录、禁入说明、回退规则、历史说明或风险提醒中。 | 若为正向主线残留,先记录 blocker,再最小修正。 |
| 概要深度 | 是否避免详细设计下沉。 | 不出现字段全集、完整函数签名、schema、DDL、topic、worker、测试证据、实施 commit 边界。 | 回退对应章节,删除或降级为 03/04/05/07 承接。 |
| 结论来源 | 是否无未讨论新增结论。 | 正式正文所有核心结论均来自 Step 1~14 已确认内容。 | 回退到对应 Step 补讨论,不得在 Step 14 直接补口。 |
| 横向一致性 | §5 组成部分是否支撑 §6~§10。 | 对象、接口、处理流、状态和异常均能回指主要组成部分。 | 回退对应 Step 的停审记录或正式章节。 |
| 后续承接 | §11~§13 是否保持后续文档边界。 | 配置、详细设计承接、风险待确认不被写成已落码事实。 | 回退 §11~§13。 |
| 三层门禁 | Step 文件、flow、项目台账是否一致。 | 三者都指向同一个下一动作。 | 先修正台账,不得继续。 |

### 3. 停审条件

`R1.16` 只有在以下条件全部满足时,才允许写入 Step 14 停审记录:

| 条件 | 说明 |
|---|---|
| formal_document_complete | 正式 `02-概要设计.md` 已完成 §1~§14 装配。 |
| source_trace_complete | 每章均有校准来源和延伸阅读入口。 |
| reference_table_clean | §14 参考材料只列当前实际使用材料。 |
| historical_material_isolated | 旧 Step 13 和历史 `03_ddd_*` 未作为当前真相源。 |
| legacy_terms_guarded | 旧术语只作为历史、禁入、回退或风险语境保留。 |
| no_detail_design_leak | 未把详细设计、配置 schema、测试证据或实施计划写入概要正文。 |
| no_undiscussed_conclusion | 未新增 Step 1~14 未确认的概要结论。 |
| gate_alignment_pass | Step 主控、flow、项目台账均对齐到同一恢复点。 |

### 4. 停审失败条件

任一条件命中时,`R1.16` 不得关闭 Step 14:

| 失败条件 | 处理 |
|---|---|
| 发现正向旧主线残留 | 回退 `R1.14`,先记录并裁决是否最小修正。 |
| 发现章节缺来源或 §14 引用旧材料 | 回退 `R1.12`,修正来源和参考。 |
| 发现正式正文出现 schema / DDL / 完整函数签名 / 测试证据 / commit 计划 | 回退相应正式章节,降级为后续文档承接。 |
| 发现 §13 风险被改写成已闭口结论 | 回退 Step 13 或正式 §13。 |
| flow 与项目台账下一动作不一致 | 先修正台账,不得推进。 |
| 发现当前 Step 14 新增了未在 Step 1~13 讨论的结论 | 不在 Step 14 补口;回退到对应 Step 或记录 open question。 |

### 5. `R1.16` 写入计划

| 写入块 | 内容 | 禁止 |
|---|---|---|
| 停审执行记录 | 记录用户确认、检查范围和检查结果。 | 不新增正文结论。 |
| 自检结果表 | 按本模块检查组逐项给出 pass / fail。 | 不用笼统 “已检查” 代替具体项。 |
| blocker 表 | 若无 blocker,明确写 no active blocker。 | 不隐藏失败条件。 |
| Step 14 关闭判断 | 若全部通过,标记 Step 14 content closed。 | 不直接推进 03。 |
| 下一动作 | 进入 `R1.17 正式全文回填记录:再写入`。 | 不跳过 `R1.17`。 |

### 6. 本模块不修改正式正文的原因

`R1.15` 属于先思考模块。正式正文已在 `R1.10` 和 `R1.12` 完成装配类修改,旧材料差异已在 `R1.14` 完成审计。本模块的价值是把 `R1.16` 的停审判断固定为可检查清单,避免直接凭记忆宣布完成。

### 7. 下一写入边界

下一步只允许进入:

```text
R1.16 自检与停审:再写入
```

`R1.16` 应按本模块自检清单写入停审记录。

`R1.16` 不得写入:

- 新的概要设计结论。
- 正式正文大改。
- 详细设计、配置、测试、验收或实施计划内容。
- 直接跳到后续 `03-详细设计`。

### 8. 自检

| 检查项 | 结果 |
|---|---|
| 是否按用户确认进入 `R1.15`? | 是。 |
| 是否未修改正式 `02-概要设计.md`? | 是。 |
| 是否制定最终自检清单? | 是。 |
| 是否明确停审条件和失败条件? | 是。 |
| 是否把下一步限制为 `R1.16`? | 是。 |

---

## R1.16 自检与停审:再写入

### 1. 执行记录

| 项目 | 记录 |
|---|---|
| 当前模块 | `R1.16 自检与停审:再写入` |
| 上一模块 | `R1.15 自检与停审:先思考` |
| 用户确认 | 已确认进入 `R1.16` |
| 本模块允许 | 按 `R1.15` 清单执行自检并写入停审记录。 |
| 本模块禁止 | 新增概要结论;修改正式正文;直接推进 `03-详细设计`。 |
| 本模块结论 | 自检未通过,Step 14 不关闭。 |
| 下一模块 | `R1.16a 章节来源缺口修正:先思考` |

### 2. 自检证据

| 检查对象 | 检查结果 |
|---|---|
| 正式 `02-概要设计.md` 章节主链 | 已识别 §1~§14,章节主链存在。 |
| 正式 `02-概要设计.md` 校准来源 | §1~§14 均存在 `校准来源`。 |
| 正式 `02-概要设计.md` 延伸阅读 | §1、§5~§14 存在 `延伸阅读`;§2、§3、§4 缺失。 |
| 正式 §14 参考表 | 当前只列本轮实际使用材料,未把旧 `02_hld_step_13_formal_document_assembly.md` 列为正式参考。 |
| 旧材料差异审计 | `R1.14` 已将旧 Step 13 降级为 historical material。 |
| 详细设计下沉词命中 | `schema`、`DDL`、`topic`、`worker`、`commit` 等命中均处于禁止、非范围或后续承接语境,未作为概要正向契约展开。 |
| 三层门禁 | 本次写入后需同步 Step 主控、flow、项目台账到同一 blocker 恢复点。 |

### 3. 自检结果表

| 检查组 | 结果 | 说明 |
|---|---|---|
| 章节主链 | pass | 正式文档存在 §1~§14。 |
| 章节来源 | pass | §1~§14 均有 `校准来源`。 |
| 延伸阅读 | fail | §2、§3、§4 缺 `延伸阅读` 块,不满足 `source_trace_complete`。 |
| §14 参考 | pass | 未发现旧 Step 13 作为当前正式参考。 |
| 旧材料 | pass | 旧 Step 13 仅作为历史差异审计对象。 |
| 旧术语 | pass | 旧术语保留点处于历史、禁入、回退或风险语境。 |
| 概要深度 | pass | 详细设计级词汇未作为正向 schema / DDL / worker / commit 契约展开。 |
| 结论来源 | pass | 本轮未发现 Step 14 新增未讨论概要结论。 |
| 横向一致性 | pass | §5 仍支撑 §6~§10 的对象、接口、流程、状态和异常主线。 |
| 后续承接 | pass | §11~§13 仍保持配置、详细设计承接和风险边界。 |
| 三层门禁 | pass_after_sync | 本模块写入后由 flow 和项目台账同步到同一 blocker 恢复点。 |

### 4. Blocker 表

| Blocker ID | 状态 | 位置 | 描述 | 处理 |
|---|---|---|---|---|
| ML-S14-GAP-001 | open | 正式 `02-概要设计.md` §2、§3、§4 | 三章缺 `延伸阅读` 块,不满足 Step 14 停审条件 `source_trace_complete`。 | 下一步先进入 `R1.16a 章节来源缺口修正:先思考`,制定最小补丁策略。 |

### 5. Step 14 关闭判断

| 判断项 | 结论 |
|---|---|
| 是否允许关闭 Step 14 内容层工作 | 否。 |
| 是否允许进入 `R1.17 正式全文回填记录:再写入` | 否。 |
| 是否允许直接启动 `03-详细设计` | 否。 |
| 是否需要修改正式正文 | 是,但不能在本模块直接修改;需先进入缺口修正先思考。 |

### 6. 下一写入边界

下一步只允许进入:

```text
R1.16a 章节来源缺口修正:先思考
```

`R1.16a` 只允许制定 §2、§3、§4 `延伸阅读` 最小补丁策略,不得新增概要结论。

### 7. 自检

| 检查项 | 结果 |
|---|---|
| 是否按用户确认进入 `R1.16`? | 是。 |
| 是否未修改正式 `02-概要设计.md`? | 是。 |
| 是否按 `R1.15` 清单执行检查? | 是。 |
| 是否如实记录停审失败原因? | 是。 |
| 是否未关闭 Step 14? | 是。 |

---

## R1.16a 章节来源缺口修正:先思考

### 1. 本模块要回答的问题

`R1.16a` 只制定 §2、§3、§4 `延伸阅读` 缺口的最小补丁策略,不修改正式 `02-概要设计.md`。

| 问题 | 本轮判断 |
|---|---|
| 缺口是什么? | 正式 §2、§3、§4 已有 `校准来源`,但缺 `延伸阅读` 块。 |
| 是否需要重写章节正文? | 不需要。三章正文结论已在前序装配中形成,当前只补追溯入口。 |
| 是否可以补充新概要结论? | 不可以。延伸阅读只能指向已存在中间产物和已讨论内容。 |
| 是否可以关闭 Step 14? | 不可以。需等 `R1.16b` 写入补丁后重新自检。 |

### 2. 最小补丁范围

| 章节 | 当前状态 | 补丁内容 |
|---|---|---|
| §2 本次设计目标与范围 | 有 `校准来源`,缺 `延伸阅读`。 | 在校准来源后补一条指向 `02_hld_step_02_scope.md` 的延伸阅读。 |
| §3 约束条件 | 有 `校准来源`,缺 `延伸阅读`。 | 在校准来源后补一条指向 `02_hld_step_03_constraints.md` 的延伸阅读。 |
| §4 代码主体框架总览 | 有 `校准来源`,缺 `延伸阅读`。 | 在校准来源后补一条指向 `02_hld_step_04_code_subject_framework.md` 的延伸阅读。 |

### 3. 拟写入文本

| 章节 | 拟补文本 |
|---|---|
| §2 | `> 延伸阅读:` / `> - 建议继续阅读 design-calibration/02_hld_step_02_scope.md 的“结构化中间产物”“回填草稿”和“旧材料差异审计”小节,了解设计目标、范围、非范围和设计深度如何收敛。` |
| §3 | `> 延伸阅读:` / `> - 建议继续阅读 design-calibration/02_hld_step_03_constraints.md 的“约束候选池”“结构化中间产物”和“回填草稿”小节,了解约束如何约束后续对象、接口、流程和状态。` |
| §4 | `> 延伸阅读:` / `> - 建议继续阅读 design-calibration/02_hld_step_04_code_subject_framework.md 的“代码主体框架”“实现分层视图”和“正式回填草稿”小节,了解架构模块如何映射为概要层代码主体。` |

### 4. 写入边界

| 允许 | 禁止 |
|---|---|
| 只在 §2、§3、§4 的 `校准来源` 后补 `延伸阅读`。 | 修改 §2、§3、§4 正文结论。 |
| 使用已存在的当前 `02_hld_step_*` 中间产物作为阅读入口。 | 引入旧 Step 13、历史 `03_ddd_*` 或 `/tmp` 临时计划。 |
| 写入后更新 flow 和项目台账,并把 blocker 标为 pending_recheck。 | 直接关闭 Step 14 或启动 `03-详细设计`。 |

### 5. `R1.16b` 验收口径

| 检查项 | 通过标准 |
|---|---|
| §2 延伸阅读 | §2 的 `校准来源` 后存在 `延伸阅读`。 |
| §3 延伸阅读 | §3 的 `校准来源` 后存在 `延伸阅读`。 |
| §4 延伸阅读 | §4 的 `校准来源` 后存在 `延伸阅读`。 |
| 正文结论 | §2、§3、§4 除新增延伸阅读外无正文结论变化。 |
| 三层门禁 | Step 主控、flow、项目台账同步到重新自检入口。 |

### 6. 下一写入边界

下一步只允许进入:

```text
R1.16b 章节来源缺口修正:再写入
```

`R1.16b` 允许对正式 `02-概要设计.md` 执行上述三处最小补丁,然后将下一动作切回停审自检。

### 7. 自检

| 检查项 | 结果 |
|---|---|
| 是否按用户确认进入 `R1.16a`? | 是。 |
| 是否未修改正式 `02-概要设计.md`? | 是。 |
| 是否只制定 §2~§4 延伸阅读补丁策略? | 是。 |
| 是否未新增概要结论? | 是。 |
| 是否把下一步限制为 `R1.16b`? | 是。 |

---

## R1.16b 章节来源缺口修正:再写入

### 1. 写入记录

| 项目 | 记录 |
|---|---|
| 当前模块 | `R1.16b 章节来源缺口修正:再写入` |
| 上一模块 | `R1.16a 章节来源缺口修正:先思考` |
| 用户确认 | 已确认进入 `R1.16b` |
| 本模块允许 | 对正式 `02-概要设计.md` §2、§3、§4 执行三处 `延伸阅读` 最小补丁。 |
| 本模块禁止 | 修改 §2、§3、§4 正文结论;新增概要结论;关闭 Step 14。 |
| 下一模块 | `R1.16c 停审复检:先思考` |

### 2. 正式正文补丁结果

| 章节 | 写入结果 |
|---|---|
| §2 本次设计目标与范围 | 已在 `校准来源` 后补 `延伸阅读`,指向 `02_hld_step_02_scope.md`。 |
| §3 约束条件 | 已在 `校准来源` 后补 `延伸阅读`,指向 `02_hld_step_03_constraints.md`。 |
| §4 代码主体框架总览 | 已在 `校准来源` 后补 `延伸阅读`,指向 `02_hld_step_04_code_subject_framework.md`。 |

### 3. 写入边界核对

| 检查项 | 结果 |
|---|---|
| 是否只补 §2、§3、§4 延伸阅读 | 是。 |
| 是否修改 §2、§3、§4 正文结论 | 否。 |
| 是否引入旧 Step 13、历史 `03_ddd_*` 或 `/tmp` 临时计划 | 否。 |
| 是否关闭 Step 14 | 否。 |

### 4. Blocker 状态

| Blocker ID | 状态 | 说明 |
|---|---|---|
| ML-S14-GAP-001 | pending_recheck | 三处延伸阅读已补齐,需进入 `R1.16c` 重新执行停审复检。 |

### 5. 下一写入边界

下一步只允许进入:

```text
R1.16c 停审复检:先思考
```

`R1.16c` 应重新检查 §1~§14 是否均有 `校准来源` 和 `延伸阅读`,并决定是否允许进入最终停审记录。

### 6. 自检

| 检查项 | 结果 |
|---|---|
| 是否按用户确认进入 `R1.16b`? | 是。 |
| 是否完成三处最小补丁? | 是。 |
| 是否未新增概要结论? | 是。 |
| 是否未关闭 Step 14? | 是。 |
| 是否把下一步限制为 `R1.16c`? | 是。 |

---

## R1.16c 停审复检:先思考

### 1. 本模块要回答的问题

`R1.16c` 只执行停审复检的先思考判断,不修改正式 `02-概要设计.md`,不关闭 Step 14。

| 问题 | 本轮判断 |
|---|---|
| `ML-S14-GAP-001` 是否已具备关闭条件? | 具备候选关闭条件。§2、§3、§4 已补 `延伸阅读`,需在最终停审记录中正式标记 resolved。 |
| §1~§14 是否均有 `校准来源` 与 `延伸阅读`? | 是。复检命中 §1~§14 均成对存在。 |
| 是否发现旧 Step 13 被重新列为正式参考? | 未发现。旧 Step 13 仍只作为历史差异审计对象。 |
| 是否发现详细设计内容下沉为概要正向契约? | 未发现。`schema`、`DDL`、`topic`、`worker`、`commit` 等词仍处于禁止、非范围或后续承接语境。 |
| 是否可以进入最终停审记录? | 可以。下一步应进入 `R1.16d 最终停审记录:再写入`。 |

### 2. 复检结果

| 检查组 | 结果 | 证据 |
|---|---|---|
| 章节主链 | pass | 正式文档存在 §1~§14。 |
| 校准来源 | pass | §1~§14 均有 `校准来源`。 |
| 延伸阅读 | pass | §1~§14 均有 `延伸阅读`;§2~§4 已由 `R1.16b` 补齐。 |
| §14 参考 | pass | §14 参考表未列旧 `02_hld_step_13_formal_document_assembly.md`。 |
| 旧材料隔离 | pass | 旧 Step 13 仅在 Step 14 审计记录中作为 historical material 出现。 |
| 概要深度 | pass | 详细设计级词汇未作为正向契约展开。 |
| 三层门禁 | pass_after_sync | 本模块写入后需同步到 `R1.16d`。 |

### 3. Blocker 处理建议

| Blocker ID | 当前状态 | 建议 |
|---|---|---|
| ML-S14-GAP-001 | pending_recheck | 在 `R1.16d` 标记为 resolved,并关闭 Step 14 内容层工作。 |

### 4. `R1.16d` 写入计划

| 写入块 | 内容 |
|---|---|
| 最终停审记录 | 记录复检通过、用户确认、检查范围和结论。 |
| 停审结果表 | 逐项确认章节主链、来源追溯、参考表、旧材料隔离和概要深度。 |
| blocker 状态 | 将 `ML-S14-GAP-001` 标记为 resolved。 |
| Step 14 关闭判断 | 标记 Step 14 content closed。 |
| 下一动作 | 进入 `R1.17 正式全文回填记录:再写入`。 |

### 5. 下一写入边界

下一步只允许进入:

```text
R1.16d 最终停审记录:再写入
```

`R1.16d` 允许写入最终停审记录,但不得新增概要结论,不得直接启动 `03-详细设计`。

### 6. 自检

| 检查项 | 结果 |
|---|---|
| 是否按用户确认进入 `R1.16c`? | 是。 |
| 是否未修改正式 `02-概要设计.md`? | 是。 |
| 是否完成复检先思考判断? | 是。 |
| 是否未关闭 Step 14? | 是。 |
| 是否把下一步限制为 `R1.16d`? | 是。 |

---

## R1.16d 最终停审记录:再写入

### 1. 停审执行记录

| 项目 | 记录 |
|---|---|
| 当前模块 | `R1.16d 最终停审记录:再写入` |
| 上一模块 | `R1.16c 停审复检:先思考` |
| 用户确认 | 已确认进入 `R1.16d` |
| 本模块允许 | 写入最终停审记录、关闭 `ML-S14-GAP-001`、标记 Step 14 内容层关闭。 |
| 本模块禁止 | 新增概要结论;修改正式 `02-概要设计.md`;直接启动 `03-详细设计`。 |
| 本模块结论 | Step 14 内容层自检通过,可进入 `R1.17 正式全文回填记录:再写入`。 |

### 2. 最终停审结果表

| 检查组 | 结果 | 说明 |
|---|---|---|
| 章节主链 | pass | 正式 `02-概要设计.md` 存在 §1~§14。 |
| 校准来源 | pass | §1~§14 均有 `校准来源`。 |
| 延伸阅读 | pass | §1~§14 均有 `延伸阅读`;§2~§4 已由 `R1.16b` 补齐。 |
| §14 参考 | pass | §14 未将旧 `02_hld_step_13_formal_document_assembly.md` 列为当前正式参考。 |
| 旧材料隔离 | pass | 旧 Step 13 和历史 `03_ddd_*` 未作为当前概要真相源。 |
| 旧术语语境 | pass | 旧术语仅作为历史、禁入、回退或风险语境保留。 |
| 概要深度 | pass | 未将 schema、DDL、topic、worker、测试证据或实施 commit 写成概要正向契约。 |
| 未讨论结论 | pass | 本阶段未新增 Step 1~14 外的新概要结论。 |
| 三层门禁 | pass_after_sync | 本模块写入后同步 flow 与项目台账到 `R1.17`。 |

### 3. Blocker 关闭记录

| Blocker ID | 原状态 | 当前状态 | 关闭依据 |
|---|---|---|---|
| ML-S14-GAP-001 | pending_final_close | resolved | §2、§3、§4 已补 `延伸阅读`;`R1.16c` 复检确认 §1~§14 来源追溯均完整。 |

### 4. Step 14 关闭判断

| 判断项 | 结论 |
|---|---|
| Step 14 内容层是否关闭 | 是。 |
| 是否允许进入 `R1.17 正式全文回填记录:再写入` | 是。 |
| 是否允许直接启动 `03-详细设计` | 否。必须先写入 `R1.17` 并同步 flow / 台账。 |
| 是否需要继续修改正式 `02-概要设计.md` | 否。当前仅需写回最终回填记录。 |

### 5. 下一写入边界

下一步只允许进入:

```text
R1.17 正式全文回填记录:再写入
```

`R1.17` 应记录正式 `02-概要设计.md` 装配完成,并更新 flow / 项目台账到后续文档等待状态。

`R1.17` 不得写入:

- 新的概要结论。
- 详细设计内容。
- 配置、测试、验收或实施计划内容。

### 6. 自检

| 检查项 | 结果 |
|---|---|
| 是否按用户确认进入 `R1.16d`? | 是。 |
| 是否未修改正式 `02-概要设计.md`? | 是。 |
| 是否关闭 `ML-S14-GAP-001`? | 是。 |
| 是否标记 Step 14 内容层关闭? | 是。 |
| 是否把下一步限制为 `R1.17`? | 是。 |

---

## R1.17 正式全文回填记录:再写入

### 1. 回填记录

| 项目 | 记录 |
|---|---|
| 当前模块 | `R1.17 正式全文回填记录:再写入` |
| 上一模块 | `R1.16d 最终停审记录:再写入` |
| 用户确认 | 已确认进入 `R1.17` |
| 本模块允许 | 记录正式 `02-概要设计.md` 装配完成,并更新 flow / 项目台账到后续文档等待状态。 |
| 本模块禁止 | 新增概要结论;修改正式正文;直接生成 03/04/05/06/07 内容。 |
| 本模块结论 | `02-概要设计.md` full-restart 装配完成。 |

### 2. 正式文档完成记录

| 项 | 结果 |
|---|---|
| 正式文档 | `projects/L3-method-library/02-概要设计.md` |
| 完成范围 | §1~§14 全文。 |
| 来源追溯 | §1~§14 均有 `校准来源` 与 `延伸阅读`。 |
| 当前 Step 文件 | `design-calibration/02_hld_step_14_formal_document_assembly.md` |
| 当前 flow | `design-calibration/02_hld_calibration_flow.md` |
| 项目台账 | `design-calibration/project_execution_ledger.md` |
| 历史材料口径 | 旧 `02_hld_step_13_formal_document_assembly.md` 和历史 `03_ddd_*` 只作 historical material / 污染审计对象。 |

### 3. 后续文档等待状态

| 后续文档 | 当前状态 | 启动门禁 |
|---|---|---|
| `03-详细设计.md` | waiting_to_start | 必须按详细设计 SOP 新建 / 重启讨论 flow,先读取当前 `00`、`01`、`02` 完成态和历史材料隔离口径。 |
| `04-配置设计.md` | blocked | blocked_by_03_not_restarted。 |
| `05-测试方案.md` | blocked | blocked_by_03_not_restarted。 |
| `06-验收标准.md` | blocked | blocked_by_03_not_restarted。 |
| `07-实施计划.md` | blocked | blocked_by_03_not_restarted。 |

### 4. 关闭状态

| 项 | 状态 |
|---|---|
| Step 14 | completed |
| `02-概要设计.md` | completed |
| `ML-S14-GAP-001` | resolved |
| 下一允许动作 | 等待用户确认启动 `03-详细设计` 讨论;不得自动生成 03 内容。 |

### 5. 自检

| 检查项 | 结果 |
|---|---|
| 是否按用户确认进入 `R1.17`? | 是。 |
| 是否未新增概要结论? | 是。 |
| 是否未修改正式正文? | 是。 |
| 是否记录正式 02 完成状态? | 是。 |
| 是否同步后续文档等待状态? | 是。 |

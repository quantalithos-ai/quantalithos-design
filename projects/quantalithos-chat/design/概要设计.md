# quantalithos-chat 概要设计

> 聊天前端 — 飞书式三栏布局，Vue 3 + Vite，通过 sdk-wasm 与平台交互

---

## 一、项目定位

### 1.1 核心职责

quantalithos-chat 是用户与 AI Agent 团队交互的唯一入口。

```
用户的日常操作：
  1. 和 Assistant 聊天发起新项目
  2. 在项目群聊里看 Agent 讨论、参与对话
  3. 审批门禁卡片（确认 PRD、验收功能）
  4. 查看项目进度、工单看板、产物文档
  5. 管理 Agent 配置和项目设置
```

**不做的事：**
- 不做业务逻辑 — 全部在 sdk-wasm / platform / flow 里
- 不直接调用 platform API — 通过 sdk-wasm 间接调用
- 不做数据持久化 — 数据在 platform，chat 只做渲染和缓存

### 1.2 在整体架构中的位置

```
用户（浏览器 / Tauri 桌面端）
         │
         ▼
┌─────────────────────────────────────────────┐
│  quantalithos-chat (Vue 3 + Vite)           │
│                                             │
│  ┌─────────────────────────────────────┐    │
│  │  UI 组件层（Vue 3 组件）             │    │
│  │  页面、布局、消息渲染、卡片、看板    │    │
│  └──────────────┬──────────────────────┘    │
│                 │                            │
│  ┌──────────────▼──────────────────────┐    │
│  │  SDK 薄封装层（TypeScript）          │    │
│  │  import { QuantalithosClient }      │    │
│  │  from '@quantalithos/sdk-wasm'      │    │
│  └──────────────┬──────────────────────┘    │
│                 │                            │
│  ┌──────────────▼──────────────────────┐    │
│  │  sdk-wasm (Rust → WASM)             │    │
│  │  API 调用、WebSocket、状态管理       │    │
│  └──────────────┬──────────────────────┘    │
└─────────────────┼───────────────────────────┘
                  │ HTTP + WebSocket
                  ▼
         quantalithos-gate (API 网关)
                  │
         ┌────────┼────────┐
         ▼        ▼        ▼
      platform   flow    runtime
```

---

## 二、页面结构

### 2.1 三栏布局

```
┌──────┬──────────┬──────────────────────────────┐
│      │          │                              │
│ 功能  │ 会话列表  │         主内容区              │
│ 导航  │          │                              │
│      │          │                              │
│ 56px │  260px   │          flex: 1             │
│      │          │                              │
└──────┴──────────┴──────────────────────────────┘
```

### 2.2 功能导航（左侧 56px）

```
┌──────┐
│  💬  │  消息 — 会话列表（默认）
│  📊  │  看板 — 当前项目的工单看板
│  📈  │  进度 — 当前项目的流程图
│  📄  │  产物 — 当前项目的产物列表
│  ⚙   │  设置 — Agent 配置、项目设置
│      │
│ (空)  │
│      │
│  👤  │  用户头像 — 个人设置
└──────┘

点击导航图标：
  💬 → 中间显示会话列表，右侧显示聊天
  📊 → 中间隐藏，右侧显示看板（全宽）
  📈 → 中间隐藏，右侧显示进度（全宽）
  📄 → 中间隐藏，右侧显示产物列表（全宽）
  ⚙  → 中间隐藏，右侧显示设置（全宽）
```

### 2.3 会话列表（中间 260px）

```
┌──────────┐
│ 消息   ＋ │  标题 + 新建对话按钮
│──────────│
│ 🔍 搜索   │  搜索会话
│──────────│
│ 置顶      │
│ 🤖 Asst  │  Assistant 私聊（永远置顶）
│──────────│
│ 项目      │
│ 📁 博客群 │  项目群聊（按最新消息排序）
│ 📁 电商群 │  未读角标 + 门禁提醒
│──────────│
│ 已归档    │  已完成项目（折叠）
│ 📁 旧项目 │
└──────────┘

会话类型：
  🤖 Assistant 私聊 — 发起新项目的入口
  📁 项目群聊 — 每个项目一个
  👤 Agent 私聊 — 和某个 Agent 单独对话（按需）
```

### 2.4 主内容区（右侧 flex:1）

根据功能导航和会话选择，显示不同内容：

| 导航 | 会话 | 主内容 |
|------|------|--------|
| 💬 消息 | 选中 Assistant | Assistant 私聊界面 |
| 💬 消息 | 选中项目群聊 | 项目群聊界面 |
| 💬 消息 | 无选中 | 欢迎页 / 新建项目引导 |
| 📊 看板 | — | 当前项目的工单看板 |
| 📈 进度 | — | 当前项目的流程图 |
| 📄 产物 | — | 当前项目的产物列表 |
| ⚙ 设置 | — | 项目设置 / Agent 配置 |

---

## 三、核心页面设计

### 3.1 聊天界面

```
┌──────────────────────────────────────────────────┐
│ 📁 博客系统 · 项目群聊                             │  头部：项目名 + 成员 + 快捷按钮
│ TL, BE-1, FE-1, QA-1 · 阶段3     [📊] [📈] [👥] │
├──────────────────────────────────────────────────┤
│                                                  │
│  🔔 阶段 3 迭代开发已启动                         │  系统消息
│                                                  │
│  ┌─TL─┐                                         │
│  │ 任务分配完成。                                 │  Agent 消息
│  │ [进度条: ✓✓✓●○○]                              │  + 内嵌进度条
│  └─────┘                                         │
│                                                  │
│  ┌─BE─┐                                         │
│  │ TASK-003 实现完成。                            │  Agent 消息
│  │ ┌─────────────────┐                           │  + 内嵌工单卡片
│  │ │ TASK-003 审查中   │                          │
│  │ └─────────────────┘                           │
│  └─────┘                                         │
│                                                  │
│  ┌────────────────────────┐                      │
│  │ 📋 需求评审确认          │                      │  门禁审批卡片
│  │ [✅确认] [✏️修改] [❌驳回]│                      │
│  └────────────────────────┘                      │
│                                                  │
│                          ┌─你─┐                  │
│                          │ 评论功能进展如何？      │  用户消息（右对齐）
│                          └─────┘                  │
│                                                  │
├──────────────────────────────────────────────────┤
│ [输入消息... (Enter 发送)]                [发送]   │  输入区
└──────────────────────────────────────────────────┘
```

**消息类型：**

| 类型 | 样式 | 说明 |
|------|------|------|
| 系统消息 | 居中，黄色背景 | 阶段启动、成员变更等 |
| Agent 消息 | 左对齐，白色气泡 | Agent 的发言 |
| 用户消息 | 右对齐，蓝色气泡 | 用户的发言 |
| 内嵌卡片 | 消息气泡内 | 工单卡片、进度条、任务状态组 |
| 门禁卡片 | 居中，独立卡片 | 审批操作（确认/修改/驳回） |

### 3.2 工单看板

```
┌──────────────────────────────────────────────────┐
│ 📊 博客系统 · 工单看板                             │
│ 5 个工单 · 3 完成 · 1 进行中 · 1 待开始            │
├──────────────────────────────────────────────────┤
│                                                  │
│  📋 待开始(1)   🔨 开发中(1)   👀 审查中(0)   ✅ 已完成(3)  │
│  ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐  │
│  │ T-005   │   │ T-004   │   │         │   │ T-001   │  │
│  │ 前端页面 │   │ 评论系统 │   │  暂无    │   │ 脚手架  │  │
│  │ FE-1    │   │ BE-1    │   │         │   ├─────────┤  │
│  └─────────┘   └─────────┘   └─────────┘   │ T-002   │  │
│                                             │ 文章CRUD │  │
│                                             ├─────────┤  │
│                                             │ T-003   │  │
│                                             │ 标签系统 │  │
│                                             └─────────┘  │
└──────────────────────────────────────────────────┘
```

### 3.3 项目进度

```
┌──────────────────────────────────────────────────┐
│ 📈 博客系统 · 项目进度                             │
│ 标准六阶段流程 · 当前：阶段 3 迭代开发              │
├──────────────────────────────────────────────────┤
│                                                  │
│  Pipeline 进度：                                  │
│  [✓校准]→[✓需求]→[✓设计]→[●开发 3/5]→[○验收]→[○发布] │
│                                                  │
│  当前阶段详情：                                    │
│  ┌──────────────────────────────────────────┐    │
│  │ ✅ T-001 脚手架   implement✓ review✓ test✓│    │
│  │ ✅ T-002 文章CRUD  implement✓ review✓ test✓│    │
│  │ ✅ T-003 标签系统   implement✓ review✓ test✓│    │
│  │ 🔨 T-004 评论系统   implement● review○ test○│    │
│  │ ⏳ T-005 前端页面   等待 T-004              │    │
│  └──────────────────────────────────────────┘    │
└──────────────────────────────────────────────────┘
```

### 3.4 Assistant 私聊（新建项目）

```
┌──────────────────────────────────────────────────┐
│ 🤖 Assistant                                     │
├──────────────────────────────────────────────────┤
│                                                  │
│  ┌─🤖─┐                                         │
│  │ 你好！我是你的 AI 助手。                       │
│  │ 你可以：                                      │
│  │ · 发起新项目 — 告诉我你想做什么                │
│  │ · 查看项目列表 — 管理已有项目                  │
│  │ · 咨询问题 — 技术、流程相关                    │
│  └─────┘                                         │
│                                                  │
│                          ┌─你─┐                  │
│                          │ 我想做一个个人博客系统  │
│                          └─────┘                  │
│                                                  │
│  ┌─🤖─┐                                         │
│  │ 好的！让我了解一下你的需求：                    │
│  │ 1. 博客需要哪些功能？                          │
│  │ 2. 技术栈有偏好吗？                            │
│  │ 3. 大概的时间预期？                            │
│  └─────┘                                         │
│                                                  │
├──────────────────────────────────────────────────┤
│ [输入消息...]                             [发送]   │
└──────────────────────────────────────────────────┘
```

---

## 四、模块划分

```
quantalithos-chat/
├── package.json
├── vite.config.ts
├── index.html
│
├── src/
│   ├── main.ts                      # 入口
│   ├── App.vue                      # 根组件（三栏布局）
│   │
│   ├── sdk/                         # SDK 薄封装
│   │   ├── client.ts                # 初始化 WASM，封装 QuantalithosClient
│   │   ├── types.ts                 # TS 类型（从 sdk-models 映射）
│   │   └── events.ts                # WebSocket 事件监听封装
│   │
│   ├── stores/                      # Pinia 状态管理
│   │   ├── app.ts                   # 全局状态（当前用户、当前项目、主题）
│   │   ├── conversation.ts          # 会话列表（排序、未读数、置顶）
│   │   ├── chat.ts                  # 聊天消息（消息列表、发送、接收）
│   │   ├── kanban.ts                # 看板数据（工单列表、分栏）
│   │   ├── progress.ts              # 进度数据（流程图、节点状态）
│   │   └── gate.ts                  # 门禁状态（待审批列表、操作）
│   │
│   ├── layouts/                     # 布局组件
│   │   ├── AppLayout.vue            # 三栏布局容器
│   │   ├── NavBar.vue               # 左侧功能导航（56px）
│   │   ├── ConvList.vue             # 中间会话列表（260px）
│   │   └── MainContent.vue          # 右侧主内容区（flex:1）
│   │
│   ├── views/                       # 页面级组件
│   │   ├── ChatView.vue             # 聊天页面（消息流 + 输入框）
│   │   ├── KanbanView.vue           # 看板页面（拖拽列）
│   │   ├── ProgressView.vue         # 进度页面（流程图）
│   │   ├── ArtifactView.vue         # 产物页面（列表 + 预览）
│   │   ├── SettingsView.vue         # 设置页面
│   │   └── WelcomeView.vue          # 欢迎页（无选中会话时）
│   │
│   ├── components/                  # 通用组件
│   │   ├── message/                 # 消息相关
│   │   │   ├── MessageBubble.vue    # 消息气泡（Agent/用户/系统）
│   │   │   ├── MessageList.vue      # 消息列表（虚拟滚动）
│   │   │   └── ChatInput.vue        # 输入框（文本 + 发送按钮）
│   │   │
│   │   ├── cards/                   # 内嵌卡片
│   │   │   ├── WorkitemCard.vue     # 工单卡片（内嵌在消息里）
│   │   │   ├── GateCard.vue         # 门禁审批卡片
│   │   │   ├── ProgressBar.vue      # 进度条（内嵌在消息里）
│   │   │   └── TaskGrid.vue         # 任务状态卡片组
│   │   │
│   │   ├── kanban/                  # 看板相关
│   │   │   ├── KanbanColumn.vue     # 看板列
│   │   │   └── KanbanCard.vue       # 看板卡片
│   │   │
│   │   ├── progress/                # 进度相关
│   │   │   ├── PipelineProgress.vue # Pipeline 进度条
│   │   │   └── StageDetail.vue      # 阶段详情（任务流水线）
│   │   │
│   │   └── common/                  # 通用
│   │       ├── Avatar.vue           # 头像（用户/Agent/项目）
│   │       ├── Badge.vue            # 角标（未读数、状态）
│   │       ├── StatusTag.vue        # 状态标签（已完成/开发中/待开始）
│   │       └── TimeAgo.vue          # 相对时间（"5分钟前"）
│   │
│   ├── router/                      # 路由
│   │   └── index.ts                 # Vue Router 配置
│   │
│   └── assets/                      # 静态资源
│       └── styles/
│           ├── variables.css        # CSS 变量（颜色、间距）
│           └── global.css           # 全局样式
│
├── prototype/                       # 原型（已有）
│   └── index.html
│
└── public/
    └── favicon.ico
```

---

## 五、技术选型

| 类别 | 选型 | 说明 |
|------|------|------|
| 框架 | Vue 3 + Composition API | 响应式、生态成熟 |
| 构建 | Vite | 快速 HMR |
| UI 库 | Naive UI | 和原型一致，组件丰富 |
| 状态管理 | Pinia | Vue 3 官方推荐 |
| 路由 | Vue Router 4 | SPA 路由 |
| SDK | @quantalithos/sdk-wasm | Rust WASM 绑定 |
| 图标 | Lucide Icons | 轻量、风格统一 |
| 看板拖拽 | vuedraggable / dnd-kit | 工单看板拖拽 |
| 流程图 | Vue Flow (基于 reactflow) | 进度页面的流程图渲染 |
| 虚拟滚动 | vue-virtual-scroller | 消息列表性能优化 |
| 桌面端 | Tauri（Phase 2） | Rust 后端，直接调用 sdk-store |

---

## 六、数据流

### 6.1 消息收发

```
发送消息：
  用户输入 → ChatInput.vue
    → chatStore.sendMessage()
    → sdk.client.sendMessage(roomId, content)
    → sdk-wasm → WebSocket → gate → flow → runtime → Agent

接收消息：
  Agent 回复 → runtime → flow → gate → WebSocket → sdk-wasm
    → sdk events.onMessage()
    → chatStore.addMessage()
    → MessageList.vue 自动更新
```

### 6.2 门禁审批

```
收到审批请求：
  flow 发送 GateCard → gate → WebSocket → sdk-wasm
    → gateStore.addPendingGate()
    → GateCard.vue 渲染在消息流中
    → 会话列表显示 🔔 提醒

用户操作：
  点击"确认通过" → GateCard.vue
    → gateStore.respondGate(gateId, "approve")
    → sdk.client.respondGate(gateId, "approve")
    → sdk-wasm → HTTP → gate → flow
    → flow 推进到下一阶段
    → 消息流中 GateCard 状态更新为"已确认"
```

### 6.3 看板数据

```
加载看板：
  用户点击 📊 → KanbanView.vue
    → kanbanStore.loadKanban(projectId)
    → sdk.client.getProjectWorkitems(projectId)
    → sdk-wasm → HTTP → gate → platform
    → 返回工单列表 → 按 status 分栏渲染
```

### 6.4 进度数据

```
加载进度：
  用户点击 📈 → ProgressView.vue
    → progressStore.loadProgress(projectId)
    → sdk.client.getProjectProgress(projectId)
    → sdk-wasm → HTTP → gate → platform
    → 返回 ProgressView（pipeline 节点状态 + 当前阶段任务列表）
    → PipelineProgress.vue 渲染进度条
    → StageDetail.vue 渲染任务流水线
```

---

## 七、Phase 分期

### Phase 1

| 功能 | 说明 |
|------|------|
| 三栏布局 | 导航 + 会话列表 + 主内容 |
| Assistant 私聊 | 发起新项目 |
| 项目群聊 | 消息收发、Agent 消息展示 |
| 内嵌卡片 | 工单卡片、进度条、任务状态组 |
| 门禁审批 | GateCard 渲染和操作 |
| 工单看板 | 基础看板（按状态分栏） |
| 项目进度 | Pipeline 进度条 + 阶段任务列表 |

Phase 1 不实现 WebSocket（用轮询），不实现虚拟滚动，不实现拖拽。

### Phase 2

| 功能 | 说明 |
|------|------|
| WebSocket 实时推送 | 消息、状态变更实时更新 |
| 看板拖拽 | 拖拽工单改变状态 |
| 产物预览 | Markdown 渲染、代码高亮 |
| 虚拟滚动 | 消息列表性能优化 |
| Tauri 桌面端 | 系统通知、托盘 |
| 主题切换 | 深色/浅色模式 |

### Phase 3

| 功能 | 说明 |
|------|------|
| 流程图交互 | 点击节点查看详情、缩放拖拽 |
| Agent 配置可视化 | 角色定义编辑器 |
| 离线支持 | 断网时缓存操作，恢复后同步 |
| 移动端适配 | 响应式布局 |

---

## 八、与其他子项目的关系

| 子项目 | 关系 | 说明 |
|--------|------|------|
| quantalithos-sdk | 依赖 sdk-wasm | 所有数据通过 SDK 获取 |
| quantalithos-gate | HTTP + WebSocket | 通过网关访问后端服务 |
| quantalithos-platform | 间接（通过 SDK） | 数据来源 |
| quantalithos-flow | 间接（通过 SDK） | 门禁卡片、进度数据 |
| quantalithos-runtime | 不直接交互 | Agent 消息通过 flow 路由 |

---

## 九、原型

HTML 原型见 `prototype/index.html`，浏览器打开即可查看。

包含：
- 三栏布局（功能导航 + 会话列表 + 聊天内容）
- Agent 消息 + 内嵌工单卡片 + 进度条
- 门禁审批卡片
- 用户消息
- 任务状态卡片组

# quantalithos-core 概要设计

> 核心共享库 — 语言无关的协议定义 + Python BPMN 引擎 + 共享数据模型

---

## 一、项目定位

### 1.1 核心职责

quantalithos-core 是所有子项目的公共基础，包含：

- **schemas/**：语言无关的协议定义（JSON Schema），Rust 和 Python 项目都从这里生成/对齐数据结构
- **python/**：Python 端共享实现（BPMN 引擎 + 通信协议 + 数据模型），flow 和 runtime 共同依赖
- **rust/**：Rust 端共享数据模型（Phase 2，和 sdk-models 对齐）

```
                    quantalithos-core
                    ┌─────────────────────────────────────┐
                    │                                     │
                    │  schemas/          语言无关定义       │
                    │  (JSON Schema)     所有项目对齐       │
                    │                                     │
                    │  python/           Python 实现        │
                    │  ├── bpmn/         BPMN 引擎         │
                    │  ├── protocol/     通信协议           │
                    │  └── models/       数据模型           │
                    │                                     │
                    │  rust/             Rust 实现(Phase 2) │
                    │  └── models/       数据模型           │
                    │                                     │
                    └──────────┬──────────────────────────┘
                               │
              ┌────────────────┼────────────────┐
              │                │                │
              ▼                ▼                ▼
         flow (Python)    runtime (Python)   sdk (Rust)
         依赖 python/     依赖 python/       对齐 schemas/
```

### 1.2 与 sdk 的区别

```
core = 定义"是什么"（协议、模型、引擎）
sdk  = 实现"怎么用"（API 客户端、WebSocket、状态管理）

core 的消费者：flow、runtime（Python）+ sdk（对齐 schemas）
sdk 的消费者：chat、sync、gate（Rust）

两者不重叠。
```

---

## 二、模块结构

```
quantalithos-core/
│
├── schemas/                         语言无关定义（JSON Schema）
│   ├── flow-message.schema.json     FlowMessage 格式
│   ├── node-result.schema.json      NodeResult 格式
│   ├── process-definition.schema.json  ProcessDefinition 格式
│   ├── role-definition.schema.json  角色定义格式
│   ├── shared-rule.schema.json      共享规则格式
│   └── workitem.schema.json         工单格式
│
├── python/                          Python 实现
│   ├── pyproject.toml               Python 包配置
│   │
│   ├── quantalithos_core/
│   │   ├── __init__.py
│   │   │
│   │   ├── bpmn/                    BPMN 引擎
│   │   │   ├── __init__.py
│   │   │   ├── models.py            FlowNode 继承体系（11 个具体类）+ FlowEdge + deserialize_node
│   │   │   ├── process.py           ProcessDefinition + ProcessInstance
│   │   │   ├── token.py             Token（状态机 + 历史）
│   │   │   ├── engine.py            BPMNEngine（Token 驱动 + 策略注册表）
│   │   │   ├── strategy.py          NodeStrategy 接口 + StrategyRegistry
│   │   │   └── context.py           ExecutionContext 基类
│   │   │
│   │   ├── protocol/                通信协议
│   │   │   ├── __init__.py
│   │   │   ├── flow_message.py      FlowMessage（6 种类型）+ parse_payload
│   │   │   ├── payloads.py          类型化 Payload（NodeExecutePayload 等）
│   │   │   ├── node_result.py       NodeResult
│   │   │   └── events.py            AG-UI 事件类型
│   │   │
│   │   └── models/                  共享数据模型
│   │       ├── __init__.py
│   │       ├── role.py              RoleDefinition/PermissionConfig
│   │       ├── rule.py              SharedRule
│   │       ├── workitem.py          WorkItem/WorkItemType/Priority
│   │       └── artifact.py          Artifact/ArtifactType
│   │
│   └── tests/
│
└── rust/                            Rust 实现（Phase 2）
    └── models/                      和 schemas/ 对齐的 Rust struct
```

---

## 三、BPMN 引擎设计

### 3.1 节点继承体系

```
FlowNode（抽象基类，纯数据 + 领域行为，不知道策略的存在）
├── id: str
├── name: str
├── status: str
├── node_type: str                    # discriminator，用于策略查表 + 反序列化
│
├── EventNode（事件基类）
│   │
│   ├── StartEvent
│   │   ├── trigger: str              # none | message | timer | signal
│   │   └── （无领域行为）
│   │
│   ├── EndEvent
│   │   ├── trigger: str              # none | terminate
│   │   └── is_terminate() → bool     # 领域行为：是否终止所有分支
│   │
│   ├── IntermediateEvent                                          ← Phase 2
│   │   ├── direction: str            # catch | throw
│   │   └── trigger: str              # message | timer | signal
│   │
│   └── BoundaryEvent                                              ← Phase 2
│       ├── attached_to: str          # 附着的 Activity 节点 ID
│       ├── trigger: str              # timer | error
│       ├── interrupting: bool        # 是否中断宿主活动
│       └── timeout: Optional[int]
│
├── ActivityNode（活动基类）
│   ├── actor: Optional[str]
│   ├── action: Optional[str]
│   │
│   ├── AbstractTask
│   │   ├── input_refs: List[str]
│   │   ├── allowed_tools: List[str]
│   │   ├── output_schema: Optional[dict]
│   │   ├── get_tool_set() → List[str]           # 领域行为
│   │   └── validate_output(output) → bool        # 领域行为
│   │
│   ├── UserTask
│   │   ├── gate_config: GateConfig
│   │   ├── get_approvers() → List[str]           # 领域行为
│   │   └── get_timeout() → Optional[int]         # 领域行为
│   │
│   ├── ServiceTask                                                ← Phase 2
│   │   ├── service_ref: str
│   │   └── service_config: dict
│   │
│   ├── CallActivity                                               ← Phase 2
│   │   ├── call_process_id: str
│   │   └── get_sub_process_id() → str            # 领域行为
│   │
│   └── AdHocSubProcess
│       ├── ordering: str = "parallel"                 # BPMN 标准：parallel | sequential
│       ├── completion_condition: str = "coordinator_submit"  # BPMN 标准：完成条件
│       ├── cancel_remaining: bool = True              # BPMN 标准：完成时取消未完成活动
│       ├── participant_ids: List[str] = []             # 通用扩展：参与者
│       ├── coordinator_id: Optional[str] = None        # 通用扩展：协调者
│       ├── inner_activities: List[dict] = []           # 通用扩展：内部可执行活动
│       ├── metadata: dict = {}                         # 场景配置（讨论：agenda/topic，准备：checklist）
│       ├── get_participants() → List[str]              # 领域行为
│       ├── is_coordinator(agent_id) → bool             # 领域行为
│       └── is_complete(context) → bool                 # 领域行为
│
└── GatewayNode（网关基类）
    ├── direction: str                # diverging | converging
    │
    ├── ExclusiveGateway
    │   ├── default_route: Optional[str]
    │   └── evaluate(edges, outputs) → str         # 领域行为：条件评估
    │
    └── ParallelGateway                                            ← Phase 2
        ├── get_fork_targets(edges) → List[str]    # 领域行为
        └── is_join_complete(waiting, expected) → bool  # 领域行为
```

> **设计决策记录：从枚举分类改为继承体系**
>
> 旧设计：FlowNode + NodeCategory 枚举 + 三个子类，靠字符串区分具体类型。
> 新设计：每种 BPMN 对象一个类，每个类只携带自己的字段 + 领域行为。
> 新增 BPMN 元素 = 新增一个类 + 注册一个策略，不改任何已有代码。

> **设计决策记录：从 visitor 改为策略注册表**
>
> 旧设计：FlowNode 有 accept(visitor) 方法，visitor 有 11 个 visit 方法。
> 问题：
> - 对象知道 visitor 的存在，不是纯领域模型
> - 新增节点类型要改所有 visitor（ISP 违反）
> - visitor 的 visit 方法命名和 BPMN 节点类型耦合
>
> 新设计：FlowNode 没有 accept/execute 方法，完全不知道策略的存在。
> BPMNEngine 通过 StrategyRegistry 按 node_type 查表调用策略。
> 对象只有领域行为（evaluate/validate_output/is_terminate 等）。
>
> **废弃方案：标准策略模式（对象持有策略实例）**
>
> 对象创建时注入策略实例（executor=AgentTaskStrategy()）。
> 废弃原因：策略实例不可序列化，从 YAML 加载时不知道注入哪个，挂起/恢复时丢失。

### 3.2 策略接口与注册表

```python
# bpmn/strategy.py

class NodeStrategy(ABC):
    """节点执行策略接口 — flow 和 runtime 各自实现"""

    @abstractmethod
    async def execute(self, node: FlowNode, ctx: "ExecutionContext") -> Optional[dict]:
        """执行节点，返回结果（None 表示异步等待回调）"""
        ...


class StrategyRegistry:
    """策略注册表 — 按 node_type 查找策略"""

    def __init__(self):
        self._strategies: Dict[str, NodeStrategy] = {}

    def register(self, node_type: str, strategy: NodeStrategy):
        self._strategies[node_type] = strategy

    def get(self, node_type: str) -> NodeStrategy:
        strategy = self._strategies.get(node_type)
        if strategy is None:
            raise ValueError(f"No strategy registered for node_type '{node_type}'")
        return strategy

    def has(self, node_type: str) -> bool:
        return node_type in self._strategies
```

```python
# bpmn/context.py

class ExecutionContext(BaseModel):
    """执行上下文基类 — 策略执行时需要的运行时依赖"""
    process_instance_id: str
    message_id: str = ""
    input_refs: List[str] = []
    control_level: str = "free"
    output_schema: Optional[dict] = None

    class Config:
        arbitrary_types_allowed = True


class FlowExecutionContext(ExecutionContext):
    """flow 侧的执行上下文"""
    runtime_client: Any               # HTTP 客户端，发送 FlowMessage
    gate_controller: Any              # 门禁控制器
    platform_client: Any              # platform API 客户端
    project_id: str = ""


class AgentExecutionContext(ExecutionContext):
    """runtime 侧的执行上下文"""
    agent: Any                        # Agent 实例
    tools: List[Any] = []             # 可用工具列表
    executor: Any                     # AgentExecutor 实例
    platform_client: Any              # platform API 客户端
```

### 3.3 Token 模型与引擎核心

```python
# bpmn/token.py

class Token(BaseModel):
    """BPMN Token — 流程中的执行指针"""
    token_id: str
    node_id: str
    parent_token_id: Optional[str] = None
    status: str = "active"            # active | completed | waiting
    history: List[dict] = []

    VALID_TRANSITIONS = {
        "active": {"completed", "waiting"},
        "waiting": {"completed"},
    }

    def transition_to(self, new_status: str):
        """状态转换 — Fail Fast"""
        allowed = self.VALID_TRANSITIONS.get(self.status, set())
        if new_status not in allowed:
            raise ValueError(
                f"Token '{self.token_id}' 非法状态转换: {self.status} → {new_status}"
            )
        self.history.append({
            "from": self.status, "to": new_status,
            "node_id": self.node_id, "timestamp": datetime.now().isoformat(),
        })
        self.status = new_status
```

```python
# bpmn/engine.py

class BPMNEngine:
    """BPMN 流程引擎 — Token 驱动 + 策略注册表"""

    def __init__(self, registry: StrategyRegistry, on_event=None):
        self.registry = registry
        self.on_event = on_event or (lambda e: None)

    async def run(self, process_def: ProcessDefinition, instance: ProcessInstance, ctx: ExecutionContext):
        """Token 驱动的流程执行"""

        # 0. 校验
        errors = process_def.validate_structure()
        if errors:
            raise ValueError(f"ProcessDefinition 校验失败: {errors}")

        # 1. 创建初始 token
        start_node = process_def.get_start_event()
        initial_token = Token(token_id=generate_id(), node_id=start_node.id)
        instance.tokens = [initial_token]
        self.on_event({"type": "engine_started", "process_id": process_def.process_id})

        # 2. 循环：每轮处理所有 active token
        while self.has_active_tokens(instance):
            for token in self.get_active_tokens(instance):
                node = process_def.get_node(token.node_id)
                self.on_event({"type": "node_enter", "node_id": node.id, "token_id": token.token_id})

                # 领域行为：Gateway 自己评估（不走策略）
                # 环境行为：Activity/Event 走策略注册表
                if not isinstance(node, (ExclusiveGateway, ParallelGateway)):
                    if self.registry.has(node.node_type):
                        strategy = self.registry.get(node.node_type)
                        result = await strategy.execute(node, ctx)
                        if result:
                            instance.node_outputs[node.id] = result

                await self.advance_token(token, node, process_def, instance)

        self.on_event({"type": "engine_completed", "process_id": process_def.process_id})

    async def advance_token(self, token, node, process_def, instance):
        """推进 token（幂等：已 completed 跳过）"""

        if token.status == "completed":
            return

        # EndEvent：token 消亡
        if isinstance(node, EndEvent):
            token.transition_to("completed")
            if node.is_terminate():
                # terminate：终止所有 token
                for t in instance.tokens:
                    if t.status == "active":
                        t.transition_to("completed")
            self.on_event({"type": "token_end", "token_id": token.token_id})
            return

        # ParallelGateway fork：token 分裂
        if isinstance(node, ParallelGateway) and node.direction == "diverging":
            token.transition_to("completed")
            outgoing = process_def.get_outgoing_edges(node.id)
            targets = node.get_fork_targets(outgoing)
            for target_id in targets:
                child = Token(token_id=generate_id(), node_id=target_id, parent_token_id=token.token_id)
                instance.tokens.append(child)
            self.on_event({"type": "token_fork", "parent": token.token_id, "children": len(targets)})
            return

        # ParallelGateway join：等待所有分支
        if isinstance(node, ParallelGateway) and node.direction == "converging":
            token.transition_to("waiting")
            waiting_at_join = [t for t in instance.tokens if t.node_id == node.id and t.status == "waiting"]
            expected = len(process_def.get_incoming_edges(node.id))
            if node.is_join_complete(len(waiting_at_join), expected):
                for t in waiting_at_join:
                    t.transition_to("completed")
                outgoing = process_def.get_outgoing_edges(node.id)
                if outgoing:
                    merged = Token(token_id=generate_id(), node_id=outgoing[0].target)
                    instance.tokens.append(merged)
                self.on_event({"type": "token_join", "node_id": node.id})
            return

        # ExclusiveGateway：对象自己评估条件
        if isinstance(node, ExclusiveGateway):
            outgoing = process_def.get_outgoing_edges(node.id)
            target = node.evaluate(outgoing, instance.node_outputs)
            token.node_id = target
            return

        # 普通节点：沿 sequence flow 推进
        outgoing = process_def.get_outgoing_edges(node.id)
        if outgoing:
            token.node_id = outgoing[0].target
        else:
            raise RuntimeError(f"节点 '{node.id}' 没有出口 edge")

    def has_active_tokens(self, instance) -> bool:
        return any(t.status in ("active", "waiting") for t in instance.tokens)

    def get_active_tokens(self, instance) -> List[Token]:
        return [t for t in instance.tokens if t.status == "active"]
```

```
BPMNEngine 驱动 Token + 对象自治 + 策略注册表：

  BPMNEngine.run(process_def, instance, ctx)
      │
      │  Token T1 → StartEvent
      │  registry.has("start_event") → 无策略 → 跳过
      │  advance_token → 推进到下一个节点
      │
      │  Token T1 → AbstractTask("implement")
      │  registry.get("abstract_task") → FlowTaskStrategy / AgentTaskStrategy
      │  strategy.execute(node, ctx) → 环境行为
      │  node.validate_output(result) → 领域行为
      │  advance_token → 推进
      │
      │  Token T1 → ExclusiveGateway("check_result")
      │  不走策略 → node.evaluate(edges, outputs) → 对象自治
      │  advance_token → 按评估结果推进
      │
      │  Token T1 → EndEvent
      │  node.is_terminate() → 领域行为
      │  advance_token → token completed
```

### 3.4 flow 和 runtime 的策略注册

```python
# ── flow 侧策略 ──

class FlowTaskStrategy(NodeStrategy):
    """flow: AbstractTask → 构造 node_execute 消息发给 runtime"""
    async def execute(self, node: AbstractTask, ctx: FlowExecutionContext) -> Optional[dict]:
        # 从 workitem.metadata 读取 workflow_config
        workflow_config = ctx.get_workflow_config(node.id)
        control_level = workflow_config.get("control_level", "free") if workflow_config else "free"

        # 如果是 enforced，加载完整 ProcessDefinition 放入 payload
        process_definition = None
        if control_level == "enforced" and workflow_config:
            process_id = workflow_config.get("process_id")
            if process_id:
                pd = await ctx.platform_client.agent_config().get_process(process_id)
                process_definition = pd.definition  # 完整的 nodes + edges

        msg = FlowMessage(
            message_id=generate_id(),
            type="node_execute",
            agent_id=ctx.resolve_actor(node.actor),
            project_id=ctx.project_id,
            payload=NodeExecutePayload(
                process_instance_id=ctx.process_instance_id,
                node=node.dict(),
                control_level=control_level,
                process_definition=process_definition,
                input_refs=node.input_refs,
                allowed_tools=node.allowed_tools,
                output_schema=node.output_schema,
            ).dict(),
            created_at=datetime.now(),
        )
        await ctx.runtime_client.send(msg)
        return None

class FlowUserTaskStrategy(NodeStrategy):
    """flow: UserTask → 创建门禁卡片，等待用户操作"""
    async def execute(self, node: UserTask, ctx: FlowExecutionContext) -> Optional[dict]:
        await ctx.gate_controller.create_gate(node)
        return None  # 等待用户操作

class FlowAdHocStrategy(NodeStrategy):
    """flow: AdHocSubProcess → 构造 node_execute 消息"""
    async def execute(self, node: AdHocSubProcess, ctx: FlowExecutionContext) -> Optional[dict]:
        msg = FlowMessage(
            message_id=generate_id(),
            type="node_execute",
            agent_id=node.coordinator_id or node.participant_ids[0],
            project_id=ctx.project_id,
            payload=NodeExecutePayload(
                process_instance_id=ctx.process_instance_id,
                node=node.dict(),
                control_level="free",
            ).dict(),
            created_at=datetime.now(),
        )
        await ctx.runtime_client.send(msg)
        return None

class FlowEndEventStrategy(NodeStrategy):
    """flow: EndEvent → 流程完成，推进阶段"""
    async def execute(self, node: EndEvent, ctx: FlowExecutionContext) -> Optional[dict]:
        await ctx.on_process_completed()
        return None

# flow 启动时注册
def create_flow_registry() -> StrategyRegistry:
    registry = StrategyRegistry()
    registry.register("abstract_task", FlowTaskStrategy())
    registry.register("user_task", FlowUserTaskStrategy())
    registry.register("ad_hoc_sub_process", FlowAdHocStrategy())
    registry.register("end_event", FlowEndEventStrategy())
    # start_event / exclusive_gateway / parallel_gateway → 不注册，引擎自动处理
    return registry
```

```python
# ── runtime 侧策略 ──

class AgentTaskStrategy(NodeStrategy):
    """runtime: AbstractTask → 注入 prompt，Agent think→act"""
    async def execute(self, node: AbstractTask, ctx: AgentExecutionContext) -> Optional[dict]:
        step_prompt = f"【当前步骤：{node.name}】\n{node.action or ''}"
        ctx.agent.conversation.append(Message(role="system", content=step_prompt))
        step_tools = ctx.tools + [create_step_complete_tool(node.output_schema)]
        result = await ctx.executor.run(ctx.agent, step_tools)
        return result

class AgentAdHocStrategy(NodeStrategy):
    """runtime: AdHocSubProcess → 启动协作活动"""
    async def execute(self, node: AdHocSubProcess, ctx: AgentExecutionContext) -> Optional[dict]:
        participants = node.get_participants()
        agenda = node.metadata.get("agenda", node.name)
        for agent in ctx.get_agents(participants):
            agent.state_stack.append(StackEntry(
                state=StackState.EXECUTING,
                context={
                    "node_type": "ad_hoc_sub_process",
                    "node_id": node.id,
                    "coordinator_id": node.coordinator_id,
                    "participant_ids": node.participant_ids,
                }
            ))
            agent.conversation.append(Message(
                role="system",
                content=f"【协作活动开始】\n议程：{agenda}\n协调者：{node.coordinator_id}"
            ))
        # 等待 coordinator 提交 step_complete
        return None

# runtime 启动时注册
def create_agent_registry() -> StrategyRegistry:
    registry = StrategyRegistry()
    registry.register("abstract_task", AgentTaskStrategy())
    registry.register("ad_hoc_sub_process", AgentAdHocStrategy())
    # start_event / end_event / exclusive_gateway → 不注册，引擎自动处理
    return registry
```

```
flow 和 runtime 的策略对比：

  node_type              flow 策略                  runtime 策略
  ─────────              ─────────                  ────────────
  start_event            不注册（引擎跳过）          不注册（引擎跳过）
  end_event              FlowEndEventStrategy       不注册（引擎处理 token）
  abstract_task          FlowTaskStrategy           AgentTaskStrategy
  user_task              FlowUserTaskStrategy       不注册（不经过 runtime）
  ad_hoc_sub_process     FlowAdHocStrategy          AgentAdHocStrategy
  call_activity          Phase 2                    Phase 2
  service_task           Phase 2                    Phase 2
  exclusive_gateway      不注册（对象自治）          不注册（对象自治）
  parallel_gateway       不注册（引擎处理 token）    不注册（引擎处理 token）

  新增节点类型的步骤：
  1. core: 新增 FlowNode 子类（纯数据 + 领域行为）
  2. flow: 新增 FlowXxxStrategy + registry.register()
  3. runtime: 新增 AgentXxxStrategy + registry.register()
  4. 不改 BPMNEngine、不改已有策略、不改已有节点类
```


## 四、通信协议

### 4.1 消息类型设计

> **设计决策记录：消息类型和 BPMN 对齐**
>
> 旧设计：10 种消息类型（task_assign / session_start / session_end / ...），
> 每种 BPMN 节点类型对应一种自造的消息类型，flow 需要理解 runtime 内部行为。
>
> 问题：BPMN 和消息类型是两套语言，每新增一种节点类型需要同时新增消息类型。
> session_start/session_end 是 BPMN 中不存在的概念，引发歧义。
>
> 新设计：6 种消息类型，节点执行统一为 node_execute，payload 里带序列化的 FlowNode。
> runtime 收到后反序列化为具体类型，BPMNEngine 按 node_type 查表调用策略，和 BPMN 完全对齐。

```
消息类型（6 种）：

  生命周期（runtime 自有，和 BPMN 无关）：
    agent_create   — 创建 Agent 实例
    agent_destroy  — 销毁 Agent 实例
    agent_suspend  — 挂起 Agent（保存快照）
    agent_resume   — 恢复 Agent（从快照）

  节点执行（和 BPMN 对齐）：
    node_execute   — 执行一个 BPMN 节点（payload 带序列化的 FlowNode）
    node_cancel    — 取消正在执行的节点

  消息路由（聊天功能）：
    chat_message   — 聊天消息（群聊/私聊/ask_reply 统一）
```

### 4.2 FlowMessage 与类型化 Payload

```python
# protocol/payloads.py

class NodeExecutePayload(BaseModel):
    """节点执行 — 和 BPMN 对齐"""
    process_instance_id: str
    node: dict                        # 序列化的 FlowNode（反序列化用 deserialize_node）
    control_level: str = "free"       # free | guided | enforced
    process_definition: Optional[dict] = None  # enforced 时必填：完整的 ProcessDefinition（不是 ID）
    input_refs: List[str] = []
    allowed_tools: List[str] = []
    output_schema: Optional[dict] = None
    suspicion_hints: List[str] = []

class NodeCancelPayload(BaseModel):
    """取消节点执行"""
    process_instance_id: str
    node_id: str
    reason: str = ""

class ChatMessagePayload(BaseModel):
    """聊天消息 — 群聊/私聊/ask_reply 统一"""
    sender_id: str
    content: str
    mentions: List[str] = []
    reply_to: Optional[dict] = None   # ask_reply 时填入 {ask_id, responder_id}

class AgentCreatePayload(BaseModel):
    role_id: str
    project_id: str

class AgentDestroyPayload(BaseModel):
    reason: str = ""

PAYLOAD_TYPE_MAP = {
    "agent_create": AgentCreatePayload,
    "agent_destroy": AgentDestroyPayload,
    "agent_suspend": None,
    "agent_resume": None,
    "node_execute": NodeExecutePayload,
    "node_cancel": NodeCancelPayload,
    "chat_message": ChatMessagePayload,
}
```

```python
# protocol/flow_message.py

class FlowMessage(BaseModel):
    """flow → runtime 统一消息"""
    message_id: str
    type: str
    agent_id: str
    project_id: str
    payload: dict                     # 传输层保持 dict（JSON 兼容）
    created_at: datetime

    def parse_payload(self) -> Optional[BaseModel]:
        """根据 type 解析 payload 为具体类型，Fail Fast"""
        cls = PAYLOAD_TYPE_MAP.get(self.type)
        if cls is None and self.type not in PAYLOAD_TYPE_MAP:
            raise ValueError(f"Unknown message type: {self.type}")
        if cls is None:
            return None
        return cls(**self.payload)     # Pydantic 校验，字段缺失/类型错误立即报错
```

```
node_execute 的处理流程：

  flow                              runtime
   │                                  │
   │  BPMN: AbstractTask              │
   │  → FlowMessage(                  │
   │      type="node_execute",        │
   │      payload={                   │
   │        node: {                   │
   │          node_type: "abstract_task",
   │          id: "implement",        │
   │          name: "编码实现",        │
   │          action: "实现标签系统",   │
   │          actor: "backend-dev-1", │
   │          allowed_tools: [...],   │
   │        },                        │
   │        control_level: "enforced",│
   │        process_id: "tdd_workflow"│
   │      })                          │
   │ ──────────────────────────────>  │
   │                                  │  parse_payload() → NodeExecutePayload
   │                                  │  deserialize_node(payload.node) → AbstractTask
   │                                  │  registry.get("abstract_task") → AgentTaskStrategy
   │                                  │  strategy.execute(node, ctx)
   │                                  │
   │  BPMN: AdHocSubProcess           │
   │  → FlowMessage(                  │
   │      type="node_execute",        │  ← 同一个消息类型！
   │      payload={                   │
   │        node: {                   │
   │          node_type: "ad_hoc_sub_process",
   │          id: "brainstorm",       │
   │          moderator_id: "tl",     │
   │          participant_ids: [...], │
   │          agenda: "技术方案讨论",   │
   │        },                        │
   │        control_level: "free",    │
   │      })                          │
   │ ──────────────────────────────>  │
   │                                  │  deserialize_node() → AdHocSubProcess
   │                                  │  registry.get("ad_hoc_sub_process") → AgentAdHocStrategy
   │                                  │  strategy.execute(node, ctx)
```

### 4.2 NodeResult

```python
# protocol/node_result.py

class NodeResult(BaseModel):
    """runtime → flow 执行结果"""
    message_id: str
    agent_id: str
    result_type: str               # completed | blocked | failed
    output: Optional[dict] = None
    reason: Optional[str] = None
    reasoning_trace: Optional[str] = None
    duration_ms: int = 0
    tool_call_count: int = 0
    total_tokens: int = 0
    created_at: datetime
```

---

## 五、Phase 分期

### Phase 1

| 模块 | 范围 |
|------|------|
| schemas/ | FlowMessage + NodeResult + ProcessDefinition + RoleDefinition + SharedRule |
| bpmn/models | StartEvent + EndEvent + AbstractTask + UserTask + AdHocSubProcess + ExclusiveGateway + FlowEdge |
| bpmn/token | Token 模型 |
| bpmn/engine | Token 驱动引擎（线性遍历 + exclusive 条件分支，退化为单 token） |
| bpmn/strategy | NodeStrategy 接口 + StrategyRegistry |
| bpmn/context | ExecutionContext / FlowExecutionContext / AgentExecutionContext |
| sandbox/ | SandboxProvider 接口（runtime 工具通过此接口调用 sandbox） |
| protocol/ | FlowMessage + NodeResult |
| models/ | RoleDefinition + SharedRule + WorkItem |

### Phase 2

| 模块 | 范围 |
|------|------|
| bpmn/models | IntermediateEvent + BoundaryEvent + ServiceTask + CallActivity + ParallelGateway |
| bpmn/engine | ParallelGateway Token 分裂/合并 + CallActivity 子流程 + BoundaryEvent 处理 |
| rust/models | 和 schemas/ 对齐的 Rust struct |

### 不实现的 BPMN 元素

> 完整的落地决策表见 `docs/design/流程图模型与节点原语设计.md` 第十章。

以下 BPMN 元素经评估后决定不实现：

| 类别 | 不实现的元素 | 原因 |
|------|------------|------|
| 网关 | Inclusive(OR) / Event-based / Complex | 无场景，Exclusive + Parallel 已覆盖需求 |
| 活动 | Script / Send / Receive / Business Rule / Manual | Agent 本身能执行脚本，消息用 FlowMessage，规则用 shared_rules |
| 事件 | Compensation / Escalation / Cancel / Signal / Message(intermediate) | 无事务回滚需求，升级用 NodeResult(blocked)，取消用 EndEvent(terminate) |
| 连接 | Message Flow | 单系统内部，不跨组织 |
| 泳道 | Pool | 同上，只有一个参与者 |
| 数据 | Data Object / Data Store | 用 Artifact + platform 数据库替代 |
| 其他 | Transaction SubProcess / Multi-Instance | 无事务性需求，并行用 ParallelGateway + 多节点替代 |

---

## 六、与其他子项目的关系

| 子项目 | 依赖 core 的什么 | 怎么依赖 |
|--------|-----------------|---------|
| flow | python/bpmn + python/protocol + python/models | pip install quantalithos-core |
| runtime | python/bpmn + python/protocol + python/models + sandbox/ | pip install quantalithos-core |
| sandbox | sandbox/（实现 SandboxProvider 接口） | 开发时参考接口定义 |
| sdk | schemas/（对齐数据结构） | 开发时参考，不是运行时依赖 |
| platform | schemas/（对齐 API 格式） | 开发时参考 |
| chat | 不依赖 | 通过 sdk 间接使用 |
| console | 不依赖 | 通过 sdk 间接使用 |
| runner | 不依赖 | 通过 sdk 间接使用 |
| gate | 不依赖 | |
| sync | 不依赖 | |
| infra | 不依赖 | |

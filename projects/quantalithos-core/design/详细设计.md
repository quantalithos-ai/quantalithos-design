# quantalithos-core 详细设计

> 核心共享库 — BPMN 引擎、通信协议、共享数据模型的完整规格

---

## 一、模块架构

```
外部消费者

flow (Python)          runtime (Python)         sdk (Rust)
pip install core       pip install core          对齐 schemas/
     │                      │                        │
     ▼                      ▼                        ▼
┌─────────────────────────────────────────────────────────────┐
│  quantalithos-core                                          │
│                                                             │
│  python/quantalithos_core/                                  │
│  ├── bpmn/          BPMN 引擎（策略注册表 + Token 驱动）    │
│  │   ├── models.py  FlowNode 继承体系（11 个具体类）        │
│  │   ├── process.py ProcessDefinition + ProcessInstance     │
│  │   ├── token.py   Token（状态机 + 历史）                  │
│  │   ├── engine.py  BPMNEngine（Token 驱动）                │
│  │   ├── strategy.py NodeStrategy + StrategyRegistry        │
│  │   └── context.py ExecutionContext 基类                   │
│  │                                                          │
│  ├── protocol/      通信协议                                 │
│  │   ├── flow_message.py  FlowMessage（6 种类型）           │
│  │   ├── payloads.py      类型化 Payload                    │
│  │   └── node_result.py   NodeResult（3 种结果）             │
│  │                                                          │
│  └── models/        共享数据模型                             │
│      ├── role.py    RoleDefinition / PermissionConfig       │
│      ├── rule.py    SharedRule                              │
│      ├── workitem.py WorkItem / WorkItemType / Priority     │
│      └── artifact.py Artifact / ArtifactType                │
│                                                             │
│  schemas/           语言无关定义（JSON Schema）              │
│  ├── flow-message.schema.json                               │
│  ├── node-result.schema.json                                │
│  ├── process-definition.schema.json                         │
│  ├── role-definition.schema.json                            │
│  ├── shared-rule.schema.json                                │
│  └── workitem.schema.json                                   │
└─────────────────────────────────────────────────────────────┘
```

### 1.1 模块职责

| 文件 | 职责 | 主要消费者 |
|------|------|-----------|
| bpmn/models.py | FlowNode 继承体系（11 个具体类）+ FlowEdge + deserialize_node | flow, runtime |
| bpmn/token.py | Token 模型（状态机 + 历史） | core 内部（BPMNEngine） |
| bpmn/process.py | ProcessDefinition（from_dict + 校验）+ ProcessInstance | flow, runtime |
| bpmn/engine.py | BPMNEngine（Token 驱动 + 策略注册表） | flow, runtime |
| bpmn/strategy.py | NodeStrategy 接口 + StrategyRegistry | flow, runtime |
| bpmn/context.py | ExecutionContext 基类 + FlowExecutionContext + AgentExecutionContext | flow, runtime |
| protocol/flow_message.py | FlowMessage（6 种消息类型）+ parse_payload | flow（构造）, runtime（接收） |
| protocol/payloads.py | 类型化 Payload（NodeExecutePayload 等） | flow, runtime |
| protocol/node_result.py | NodeResult 执行结果 | runtime（构造）, flow（接收） |
| models/role.py | RoleDefinition 角色定义 | runtime |
| models/rule.py | SharedRule 共享规则 | runtime |
| models/workitem.py | WorkItem 工单模型 | flow, runtime, platform |
| models/artifact.py | Artifact 产物模型 | flow, runtime, platform |

### 1.2 设计原则

- **零业务逻辑**：core 不知道 TDD、不知道博客项目、不知道 Agent 池。它只定义数据结构和引擎接口
- **语言无关**：schemas/ 是 Python 和 Rust 的共同基准，Python Pydantic 模型是权威实现
- **向后兼容**：ProcessDefinition 带 version 字段，消费者可按版本加载
- **策略注册表**：BPMNEngine 不包含任何节点处理逻辑，按 node_type 查表调用策略

---



---

## 二、数据结构 — bpmn 模块

### 2.1 FlowNode 基类

```python
# bpmn/models.py

class FlowNode(BaseModel):
    """BPMN 节点抽象基类 — 纯数据 + 领域行为，不知道策略的存在"""
    id: str
    name: str = ""
    status: str = "pending"
    node_type: str                    # discriminator，用于策略查表 + 反序列化

    class Config:
        extra = "allow"               # 允许子类扩展字段
```

### 2.2 事件节点

```python
class StartEvent(FlowNode):
    """开始事件 — 流程入口"""
    node_type: str = "start_event"
    trigger: str = "none"             # none | message | timer | signal

class EndEvent(FlowNode):
    """结束事件 — 流程出口"""
    node_type: str = "end_event"
    trigger: str = "none"             # none | terminate

    # 领域行为
    def is_terminate(self) -> bool:
        """是否终止所有分支（terminate 类型终止整个流程）"""
        return self.trigger == "terminate"

class IntermediateEvent(FlowNode):    # Phase 2
    """中间事件 — 流程中间的等待/触发"""
    node_type: str = "intermediate_event"
    direction: str = "catch"          # catch | throw
    trigger: str = "timer"            # message | timer | signal

class BoundaryEvent(FlowNode):        # Phase 2
    """边界事件 — 附着在活动节点上"""
    node_type: str = "boundary_event"
    attached_to: str                  # 附着的 Activity 节点 ID
    trigger: str = "timer"            # timer | error
    interrupting: bool = True         # 是否中断宿主活动
    timeout: Optional[int] = None     # 超时秒数（timer 时使用）
```

### 2.3 活动节点

```python
class AbstractTask(FlowNode):
    """抽象任务 — Agent 执行的基础任务"""
    node_type: str = "abstract_task"
    actor: Optional[str] = None       # 执行者角色 ID
    action: Optional[str] = None      # 自然语言描述的任务内容
    input_refs: List[str] = []        # 输入引用（工单 ID / 产物 ID）
    allowed_tools: List[str] = []     # 允许使用的工具列表
    output_schema: Optional[dict] = None  # 输出 schema（step_complete 校验用）

    # 领域行为
    def get_tool_set(self) -> List[str]:
        """返回允许的工具集"""
        return self.allowed_tools

    def validate_output(self, output: dict) -> bool:
        """校验输出是否符合 schema"""
        if self.output_schema is None:
            return True
        # Phase 1：简单字段存在性检查
        required = self.output_schema.get("required", [])
        return all(k in output for k in required)

class UserTask(FlowNode):
    """用户任务 — 门禁审批，等待人类操作"""
    node_type: str = "user_task"
    actor: Optional[str] = None
    action: Optional[str] = None
    gate_config: dict = {}            # gate_type, approvers, timeout, auto_approve_condition

    # 领域行为
    def get_approvers(self) -> List[str]:
        return self.gate_config.get("approvers", [])

    def get_timeout(self) -> Optional[int]:
        return self.gate_config.get("timeout")

    def is_hard_gate(self) -> bool:
        return self.gate_config.get("gate_type") == "hard"

class ServiceTask(FlowNode):          # Phase 2
    """服务任务 — 系统自动执行"""
    node_type: str = "service_task"
    service_ref: str = ""             # 外部服务标识
    service_config: dict = {}         # 服务配置

class CallActivity(FlowNode):         # Phase 2
    """调用活动 — 启动子流程"""
    node_type: str = "call_activity"
    actor: Optional[str] = None
    call_process_id: str = ""         # 子流程 ProcessDefinition ID

    # 领域行为
    def get_sub_process_id(self) -> str:
        return self.call_process_id

class AdHocSubProcess(FlowNode):
    """Ad-Hoc 子流程 — 通用的无序多人活动"""
    node_type: str = "ad_hoc_sub_process"

    # BPMN 标准字段
    ordering: str = "parallel"                    # parallel | sequential
    completion_condition: str = "coordinator_submit"  # 完成条件
    cancel_remaining: bool = True                 # 完成时取消未完成活动

    # 通用扩展字段
    participant_ids: List[str] = []               # 参与者列表
    coordinator_id: Optional[str] = None          # 协调者
    inner_activities: List[dict] = []             # 内部可执行活动

    # 场景配置（不污染类定义）
    metadata: dict = {}                           # 讨论：{agenda, topic}，准备：{checklist}

    # 领域行为
    def get_participants(self) -> List[str]:
        return self.participant_ids

    def is_coordinator(self, agent_id: str) -> bool:
        return agent_id == self.coordinator_id

    def is_complete(self, context: dict) -> bool:
        """评估完成条件"""
        match self.completion_condition:
            case "coordinator_submit":
                return context.get("coordinator_submitted", False)
            case "all_done":
                completed = context.get("completed_activities", [])
                return len(completed) >= len(self.inner_activities)
            case _:
                return False

    def get_metadata(self, key: str, default=None):
        return self.metadata.get(key, default)
```

### 2.4 网关节点

```python
class ExclusiveGateway(FlowNode):
    """排他网关 — 条件分支，只走一条"""
    node_type: str = "exclusive_gateway"
    direction: str = "diverging"      # diverging | converging
    default_route: Optional[str] = None  # 默认出口节点 ID

    # 领域行为：对象自治，不依赖外部
    def evaluate(self, outgoing_edges: List["FlowEdge"], node_outputs: dict) -> str:
        """评估条件，返回目标 node_id — Fail Fast"""
        for edge in outgoing_edges:
            if edge.flow_type == "conditional" and edge.condition:
                if self._match_condition(edge.condition, node_outputs):
                    return edge.target

        if self.default_route:
            return self.default_route

        raise RuntimeError(
            f"ExclusiveGateway '{self.id}' 所有条件都不满足且没有 default_route"
        )

    def _match_condition(self, condition: str, outputs: dict) -> bool:
        """Phase 1：简单字段匹配（field == value / field != value）"""
        if "==" in condition:
            field, value = [s.strip().strip("'\"") for s in condition.split("==")]
            return str(outputs.get(field)) == value
        if "!=" in condition:
            field, value = [s.strip().strip("'\"") for s in condition.split("!=")]
            return str(outputs.get(field)) != value
        return False

class ParallelGateway(FlowNode):      # Phase 2
    """并行网关 — 分支时 token 分裂，合并时等待所有分支"""
    node_type: str = "parallel_gateway"
    direction: str = "diverging"      # diverging | converging

    # 领域行为
    def get_fork_targets(self, outgoing_edges: List["FlowEdge"]) -> List[str]:
        """fork 时返回所有出口目标"""
        return [e.target for e in outgoing_edges]

    def is_join_complete(self, waiting_count: int, expected_count: int) -> bool:
        """join 时判断是否所有分支都到达"""
        return waiting_count >= expected_count
```

### 2.5 连接

```python
class FlowEdge(BaseModel):
    """BPMN 连接 — 节点间的边"""
    id: str
    source: str                       # 源节点 ID
    target: str                       # 目标节点 ID
    flow_type: str = "sequence"       # sequence | conditional | default
    condition: Optional[str] = None   # 条件表达式（conditional 时使用）
    label: Optional[str] = None       # 显示标签
```

### 2.6 反序列化

```python
# bpmn/models.py

NODE_TYPE_MAP = {
    "start_event": StartEvent,
    "end_event": EndEvent,
    "intermediate_event": IntermediateEvent,
    "boundary_event": BoundaryEvent,
    "abstract_task": AbstractTask,
    "user_task": UserTask,
    "service_task": ServiceTask,
    "call_activity": CallActivity,
    "ad_hoc_sub_process": AdHocSubProcess,
    "exclusive_gateway": ExclusiveGateway,
    "parallel_gateway": ParallelGateway,
}

def deserialize_node(data: dict) -> FlowNode:
    """根据 node_type 反序列化为具体类型 — Fail Fast"""
    node_type = data.get("node_type")
    cls = NODE_TYPE_MAP.get(node_type)
    if cls is None:
        raise ValueError(f"Unknown node_type: {node_type}")
    return cls(**data)
```


### 2.7 Token 模型

```python
# bpmn/token.py

class Token(BaseModel):
    """BPMN Token — 流程中的执行指针
    
    Token 是 BPMNEngine 的核心概念，代表一个独立的执行路径。
    线性流程只有 1 个 Token，ParallelGateway 会分裂出多个。
    Token 到达 EndEvent 时消亡（status=completed）。
    
    为什么需要 Token 而不是单指针：
      单指针（current_node_id）无法表达并行执行。
      ParallelGateway fork 时需要同时跟踪多条路径，
      每条路径是一个独立的 Token。
    """
    token_id: str                      # 唯一标识，用于日志追踪和崩溃恢复时匹配
    node_id: str                       # 当前所在节点 ID，引擎每轮循环读取此字段决定处理哪个节点
    parent_token_id: Optional[str] = None  # fork 时记录父 Token ID，join 时用于匹配同一组分支
    status: str = "active"             # active（可执行）| waiting（等待 join）| completed（已消亡）
    history: List[dict] = []           # 状态变更历史，用于调试和审计（谁在什么时候从哪个节点改了状态）

    # 状态机约束 — 为什么只有 3 种状态：
    # BPMN 标准中 Token 只有这三种生命周期状态。
    # active = 正在被引擎处理
    # waiting = 到达 join 节点，等待其他分支的 Token 也到达
    # completed = 生命周期结束（到达 EndEvent / fork 后消亡 / join 合并后消亡）
    VALID_TRANSITIONS = {
        "active": {"completed", "waiting"},
        "waiting": {"completed"},
        # completed 是终态，不可再转换
    }

    def transition_to(self, new_status: str):
        """状态转换 — Fail Fast，非法转换立即报错
        
        为什么用方法而不是直接赋值 self.status = new_status：
          1. 强制状态机约束（completed → active 是非法的）
          2. 自动记录 history（可观测性）
          3. 集中一个入口，方便加日志和事件回调
        """
        allowed = self.VALID_TRANSITIONS.get(self.status, set())
        if new_status not in allowed:
            raise ValueError(
                f"Token '{self.token_id}' 非法状态转换: {self.status} → {new_status}，"
                f"允许: {self.status} → {allowed or '(终态)'}"
            )
        self.history.append({
            "from": self.status,
            "to": new_status,
            "node_id": self.node_id,
            "timestamp": datetime.now().isoformat(),
        })
        self.status = new_status
```

### 2.8 ProcessDefinition

```python
# bpmn/process.py

class ProcessDefinition(BaseModel):
    """BPMN 流程定义 — YAML/JSON → 节点 + 边"""
    process_id: str
    name: str = ""
    description: str = ""
    version: str = "1.0"
    nodes: List[FlowNode]
    edges: List[FlowEdge]

    @classmethod
    def from_dict(cls, data: dict) -> "ProcessDefinition":
        """工厂方法：从 dict（YAML/JSON 解析后）构造并校验"""
        nodes = [deserialize_node(n) for n in data.get("nodes", [])]
        edges = [FlowEdge(**e) for e in data.get("edges", [])]
        instance = cls(
            process_id=data["process_id"],
            name=data.get("name", ""),
            description=data.get("description", ""),
            version=data.get("version", "1.0"),
            nodes=nodes,
            edges=edges,
        )
        errors = instance.validate_structure()
        if errors:
            raise ValueError(f"ProcessDefinition '{data['process_id']}' 校验失败: {errors}")
        return instance

    def validate_structure(self) -> List[str]:
        """校验流程定义的结构完整性 — 契约设计前置条件"""
        errors = []
        node_ids = {n.id for n in self.nodes}

        # 1. 有且只有一个 StartEvent
        starts = [n for n in self.nodes if isinstance(n, StartEvent)]
        if len(starts) != 1:
            errors.append(f"必须有且只有一个 StartEvent，发现 {len(starts)} 个")

        # 2. 至少一个 EndEvent
        ends = [n for n in self.nodes if isinstance(n, EndEvent)]
        if len(ends) == 0:
            errors.append("必须至少有一个 EndEvent")

        # 3. 所有 edge 的 source/target 必须存在
        for edge in self.edges:
            if edge.source not in node_ids:
                errors.append(f"Edge '{edge.id}' source '{edge.source}' 不存在")
            if edge.target not in node_ids:
                errors.append(f"Edge '{edge.id}' target '{edge.target}' 不存在")

        # 4. StartEvent 不能有入口 edge
        if starts:
            for edge in self.edges:
                if edge.target == starts[0].id:
                    errors.append("StartEvent 不能有入口 edge")

        # 5. EndEvent 不能有出口 edge
        end_ids = {n.id for n in ends}
        for edge in self.edges:
            if edge.source in end_ids:
                errors.append(f"EndEvent '{edge.source}' 不能有出口 edge")

        # 6. ExclusiveGateway(diverging) 至少有一个出口
        for node in self.nodes:
            if isinstance(node, ExclusiveGateway) and node.direction == "diverging":
                if not self.get_outgoing_edges(node.id):
                    errors.append(f"ExclusiveGateway '{node.id}' 没有出口 edge")

        # 7. ParallelGateway(diverging) 至少有 2 个出口
        for node in self.nodes:
            if isinstance(node, ParallelGateway) and node.direction == "diverging":
                if len(self.get_outgoing_edges(node.id)) < 2:
                    errors.append(f"ParallelGateway '{node.id}' fork 至少需要 2 个出口")

        # 8. 非 EndEvent 节点至少有一个出口
        for node in self.nodes:
            if not isinstance(node, EndEvent) and not self.get_outgoing_edges(node.id):
                errors.append(f"节点 '{node.id}' 没有出口 edge")

        return errors

    def get_node(self, node_id: str) -> FlowNode:
        """按 ID 获取节点 — Fail Fast"""
        for n in self.nodes:
            if n.id == node_id:
                return n
        raise ValueError(f"Node '{node_id}' not found in '{self.process_id}'")

    def get_start_event(self) -> StartEvent:
        """获取唯一的 StartEvent"""
        for n in self.nodes:
            if isinstance(n, StartEvent):
                return n
        raise ValueError(f"ProcessDefinition '{self.process_id}' 没有 StartEvent")

    def get_outgoing_edges(self, node_id: str) -> List[FlowEdge]:
        return [e for e in self.edges if e.source == node_id]

    def get_incoming_edges(self, node_id: str) -> List[FlowEdge]:
        return [e for e in self.edges if e.target == node_id]
```

### 2.9 ProcessInstance

```python
# bpmn/process.py

class ProcessInstance(BaseModel):
    """流程实例 — 运行时状态（core 基础版）"""
    instance_id: str
    process_id: str
    project_id: str
    scope: str = "stage"              # pipeline | stage | agent
    tokens: List[Token] = []          # Token 列表（执行指针）
    node_outputs: Dict[str, dict] = {}  # node_id → 执行结果
    status: str = "running"           # running | completed | failed | cancelled
    created_at: datetime = None

    def model_post_init(self, __context):
        if self.created_at is None:
            self.created_at = datetime.now()
```

```python
# flow 扩展版（在 flow 项目中定义，继承 core 基础版）

class FlowProcessInstance(ProcessInstance):
    """flow 的扩展 — 增加追踪字段"""
    current_stage_id: Optional[str] = None
    edge_history: List[dict] = []     # 边遍历历史
    gate_history: List[dict] = []     # 门禁审批历史
```



---

## 三、数据结构 — protocol 模块

### 3.1 消息类型（6 种）

```python
# protocol/flow_message.py

class FlowMessageType(str, Enum):
    """消息类型 — 和 BPMN 对齐"""
    # 生命周期（runtime 自有）
    AGENT_CREATE = "agent_create"
    AGENT_DESTROY = "agent_destroy"
    AGENT_SUSPEND = "agent_suspend"
    AGENT_RESUME = "agent_resume"
    # 节点执行（和 BPMN 对齐）
    NODE_EXECUTE = "node_execute"
    NODE_CANCEL = "node_cancel"
    # 消息路由
    CHAT_MESSAGE = "chat_message"
```

### 3.2 类型化 Payload

```python
# protocol/payloads.py

class NodeExecutePayload(BaseModel):
    """节点执行 — payload 带序列化的 FlowNode + 完整 ProcessDefinition"""
    process_instance_id: str
    node: dict                        # 序列化的 FlowNode（反序列化用 deserialize_node）
    control_level: str = "free"       # free | guided | enforced
    process_definition: Optional[dict] = None  # enforced 时必填：完整的 ProcessDefinition（由 flow 加载后传入）
    input_refs: List[str] = []
    allowed_tools: List[str] = []
    output_schema: Optional[dict] = None
    suspicion_hints: List[str] = []

class NodeCancelPayload(BaseModel):
    """取消节点执行"""
    process_instance_id: str
    node_id: str
    reason: str = ""

class ChatMessagePayload(BaseModel):
    """聊天消息 — 群聊/私聊/ask_reply 统一"""
    sender_id: str
    content: str
    mentions: List[str] = []
    reply_to: Optional[dict] = None   # ask_reply 时填入 {ask_id, responder_id}

class AgentCreatePayload(BaseModel):
    """创建 Agent"""
    role_id: str
    project_id: str

class AgentDestroyPayload(BaseModel):
    """销毁 Agent"""
    reason: str = ""

PAYLOAD_TYPE_MAP = {
    "agent_create": AgentCreatePayload,
    "agent_destroy": AgentDestroyPayload,
    "agent_suspend": None,
    "agent_resume": None,
    "node_execute": NodeExecutePayload,
    "node_cancel": NodeCancelPayload,
    "chat_message": ChatMessagePayload,
}
```

### 3.3 FlowMessage

```python
# protocol/flow_message.py

class FlowMessage(BaseModel):
    """flow → runtime 统一消息"""
    message_id: str
    type: str                         # FlowMessageType
    agent_id: str
    project_id: str
    payload: dict                     # 传输层保持 dict（JSON 兼容）
    created_at: datetime

    def parse_payload(self) -> Optional[BaseModel]:
        """根据 type 解析 payload 为具体类型 — Fail Fast"""
        cls = PAYLOAD_TYPE_MAP.get(self.type)
        if cls is None and self.type not in PAYLOAD_TYPE_MAP:
            raise ValueError(f"Unknown message type: {self.type}")
        if cls is None:
            return None               # 该类型无 payload（suspend/resume）
        return cls(**self.payload)     # Pydantic 校验
```

### 3.4 NodeResult

```python
# protocol/node_result.py

class NodeResultType(str, Enum):
    COMPLETED = "completed"
    BLOCKED = "blocked"
    FAILED = "failed"

class NodeResult(BaseModel):
    """runtime → flow 执行结果"""
    message_id: str                   # 对应的 FlowMessage.message_id
    agent_id: str
    result_type: str                  # completed | blocked | failed
    output: Optional[dict] = None     # 执行产出
    reason: Optional[str] = None      # blocked/failed 时的原因
    reasoning_trace: Optional[str] = None  # 推理链摘要（可追溯性）
    duration_ms: int = 0
    tool_call_count: int = 0
    llm_call_count: int = 0
    total_tokens: int = 0
    created_at: datetime
```

## 四、数据结构 — models 模块

### 4.1 RoleDefinition + PermissionConfig

```python
# models/role.py

class PermissionConfig(BaseModel):
    """路径权限"""
    read_paths: List[str] = []        # 可读路径（支持 glob，如 "workspace/src/**"）
    write_paths: List[str] = []       # 可写路径
    forbidden_paths: List[str] = []   # 禁止访问路径（优先级最高）


class RoleDefinition(BaseModel):
    """角色定义 — 从 YAML 加载"""
    role_id: str                      # "tech_lead"
    role_name: str                    # "项目负责人"
    description: str = ""
    extends: Optional[str] = None     # 继承自哪个角色（Phase 2）

    # 共享规则引用
    shared_rules_include: List[str] = []
    shared_rules_exclude: List[str] = []

    # 身份与约束
    identity: str                     # 自然语言身份描述（注入 prompt 第二层）
    constraints: List[str] = []       # 行为约束列表（注入 prompt 第二层）

    # 能力
    tools: List[str] = []             # 角色级工具白名单（三层权限第二层）
    skills: List[str] = []            # 技能列表（Phase 3）
    workflows: List[str] = []         # 可安装的工作流 ID 列表

    # 权限
    permissions: PermissionConfig = Field(default_factory=PermissionConfig)

    # 元信息
    version: str = "1.0.0"
    created_by: str = "system"

    @validator("role_id")
    def validate_role_id(cls, v):
        if not re.match(r'^[a-z][a-z0-9_]*$', v):
            raise ValueError(f"role_id 格式无效: {v}")
        return v
```

### 4.2 SharedRule

```python
# models/rule.py

class SharedRule(BaseModel):
    """共享规则 — 从 YAML 加载"""
    rule_id: str                      # "communication"
    name: str
    description: str = ""
    enabled: bool = True              # 项目级可覆盖为 false

    # 双层消费
    schema_def: Optional[dict] = None # 有值 → 注入 prompt 第一层（系统硬约束）
    content: str                      # 无 schema_def → 注入 prompt 第三层（软指引）

    # 元数据
    applicable_roles: Union[str, List[str]] = "all"
    tags: List[str] = []
    version: str = "1.0.0"
    created_by: str = "system"
```

### 4.3 WorkItem

```python
# models/workitem.py

class WorkItemType(str, Enum):
    TASK = "task"
    BUG = "bug"
    REQUIREMENT = "requirement"
    EPIC = "epic"

class Priority(str, Enum):
    CRITICAL = "critical"
    HIGH = "high"
    MEDIUM = "medium"
    LOW = "low"

class WorkItemStatus(str, Enum):
    # 通用状态
    CREATED = "created"
    ASSIGNED = "assigned"
    IN_PROGRESS = "in_progress"
    IN_REVIEW = "in_review"
    IN_TESTING = "in_testing"
    COMPLETED = "completed"
    BLOCKED = "blocked"
    # Bug 专属
    CONFIRMED = "confirmed"
    FIXED = "fixed"
    CANNOT_REPRODUCE = "cannot_reproduce"
    # 需求专属
    REJECTED = "rejected"
    # 通用
    CLOSED = "closed"
    REOPEN = "reopen"


class WorkItem(BaseModel):
    """工单"""
    workitem_id: str                  # "TASK-003" / "BUG-002"
    type: WorkItemType
    title: str
    description: str = ""
    status: WorkItemStatus
    priority: Priority = Priority.MEDIUM
    assignee: Optional[str] = None    # 当前负责人 Agent ID
    reporter: Optional[str] = None    # 创建者 Agent ID
    project_id: str
    dependencies: List[str] = []      # 依赖的工单 ID 列表
    verification_command: Optional[str] = None  # 验收命令
    tdd: bool = True                  # 是否强制 TDD
    severity: Optional[str] = None    # Bug 严重程度
    created_at: datetime
    updated_at: datetime
```

### 4.4 Artifact

```python
# models/artifact.py

class ArtifactType(str, Enum):
    PRD = "prd"
    DESIGN_DOC = "design_doc"
    CODE = "code"
    TEST_REPORT = "test_report"
    EXECUTION_TRACE = "execution_trace"
    RELEASE_NOTE = "release_note"
    HANDOFF = "handoff"


class Artifact(BaseModel):
    """产物"""
    artifact_id: str
    type: ArtifactType
    name: str
    project_id: str
    task_ref: Optional[str] = None    # 关联的工单 ID
    file_path: Optional[str] = None   # 文件路径
    structured_data: Optional[dict] = None  # 结构化数据
    created_by: str
    created_at: datetime
```

---



---

## 五、BPMN 引擎核心

### 5.1 NodeStrategy 接口 + StrategyRegistry

```python
# bpmn/strategy.py

class NodeStrategy(ABC):
    """节点执行策略接口 — flow 和 runtime 各自实现"""

    @abstractmethod
    async def execute(self, node: FlowNode, ctx: "ExecutionContext") -> Optional[dict]:
        """执行节点，返回结果（None 表示异步等待回调）"""
        ...


class StrategyRegistry:
    """策略注册表 — 按 node_type 查找策略"""

    def __init__(self):
        self._strategies: Dict[str, NodeStrategy] = {}

    def register(self, node_type: str, strategy: NodeStrategy):
        self._strategies[node_type] = strategy

    def get(self, node_type: str) -> NodeStrategy:
        strategy = self._strategies.get(node_type)
        if strategy is None:
            raise ValueError(f"No strategy registered for node_type '{node_type}'")
        return strategy

    def has(self, node_type: str) -> bool:
        return node_type in self._strategies
```

### 5.2 ExecutionContext

```python
# bpmn/context.py

class ExecutionContext(BaseModel):
    """执行上下文基类 — 策略执行时需要的运行时依赖"""
    process_instance_id: str
    message_id: str = ""
    input_refs: List[str] = []
    control_level: str = "free"
    output_schema: Optional[dict] = None

    class Config:
        arbitrary_types_allowed = True


class FlowExecutionContext(ExecutionContext):
    """flow 侧的执行上下文"""
    runtime_client: Any               # HTTP 客户端
    gate_controller: Any              # 门禁控制器
    platform_client: Any              # platform API 客户端
    project_id: str = ""

    def resolve_actor(self, actor: Optional[str]) -> str:
        """解析角色 ID 为 agent_id"""
        # Phase 1：直接返回（角色 ID = agent_id）
        return actor or ""

    async def on_process_completed(self):
        """流程完成回调"""
        ...


class AgentExecutionContext(ExecutionContext):
    """runtime 侧的执行上下文"""
    agent: Any                        # Agent 实例
    tools: List[Any] = []             # 可用工具列表
    executor: Any                     # AgentExecutor 实例
    platform_client: Any              # platform API 客户端

    def get_agents(self, participant_ids: List[str]) -> List[Any]:
        """获取参与者的 Agent 实例列表"""
        ...
```

### 5.3 flow 侧策略

```python
# flow 项目中定义

class FlowTaskStrategy(NodeStrategy):
    """flow: AbstractTask → 构造 node_execute 消息发给 runtime"""
    async def execute(self, node: AbstractTask, ctx: FlowExecutionContext) -> Optional[dict]:
        msg = FlowMessage(
            message_id=generate_id(),
            type="node_execute",
            agent_id=ctx.resolve_actor(node.actor),
            project_id=ctx.project_id,
            payload=NodeExecutePayload(
                process_instance_id=ctx.process_instance_id,
                node=node.dict(),
                control_level="free",
                input_refs=node.input_refs,
                allowed_tools=node.allowed_tools,
                output_schema=node.output_schema,
            ).dict(),
            created_at=datetime.now(),
        )
        await ctx.runtime_client.send(msg)
        return None  # 异步等待 NodeResult

class FlowUserTaskStrategy(NodeStrategy):
    """flow: UserTask → 创建门禁卡片"""
    async def execute(self, node: UserTask, ctx: FlowExecutionContext) -> Optional[dict]:
        await ctx.gate_controller.create_gate(node)
        return None

class FlowAdHocStrategy(NodeStrategy):
    """flow: AdHocSubProcess → 构造 node_execute 消息"""
    async def execute(self, node: AdHocSubProcess, ctx: FlowExecutionContext) -> Optional[dict]:
        msg = FlowMessage(
            message_id=generate_id(),
            type="node_execute",
            agent_id=node.coordinator_id or node.participant_ids[0],
            project_id=ctx.project_id,
            payload=NodeExecutePayload(
                process_instance_id=ctx.process_instance_id,
                node=node.dict(),
                control_level="free",
            ).dict(),
            created_at=datetime.now(),
        )
        await ctx.runtime_client.send(msg)
        return None

class FlowEndEventStrategy(NodeStrategy):
    """flow: EndEvent → 流程完成，推进阶段"""
    async def execute(self, node: EndEvent, ctx: FlowExecutionContext) -> Optional[dict]:
        await ctx.on_process_completed()
        return None

def create_flow_registry() -> StrategyRegistry:
    registry = StrategyRegistry()
    registry.register("abstract_task", FlowTaskStrategy())
    registry.register("user_task", FlowUserTaskStrategy())
    registry.register("ad_hoc_sub_process", FlowAdHocStrategy())
    registry.register("end_event", FlowEndEventStrategy())
    return registry
```

### 5.4 runtime 侧策略

```python
# runtime 项目中定义

class AgentTaskStrategy(NodeStrategy):
    """runtime: AbstractTask → 注入 prompt，Agent think→act"""
    async def execute(self, node: AbstractTask, ctx: AgentExecutionContext) -> Optional[dict]:
        step_prompt = f"【当前步骤：{node.name}】\n{node.action or ''}"
        ctx.agent.conversation.append(Message(role="system", content=step_prompt))
        step_tools = ctx.tools + [create_step_complete_tool(node.output_schema)]
        result = await ctx.executor.run(ctx.agent, step_tools)
        return result

class AgentAdHocStrategy(NodeStrategy):
    """runtime: AdHocSubProcess → 启动协作活动"""
    async def execute(self, node: AdHocSubProcess, ctx: AgentExecutionContext) -> Optional[dict]:
        agenda = node.get_metadata("agenda", node.name)
        for agent in ctx.get_agents(node.get_participants()):
            agent.state_stack.append(StackEntry(
                state=StackState.EXECUTING,
                context={
                    "node_type": "ad_hoc_sub_process",
                    "node_id": node.id,
                    "coordinator_id": node.coordinator_id,
                    "participant_ids": node.participant_ids,
                }
            ))
            agent.conversation.append(Message(
                role="system",
                content=f"【协作活动开始】\n议程：{agenda}\n协调者：{node.coordinator_id}"
            ))
        return None

def create_agent_registry() -> StrategyRegistry:
    registry = StrategyRegistry()
    registry.register("abstract_task", AgentTaskStrategy())
    registry.register("ad_hoc_sub_process", AgentAdHocStrategy())
    return registry
```

### 5.5 BPMNEngine

```python
# bpmn/engine.py

class BPMNEngine:
    """BPMN 流程引擎 — Token 驱动 + 策略注册表
    
    这是整个系统的核心引擎，flow 和 runtime 都通过它驱动流程执行。
    
    设计原则：
      - 引擎只负责 Token 管理和节点遍历（"怎么走"）
      - 节点的具体行为由策略决定（"到了这里做什么"）
      - Gateway 的条件评估由对象自己完成（对象自治）
      - 引擎不包含任何业务逻辑
    
    为什么 Gateway 不走策略：
      Gateway 的行为（条件评估 / Token 分裂合并）是 BPMN 标准语义，
      在 flow 和 runtime 中完全一样，不需要按环境区分。
      而且 Gateway 的行为和 Token 管理紧密耦合（分裂/合并），
      放在引擎内部比委托给外部策略更自然。
    """

    def __init__(self, registry: StrategyRegistry, on_event=None):
        self.registry = registry
        # on_event 回调用于可观测性：引擎每个关键动作都会通知外部
        # flow 用它记录 edge_history，runtime 用它发送 AG-UI 事件给前端
        self.on_event = on_event or (lambda e: None)

    async def run(self, process_def: ProcessDefinition, instance: ProcessInstance, ctx: ExecutionContext):
        """Token 驱动的流程执行
        
        为什么是 async：
          策略的 execute() 可能涉及网络调用（flow 发 HTTP 给 runtime，
          runtime 调用 LLM），必须是异步的。
        """

        # 0. 校验 — 契约设计的前置条件
        # 为什么在 run() 里校验而不是在 from_dict() 里：
        #   from_dict() 已经校验了，但 ProcessDefinition 可能被手动构造（测试场景），
        #   所以 run() 再校验一次作为防御性编程。
        errors = process_def.validate_structure()
        if errors:
            raise ValueError(f"ProcessDefinition 校验失败: {errors}")

        # 1. 创建初始 Token — 流程从 StartEvent 开始
        start_node = process_def.get_start_event()
        initial_token = Token(token_id=generate_id(), node_id=start_node.id)
        instance.tokens = [initial_token]
        self.on_event({"type": "engine_started", "process_id": process_def.process_id})

        # 2. 主循环：每轮处理所有 active Token
        # 为什么是 while + for 而不是递归：
        #   递归在深层流程中会栈溢出（100 个节点 = 100 层递归）。
        #   while 循环 + Token 列表是迭代式的，无栈深度限制。
        while self.has_active_tokens(instance):
            for token in self.get_active_tokens(instance):
                node = process_def.get_node(token.node_id)
                self.on_event({"type": "node_enter", "node_id": node.id, "token_id": token.token_id})

                # Gateway 对象自治（条件评估 / Token 分裂合并）
                # Activity/Event 走策略注册表（由 flow/runtime 各自实现）
                # 为什么用 isinstance 而不是 node_type 字符串：
                #   isinstance 是类型安全的，IDE 能提示方法；
                #   而且 Gateway 的处理逻辑在 advance_token 中，不在这里。
                if not isinstance(node, (ExclusiveGateway, ParallelGateway)):
                    if self.registry.has(node.node_type):
                        strategy = self.registry.get(node.node_type)
                        result = await strategy.execute(node, ctx)
                        if result:
                            instance.node_outputs[node.id] = result

                await self.advance_token(token, node, process_def, instance)

        self.on_event({"type": "engine_completed", "process_id": process_def.process_id})

    async def advance_token(self, token, node, process_def, instance):
        """推进 token（幂等：已 completed 跳过）"""

        if token.status == "completed":
            return

        if isinstance(node, EndEvent):
            token.transition_to("completed")
            if node.is_terminate():
                for t in instance.tokens:
                    if t.status == "active":
                        t.transition_to("completed")
            self.on_event({"type": "token_end", "token_id": token.token_id})
            return

        if isinstance(node, ParallelGateway) and node.direction == "diverging":
            token.transition_to("completed")
            targets = node.get_fork_targets(process_def.get_outgoing_edges(node.id))
            for target_id in targets:
                child = Token(token_id=generate_id(), node_id=target_id, parent_token_id=token.token_id)
                instance.tokens.append(child)
            self.on_event({"type": "token_fork", "parent": token.token_id, "children": len(targets)})
            return

        if isinstance(node, ParallelGateway) and node.direction == "converging":
            token.transition_to("waiting")
            waiting = [t for t in instance.tokens if t.node_id == node.id and t.status == "waiting"]
            expected = len(process_def.get_incoming_edges(node.id))
            if node.is_join_complete(len(waiting), expected):
                for t in waiting:
                    t.transition_to("completed")
                outgoing = process_def.get_outgoing_edges(node.id)
                if outgoing:
                    merged = Token(token_id=generate_id(), node_id=outgoing[0].target)
                    instance.tokens.append(merged)
                self.on_event({"type": "token_join", "node_id": node.id})
            return

        if isinstance(node, ExclusiveGateway):
            outgoing = process_def.get_outgoing_edges(node.id)
            target = node.evaluate(outgoing, instance.node_outputs)
            token.node_id = target
            return

        # 普通节点：沿 sequence flow 推进
        outgoing = process_def.get_outgoing_edges(node.id)
        if outgoing:
            token.node_id = outgoing[0].target
        else:
            raise RuntimeError(f"节点 '{node.id}' 没有出口 edge")

    def has_active_tokens(self, instance) -> bool:
        return any(t.status in ("active", "waiting") for t in instance.tokens)

    def get_active_tokens(self, instance) -> List[Token]:
        return [t for t in instance.tokens if t.status == "active"]
```



---

## 六、网关条件求值

> 条件求值已封装在 ExclusiveGateway.evaluate() 中（对象自治），
> BPMNEngine 不再包含求值逻辑。此章描述求值规则的详细规格。

### 6.1 Phase 1：简单字段匹配

```python
# ExclusiveGateway._match_condition() 的详细规格

支持的操作符：
  field == value        # 等于
  field != value        # 不等于

求值上下文：
  instance.node_outputs 中所有已完成节点的 output 合并为一个 flat dict
  例：上一个节点 output = {"verdict": "approved", "score": 85}
      condition = "verdict == approved" → True

边界情况：
  - field 不存在 → str(None) == value → 通常为 False
  - value 带引号 → 自动去除（"approved" / 'approved' → approved）
  - 多个 == → 只取第一个分割点
```

### 6.2 Phase 2：表达式引擎预留

```python
# Phase 2 扩展 ExclusiveGateway._match_condition()

支持的操作符扩展：
  field > value         # 大于（数值比较）
  field < value         # 小于
  field >= value        # 大于等于
  field in [a, b, c]   # 包含
  field.contains(str)   # 字符串包含

实现方式：
  Phase 1：字符串分割（简单可靠）
  Phase 2：引入轻量表达式引擎（如 simpleeval）
  不使用 Python eval（安全风险）
```

## 七、JSON Schema 规范

### 7.1 目录结构

```
schemas/
├── flow-message.schema.json        # FlowMessage 格式
├── node-result.schema.json         # NodeResult 格式
├── process-definition.schema.json  # ProcessDefinition 格式
├── role-definition.schema.json     # RoleDefinition 格式
├── shared-rule.schema.json         # SharedRule 格式
└── workitem.schema.json            # WorkItem 格式
```

### 7.2 schema 与 Pydantic 模型的关系

Python Pydantic 模型是权威实现，JSON Schema 从模型生成：

```python
# 生成 schema 的脚本
from quantalithos_core.protocol import FlowMessage
import json

schema = FlowMessage.model_json_schema()
with open("schemas/flow-message.schema.json", "w") as f:
    json.dump(schema, f, indent=2, ensure_ascii=False)
```

开发流程：修改 Pydantic 模型 → 重新生成 schema → 提交到 schemas/ 目录。

### 7.3 process-definition.schema.json 示例片段

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ProcessDefinition",
  "type": "object",
  "required": ["process_id", "name", "nodes", "edges"],
  "properties": {
    "process_id": { "type": "string" },
    "name": { "type": "string" },
    "version": { "type": "string", "default": "1.0.0" },
    "nodes": {
      "type": "array",
      "items": {
        "oneOf": [
          { "$ref": "#/$defs/EventNode" },
          { "$ref": "#/$defs/ActivityNode" },
          { "$ref": "#/$defs/GatewayNode" }
        ],
        "discriminator": { "propertyName": "category" }
      }
    },
    "edges": {
      "type": "array",
      "items": { "$ref": "#/$defs/FlowEdge" }
    }
  }
}
```

### 7.4 sdk (Rust) 对齐方式

sdk-models 的 Rust struct 从 schemas/ 手动对齐（Phase 1）或自动生成（Phase 2）：

```rust
// sdk-models/src/process.rs — 手动对齐 process-definition.schema.json
#[derive(Serialize, Deserialize)]
pub struct ProcessDefinition {
    pub process_id: String,
    pub name: String,
    pub version: String,
    pub nodes: Vec<FlowNode>,
    pub edges: Vec<FlowEdge>,
}
```

Phase 2 考虑引入 `typify` 或 `schemafy` 从 JSON Schema 自动生成 Rust struct。

---

## 八、错误类型

```python
# bpmn/errors.py

class CoreError(Exception):
    """core 错误基类"""

class ProcessDefinitionError(CoreError):
    """流程定义错误"""
    # 触发场景：
    #   - 没有 start 节点
    #   - 节点 ID 重复
    #   - 边引用不存在的节点
    #   - nodes 列表为空

class NoMatchingCondition(CoreError):
    """排他网关无匹配条件"""
    # 触发场景：
    #   - exclusive gateway 所有条件边都不匹配
    #   - 且没有 default 边
    #   - 且没有 default_route

class UnsupportedGatewayType(CoreError):
    """不支持的网关类型"""
    # 触发场景：
    #   - Phase 1 使用 parallel gateway
    #   - 使用 inclusive gateway

class InvalidCondition(CoreError):
    """条件表达式格式错误"""
    # 触发场景：
    #   - 条件字符串无法解析（不符合 "field op value" 格式）

class InvalidFlowMessage(CoreError):
    """FlowMessage 格式错误"""
    # 触发场景：
    #   - type 不在 FlowMessageType 枚举中
    #   - payload 缺少必填字段（由消费者校验，core 只提供类型定义）
```

ProcessDefinition 加载时的校验（在 `__init__` 或 validator 中）：

```python
@validator("nodes")
def validate_nodes(cls, v):
    ids = [n.id for n in v]
    if len(ids) != len(set(ids)):
        raise ProcessDefinitionError("节点 ID 重复")
    starts = [n for n in v if isinstance(n, StartEvent)]
    if len(starts) == 0:
        raise ProcessDefinitionError("缺少 start 节点")
    if len(starts) > 1:
        raise ProcessDefinitionError("多个 start 节点")
    return v

@validator("edges")
def validate_edges(cls, v, values):
    node_ids = {n.id for n in values.get("nodes", [])}
    for edge in v:
        if edge.source not in node_ids:
            raise ProcessDefinitionError(f"边 {edge.id} 的 source '{edge.source}' 不存在")
        if edge.target not in node_ids:
            raise ProcessDefinitionError(f"边 {edge.id} 的 target '{edge.target}' 不存在")
    return v
```

---



---

## 九、完整示例

### 9.1 YAML 定义（TDD 工作流）

```yaml
process_id: tdd_workflow
name: TDD 工作流
version: "1.0"

nodes:
  - id: start
    node_type: start_event

  - id: write_test
    node_type: abstract_task
    name: "写失败测试"
    action: "为当前任务编写单元测试，测试应该失败（红灯）"
    allowed_tools: [file_read, file_write]
    output_schema:
      type: object
      properties:
        test_file: { type: string }
      required: [test_file]

  - id: run_red
    node_type: abstract_task
    name: "运行测试（确认红灯）"
    action: "运行测试，确认新写的测试失败"
    allowed_tools: [code_execute]
    output_schema:
      type: object
      properties:
        passed: { type: boolean }
        output: { type: string }
      required: [passed]

  - id: write_code
    node_type: abstract_task
    name: "写代码"
    action: "编写最少的代码让测试通过"
    allowed_tools: [file_read, file_write]

  - id: run_green
    node_type: abstract_task
    name: "运行测试（确认绿灯）"
    action: "运行测试，确认所有测试通过"
    allowed_tools: [code_execute]
    output_schema:
      type: object
      properties:
        passed: { type: boolean }
        total: { type: integer }
        failed: { type: integer }
      required: [passed]

  - id: check_result
    node_type: exclusive_gateway
    direction: diverging
    default_route: write_code

  - id: end
    node_type: end_event

edges:
  - id: e1
    source: start
    target: write_test
    flow_type: sequence

  - id: e2
    source: write_test
    target: run_red
    flow_type: sequence

  - id: e3
    source: run_red
    target: write_code
    flow_type: sequence

  - id: e4
    source: write_code
    target: run_green
    flow_type: sequence

  - id: e5
    source: run_green
    target: check_result
    flow_type: sequence

  - id: e6
    source: check_result
    target: end
    flow_type: conditional
    condition: "passed == True"
    label: "pass"

  - id: e7
    source: check_result
    target: write_code
    flow_type: default
    label: "fail → retry"
```

### 9.2 加载与校验

```python
import yaml

# 从 YAML 加载
with open("tdd_workflow.yaml") as f:
    data = yaml.safe_load(f)

# 工厂方法：反序列化 + 校验
process_def = ProcessDefinition.from_dict(data)
# → 内部调用 deserialize_node() 将每个 dict 转为具体类型
# → 内部调用 validate_structure() 校验 8 条规则
# → 校验失败立即 raise ValueError
```

### 9.3 runtime 侧使用

```python
# WorkflowEngine.run() 内部

# 1. 加载
process_def = ProcessDefinition.from_dict(
    await platform_client.get_process("tdd_workflow")
)

# 2. 创建实例
instance = ProcessInstance(
    instance_id=generate_id(),
    process_id="tdd_workflow",
    project_id="blog",
    scope="agent",
)

# 3. 构造上下文
ctx = AgentExecutionContext(
    process_instance_id=instance.instance_id,
    agent=agent,
    tools=tools,
    executor=executor,
    platform_client=platform_client,
)

# 4. 构造策略注册表
registry = create_agent_registry()
# → {"abstract_task": AgentTaskStrategy(), "ad_hoc_sub_process": AgentAdHocStrategy()}

# 5. 引擎驱动
engine = BPMNEngine(registry)
await engine.run(process_def, instance, ctx)

# 执行过程：
# Token T1 → StartEvent → 无策略 → 跳过 → 推进
# Token T1 → AbstractTask("写失败测试") → AgentTaskStrategy → Agent think→act → step_complete
# Token T1 → AbstractTask("运行测试") → AgentTaskStrategy → Agent 执行 → {passed: false}
# Token T1 → AbstractTask("写代码") → AgentTaskStrategy → Agent 编码
# Token T1 → AbstractTask("运行测试") → AgentTaskStrategy → {passed: true}
# Token T1 → ExclusiveGateway → node.evaluate() → "passed == True" → 走 end
# Token T1 → EndEvent → token completed → 流程结束
```

### 9.4 flow 侧使用

```python
# FlowEngine.start_process() 内部

# 1. 加载
process_def = ProcessDefinition.from_dict(
    await platform_client.get_process("requirement_analysis")
)

# 2. 创建实例（flow 扩展版）
instance = FlowProcessInstance(
    instance_id=generate_id(),
    process_id="requirement_analysis",
    project_id="blog",
    scope="stage",
)

# 3. 构造上下文
ctx = FlowExecutionContext(
    process_instance_id=instance.instance_id,
    project_id="blog",
    runtime_client=runtime_client,
    gate_controller=gate_controller,
    platform_client=platform_client,
)

# 4. 构造策略注册表
registry = create_flow_registry()
# → {"abstract_task": FlowTaskStrategy(), "user_task": FlowUserTaskStrategy(),
#    "ad_hoc_sub_process": FlowAdHocStrategy(), "end_event": FlowEndEventStrategy()}

# 5. 引擎驱动
engine = BPMNEngine(registry)
await engine.run(process_def, instance, ctx)

# 执行过程：
# Token T1 → StartEvent → 无策略 → 跳过
# Token T1 → AbstractTask("收集需求") → FlowTaskStrategy → 发 node_execute 给 runtime → 等待 NodeResult
# Token T1 → AdHocSubProcess("头脑风暴") → FlowAdHocStrategy → 发 node_execute → 等待
# Token T1 → AbstractTask("写 PRD") → FlowTaskStrategy → 发 node_execute → 等待
# Token T1 → UserTask("用户确认") → FlowUserTaskStrategy → 创建 GateCard → 等待用户操作
# Token T1 → EndEvent → FlowEndEventStrategy → 推进阶段
```

## 十、Phase 分期

### Phase 1

| 模块 | 文件 | 范围 |
|------|------|------|
| bpmn | models.py | FlowNode 继承体系：StartEvent + EndEvent + AbstractTask + UserTask + AdHocSubProcess + ExclusiveGateway + FlowEdge + NODE_TYPE_MAP + deserialize_node |
| bpmn | token.py | Token（状态机 + history + transition_to） |
| bpmn | process.py | ProcessDefinition（from_dict + validate_structure + get_node/edges）+ ProcessInstance |
| bpmn | engine.py | BPMNEngine（Token 驱动 + advance_token + on_event） |
| bpmn | strategy.py | NodeStrategy ABC + StrategyRegistry |
| bpmn | context.py | ExecutionContext + FlowExecutionContext + AgentExecutionContext |
| protocol | flow_message.py | FlowMessage + FlowMessageType（6 种）+ parse_payload |
| protocol | payloads.py | NodeExecutePayload + NodeCancelPayload + ChatMessagePayload + AgentCreatePayload + AgentDestroyPayload |
| protocol | node_result.py | NodeResult + NodeResultType（3 种） |
| models | role.py | RoleDefinition + PermissionConfig |
| models | rule.py | SharedRule |
| models | workitem.py | WorkItem + WorkItemType + Priority + WorkItemStatus |
| models | artifact.py | Artifact + ArtifactType |
| schemas | *.schema.json | 6 个 JSON Schema 文件 |

### Phase 2

| 模块 | 新增内容 |
|------|---------|
| bpmn/models | IntermediateEvent + BoundaryEvent + ServiceTask + CallActivity + ParallelGateway |
| bpmn/engine | ParallelGateway Token 分裂/合并 + CallActivity 子流程 + BoundaryEvent 处理 |
| bpmn/models | ExclusiveGateway._match_condition 扩展（>, <, in, and, or） |
| bpmn/process | ProcessDefinition.version 迁移支持 |
| models/role | RoleDefinition.extends 继承合并 |
| rust/models | 和 schemas/ 对齐的 Rust struct |


---

## 十一、SandboxProvider 完整接口

```python
# bpmn/sandbox.py

from abc import ABC, abstractmethod
from typing import Optional
from pydantic import BaseModel


class ExecResult(BaseModel):
    """命令执行结果"""
    exit_code: int
    stdout: str
    stderr: str
    duration_ms: int

    @property
    def success(self) -> bool:
        return self.exit_code == 0


class FileInfo(BaseModel):
    """文件信息"""
    path: str
    size: int
    exists: bool


class SandboxProvider(ABC):
    """沙箱环境抽象接口 — runtime 工具通过此接口在容器内执行操作

    实现者：
      - DockerSandboxProvider（Phase 1：调用 sandbox-manager API）
      - HostSandboxProvider（测试用：直接在宿主机执行）
      - MockSandboxProvider（单元测试用）
    """

    @abstractmethod
    async def exec(self, agent_id: str, command: str, timeout: int = 300) -> ExecResult:
        """在指定 Agent 的容器内执行命令

        Args:
            agent_id: Agent 标识（决定在哪个容器执行）
            command: shell 命令（如 "cargo test"）
            timeout: 超时秒数（默认 300s）

        Returns:
            ExecResult: 包含 exit_code / stdout / stderr / duration_ms

        Raises:
            SandboxNotAvailable: 容器未启动或不可达
            SandboxTimeout: 命令执行超时
        """
        ...

    @abstractmethod
    async def write_file(self, agent_id: str, path: str, content: str) -> None:
        """写文件到指定 Agent 的容器内

        Args:
            agent_id: Agent 标识
            path: 容器内路径（相对于 /workspace/，如 "src/models/tag.py"）
            content: 文件内容

        Raises:
            SandboxNotAvailable: 容器未启动
            SandboxPermissionError: 路径不在 /workspace/ 内（安全限制）
        """
        ...

    @abstractmethod
    async def read_file(self, agent_id: str, path: str) -> str:
        """读取指定 Agent 容器内的文件

        Args:
            agent_id: Agent 标识
            path: 容器内路径

        Returns:
            文件内容字符串

        Raises:
            SandboxNotAvailable: 容器未启动
            FileNotFoundError: 文件不存在
        """
        ...

    @abstractmethod
    async def list_files(self, agent_id: str, path: str = ".") -> list[FileInfo]:
        """列出目录内容

        Args:
            agent_id: Agent 标识
            path: 目录路径（相对于 /workspace/）

        Returns:
            FileInfo 列表
        """
        ...

    @abstractmethod
    async def git_commit(self, agent_id: str, message: str, files: list[str] = None) -> str:
        """在容器内执行 git add + git commit

        Args:
            agent_id: Agent 标识
            message: commit message
            files: 要 stage 的文件列表（None = git add -A）

        Returns:
            commit hash

        Raises:
            SandboxNotAvailable: 容器未启动
            GitError: git 操作失败（如没有变更）
        """
        ...

    @abstractmethod
    async def git_push(self, agent_id: str) -> None:
        """在容器内执行 git push

        Raises:
            SandboxNotAvailable: 容器未启动
            GitError: push 失败（如远程拒绝）
        """
        ...

    @abstractmethod
    async def get_service_url(self, service_name: str) -> str:
        """获取共享服务的内部 URL

        Args:
            service_name: 服务名（"postgres" / "redis" / "gitea"）

        Returns:
            内部 URL（如 "postgres://user:pass@blog-postgres:5432/blog"）
        """
        ...

    @abstractmethod
    async def get_status(self, agent_id: str) -> dict:
        """获取 Agent 容器的状态

        Returns:
            {"status": "running", "cpu_percent": 45, "memory_mb": 512}
        """
        ...


# 异常类型
class SandboxNotAvailable(Exception):
    """沙箱环境不可用（容器未启动 / Docker 不可达）"""
    pass

class SandboxTimeout(Exception):
    """命令执行超时"""
    pass

class SandboxPermissionError(Exception):
    """权限错误（试图访问 /workspace/ 之外的路径）"""
    pass

class GitError(Exception):
    """Git 操作失败"""
    pass
```

---

## 十二、单元测试策略

### 12.1 测试范围

| 模块 | 测试重点 | 测试方式 |
|------|---------|---------|
| bpmn/models | 每个 FlowNode 子类的领域行为 | 单元测试 |
| bpmn/token | 状态机转换（合法/非法）+ history 记录 | 单元测试 |
| bpmn/process | validate_structure() 8 条规则 + from_dict() | 单元测试 |
| bpmn/engine | Token 驱动遍历 + 策略调用 + advance_token | 集成测试（mock 策略） |
| bpmn/strategy | StrategyRegistry 注册/查找/缺失 | 单元测试 |
| protocol | FlowMessage.parse_payload() + 各 Payload 校验 | 单元测试 |
| models | Pydantic 模型序列化/反序列化 | 单元测试 |

### 12.2 测试示例

```python
# tests/test_token.py

def test_token_valid_transitions():
    token = Token(token_id="t1", node_id="n1")
    assert token.status == "active"

    token.transition_to("completed")
    assert token.status == "completed"
    assert len(token.history) == 1
    assert token.history[0]["from"] == "active"
    assert token.history[0]["to"] == "completed"

def test_token_invalid_transition():
    token = Token(token_id="t1", node_id="n1")
    token.transition_to("completed")

    with pytest.raises(ValueError, match="非法状态转换"):
        token.transition_to("active")  # completed → active 不允许

def test_token_idempotent():
    """已 completed 的 token 不应该被再次处理"""
    token = Token(token_id="t1", node_id="n1")
    token.transition_to("completed")
    # advance_token 应该跳过
```

```python
# tests/test_process_definition.py

def test_validate_no_start_event():
    pd = ProcessDefinition(
        process_id="test",
        nodes=[EndEvent(id="end", node_type="end_event")],
        edges=[],
    )
    errors = pd.validate_structure()
    assert "必须有一个 StartEvent" in errors[0]

def test_validate_edge_target_not_exist():
    pd = ProcessDefinition(
        process_id="test",
        nodes=[
            StartEvent(id="start", node_type="start_event"),
            EndEvent(id="end", node_type="end_event"),
        ],
        edges=[FlowEdge(id="e1", source="start", target="nonexist")],
    )
    errors = pd.validate_structure()
    assert "不存在" in errors[0]

def test_from_dict_valid():
    data = {
        "process_id": "tdd",
        "nodes": [
            {"node_type": "start_event", "id": "s"},
            {"node_type": "abstract_task", "id": "t1", "name": "写测试"},
            {"node_type": "end_event", "id": "e"},
        ],
        "edges": [
            {"id": "e1", "source": "s", "target": "t1"},
            {"id": "e2", "source": "t1", "target": "e"},
        ],
    }
    pd = ProcessDefinition.from_dict(data)
    assert pd.process_id == "tdd"
    assert len(pd.nodes) == 3
    assert isinstance(pd.nodes[1], AbstractTask)
```

```python
# tests/test_engine.py

async def test_engine_linear_flow():
    """线性流程：start → task → end"""
    # 准备
    pd = ProcessDefinition.from_dict({...})
    instance = ProcessInstance(instance_id="i1", process_id="test", project_id="p1")
    results = []

    class MockStrategy(NodeStrategy):
        async def execute(self, node, ctx):
            results.append(node.id)
            return {"done": True}

    registry = StrategyRegistry()
    registry.register("abstract_task", MockStrategy())

    ctx = ExecutionContext(process_instance_id="i1")
    engine = BPMNEngine(registry)

    # 执行
    await engine.run(pd, instance, ctx)

    # 验证
    assert results == ["t1"]  # 只有 abstract_task 被策略处理
    assert instance.tokens[0].status == "completed"
    assert instance.node_outputs["t1"] == {"done": True}
```

### 12.3 测试工具

```
pytest + pytest-asyncio（异步测试）
coverage（覆盖率目标：核心模块 > 90%）
```

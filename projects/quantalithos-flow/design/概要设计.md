# quantalithos-flow 概要设计

> 流程编排引擎 — BPMN 2.0 流程编排、门禁控制、任务调度、消息路由（依赖 quantalithos-core）

---

## 一、项目定位

### 1.1 核心职责

quantalithos-flow 是整个平台的**编排中枢（daemon）**，负责宏观流程控制：

- **阶段状态机**：管理项目在 6 个阶段之间的流转、门禁、回退
- **工作流模板**：定义阶段内部的 step 顺序、触发条件、完成判定
- **任务状态机**：驱动任务卡在 created→assigned→in_development→...→completed 之间流转
- **step 调度**：决定当前节点由哪个 Agent 执行，通过 BPMNEngine + 策略注册表驱动
- **消息路由**：控制消息分发、@提及路由、参与度控制
- **门禁控制**：硬门禁（用户必须确认）、软门禁（默认自动通过）
- **交接包管理**：阶段间的正式交接对象（entry_packet → exit_packet）

### 1.2 设计目标与非目标

| 设计目标 | 说明 |
|---------|------|
| daemon 长驻进程 | flow 是一个持续运行的服务，监听事件并驱动流程 |
| 事件驱动 | 监听 platform 事件（WorkItemAssigned 等），不轮询 |
| BPMN 对齐 | 所有流程用 ProcessDefinition 描述，BPMNEngine + Token 驱动 |
| 门禁控制 | 关键节点暂停等待人类确认，硬门禁不可跳过 |
| 持久执行 | 每步持久化状态，崩溃后可从断点恢复 |

| 非目标 | 说明 |
|--------|------|
| 不做 Agent 执行 | "怎么做"由 runtime 负责，flow 只负责"谁做什么" |
| 不做数据存储 | 数据在 platform，flow 通过 API 读写 |
| 不做任务调度 | 依赖图和波次调度由 platform TaskScheduler 负责 |
| 不做消息渲染 | 消息展示由 chat 负责 |

### 1.3 与 quantalithos-core 的关系

flow 依赖 core（`pip install quantalithos-core`）获取：

- **BPMN 引擎**：`BPMNEngine` + `StrategyRegistry` — flow 注册 `FlowTaskStrategy` / `FlowUserTaskStrategy` / `FlowAdHocStrategy`
- **BPMN 节点模型**：`StartEvent`、`EndEvent`、`AbstractTask`、`UserTask`、`AdHocSubProcess`、`ExclusiveGateway`、`ParallelGateway`
- **通信协议**：`FlowMessage`（构造 node_execute 发给 runtime）、`NodeResult`（从 runtime 接收）
- **数据模型**：`RoleDefinition`、`SharedRule`、`WorkItem`

```
from quantalithos_core.bpmn import BPMNEngine, StrategyRegistry, ProcessDefinition
from quantalithos_core.bpmn import AbstractTask, UserTask, AdHocSubProcess, ExclusiveGateway
from quantalithos_core.protocol import FlowMessage, NodeResult
from quantalithos_core.models import WorkItem
```

### 1.3 在整体架构中的位置

```
quantalithos-chat (聊天前端)
      │
      │ HTTP/WebSocket
      ▼
quantalithos-gate (API 网关)
      │
      ├──────────────────┬──────────────────┐
      │                  │                  │
      ▼                  ▼                  ▼
╔═══════════════╗  quantalithos-platform  quantalithos-runtime
║ quantalithos- ║  (平台数据服务)          (Agent 运行时)
║ flow (daemon) ║        │                  │
╚═══════╤═══════╝        │                  │
        │                │                  │
        └────────┬───────┘                  │
                 │                          │
                 └──────────┬───────────────┘
                            │ pip install
                            ▼
                     quantalithos-core
                     (BPMN 引擎 + 协议 + 模型)
```

### 1.4 与 quantalithos-runtime 的分工

| 维度 | flow | runtime |
|------|------|---------|
| 职责 | "谁做什么、什么顺序" | "怎么做" |
| 粒度 | 项目级 / 阶段级 | Agent 级 |
| 驱动方式 | BPMNEngine + FlowTaskStrategy 等 | BPMNEngine + AgentTaskStrategy 等 |
| 消息 | 构造 FlowMessage(node_execute) | 接收并执行 |
| 门禁 | FlowUserTaskStrategy 创建 GateCard | 不参与 |
| 状态 | FlowProcessInstance（含 edge_history） | ProcessInstance（基础版） |

---

## 二、核心概念

| 概念 | 说明 |
|------|------|
| ProcessDefinition | BPMN 流程定义（YAML → 节点 + 边），三种 scope：pipeline / stage / agent |
| ProcessInstance | 流程实例（运行时状态），flow 用 FlowProcessInstance 扩展版 |
| Token | 执行指针，由 BPMNEngine 管理（分裂/合并/推进） |
| FlowMessage | flow → runtime 的统一消息（type=node_execute / chat_message / agent_create 等） |
| NodeResult | runtime → flow 的执行结果（completed / blocked / failed） |
| GateCard | 门禁审批卡片，发给 chat 展示，等待用户操作 |
| FlowProcessInstance | flow 扩展的 ProcessInstance，增加 edge_history / gate_history |

---

## 三、模块划分

```
quantalithos-flow/
│
├── engine/                      # BPMN 流程引擎
│   ├── strategies.py            # 节点执行策略（FlowTaskStrategy 等）
│   ├── flow_engine.py           # FlowEngine（委托 core.BPMNEngine）
│   └── process_manager.py       # ProcessDefinition 加载
│
├── business/                    # 业务逻辑
│   ├── platform_listener.py     # 监听 platform 事件
│   ├── gate_controller.py       # 门禁审批
│   └── agent_lifecycle.py       # Agent 生命周期管理
│
├── infra/                       # 基础设施
│   ├── state_persister.py       # 状态持久化
│   ├── runtime_client.py        # 与 runtime 通信
│   └── platform_client.py       # 与 platform 通信
│
└── daemon/                      # daemon 主进程
    ├── main.py                  # 入口
    └── event_loop.py            # 事件循环
```

---

## 四、与其他子项目的接口

| 对接方 | 接口方式 | 说明 |
|--------|----------|------|
| quantalithos-runtime | FlowMessage(node_execute) / NodeResult | flow 下发节点执行，runtime 上报结果 |
| quantalithos-chat | WebSocket / API | 门禁审批卡片、状态更新推送 |
| quantalithos-platform | API | 读写工单、产物、快照、流程模板 |
| quantalithos-sync | CLI 调用 | 项目启动时 restore |

---

## 五、flow 需要实现的 BPMN 元素清单

> 完整的落地决策表见 `docs/design/流程图模型与节点原语设计.md` 第十章。

### Phase 1（10 种）

| BPMN 元素 | flow 的处理方式 |
|-----------|---------------|
| StartEvent (none) | 不注册策略，BPMNEngine 自动跳过 |
| EndEvent (none) | FlowEndEventStrategy → 流程完成，推进阶段 |
| EndEvent (terminate) | EndEvent.is_terminate() → BPMNEngine 终止所有 token |
| Abstract Task | FlowTaskStrategy → 构造 FlowMessage(node_execute) 发给 runtime |
| User Task | FlowUserTaskStrategy → 创建 GateCard，等待用户操作 |
| Ad-Hoc SubProcess | FlowAdHocStrategy → 构造 FlowMessage(node_execute) 发给 runtime |
| Exclusive Gateway (XOR) | 不注册策略，ExclusiveGateway.evaluate() 对象自治 |
| Sequence Flow | BPMNEngine.advance_token → 沿边推进 |
| Conditional Flow | ExclusiveGateway.evaluate() 评估 edge.condition |
| Default Flow | ExclusiveGateway.default_route 兜底 |

### Phase 2 新增（4 种）

| BPMN 元素 | flow 的处理方式 |
|-----------|---------------|
| Parallel Gateway (AND) | Token 分裂/合并（BPMNEngine 处理） |
| Call Activity | 启动子流程 ProcessInstance |
| Boundary Event (timer) | 超时后走超时分支 |
| Boundary Event (error) | NodeResult(failed) 后走错误分支 |

---

## 六、Phase 分期

### Phase 1

| 模块 | 范围 |
|------|------|
| engine/strategies | FlowTaskStrategy / FlowUserTaskStrategy / FlowAdHocStrategy / FlowEndEventStrategy |
| engine/flow_engine | FlowEngine + StrategyRegistry + FlowExecutionContext |
| engine/process_manager | ProcessDefinition 加载（从 platform API） |
| business/gate_controller | hard/soft 门禁、审批卡片 |
| business/platform_listener | 监听 WorkItemAssigned / TaskBecameReady 事件 |
| business/agent_lifecycle | agent_create / agent_destroy |
| infra/state_persister | 文件持久化（current.yaml） |
| daemon | 基础事件循环 |

### Phase 2

| 模块 | 范围 |
|------|------|
| engine/strategies | ParallelGateway（Token 分裂/合并）、CallActivity 策略 |
| engine/flow_engine | 表达式引擎扩展 |
| business/agent_lifecycle | agent_suspend / agent_resume |
| infra/state_persister | 事件日志（Event Sourcing） |
| 门禁 | 超时处理 |

---

## 七、相关文档

- 详细设计：`projects/quantalithos-flow/design/详细设计.md`
- 核心共享库：`projects/quantalithos-core/design/概要设计.md`
- 统一 Agent 模型：`docs/design/Agent统一模型与三级控制设计.md`
- 节点原语设计：`docs/design/流程图模型与节点原语设计.md`
- BPMN 2.0 标准：`docs/design/流程图标准-BPMN2.md`

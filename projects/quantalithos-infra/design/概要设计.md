# quantalithos-infra 概要设计

> 基础设施 — 容器编排、CI/CD、监控告警、数据库管理

---

## 一、项目定位

### 1.1 核心职责

quantalithos-infra 不是运行时服务，是一组部署配置和运维工具，管理所有子项目的运行环境。

```
┌─────────────────────────────────────────────────────────────┐
│  quantalithos-infra 管理的范围                               │
│                                                             │
│  ┌─────────┐  ┌─────────┐  ┌──────────┐  ┌─────────┐     │
│  │  gate   │  │platform │  │  flow    │  │ runtime │     │
│  │  :8000  │  │  :8001  │  │  :8002   │  │  :8003  │     │
│  └────┬────┘  └────┬────┘  └────┬─────┘  └────┬────┘     │
│       │            │            │              │           │
│  ┌────┴────────────┴────────────┴──────────────┴────┐     │
│  │              Docker Compose / K8s                 │     │
│  └──────────────────────┬───────────────────────────┘     │
│                         │                                  │
│  ┌──────────────────────┴───────────────────────────┐     │
│  │              基础设施层                            │     │
│  │  PostgreSQL  Redis  MinIO/S3  NATS(Phase 2)      │     │
│  └──────────────────────────────────────────────────┘     │
│                                                             │
│  ┌──────────────────────────────────────────────────┐     │
│  │              CI/CD                                │     │
│  │  GitHub Actions / GitLab CI                       │     │
│  └──────────────────────────────────────────────────┘     │
│                                                             │
│  ┌──────────────────────────────────────────────────┐     │
│  │              监控告警                              │     │
│  │  Prometheus + Grafana + Loki                      │     │
│  └──────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────┘
```

**不做的事：**
- 不包含业务代码 — 只有配置文件和脚本
- 不做应用层逻辑 — 应用配置在各子项目自己的 config/ 里

### 1.2 与其他子项目的关系

| 子项目 | infra 提供什么 |
|--------|---------------|
| gate (Rust) | Dockerfile、K8s Deployment、健康检查 |
| platform (Rust) | Dockerfile、数据库迁移、MinIO 配置 |
| flow (Python) | Dockerfile、Python 依赖管理 |
| runtime (Python) | Dockerfile、沙箱配置 |
| chat (Vue 3) | Dockerfile（Nginx 静态托管）、构建配置 |
| sdk (Rust) | CI 构建（WASM 编译）、npm 发布 |
| sync (Rust) | CI 构建、跨平台二进制发布 |

---

## 二、模块划分

```
quantalithos-infra/
│
├── docker/                          # Docker 相关
│   ├── docker-compose.yaml          # 本地开发一键启动
│   ├── docker-compose.prod.yaml     # 生产环境
│   │
│   ├── gate/
│   │   └── Dockerfile               # gate 镜像
│   ├── platform/
│   │   └── Dockerfile               # platform 镜像
│   ├── flow/
│   │   └── Dockerfile               # flow 镜像
│   ├── runtime/
│   │   └── Dockerfile               # runtime 镜像
│   └── chat/
│       ├── Dockerfile               # chat 镜像（Nginx + 静态文件）
│       └── nginx.conf               # Nginx 配置
│
├── k8s/                             # Kubernetes 配置（Phase 2）
│   ├── namespace.yaml
│   ├── gate/
│   │   ├── deployment.yaml
│   │   ├── service.yaml
│   │   └── ingress.yaml
│   ├── platform/
│   │   ├── deployment.yaml
│   │   ├── service.yaml
│   │   └── hpa.yaml                 # 自动扩缩容
│   ├── flow/
│   │   ├── deployment.yaml
│   │   └── service.yaml
│   ├── runtime/
│   │   ├── deployment.yaml
│   │   └── service.yaml
│   └── infra/
│       ├── postgres.yaml            # PostgreSQL StatefulSet
│       ├── redis.yaml               # Redis Deployment
│       └── minio.yaml               # MinIO Deployment
│
├── db/                              # 数据库管理
│   ├── migrations/                  # SQL 迁移文件
│   │   ├── 001_create_projects.sql
│   │   ├── 002_create_workitems.sql
│   │   ├── 003_create_artifacts.sql
│   │   ├── 004_create_agent_config.sql
│   │   └── 005_create_snapshots.sql
│   ├── seeds/                       # 初始数据
│   │   ├── system_roles.sql         # 系统预置角色
│   │   ├── shared_rules.sql         # 系统共享规则
│   │   └── pipeline_templates.sql   # 标准流程模板
│   └── migrate.sh                   # 迁移脚本
│
├── ci/                              # CI/CD 配置
│   ├── github-actions/
│   │   ├── rust-build.yaml          # Rust 项目构建（gate/platform/sync/sdk）
│   │   ├── python-build.yaml        # Python 项目构建（flow/runtime）
│   │   ├── chat-build.yaml          # Vue 3 构建 + WASM 编译
│   │   ├── test.yaml                # 全项目测试
│   │   └── deploy.yaml              # 部署流水线
│   └── scripts/
│       ├── build-all.sh             # 本地全量构建
│       ├── build-wasm.sh            # sdk-wasm 编译
│       └── release-sync.sh          # sync CLI 跨平台发布
│
├── monitoring/                      # 监控告警（Phase 2）
│   ├── prometheus/
│   │   └── prometheus.yaml          # Prometheus 配置
│   ├── grafana/
│   │   ├── datasources.yaml
│   │   └── dashboards/
│   │       ├── overview.json        # 全局概览
│   │       ├── gate.json            # 网关指标
│   │       └── platform.json        # 平台指标
│   └── loki/
│       └── loki.yaml                # 日志收集配置
│
├── scripts/                         # 运维脚本
│   ├── setup-dev.sh                 # 开发环境一键搭建
│   ├── backup-db.sh                 # 数据库备份
│   ├── restore-db.sh               # 数据库恢复
│   └── health-check.sh             # 健康检查
│
└── envs/                            # 环境配置
    ├── .env.dev                     # 开发环境变量
    ├── .env.staging                 # 预发布环境变量
    └── .env.prod                    # 生产环境变量
```

---

## 三、核心配置

### 3.1 Docker Compose（本地开发）

```yaml
# docker/docker-compose.yaml

version: "3.8"

services:
  # ── 基础设施 ──
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: quantalithos
      POSTGRES_PASSWORD: dev_password
      POSTGRES_DB: quantalithos
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db/migrations:/docker-entrypoint-initdb.d

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  minio:
    image: minio/minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: quantalithos
      MINIO_ROOT_PASSWORD: dev_password
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data

  # ── 应用服务 ──
  gate:
    build:
      context: ../projects/quantalithos-gate
      dockerfile: ../infra/docker/gate/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - CONFIG_PATH=/app/config/gate.yaml
    depends_on:
      - platform
      - flow

  platform:
    build:
      context: ../projects/quantalithos-platform
      dockerfile: ../infra/docker/platform/Dockerfile
    ports:
      - "8001:8001"
    environment:
      - DATABASE_URL=postgres://quantalithos:dev_password@postgres:5432/quantalithos
      - MINIO_ENDPOINT=http://minio:9000
    depends_on:
      - postgres
      - redis
      - minio

  flow:
    build:
      context: ../projects/quantalithos-flow
      dockerfile: ../infra/docker/flow/Dockerfile
    ports:
      - "8002:8002"
    environment:
      - PLATFORM_URL=http://platform:8001
      - RUNTIME_URL=http://runtime:8003
    depends_on:
      - platform

  runtime:
    build:
      context: ../projects/quantalithos-runtime
      dockerfile: ../infra/docker/runtime/Dockerfile
    ports:
      - "8003:8003"
    environment:
      - PLATFORM_URL=http://platform:8001
      - FLOW_URL=http://flow:8002
      - LLM_API_KEY=${LLM_API_KEY}
    depends_on:
      - platform
      - flow

  chat:
    build:
      context: ../projects/quantalithos-chat
      dockerfile: ../infra/docker/chat/Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - gate

volumes:
  postgres_data:
  minio_data:
```

### 3.2 环境变量

```bash
# envs/.env.dev

# 数据库
DATABASE_URL=postgres://quantalithos:dev_password@localhost:5432/quantalithos

# Redis
REDIS_URL=redis://localhost:6379

# MinIO
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=quantalithos
MINIO_SECRET_KEY=dev_password

# LLM
LLM_API_KEY=sk-your-api-key
LLM_MODEL=gpt-4
LLM_MAX_RETRIES=3

# Gate
GATE_JWT_SECRET=dev-jwt-secret
GATE_PORT=8000

# 服务地址
PLATFORM_URL=http://localhost:8001
FLOW_URL=http://localhost:8002
RUNTIME_URL=http://localhost:8003
GATE_URL=http://localhost:8000
```

### 3.3 开发环境一键搭建

```bash
#!/bin/bash
# scripts/setup-dev.sh

echo "=== Quantalithos 开发环境搭建 ==="

# 1. 检查依赖
echo "检查依赖..."
command -v docker >/dev/null 2>&1 || { echo "需要安装 Docker"; exit 1; }
command -v rustup >/dev/null 2>&1 || { echo "需要安装 Rust"; exit 1; }
command -v python3 >/dev/null 2>&1 || { echo "需要安装 Python 3"; exit 1; }
command -v node >/dev/null 2>&1 || { echo "需要安装 Node.js"; exit 1; }

# 2. 启动基础设施
echo "启动 PostgreSQL + Redis + MinIO..."
docker compose -f docker/docker-compose.yaml up -d postgres redis minio

# 3. 等待数据库就绪
echo "等待数据库就绪..."
until docker compose exec postgres pg_isready; do sleep 1; done

# 4. 执行数据库迁移
echo "执行数据库迁移..."
bash db/migrate.sh

# 5. 导入初始数据
echo "导入初始数据..."
docker compose exec -T postgres psql -U quantalithos < db/seeds/system_roles.sql
docker compose exec -T postgres psql -U quantalithos < db/seeds/shared_rules.sql
docker compose exec -T postgres psql -U quantalithos < db/seeds/pipeline_templates.sql

# 6. 编译 sdk-wasm
echo "编译 sdk-wasm..."
cd ../projects/quantalithos-sdk
wasm-pack build crates/sdk-wasm --target web

# 7. 安装 chat 依赖
echo "安装 chat 依赖..."
cd ../quantalithos-chat
npm install

echo "=== 开发环境就绪 ==="
echo "启动服务："
echo "  platform:  cd projects/quantalithos-platform && cargo run"
echo "  flow:      cd projects/quantalithos-flow && python -m quantalithos_flow"
echo "  runtime:   cd projects/quantalithos-runtime && python -m quantalithos_runtime"
echo "  gate:      cd projects/quantalithos-gate && cargo run"
echo "  chat:      cd projects/quantalithos-chat && npm run dev"
```

---

## 四、CI/CD

### 4.1 构建流水线

```
代码提交
    │
    ▼
┌─────────────────────────────────────────────────┐
│  CI 流水线                                       │
│                                                 │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │ Rust    │  │ Python  │  │ Vue 3   │        │
│  │ 构建    │  │ 构建    │  │ 构建    │        │
│  │         │  │         │  │         │        │
│  │ gate    │  │ flow    │  │ chat    │        │
│  │ platform│  │ runtime │  │ +wasm   │        │
│  │ sync    │  │         │  │         │        │
│  │ sdk     │  │         │  │         │        │
│  └────┬────┘  └────┬────┘  └────┬────┘        │
│       │            │            │              │
│       └────────────┼────────────┘              │
│                    │                            │
│              ┌─────▼─────┐                     │
│              │   测试     │                     │
│              │ 单元+集成  │                     │
│              └─────┬─────┘                     │
│                    │                            │
│              ┌─────▼─────┐                     │
│              │ Docker 镜像│                     │
│              │   构建     │                     │
│              └─────┬─────┘                     │
│                    │                            │
│              ┌─────▼─────┐                     │
│              │   部署     │                     │
│              │ staging →  │                     │
│              │ production │                     │
│              └───────────┘                     │
└─────────────────────────────────────────────────┘
```

### 4.2 服务端口分配

| 服务 | 端口 | 说明 |
|------|------|------|
| gate | 8000 | 外部唯一入口 |
| platform | 8001 | 平台数据服务 |
| flow | 8002 | 流程引擎 |
| runtime | 8003 | Agent 运行时 |
| chat (dev) | 5173 | Vite 开发服务器 |
| chat (prod) | 3000 | Nginx 静态托管 |
| PostgreSQL | 5432 | 数据库 |
| Redis | 6379 | 缓存/事件总线 |
| MinIO | 9000/9001 | 对象存储 |
| Prometheus | 9090 | 监控（Phase 2） |
| Grafana | 3001 | 监控面板（Phase 2） |

---

## 五、Phase 分期

### Phase 1

| 功能 | 说明 |
|------|------|
| Docker Compose | 本地开发一键启动 |
| Dockerfile × 5 | gate/platform/flow/runtime/chat |
| 数据库迁移 | SQL 迁移脚本 |
| 初始数据 | 系统角色、共享规则、流程模板 |
| setup-dev.sh | 开发环境一键搭建 |
| .env 配置 | 开发/生产环境变量 |
| CI 基础 | 构建 + 测试 |

### Phase 2

| 功能 | 说明 |
|------|------|
| K8s 配置 | Deployment/Service/Ingress |
| 监控告警 | Prometheus + Grafana + Loki |
| 自动扩缩容 | HPA（platform/runtime） |
| 数据库备份 | 定时备份脚本 |
| CI/CD 完整 | 自动部署到 staging/production |

### Phase 3

| 功能 | 说明 |
|------|------|
| Helm Chart | K8s 包管理 |
| 多环境管理 | dev/staging/prod 配置分离 |
| 蓝绿部署 | 零停机发布 |
| 日志聚合 | ELK / Loki 集中日志 |
| 安全扫描 | 镜像漏洞扫描、依赖审计 |

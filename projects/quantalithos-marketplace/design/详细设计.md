# quantalithos-marketplace 详细设计

> 资产市场 — 发布/搜索/下载/导入，集成到 platform hub-marketplace 模块

---

## 一、模块架构

```
console 市场页面
      │
      │ sdk-api
      ▼
gate → platform hub-marketplace
            │
            ├── assets/          资产 CRUD + 版本管理
            ├── search/          搜索（标签 + 全文）
            ├── review/          评分 + 评论
            ├── download/        下载 + 统计
            └── import/          一键导入到项目
                  │
                  │ 根据 asset_type 分发
                  ▼
            hub-agent-config     （导入 process_template / role / rule）
            hub-design           （导入 component）
            MinIO                （下载 app 构建产物）
```

---

## 二、Rust 数据模型

```rust
// hub-marketplace/src/model.rs
//
// 资产市场数据模型 — 6 种资产类型的统一存储。
//
// 设计意图：
//   所有资产类型（流程模板/角色定义/共享规则/组件/技能包/应用）共用一张表，
//   通过 asset_type 字段区分。这样搜索和排行榜可以跨类型聚合。
//
// content_data vs file_path（互斥）：
//   结构化资产（process_template/role_definition/shared_rule/component）→ content_data（JSONB）
//   文件类资产（app）→ file_path（指向 MinIO/S3）
//   两者不会同时有值。
//
// status 生命周期：
//   draft → published（作者发布）
//   published → unlisted（作者下架，已安装的不受影响）
//   published → removed（管理员删除，违规内容）

#[derive(Debug, Clone, Serialize, Deserialize, sqlx::FromRow)]
pub struct MarketplaceAsset {
    pub id: i64,                         // 自增主键
    pub asset_type: String,              // 资产类型 — 合法值：
                                         //   "process_template" — 流程模板（BPMN 定义）
                                         //   "role_definition" — 角色定义（identity + tools + constraints）
                                         //   "shared_rule" — 共享规则（行为规范）
                                         //   "skill" — 技能包（prompt + 工具组合）
                                         //   "component" — UI 组件（template + styles + props）
                                         //   "app" — 应用作品（构建产物，Docker 镜像/静态文件）
    pub name: String,                    // 资产名称（如 "TDD 工作流"）
    pub description: String,             // 详细描述（Markdown）
    pub author_id: String,               // 作者 ID（用户 ID 或 "system"）
    pub author_name: String,             // 作者显示名称
    pub version: String,                 // 当前版本号（如 "1.2.0"）
    pub tags: serde_json::Value,         // 标签列表（JSON 数组，如 ["tdd", "testing", "workflow"]）
    pub category: String,                // 分类（如 "development" / "testing" / "deployment"）
    pub license: String,                 // 许可证（如 "MIT" / "Apache-2.0" / "proprietary"）
    pub content_type: String,            // 内容格式："yaml" | "json" | "zip" | "docker_image"
    pub content_data: Option<serde_json::Value>,  // 结构化内容（process_template/role/rule/component）
    pub file_path: Option<String>,       // 文件路径（app 类型，指向 MinIO）
    pub file_size: Option<i64>,          // 文件大小（字节）
    pub readme: String,                  // README 内容（Markdown，展示在详情页）
    pub screenshots: serde_json::Value,  // 截图 URL 列表（JSON 数组）
    pub download_count: i32,             // 下载/导入次数 — 用于排行榜
    pub rating_avg: f64,                 // 平均评分（1.0–5.0）— 用于排序
    pub rating_count: i32,               // 评分人数
    pub status: String,                  // 状态：draft | published | unlisted | removed
    pub created_at: DateTime<Utc>,
    pub updated_at: DateTime<Utc>,
}

/// 资产版本历史 — 每次更新创建新版本，旧版本保留
#[derive(Debug, Clone, Serialize, Deserialize, sqlx::FromRow)]
pub struct MarketplaceVersion {
    pub id: i64,
    pub asset_id: i64,                   // 所属资产 ID
    pub version: String,                 // 版本号（如 "1.1.0"）
    pub changelog: String,               // 更新日志（Markdown）
    pub content_data: Option<serde_json::Value>,  // 该版本的内容快照
    pub file_path: Option<String>,       // 该版本的文件路径
    pub created_at: DateTime<Utc>,
}

/// 用户评价 — Phase 3 实现
#[derive(Debug, Clone, Serialize, Deserialize, sqlx::FromRow)]
pub struct MarketplaceReview {
    pub id: i64,
    pub asset_id: i64,                   // 所属资产 ID
    pub user_id: String,                 // 评价者 ID
    pub rating: i32,                     // 评分（1–5 星）
    pub comment: String,                 // 评论内容
    pub created_at: DateTime<Utc>,
}
```

---

## 三、API Handler 实现

### 3.1 搜索

```rust
// hub-marketplace/src/handler.rs
//
// 资产搜索 — 支持按类型/关键词/标签/分类筛选，按下载量/评分/时间排序。
//
// 搜索策略：
//   Phase 2：PostgreSQL ILIKE 模糊匹配（小数据量足够）
//   Phase 3：Meilisearch 全文搜索（大数据量 + 中文分词）
//
// tags @> 操作符：
//   PostgreSQL JSONB 包含操作符 — 检查 tags 数组是否包含指定标签。
//   如 tags = ["tdd", "testing"]，查询 tags @> '["tdd"]' → true。
//
// 分页限制（page_size.min(100)）：
//   防止客户端请求过大的页面导致数据库压力。最大 100 条/页。

#[derive(Deserialize)]
pub struct SearchQuery {
    pub asset_type: Option<String>,      // 按资产类型筛选
    pub q: Option<String>,               // 关键词搜索（匹配 name + description）
    pub tag: Option<String>,             // 按标签筛选
    pub category: Option<String>,        // 按分类筛选
    pub sort: Option<String>,            // 排序方式：downloads | rating | newest（默认 newest）
    pub page: Option<i32>,               // 页码（从 1 开始）
    pub page_size: Option<i32>,          // 每页条数（默认 20，最大 100）
}

pub async fn search_assets(
    State(state): State<AppState>,
    Query(q): Query<SearchQuery>,
) -> Result<Json<PaginatedResult<MarketplaceAsset>>, PlatformError> {
    let mut query = sqlx::QueryBuilder::new("SELECT * FROM marketplace_assets WHERE status = 'published'");

    if let Some(t) = &q.asset_type {
        query.push(" AND asset_type = ").push_bind(t);
    }
    if let Some(keyword) = &q.q {
        query.push(" AND (name ILIKE ").push_bind(format!("%{}%", keyword))
             .push(" OR description ILIKE ").push_bind(format!("%{}%", keyword)).push(")");
    }
    if let Some(tag) = &q.tag {
        query.push(" AND tags @> ").push_bind(serde_json::json!([tag]));
    }

    match q.sort.as_deref() {
        Some("downloads") => query.push(" ORDER BY download_count DESC"),
        Some("rating") => query.push(" ORDER BY rating_avg DESC"),
        _ => query.push(" ORDER BY created_at DESC"),
    };

    // 分页
    let page = q.page.unwrap_or(1);
    let size = q.page_size.unwrap_or(20).min(100);
    query.push(" LIMIT ").push_bind(size).push(" OFFSET ").push_bind((page - 1) * size);

    let assets = query.build_query_as::<MarketplaceAsset>()
        .fetch_all(&state.db).await?;

    Ok(Json(PaginatedResult { items: assets, page, page_size: size }))
}
```

### 3.2 一键导入

```rust
// hub-marketplace/src/handler.rs
//
// 一键导入 — 将市场资产导入到用户的项目中。
//
// 设计意图：
//   用户在市场浏览到感兴趣的资产后，点击"导入到项目"即可使用。
//   根据 asset_type 分发到不同的目标表：
//     process_template → hub-agent-config.process_templates
//     role_definition → hub-agent-config.role_definitions
//     shared_rule → hub-agent-config.shared_rules
//     component → hub-design.components
//     app → 返回下载链接（Runner 下载运行，不入库）
//
// 导入后的 scope：
//   导入的资产 scope="project"，只对目标项目生效，不影响其他项目。
//   用户可以在导入后修改（fork 语义）。
//
// increment_download 的调用时机：
//   导入成功后才计数（失败不计），确保统计准确。

pub async fn import_to_project(
    State(state): State<AppState>,
    Path((asset_id, project_id)): Path<(i64, String)>,
) -> Result<Json<ImportResult>, PlatformError> {
    let asset = state.marketplace_repo.get(asset_id).await?
        .ok_or(PlatformError::NotFound("Asset not found".into()))?;

    let result = match asset.asset_type.as_str() {
        "process_template" => {
            let data = asset.content_data.ok_or(PlatformError::ValidationError("No content".into()))?;
            state.agent_config_repo.insert_process_template(&project_id, "project", data).await?;
            ImportResult { imported_to: "process_templates".into(), success: true }
        },
        "role_definition" => {
            let data = asset.content_data.ok_or(PlatformError::ValidationError("No content".into()))?;
            state.agent_config_repo.insert_role(&project_id, "project", data).await?;
            ImportResult { imported_to: "role_definitions".into(), success: true }
        },
        "shared_rule" => {
            let data = asset.content_data.ok_or(PlatformError::ValidationError("No content".into()))?;
            state.agent_config_repo.insert_rule(&project_id, "project", data).await?;
            ImportResult { imported_to: "shared_rules".into(), success: true }
        },
        "component" => {
            let data = asset.content_data.ok_or(PlatformError::ValidationError("No content".into()))?;
            state.design_repo.insert_component(&project_id, "project", data).await?;
            ImportResult { imported_to: "components".into(), success: true }
        },
        "app" => {
            // app 类型返回下载链接，不导入数据库
            ImportResult { imported_to: "download".into(), success: true }
        },
        _ => return Err(PlatformError::ValidationError(format!("Unknown asset_type: {}", asset.asset_type))),
    };

    // 记录下载统计
    state.marketplace_repo.increment_download(asset_id).await?;

    Ok(Json(result))
}
```

---

## 四、Phase 分期

### Phase 2

| 模块 | 范围 |
|------|------|
| model | MarketplaceAsset + MarketplaceVersion |
| handler | 发布 / 搜索 / 下载 / 导入 |
| 资产类型 | process_template + role_definition + shared_rule |

### Phase 3

| 模块 | 范围 |
|------|------|
| model | MarketplaceReview + MarketplaceFavorite |
| handler | 评分 / 评论 / 收藏 / 排行榜 |
| 资产类型 | skill + component + app |
| 审核 | 发布审核流程 |

---

## 五、相关文档

- 概要设计：`projects/quantalithos-marketplace/design/概要设计.md`
- platform hub-marketplace：`projects/quantalithos-platform/design/概要设计.md`
- console 市场页面：`projects/quantalithos-console/design/详细设计.md`

---

## 六、并发与一致性

### 6.1 并发模型

hub-marketplace 作为 platform 的一个 crate，共享 platform 的 tokio 异步运行时和 PgPool 连接池。
多个 HTTP 请求并发处理，无进程内可变状态（所有状态在 PostgreSQL 中）。

```
tokio 运行时（多线程）
  │
  ├── task: POST /marketplace/assets（发布者 A 发布新资产）
  ├── task: POST /marketplace/assets/42/versions（发布者 A 同时发布新版本）
  ├── task: GET /marketplace/assets?q=tdd（消费者 B 搜索）
  ├── task: POST /marketplace/assets/42/import/blog（消费者 C 导入）
  └── task: POST /marketplace/assets/42/reviews（消费者 D 评分）

共享资源：
  - PgPool（连接池，tokio 安全）
  - EventBus（broadcast channel，tokio 安全）
  - MinIO Client（HTTP 客户端，可并发）
```

### 6.2 竞态场景分析

| # | 竞态场景 | 共享资源 | 竞态类型 | 控制策略 |
|---|---------|---------|---------|---------|
| 1 | 两人同时发布同一资产的新版本 | marketplace_versions | 写-写 | UNIQUE(asset_id, version) 约束，后者收到 409 |
| 2 | 发布新版本与下载当前版本并发 | marketplace_assets.version | 读-写 | 下载读取时的版本是一致的（MVCC 快照隔离） |
| 3 | 同一用户重复评分 | marketplace_reviews | 写-写 | UNIQUE(asset_id, user_id) 约束 |
| 4 | 多人同时导入同一资产 | download_count | 写-写 | 原子递增（UPDATE ... SET download_count = download_count + 1） |
| 5 | 发布者下架资产时有人正在导入 | marketplace_assets.status | 读-写 | 导入时检查 status='published'，下架后新导入返回 404 |

### 6.3 事务边界

```
发布新版本的事务边界：

  BEGIN（PostgreSQL 事务）
    1. SELECT asset FOR UPDATE                ← 行锁，防止并发修改
    2. 校验 version 不重复                     ← UNIQUE 约束兜底
    3. INSERT marketplace_versions             ← 写入版本记录
    4. UPDATE marketplace_assets SET version=  ← 更新主表当前版本
  COMMIT

  事务外：
    5. event_bus.publish(AssetVersionPublished)

导入的事务边界：

  BEGIN
    1. SELECT asset WHERE status='published'   ← 确认资产可用
    2. 根据 asset_type 调用目标模块 API         ← 跨模块调用（独立事务）
  COMMIT

  事务外：
    3. UPDATE download_count = download_count + 1  ← 原子递增（独立语句）
    4. event_bus.publish(AssetDownloaded)

  注意：导入和计数分离 able 导入失败不计数，导入成功才递增。
```

---

## 七、性能设计

### 7.1 搜索性能

```
Phase 2（PostgreSQL ILIKE）：

  目标：P95 < 200ms（资产量 < 500）

  优化手段：
  ┌─────────────────────────────────────────────────────┐
  │  1. 索引覆盖                                         │
  │     idx_ma_type     → asset_type 筛选               │
  │     idx_ma_category → category 筛选                 │
  │     idx_ma_status   → status='published' 过滤       │
  │                                                     │
  │  2. GIN 索引（pg_trgm）                              │
  │     CREATE INDEX idx_ma_name_trgm                   │
  │       ON marketplace_assets                         │
  │       USING GIN (name gin_trgm_ops);                │
  │     → ILIKE '%keyword%' 走索引而非全表扫描           │
  │                                                     │
  │  3. JSONB 标签索引                                   │
  │     CREATE INDEX idx_ma_tags                        │
  │       ON marketplace_assets                         │
  │       USING GIN (tags);                             │
  │     → tags @> '["tdd"]' 走 GIN 索引                 │
  │                                                     │
  │  4. 分页限制                                         │
  │     page_size.min(100) — 防止大页面查询              │
  └─────────────────────────────────────────────────────┘

Phase 3（Meilisearch）：

  目标：P95 < 50ms（资产量 < 5000）

  架构：
    PostgreSQL（主存储）──同步──► Meilisearch（搜索索引）

  同步策略：
    - 资产发布/更新/下架时，同步写入 Meilisearch
    - 全量重建：定时任务每日凌晨全量同步
    - 降级：Meilisearch 不可用时回退到 ILIKE
```

### 7.2 下载吞吐

```
文件类资产（app）下载流程：

  消费者 → GET /marketplace/assets/42/download
              │
              ▼
  hub-marketplace：
    1. 查询 asset（确认 status=published）
    2. 生成 MinIO 预签名 URL（有效期 10 分钟）
    3. 返回 302 重定向到预签名 URL
              │
              ▼
  消费者 → 直接从 MinIO 下载（不经过 platform）

  优势：
    - platform 不做文件代理，无带宽瓶颈
    - MinIO 原生支持断点续传
    - 预签名 URL 有时效性，防止链接泄露

结构化资产（JSONB）下载流程：

  消费者 → GET /marketplace/assets/42/download
              │
              ▼
  hub-marketplace：
    1. 查询 asset（确认 status=published）
    2. 读取 content_data（JSONB）
    3. 返回 JSON 响应（Content-Type: application/json）

  性能：直接从 PostgreSQL 读取，P95 < 50ms
```

### 7.3 排行榜性能

```
Phase 2（实时查询）：

  SELECT * FROM marketplace_assets
  WHERE status = 'published'
    AND asset_type = $1
  ORDER BY download_count DESC
  LIMIT 20;

  性能：资产量 < 500 时，< 10ms

Phase 3（Redis 缓存）：

  缓存 key：marketplace:ranking:{type}:{period}
  TTL：5 分钟
  更新策略：查询时检查缓存，miss 则查库并写入
```

---

## 八、测试设计

### 8.1 核心测试场景

| # | 场景 | 测试类型 | 验证点 |
|---|------|---------|--------|
| 1 | 发布资产（6 种类型） | 单元测试 | 各类型的 content_data/file_path 互斥校验 |
| 2 | 搜索（关键词 + 标签 + 分类 + 排序） | 集成测试 | 搜索结果正确性 + 分页 + 排序 |
| 3 | 发布新版本 | 单元测试 | 版本号唯一 + 主表 version 更新 |
| 4 | 一键导入（process_template） | 集成测试 | 导入到 hub-agent-config + download_count 递增 |
| 5 | 一键导入（component） | 集成测试 | 导入到 hub-design + download_count 递增 |
| 6 | 一键导入（app） | 集成测试 | 返回下载链接 + download_count 递增 |
| 7 | 评分（一人一票） | 单元测试 | 重复评分返回 409 + rating_avg 正确计算 |
| 8 | 下架资产 | 单元测试 | status 变为 unlisted + 已导入的不受影响 |
| 9 | 并发发布新版本 | 并发测试 | 一个成功一个 409 |
| 10 | 并发导入同一资产 | 并发测试 | 两个都成功 + download_count = 2 |

### 8.2 测试框架

```
单元测试：
  - 框架：Rust 内置 #[cfg(test)] + sqlx::test
  - 数据库：sqlx::test 宏自动创建临时 PostgreSQL 数据库
  - Mock：hub-agent-config / hub-design 的 Repo trait mock

集成测试：
  - 框架：axum::test（HTTP 请求模拟）
  - 数据库：共享测试数据库（每个测试用事务隔离）
  - MinIO：测试用 MinIO 容器（docker-compose）

测试目录结构：
  hub-marketplace/
  ├── src/
  │   ├── handler.rs
  │   ├── model.rs
  │   └── repo.rs
  └── tests/
      ├── publish_test.rs        # 发布资产测试
      ├── search_test.rs         # 搜索测试
      ├── import_test.rs         # 导入测试
      ├── version_test.rs        # 版本管理测试
      ├── review_test.rs         # 评分评论测试
      └── concurrent_test.rs     # 并发测试
```


---

## 九、部署与运维

### 9.1 配置项五要素

| 配置项 | 默认值 | 环境变量 | 说明 | 变更影响 |
|--------|--------|---------|------|---------|
| minio_endpoint | http://localhost:9000 | MINIO_ENDPOINT | MinIO 服务地址 | 文件上传/下载不可用 |
| minio_bucket | marketplace | MINIO_MARKETPLACE_BUCKET | 资产文件存储桶 | 需要手动创建桶 |
| minio_access_key | — | MINIO_ACCESS_KEY | MinIO 访问密钥 | 文件操作鉴权失败 |
| minio_secret_key | — | MINIO_SECRET_KEY | MinIO 密钥 | 文件操作鉴权失败 |
| search_backend | postgres | MARKETPLACE_SEARCH_BACKEND | 搜索后端（postgres/meilisearch） | 搜索行为变化 |
| meilisearch_url | http://localhost:7700 | MEILISEARCH_URL | Meilisearch 地址（Phase 3） | 全文搜索不可用 |
| meilisearch_key | — | MEILISEARCH_KEY | Meilisearch API Key（Phase 3） | 搜索鉴权失败 |
| upload_max_size | 52428800 | MARKETPLACE_UPLOAD_MAX_SIZE | 最大上传文件大小（默认 50MB） | 大文件上传被拒绝 |
| presign_ttl | 600 | MARKETPLACE_PRESIGN_TTL | 预签名 URL 有效期（秒，默认 10min） | 下载链接过期时间变化 |

### 9.2 健康检查

```
GET /health/marketplace

检查项：
  1. PostgreSQL 连接 — SELECT 1
  2. MinIO 连接 — HEAD bucket（检查桶是否存在）
  3. Meilisearch 连接（Phase 3）— GET /health

响应：
  200 { "status": "healthy", "postgres": "ok", "minio": "ok" }
  503 { "status": "degraded", "postgres": "ok", "minio": "error" }
```

### 9.3 数据备份

| 数据 | 备份策略 | 恢复方式 |
|------|---------|---------|
| marketplace_* 表 | 随 platform PostgreSQL 一起备份（pg_dump） | pg_restore |
| MinIO 文件 | MinIO 自带版本控制 + 定期 mc mirror 到备份存储 | mc mirror 恢复 |
| Meilisearch 索引（Phase 3） | 不备份（可从 PostgreSQL 全量重建） | 全量同步脚本 |


---

## 十、幂等设计

### 10.1 发布资产幂等

```
场景：用户点击"发布"按钮，网络超时后重试。

幂等策略：
  - 客户端生成 idempotency_key（UUID），放在请求头 X-Idempotency-Key
  - 服务端在 marketplace_assets 表增加 idempotency_key 列（UNIQUE）
  - 第一次请求：INSERT 成功，返回 201
  - 重试请求：INSERT 违反 UNIQUE → 查询已有记录 → 返回 200 + 已有资产

流程：
  POST /marketplace/assets
  Header: X-Idempotency-Key: uuid-abc-123

  服务端：
    1. 查询 idempotency_key = 'uuid-abc-123'
       → 存在 → 返回 200 + 已有资产（幂等）
       → 不存在 ↓
    2. INSERT marketplace_assets (idempotency_key = 'uuid-abc-123', ...)
       → 成功 → 返回 201
       → UNIQUE 冲突 → 查询已有记录 → 返回 200（并发竞争兜底）
```

### 10.2 下载/导入计数幂等

```
场景：导入请求超时重试，download_count 不应重复递增。

幂等策略：
  - 记录下载日志表 marketplace_downloads（asset_id, user_id, project_id, created_at）
  - 导入前检查：同一 user_id + project_id + asset_id 在 5 分钟内是否已有记录
  - 有记录 → 跳过计数，直接执行导入（导入本身是幂等的，fork 语义）
  - 无记录 → INSERT 下载日志 + INCREMENT download_count

  注意：导入操作本身是幂等的 — 重复导入同一资产到同一项目，
  会覆盖已有的导入记录（UPSERT 语义），不会产生重复数据。
```

### 10.3 评分幂等

```
场景：用户提交评分，网络超时后重试。

幂等策略：
  - UNIQUE(asset_id, user_id) 约束保证一人一票
  - 重复提交 → INSERT ON CONFLICT UPDATE（更新评分和评论）
  - 更新后重新计算 rating_avg：
    UPDATE marketplace_assets SET
      rating_avg = (SELECT AVG(rating) FROM marketplace_reviews WHERE asset_id = $1),
      rating_count = (SELECT COUNT(*) FROM marketplace_reviews WHERE asset_id = $1)
    WHERE id = $1;
```

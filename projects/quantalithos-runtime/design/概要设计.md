# Agent Runtime 概要设计

> AI 驱动研发协作平台 - Agent 运行时核心模块设计文档

---

## 一、项目定位

### 1.1 核心职责

Agent Runtime 是整个 AI 研发协作平台的**核心引擎**，负责：

- **Agent 生命周期管理**：实例化、销毁、持久化
- **Agent 角色定义**：Prompt 管理、工具权限、行为约束
- **LLM 调用封装**：统一接口、流式输出、成本追踪
- **工具调用系统**：工具注册、执行、沙箱隔离
- **多 Agent 协作**：消息路由、群聊管理
- **状态机管理**：状态转换、事件驱动

**技术选型：** 当前阶段（Phase 1）使用纯 Python 实现，快速验证设计。设计验证完成后（Phase 2）逐步将框架骨架（进程管理、消息总线、权限引擎、沙箱）下沉到 Rust，Python 保留业务模块（LLM 调用、prompt 组装、记忆、工具、技能）。详见 `技术选型.md`。

### 1.2 在整体架构中的位置

```
quantalithos-chat (聊天前端)
      │
      │ HTTP/WebSocket
      ▼
quantalithos-gate (API 网关)
      │
      ├──────────────────┬──────────────────┐
      │                  │                  │
      ▼                  ▼                  ▼
quantalithos-flow       quantalithos-vault     ╔═════════════════╗
(流程引擎/daemon)   (产物服务)        ║ quantalithos-runtime  ║
      │                  │           ║ (Agent 运行时)    ║
      │                  │           ╚═════════════════╝
      └──────────────────┴────────────────────┘
                              │
                     ┌────────┴────────┐
                     │                 │
                     ▼                 ▼
              quantalithos-sync       quantalithos-infra
              (工作区同步)        (基础设施)
```

### 1.3 与 quantalithos-flow 的分工

quantalithos-runtime 负责 step 内部的 think → act 循环（Agent 执行层），宏观编排由 quantalithos-flow（daemon）控制：

- **quantalithos-flow（daemon）**：阶段状态机、工作流模板解析、step 调度、门禁校验、交接包管理
- **quantalithos-runtime**：Agent 生命周期、LLM 调用、工具执行、记忆管理、角色定义

Agent 不需要理解完整工作流，daemon 在每个 step 触发时注入最小必要上下文（当前任务、完成条件、完成后动作）。

### 1.4 与 quantalithos-sync 的衔接

quantalithos-sync 是 Rust 实现的 CLI 工具，负责项目工作区的初始化、恢复与双向同步。quantalithos-runtime 不直接管理配置文件的跨机器同步，而是从 quantalithos-sync 拉取到本地的目录结构中读取配置。

**衔接关系：**

- quantalithos-sync 负责将角色定义、共享规则、快照、任务卡、文档等从云端同步到本地项目目录
- quantalithos-runtime 从本地目录加载配置，不关心配置来源是本地编辑还是 quantalithos-sync 拉取
- quantalithos-flow (daemon) 在项目启动时调用 quantalithos-sync restore，确保本地工作区就绪后再启动 Agent

**本地项目目录结构（由 quantalithos-sync 管理）：**

```
project_files/{project}/
├── agents/                      # Agent 配置 → 云端：AI 编排与知识平台
│   ├── shared_rules/            # 项目级共享规则覆盖
│   │   └── tdd.yaml             # 如：本项目关闭 TDD
│   ├── role_store/              # 项目级角色定义覆盖
│   │   └── backend_dev.yaml     # 如：本项目后端用 Rust
│   └── manifest.yaml            # 项目配置清单（quantalithos-sync 驱动文件）
│
├── docs/                        # 项目文档 → 云端：研发协作平台 / 文档库
│   ├── prd/                     # 需求文档
│   ├── design/                  # 设计文档
│   └── meeting_notes/           # 会议纪要
│
├── tasks/                       # 任务卡 → 云端：研发协作平台 / 任务库
│   ├── TASK-001.md
│   └── BUG-001.md
│
├── snapshots/                   # 快照 → 云端：AI 编排与知识平台 / 快照库
│   ├── agents/                  # Agent 运行状态快照（挂起时生成）
│   └── workflow/                # 流程状态快照（阶段切换时生成）
│
└── workspace/                   # 工作区 → 云端：工程执行平台 / 代码库 (Git)
    ├── src/                     # 源代码
    ├── tests/                   # 测试
    ├── scripts/                 # 脚本
    └── ...                      # 其他工程文件（Cargo.toml、package.json 等）
```

**目录与云端库的映射及同步策略：**

| 本地目录 | 云端库 | 同步策略 |
|----------|--------|----------|
| `agents/` | AI 编排与知识平台 | 双向同步（配置变更） |
| `docs/` | 研发协作平台 / 文档库 | 双向同步（文档更新） |
| `tasks/` | 研发协作平台 / 任务库 | 双向同步（状态流转） |
| `snapshots/` | AI 编排与知识平台 / 快照库 | push 为主（保存快照） |
| `workspace/` | 工程执行平台 / 代码库 | Git（不走 sync，直接 git clone/push） |

**agents/ 与 snapshots/ 的区别：**

- `agents/`：角色模板、规则、配置 — 项目创建时确定，偶尔修改，用于实例化 Agent
- `snapshots/`：运行状态存档 — Agent 挂起或阶段切换时生成，用于恢复 Agent 和流程

**quantalithos-runtime 的配置加载顺序（三级覆盖）：**

```
项目级（最高优先级）：  project_files/{project}/agents/shared_rules/
                       project_files/{project}/agents/role_store/
公司级：               company_templates/shared_rules/
                       company_templates/role_store/
系统级（最低优先级）：  quantalithos-runtime/shared_rules/
                       quantalithos-runtime/role_store/
```

quantalithos-sync 的详细设计见 `projects/quantalithos-sync/design/`。

---

## 二、模块划分

### 2.1 模块总览

```
quantalithos-runtime/
│
├── core/                    # 核心抽象层
│   ├── agent.py             # Agent 基类
│   ├── state.py             # 状态定义与状态机
│   ├── message.py           # 消息结构
│   ├── session.py           # 会话管理
│   └── event.py             # 事件系统
│
├── llm/                     # LLM 调用层
│   ├── client.py            # 统一客户端接口
│   ├── openai_client.py     # OpenAI 实现
│   ├── types.py             # 请求/响应类型
│   └── streaming.py         # 流式输出处理
│
├── memory/                  # 记忆系统
│   ├── base.py              # 记忆基类
│   ├── conversation.py      # 对话记忆
│   ├── working.py           # 工作记忆
│   ├── long_term.py         # 长期记忆
│   └── compression.py       # 记忆压缩
│
├── tools/                   # 工具系统
│   ├── base.py              # 工具基类与注册
│   ├── executor.py          # 工具执行器
│   ├── sandbox.py           # 沙箱执行环境
│   └── builtin/             # 内置工具
│       ├── file.py          # 文件操作
│       ├── code.py          # 代码执行
│       ├── search.py        # 搜索
│       └── git.py           # Git 操作
│
├── roles/                   # 角色加载与实例化
│   ├── base.py              # 角色基类
│   ├── loader.py            # 从 YAML 加载角色定义
│   ├── registry.py          # 角色注册表
│   └── factory.py           # 角色实例化工厂
│
├── shared_rules/            # 系统级共享规则（YAML）
│   ├── communication.yaml   # 沟通规则（schema + content）
│   ├── tdd.yaml             # TDD 规范（content）
│   ├── debugging.yaml       # 调试规范（content）
│   ├── verification.yaml    # 完成前验证（content）
│   └── review.yaml          # 代码审查规范（content）
│
├── role_store/              # 系统级角色定义（YAML）
│   ├── assistant.yaml       # 入口助手（Assistant，私人助理）
│   ├── tech_lead.yaml       # 项目负责人
│   ├── backend_dev.yaml     # 后端开发
│   ├── frontend_dev.yaml    # 前端开发
│   ├── tester.yaml          # 测试工程师
│   └── devops.yaml          # 运维工程师
│
└── runtime/                 # 运行时引擎
    ├── executor.py          # 执行器
    ├── router.py            # 消息路由
    ├── group.py             # 群聊管理
    ├── lifecycle.py         # Agent 生命周期
    └── context.py           # 运行时上下文
```

### 2.2 模块依赖关系

```
                    ┌─────────────┐
                    │   runtime   │  ← 顶层调度
                    └──────┬──────┘
                           │
           ┌───────────────┼───────────────┐
           │               │               │
           ▼               ▼               ▼
    ┌──────────┐    ┌──────────┐    ┌──────────┐
    │  roles   │    │  router  │    │ lifecycle│
    └────┬─────┘    └──────────┘    └──────────┘
         │
         ▼
    ┌──────────┐
    │ prompts  │  ← Prompt 组装
    └────┬─────┘
         │
    ┌────┴─────┬─────────────┐
    │          │             │
    ▼          ▼             ▼
┌───────┐ ┌─────────┐ ┌─────────┐
│ core  │ │  tools  │ │  memory │
└───┬───┘ └────┬────┘ └────┬────┘
    │          │           │
    └──────────┴───────────┘
               │
               ▼
          ┌─────────┐
          │   llm   │  ← 底层调用
          └─────────┘
```

---

## 三、核心模块详细设计

### 3.1 core - 核心抽象层

#### 3.1.1 Agent 基类

```python
class Agent(ABC):
    """Agent 基类，所有角色继承此类"""
    
    # 核心属性
    agent_id: str              # 唯一标识
    role_id: str               # 角色定义 ID
    state: AgentState          # 当前状态
    config: AgentConfig        # 配置信息
    
    # 组件
    memory: Memory             # 记忆系统
    tools: List[Tool]          # 可用工具
    llm_client: LLMClient      # LLM 客户端
    
    # 核心方法
    async def process_message(message: Message) -> Message
    async def think() -> Optional[ToolCall]
    async def act(tool_call: ToolCall) -> ToolResult
```

#### 3.1.2 状态机

quantalithos-runtime 只维护 Agent 执行级别的状态机（think→act 循环）。项目级阶段状态机和任务级状态机由 quantalithos-flow (daemon) 负责。

**三层状态机归属：**

| 状态机 | 归属 | 说明 |
|--------|------|------|
| 项目级阶段状态机（6 阶段） | quantalithos-flow | daemon 控制阶段流转、门禁、回退 |
| 任务级状态机 | quantalithos-flow | daemon 驱动任务状态流转（created→assigned→in_development→...） |
| Agent 执行状态机 | quantalithos-runtime | Agent 内部 think→act 循环 |

quantalithos-runtime 对任务状态的关系是**只读消费 + 事件上报**：
- 读取：从任务卡文件读取当前任务状态，作为执行上下文的一部分
- 上报：Agent 完成 step 后向 quantalithos-flow 发送事件（如 `DELIVERABLE`、`BLOCKED`），由 quantalithos-flow 决定是否推进任务状态

**Agent 执行状态机：**

```
         ┌─────────┐
         │  idle   │ ← 初始状态
         └────┬────┘
              │ 接收消息
              ▼
         ┌─────────┐
         │ thinking│ ← 调用 LLM 思考
         └────┬────┘
              │ 决定行动
              ▼
         ┌─────────┐
         │ acting  │ ← 执行工具/发送消息
         └────┬────┘
              │ 需要等待
              ▼
         ┌─────────┐
         │ waiting │ ← 等待用户/其他Agent响应
         └────┬────┘
              │ 收到响应
              ├──────────────┐
              │              │
              ▼              ▼
         ┌─────────┐   ┌─────────┐
         │ thinking│   │completed│
         └─────────┘   └─────────┘
```

#### 3.1.3 消息结构

```python
class Message(BaseModel):
    """消息结构 — runtime 内部的 LLM 对话消息"""
    message_id: str                       # 消息 ID
    role: str                             # LLM API 要求："user" | "assistant" | "system" | "tool"
    content: str                          # 消息正文
    sender_agent_id: Optional[str]        # 发送者 Agent ID
    target_agent_id: Optional[str]        # 目标 Agent ID
    metadata: dict = {}                   # 扩展字段（reply_type、output、verification 等由此承载）
    created_at: datetime                  # 时间戳
```

**与 quantalithos-flow 的接口结构：**

runtime 通过以下结构与 daemon 交互（正式定义在 quantalithos-flow 概要设计第五章）：

```python
# ── flow → runtime：两种 Assignment ──

class TaskAssignment(BaseModel):
    """任务执行 — 一个 Agent 独立完成（mode: task）"""
    assignment_id: str              # 本次分配的唯一 ID
    project_id: str
    step_id: str                    # 工作流中的 step（如 implement）
    agent_id: str                   # 执行者
    action: str                     # 任务描述（自然语言，注入 prompt）
    input_refs: List[str]           # 输入引用（平台工单 ID 等）
    allowed_tools: List[str]        # step 级工具权限（与角色级取交集）
    workflow: Optional[str]         # Agent 级工作流 ID（如 tdd_workflow），None 时自由执行
    output_schema: Optional[dict]   # 输出格式（用于动态注册 submit_step_result 工具）


class SessionAssignment(BaseModel):
    """协作会议 — 多人讨论（mode: session）"""
    assignment_id: str
    project_id: str
    step_id: str
    moderator_id: str               # 主持人（负责提交结论）
    participant_ids: List[str]      # 所有参与者（含主持人）
    agenda: str                     # 议程（注入所有参与者的 prompt）
    input_refs: List[str]
    allowed_tools: List[str]
    output_schema: Optional[dict]   # 会议结论格式（仅主持人可提交）


# ── runtime → flow：统一的 StepEvent ──

class StepEvent(BaseModel):
    """上报执行结果 — 不管是任务完成还是会议结束"""
    assignment_id: str              # 对应哪次分配（原样回传）
    agent_id: str                   # 提交者（task 是执行者，session 是主持人）
    event_type: str                 # completed | blocked | failed
    output: Optional[dict]          # 产出内容（submit_step_result 的参数）
    reason: Optional[str]           # blocked/failed 时的原因
```

**runtime 收到 Assignment 后的处理：**

```
收到 Assignment
    │
    ├── TaskAssignment（任务执行）
    │       │
    │       ▼
    │   1. 注入 action 到 Agent prompt
    │   2. allowed_tools 与角色级权限取交集
    │   3. 如果有 output_schema → 动态注册 submit_step_result 工具
    │   4. 如果有 workflow → 加载 Agent 级工作流模板
    │   5. Agent 执行 think→act 循环
    │   6. Agent 调用 submit_step_result → runtime 构造 StepEvent 上报
    │
    └── SessionAssignment（协作会议）
            │
            ▼
        1. 激活所有 participant Agent
        2. 注入 agenda 到每人 prompt
        3. 如果有 output_schema → 为 moderator 注册 submit_step_result
        4. 开启群聊讨论（消息经 flow 路由）
        5. moderator 调用 submit_step_result → runtime 构造 StepEvent 上报
```

**submit_step_result — 动态注册的完成工具：**

当 Assignment 携带 output_schema 时，runtime 为 Agent 动态注册一个 `submit_step_result` 工具，
其参数 schema 就是 output_schema。Agent 通过 LLM function calling 调用该工具提交结果，
格式由 LLM 原生保证（与 OpenAI Function Calling、LangChain Structured Output 机制一致）。

**这不是自己实现的校验逻辑，而是复用 LLM 原生的 function calling 能力：**

```
原理：和普通工具调用完全一样

你定义一个工具（如 get_weather），指定参数 JSON Schema：
    tools=[{
        "function": {
            "name": "get_weather",
            "parameters": {"type": "object", "properties": {"city": {"type": "string"}}}
        }
    }]

LLM 返回时自动按 schema 生成参数：
    tool_calls: [{ function: { name: "get_weather", arguments: '{"city": "北京"}' } }]

submit_step_result 完全一样，只是工具名固定、参数从 output_schema 动态生成。
```

**runtime 的处理流程：**

```
runtime 收到 TaskAssignment
    │
    │ 检查 output_schema 是否存在
    │
    ├── 有 output_schema
    │       │
    │       ▼
    │   动态生成 submit_step_result 工具：
    │   {
    │       "name": "submit_step_result",
    │       "description": "提交当前任务的执行结果",
    │       "parameters": output_schema    ← 直接用 output_schema 作为参数 schema
    │   }
    │       │
    │       ▼
    │   注册到 Agent 的工具列表中
    │   （和 file_read、code_execute 等工具并列）
    │       │
    │       ▼
    │   Agent 执行 think→act 循环...
    │   Agent 决定调用 submit_step_result
    │   LLM function calling 保证参数格式正确
    │       │
    │       ▼
    │   runtime 收到工具调用 → 构造 StepEvent → 上报给 flow
    │
    └── 无 output_schema
            │
            ▼
        不注册 submit_step_result
        Agent 自由执行，runtime 根据 Agent 的最终回复构造 StepEvent
```

```python
def register_submit_tool(output_schema: dict) -> Tool:
    """根据 output_schema 动态生成 submit_step_result 工具"""
    return Tool(
        name="submit_step_result",
        description="提交当前 step 的执行结果",
        parameters=output_schema,       # JSON Schema，LLM function calling 保证格式
    )
```

```
例：implement step 的 output_schema

output_schema = {
    "type": "object",
    "properties": {
        "summary": {"type": "string", "description": "交付摘要"},
        "changed_files": {"type": "array", "items": {"type": "string"}},
        "test_result": {"type": "string", "enum": ["passed", "failed"]}
    },
    "required": ["summary", "changed_files", "test_result"]
}

→ runtime 自动生成工具注册到 LLM：
  tools=[{
      "type": "function",
      "function": {
          "name": "submit_step_result",
          "description": "提交当前任务的执行结果",
          "parameters": output_schema       ← 直接透传
      }
  }]

→ Agent 写完代码后，LLM 决定调用这个工具：
  tool_calls: [{
      function: {
          name: "submit_step_result",
          arguments: '{
              "summary": "实现文章标签系统 CRUD",
              "changed_files": ["src/models/tag.rs", "src/routes/tags.rs"],
              "test_result": "passed"
          }'
      }
  }]

→ runtime 收到工具调用，构造 StepEvent 上报给 flow:
  StepEvent(
      assignment_id="assign-00042",
      agent_id="backend-dev-1",
      event_type="completed",
      output={"summary": "...", "changed_files": [...], "test_result": "passed"}
  )
```

> **设计决策记录：用 submit_step_result 替代 done_when + verification_command**
>
> 改动前：
> - `done_when: ["tests_passed", "deliverable_submitted"]` — 字符串列表，runtime 不知道怎么校验
> - `verification_command: "cargo test tests::tags"` — runtime 要自己跑命令判断是否通过
> - `verification: {...}` — StepEvent 里单独携带验证证据
>
> 问题：runtime 需要硬编码"tests_passed 意味着跑 verification_command 返回码为 0"这类映射逻辑。
> 每加一种 done_when 条件就要改 runtime 代码。
>
> 改动后：
> - `output_schema` — 定义 Agent 需要提交什么格式的结果
> - `submit_step_result` — 动态注册的工具，参数就是 output_schema
> - LLM function calling 原生保证格式正确，runtime 零校验逻辑
> - Agent 自己决定怎么验证（跑测试、检查文件等），结果通过工具参数提交
>
> verification_command 合并到 action 自然语言描述中（如"完成后请运行 cargo test tests::tags 确认通过"），
> 由 Agent 自行决定验证方式，不再由 runtime 强制执行。

**ask_teammate — 即时沟通工具：**

Agent 在执行 TaskAssignment 时可以通过 ask_teammate 工具向其他 Agent 提问：

```python
class AskTeammateTool(Tool):
    name = "ask_teammate"
    description = "向团队成员提问，等待回复"

    async def execute(self, target: str, question: str) -> str:
        """
        1. runtime 将问题发送给 flow
        2. flow 路由消息给目标 Agent
        3. 目标 Agent 回复
        4. flow 将回复路由回来
        5. 作为工具结果返回给调用者
        """
```

> **设计决策记录：StepAssignment 拆分为 TaskAssignment + SessionAssignment**
>
> 原设计中只有一个 StepAssignment，同时承载任务执行和群聊讨论。经讨论后拆分为两种：
> - TaskAssignment：一对一任务执行
> - SessionAssignment：多人协作会议
>
> 同时去掉了 done_when 和 verification_command 字段，改为 output_schema + submit_step_result 工具。
> Agent 级工作流（scope: agent）内部仍保留 done_when，由 AgentWorkflowEngine 在 runtime 内部判断。

**两种执行模式（与 Agent 级工作流的关系）：**

```
收到 TaskAssignment
    │
    ├── workflow = null（自由执行）
    │       │
    │       ▼
    │   注入 action 到 prompt
    │   注册 submit_step_result 工具
    │   Agent 自由 think→act 循环
    │   Agent 调用 submit_step_result → 上报 StepEvent
    │
    └── workflow = "tdd_workflow"（模板驱动）
            │
            ▼
        加载 workflow_templates/agent/tdd_workflow.yaml
        初始化所有 step 状态为 pending
            │
            ▼
        ┌─────────────────────────────────────┐
        │  Agent 级工作流引擎                   │
        │                                     │
        │  当前 step: read_task               │
        │    → 注入 step.action 到 prompt     │
        │    → Agent think→act               │
        │    → 检查 done_when（Agent 级保留） │
        │      ├── 满足 → 推进到 next step   │
        │      └── 不满足 → 继续循环          │
        │                                     │
        │  ...（按模板 step 顺序执行）         │
        │                                     │
        │  最后一个 step: submit              │
        │    → Agent 调用 submit_step_result  │
        │    → 上报 StepEvent: completed      │
        └─────────────────────────────────────┘
```

**回复格式校验：**

Agent 产出 `role: assistant` 的消息时，runtime 读取 shared_rules/communication.yaml 的 schema，校验 metadata 中的 reply_type 及其必填字段：

```python
# Agent 回复示例
Message(
    role="assistant",
    content="标签系统实现完成",
    metadata={
        "reply_type": "result",
        "output": "实现了标签 CRUD 和按标签筛选",
        "verification": "cargo test tests::tags — 5 passed, 0 failed",
    },
)

# runtime 校验逻辑：读 communication.yaml schema
# reply_type = "result" → 必须有 output 和 verification → 通过
```

> **设计变更记录：**
> 以下字段从 Message 中移除，原因和去向记录如下，后续如有需要可重新讨论：
>
> | 移除字段 | 原因 | 去向 |
> |----------|------|------|
> | `message_type` (MessageType 枚举) | 业务事件类型（TASK_ASSIGN、DELIVERABLE 等），runtime 无处理逻辑 | 由 TaskAssignment/SessionAssignment/StepEvent 在 flow 层承载 |
> | `reply_type` (一等字段) | 校验逻辑由 communication.yaml schema 驱动，不应硬编码在 Message 类中 | 降为 metadata 字段，校验逻辑不变 |
> | `task_id` | runtime 不消费任务 ID，任务关联由 daemon 维护 | 在 TaskAssignment/StepEvent 中透传 |
> | `mentions` | @路由和参与度控制归 quantalithos-flow | 由 daemon 在消息路由层处理 |
> | `output` / `verification` / `reason` / `help_needed` / `question` (一等字段) | 不同 reply_type 的必填字段不应硬编码在 Message 类中 | 统一放入 metadata，由 schema 校验 |

---

### 3.2 llm - LLM 调用层

#### 3.2.1 统一客户端接口

```python
class LLMClient(ABC):
    """LLM 客户端基类"""
    
    async def chat(
        messages: List[Message],
        model: str,
        temperature: float = 0.7,
        max_tokens: int = 4096,
        tools: Optional[List[Tool]] = None,
    ) -> ChatResponse:
        """发送聊天请求"""
        pass
    
    async def chat_stream(
        messages: List[Message],
        model: str,
        **kwargs
    ) -> AsyncIterator[ChatChunk]:
        """流式聊天"""
        pass
```

#### 3.2.2 关键特性

| 特性 | 说明 |
|------|------|
| 多模型支持 | OpenAI、Claude、本地模型 |
| 流式输出 | SSE 流式处理、增量拼接 |
| Token 追踪 | 请求/响应 token 统计 |
| 重试机制 | 超时、限流自动重试 |
| 成本追踪 | API 调用费用统计 |

---

### 3.3 memory - 记忆系统

#### 3.3.1 记忆架构

记忆系统分为两层，分阶段实现：

```
┌─────────────────────────────────────────────────────────┐
│                      记忆系统                            │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 对话记忆 (Conversation Memory)                   │   │
│  │ - 完整对话历史（含 daemon 注入的 step 上下文）    │   │
│  │ - 按会话/项目组织                                 │   │
│  │ - Phase 1：内存                                   │   │
│  │ - Phase 2：持久化到数据库（PostgreSQL）            │   │
│  └─────────────────────────────────────────────────┘   │
│                         │                               │
│                         ▼                               │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 长期记忆 (Long-term Memory)          [Phase 3]   │   │
│  │ - 向量存储（Chroma / Pinecone）                   │   │
│  │ - 语义检索：用当前任务描述检索历史经验             │   │
│  │ - 来源：从历史项目的对话记忆中提取沉淀             │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**设计决策：**

- 不设独立的"工作记忆"概念。daemon 注入的 step 上下文（当前任务、输入、完成条件）作为系统消息插入对话历史，LLM 自然理解"现在换任务了"。这与 Claude Code、Cursor、Superpowers 等主流工具的做法一致。
- 对话记忆是 Agent 的核心记忆，记录所有交互历史。Phase 1 在内存中，会话结束即丢失；Phase 2 持久化到数据库，支持 Agent 挂起/恢复。
- 长期记忆是跨项目经验积累，Phase 3 实现。从历史对话中提取经验片段，向量化后存入向量库，新任务开始时语义检索相关经验注入上下文。

**与快照系统的关系：**

```
Agent 被挂起时：
  对话记忆 ──→ 持久化到数据库（Phase 2）
  长期记忆 ──→ 已在向量库中（Phase 3）
  快照元数据 ──→ 写入 snapshots/agents/ 目录（YAML 文件）
                 记录 agent_id、role_id、state、conversation_ref、memory_ref

Agent 被恢复时：
  读取快照元数据 ──→ 从数据库恢复对话记忆
                     从向量库恢复长期记忆
                     daemon 重新注入当前 step 上下文
```

快照元数据是恢复的索引，不是记忆本身。它存在本地 `snapshots/` 目录，由 quantalithos-sync 同步到云端 AI 编排与知识平台的快照库。

**分阶段实现计划：**

| 阶段 | 内容 | 存储 |
|------|------|------|
| Phase 1（MVP） | 对话记忆（会话内） | 内存，会话结束丢失 |
| Phase 2 | 对话记忆持久化 + Agent 快照恢复 | PostgreSQL + snapshots/ YAML |
| Phase 3 | 长期记忆（跨项目经验） | 向量库（Chroma/Pinecone） |

#### 3.3.2 记忆压缩

当对话历史过长时，需要进行压缩：

```
原始对话（100条消息）
       │
       ▼
摘要压缩
       │
       ▼
压缩后：关键决策 + 最后 N 条完整消息
```

---

### 3.4 tools - 工具系统

#### 3.4.0 工具与技能的区别

**工具（Tool）**：原子操作能力，单一职责
- 例如：`file_read`、`git_commit`、`terminal_execute`
- 运行时注册，全局可用
- 权限控制：角色定义中的 `tools` 列表

**技能（Skill）**：工具组合 + 执行流程 + 领域知识
- 例如：`代码审查`、`故障诊断`、`部署流程`
- 可由助手动态创建（参考 Hermes 自进化）
- 权限控制：角色定义中的 `skills` 列表

```
工具层级结构：

┌─────────────────────────────────────────────────────────┐
│                        技能层                            │
│  code_review_skill = [git_diff, file_read, lint_check]  │
│  deploy_skill = [build, test, deploy, notify]           │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                        巀具层                            │
│  file_read | git_commit | terminal_execute | ...       │
└─────────────────────────────────────────────────────────┘
```

**技能示例：**

```yaml
# skills/code_review.yaml
skill_id: code_review
name: 代码审查技能
description: 自动化代码审查流程
steps:
  - tool: git_diff
    params: {target_branch: "main"}
  - tool: file_read
    params: {files: "${git_diff.output.changed_files}"}
  - tool: lint_check
    params: {files: "${git_diff.output.changed_files}"}
  - tool: generate_review_comment
    params: {context: "${previous_steps.output}"}
validation:
  - lint_check.output.passed == true
on_failure:
  - post_comment
  - notify_author
```

#### 3.4.1 工具基类

```python
class Tool(ABC):
    """工具基类"""
    
    tool_name: str             # 工具名称
    description: str           # 工具描述
    parameters: dict           # JSON Schema 参数定义
    
    async def execute(**kwargs) -> ToolResult:
        """执行工具"""
        pass
    
    def validate_parameters(**kwargs) -> bool:
        """校验参数"""
        pass
```

#### 3.4.2 工具权限控制

工具权限采用**两层控制**：角色级静态权限 + step 级动态权限，两者取交集。

**第一层：角色级静态权限**

由角色定义中的 `tools` 字段控制，定义该角色能力的最大边界：

```python
role_def = RoleDefinition(
    role_id="tech_lead",
    role_name="Tech Lead",
    tools=[
        "file_read",
        "file_write",
        "git_status",
        "git_commit",
        "search_docs",
        "create_task",
        "assign_task",
        "create_role"
    ]
)
```

**第二层：step 级动态权限**

由 quantalithos-flow (daemon) 在分配 step 时注入 `allowed_tools`，将角色能力收缩到当前 step 所需的最小集：

```python
# daemon 注入的 step 上下文
step_context = {
    "step_id": "brainstorm",
    "action": "从各自角度讨论方案和风险",
    "allowed_tools": ["file_read", "search_docs"],  # 讨论阶段只允许读取
}
```

**运行时校验逻辑：**

```python
class ToolExecutor:
    def can_execute(self, agent: Agent, tool_name: str, step_context: Optional[dict] = None) -> bool:
        """两层权限取交集"""
        # 第一层：角色级静态权限
        role_def = role_store.get(agent.role_id)
        if tool_name not in role_def.tools:
            return False
        
        # 第二层：step 级动态权限（如果 daemon 注入了 allowed_tools）
        if step_context and "allowed_tools" in step_context:
            if tool_name not in step_context["allowed_tools"]:
                return False
        
        return True
```

**两层权限的关系：**

```
最终可用工具 = 角色定义的 tools ∩ daemon 下发的 step.allowed_tools
```

- 角色级权限定义能力上限（后端开发永远不能 deploy）
- step 级权限收缩当前场景（编码阶段不能合并分支）
- 如果 daemon 未注入 allowed_tools，则退化为仅角色级权限
- step 级权限不能超出角色级权限（交集，不是并集）

#### 3.4.3 平台集成工具

Agent 操作任务卡、需求、缺陷等业务对象时，不直接读写本地文件，而是通过平台集成工具调用研发协作平台 API。本地 `tasks/` 目录只是 quantalithos-sync 同步下来的镜像缓存。

> 任务、需求、缺陷的主数据归研发协作平台（3+1 架构中的第一层），详细数据结构和 API 定义见研发协作平台设计文档。quantalithos-runtime 只定义 Agent 侧的工具接口。

```python
class TaskReadTool(Tool):
    """读取任务卡"""
    tool_name = "task_read"
    description = "从研发协作平台读取任务卡详情"
    parameters = {
        "type": "object",
        "properties": {
            "task_id": {"type": "string", "description": "任务 ID，如 TASK-003"}
        },
        "required": ["task_id"]
    }

    async def execute(self, task_id: str) -> ToolResult:
        return await collaboration_api.get_task(task_id)


class TaskUpdateTool(Tool):
    """更新任务卡字段"""
    tool_name = "task_update"
    description = "更新任务卡的指定字段（受权限控制）"
    parameters = {
        "type": "object",
        "properties": {
            "task_id": {"type": "string"},
            "fields": {"type": "object", "description": "要更新的字段"}
        },
        "required": ["task_id", "fields"]
    }

    async def execute(self, task_id: str, fields: dict) -> ToolResult:
        # 平台 API 负责权限校验和状态机校验
        return await collaboration_api.update_task(task_id, fields)


class TaskCreateTool(Tool):
    """创建任务卡或 BUG 卡"""
    tool_name = "task_create"
    description = "在研发协作平台创建新的任务或缺陷工单"
    parameters = {
        "type": "object",
        "properties": {
            "type": {"type": "string", "enum": ["dev", "fix", "test", "doc", "ops"]},
            "title": {"type": "string"},
            "description": {"type": "string"},
            "priority": {"type": "string", "enum": ["blocker", "high", "normal", "low"]},
            "related_task": {"type": "string", "description": "关联任务 ID（BUG 卡时填写）"}
        },
        "required": ["type", "title"]
    }

    async def execute(self, **kwargs) -> ToolResult:
        return await collaboration_api.create_task(**kwargs)


class TaskQueryTool(Tool):
    """查询任务列表"""
    tool_name = "task_query"
    description = "按条件查询任务列表"
    parameters = {
        "type": "object",
        "properties": {
            "status": {"type": "string"},
            "assignee": {"type": "string"},
            "priority": {"type": "string"},
            "type": {"type": "string"}
        }
    }

    async def execute(self, **kwargs) -> ToolResult:
        return await collaboration_api.query_tasks(**kwargs)
```

**任务卡操作的权限边界（由平台 API 校验）：**

| 角色 | task_read | task_create | task_update 可改字段 |
|------|-----------|-------------|---------------------|
| Tech Lead | 所有任务 | 任务卡、BUG 卡 | 所有字段 |
| 开发工程师 | 所有任务 | ❌ | 仅自己任务的交付物字段 |
| 测试工程师 | 所有任务 | 仅 BUG 卡 | ❌ |
| daemon | 所有任务 | ❌ | status、assignee（状态机驱动） |

#### 3.4.4 沙箱执行

代码执行工具必须在沙箱中运行：

```
Agent 请求执行代码
       │
       ▼
┌──────────────────┐
│    Sandbox       │
│  ┌────────────┐  │
│  │ 隔离环境    │  │  ← Docker / Firecracker
│  │ 执行代码    │  │
│  └────────────┘  │
│  资源限制         │
│  网络隔离         │
└──────────────────┘
       │
       ▼
返回执行结果
```

#### 3.4.5 角色管理工具

助手可以通过工具动态创建新角色：

```python
class CreateRoleTool(Tool):
    """让助手可以动态创建新角色"""
    
    tool_name = "create_role"
    description = "创建一个新的角色定义"
    parameters = {
        "type": "object",
        "properties": {
            "role_name": {
                "type": "string",
                "description": "角色显示名称"
            },
            "identity": {
                "type": "string",
                "description": "角色身份描述（自然语言）"
            },
            "constraints": {
                "type": "array",
                "items": {"type": "string"},
                "description": "行为约束列表"
            },
            "tools": {
                "type": "array",
                "items": {"type": "string"},
                "description": "可用工具列表"
            },
            "read_paths": {
                "type": "array",
                "items": {"type": "string"},
                "description": "可读路径"
            },
            "write_paths": {
                "type": "array",
                "items": {"type": "string"},
                "description": "可写路径"
            },
            "forbidden_paths": {
                "type": "array",
                "items": {"type": "string"},
                "description": "禁止访问路径"
            },
            "extends": {
                "type": "string",
                "description": "继承自哪个角色（可选）"
            }
        },
        "required": ["role_name", "identity", "tools"]
    }
    
    async def execute(self, **kwargs) -> ToolResult:
        """执行角色创建"""
        
        # 1. 创建角色定义
        role_def = RoleDefinition(
            role_id=generate_role_id(kwargs["role_name"]),
            role_name=kwargs["role_name"],
            identity=kwargs["identity"],
            constraints=kwargs.get("constraints", []),
            tools=kwargs["tools"],
            permissions=PermissionConfig(
                read_paths=kwargs.get("read_paths", []),
                write_paths=kwargs.get("write_paths", []),
                forbidden_paths=kwargs.get("forbidden_paths", [])
            ),
            created_by="assistant",
            extends=kwargs.get("extends")
        )
        
        # 2. 验证角色定义
        validation_result = self.validate_role_definition(role_def)
        if not validation_result.is_valid:
            return ToolResult(
                success=False,
                error=f"角色定义验证失败: {validation_result.error}"
            )
        
        # 3. 存储到角色库
        role_store.save(role_def)
        
        # 4. 注册到运行时
        agent_runtime.register_role(role_def)
        
        return ToolResult(
            success=True,
            data={
                "role_id": role_def.role_id,
                "message": f"角色 {role_def.role_name} 创建成功"
            }
        )
    
    def validate_role_definition(self, role_def: RoleDefinition) -> ValidationResult:
        """验证角色定义的合法性"""
        
        # 检查 role_id 是否已存在
        if role_store.exists(role_def.role_id):
            return ValidationResult(
                is_valid=False,
                error=f"角色 {role_def.role_id} 已存在"
            )
        
        # 检查继承的角色是否存在
        if role_def.extends and not role_store.exists(role_def.extends):
            return ValidationResult(
                is_valid=False,
                error=f"继承的目标角色 {role_def.extends} 不存在"
            )
        
        # 检查工具是否都可用
        available_tools = tool_registry.list_tools()
        for tool_name in role_def.tools:
            if tool_name not in available_tools:
                return ValidationResult(
                    is_valid=False,
                    error=f"工具 {tool_name} 不存在"
                )
        
        return ValidationResult(is_valid=True)
```

**使用场景：**

```
用户: "我需要一个 Flutter 开发工程师"
      │
      ▼
Assistant:
      │ 1. 理解需求：需要创建移动开发角色
      │ 2. 调用 create_role 工具
      │    {
      │      "role_name": "Flutter 开发工程师",
      │      "identity": "你是 Flutter 开发工程师，负责移动端应用开发",
      │      "constraints": [
      │        "优先使用 Flutter 推荐的架构模式",
      │        "遵循 Material Design / Cupertino 设计规范"
      │      ],
      │      "tools": ["code_editor", "git", "terminal", "flutter_cli"],
      │      "read_paths": ["lib/", "test/", "docs/"],
      │      "write_paths": ["lib/", "test/"],
      │      "forbidden_paths": ["backend/", "deploy/"]
      │    }
      ▼
CreateRoleTool:
      │ 1. 创建 RoleDefinition
      │ 2. 验证定义
      │ 3. 存储到 role_store
      │ 4. 注册到 agent_runtime
      ▼
Assistant: "已创建 Flutter 开发工程师角色，是否需要加入当前项目？"
```

---

### 3.5 roles - 角色定义

#### 3.5.1 设计理念

**核心原则：角色定义数据化，而非代码化。**

传统做法是将角色写死在代码中，每次新增角色都需要修改代码并重新部署。本系统采用数据化的角色定义：

- 角色定义是数据对象，存储在 `role_store` 中
- 助手可以通过工具动态创建新角色
- 运行时动态加载和注册角色
- 支持角色继承与组合

#### 3.5.1.1 Assistant（私人助理）角色设计

Assistant 是用户进入系统后的入口角色。它不属于任何单一项目，而是面向用户本人提供工作台级服务。

**生命周期：** 临时 Agent — 用户在线时存活，离线后释放。用户偏好和项目列表持久化在 `user_profile` 中。

**职责：**

| 职责 | 说明 |
|------|------|
| 发起新项目 | 协助用户整理项目申请表 → 创建群聊与 Tech Lead → 完成上下文转交 |
| 进入已有项目 | 查找用户项目列表 → 从快照恢复项目群组和 Agent 团队 |
| 管理项目列表 | 维护用户的项目列表（存在 user_profile 中） |
| 文档与知识服务 | 帮用户查阅文档、汇总上下文、做项目导航 |

**不做的事：** 不深度参与项目内讨论、不主导需求分析、不做技术决策、不做任务拆解（这些由项目 Tech Lead 负责）。

**流程能力：** Assistant 的启动流程不是硬编码的，而是通过安装 Agent 级工作流模板实现。runtime 在识别用户意图后激活对应流程，按模板 step 顺序驱动 Assistant 执行。

```yaml
# Assistant 角色定义中声明可用流程
workflows:
  - assistant_startup        # 项目启动流程（7 步）
  - project_navigation       # 项目导航流程
  - knowledge_query          # 知识查询流程
```

启动流程模板定义在 `workflow_templates/agent/assistant_startup.yaml`（详见 quantalithos-flow 概要设计 4.4 节）。

**启动流程概览（由 assistant_startup 工作流模板驱动）：**

```
identify_intent → collect_context → draft_application → confirm_application
    → create_workspace → build_handoff → exit_startup
```

阻断规则（在模板的 done_when 中定义）：
- 未确认的 `project_application` 不能进入 `create_workspace`
- 未完成建场的项目不能进入 `build_handoff`
- 未成功生成 `assistant_startup_handoff` 不能标记为 `exit_startup`

**交接包：`assistant_startup_handoff`**

```yaml
assistant_startup_handoff:
  startup_id: string                    # 一次启动流程的唯一标识
  source: assistant                     # 固定为 assistant
  handoff_status: draft | ready_for_tech_lead | accepted
  user_request_summary: string          # 用户原始诉求摘要

  project_application:                  # 结构化申请表
    name: string
    type: new_project | enhancement | bugfix | ops
    description: string
    scope: small | medium | large
    feature_count: int
    has_frontend: bool
    has_mobile: bool
    priority: urgent | normal | low
    reference: string
    constraints: string

  startup_context:
    entry_mode: form | dialogue         # 启动入口类型
    user_confirmed: bool                # 用户是否确认申请口径
    known_constraints: string[]         # 已知约束
    open_questions: string[]            # 尚未澄清的问题
    assumptions: string[]               # 启动阶段默认假设

  routing_hint:
    suggested_owner: tech_lead          # 建议接手者
    suggested_next_step: goal_alignment # 建议下一动作
    candidate_route: stage1_requirement_analysis | bugfix_flow | ops_flow | exploration_flow
    urgency: low | normal | urgent
```

写入与消费责任：
- **Assistant** 负责创建、补齐、确认并投递交接包
- **Tech Lead** 负责读取、接受、基于其进入目标校准
- **系统/编排层** 负责保证交接包与项目现场、群聊、Tech Lead 实例之间的关联完整性

更新规则：
- `user_request_summary` 和用户确认后的 `project_application` 不应被无痕重写
- `known_constraints`、`open_questions`、`assumptions` 允许增补
- 一旦 `handoff_status = accepted`，项目主导责任从 Assistant 转移到 Tech Lead

**目标校准：`goal_alignment_brief`**

Tech Lead 接手后执行目标校准，产出结构化校准结果：

```yaml
goal_alignment_brief:
  source_startup_id: string             # 关联本次启动交接
  aligned_by: tech_lead                 # 固定为 tech_lead
  alignment_status: passed | needs_clarification | reroute

  work_item_type: new_project | enhancement | bugfix | optimization | exploration | ops
  core_problem: string                  # 核心要解决的问题
  target_outcome: string                # 本轮希望达成的结果
  target_users_or_scope: string         # 面向谁、影响范围
  priority_reason: string               # 为什么现在做
  success_signal: string[]              # 什么现象代表值得继续

  uncertainty_level: low | medium | high
  recommended_route: stage1_requirement_analysis | bugfix_flow | ops_flow | exploration_flow
  followup_questions: string[]          # needs_clarification 时的待补问项
  blocking_issue: string                # reroute 或阻断时的主因
```

通过条件：能判断类型、能写出问题、能给出目标、能定义成功信号、能确定路由。不要求信息完整，只要求足以支撑流程路由。

**阶段 1 入口规则：**

```
assistant_startup_handoff accepted
  → Tech Lead goal alignment
  → goal_alignment_brief passed
  → recommended_route = stage1_requirement_analysis
  → Stage 1 entry
```

**用户档案：`user_profile`**

```yaml
# user_profiles/<user_id>.yaml
user:
  id: user-001
  preferences:
    default_backend: "Rust"
    preferred_style: "简洁"
  projects:
    - name: "个人博客系统"
      file_space: "project_files/personal-blog/"
      status: idle
      last_active: 2026-04-10
```

**评估方式：** Tech Lead 作为 LLM Agent 综合理解申请表内容做判断。规模评估表只是参考指南（写入 tech-lead 的 identity.md），不是硬编码 if-else。

#### 3.5.2 角色定义结构

**设计原则：优先使用 YAML/JSON，而非 Markdown**

原因：
- ✅ 结构化：YAML/JSON 天然支持嵌套、数组、对象
- ✅ 可验证：可通过 JSON Schema 校验格式
- ✅ 可编程：易于序列化/反序列化，无需解析 Markdown
- ✅ 可继承：YAML 支持锚点和合并，便于角色继承

```python
class RoleDefinition(BaseModel):
    """角色定义 - 数据对象"""
    
    # 基本信息
    role_id: str                      # 唯一标识
    role_name: str                    # 显示名称
    description: str                  # 简短描述
    
    # 共享规则引用
    shared_rules: SharedRulesConfig   # 声明加载哪些共享规则
    
    # 身份与约束
    identity: str                     # 身份描述（自然语言）
    constraints: List[str]            # 行为约束
    
    # 能力配置
    tools: List[str]                  # 可用工具列表
    skills: List[str]                 # 可用技能列表（技能是工具的组合）
    workflows: List[str] = []         # 可安装的 Agent 级工作流（如 tdd_workflow, assistant_startup）
    
    # 记忆配置
    memory_config: MemoryConfig       # 记忆策略配置
    
    # 权限配置
    permissions: PermissionConfig     # 路径权限
    
    # 自进化配置
    evolution_config: EvolutionConfig # 技能自迭代配置
    
    # 元信息
    created_by: str                   # "system" | "assistant" | "user"
    created_at: datetime
    version: str
    
    # 可选：继承
    extends: Optional[str] = None     # 继承自哪个角色
    overrides: Optional[dict] = None  # 覆盖配置


class SharedRulesConfig(BaseModel):
    """共享规则引用配置"""
    include: List[str]                # 要加载的共享规则 rule_id 列表
    exclude: List[str] = []           # 要排除的共享规则（项目级可用此字段关闭某些规则）


class MemoryConfig(BaseModel):
    """记忆策略配置"""
    enable_long_term: bool            # 是否启用长期记忆
    compression_threshold: int        # 压缩阈值（消息数）
    retention_days: int               # 记忆保留天数
    enable_user_profile: bool         # 是否学习用户偏好


class EvolutionConfig(BaseModel):
    """自进化配置（参考 Hermes）"""
    enable_auto_skill: bool           # 是否自动生成技能
    skill_improvement: bool           # 是否在使用中迭代技能
    frozen_snapshot: bool             # 是否启用冻结快照（降低 Token）


class PermissionConfig(BaseModel):
    """权限配置"""
    read_paths: List[str]             # 可读路径
    write_paths: List[str]            # 可写路径
    forbidden_paths: List[str]        # 禁止访问路径
    
    # 工具级权限细化（参考 OpenClaw）
    tool_restrictions: Dict[str, Any] # 工具限制条件
    # 例如：{"code_execute": {"max_timeout": 30, "sandbox": true}}
```

#### 3.5.2.1 YAML 角色定义规范

**完整示例：Tech Lead 角色**

```yaml
# role_store/tech_lead.yaml

# 基本信息
role_id: tech_lead
role_name: "项目负责人"
description: "负责需求分析、技术方案设计、任务拆解和工作分配"

# 共享规则引用
shared_rules:
  include:
    - communication
    - verification
    - review
  exclude: []              # 项目级可在此排除，如 exclude: [tdd]

# 身份定义
identity: |
  你是项目负责人，负责需求分析、技术方案设计、任务拆解和工作分配。
  你需要主导项目从需求到交付的全流程，但不直接实现代码。

# 行为约束
constraints:
  - "不直接实现代码（由开发工程师完成）"
  - "不跳过需求分析直接进入开发"
  - "不在用户未确认时推进到下一阶段"
  - "必须为每个任务指定明确的验收标准"

# 能力配置
tools:
  - file_read
  - file_write
  - git_status
  - git_commit
  - search_docs
  - create_task
  - assign_task
  - create_role

skills:
  - code_review        # 代码审查技能
  - tech_design        # 技术方案设计技能
  - task_breakdown     # 任务拆解技能

workflows:
  - review_workflow    # 代码审查流程

# 记忆策略
memory_config:
  enable_long_term: true
  compression_threshold: 50
  retention_days: 90
  enable_user_profile: true

# 权限配置
permissions:
  read_paths:
    - src/
    - docs/
    - tests/
    - tasks/
  
  write_paths:
    - docs/
    - tasks/
  
  forbidden_paths:
    - deploy/
    - secrets/
    - .env
  
  # 工具级限制
  tool_restrictions:
    code_execute:
      max_timeout: 30
      sandbox: true
      forbidden_commands:
        - "rm -rf"
        - "sudo"
    
    git_commit:
      require_review: true
      protected_branches:
        - main
        - master

# 自进化配置
evolution_config:
  enable_auto_skill: true      # 允许自动生成技能
  skill_improvement: true       # 允许迭代优化
  frozen_snapshot: true         # 启用冻结快照

# 元信息
created_by: system
created_at: "2024-01-01T00:00:00Z"
version: "1.0.0"
```

#### 3.5.2.2 角色继承（YAML 锚点）

利用 YAML 的锚点和合并功能，实现角色继承：

```yaml
# role_store/backend_dev.yaml

# 继承基础开发者角色
extends: base_dev

# 基础开发者定义（可放在单独文件或使用锚点）
_base_dev: &base_dev
  constraints:
    - "遵循项目代码规范"
    - "编写单元测试"
    - "不跳过代码审查"
  
  memory_config:
    enable_long_term: true
    compression_threshold: 30
    retention_days: 60
    enable_user_profile: false
  
  evolution_config:
    enable_auto_skill: true
    skill_improvement: true
    frozen_snapshot: false

# 覆盖和扩展
role_id: backend_dev
role_name: "后端开发工程师"

identity: |
  你是后端开发工程师，负责服务端编码实现和 API 开发。

constraints:
  - <<: *base_dev.constraints    # 继承基础约束
  - "不修改 tasks/ 下的任务卡"    # 新增约束
  - "不修改其他角色的代码文件"

tools:
  - file_read
  - file_write
  - git_commit
  - code_execute
  - terminal_execute
  - mcp_database

skills:
  - api_design
  - db_migration

workflows:
  - tdd_workflow             # TDD 流程
  - debugging_workflow       # 系统化调试流程

permissions:
  read_paths:
    - src/
    - docs/
    - tests/
  
  write_paths:
    - src/backend/
    - tests/backend/
  
  forbidden_paths:
    - src/frontend/
    - deploy/
```

#### 3.5.2.3 共享规则（YAML 格式）

共享规则是所有角色通用的行为规范。每条规则是一个独立 YAML 文件，存储在 `shared_rules/` 目录下。

**设计原则：**
- 共享规则不是硬编码的固定列表，而是可扩展的规则库
- 用户/公司可以新增自己的共享规则（如"安全编码规范"、"国际化规范"）
- 项目级别可以覆盖或禁用某些规则
- 角色定义中通过 `shared_rules.include/exclude` 显式声明加载哪些规则

**共享规则文件结构：**

共享规则分为两部分：
- `schema`：给 runtime 框架层消费，做硬控制（校验、拦截、报错）
- `content`：给 LLM 消费，注入 system prompt 做风格指引

设计原则：**schema 管"能不能"，content 管"好不好"。** 凡是能在框架层控制的，不交给 prompt。

> 注意：消息路由、发言权控制（participation）、@提及路由、消息类型分发等属于编排层职责，由 quantalithos-flow (daemon) 在工作流模板或阶段配置中定义，不在 quantalithos-runtime 的共享规则中。quantalithos-runtime 的共享规则只管 Agent 产出消息后的格式校验和 prompt 层行为指引。

```yaml
# shared_rules/communication.yaml

rule_id: communication
name: "沟通规则"
description: "Agent 回复格式与沟通风格规范"
version: "1.0.0"
enabled: true                    # 全局开关

# runtime 框架层消费 — 硬控制
schema:
  # 回复类型约束
  reply_types:
    allowed: [result, blocked, clarification]
    required_fields:
      result: [output, verification]
      blocked: [reason, help_needed]
      clarification: [question]
    forbidden_patterns:
      - action: reject           # reject | warn | log
        patterns:
          - "收到，我来处理"
          - "正在进行中"
          - "好的，我了解了"

# LLM prompt 层消费 — 风格指引
content: |
  ## 沟通风格
  - 回复时直接给出结论和产出，不做寒暄
  - result 类型必须附带实际运行证据，不能只说"我做完了"
  - blocked 时必须说清楚卡在哪里、需要谁提供什么
  - clarification 时问题要具体，不能问"还有什么要求吗"

# 元信息
created_by: system               # system | company | user
applicable_roles: all            # all | [role_id_list]
tags: [communication, protocol]
```

```yaml
# shared_rules/tdd.yaml

rule_id: tdd
name: "测试驱动开发"
description: "TDD 红-绿-重构循环规范"
version: "1.0.0"
enabled: true

# 纯行为指引，无需 schema — runtime 不校验 TDD 步骤
content: |
  ## TDD 工作方式

  默认强制 TDD（工单中 tdd: false 时可跳过）。每个任务按以下步骤执行：

  1. 读工单，理解要做什么
  2. 写一个失败的单元测试（定义"完成"长什么样）
  3. 跑测试，确认失败（红色）— 如果没失败，说明测试写得有问题
  4. 写最少的代码让测试通过
  5. 跑测试，确认通过（绿色）
  6. 重构代码（测试保护下放心改）
  7. 跑工单中的 verification_command，确认全部通过
  8. 提交代码 + 测试 → 进入代码审查

created_by: system
applicable_roles: [backend_dev, frontend_dev]
tags: [development, testing]
```

**三级覆盖机制：**

共享规则支持三级覆盖，高优先级覆盖低优先级的同名规则：

```
查找顺序（高优先级 → 低优先级）：

项目级：  project_files/{project}/agents/shared_rules/{rule_id}.yaml
公司级：  company_templates/shared_rules/{rule_id}.yaml
系统级：  system_templates/shared_rules/{rule_id}.yaml
```

项目级可以覆盖同名规则（如某项目的 tdd.yaml 设置 `enabled: false`），也可以新增项目专属规则。

#### 3.5.2.4 技能定义（YAML 格式）

```yaml
# skill_store/code_review.yaml

skill_id: code_review
skill_name: "代码审查"
description: "自动化代码审查流程"
version: "1.0.0"

# 创建信息
created_by: system
auto_generated: false
usage_count: 0

# 依赖工具
tools:
  - git_diff
  - file_read
  - lint_check
  - generate_review_comment

# 执行步骤
steps:
  - step_id: get_diff
    tool: git_diff
    params:
      target_branch: main
    output_var: diff_result
  
  - step_id: read_files
    tool: file_read
    params:
      files: "${diff_result.changed_files}"
    condition: "${diff_result.has_changes}"
    output_var: file_contents
  
  - step_id: lint
    tool: lint_check
    params:
      files: "${diff_result.changed_files}"
    output_var: lint_result
  
  - step_id: generate_comment
    tool: generate_review_comment
    params:
      context:
        diff: "${diff_result}"
        lint: "${lint_result}"
        files: "${file_contents}"

# 验证条件
validation:
  - condition: "${lint_result.passed == true}"
    error_message: "代码检查未通过"
  
  - condition: "${diff_result.changed_files_count <= 20}"
    error_message: "单次提交文件过多，建议拆分"

# 失败处理
on_failure:
  - action: post_comment
    params:
      content: "${generate_comment.output}"
  
  - action: notify_author
    params:
      message: "代码审查未通过，请查看评论"

# 成功处理
on_success:
  - action: approve_pr
    params: {}
```

#### 3.5.2.5 YAML vs Markdown 对比

| 维度 | YAML | Markdown | 结论 |
|------|------|----------|------|
| **结构化** | ✅ 原生支持嵌套、数组 | ❌ 需手动解析 | **YAML 胜** |
| **可验证** | ✅ JSON Schema 校验 | ❌ 无标准校验 | **YAML 胜** |
| **可读性** | ⚠️ 略低（需熟悉语法） | ✅ 高（人类友好） | **Markdown 胜** |
| **可维护** | ✅ 工具支持完善 | ⚠️ 易出现格式混乱 | **YAML 胜** |
| **可继承** | ✅ 锚点/合并机制 | ❌ 不支持 | **YAML 胜** |
| **跨语言** | ✅ 通用序列化格式 | ⚠️ 需解析库 | **YAML 胜** |

**最终决策：全部使用 YAML**
- 角色定义：✅ YAML（`role_store/*.yaml`）
- 技能定义：✅ YAML（`skill_store/*.yaml`）
- 共享规则：✅ YAML（`shared_rules/*.yaml`），规则正文放在 `content` 字段中
- Prompt 组装：在代码中按角色的 `shared_rules.include/exclude` 加载规则，拼接 `content` 字段

> 注：design 侧原方案中共享规则和角色身份使用 Markdown 文件，经讨论后统一改为 YAML。三层分离的概念模型（共享规则 / 角色定义 / 角色权限）保留，但不再体现为不同文件格式。

#### 3.5.2.6 Prompt 组装逻辑（从 YAML 到自然语言）

```python
def build_system_prompt(
    role_def: dict,
    shared_rules_dir: str,
    project_root: Optional[str] = None
) -> str:
    """从 YAML 角色定义组装系统 Prompt"""
    
    # 1. 加载角色声明的共享规则
    rule_ids = role_def['shared_rules']['include']
    excludes = role_def['shared_rules'].get('exclude', [])
    
    rules_content = []
    for rule_id in rule_ids:
        if rule_id in excludes:
            continue
        rule = resolve_shared_rule(rule_id, shared_rules_dir, project_root)
        if rule and rule.get('enabled', True):
            rules_content.append(rule['content'])
    
    # 2. 组装 prompt：共享规则 + 角色身份 + 行为约束
    prompt = "\n\n".join(rules_content)
    prompt += f"\n\n# 角色身份\n{role_def['identity']}"
    prompt += f"\n\n# 行为约束\n"
    prompt += "\n".join(f"- {c}" for c in role_def['constraints'])
    
    # 3. 阶段规则和任务上下文由 quantalithos-flow (daemon) 在运行时注入
    return prompt


def resolve_shared_rule(
    rule_id: str,
    shared_rules_dir: str,
    project_root: Optional[str] = None
) -> Optional[dict]:
    """按三级优先级查找共享规则"""
    
    search_paths = []
    
    # 项目级（最高优先级）
    if project_root:
        search_paths.append(f"{project_root}/agents/shared_rules/{rule_id}.yaml")
    
    # 公司级
    search_paths.append(f"company_templates/shared_rules/{rule_id}.yaml")
    
    # 系统级
    search_paths.append(f"{shared_rules_dir}/{rule_id}.yaml")
    
    for path in search_paths:
        if os.path.exists(path):
            return load_yaml(path)
    
    return None
```

**prompt 组装顺序：**

```
最终 system_prompt =
    shared_rules（按 include/exclude 加载的共享规则 content）
  + identity（角色身份描述）
  + constraints（行为约束）
  + 当前阶段规则（由 quantalithos-flow daemon 注入）
  + 当前任务上下文（工单内容，如果在阶段 3）
```

permissions 不注入 prompt，而是由运行时在工具调用时做权限校验。

#### 3.5.3 项目级角色团队

**核心问题：如何为每个项目独立创建不同的角色团队？**

##### 3.5.3.1 当前设计的不足

当前设计存在的问题：
- ❌ 角色定义存储在全局 `role_store/`，所有项目共享
- ❌ 项目无法自定义角色（如修改权限、添加技能）
- ❌ 同一角色在不同项目中可能有不同的权限需求
- ❌ 无法为特定项目创建专属角色

##### 3.5.3.2 解决方案：三级角色定义体系

```
角色定义优先级（从高到低）：

① project_roles/           项目级角色定义（优先级最高）
   └── {project_id}/
       ├── tech_lead.yaml      # 覆盖全局角色定义
       ├── backend_dev.yaml
       └── custom_role.yaml    # 项目专属角色

② company_templates/       公司级角色模板
   ├── mobile-developer/
   ├── ml-engineer/
   └── payment-specialist/

③ system_templates/        系统预置模板
   ├── assistant/
   ├── tech-lead/
   ├── backend-developer/
   └── frontend-developer/
```

**查找顺序：**
```
项目启动时查找角色定义：
project_roles/{project_id}/{role_id}.yaml
  ↓ 不存在
company_templates/{role_id}/
  ↓ 不存在
system_templates/{role_id}/
  ↓ 不存在
创建新角色（AI 辅助生成）
```

##### 3.5.3.3 项目级角色定义示例

```yaml
# project_roles/blog-project/tech_lead.yaml

# 继承全局角色定义
extends: tech_lead@system  # 继承系统级 Tech Lead

# 项目级覆盖
role_id: tech_lead
project_id: blog-project  # 绑定项目

# 身份定制（可追加项目特定描述）
identity: |
  你是博客项目的负责人。
  
  {{inherits:identity}}  # 继承父级身份描述
  
  本项目使用 Rust 技术栈，请特别注意内存安全和性能优化。

# 权限定制（项目级路径）
permissions:
  # 继承基础权限
  <<: *base_permissions
  
  # 项目级路径覆盖
  read_paths:
    - "projects/blog-project/src/"
    - "projects/blog-project/docs/"
  
  write_paths:
    - "projects/blog-project/docs/"
    - "projects/blog-project/tasks/"
  
  forbidden_paths:
    - "projects/blog-project/.secrets/"

# 项目级工具限制
tool_restrictions:
  git_commit:
    require_review: true
    protected_branches:
      - main
      - blog-release  # 项目专属分支
```

##### 3.5.3.4 项目专属角色示例

```yaml
# project_roles/blog-project/seo_specialist.yaml

# 项目专属角色（系统没有）
role_id: seo_specialist
role_name: "SEO 优化专员"
project_id: blog-project
description: "负责博客项目的 SEO 优化"

identity: |
  你是博客项目的 SEO 优化专员，负责：
  - 关键词分析和优化
  - 文章 SEO 标题和描述生成
  - 网站结构优化建议

constraints:
  - "不修改后端代码"
  - "不修改数据库结构"
  - "只可修改 meta 标签和文章标题"

tools:
  - file_read
  - file_write
  - search_docs

skills:
  - keyword_analysis
  - meta_generation

permissions:
  read_paths:
    - "projects/blog-project/src/frontend/"
    - "projects/blog-project/content/"
  
  write_paths:
    - "projects/blog-project/content/articles/"
    - "projects/blog-project/public/meta/"
  
  forbidden_paths:
    - "projects/blog-project/src/backend/"
    - "projects/blog-project/.secrets/"
```

##### 3.5.3.5 项目团队配置文件

每个项目有一个团队配置文件，定义项目需要哪些角色：

```yaml
# projects/blog-project/team.yaml

project_id: blog-project
project_name: "博客系统"

# 团队配置
team:
  # 核心角色
  - role_id: tech_lead
    required: true
    count: 1
    source: project_roles  # 使用项目级定义
    
  - role_id: backend_dev
    required: true
    count: 2
    source: system_templates  # 使用系统模板
    
  - role_id: frontend_dev
    required: true
    count: 1
    source: company_templates  # 使用公司模板
    
  # 项目专属角色
  - role_id: seo_specialist
    required: false
    count: 1
    source: project_roles  # 项目专属定义
    
  # 可选角色
  - role_id: ui_designer
    required: false
    count: 1
    source: system_templates

# 项目级权限边界
project_permissions:
  # 所有角色的基础路径前缀
  base_path: "projects/blog-project/"
  
  # 全局禁止访问
  global_forbidden:
    - ".secrets/"
    - ".env"
    - "deploy/production/"
```

##### 3.5.3.6 团队组建流程

```python
async def create_project_team(project_id: str, team_config: dict):
    """为项目组建团队"""
    
    team_members = []
    
    for role_config in team_config['team']:
        role_id = role_config['role_id']
        count = role_config['count']
        source = role_config['source']
        
        # 1. 查找角色定义
        role_def = find_role_definition(
            role_id=role_id,
            project_id=project_id,
            source=source
        )
        
        if not role_def:
            # 2. 角色不存在，AI 辅助创建
            role_def = await create_role_with_ai(
                role_id=role_id,
                project_context=team_config,
                user_confirmation=True
            )
        
        # 3. 实例化多个 Agent
        for i in range(count):
            agent = await instantiate_agent(
                role_def=role_def,
                project_id=project_id,
                agent_id=f"{role_id}-{i+1}"
            )
            team_members.append(agent)
    
    return team_members


def find_role_definition(role_id: str, project_id: str, source: str):
    """按优先级查找角色定义"""
    
    search_paths = [
        f"project_roles/{project_id}/{role_id}.yaml",     # 项目级
        f"company_templates/{role_id}/definition.yaml",   # 公司级
        f"system_templates/{role_id}/definition.yaml",    # 系统级
    ]
    
    for path in search_paths:
        if os.path.exists(path):
            return load_yaml(path)
    
    return None
```

##### 3.5.3.7 运行时权限隔离

```python
class ProjectAwarePermissionChecker:
    """项目感知的权限检查器"""
    
    def check_permission(
        self,
        agent: Agent,
        action: str,
        target_path: str
    ) -> bool:
        """检查 Agent 在项目中的权限"""
        
        # 1. 获取项目 ID
        project_id = agent.project_id
        
        # 2. 加载项目级角色定义
        role_def = self.load_project_role(
            project_id=project_id,
            role_id=agent.role_id
        )
        
        # 3. 检查路径前缀
        project_base = f"projects/{project_id}/"
        if not target_path.startswith(project_base):
            # 不允许访问其他项目的文件
            return False
        
        # 4. 检查项目级权限
        relative_path = target_path[len(project_base):]
        permissions = role_def['permissions']
        
        # 检查禁止访问
        if self.match_paths(relative_path, permissions['forbidden_paths']):
            return False
        
        # 检查写权限
        if action == 'write':
            return self.match_paths(relative_path, permissions['write_paths'])
        
        # 检查读权限
        if action == 'read':
            return self.match_paths(relative_path, permissions['read_paths'])
        
        return False
```

##### 3.5.3.8 文件系统结构

```
agent-runtime/
├── role_store/                    # 全局角色定义
│   ├── system_templates/          # 系统预置
│   │   ├── tech_lead.yaml
│   │   └── backend_dev.yaml
│   └── company_templates/         # 公司自定义
│       ├── ml_engineer.yaml
│       └── payment_specialist.yaml
│
├── project_roles/                 # 项目级角色定义
│   ├── blog-project/
│   │   ├── tech_lead.yaml         # 覆盖全局定义
│   │   └── seo_specialist.yaml    # 项目专属
│   │
│   └── ecommerce-project/
│       ├── tech_lead.yaml
│       └── payment_integration.yaml
│
├── skill_store/                   # 全局技能库
│   ├── code_review.yaml
│   └── api_design.yaml
│
└── project_skills/                # 项目级技能库
    ├── blog-project/
    │   └── seo_optimization.yaml
    └── ecommerce-project/
        └── payment_flow.yaml
```

##### 3.5.3.9 角色定义的存储位置

**核心问题：项目级角色应该定义在哪里？**

有两种方案：

###### 方案 A：角色定义在 agent-runtime 内部

```
agent-runtime/
├── project_roles/                 # 项目级角色定义（集中存储）
│   ├── blog-project/
│   │   ├── tech_lead.yaml
│   │   └── seo_specialist.yaml
│   │
│   └── ecommerce-project/
│       ├── tech_lead.yaml
│       └── payment_integration.yaml
│
└── projects/                      # 项目工作区
    ├── blog-project/
    │   ├── src/
    │   └── .team.yaml             # 团队配置文件
    │
    └── ecommerce-project/
        ├── src/
        └── .team.yaml
```

**优点：**
- ✅ 角色定义集中管理，便于统一维护
- ✅ 全局视角可见所有项目的角色配置
- ✅ 角色定义独立于项目代码，可跨项目复用
- ✅ 更适合单体架构或中心化管理

**缺点：**
- ❌ 项目角色定义与项目代码分离，不够直观
- ❌ 项目迁移时需要同步迁移角色定义
- ❌ 开发者可能忘记角色定义在 agent-runtime 中

---

###### 方案 B：角色定义在项目仓库内（推荐）

```
项目仓库结构：

blog-project/                      # 项目根目录
├── agents/                        # Agent 配置目录
│   ├── team.yaml                  # 团队配置
│   ├── roles/                     # 项目级角色定义
│   │   ├── tech_lead.yaml         # 覆盖全局角色
│   │   └── seo_specialist.yaml    # 项目专属角色
│   │
│   ├── skills/                    # 项目级技能
│   │   └── seo_optimization.yaml
│   │
│   └── permissions.yaml           # 项目级权限边界
│
├── workspace/                     # 工作区（代码、测试、脚本等）
│   ├── src/
│   └── tests/
│
├── docs/                          # 项目文档
├── tasks/                         # 任务卡
└── snapshots/                     # 快照
```

**优点：**
- ✅ **角色定义与项目代码同仓库**，更直观
- ✅ **版本控制同步** - 角色定义随项目代码一起演进
- ✅ **项目迁移简单** - 整个仓库直接迁移
- ✅ **开发者友好** - 在项目中就能看到所有 Agent 配置
- ✅ **分支隔离** - 不同分支可有不同的角色配置

**缺点：**
- ❌ 需要在项目启动时加载项目仓库中的角色定义
- ❌ 角色定义分散在多个仓库，全局视角不直观

---

###### 推荐方案：混合模式

**核心思路：**
- **系统级/公司级角色** → 存储在 `agent-runtime` 内
- **项目级角色** → 存储在项目的 `agents/` 目录

```
角色定义查找顺序：

1. 项目仓库：{project_root}/agents/roles/{role_id}.yaml
   ↓ 不存在
   
2. 公司模板：agent-runtime/company_templates/{role_id}/
   ↓ 不存在
   
3. 系统模板：agent-runtime/system_templates/{role_id}/
   ↓ 不存在
   
4. AI 辅助创建 → 保存到项目 agents/roles/
```

**代码实现：**

```python
class RoleDefinitionResolver:
    """角色定义解析器"""
    
    def resolve_role_definition(
        self,
        role_id: str,
        project_root: str
    ) -> RoleDefinition:
        """按优先级查找角色定义"""
        
        # 1. 项目仓库中的角色定义（最高优先级）
        project_role_path = f"{project_root}/agents/roles/{role_id}.yaml"
        if os.path.exists(project_role_path):
            return self.load_yaml(project_role_path)
        
        # 2. 公司级模板
        company_role_path = f"company_templates/{role_id}/definition.yaml"
        if os.path.exists(company_role_path):
            return self.load_yaml(company_role_path)
        
        # 3. 系统级模板
        system_role_path = f"system_templates/{role_id}/definition.yaml"
        if os.path.exists(system_role_path):
            return self.load_yaml(system_role_path)
        
        # 4. 角色不存在，返回 None
        return None
    
    async def create_role_if_missing(
        self,
        role_id: str,
        project_root: str,
        project_context: dict
    ) -> RoleDefinition:
        """如果角色缺失，AI 辅助创建并保存到项目仓库"""
        
        role_def = self.resolve_role_definition(role_id, project_root)
        if role_def:
            return role_def
        
        # AI 辅助创建
        role_def = await self.ai_create_role(
            role_id=role_id,
            context=project_context
        )
        
        # 保存到项目仓库
        save_path = f"{project_root}/agents/roles/{role_id}.yaml"
        self.save_yaml(role_def, save_path)
        
        return role_def
```

**项目启动流程：**

```python
async def bootstrap_project(project_root: str):
    """项目启动流程"""
    
    # 1. 加载团队配置
    team_config = load_yaml(f"{project_root}/agents/team.yaml")
    
    # 2. 解析所有角色定义
    resolver = RoleDefinitionResolver()
    
    for role_config in team_config['team']:
        role_id = role_config['role_id']
        
        # 从项目仓库 → 公司模板 → 系统模板查找
        role_def = resolver.resolve_role_definition(
            role_id=role_id,
            project_root=project_root
        )
        
        # 如果缺失，AI 创建并保存到项目仓库
        if not role_def:
            role_def = await resolver.create_role_if_missing(
                role_id=role_id,
                project_root=project_root,
                project_context=team_config
            )
        
        # 实例化 Agent
        await instantiate_agent(role_def, project_root)
```

---

###### 最终文件系统结构

```
agent-runtime/                     # Agent 运行时（系统级）
├── system_templates/              # 系统预置角色
│   ├── tech_lead.yaml
│   ├── backend_dev.yaml
│   └── frontend_dev.yaml
│
├── company_templates/             # 公司级角色模板
│   ├── ml_engineer.yaml
│   └── payment_specialist.yaml
│
└── skill_store/                   # 全局技能库
    ├── code_review.yaml
    └── api_design.yaml

projects/                          # 项目工作区
├── blog-project/                  # 博客项目
│   ├── agents/                    # Agent 配置（quantalithos-sync 同步）
│   │   ├── team.yaml              # 团队配置
│   │   ├── roles/                 # 项目级角色
│   │   │   ├── tech_lead.yaml     # 覆盖全局 Tech Lead
│   │   │   └── seo_specialist.yaml  # 项目专属角色
│   │   │
│   │   ├── skills/                # 项目级技能
│   │   │   └── seo_optimization.yaml
│   │   │
│   │   └── permissions.yaml       # 项目级权限边界
│   │
│   ├── docs/                      # 项目文档
│   ├── tasks/                     # 任务卡
│   ├── snapshots/                 # 快照
│   └── workspace/                 # 工作区（代码、测试、脚本）
│
└── ecommerce-project/             # 电商项目
    ├── agents/
    │   ├── team.yaml
    │   ├── roles/
    │   │   ├── tech_lead.yaml
    │   │   └── payment_integration.yaml
    │   └── skills/
    │       └── payment_flow.yaml
    │
    ├── docs/
    ├── tasks/
    ├── snapshots/
    └── workspace/
```

---

###### 关键设计决策

| 决策点 | 选择 | 理由 |
|--------|------|------|
| 项目级角色存储位置 | **项目 `agents/` 目录** | 随项目同步，迁移方便 |
| 角色定义查找顺序 | 项目 → 公司 → 系统 | 项目优先，逐步回退 |
| AI 创建的角色保存位置 | **项目仓库** | 确保下次启动可用 |
| 是否需要注册到中心 | 可选 | 大型企业可选注册到中心服务 |

**推荐：方案 B（项目仓库内）**

优点最明显：
- ✅ **开发者直观** - 在项目中就能看到所有 Agent 配置
- ✅ **版本同步** - 角色定义与代码版本一致
- ✅ **独立迁移** - 项目可独立迁移，无需额外配置

##### 3.5.3.10 预置角色

| 角色 | 类型 | 职责 | 参与阶段 |
|------|------|------|----------|
| assistant | 系统级（临时） | 用户入口、项目启动、项目导航、文档服务 | 阶段 0 |
| tech_lead | 核心 | 项目负责人、需求分析、方案设计、任务拆解、代码审查 | 阶段 0-5 |
| backend_dev | 核心 | 后端编码实现、API 开发 | 阶段 3 |
| frontend_dev | 核心 | 前端编码实现、页面开发 | 阶段 3 |
| tester | 核心 | 编写测试用例、执行测试、报告 Bug | 阶段 2-4 |
| devops | 核心 | CI/CD、部署、环境管理 | 阶段 2-5 |
| ui_designer | 可选 | 界面布局、交互设计、设计规范 | 阶段 2 |
| mobile_dev | 可选 | iOS/Android 移动端开发 | 阶段 3 |

预置角色以 YAML 模板形式存储在 `system_templates/role_store/` 中。所有角色统一通过 YAML 定义加载，不再需要每个角色一个 .py 文件。用户/公司可自定义新角色模板，新角色自动继承 `shared_rules.include` 中声明的共享规则。

#### 3.5.4 角色实例化

```
RoleDefinition (数据)
       │
       │ 实例化
       ▼
┌─────────────────────┐
│ Agent Instance      │
│ ┌─────────────────┐ │
│ │ agent_id: "001" │ │
│ │ role_id: "tech_lead" │ │
│ │ memory: {...}   │ │
│ │ tools: [...]    │ │
│ └─────────────────┘ │
└─────────────────────┘
       │
       │ 同一角色可实例化多个 Agent
       ▼
┌─────────────────────┐
│ Agent Instance      │
│ agent_id: "002"     │
│ role_id: "tech_lead" │
└─────────────────────┘
```

---

### 3.6 prompts - Prompt 管理（三层架构）

#### 3.6.1 架构设计

Prompt 管理采用三层架构，**角色定义和共享规则均使用 YAML 格式**：

```
prompts/
└── shared/              # 共享规则（YAML 格式）
    ├── communication.yaml   # 沟通规则
    ├── tdd.yaml            # TDD 规范
    ├── debugging.yaml      # 调试规范
    ├── verification.yaml   # 验证规范
    └── review.yaml         # 审查规范

role_store/              # 角色定义存储（YAML 格式）
├── assistant.yaml          # Assistant 角色定义
├── tech_lead.yaml          # Tech Lead 角色定义
├── backend_dev.yaml        # 后端开发角色定义
├── frontend_dev.yaml       # 前端开发角色定义
├── tester.yaml             # 测试工程师角色定义
└── devops.yaml             # 运维工程师角色定义

skill_store/             # 技能定义存储（YAML 格式）
├── code_review.yaml        # 代码审查技能
├── api_design.yaml         # API 设计技能
└── deploy.yaml             # 部署流程技能
```

**变化说明：**

- ~~`roles/` 和 `permissions/` 目录~~ → 合并为 `role_store/*.yaml`
- ~~`.md` + `.yaml` 文件~~ → 统一为 `.yaml` 格式
- ~~共享规则 `.md`~~ → 改为 `.yaml` 格式
- **优势**：
  - ✅ 结构化数据，易于程序处理
  - ✅ JSON Schema 校验，保证格式正确
  - ✅ YAML 锚点支持继承和复用
  - ✅ 助手可通过工具动态创建和修改

#### 3.6.2 Prompt 组装流程

```python
def build_system_prompt(role_id: str) -> str:
    """从 YAML 角色定义组装系统 Prompt"""
    
    # 第一层：加载共享规则（YAML）
    shared_rules = load_shared_rules_from_yaml()
    # communication.yaml + tdd.yaml + debugging.yaml + ...
    
    # 第二层：加载角色定义（YAML）
    role_def = role_store.load_yaml(role_id)
    # 从 role_store/role_id.yaml 读取角色定义数据
    
    # 第三层：转换为 Prompt 文本
    role_prompt = format_role_prompt(role_def)
    # 将 role_def['identity'], role_def['constraints'],
    # role_def['permissions'] 转换为自然语言描述
    
    # 组装
    system_prompt = f"""
{format_shared_rules(shared_rules)}

{role_prompt}
"""
    return system_prompt


def format_role_prompt(role_def: dict) -> str:
    """将 YAML 角色定义转换为 Prompt 文本"""
    
    constraints_text = '\n'.join(f'- {c}' for c in role_def['constraints'])
    
    prompt = f"""
# 角色身份
{role_def['identity']}

# 行为约束
{constraints_text}

# 权限说明
可访问路径：{', '.join(role_def['permissions']['read_paths'])}
可修改路径：{', '.join(role_def['permissions']['write_paths'])}
禁止访问：{', '.join(role_def['permissions']['forbidden_paths'])}
"""
    return prompt


def load_shared_rules_from_yaml() -> dict:
    """从 YAML 文件加载共享规则"""
    
    import yaml
    from pathlib import Path
    
    rules = {}
    shared_dir = Path('prompts/shared/')
    
    for yaml_file in shared_dir.glob('*.yaml'):
        with open(yaml_file, 'r', encoding='utf-8') as f:
            rule = yaml.safe_load(f)
            rules[rule['rule_id']] = rule
    
    return rules
```

#### 3.6.3 示例：Tech Lead Prompt

```markdown
# 第一层：共享规则

## 沟通规则
- 被 @提及时必须响应
- 三种回复类型：答复、@他人、无需回复
- 不参与无关讨论

## TDD 规范
- 先写失败测试，再写实现
- 红-绿-重构循环

## 调试规范
- 四步调试：复现 → 定位 → 写测试 → 修复

---

# 第二层：角色定义

## Tech Lead 身份
你是项目负责人，负责需求分析、技术方案设计、任务拆解和工作分配。

## 职责
- 主导需求讨论，形成 PRD
- 设计技术方案
- 拆解任务并分配给团队成员
- 审查代码

## 约束
- 不直接实现代码（由开发工程师完成）
- 不跳过需求分析直接进入开发
- 不在用户未确认时推进到下一阶段

---

# 第三层：权限约束

## 可用工具
- file_read / file_write
- create_task / assign_task
- search_docs
- git_status / git_commit

## 禁用工具
- code_execute_sandbox
```

---

### 3.7 runtime - 运行时引擎

#### 3.7.1 执行器

```python
class AgentExecutor:
    """Agent 执行器"""
    
    async def run(self, agent: Agent, message: Message):
        """运行 Agent 处理消息"""
        
        while True:
            # 1. 更新状态
            agent.state = AgentState.THINKING
            
            # 2. 思考
            tool_call = await agent.think()
            
            if tool_call:
                # 3. 执行工具
                agent.state = AgentState.ACTING
                result = await agent.act(tool_call)
                
                # 将结果加入记忆，继续思考
                agent.memory.add_tool_result(result)
                continue
            
            # 4. 生成响应
            response = await agent.generate_response()
            
            # 5. 发送消息
            await self.send_message(response)
            
            # 6. 更新状态
            agent.state = AgentState.WAITING
            break
```

#### 3.7.1.1 Agent 级工作流引擎

当 TaskAssignment 携带 `workflow` 字段时，AgentExecutor 不直接进入自由 think→act 循环，而是委托给 AgentWorkflowEngine 按模板 step 顺序驱动。

> Agent 级工作流内部仍保留 done_when，由 AgentWorkflowEngine 在 runtime 内部判断。
> 项目级 step 的完成由 submit_step_result 工具保证（见 3.1.3）。

```python
class AgentWorkflowEngine:
    """Agent 级工作流引擎 — 按模板驱动单个 Agent 执行多步骤任务"""
    
    async def run(self, agent: Agent, assignment: TaskAssignment):
        """加载工作流模板，按 step 顺序驱动 Agent"""
        
        # 1. 加载模板
        template = self.load_template(assignment.workflow)
        instance = WorkflowInstance(
            workflow_id=template.workflow_id,
            scope="agent",
            project_id=assignment.project_id,
        )
        
        # 2. 按 step 顺序执行
        current_step = template.steps[0]
        
        while current_step:
            instance.step_states[current_step.id] = "running"
            
            # 注入当前 step 的 action 到 prompt
            step_prompt = f"当前步骤：{current_step.name}\n{current_step.action or ''}"
            agent.memory.add_system_message(step_prompt)
            
            # Agent think→act 循环（复用 AgentExecutor）
            result = await self.executor.run(agent, step_context=current_step)
            
            # 检查 done_when（Agent 级保留）
            if self.check_done_when(result, current_step.done_when):
                instance.step_states[current_step.id] = "completed"
                current_step = self.get_next_step(template, current_step)
            else:
                # 失败处理
                if current_step.on_fail:
                    current_step = self.get_step_by_id(template, current_step.on_fail)
                else:
                    instance.step_states[current_step.id] = "failed"
                    break
        
        # 3. 所有 step 完成 → Agent 调用 submit_step_result 提交最终结果
        return instance
```

**执行流程：**

```
TaskAssignment (workflow = "tdd_workflow")
    │
    ▼
AgentWorkflowEngine
    │
    │  加载 workflow_templates/agent/tdd_workflow.yaml
    │
    ▼
┌─────────────────────────────────────────────────┐
│  step: read_task                                │
│    → 注入 "当前步骤：读取工单" 到 prompt        │
│    → Agent think→act                            │
│    → 检查 done_when: [task_understood]           │
│    → ✅ 通过 → 推进到 next                      │
├─────────────────────────────────────────────────┤
│  step: write_failing_test                       │
│    → 注入 "当前步骤：写失败测试" 到 prompt      │
│    → Agent think→act                            │
│    → 检查 done_when: [test_file_created]         │
│    → ✅ 通过 → 推进到 next                      │
├─────────────────────────────────────────────────┤
│  step: run_test_red                             │
│    → Agent 执行 code_execute                    │
│    → 检查 done_when: [test_failed]               │
│    → ❌ 测试没失败 → on_fail: write_failing_test │
│    → 回退重写测试                                │
├─────────────────────────────────────────────────┤
│  ...（write_code → run_test_green → refactor）  │
├─────────────────────────────────────────────────┤
│  step: submit                                   │
│    → Agent 提交 DELIVERABLE                     │
│    → 检查 done_when: [deliverable_submitted]     │
│    → ✅ 全部完成                                │
└─────────────────────────────────────────────────┘
    │
    ▼
返回 WorkflowInstance（所有 step completed）
    │
    ▼
AgentExecutor 构造 StepEvent 上报给 flow
```

**模板加载优先级：**

```
1. 角色定义的 workflows 字段中是否包含该 workflow_id → 不包含则拒绝
2. 项目级：project_files/{project}/agents/workflows/{workflow_id}.yaml
3. 系统级：workflow_templates/agent/{workflow_id}.yaml
```

**无 workflow 时的退化：**

当 TaskAssignment.workflow = None 时，AgentWorkflowEngine 不参与，AgentExecutor 直接执行自由 think→act 循环。这保证了向后兼容 — 不是所有 step 都需要模板驱动。

#### 3.7.2 消息路由

runtime 的消息路由只负责**投递**，不负责**决策**。谁该收到消息、谁该发言，由 quantalithos-flow (daemon) 决定。

```
quantalithos-flow (daemon)
       │
       │ 决定目标 Agent
       │
       ▼
quantalithos-runtime MessageRouter
       │
       │ 按 target_agent_id 投递
       │
       ▼
  目标 Agent
```

runtime MessageRouter 的职责：
- 收到 daemon 转发的消息 → 按 target_agent_id 投递给目标 Agent
- 收到 Agent 产出的回复 → 校验回复格式（communication schema）→ 上报给 daemon
- 不做路由决策（不判断"群聊无 @ 时谁该回复"）

> **设计变更记录：**
> 旧设计中 MessageRouter 包含路由决策逻辑（私聊→单 Agent、@提及→被 @ 的 Agent、群聊无 @→根据上下文判断）。
> 现已明确：路由决策归 quantalithos-flow，runtime 只做投递。

#### 3.7.3 群聊管理

```python
class GroupChat:
    """群聊管理"""
    
    group_id: str
    project_id: str
    members: List[Agent]      # 群成员（Agent 实例）
    message_history: List[Message]
    
    async def add_agent(self, agent: Agent):
        """添加 Agent 到群聊"""
        self.members.append(agent)
        # 发送系统消息：XXX 已加入群聊
        
    async def remove_agent(self, agent_id: str):
        """移除 Agent"""
        # 发送系统消息：XXX 已离开群聊
        
    async def broadcast(self, message: Message):
        """广播消息给所有成员"""
        for agent in self.members:
            if agent.agent_id != message.agent_id:
                await agent.receive_message(message)
```

#### 3.7.4 Agent 生命周期

```
┌─────────────────────────────────────────────────────────┐
│                   Agent 生命周期                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  创建                                                    │
│  ────                                                    │
│  Template → Instance → Initialize → Active              │
│                                                         │
│  运行                                                    │
│  ────                                                    │
│  Active ⇄ Thinking ⇄ Acting ⇄ Waiting                  │
│                                                         │
│  挂起（Phase 2）                                         │
│  ──────                                                  │
│  Active → Save Snapshot → Suspended                     │
│                                                         │
│  恢复（Phase 2）                                         │
│  ────                                                    │
│  Snapshot → Load State → Restore Memory → Active        │
│                                                         │
│  销毁                                                    │
│  ────                                                    │
│  Cleanup → Release Resources → Destroy                  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**快照与恢复接口（Phase 2 实现）：**

```python
class AgentSnapshot(BaseModel):
    """Agent 快照元数据 — 存储在 snapshots/agents/ 目录"""
    agent_id: str
    role_id: str
    state: str                        # 挂起时的执行状态
    suspended_at: datetime
    conversation_ref: Optional[str]   # 指向数据库中的对话记忆
    memory_ref: Optional[str]         # 指向向量库中的长期记忆（Phase 3）
    last_step: Optional[dict]         # 上次执行到哪个 step（step_id、task_id、progress）


class AgentLifecycle:
    async def create(self, role_id: str, config: dict) -> Agent:
        """从角色定义实例化 Agent"""

    async def suspend(self, agent_id: str) -> AgentSnapshot:
        """挂起 Agent，保存快照到 snapshots/agents/"""

    async def resume(self, snapshot: AgentSnapshot) -> Agent:
        """从快照恢复 Agent（从 DB 恢复对话记忆，从向量库恢复长期记忆）"""

    async def destroy(self, agent_id: str) -> None:
        """销毁 Agent，释放资源"""
```

> Phase 1 不实现 suspend/resume，Agent 用完即销毁。Phase 2 加入快照持久化后支持跨会话恢复。

---

## 四、关键设计决策

### 4.1 Agent 类型

| 类型 | 生命周期 | 适用场景 |
|------|----------|----------|
| **持久 Agent** | 长期存在 | 核心骨干（Tech Lead、资深开发） |
| **临时 Agent** | 项目期间 | 并行增援、特定项目角色 |

### 4.2 消息模型

runtime 内部的 Message 只承载 LLM 对话语义，不承载业务事件语义：

| 字段 | 说明 |
|------|------|
| `role` | LLM API 要求：`user` / `assistant` / `system` / `tool` |
| `content` | 消息正文 |
| `metadata` | 扩展字段（reply_type、output、verification 等） |

业务事件（任务分配、交付物提交、状态变更等）由 quantalithos-flow 在 TaskAssignment / SessionAssignment / StepEvent 层面承载，详见 3.1.3。

### 4.3 工具权限模型

两层权限取交集：

```
Agent 请求调用工具
       │
       ▼
第一层：角色级静态权限（role_store 中的 tools 字段）
       │
       ├── 不在角色 tools 中 → 拒绝
       │
       ▼
第二层：step 级动态权限（daemon 通过 TaskAssignment.allowed_tools / SessionAssignment.allowed_tools 下发）
       │
       ├── 未注入 allowed_tools → 仅角色级权限生效，允许执行
       ├── 在 allowed_tools 中 → 允许执行
       └── 不在 allowed_tools 中 → 拒绝

最终可用工具 = 角色定义的 tools ∩ daemon 下发的 step.allowed_tools
```

---

## 五、非功能设计

### 5.1 错误处理与重试策略

核心原则：**能让 LLM 自己修正的，交给 LLM；LLM 也搞不定的，上报给 daemon。** runtime 不做业务层面的错误恢复决策。

| 场景 | 策略 |
|------|------|
| LLM 调用超时/限流 | 指数退避重试，最多 3 次，最终失败上报 StepEvent(failed) |
| LLM 返回异常（格式错误等） | 重试 1 次，仍失败则上报 StepEvent(failed) |
| 工具执行失败 | 不重试，把错误信息作为 `role: tool` 消息返回给 LLM，让 LLM 自己决定下一步 |
| think→act 循环超限 | 设置最大轮次（如 20 轮），超限则上报 StepEvent(failed) |
| 回复格式校验不通过 | 把校验错误作为 `role: system` 消息注入，让 LLM 重新生成，最多重试 2 次 |
| StepEvent 上报失败 | 本地缓存，重试 3 次，最终失败记录日志 |

### 5.2 配置管理

runtime 自身的运行配置使用环境变量 + 配置文件，Phase 1 使用 `.env` + Pydantic Settings：

```python
class RuntimeConfig(BaseSettings):
    # LLM
    llm_api_key: str
    llm_model: str = "gpt-4"
    llm_max_retries: int = 3
    llm_timeout: int = 60

    # 执行限制
    max_think_act_rounds: int = 20
    max_reply_retries: int = 2

    # 数据库（Phase 2）
    database_url: Optional[str] = None

    # 日志
    log_level: str = "info"
    log_format: str = "json"

    class Config:
        env_file = ".env"
```

### 5.3 可观测性

使用 JSON 结构化日志，便于后续接入 ELK、Grafana Loki 等工具进行查询和过滤。

```json
{
  "ts": "2026-04-20T10:01:30Z",
  "level": "info",
  "agent_id": "backend-dev-1",
  "step_id": "implement",
  "task_id": "TASK-003",
  "action": "tool_execute",
  "tool": "code_execute",
  "command": "cargo test tests::tags",
  "result": "success",
  "duration_ms": 3200
}
```

日志上下文标识（agent_id、step_id）来自 TaskAssignment / SessionAssignment，runtime 在执行循环中透传到每条日志。

Phase 1 使用 `structlog` 或 `python-json-logger` 实现。Phase 2 如需分布式追踪，引入 OpenTelemetry。

### 5.4 并发模型

使用 Python asyncio 并发，每个 Agent 的 think→act 循环是一个独立的 async task。

**关键约束：**

- 同一个 Agent 同一时刻只执行一个 step，串行。Agent 的对话历史是线性的，LLM 不能同时处理两段不相关的上下文。daemon 负责保证不会给同一个 Agent 同时分配多个 step。
- 不同 Agent 之间天然隔离：每个 Agent 实例有独立的 memory、独立的工具执行上下文。
- 文件系统冲突通过任务分支隔离解决（每个任务在 task/* 分支上工作）。
- LLM 调用共享同一个 LLM client 实例，每次调用独立，不需要额外隔离。

不需要锁、不需要消息队列、不需要复杂的调度器。asyncio 的事件循环天然处理 IO 并发。

---

## 六、接口设计

### 6.1 对外接口

```python
class AgentRuntimeAPI:
    """Agent Runtime 对外 API"""
    
    # Agent 管理
    async def create_agent(role: str, config: dict) -> Agent
    async def destroy_agent(agent_id: str) -> None
    
    # 消息处理
    async def send_message(agent_id: str, message: Message) -> Message
    async def broadcast(group_id: str, message: Message) -> None
    
    # 群聊管理
    async def create_group(project_id: str) -> GroupChat
    async def add_to_group(group_id: str, agent_id: str) -> None
    async def remove_from_group(group_id: str, agent_id: str) -> None
    
    # 状态查询
    async def get_agent_state(agent_id: str) -> AgentState
    async def get_group_members(group_id: str) -> List[Agent]
```

### 6.2 与其他服务的交互

```
quantalithos-runtime
      │
      ├──→ quantalithos-flow (daemon)
      │    - 接收 TaskAssignment / SessionAssignment（任务分配）
      │    - 上报 StepEvent（执行结果/阻塞/失败）
      │
      ├──→ quantalithos-vault (产物服务)
      │    - 读取产物（PRD、设计文档等）
      │    - 写入产物（代码、测试报告等）
      │
      ├──→ quantalithos-gate (API 网关)
      │    - 接收用户消息
      │    - 推送 Agent 响应
      │
      └──→ 研发协作平台 (通过平台 API 工具)
           - 查询/创建/更新任务卡
           - 查询/创建 BUG 工单
```

---

## 七、开发计划

> 技术选型：Phase 1 全部使用纯 Python 实现。设计验证完成后再按需引入 Rust（详见技术选型.md）。

### Phase 1: 核心框架（MVP — 跑通单 Agent 执行闭环）

- [ ] Agent 基类与执行状态机（think→act 循环）
- [ ] LLM 客户端（OpenAI SDK，流式输出）
- [ ] 对话记忆（会话内，内存存储）
- [ ] 基础工具框架（工具注册、执行、沙箱）
- [ ] 内置工具（file_read、file_write、code_execute、git_commit）
- [ ] Message 结构（role + content + metadata）
- [ ] AgentExecutor 执行器
- [ ] 接收 TaskAssignment / SessionAssignment / 上报 StepEvent 接口

### Phase 2: 角色系统与协作

- [ ] RoleDefinition 数据结构（YAML 加载）
- [ ] SharedRulesConfig（include/exclude + 三级覆盖）
- [ ] Prompt 组装逻辑（shared_rules content + identity + constraints）
- [ ] communication.yaml schema 校验（reply_type + 必填字段）
- [ ] 两层工具权限（角色级 ∩ step 级）
- [ ] 预置角色（assistant、tech_lead、backend_dev、frontend_dev、tester、devops）
- [ ] 消息路由（按 target_agent_id 投递）
- [ ] 群聊管理
- [ ] 平台集成工具（task_read、task_create、task_update、task_query）

### Phase 3: 持久化与恢复

- [ ] 对话记忆持久化（PostgreSQL）
- [ ] Agent 快照（suspend/resume — 元数据存 YAML，对话历史存 DB）
- [ ] Agent 生命周期管理（实例化、挂起、恢复、销毁）
- [ ] 技能系统（SkillDefinition、技能执行引擎、预置技能）
- [ ] 记忆压缩机制

### Phase 4: 高级能力

- [ ] 长期记忆（向量库，跨项目经验检索）
- [ ] 自进化能力（自动技能生成、技能迭代优化）
- [ ] 冻结快照模式（降低 Token）
- [ ] 角色继承与组合机制
- [ ] CreateRoleTool（Assistant 动态创建角色）

---

## 八、角色能力维度总览

基于 OpenClaw 和 Hermes 的设计，角色定义应包含以下能力维度：

### 7.1 核心能力维度

| 维度 | 说明 | 数据字段 |
|------|------|----------|
| **身份定义** | 角色身份描述、职责 | `identity`, `constraints` |
| **工具能力** | 可用的原子操作工具 | `tools` |
| **技能能力** | 可用的工具组合技能 | `skills` |
| **权限边界** | 文件/路径访问权限 | `permissions` |
| **记忆策略** | 记忆存储和检索策略 | `memory_config` |
| **自进化** | 技能自动生成和迭代 | `evolution_config` |

### 7.2 OpenClaw vs Hermes 对比参考

| 能力维度 | OpenClaw | Hermes | 我们的方案 |
|----------|----------|--------|------------|
| **角色定义** | Workspace 文件系统 | 单体 Agent | `RoleDefinition` 数据对象 |
| **技能创建** | 手动创建 | **自动生成** | 支持两种方式 |
| **技能迭代** | 手动编辑 | **自动优化** | 可选开启（`evolution_config`） |
| **记忆系统** | Markdown 文件 | SQLite + FTS5 | 分层记忆 + 可选 SQLite |
| **用户画像** | USER.md 手动维护 | **Honcho 自动学习** | 可选开启（`memory_config`） |
| **权限控制** | Agent 级 Skills 白名单 | 最小权限原则 | `PermissionConfig` + 工具级限制 |
| **MCP 支持** | 非原生 | **原生支持** | 规划中 |

### 7.3 技能系统设计要点

#### 7.3.1 技能定义结构

```python
class SkillDefinition(BaseModel):
    """技能定义"""
    
    skill_id: str                     # 唯一标识
    skill_name: str                   # 显示名称
    description: str                  # 描述
    
    # 技能组成
    tools: List[str]                  # 依赖的工具列表
    steps: List[SkillStep]            # 执行步骤
    validation: List[str]             # 验证条件
    on_failure: List[str]             # 失败处理
    
    # 元信息
    created_by: str                   # "system" | "assistant" | "evolution"
    auto_generated: bool              # 是否自动生成
    version: str
    usage_count: int                  # 使用次数（用于迭代优化）


class SkillStep(BaseModel):
    """技能执行步骤"""
    tool: str                         # 工具名称
    params: Dict[str, Any]            # 参数（支持模板变量）
    condition: Optional[str]          # 执行条件
```

#### 7.3.2 技能自动生成（参考 Hermes）

```
任务完成流程：

1. Agent 完成复杂任务
   │
   ▼
2. 分析执行轨迹
   - 哪些工具被组合使用
   - 参数传递模式
   - 验证逻辑
   │
   ▼
3. 生成技能定义
   skill_manager.create_skill_from_trace(
       task_trace=execution_trace,
       skill_name="数据迁移流程",
       min_reuse_potential=0.7
   )
   │
   ▼
4. 存储到技能库
   skill_store.save(skill_def)
   │
   ▼
5. 后续自动复用
   当识别到相似任务时，自动推荐使用该技能
```

#### 7.3.3 技能迭代优化

```python
class SkillEvolutionEngine:
    """技能自进化引擎"""
    
    async def improve_skill(
        self,
        skill_id: str,
        usage_feedback: UsageFeedback
    ) -> SkillDefinition:
        """根据使用反馈迭代技能"""
        
        # 1. 分析反馈
        # - 成功率、失败原因、用户修正
        
        # 2. 识别改进点
        # - 参数默认值优化
        # - 步骤顺序调整
        # - 验证条件补充
        
        # 3. 生成新版本
        new_skill = self.generate_improved_skill(
            current_skill=skill_store.get(skill_id),
            feedback=usage_feedback
        )
        
        # 4. 保存新版本
        skill_store.save(new_skill)
        
        return new_skill
```

### 7.4 记忆系统设计要点

#### 7.4.1 三层记忆架构

```
┌─────────────────────────────────────────────────────────┐
│                    工作记忆（Session 级）                 │
│  - 滑动窗口：最近 N 条消息                               │
│  - Token 限制：~4000                                    │
│  - 压缩策略：`memory_config.compression_threshold`       │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                    对话记忆（项目级）                      │
│  - 完整对话历史                                          │
│  - 持久化存储：JSONL / SQLite                           │
│  - 保留策略：`memory_config.retention_days`             │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                    长期记忆（跨项目）                      │
│  - 向量存储（可选）                                      │
│  - 项目经验、决策记录                                     │
│  - 用户偏好学习（`memory_config.enable_user_profile`）   │
└─────────────────────────────────────────────────────────┘
```

#### 7.4.2 冻结快照模式（参考 Hermes）

```python
class FrozenSnapshot:
    """冻结快照 - 降低 Token 消耗"""
    
    def create_snapshot(self, messages: List[Message]):
        """创建不可变的对话快照"""
        # 1. 计算快照 Hash
        snapshot_hash = self.compute_hash(messages)
        
        # 2. 存储快照
        self.snapshot_store.save(
            hash=snapshot_hash,
            content=messages,
            created_at=datetime.now()
        )
        
        # 3. 后续请求复用快照
        # 只发送快照 Hash + 新消息
        # LLM 服务端可根据 Hash 恢复上下文
        return snapshot_hash
```

**优势：**
- 显著降低 API Token 消耗
- 支持 Prompt Cache（Anthropic 原生优化）
- 避免重复发送相同上下文

### 7.5 权限控制设计要点

#### 7.5.1 多层权限控制

```
权限检查流程：

Agent 请求操作
       │
       ├─ 第1层：角色级工具权限
       │    工具是否在 role_def.tools 中？
       │
       ├─ 第2层：角色级技能权限
       │    技能是否在 role_def.skills 中？
       │
       ├─ 第3层：路径权限
       │    路径是否在 permissions.read/write_paths 中？
       │
       ├─ 第4层：工具级限制
       │    是否满足 permissions.tool_restrictions？
       │    例如：code_execute 的 max_timeout
       │
       └─ 第5层：运行时安全检查
            沙箱隔离、敏感操作审计
```

#### 7.5.2 工具级限制示例

```python
# Tech Lead 角色定义
role_def = RoleDefinition(
    role_id="tech_lead",
    tools=["code_execute", "git_commit", ...],
    permissions=PermissionConfig(
        read_paths=["src/", "docs/", "tests/"],
        write_paths=["docs/", "tasks/"],
        forbidden_paths=["deploy/", "secrets/"],
        tool_restrictions={
            "code_execute": {
                "max_timeout": 30,           # 最多执行 30 秒
                "sandbox": True,              # 必须在沙箱中执行
                "forbidden_commands": ["rm -rf"]
            },
            "git_commit": {
                "require_review": True,       # 需要代码审查
                "protected_branches": ["main", "master"]
            }
        }
    )
)
```

---

## 九、相关文档

- [../teams/design/设计优化方案.md](../../teams/design/设计优化方案.md)
- [../teams/design/阶段0-项目启动.md](../../teams/design/阶段0-项目启动.md)
- [../teams/design/工作流编排讨论.md](../../teams/design/工作流编排讨论.md)
- [../teams/architecture/仓库拆分方案.md](../../teams/architecture/仓库拆分方案.md)
- [../teams/architecture/开发路线图与优先级.md](../../teams/architecture/开发路线图与优先级.md)

### 参考框架

- **OpenClaw**: 多 Agent 编排平台，企业级管控
  - Workspace 文件系统
  - 52+ 内置 Skills
  - 权限控制体系
- **Hermes Agent**: 自进化智能体
  - 自动技能生成
  - SQLite + FTS5 记忆系统
  - 冻结快照模式
  - Honcho 用户画像引擎

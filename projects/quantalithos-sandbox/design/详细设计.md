# quantalithos-sandbox 详细设计

> 项目沙箱环境 — 三种模式（dev/preview/build），Docker 容器化，Agent 隔离执行

---

## 一、模块架构

```
runtime 的 Agent 工具                    console / chat
  │                                        │
  │ SandboxProvider 接口                   │ GET /sandbox/:id/status
  │ (core 定义，sandbox 实现)              │
  ▼                                        ▼
┌─────────────────────────────────────────────────────────────┐
│  sandbox-manager (Rust, axum, :8004)                         │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  api/                                                  │  │
│  │  创建 / 销毁 / 暂停 / 恢复 / 切换模式                  │  │
│  │  exec / write-file / read-file / logs                  │  │
│  └──────────────────────────┬────────────────────────────┘  │
│                             │                               │
│  ┌──────────────────────────▼────────────────────────────┐  │
│  │  docker/                                               │  │
│  │  compose_generator  → 生成 docker-compose.yml          │  │
│  │  container_manager  → bollard Docker API               │  │
│  │  port_allocator     → 端口分配（避免冲突）              │  │
│  └──────────────────────────┬────────────────────────────┘  │
│                             │                               │
│  ┌──────────────────────────▼────────────────────────────┐  │
│  │  gitea/                                                │  │
│  │  client  → Gitea API（创建仓库 / webhook）             │  │
│  └───────────────────────────────────────────────────────┘  │
│                             │                               │
│                    ┌────────┴────────┐                      │
│                    ▼                 ▼                      │
│              Docker Engine        Gitea :3000               │
│              (容器生命周期)        (Git 仓库)                │
└─────────────────────────────────────────────────────────────┘
```


---

## 二、数据结构

### 2.1 SandboxInstance

```rust
// src/models.rs

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct SandboxInstance {
    pub id: String,                       // "sandbox-blog-001"
    pub project_id: String,               // "blog"
    pub mode: SandboxMode,                // dev | preview | build
    pub status: SandboxStatus,            // creating | running | paused | stopped | failed
    pub tech_stack: TechStack,            // 从 platform 读取的技术栈配置
    pub containers: Vec<ContainerInfo>,   // 当前运行的容器列表
    pub ports: PortMapping,               // 端口映射
    pub created_at: DateTime<Utc>,
    pub updated_at: DateTime<Utc>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum SandboxMode {
    Dev,
    Preview,
    Build,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum SandboxStatus {
    Creating,
    Running,
    Paused,
    Stopped,
    Failed,
}
```

### 2.2 TechStack（从 platform 读取）

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct TechStack {
    pub backend: Option<StackConfig>,
    pub frontend: Option<StackConfig>,
    pub database: Option<DatabaseConfig>,
    pub extra_services: Vec<ServiceConfig>,  // Redis / Elasticsearch / ...
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct StackConfig {
    pub language: String,             // "rust" | "python" | "node" | "go" | "java"
    pub framework: Option<String>,    // "axum" | "fastapi" | "vue3" | "react"
    pub version: Option<String>,      // "1.75" | "3.12" | "20"
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DatabaseConfig {
    pub db_type: String,              // "postgresql" | "mysql" | "mongodb"
    pub version: String,              // "16" | "8.0" | "7"
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ServiceConfig {
    pub name: String,                 // "redis" | "elasticsearch"
    pub image: String,                // "redis:7-alpine"
    pub ports: Vec<String>,           // ["6379:6379"]
}
```

### 2.3 ContainerInfo

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ContainerInfo {
    pub container_id: String,         // Docker container ID
    pub name: String,                 // "sandbox-blog-be1"
    pub role: ContainerRole,          // agent / shared_service / preview / build
    pub agent_id: Option<String>,     // dev 模式下关联的 Agent ID
    pub image: String,                // "rust:latest"
    pub status: String,               // "running" | "exited" | "paused"
    pub ports: Vec<PortMap>,          // 端口映射
    pub created_at: DateTime<Utc>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum ContainerRole {
    Agent,                            // dev 模式：Agent 独立容器
    SharedService,                    // 共享服务（数据库 / Redis）
    Preview,                          // preview 模式：统一部署容器
    Builder,                          // build 模式：构建容器
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PortMap {
    pub host_port: u16,               // 宿主机端口
    pub container_port: u16,          // 容器内端口
    pub protocol: String,             // "tcp" | "udp"
}
```

### 2.4 PortMapping

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PortMapping {
    pub base_port: u16,               // 项目基础端口（如 4000）
    pub allocations: HashMap<String, u16>,  // "backend" → 4001, "frontend" → 4002, "db" → 4003
}
```

### 2.5 ExecRequest / ExecResult

```rust
/// Agent 工具调用 sandbox 执行命令
#[derive(Debug, Serialize, Deserialize)]
pub struct ExecRequest {
    pub agent_id: String,             // 在哪个 Agent 的容器中执行
    pub command: String,              // "cargo test"
    pub working_dir: Option<String>,  // 工作目录，默认 /workspace
    pub timeout_secs: Option<u64>,    // 超时，默认 300 秒
    pub env: HashMap<String, String>, // 额外环境变量
}

#[derive(Debug, Serialize, Deserialize)]
pub struct ExecResult {
    pub exit_code: i32,
    pub stdout: String,
    pub stderr: String,
    pub duration_ms: u64,
}
```

### 2.6 FileRequest

```rust
#[derive(Debug, Serialize, Deserialize)]
pub struct WriteFileRequest {
    pub agent_id: String,
    pub path: String,                 // 相对于 /workspace 的路径
    pub content: String,
    pub create_dirs: bool,            // 自动创建父目录
}

#[derive(Debug, Serialize, Deserialize)]
pub struct ReadFileRequest {
    pub agent_id: String,
    pub path: String,
}

#[derive(Debug, Serialize, Deserialize)]
pub struct ReadFileResult {
    pub content: String,
    pub size: u64,
    pub modified_at: DateTime<Utc>,
}
```


---

## 三、核心流程 — dev 模式创建

用博客系统举例，TL 决定技术栈后创建沙箱：

```
TL 调用 sandbox_create 工具
         │
         │ runtime → SandboxProvider → sandbox-manager API
         ▼
POST /sandbox/create
{
  project_id: "blog",
  mode: "dev",
  tech_stack: {
    backend: { language: "rust", framework: "axum" },
    frontend: { language: "node", framework: "vue3" },
    database: { db_type: "postgresql", version: "16" }
  },
  agents: [
    { agent_id: "be-1", repos: ["blog-backend"], image_hint: "rust" },
    { agent_id: "fe-1", repos: ["blog-frontend"], image_hint: "node" },
    { agent_id: "qa-1", repos: ["blog-backend", "blog-frontend"], image_hint: "rust-node" },
    { agent_id: "tl",   repos: ["blog-backend", "blog-frontend"], image_hint: "rust-node" },
  ]
}
```

```
sandbox-manager 内部处理流程：

  ① 分配端口
     port_allocator.allocate("blog")
     → base_port: 4000
     → backend: 4001, frontend: 4002, db: 4003, redis: 4004

  ② 生成 docker-compose.yml
     compose_generator.generate(config)
     → 见下方生成结果

  ③ 启动容器组
     container_manager.up("sandbox-blog")
     → docker-compose -f /sandboxes/blog/docker-compose.yml up -d

  ④ 等待健康检查
     → PostgreSQL ready? ✓
     → Redis ready? ✓
     → Agent 容器 ready? ✓

  ⑤ 上报状态到 platform
     POST /api/projects/blog/sandbox/status
     → { mode: "dev", status: "running", containers: [...], ports: {...} }

  ⑥ 返回结果给 TL
     → { sandbox_id: "sandbox-blog-001", status: "running", ports: {...} }
```

### 3.1 生成的 docker-compose.yml

```yaml
# /sandboxes/blog/docker-compose.yml（由 compose_generator 自动生成）

version: "3.8"

networks:
  blog_net:
    driver: bridge

volumes:
  blog_pgdata:
  blog_redis:
  be1_cargo_cache:
  fe1_node_modules:

services:
  # ── 共享服务 ──
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: blog
      POSTGRES_USER: blog
      POSTGRES_PASSWORD: dev_password
    ports:
      - "4003:5432"
    volumes:
      - blog_pgdata:/var/lib/postgresql/data
    networks: [blog_net]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U blog"]
      interval: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "4004:6379"
    volumes:
      - blog_redis:/data
    networks: [blog_net]

  # ── Agent 容器 ──
  be-1:
    image: rust:latest
    working_dir: /workspace
    command: ["sleep", "infinity"]    # 保持容器运行，等待 exec 命令
    environment:
      DATABASE_URL: postgres://blog:dev_password@db:5432/blog
      REDIS_URL: redis://redis:6379
    ports:
      - "4001:8080"                   # 后端服务端口
    volumes:
      - be1_cargo_cache:/usr/local/cargo/registry
    networks: [blog_net]
    depends_on:
      db: { condition: service_healthy }

  fe-1:
    image: node:20
    working_dir: /workspace
    command: ["sleep", "infinity"]
    ports:
      - "4002:5173"                   # Vite dev server 端口
    volumes:
      - fe1_node_modules:/workspace/node_modules
    networks: [blog_net]

  qa-1:
    image: rust-node:latest           # 自定义镜像（Rust + Node.js）
    working_dir: /workspace
    command: ["sleep", "infinity"]
    environment:
      DATABASE_URL: postgres://blog:dev_password@db:5432/blog
    networks: [blog_net]
    depends_on:
      db: { condition: service_healthy }

  tl:
    image: rust-node:latest
    working_dir: /workspace
    command: ["sleep", "infinity"]
    networks: [blog_net]
```

### 3.2 compose_generator 实现

```rust
// docker/compose_generator.rs

pub struct ComposeGenerator {
    templates: TemplateRegistry,
}

impl ComposeGenerator {
    /// 根据项目配置生成 docker-compose.yml
    pub fn generate(&self, config: &CreateSandboxRequest) -> Result<String, SandboxError> {
        let mut compose = ComposeFile::new();

        // 1. 创建网络
        let network_name = format!("{}_net", config.project_id);
        compose.add_network(&network_name);

        // 2. 添加共享服务
        if let Some(db) = &config.tech_stack.database {
            let template = self.templates.get_db_template(&db.db_type, &db.version)?;
            compose.add_service("db", template.with_port(config.ports.get("db")));
        }
        for svc in &config.tech_stack.extra_services {
            compose.add_service(&svc.name, ServiceDef::from_config(svc));
        }

        // 3. 添加 Agent 容器
        for agent in &config.agents {
            let image = self.resolve_image(&agent.image_hint, &config.tech_stack)?;
            let svc = ServiceDef {
                image,
                working_dir: "/workspace".into(),
                command: vec!["sleep".into(), "infinity".into()],
                environment: self.build_env(&config.tech_stack),
                ports: self.agent_ports(agent, &config.ports),
                volumes: self.agent_volumes(agent, &config.project_id),
                networks: vec![network_name.clone()],
                depends_on: self.agent_depends_on(&config.tech_stack),
            };
            compose.add_service(&agent.agent_id, svc);
        }

        // 4. 序列化为 YAML
        Ok(compose.to_yaml()?)
    }

    /// 根据 image_hint 选择基础镜像
    fn resolve_image(&self, hint: &str, stack: &TechStack) -> Result<String, SandboxError> {
        match hint {
            "rust" => Ok(format!("rust:{}", stack.backend.as_ref()
                .and_then(|b| b.version.as_deref()).unwrap_or("latest"))),
            "node" => Ok(format!("node:{}", stack.frontend.as_ref()
                .and_then(|f| f.version.as_deref()).unwrap_or("20"))),
            "python" => Ok(format!("python:{}", stack.backend.as_ref()
                .and_then(|b| b.version.as_deref()).unwrap_or("3.12"))),
            "rust-node" => Ok("quantalithos/rust-node:latest".into()),
            _ => Err(SandboxError::UnsupportedImage(hint.into())),
        }
    }
}
```


---

## 四、核心流程 — Agent 在容器内执行

用 BE-1 执行 TASK-003 标签系统举例：

```
完整调用链：

  BE-1 Agent                runtime                 sandbox-manager          Docker
  ──────────               ───────                 ───────────────          ──────
       │                      │                          │                    │
       │  think: 需要写标签模型│                          │                    │
       │                      │                          │                    │
       │  act: file_write     │                          │                    │
       │  ("src/models/tag.py"│                          │                    │
       │   content)           │                          │                    │
       │ ──────────────────>  │                          │                    │
       │                      │  SandboxProvider         │                    │
       │                      │  .write_file()           │                    │
       │                      │ ──────────────────────>  │                    │
       │                      │                          │  POST /sandbox/    │
       │                      │                          │  sandbox-blog-001/ │
       │                      │                          │  write-file        │
       │                      │                          │ ────────────────>  │
       │                      │                          │  docker exec be-1  │
       │                      │                          │  写入文件           │
       │                      │                          │ <────────────────  │
       │                      │                          │  Ok                │
       │                      │ <──────────────────────  │                    │
       │ <──────────────────  │  Ok                      │                    │
       │                      │                          │                    │
       │  act: code_execute   │                          │                    │
       │  ("cargo test")      │                          │                    │
       │ ──────────────────>  │                          │                    │
       │                      │  SandboxProvider         │                    │
       │                      │  .exec()                 │                    │
       │                      │ ──────────────────────>  │                    │
       │                      │                          │  docker exec be-1  │
       │                      │                          │  cargo test        │
       │                      │                          │ ────────────────>  │
       │                      │                          │                    │
       │                      │                          │  ... 编译运行 ...   │
       │                      │                          │                    │
       │                      │                          │ <────────────────  │
       │                      │                          │  exit_code: 0      │
       │                      │                          │  stdout: 12 passed │
       │                      │ <──────────────────────  │                    │
       │ <──────────────────  │  ExecResult              │                    │
       │  {exit_code: 0,      │                          │                    │
       │   stdout: "12 passed"│                          │                    │
       │  }                   │                          │                    │
```

### 4.1 exec 实现（最核心的 handler）

```rust
// api/exec.rs

pub async fn exec(
    State(state): State<AppState>,
    Path(sandbox_id): Path<String>,
    Json(req): Json<ExecRequest>,
) -> Result<Json<ExecResult>, SandboxError> {
    // 1. 查找 sandbox 实例
    let sandbox = state.instances.get(&sandbox_id)
        .ok_or(SandboxError::NotFound(sandbox_id.clone()))?;

    // 2. 查找 Agent 对应的容器
    let container = sandbox.containers.iter()
        .find(|c| c.agent_id.as_deref() == Some(&req.agent_id))
        .ok_or(SandboxError::AgentNotFound(req.agent_id.clone()))?;

    // 3. 在容器内执行命令
    let exec_opts = bollard::exec::CreateExecOptions {
        cmd: Some(vec!["/bin/sh", "-c", &req.command]),
        working_dir: Some(req.working_dir.as_deref().unwrap_or("/workspace")),
        env: Some(req.env.iter().map(|(k, v)| format!("{}={}", k, v)).collect()),
        attach_stdout: Some(true),
        attach_stderr: Some(true),
        ..Default::default()
    };

    let exec_id = state.docker.create_exec(&container.container_id, exec_opts).await?;
    let start = std::time::Instant::now();

    // 4. 启动执行（带超时）
    let timeout = Duration::from_secs(req.timeout_secs.unwrap_or(300));
    let output = tokio::time::timeout(
        timeout,
        state.docker.start_exec(&exec_id.id, None),
    ).await
        .map_err(|_| SandboxError::ExecTimeout(req.command.clone()))?
        .map_err(SandboxError::DockerError)?;

    // 5. 收集输出
    let (stdout, stderr) = collect_output(output).await;
    let inspect = state.docker.inspect_exec(&exec_id.id).await?;

    Ok(Json(ExecResult {
        exit_code: inspect.exit_code.unwrap_or(-1) as i32,
        stdout,
        stderr,
        duration_ms: start.elapsed().as_millis() as u64,
    }))
}
```

### 4.2 write_file 实现

```rust
// api/files.rs

pub async fn write_file(
    State(state): State<AppState>,
    Path(sandbox_id): Path<String>,
    Json(req): Json<WriteFileRequest>,
) -> Result<StatusCode, SandboxError> {
    let sandbox = state.instances.get(&sandbox_id)
        .ok_or(SandboxError::NotFound(sandbox_id.clone()))?;

    let container = sandbox.containers.iter()
        .find(|c| c.agent_id.as_deref() == Some(&req.agent_id))
        .ok_or(SandboxError::AgentNotFound(req.agent_id.clone()))?;

    // 1. 如果需要创建父目录
    if req.create_dirs {
        let dir = std::path::Path::new(&req.path).parent()
            .map(|p| p.to_string_lossy().to_string())
            .unwrap_or_default();
        if !dir.is_empty() {
            let mkdir_cmd = format!("mkdir -p /workspace/{}", dir);
            exec_in_container(&state.docker, &container.container_id, &mkdir_cmd).await?;
        }
    }

    // 2. 写入文件（通过 docker cp 或 exec echo）
    //    小文件用 exec，大文件用 docker cp
    let full_path = format!("/workspace/{}", req.path);
    if req.content.len() < 65536 {
        // 小文件：base64 编码后 exec 写入
        let encoded = base64::encode(&req.content);
        let cmd = format!("echo '{}' | base64 -d > {}", encoded, full_path);
        exec_in_container(&state.docker, &container.container_id, &cmd).await?;
    } else {
        // 大文件：docker cp
        let tar = create_tar_with_file(&req.path, req.content.as_bytes())?;
        state.docker.upload_to_container(
            &container.container_id,
            bollard::container::UploadToContainerOptions {
                path: "/workspace",
                ..Default::default()
            },
            tar.into(),
        ).await?;
    }

    Ok(StatusCode::CREATED)
}
```

### 4.3 git 操作

```rust
// Agent 的 git_commit 和 git_push 本质上是 exec 的封装

// git_commit：在 Agent 容器内执行 git add + git commit
pub async fn git_commit(sandbox_id: &str, agent_id: &str, message: &str) -> Result<ExecResult> {
    // 先 git add
    let add_result = exec(sandbox_id, ExecRequest {
        agent_id: agent_id.into(),
        command: "git add -A".into(),
        ..Default::default()
    }).await?;

    if add_result.exit_code != 0 {
        return Err(SandboxError::GitError(add_result.stderr));
    }

    // 再 git commit
    exec(sandbox_id, ExecRequest {
        agent_id: agent_id.into(),
        command: format!(
            "git commit -m '{}' --author='{} <{}@quantalithos.ai>'",
            message, agent_id, agent_id
        ),
        ..Default::default()
    }).await
}

// git_push：在 Agent 容器内执行 git push 到 Gitea
pub async fn git_push(sandbox_id: &str, agent_id: &str) -> Result<ExecResult> {
    exec(sandbox_id, ExecRequest {
        agent_id: agent_id.into(),
        command: "git push origin HEAD".into(),
        ..Default::default()
    }).await
}
```


---

## 五、核心流程 — preview 模式

```
TL 调用 sandbox_switch(mode="preview")

  sandbox-manager 内部处理：

  ① 停止 dev 模式的 Agent 容器
     docker-compose stop be-1 fe-1 qa-1 tl
     （保留共享服务：db / redis）

  ② 在 Agent 容器中构建产物
     docker exec be-1 "cd /workspace && cargo build --release"
     docker exec fe-1 "cd /workspace && npm run build"

  ③ 启动 preview 容器
     ┌─ preview 容器 ──────────────────────────────┐
     │                                              │
     │  Nginx 配置：                                 │
     │  ├── / → /usr/share/nginx/html/（Vue 产物）  │
     │  └── /api → http://localhost:8080（Rust 后端）│
     │                                              │
     │  后端二进制：/app/blog-backend（release 版）   │
     │  前端产物：/usr/share/nginx/html/（dist/）    │
     │  数据库：复用 dev 模式的 PostgreSQL            │
     │                                              │
     │  暴露 :8080                                   │
     └──────────────┬───────────────────────────────┘
                    │
  ④ 注入测试数据
     docker exec preview "psql -f /seeds/test_data.sql"

  ⑤ 注册到 gate 反向代理
     POST gate /api/preview-routes
     { project_id: "blog", target: "http://sandbox-blog-preview:8080" }
     → 用户可通过 https://preview-blog.quantalithos.local 访问

  ⑥ 上报状态
     POST platform /api/projects/blog/sandbox/status
     { mode: "preview", status: "running", preview_url: "https://preview-blog.quantalithos.local" }
```

```
用户体验博客系统：

  用户在 console 看到：
  ┌─────────────────────────────────────────────┐
  │  博客系统 · 沙箱环境                          │
  │                                             │
  │  模式：preview ● 运行中                      │
  │  预览地址：https://preview-blog.quant...     │
  │  [打开预览]  [切换到 dev]  [开始构建]         │
  │                                             │
  │  容器状态：                                   │
  │  ├── preview   ● running   CPU 12%  RAM 256M│
  │  ├── db        ● running   CPU 3%   RAM 128M│
  │  └── redis     ● running   CPU 1%   RAM 32M │
  └─────────────────────────────────────────────┘

  用户点击"打开预览"：
  → 浏览器打开 https://preview-blog.quantalithos.local
  → 看到博客首页
  → 可以试试发文章、加标签、搜索
  → 体验和正式版一样（只是数据是测试数据）
```

---

## 六、核心流程 — build 模式

```
TL 调用 sandbox_switch(mode="build")

  sandbox-manager 内部处理：

  ① 启动 build 容器
     ┌─ build 容器 ────────────────────────────────┐
     │                                              │
     │  1. git pull 最新代码（从 Gitea）             │
     │     git clone http://gitea:3000/q/blog-backend.git
     │     git clone http://gitea:3000/q/blog-frontend.git
     │                                              │
     │  2. 后端构建                                  │
     │     cd blog-backend && cargo build --release  │
     │     → /target/release/blog-backend            │
     │                                              │
     │  3. 前端构建                                  │
     │     cd blog-frontend && npm ci && npm run build│
     │     → /dist/                                  │
     │                                              │
     │  4. 运行集成测试                              │
     │     cargo test --release                      │
     │     npm test                                  │
     │     → 全部通过 ✓                              │
     │                                              │
     │  5. 打包                                      │
     │     ├── Docker 镜像：blog-backend:v0.3        │
     │     ├── 静态文件：blog-frontend-v0.3.tar.gz   │
     │     └── Web 应用包：blog-v0.3.zip             │
     │         （后端二进制 + 前端产物 + Nginx 配置）  │
     └──────────────────────────────────────────────┘

  ② 上传构建产物到 platform
     POST /api/projects/blog/builds
     {
       version: "0.3",
       build_type: "web_app",
       artifacts: [
         { name: "blog-backend", type: "binary", size: 12_000_000 },
         { name: "blog-frontend", type: "static", size: 3_500_000 },
         { name: "blog-v0.3.zip", type: "web_app_bundle", size: 15_500_000 },
       ],
       test_result: { total: 22, passed: 22, failed: 0 },
       git_commit: "abc123",
     }

  ③ 上报状态
     { mode: "build", status: "completed", build_id: "build-blog-003" }
```

```
构建完成后 runner 可以下载：

  runner App：
  ┌─────────────────────────────────────┐
  │  博客系统                            │
  │  v0.3 · 刚刚构建                    │
  │  [下载并运行]                        │
  └─────────────────────────────────────┘
       │
       │ GET /api/builds/build-blog-003/download
       ▼
  下载 blog-v0.3.zip → 解压 → WebView 加载
```

---

## 七、SandboxProvider 接口（core 定义）

```python
# core: sandbox/provider.py

class SandboxProvider(ABC):
    """沙箱提供者接口 — runtime 的工具通过此接口调用 sandbox"""

    @abstractmethod
    async def exec(self, sandbox_id: str, req: ExecRequest) -> ExecResult:
        """在指定 Agent 的容器内执行命令"""

    @abstractmethod
    async def write_file(self, sandbox_id: str, req: WriteFileRequest) -> None:
        """写文件到指定 Agent 的容器"""

    @abstractmethod
    async def read_file(self, sandbox_id: str, req: ReadFileRequest) -> ReadFileResult:
        """读取指定 Agent 容器内的文件"""

    @abstractmethod
    async def git_commit(self, sandbox_id: str, agent_id: str, message: str) -> ExecResult:
        """在 Agent 容器内 git commit"""

    @abstractmethod
    async def git_push(self, sandbox_id: str, agent_id: str) -> ExecResult:
        """在 Agent 容器内 git push"""

    @abstractmethod
    async def get_service_url(self, sandbox_id: str, service: str) -> str:
        """获取共享服务的 URL（如数据库连接串）"""


class HttpSandboxProvider(SandboxProvider):
    """通过 HTTP 调用 sandbox-manager API 的实现"""

    def __init__(self, base_url: str):
        self.base_url = base_url  # "http://sandbox-manager:8004"
        self.client = httpx.AsyncClient()

    async def exec(self, sandbox_id: str, req: ExecRequest) -> ExecResult:
        resp = await self.client.post(
            f"{self.base_url}/sandbox/{sandbox_id}/exec",
            json=req.dict(),
        )
        resp.raise_for_status()
        return ExecResult(**resp.json())

    async def write_file(self, sandbox_id: str, req: WriteFileRequest) -> None:
        resp = await self.client.post(
            f"{self.base_url}/sandbox/{sandbox_id}/write-file",
            json=req.dict(),
        )
        resp.raise_for_status()

    # ... 其他方法类似
```

```
runtime 的工具如何使用 SandboxProvider：

  # runtime: tools/code_execute.py

  async def code_execute_handler(args: dict, ctx: AgentExecutionContext) -> ToolResult:
      sandbox_id = ctx.sandbox_id          # 从执行上下文获取
      agent_id = ctx.agent.agent_id

      result = await ctx.sandbox_provider.exec(
          sandbox_id,
          ExecRequest(
              agent_id=agent_id,
              command=args["command"],
              timeout_secs=args.get("timeout", 300),
          )
      )

      return ToolResult(
          success=result.exit_code == 0,
          output=result.stdout,
          error=result.stderr if result.exit_code != 0 else "",
      )
```

---

## 八、端口分配

```rust
// docker/port_allocator.rs

pub struct PortAllocator {
    base: u16,                        // 起始端口（如 4000）
    step: u16,                        // 每个项目占用的端口数（如 10）
    allocated: HashMap<String, u16>,  // project_id → base_port
}

impl PortAllocator {
    /// 为项目分配一组端口
    pub fn allocate(&mut self, project_id: &str) -> PortMapping {
        let base = self.next_available();
        self.allocated.insert(project_id.to_string(), base);

        PortMapping {
            base_port: base,
            allocations: HashMap::from([
                ("backend".into(), base + 1),
                ("frontend".into(), base + 2),
                ("db".into(), base + 3),
                ("redis".into(), base + 4),
                ("preview".into(), base + 5),
                // base + 6..9 预留
            ]),
        }
    }

    fn next_available(&self) -> u16 {
        let max = self.allocated.values().max().copied().unwrap_or(self.base);
        max + self.step
    }
}
```

```
端口分配示例：

  博客项目：base = 4000
    backend  :4001
    frontend :4002
    db       :4003
    redis    :4004
    preview  :4005

  电商项目：base = 4010
    backend  :4011
    frontend :4012
    db       :4013
    redis    :4014
    preview  :4015
```

---

## 九、错误处理

```rust
// src/error.rs

#[derive(Debug, thiserror::Error)]
pub enum SandboxError {
    #[error("Sandbox not found: {0}")]
    NotFound(String),

    #[error("Agent not found in sandbox: {0}")]
    AgentNotFound(String),

    #[error("Docker error: {0}")]
    DockerError(#[from] bollard::errors::Error),

    #[error("Exec timeout: {0}")]
    ExecTimeout(String),

    #[error("Unsupported image: {0}")]
    UnsupportedImage(String),

    #[error("Git error: {0}")]
    GitError(String),

    #[error("Port allocation failed: no available ports")]
    PortExhausted,

    #[error("Mode switch failed: {0}")]
    ModeSwitchFailed(String),

    #[error("Build failed: {0}")]
    BuildFailed(String),
}
```

---

## 十、Phase 分期

### Phase 1

| 模块 | 范围 |
|------|------|
| api | create / destroy / exec / write-file / read-file / status |
| docker | compose_generator（dev 模式）+ container_manager（bollard） |
| templates | Rust / Node.js / Python 开发容器模板 + PostgreSQL / Redis 共享服务模板 |
| port_allocator | 静态端口分配 |

Phase 1 的 Git：Agent 在容器内 git init + git commit，不 push 到 Gitea

### Phase 2

| 模块 | 范围 |
|------|------|
| gitea | Gitea 集成（创建仓库 / webhook / git push） |
| api | pause / resume / switch（模式切换） |
| docker | preview 模式模板 + build 模式模板 |
| preview | Nginx 统一部署 + 测试数据注入 + gate 路由注册 |
| build | 构建流水线 + 产物打包 + 上传 platform |

### Phase 3

| 模块 | 范围 |
|------|------|
| 多机 | sandbox 容器分布到多台机器（K8s Pod） |
| 资源限制 | CPU / 内存 / 磁盘配额（cgroups） |
| 快照 | 环境快照（docker commit）+ 恢复 |
| 自定义镜像 | 用户上传 Dockerfile，构建自定义开发环境 |

---

## 十一、项目创建完整流程（多仓库）

用博客系统举例，TL 决定前后端分离，需要两个 Git 仓库：

```
TL 调用 sandbox_create 工具时的完整流程：

  TL Agent                sandbox-manager              Gitea              platform
     │                         │                         │                   │
     │  sandbox_create({       │                         │                   │
     │    project_id: "blog",  │                         │                   │
     │    mode: "dev",         │                         │                   │
     │    tech_stack: {...},   │                         │                   │
     │    agents: [...],       │                         │                   │
     │    repositories: [      │                         │                   │
     │      {name: "blog-backend", type: "backend"},     │                   │
     │      {name: "blog-frontend", type: "frontend"},   │                   │
     │    ]                    │                         │                   │
     │  })                     │                         │                   │
     │ ─────────────────────>  │                         │                   │
     │                         │                         │                   │
     │                         │  ① 创建 Git 仓库        │                   │
     │                         │  POST /api/v1/orgs/     │                   │
     │                         │  quantalithos/repos     │                   │
     │                         │  {name: "blog-backend"} │                   │
     │                         │ ─────────────────────>  │                   │
     │                         │  201 Created            │                   │
     │                         │ <─────────────────────  │                   │
     │                         │                         │                   │
     │                         │  POST .../repos         │                   │
     │                         │  {name: "blog-frontend"}│                   │
     │                         │ ─────────────────────>  │                   │
     │                         │  201 Created            │                   │
     │                         │ <─────────────────────  │                   │
     │                         │                         │                   │
     │                         │  ② 回写 platform        │                   │
     │                         │  POST /api/projects/    │                   │
     │                         │  blog/git/repos         │                   │
     │                         │ ──────────────────────────────────────────> │
     │                         │  [{name: "blog-backend",│                   │
     │                         │    url: "http://gitea:3000/q/blog-backend.git"},
     │                         │   {name: "blog-frontend",                   │
     │                         │    url: "http://gitea:3000/q/blog-frontend.git"}]
     │                         │                         │                   │
     │                         │  ③ 分配端口              │                   │
     │                         │  backend:4001 frontend:4002 db:4003         │
     │                         │                         │                   │
     │                         │  ④ 生成 docker-compose   │                   │
     │                         │  Agent 容器内 git clone  │                   │
     │                         │  各自的仓库              │                   │
     │                         │                         │                   │
     │                         │  ⑤ 启动容器组            │                   │
     │                         │  docker-compose up -d   │                   │
     │                         │                         │                   │
     │                         │  ⑥ 初始化仓库            │                   │
     │                         │  BE-1 容器内：           │                   │
     │                         │  git clone blog-backend │                   │
     │                         │  FE-1 容器内：           │                   │
     │                         │  git clone blog-frontend│                   │
     │                         │                         │                   │
     │                         │  ⑦ 上报状态              │                   │
     │                         │ ──────────────────────────────────────────> │
     │                         │  {mode: "dev", status: "running",           │
     │                         │   containers: [...], ports: {...}}          │
     │                         │                         │                   │
     │  {sandbox_id, status,   │                         │                   │
     │   ports, repos}         │                         │                   │
     │ <─────────────────────  │                         │                   │
```

```
创建后的目录结构（各 Agent 容器内）：

  BE-1 容器：
    /workspace/
    └── blog-backend/          ← git clone 从 Gitea
        ├── .git/
        ├── Cargo.toml
        └── src/

  FE-1 容器：
    /workspace/
    └── blog-frontend/         ← git clone 从 Gitea
        ├── .git/
        ├── package.json
        └── src/

  QA-1 容器（两个仓库都有）：
    /workspace/
    ├── blog-backend/
    └── blog-frontend/

  TL 容器（两个仓库都有）：
    /workspace/
    ├── blog-backend/
    └── blog-frontend/
```

---

## 十二、相关文档

- 概要设计：`projects/quantalithos-sandbox/design/概要设计.md`
- core SandboxProvider：`projects/quantalithos-core/design/概要设计.md`
- runtime 工具系统：`projects/quantalithos-runtime/design/详细设计.md`
- platform sandbox 状态：`projects/quantalithos-platform/design/详细设计.md`
- infra Gitea 部署：`projects/quantalithos-infra/design/详细设计.md`
- console 沙箱监控：`projects/quantalithos-console/design/详细设计.md`
- 端到端流程说明：`docs/design/端到端流程说明.md`

# 实施计划书写规范

> 实施计划回答 **"如何把已完成的设计按可验证顺序落地为代码"**。
> 它位于详细设计、测试方案、验收标准之后，负责定义实施前置条件、阅读清单、阶段顺序、任务边界、提交纪律、测试门禁、验收嵌入和完成判定。
> 实施计划不是详细设计的重复，也不是测试方案、验收报告、部署手册或个人日程表。

---

## 修订记录

| 版本 | 日期 | 讨论主题 | 修订内容 |
|---|---|---|---|
| v0.2 | 2026-05-26 | 提交规范约束补强 | 将旧版实施计划中的 commit 规范完整迁移为新版规则块，并补充设计仓 / 实现仓语言边界 |
| v0.1 | 2026-05-26 | 实施计划换版骨架定稿 | 归档旧版实施计划书写规范，建立以“设计到实现的执行计划”为主轴的新版实施计划书写规范 |

---

## 一、实施计划的目标

实施计划要解决的问题包括：

1. 实施者开始编码前必须阅读哪些文档、确认哪些配置、遵循哪些规范。
2. 当前仓的实施对象、交付物和非交付物是什么。
3. 哪些功能能力、模块、接口、事件、持久化和测试必须按什么顺序落地。
4. 每个实施阶段的输入、输出、完成判定和提交边界是什么。
5. 测试方案和验收标准如何嵌入实施过程，而不是最后补做。
6. 哪些风险、Spike、待确认项必须前置处理。
7. 什么时候可以暂停、回退、变更计划或判定实施完成。

实施计划不回答：

```text
重新定义需求目标、用户故事、功能需求或验收标准
重新定义架构边界、模块拆分、对象模型或协议细节
重写详细设计中的 struct / enum / trait / API / DDL / 伪代码
展开部署拓扑、告警阈值、运维处置步骤或环境参数全集
编写每日排期、人员工时排班或个人待办流水账
```

实施计划的完成标准是：

```text
其他 agent 可以按实施计划先读正确文档、确认正确环境、按正确顺序实施、
按阶段提交、按门禁验证，并知道每个阶段做到什么程度才可以进入下一阶段。
```

---

## 二、与上游 / 下游文档的边界

### 2.1 相对需求文档

实施计划承接需求文档中的用户故事、功能需求、非功能需求和验收方向，但不重写需求。

实施计划中允许写：

- 本轮实施覆盖哪些需求编号。
- 哪些需求暂不实施、延期实施或只做 Spike。
- 每个实施阶段覆盖哪些需求能力。

实施计划中禁止写：

- 新增未在需求文档中出现的用户故事。
- 修改需求优先级。
- 用实施便利性改变需求边界。

### 2.2 相对架构设计

实施计划承接架构设计中的系统位置、依赖方向、通信方式、数据所有权和技术取舍。

实施计划中允许写：

- 阶段顺序如何尊重架构依赖。
- 哪些外部仓、外部服务或事件总线能力是实施前置条件。
- 哪些架构红线必须作为实施门禁。

实施计划中禁止写：

- 重新选择通信方式。
- 重新划分仓边界或模块边界。
- 绕过架构红线先做临时实现。

### 2.3 相对概要设计

实施计划承接概要设计中的主要组成部分、关键对象轮廓、接口骨架、处理流和状态机轮廓。

实施计划中允许写：

- 哪些主要组成部分先落地，哪些后落地。
- 哪些关键对象或接口构成阶段完成判定。
- 哪些处理流要先形成可运行纵切。

实施计划中禁止写：

- 重新组织主要组成部分。
- 把概要设计未确认的对象当作正式实现对象。
- 用对象清单代替功能实施顺序。

### 2.4 相对详细设计

实施计划必须以详细设计为直接输入。

详细设计提供：

- 文件布局、模块实现契约和对象实现契约。
- API / Command / Query / Event / Job 协议契约。
- 逐接口函数级处理流。
- 状态机、持久化、事务、一致性、错误、并发和测试切口。

实施计划继续展开：

- 实施阶段与依赖顺序。
- 阶段任务拆分、编写顺序与提交边界。
- 每阶段需要执行的测试与验收门禁。
- 实施风险、暂停点、回退点和完成判定。

实施计划禁止重写详细设计中的字段、函数、协议、DDL 和伪代码。

### 2.5 相对测试方案与验收标准

实施计划必须引用测试方案和验收标准。

| 下游文档 | 实施计划中允许写 | 实施计划中禁止写 |
|---|---|---|
| 测试方案 | 每个阶段需要执行哪些测试切口、自动化命令和证据归档动作 | 重写完整测试策略、测试用例矩阵或专项测试方案 |
| 验收标准 | 每个阶段对应哪些验收门禁、哪些一票否决项必须提前规避 | 重写完整验收标准或替代验收结论 |

### 2.6 相对部署、运维和配置文档

实施计划可以写实施前必须准备的配置、环境和外部依赖，但不替代部署与运维手册。

---

## 三、实施计划章节主链

正式文档建议使用以下章节顺序：

```text
1. 与上游文档的关系声明
2. 实施目标与范围
3. 实施前置条件与阅读清单
4. 实施对象与交付物清单
5. 实施阶段与依赖顺序
6. 阶段任务拆分、编写顺序与提交边界
7. 测试与验收门禁嵌入
8. 配置、环境与外部依赖准备
9. Spike、风险与待确认事项
10. 回退、暂停与变更控制
11. 提交、评审与交付纪律
12. 实施完成判定
13. 参考
```

### 3.1 主组织轴

实施计划的主组织轴是 **阶段化的可验证功能增量**。

```text
正确组织方式：
  按功能能力 / 纵切链路 / 可验证阶段展开
  每个阶段有明确输入、输出、提交边界、测试门禁和验收门禁

错误组织方式：
  按对象逐个实现
  按函数逐个实现
  按文件逐个实现
  把所有模块混成一笔大提交
  用个人待办替代阶段计划
```

阶段可以引用模块、对象和接口，但阶段本身不应退化成对象清单。

### 3.2 阶段粒度规则

一个实施阶段必须满足：

- 能形成可检查的代码增量。
- 能执行至少一个明确测试切口。
- 能产生明确提交边界。
- 能说明失败时如何暂停、回退或调整。
- 能对应至少一个需求、设计切口或验收门禁。

一个实施阶段不应过大：

```text
错误：
  Phase 1. 完成全部后端

正确：
  Phase 1. 建立成员创建纵切
    覆盖 command -> domain -> repository -> outbox -> query projection 的最小闭环
```

---

## 四、通用输出规则

### 4.1 表 / 图 / 清单使用规则

实施计划线整体采用：

- 表格收稳阶段、任务、依赖、门禁和提交边界。
- ASCII 图表达阶段依赖、外部准备和门禁嵌入。
- 清单表达前置阅读、配置检查和完成判定。
- 短文解释阶段拆分依据、风险和取舍。

其中：

- 表格必须有稳定列名，不能退化成自由 checklist。
- ASCII 图必须使用 `text` 代码块。
- 命令示例必须使用 `bash` 代码块。
- JSON / Proto 示例通常不应出现在实施计划；如确需引用，必须说明来自详细设计或配置设计。
- 不得把详细设计中的完整对象、函数、协议和 DDL 复制到实施计划。

### 4.2 ASCII 图统一格式

所有 ASCII 图必须采用以下结构：

````md
#### <图类型>: <图标题>

```text
<ASCII 图正文>
```

关键说明：
- <图表达了什么实施关系>
- <图没有表达哪些详细设计内容>
- <图中最容易误解的边界是什么>
````

ASCII 图写法约束：

- 只使用稳定文本字符：`+ - | / \ > < v ^ [ ] ( ) : .`
- 不使用 Mermaid、PlantUML、Graphviz 或截图。
- 图中节点名必须与正文中的阶段名、模块名、门禁名或外部依赖名一致。
- 箭头旁必须标注关系类型，例如 `depends_on`、`enables`、`gate`、`read`、`verify`。
- 图后必须有 2~5 条关键说明。
- 图不能承载完整任务表、完整测试用例或完整提交说明。

### 4.3 阶段编号规则

阶段使用 `PH-<两位数字>` 编号。

```text
PH-01 前置准备与仓初始化
PH-02 最小纵切链路
PH-03 查询与投影能力
PH-04 状态机与一致性补强
```

编号规则：

- 编号必须稳定，不因标题微调而变化。
- 阶段之间有依赖时，必须在阶段表中显式写出。
- 不允许用 `第一阶段 / 第二阶段` 替代正式编号。

### 4.4 任务编号规则

阶段内任务使用 `IMPL-<阶段号>-<序号>` 编号。

```text
IMPL-02-01 建立 command handler 骨架
IMPL-02-02 接入 domain aggregate
IMPL-02-03 接入 repository port 与 fake repository test
```

任务规则：

- 任务以功能实施动作命名，不以对象名裸命名。
- 任务必须有完成判定。
- 任务必须归属某个阶段。
- 如果任务大到需要多人协作、独立排期或独立验收，应升级为正式 WorkItem，而不是留在实施计划内部。

### 4.5 门禁编号规则

测试与验收门禁使用 `GATE-<两位数字>` 编号。

```text
GATE-01 编译与格式化通过
GATE-02 核心 command 单元测试通过
GATE-03 最小 API 集成测试通过
GATE-04 验收标准 AC-001~AC-004 可判定
```

门禁规则：

- 门禁必须说明验证方式。
- 门禁必须引用测试方案或验收标准中的编号。
- 门禁必须说明失败后的处理口径。

### 4.6 提交边界规则

实施计划必须提前定义提交边界。

提交边界应按：

```text
可验证功能增量
阶段完成点
风险隔离点
跨模块一致性完成点
```

提交边界不应按：

```text
单个 struct
单个函数
单个文件
当天做了什么
无关改动混合
```

提交前必须确认当前项目的提交规范和项目级 git 用户配置。

提交粒度判断：

| 粒度 | 示例 | 结论 | 原因 |
|---|---|---|---|
| 过细 | 每写一个函数、一个 struct 或一个文件就 commit | 不推荐 | 噪音过多，review 和回退价值低 |
| 适中 | 一个可独立验证的功能增量或阶段边界完成后 commit | 推荐 | 能用一句话描述，能独立验证，便于 review 和回退 |
| 过粗 | 多个无关功能、格式化、测试和文档一次性混入 | 不推荐 | diff 不可审查，失败后难以定位和回退 |

判断规则：

- 这次改动能否用一句话说明。
- 这次改动是否对应一个提交边界。
- 这次改动是否已经通过该边界的测试或验收门禁。
- 这次改动是否混入了无关功能、无关格式化或未确认设计变更。

### 4.7 编写代码顺序规则

实施计划必须说明每个阶段内部的推荐编码顺序。

编码顺序的目标不是规定每一行代码先写什么，而是避免实现者在没有行为边界、测试切口和提交门禁的情况下随意铺代码。

推荐顺序：

```text
1. 阅读本阶段输入
2. 确认本阶段对外契约
3. 建立或更新测试切口
4. 实现核心领域 / 应用逻辑
5. 接入 port / adapter / repository / external client
6. 接入 API / command / query / event / job 入口
7. 补齐错误、状态、事务、幂等、审计和日志
8. 运行本阶段门禁
9. 整理证据和文档同步
10. 按提交边界提交
```

写法要求：

- 编码顺序必须服务于阶段目标，不能变成对象清单。
- 优先锁定外部契约和测试切口，再填内部实现。
- 核心规则、状态转换和事务边界必须早于外围优化。
- 测试、证据和文档同步不能留到所有代码完成之后再补。
- 如果阶段内存在特殊顺序，必须在该阶段小节单独说明。

错误示例：

```text
1. 写所有 struct
2. 写所有 repository
3. 写所有 service
4. 最后写测试
```

问题：

- 这是按对象层堆叠，不是按可验证功能增量推进。
- 直到最后才验证，容易积累大范围返工。
- 无法形成清晰提交边界。

### 4.8 提交时机规则

实施计划必须说明 commit 的时机。

推荐提交时机：

```text
阶段前置准备完成且可独立审查
最小纵切链路打通且门禁通过
高风险 Spike 有明确结论
跨模块一致性修改完成且门禁通过
阶段完成且测试 / 验收证据已归档
设计偏离已回写文档并完成复核
```

不应提交的时机：

```text
代码尚不能编译
阶段门禁未跑或失败
只改了半个对象或半个函数
无关格式化、功能、测试、文档混在一起
设计已经偏离但没有回写文档
为了保存进度而提交不可审查的 WIP
```

写法要求：

- 每笔提交必须对应一个提交边界。
- 提交前必须通过该边界声明的门禁。
- 提交 message 必须遵循项目提交规范，并参考历史提交。
- 项目要求固定 footer 时，只保留项目规定的 footer。
- 除非项目明确允许，不提交 `WIP`。

### 4.9 Commit 规范完整规则

实施计划必须完整说明 commit 规则，不能只写“按规范提交”。如果目标实现仓已有更严格的提交规范，本章应引用目标仓规范，并说明与本规则的差异。

#### 4.9.1 提交时机与粒度

提交时机：

每完成一个功能模块、可验证功能增量或阶段开发任务，必须进行一次 commit。不要攒一堆不相关的改动一次性提交，也不要一个函数一个 commit。

| 粒度 | 判断标准 | 是否合格 | 原因 |
|---|---|---|---|
| 过细 | 每写一个函数、一个 struct 或一个文件就 commit | 不合格 | 噪音太多，git log 不可读，review 价值低 |
| 适中 | 一个可独立验证的功能模块、阶段边界或风险隔离点完成后 commit | 合格 | 能用一句话描述，能独立验证，便于 review 和 revert |
| 过粗 | 一整个大阶段、多个无关功能或格式化 / 测试 / 文档混成一笔 | 不合格 | 无法定位问题，无法回退单个功能 |

判断“该不该 commit”的简单规则：

- 这次改动能用一句话描述吗。能，则可以进入提交判断。
- 这次改动包含两个不相关的功能吗。是，则拆成两个 commit。
- 这次改动的测试和阶段门禁通过了吗。通过，才允许 commit；未通过，不提交半成品。
- 这次改动是否对应 §6 中一个明确提交边界。不能对应，则先调整提交边界。

#### 4.9.2 Commit message 基本结构

commit 必须遵循以下结构：

```text
<type>(<scope>): <subject>

<body>

<footer>
```

说明：

- `scope` 是否必填由项目提交规范决定；如果历史提交使用 `<type>: <subject>`，则可以省略 scope。
- `subject` 必须能一句话说明本次改动目的。
- `body` 必须说明本次提交范围、关键变更、验证结果和证据。
- `footer` 必须遵循项目 footer 策略。

Type 列表：

| Type | 含义 |
|---|---|
| `feat` | 新功能 |
| `fix` | 修复 Bug |
| `refactor` | 重构，不改变外部行为 |
| `docs` | 文档变更 |
| `test` | 测试相关 |
| `chore` | 构建、工具、依赖或杂项维护 |
| `perf` | 性能优化 |
| `ci` | CI / CD 变更 |
| `style` | 代码格式，不影响逻辑 |

Scope 列表按项目实际情况调整。可选示例：

```text
project | workitem | artifact | agent-config | snapshot | event | api | common | design | integration | knowledge
```

#### 4.9.3 语言边界

| 场景 | commit message | 文档 / 代码注释 | 说明 |
|---|---|---|---|
| 设计文档仓 | 允许按当前项目约定使用中文主题和中文正文 | 设计文档、伪代码说明和示例注释允许中文 | 当前 `quantalithos-design` 属于此类 |
| 实际实现代码仓 | 按实现仓自己的提交规范执行；没有单独规范时，默认 subject / body 使用英文 | 源码标识符、rustdoc、普通注释、测试名默认只使用英文 | 不得把设计文档中的中文注释示例直接搬入源码 |

实现代码仓如果确需出现中文，只能作为明确的业务数据、协议样例、国际化资源或测试夹具，并应在实施计划中单独说明原因和落点。

#### 4.9.4 Footer 规范

每次 commit 如果有 AI 参与编写，必须在 footer 中标注 `Co-Authored-By`，除非项目明确规定人工独立提交或不使用 AI 注脚。

通用注脚格式：

```text
Co-Authored-By: <模型显示名> <邮箱>
```

通用 AI 注脚表：

| AI 模型 | 注脚 |
|---|---|
| Claude (Anthropic) | `Co-Authored-By: Claude <noreply@anthropic.com>` |
| ChatGPT (OpenAI) | `Co-Authored-By: ChatGPT <noreply@openai.com>` |
| Codex (OpenAI) | `Co-Authored-By: Codex <noreply@openai.com>` |
| Gemini (Google) | `Co-Authored-By: Gemini <noreply@google.com>` |
| DeepSeek | `Co-Authored-By: DeepSeek <noreply@deepseek.com>` |
| Kiro (AWS) | `Co-Authored-By: Kiro <noreply@kiro.dev>` |
| 其他模型 | `Co-Authored-By: <模型名> <noreply@<厂商域名>>` |

通用规则：

1. 一次 commit 只标注实际参与的模型。人工独立编写时，可以不加 `Co-Authored-By`。
2. 如果多个模型参与同一次 commit，且项目允许多注脚，每个模型各占一行。
3. 模型名写产品名而非版本号。写 `Claude` 不写 `Claude Opus 4`；写 `Codex` 不写 `Codex 5.4`；写 `ChatGPT` 不写 `GPT-4o`。
4. 同一厂商的不同产品线要区分。ChatGPT 和 Codex 虽然都是 OpenAI，但产品定位不同，应分开标注。
5. 邮箱统一用 `noreply@<厂商域名>`，不要用个人邮箱或虚构邮箱。

项目覆盖规则：

- 如果项目规定固定 footer，则以项目固定 footer 为准，不展开通用多模型注脚。
- 当前 `quantalithos-design` 固定 footer 为：

```text
Co-Authored-By: codex <noreply@openai.com>
```

其他 footer 场景：

| 场景 | footer 内容 |
|---|---|
| 关联 issue | `Closes #42` 或 `Refs #42` |
| 破坏性变更 | `BREAKING CHANGE: <description>` |

footer 中各项顺序：

```text
Co-Authored-By
Closes / Refs
BREAKING CHANGE
```

#### 4.9.5 Commit Body 格式

body 是 commit message 中最重要的部分。它应让 reviewer 不看 diff 也能理解这次改动做了什么。

核心规则：

```text
按功能分组，每组一句话总结 + 文件列表。
```

body 的结构：

```text
<功能总结 1，一句话描述这组改动做了什么>:
- <文件名> (+/-<行数>): <一句话描述这个文件的改动>
- <文件名> (+/-<行数>): <一句话描述这个文件的改动>

<功能总结 2，一句话描述这组改动做了什么>:
- <文件名> (+/-<行数>): <一句话描述这个文件的改动>
```

每一行的格式拆解：

| 部分 | 规则 | 正确示例 | 错误示例 |
|---|---|---|---|
| 文件名 | 只写文件名本身，不带目录路径 | `executor.py` | `quantalithos_runtime/engine/executor.py` |
| 行数标记 | `+` 表示新增行数，`-` 表示删除行数，`~` 表示修改行数 | `(+120)` | `(120 lines)` |
| 文件描述 | 一句话说明这个文件改了什么，用动词开头 | `implement multi-round think-act loop` | `think-act loop` |
| 功能总结 | 一句完整描述，说明这组改动的目的，末尾加冒号 | `Scheduling execution lifecycle with start and completion hooks:` | `Task scheduling:` |

语言要求：

- 实际实现代码仓默认使用英文功能总结和英文文件描述。
- 设计文档仓可使用中文功能总结和中文文件描述，但仍必须按功能分组，并避免同一部分内部过度硬换行。

功能总结的写法：

| 写法 | 是否合格 | 原因 |
|---|---|---|
| `Scheduling execution lifecycle with start and completion hooks:` | 合格 | 完整描述了功能目的和范围 |
| `Task scheduling:` | 不合格 | 太笼统，不知道具体做了什么 |
| `Add task_scheduler.rs and handler.rs:` | 不合格 | 描述的是文件而非功能 |
| `收稳实施计划提交规范:` | 仅设计文档仓合格 | 中文总结只适用于允许中文提交正文的文档仓 |

#### 4.9.6 Commit 示例

设计文档仓示例：

```text
docs: 段 4 预备 · 收稳实施计划提交规范

补齐实施计划提交规则:
- 实施计划书写规范.md (+120): 迁移 commit 粒度、message 结构、footer、body 和自检清单
- 实施计划讨论流程_SOP.md (+60): 补充提交纪律讨论问题和输出格式

Co-Authored-By: codex <noreply@openai.com>
```

实现代码仓单功能示例：

```text
feat(workitem): implement state machine validation

- state_machine.rs (+95): add TASK/BUG/REQUIREMENT transition tables
- handler.rs (+5): call validate_transition before status update
- state_machine_test.rs (+120): cover all legal and illegal transitions

Co-Authored-By: Codex <noreply@openai.com>
```

实现代码仓阶段提交示例：

```text
feat(platform): add task scheduling and auto-assign

Scheduling execution lifecycle with start and completion hooks:
- task_scheduler.rs (+180): implement start_execution and on_task_completed
- handler.rs (+45): register start-execution route

Auto-assign workitems based on role-based rules on transition:
- service.rs (+60): implement auto_assign with role-based rules
- handler.rs (+12): call auto_assign in transition handler

Publish lifecycle events for downstream consumers:
- event_bus.rs (+8): add TaskBecameReady and AllTasksCompleted variants

Co-Authored-By: Codex <noreply@openai.com>
```

AI 辅助修复示例：

```text
fix(engine): prevent token advancement when gateway has no matching condition

- state_machine.rs (~3): change silent None return to RuntimeError

Co-Authored-By: Codex <noreply@openai.com>
```

多功能阶段提交示例：

```text
feat(runtime): implement stage-3 core execution path

Message dispatch and agent routing in the main loop:
- main_loop.py (+185): implement dispatch() with message type routing
- main_loop.py (+40): add agent_pool management and dedup window

Think-act execution loop with tool call handling:
- executor.py (+120): implement multi-round ReAct loop with max_rounds guard
- executor.py (+15): append tool results as tool-role messages

Tool permission enforcement and execution:
- tool_executor.py (+95): implement execute() with path and sandbox permission checks
- registry.py (+30): add get_tools_for_agent filtered by role permissions

LLM client with retry and timeout protection:
- client.py (+80): implement chat() with exponential backoff retry
- client.py (+25): add _normalize_response for provider-agnostic output

Co-Authored-By: Codex <noreply@openai.com>
```

#### 4.9.7 Commit 自检清单

每次 commit 前必须对照以下清单检查：

- [ ] subject 是否用一句话概括了这次改动的目的。
- [ ] commit 是否只做了一件事，或一个阶段的完整可验证交付。
- [ ] body 是否按功能分组，每组有一句功能总结。
- [ ] 每个文件是否只写了文件名，不带目录路径。
- [ ] 每个文件是否标注了增删改行数。
- [ ] 每个文件是否有一句话描述改动内容。
- [ ] 功能总结是否是完整句子，而不是名词标签。
- [ ] 如果是实现代码仓，subject、body、源码标识符、rustdoc、普通注释和测试名是否使用英文。
- [ ] 如果是设计文档仓，中文正文是否符合项目历史提交风格，且同一部分没有过度硬换行。
- [ ] 如果有 AI 参与，是否标注了项目要求的 `Co-Authored-By`。
- [ ] 如果项目要求固定 footer，是否只保留项目规定的 footer。
- [ ] 如果项目允许多模型 footer，是否每个实际参与模型各占一行。
- [ ] 该提交边界要求的 fmt / lint / test / acceptance 是否通过。
- [ ] 设计偏离、接口变化或验收口径变化是否已回写文档。

---

## 五、章节级规范

### 5.1 与上游文档的关系声明

本章说明实施计划依赖哪些上游文档，以及每份文档在实施中的作用。

必须包含表格：

| 上游文档 | 版本 / 路径 | 本计划如何使用 | 必须先读 | 风险 |
|---|---|---|---|---|
| `00-需求文档.md` | v0.x | 确认实施覆盖需求编号 | 是 | 需求未收稳会导致阶段返工 |
| `03-详细设计.md` | v0.x | 作为实现契约输入 | 是 | 详细设计不完整会导致实现猜测 |

写法要求：

- 必须列出需求、架构、概要、详细、测试方案、验收标准。
- 如果某份文档不存在，必须说明当前计划是否可以继续，以及补齐责任。
- 不得只写“参考上游文档”。

### 5.2 实施目标与范围

本章说明本轮实施要交付什么、不交付什么。

必须包含表格：

| 类别 | 内容 | 来源 | 是否本轮实施 | 说明 |
|---|---|---|---|---|
| 功能能力 | 创建成员 command 纵切 | FR-001 / DD-API-001 | 是 | 本轮 P0 |
| 非目标 | 管理后台 UI | 需求非目标 | 否 | 另行规划 |

写法要求：

- 范围必须引用需求、设计或验收编号。
- 非范围必须写清楚，避免实施中自然膨胀。
- 不得把“所有内容全部实现”作为目标。

### 5.3 实施前置条件与阅读清单

本章说明实施者开始前必须确认的阅读、配置和规范。

必须包含三类清单。

#### 阅读清单

| 文档 | 路径 | 阅读目的 | 未读风险 | 确认方式 |
|---|---|---|---|---|
| 需求文档 | `<repo>/00-需求文档.md` | 理解功能边界 | 实现偏离需求 | 能说明本轮覆盖 FR |
| Rust 编码规范 | `standards/coding/...` | 统一代码风格与注释 | Review 返工 | 能按规范写 rustdoc |

#### 项目级 Git 配置

```bash
git config user.name "quantalithos-labs"
git config user.email "quantalithos.ai@gmail.com"
git config user.name
git config user.email
```

写法要求：

- 必须说明使用项目级配置，不使用 `--global` 污染全局。
- 必须要求阅读提交规范。
- Rust 项目必须要求阅读 `standards/coding` 下的 Rust 编码规范。
- 如果项目不是 Rust，必须列出对应语言规范；没有规范时要列为待确认风险。

#### 工具与环境前置

| 前置项 | 要求 | 检查命令 / 检查方式 | 失败处理 |
|---|---|---|---|
| Rust toolchain | 与项目约定版本一致 | `rustc --version` | 暂停实施并补齐环境 |
| 数据库 | 本地或测试环境可用 | 连接检查 | 暂停涉及持久化阶段 |

### 5.4 实施对象与交付物清单

本章说明本轮实施会产生哪些代码、测试、配置和文档交付物。

必须包含表格：

| 交付物 | 类型 | 来源章节 | 预计落点 | 完成判定 |
|---|---|---|---|---|
| command handler | code | 详细设计 §5 / §7 | `src/application/...` | 编译通过且单测覆盖 |
| integration tests | test | 测试方案 TC-API-* | `tests/...` | CI 执行通过 |

写法要求：

- 交付物必须能追溯到详细设计、测试方案或验收标准。
- 不得写“完善相关代码”这类不可判定交付物。
- 若交付物会影响多个仓，必须列出仓名和交互边界。

### 5.5 实施阶段与依赖顺序

本章是实施计划的核心，定义阶段顺序与依赖。

必须包含阶段依赖图：

````md
#### 阶段依赖图: <仓名> 实施阶段顺序

```text
[PH-01 前置准备]
  | enables
  v
[PH-02 最小 command 纵切]
  | depends_on
  v
[PH-03 query / projection 能力]
  | enables
  v
[PH-04 状态机与一致性补强]
```

关键说明：
- 图表达阶段之间的实施依赖，不表达完整函数调用链。
- 每个阶段必须在阶段表中继续说明输入、输出和门禁。
- 阶段不能按对象裸拆，必须形成可验证功能增量。
````

必须包含阶段总表：

| 阶段编号 | 阶段名称 | 实施目标 | 依赖阶段 | 核心交付物 | 阶段门禁 |
|---|---|---|---|---|---|
| PH-02 | 最小 command 纵切 | 打通创建命令到持久化的最小闭环 | PH-01 | command handler / aggregate / repo fake | GATE-01 / GATE-02 |

写法要求：

- 阶段顺序必须能解释为什么这样排。
- 高风险、不确定或跨仓依赖应尽量前置 Spike。
- 阶段必须绑定测试或验收门禁。

### 5.6 阶段任务拆分、编写顺序与提交边界

本章展开每个阶段内部的实施任务、完成判定和提交边界。

每个阶段必须使用独立小节。

推荐格式：

```md
### PH-02 最小 command 纵切

#### 阶段任务表

| 任务编号 | 编写顺序 | 实施动作 | 输入 | 输出 | 完成判定 |
|---|---:|---|---|---|---|
| IMPL-02-01 | 1 | 建立 command handler 骨架 | 详细设计 DD-API-001 | handler 编译通过 | 单测能构造 command |

#### 提交边界

| 提交边界 | commit 时机 | 包含内容 | 不包含内容 | 提交前门禁 |
|---|---|---|---|---|
| commit-02-a | 最小 command 纵切打通且门禁通过后 | command handler + domain 最小闭环 | query / projection | GATE-01 / GATE-02 |
```

写法要求：

- 任务必须以实施动作命名。
- 任务必须写明阶段内编写顺序。
- 提交边界必须说明包含和不包含。
- 提交边界必须说明 commit 时机。
- 不允许把多个无关阶段合成一个提交。
- 不允许把未验证代码作为阶段完成。

### 5.7 测试与验收门禁嵌入

本章说明测试方案和验收标准如何进入实施过程。

必须包含表格：

| 阶段编号 | 测试门禁 | 验收门禁 | 证据产物 | 失败处理 |
|---|---|---|---|---|
| PH-02 | TC-UNIT-001 / TC-API-001 | AC-001 | test log / coverage summary | 修复后重跑本阶段门禁 |

写法要求：

- 每个阶段至少有一个测试门禁。
- 涉及用户可见行为、跨仓接口、状态转换或数据一致性的阶段，必须有验收门禁引用。
- 门禁必须说明证据归档方式。

### 5.8 配置、环境与外部依赖准备

本章说明实施前或阶段前必须准备的配置、环境和外部依赖。

必须包含表格：

| 依赖项 | 类型 | 使用阶段 | 提供方 | 检查方式 | 不可用时处理 |
|---|---|---|---|---|---|
| event bus | external service | PH-03 | L1-event-bus | health check | 使用 fake adapter 或暂停该阶段 |

写法要求：

- 外部依赖必须标明提供方。
- 不能把外部依赖隐含在任务里。
- 如果允许 fake / mock，必须说明只允许用于哪些阶段。

### 5.9 Spike、风险与待确认事项

本章前置识别不确定性。

必须包含表格：

| 编号 | 类型 | 描述 | 影响阶段 | 处理方式 | 截止点 |
|---|---|---|---|---|---|
| R-001 | risk | event bus 本地不可用 | PH-03 | 先写 port + fake，真实联调后补门禁 | PH-03 前 |
| SP-001 | spike | 验证迁移工具是否满足索引需求 | PH-04 | 单独 Spike，不混入功能提交 | PH-04 前 |

写法要求：

- 风险必须对应阶段。
- Spike 必须有输出，不得无限探索。
- 待确认事项必须有截止点，不能长期悬空。

### 5.10 回退、暂停与变更控制

本章说明实施中断、失败或设计变更时如何处理。

必须包含表格：

| 触发条件 | 动作 | 责任方 | 保留证据 | 恢复条件 |
|---|---|---|---|---|
| 详细设计契约不完整 | 暂停对应阶段 | implementer / designer | blocker note | 详细设计补齐并确认 |
| 阶段门禁失败 | 停止进入下一阶段 | implementer | test log | 修复并重跑门禁 |

写法要求：

- 不允许用“视情况处理”替代明确动作。
- 涉及设计变化时，必须回写上游设计文档，不得只改代码。
- 回退规则必须优先保护已验证阶段。

### 5.11 提交、评审与交付纪律

本章定义实施过程中的提交和评审规则。

必须包含：

| 项 | 要求 | 检查方式 |
|---|---|---|
| git user.name | `quantalithos-labs` | `git config user.name` |
| git user.email | `quantalithos.ai@gmail.com` | `git config user.email` |
| 提交规范 | 遵循项目提交规范 | 提交前阅读规范和历史提交 |
| 提交粒度 | 对应一个可验证提交边界 | 对照 §6 提交边界 |
| 提交信息 | subject / body / footer 符合项目规范 | 对照近期合格提交 |
| 提交前检查 | diff、门禁、文档、证据和 footer 均已确认 | 对照本章检查清单 |
| 代码规范 | 遵循对应语言编码规范 | fmt / lint / review |
| 源码语言 | 实现代码仓源码标识符、rustdoc、普通注释和测试名默认英文 | review / lint / grep |
| 文档同步 | 设计偏离时回写文档 | diff review |

提交规范明细必须包含：

| 明细项 | 必须说明的内容 |
|---|---|
| 提交时机 | 何时允许 commit，何时禁止 commit |
| 提交粒度 | 过细、适中、过粗的判断标准 |
| message 结构 | `<type>(<scope>): <subject>`、body、footer 的结构 |
| Type / Scope | 可用 type 列表，以及本项目可用 scope 或省略 scope 的理由 |
| body 格式 | 是否按功能分组、是否列文件、是否标记增删改行数 |
| footer 策略 | AI 注脚、固定 footer、issue / breaking change footer 的顺序 |
| 语言边界 | design 文档仓与实际实现代码仓的提交语言和源码注释语言差异 |
| 示例 | 至少给出一条符合当前项目的 commit 示例 |
| 自检清单 | 提交前必须检查的 subject、body、footer、门禁、文档同步和源码语言项 |

提交前检查清单必须包含：

| 检查项 | 通过条件 |
|---|---|
| git 配置 | `user.name`、`user.email` 符合项目要求 |
| diff 范围 | 当前 diff 只覆盖一个提交边界，不混入无关修改 |
| 门禁结果 | 该提交边界要求的 fmt / lint / test / acceptance 已执行并记录结果 |
| 文档同步 | 设计偏离、接口变更或验收口径变化已回写文档 |
| 源码语言 | 实现仓源码标识符、rustdoc、普通注释和测试名未混入中文 |
| 证据记录 | 测试输出、验收证据或人工复核结论已有落点 |
| 提交信息 | subject、body、footer 符合项目提交规范和历史风格 |

写法要求：

- 必须要求提交前阅读提交规范和历史提交。
- 必须要求提交范围与阶段边界一致。
- 必须要求提交时机与 §6 的提交边界一致。
- 必须禁止把格式化、设计变更、功能实现、测试补充混成不可审查的一笔。
- 提交注脚如项目要求使用固定 footer，必须在本章说明。
- 必须给出当前项目固定 footer 的具体文本；不得只写“添加 AI 注脚”。
- 必须区分设计文档仓和实现代码仓：设计文档允许中文说明；实现代码仓的源码注释、rustdoc、测试名和标识符默认必须英文。

### 5.12 实施完成判定

本章说明什么时候可以宣称实施完成。

必须包含表格：

| 判定项 | 标准 | 证据 | 结论 |
|---|---|---|---|
| 需求覆盖 | 本轮 FR 全部有实现或明确延期记录 | trace matrix | 通过 / 不通过 |
| 测试门禁 | 阶段门禁全部通过 | CI log / test log | 通过 / 不通过 |
| 验收门禁 | AC 项可判定且无一票否决 | acceptance evidence | 通过 / 不通过 |

写法要求：

- 完成判定必须可审查。
- 未完成项必须转为风险接受、延期项或 blocker。
- 不允许用“基本完成”作为最终结论。

### 5.13 参考

本章列出实施计划引用的规范、模板、标准和历史样例。

写法要求：

- 只列真实引用过的材料。
- 不得把未阅读文档堆入参考。
- 必须包含提交规范、编码规范和本书写规范。

---

## 六、评审清单

实施计划完成后，至少按以下清单自查：

| 检查项 | 是否通过 |
|---|---|
| 是否明确引用需求、架构、概要、详细、测试方案和验收标准 |
| 是否列出实施前置阅读清单、编码规范和提交规范 |
| 是否包含项目级 git user.name / user.email 检查 |
| 是否按可验证功能增量拆分阶段，而不是按对象、函数或文件拆分 |
| 是否每个阶段都有输入、输出、依赖、门禁和提交边界 |
| 是否每个阶段都说明阶段内编写顺序 |
| 是否每个提交边界都说明 commit 时机 |
| 是否测试方案和验收标准被嵌入阶段门禁 |
| 是否列出配置、环境和外部依赖准备 |
| 是否列出 Spike、风险、待确认事项和截止点 |
| 是否定义暂停、回退和变更控制 |
| 是否定义实施完成判定和证据 |
| 是否没有复制详细设计中的完整实现契约 |

---

## 七、最小模板

```md
# 07-实施计划 · <仓名 / 模块名>

## 文档元信息

| 项 | 内容 |
|---|---|
| 文档版本 | v0.1 |
| 适用仓库 | <repo-name> |
| 上游文档 | 00 / 01 / 02 / 03 / 05 / 06 |
| 当前状态 | draft / reviewed / approved |

### 变更记录

| 版本 | 日期 | 变更说明 | 作者 |
|---|---|---|---|
| v0.1 | YYYY-MM-DD | 初稿 | <author> |

## 1. 与上游文档的关系声明

## 2. 实施目标与范围

## 3. 实施前置条件与阅读清单

## 4. 实施对象与交付物清单

## 5. 实施阶段与依赖顺序

## 6. 阶段任务拆分、编写顺序与提交边界

## 7. 测试与验收门禁嵌入

## 8. 配置、环境与外部依赖准备

## 9. Spike、风险与待确认事项

## 10. 回退、暂停与变更控制

## 11. 提交、评审与交付纪律

## 12. 实施完成判定

## 13. 参考
```

---

## 八、正反例

### 8.1 正确示例

| 阶段编号 | 阶段名称 | 实施目标 | 依赖阶段 | 核心交付物 | 阶段门禁 |
|---|---|---|---|---|---|
| PH-02 | 创建成员最小纵切 | 打通 API -> application -> domain -> repository 的创建闭环 | PH-01 | command handler / aggregate / repo fake / unit tests | GATE-01 / GATE-02 |

说明：

- 阶段按功能纵切命名。
- 阶段有依赖、交付物和门禁。
- 实施者知道本阶段完成后可以提交什么。

### 8.2 错误示例

```text
第一阶段：写 GlobalMember
第二阶段：写 ProjectMember
第三阶段：写 Repository
第四阶段：写测试
```

问题：

- 这是按对象拆分，不是按可验证功能增量拆分。
- 每阶段都缺少输入、输出、门禁和提交边界。
- 测试被放到最后，无法形成阶段验证。
- 无法判断每个阶段是否真的完成。

# 架构设计讨论流程_SOP

> 目标：定义“架构设计内容如何生成”，用于补足 `standards/document/架构设计书写规范.md` 目前只定义结果框架、不定义生成流程的问题。  
> 适用对象：人类作者、Claude/其他 Agent、多人协作讨论。

---

## 修订记录

| 版本 | 日期 | 讨论主题 | 修订内容 |
|---|---|---|---|
| v0.1-discussion | 2026-05-20 | Step 3/4 顺序回正 | 明确“职责边界”先于“系统边界与上下文”，并同步回正架构 SOP 中两步的顺序与命名 |
| v0.1-discussion | 2026-05-20 | Step 5 与 4.6 同步 | 将 Step 5 收敛为“限界上下文与子域划分”过程步骤，保留子域问题、输入/输出、回填位置、短错误提醒和门禁，具体划分表与关系图规则统一引用规范 4.6 |
| v0.1-discussion | 2026-05-20 | Step 4 与 4.5 同步 | 将 Step 4 收敛为“系统边界与上下文”过程步骤，保留上下文问题、输入/输出面问题、回填位置、短错误提醒和门禁，具体图与表结构统一引用规范 4.5 |
| v0.1-discussion | 2026-05-20 | Step 3 与 4.4 同步 | 将 Step 3 收敛为“职责边界”过程步骤，保留职责问题、边界问题、回填位置、短错误提醒和门禁，具体职责边界表结构统一引用规范 4.4 |
| v0.1-discussion | 2026-05-20 | Step 2 与 4.2 / 4.3 同步 | 将 Step 2 收敛为“架构目标与约束”过程步骤，保留目标问题、约束问题、取舍问题和回填位置，结果分别回填规范 4.2 业务背景与驱动力、4.3 约束条件 |
| v0.1-discussion | 2026-05-20 | Step 1 与 4.1 / 4.3 / 4.16 同步 | 将 Step 1 收敛为“确认需求基线”过程步骤，保留基线问题、输入、输出、回填位置和门禁，结果用于支撑规范 4.1 来源承接前提、4.3 约束条件与 4.16 需求追溯矩阵 |
| v0.1-discussion | 2026-05-20 | 架构 SOP 方法论对齐 | 借鉴需求 SOP，补入“结果结构与生成流程分离”“按输入驱动输出收敛”“门禁真实生效”等核心原则 |
| v0.1-discussion | 2026-05-20 | Step 模板字段关系 | 增补 Step 模板的 8 字段关系说明，明确目标、输入、输出、问题、产出、回填、约束、门禁之间的执行逻辑 |
| v0.1-discussion | 2026-05-20 | 架构 SOP 工作底稿初始化 | 基于架构设计 agent guide 创建正式工作稿，后续按章节逐步收敛 |
| v0.2 | 2026-05-25 | 对齐架构书写规范收口 | 移除 Step 5 ~ Step 16 未定稿旧提示，补充产图 Step 的 ASCII 图统一输出约束 |

---

## 1. 适用前提

本 SOP 适用于：

```text
需要为一个仓 / 一个模块 / 一个子系统编写 01-架构设计.md
且希望先通过讨论逐步收敛内容，再落正式文档
```

它不适用于：

```text
小型修订
错别字修复
纯格式清理
已有文档的局部补丁
```

---

## 2. 核心原则

### 2.1 先讨论，后成文

架构设计不应由 Agent 一次性生成完整正文。正确方式是逐章讨论、逐章收敛，再逐章填入正式文档。

### 2.2 先需求基线，后架构推导

架构设计必须建立在需求基线稳定的前提下。

### 2.3 先前提，后展开

架构设计应先收敛：

```text
需求基线
架构目标与约束
系统边界
职责边界
数据所有权
```

再展开容器、交互、通信方式和技术选型。

### 2.4 结果结构与生成流程分离

- `架构设计书写规范.md` 负责定义结果结构。
- `架构设计讨论流程_SOP.md` 负责定义生成流程。

### 2.5 按输入驱动输出收敛

每一步不是先看问题再自由作答，而是：

```text
先检查本步输入是否满足
再确认本步输出是什么
然后围绕输出反推问题
最后按书写规范落文
```

### 2.6 门禁真实生效

如果本步输入不足，或本步输出未收敛，就不应进入下一步。

### 2.7 不确定项不脑补

架构上没有收稳的内容必须进入风险与待确认事项。

### 2.8 ASCII 图输出统一

凡是本 SOP 中要求或允许输出 ASCII 图的 Step，都必须在讨论阶段同时收稳：

```text
图类型
图标题
ASCII 图正文
图后 2~5 条说明
```

讨论阶段禁止只说“画一个图”或只输出裸图。所有 ASCII 图必须遵守 `架构设计书写规范` 5.3 `ASCII 图统一格式`。

如果某一步按需可画图但本轮不画，必须说明“不画图的原因”，不能留下空图占位。

---

## 3. Step 模板的字段关系

每个 Step 都由以下字段组成：

- 本步目标
- 本步输入
- 本步输出
- 应问的问题
- 期望产出
- 回填位置
- 执行约束
- 进入下一步的条件

这些字段不是并列 checklist，而是一个有顺序的收敛单元。

### 字段职责说明

| 字段 | 作用 |
|---|---|
| 本步目标 | 定义本步存在的意义，说明这一步要解决什么问题。 |
| 本步输入 | 定义本步开始前必须具备的前提、上游结论或已收敛结果。 |
| 本步输出 | 定义本步必须收敛出的结果类型。 |
| 应问的问题 | 用于把输入收敛成输出的问题集合，不是独立问卷。 |
| 期望产出 | 本步应交付的具体结论项，是对“本步输出”的展开。 |
| 回填位置 | 指明本步产出最终写入的规范章节或正式文档位置。 |
| 执行约束 | 规定本步过程中哪些做法允许，哪些做法禁止。 |
| 进入下一步的条件 | 判断本步是否完成的门槛。 |

### 执行顺序

执行顺序不是“按字段随意阅读”，而应按以下顺序推进：

```text
本步目标
  -> 本步输入
  -> 本步输出
  -> 应问的问题
  -> 执行约束
  -> 期望产出
  -> 回填位置
  -> 进入下一步的条件
```

### 关系说明

```text
输入限定本步前提。
输出定义本步目标结果。
问题用于把输入收敛成输出。
产出是输出的具体化清单。
回填位置决定这些产出最终落在哪里。
执行约束防止本步跑偏。
进入下一步的条件判断本步是否完成。
```

也就是说：

```text
t目标定意义
输入定前提
输出定结果
问题做收敛
约束防跑偏
产出做交付
回填定落点
门禁定完成
```

---

## 4. 建议的讨论总流程

```text
Step 1. 确认需求基线
Step 2. 明确架构目标与约束
Step 3. 职责边界
Step 4. 系统边界与上下文
Step 5. 限界上下文与子域划分
Step 6. 容器与部署视图
Step 7. 依赖方向与层间约束
Step 8. 数据所有权与一致性策略
Step 9. 关键交互与通信方式
Step 10. 关键技术选型
Step 11. 备选方案与取舍
Step 12. 横切关注点
Step 13. 演进路线
Step 14. 风险与待确认事项
Step 15. ADR 与需求追溯
Step 16. 整理正式文档
```

---

## 5. 每一步的讨论目标、输入与产出

> 说明：当前正式正文已补齐 Step 1 ~ Step 16 的 8 字段骨架，并与 `架构设计书写规范.md` 4.1 ~ 4.18 形成对应关系。后续讨论应以本 SOP 的 Step 顺序生成内容，以书写规范的章节格式约束最终输出。

### Step 1. 确认需求基线

#### 本步目标
确认当前架构设计依赖的需求结论已经收敛到足以支撑架构推导的程度，并识别哪些需求结论会直接影响系统边界、数据所有权和依赖方向。

#### 本步输入
- 当前仓对应的需求文档
- 上游产品文档与全局架构文档中的相关结论
- 当前已知的仓边界、核心能力闭环、数据归属、依赖与风险结论

#### 本步输出
- 架构需求基线清单
- 架构硬约束清单
- 尚未关闭的需求风险清单

#### 应问的问题

```text
当前架构设计依赖哪些需求结论？
这些需求结论里，哪些已经稳定？
哪些需求结论仍然待确认？
哪些需求会直接影响架构边界？
哪些需求会直接影响数据所有权？
哪些需求会直接影响依赖方向或一致性策略？
```

#### 期望产出

```text
需求基线结论
架构硬约束结论
未关闭需求风险结论
```

#### 回填位置
- `架构设计书写规范` 3 约束条件
- `架构设计书写规范` 16 需求追溯矩阵

#### 执行约束
- 具体输出形式、粒度要求和正反例，以 `架构设计书写规范` 4.1 为准。
- 本步只提炼对架构有约束力的需求结论，不重写需求文档全文。
- 不确定项必须显式挂入风险，不得脑补成架构事实。

#### 进入下一步的条件

```text
已明确哪些需求可直接作为架构前提，哪些需求仍然待确认，且足以支撑进入架构目标与约束讨论。
```

---


### Step 2. 明确架构目标与约束

#### 本步目标
把需求层已经收稳的边界、能力和数据前提转译成架构必须确保成立的结构目标、不可变约束、当前阶段可接受取舍和架构非目标。

#### 本步输入
- Step 1 已收敛的架构需求基线清单
- 当前仓的上游全局架构约束
- 当前阶段的现实前提与已知风险

#### 本步输出
- 架构目标结论
- 不可变约束结论
- 可接受取舍结论
- 架构非目标结论

#### 应问的问题

```text
这个仓在架构层面要确保什么成立？
哪些约束是不可变的？
哪些约束是当前阶段可以接受的取舍？
哪些目标可以明确判断，甚至量化？
哪些事情虽然相关，但不是本仓架构当前要解决的问题？
```

#### 期望产出

```text
架构目标结论
不可变约束结论
可接受取舍结论
架构非目标结论
```

#### 回填位置
- `架构设计书写规范` 2 业务背景与驱动力
- `架构设计书写规范` 3 约束条件

#### 执行约束
- 具体输出形式、粒度要求和正反例，以 `架构设计书写规范` 4.2 为准。
- 不写空泛口号，不提前写容器、依赖图、数据库或技术方案。
- 架构目标写“必须成立什么”，约束写“绝不能碰什么”，取舍写“当前有意识不做深什么”，非目标写“明确不展开什么范围”。

#### 进入下一步的条件

```text
已明确架构必须确保什么成立、哪些边界不可变、哪些内容当前可接受收缩、哪些事项不属于当前架构主线。
```

---

### Step 3. 职责边界

#### 本步目标
明确本仓在全局职责分工中的承担范围，收稳“做什么 / 不做什么 / 易混淆职责 / 边界红线”。

#### 本步输入
- Step 1 已收敛的架构需求基线
- Step 2 已收敛的架构目标与约束
- 已知的仓级职责边界线索

#### 本步输出
- 职责边界结论
- 非职责结论
- 易混淆职责结论
- 边界红线结论

#### 应问的问题

```text
这个仓具体做什么？
这个仓具体不做什么？
哪些能力看起来相关但必须属于其他仓？
哪些行为绝不能隐式发生？
哪些边界如果不写清，后续设计最容易串线？
```

#### 期望产出

```text
做 / 不做清单
职责边界表
边界红线清单
```

#### 回填位置
- `架构设计书写规范` 4 职责边界

#### 执行约束
- 具体输出形式、粒度要求和正反例，以 `架构设计书写规范` 4.4 为准。
- 不重画系统上下文图，不提前展开子域、数据所有权或接口协议。

#### 常见错误提醒

```text
不要把上下文关系写成职责，不要把功能项直接写成职责，不要把实现方案写进说明列。
```

#### 进入下一步的条件

```text
已明确本仓做什么、不做什么、易混淆职责和边界红线，且不会把相邻章节内容混写进本章。
```

---

### Step 4. 系统边界与上下文

#### 本步目标
说明这个仓在全局系统中的位置，明确它有哪些正式上下文对象、输入面、输出面以及外部边界。

#### 本步输入
- Step 1 已收敛的架构需求基线清单
- Step 2 已收敛的架构目标与约束
- 已知的上下游仓 / 外部系统 / 外部能力事实

#### 本步输出
- 系统上下文图
- 上下游与输入/输出面结论
- 边界说明结论

#### 应问的问题

```text
这个仓在全局系统中的位置是什么？
它有哪些正式上游？
它有哪些正式下游？
它从外部接收哪些输入面？
它向外部提供哪些输出面？
哪些外部系统或相邻仓构成正式上下文边界？
依赖失效时，本仓的降级口径是什么？
```

#### 期望产出

```text
系统上下文图
上下游与输入/输出面表
边界说明短文
```

#### 回填位置
- `架构设计书写规范` 5 系统边界与上下文

#### 执行约束
- 具体输出形式、粒度要求和正反例，以 `架构设计书写规范` 4.5 为准。
- 只表达正式上下文关系，不展开内部职责、数据所有权、容器部署或接口协议。
- 图中不画角色，不画文档来源对象，不写接口名或事件名。
- 系统上下文图必须使用 `text` 代码块，并在图后写 2~5 条说明。
- 系统上下文图必须遵守 `架构设计书写规范` 5.3 `ASCII 图统一格式`。

#### 常见错误提醒

```text
不要把文档来源对象画进图，不要把角色画进图，不要把接口名或事件名写进图和表。
```

#### 进入下一步的条件

```text
已明确本仓在全局系统中的位置，已画出正式对象的上下文图，并已用表格和短文解释其关系语义。
```

---

### Step 5. 限界上下文与子域划分

#### 本步目标
说明本仓内部语义结构如何划分：哪些是核心子域，哪些是支撑子域，哪些只是本地索引 / 本地投影 / 本地引用，以及它们之间的上下文映射关系。

#### 本步输入
- Step 3 已收敛的职责边界
- Step 4 已收敛的系统边界与上下文
- 当前仓核心语义对象、快照对象、引用对象的已知线索

#### 本步输出
- 子域 / 上下文划分结论
- 上下文关系结论
- 本地索引 / 投影 / 引用边界结论
- 统一语言词汇结论

#### 应问的问题

```text
本仓内部有哪些子域或本地上下文？
哪些是核心子域？
哪些是支撑子域？
哪些只是外部上下文的本地索引 / 投影 / 引用？
它们之间的上下文映射关系是什么？
为什么这些部分不能混成一个上下文？
```

#### 期望产出

```text
子域 / 上下文划分结论
上下文关系结论
本地索引 / 投影 / 引用边界结论
统一语言词汇结论
```

#### 回填位置
- `架构设计书写规范` 6 限界上下文与子域划分

#### 执行约束
- 具体输出形式、粒度要求和正反例，以 `架构设计书写规范` 4.6 为准。
- 只讨论本仓内部语义结构，不提前展开容器部署、层间依赖、数据一致性或接口协议。
- 不写对象字段、数据库表、代码目录、函数接口或实现组件。
- 不确定项必须显式保留，不得把本地影子结构误写成核心子域。
- 如输出上下文关系图，必须使用 `text` 代码块，并在图后写 2~5 条说明。
- 上下文关系图必须遵守 `架构设计书写规范` 5.3 `ASCII 图统一格式`。

#### 进入下一步的条件

```text
已明确本仓内部语义结构层次，已区分核心子域、支撑子域和本地索引 / 投影 / 引用，且不会把实现结构误写成上下文划分。
```

---

### Step 6. 容器 / 部署架构

#### 本步目标
说明这个仓在运行时由哪些容器、入口、后台处理单元和存储承载，并明确它们之间的正式通信与部署关系。

#### 本步输入
- Step 4 已收敛的系统边界与上下文
- Step 5 已收敛的限界上下文与子域划分
- 当前仓已知的运行单元、入口形态、后台处理形态与数据存储线索

#### 本步输出
- 容器划分结论
- 容器职责结论
- 部署关系结论
- 通信方式结论

#### 应问的问题

```text
这个仓运行时有哪些正式容器或运行单元？
同步入口在哪里？
异步消费者或后台任务在哪里？
数据库 / 缓存 / 总线如何接入？
哪些运行单元必须分开部署，哪些可以同部署？
哪些通信关系是正式主路径？
```

#### 期望产出

```text
容器图
容器职责结论
部署关系结论
通信方式结论
```

#### 回填位置
- `架构设计书写规范` 7 容器 / 部署架构

#### 执行约束
- 具体输出形式、粒度要求和正反例，以 `架构设计书写规范` 4.7 为准。
- 本步只讨论运行时容器、正式通信和部署拓扑，不展开代码目录、模块拆分或函数职责。
- 容器图不是源码结构图，不得写 `src/` 目录、包名、文件名或实现组件细节。
- 容器 / 部署架构图必须使用 `text` 代码块，并在图后写 2~5 条说明。
- 容器 / 部署架构图必须遵守 `架构设计书写规范` 5.3 `ASCII 图统一格式`。
- 不提前展开数据所有权矩阵、协议 schema 或技术选型理由。

#### 进入下一步的条件

```text
已明确本仓运行时由哪些正式容器承载、它们各自承担什么职责、如何通信与部署，且不会把源码结构误写成容器视图。
```

---

### Step 7. 依赖方向与层间约束

#### 本步目标
明确本仓内部层次如何划分、允许哪些依赖方向、禁止哪些反向依赖，以及哪些依赖必须通过边界倒置来接入。

#### 本步输入
- Step 5 已收敛的限界上下文与子域划分
- Step 6 已收敛的容器与部署视图
- 当前仓已知的内部层次、外部接入点和适配边界线索

#### 本步输出
- 层次划分结论
- 依赖方向结论
- 层间约束结论
- 依赖倒置结论

#### 应问的问题

```text
本仓内部层次如何划分？
允许哪些依赖方向？
禁止哪些反向依赖？
外部系统通过哪些正式边界接入？
哪些依赖必须倒置，不能直接侵入核心语义层？
哪些规则若不先写清，后续实现最容易失控？
```

#### 期望产出

```text
层次划分结论
依赖方向结论
层间约束结论
依赖倒置结论
```

#### 回填位置
- `架构设计书写规范` 8 依赖方向与层间约束

#### 执行约束
- 具体输出形式、粒度要求和正反例，以 `架构设计书写规范` 4.8 为准。
- 本步只讨论依赖方向和层间规则，不展开接口协议、数据库细节或具体代码调用链。
- 不把运行时通信方式误写成层间依赖规则，不把模块目录误写成架构层次。
- 禁止把 adapter、repository、handler 之类实现名词直接当成通用架构规则本身。
- 依赖方向图必须使用 `text` 代码块，并在图后写 2~5 条说明。
- 依赖方向图必须遵守 `架构设计书写规范` 5.3 `ASCII 图统一格式`。

#### 进入下一步的条件

```text
已明确内部层次、允许与禁止的依赖方向、必要的倒置边界，且不会把实现目录或调用链误写成层间约束。
```

---

### Step 8. 数据所有权与一致性策略

#### 本步目标
明确哪些数据由本仓拥有正式真相，哪些只是快照、投影或引用，以及不同数据关系需要采用什么一致性口径。

#### 本步输入
- Step 3 已收敛的职责边界
- Step 5 已收敛的限界上下文与子域划分
- 当前仓已知的数据归属线索、状态变化线索与跨仓同步线索

#### 本步输出
- 数据所有权结论
- 数据分类结论
- 一致性口径结论
- 补偿 / 约束结论

#### 应问的问题

```text
哪些数据由本仓拥有真相？
哪些数据只是快照、投影或引用？
哪些关系必须强一致？
哪些关系可以最终一致？
失败时靠什么口径约束、补偿或挂起？
哪些数据边界如果不写清，后续最容易串仓？
```

#### 期望产出

```text
数据所有权结论
数据分类结论
一致性口径结论
补偿 / 约束结论
```

#### 回填位置
- `架构设计书写规范` 9 数据所有权与一致性策略

#### 执行约束
- 具体输出形式、粒度要求和正反例，以 `架构设计书写规范` 4.9 为准。
- 不写表结构、字段清单、DDL、缓存策略或同步实现细节。
- 一致性策略写需求层和架构层口径，不写事务脚本、事件 schema 或重试实现。
- 不得把“当前尚未确定”的同步关系硬写成真相归属或一致性事实。
- 如补简化关系示意图，必须使用 `text` 代码块，并在图后写 2~5 条说明。
- 简化关系示意图必须遵守 `架构设计书写规范` 5.3 `ASCII 图统一格式`。

#### 进入下一步的条件

```text
已明确本仓拥有与不拥有的正式数据边界，已区分强一致与最终一致口径，且不会把实现层存储细节误写成所有权策略。
```

---

### Step 9. 关键交互与通信方式

#### 本步目标
明确本仓与外部或内部相邻部分之间的关键交互应采用同步调用、异步事件还是后台任务，并说明这些通信方式的边界依据。

#### 本步输入
- Step 4 已收敛的系统边界与上下文
- Step 6 已收敛的容器与部署视图
- Step 8 已收敛的数据所有权与一致性策略

#### 本步输出
- 关键交互结论
- 通信方式结论
- 失败降级结论
- 边界约束结论

#### 应问的问题

```text
哪些交互适合同步能力边界？
哪些交互适合异步事件？
哪些交互适合后台任务或补偿路径？
哪些交互必须经过总线或正式边界，不能直接穿透？
关键依赖失效时，本仓如何降级或挂起？
哪些通信口径若不先写清，后续最容易误入协议细节？
```

#### 期望产出

```text
关键交互结论
通信方式结论
失败降级结论
边界约束结论
```

#### 回填位置
- `架构设计书写规范` 10 关键交互与通信方式

#### 执行约束
- 具体输出形式、粒度要求和正反例，以 `架构设计书写规范` 4.10 为准。
- 只讨论通信方式选择与边界理由，不写 API 路径、事件名、DTO、schema 或时序细节。
- 不把运行顺序图误写成通信方式结论，不把协议实现误写成架构交互原则。
- 不提前写技术选型理由；本步只回答“该用哪类通信方式及其边界原因”。
- 如补简化交互示意图，必须使用 `text` 代码块，并在图后写 2~5 条说明。
- 简化交互示意图必须遵守 `架构设计书写规范` 5.3 `ASCII 图统一格式`。

#### 进入下一步的条件

```text
已明确关键交互分别采用何种正式通信方式、为什么成立、失败时如何降级或挂起，且不会把协议细节误写成通信原则。
```

---

### Step 10. 关键技术选型

#### 本步目标
明确当前架构中哪些关键技术机制、风格或治理手段被正式采用，并说明它们各自解决什么问题、带来什么代价。

#### 本步输入
- Step 2 已收敛的架构目标与约束
- Step 7 已收敛的依赖方向与层间约束
- Step 8 已收敛的数据所有权与一致性策略
- Step 9 已收敛的关键交互与通信方式

#### 本步输出
- 关键技术选型结论
- 选择理由结论
- 代价 / 风险结论
- 不采用口径结论

#### 应问的问题

```text
当前采用哪些关键架构机制？
每个机制解决什么问题？
为什么不用其他方案？
每个选型带来什么代价或新风险？
哪些选型是当前阶段必要的，哪些暂不引入？
```

#### 期望产出

```text
关键技术选型结论
选择理由结论
代价 / 风险结论
不采用口径结论
```

#### 回填位置
- `架构设计书写规范` 11 关键技术选型

#### 执行约束
- 具体输出形式、粒度要求和正反例，以 `架构设计书写规范` 4.11 为准。
- 每个选型都必须说明“为什么”，不能只列技术名词。
- 不把部署事实、接口协议或实现细节直接当成选型理由。
- 不提前展开完整备选方案对比；本步只收稳“当前选什么、为何成立、代价是什么”。

#### 进入下一步的条件

```text
已明确当前正式采用哪些关键技术机制、各自解决什么问题、代价是什么，且不会把技术名词清单误当成选型结论。
```

---

### Step 11. 备选方案与取舍

#### 本步目标
把当前架构方案与主要备选方案放到同一判断框架下比较，说明为什么选择当前方案，以及为什么放弃其他方案。

#### 本步输入
- Step 2 已收敛的架构目标与约束
- Step 10 已收敛的关键技术选型
- 当前已知的替代架构路径与取舍前提

#### 本步输出
- 备选方案结论
- 方案对比结论
- 最终选择结论
- 不采用原因结论

#### 应问的问题

```text
这个仓有哪些主要可选架构方案？
为什么当前选择这一种？
被放弃方案的主要优点是什么？
为什么即便有这些优点，当前仍不采用？
当前选择牺牲了什么，换来了什么？
```

#### 期望产出

```text
备选方案结论
方案对比结论
最终选择结论
不采用原因结论
```

#### 回填位置
- `架构设计书写规范` 12 备选方案与取舍

#### 执行约束
- 具体输出形式、粒度要求和正反例，以 `架构设计书写规范` 4.12 为准。
- 备选方案必须是架构层有效替代路径，不写琐碎实现变体。
- 不把“当前还没做”误写成备选方案，不把边界外事项误写成被放弃选项。
- 对比应围绕边界、依赖、数据、一致性、演进代价等架构问题，不写空泛优缺点。

#### 进入下一步的条件

```text
已明确当前方案与主要替代方案之间的关键差异、为什么选择当前方案、为什么放弃其他方案，且不会把阶段延后事项误写成备选方案。
```

---

### Step 12. 横切关注点

#### 本步目标
明确安全、可观测性、可用性、性能预算、配置管理、审计追溯等横切要求在本仓架构层如何被正式约束。

#### 本步输入
- Step 2 已收敛的架构目标与约束
- Step 8 已收敛的数据所有权与一致性策略
- Step 9 已收敛的关键交互与通信方式
- Step 10 已收敛的关键技术选型

#### 本步输出
- 横切类别结论
- 横切约束结论
- 判断口径结论
- 边界适用性结论

#### 应问的问题

```text
安全边界如何处理？
可观测性需要覆盖哪些正式对象和关键链路？
可用性和韧性需要守住什么底线？
性能预算是否需要给出口径？
配置如何管理，哪些配置不应散落？
审计与可追溯性如何被正式保证？
哪些横切项与本仓无关，不应机械照抄模板？
```

#### 期望产出

```text
横切类别结论
横切约束结论
判断口径结论
边界适用性结论
```

#### 回填位置
- `架构设计书写规范` 13 横切关注点

#### 执行约束
- 具体输出形式、粒度要求和正反例，以 `架构设计书写规范` 4.13 为准。
- 横切关注点必须与本仓边界、通信和数据语义相关，不写与本仓无关的通用系统常识。
- 不写具体监控实现、告警配置、密钥存放脚本或性能压测脚本。
- 不能量化时，也必须给出清晰判断口径，而不是空洞口号。

#### 进入下一步的条件

```text
已按横切类别检查本仓适用项并收敛正式约束与判断口径，且不会把通用实现清单误写成横切架构结论。
```

---

### Step 13. 演进路线

#### 本步目标
说明当前阶段做到哪里、后续按什么阶段演进、已知设计债务哪些可接受，以及哪些条件会触发下一阶段调整。

#### 本步输入
- Step 10 已收敛的关键技术选型
- Step 11 已收敛的备选方案与取舍
- Step 12 已收敛的横切关注点
- 当前阶段边界与已知设计债务

#### 本步输出
- 当前阶段结论
- 演进阶段结论
- 已知债务结论
- 触发条件结论

#### 应问的问题

```text
当前阶段做到哪里才算足够？
第一批必须守住哪些结构？
哪些能力或约束留到后续阶段演进？
哪些设计债务当前可接受，哪些不可接受？
未来哪些触发条件会迫使架构调整？
```

#### 期望产出

```text
当前阶段结论
演进阶段结论
已知债务结论
触发条件结论
```

#### 回填位置
- `架构设计书写规范` 14 演进路线

#### 执行约束
- 具体输出形式、粒度要求和正反例，以 `架构设计书写规范` 4.14 为准。
- 演进路线写“当前到后续”的结构演进，不写实施排期、任务拆单或项目管理细节。
- 已知债务必须说清为何当前可接受，不能用“以后再说”替代判断。
- 不把边界外能力误写成未来必做主线。

#### 进入下一步的条件

```text
已明确当前阶段边界、后续演进阶段、可接受的设计债务与触发调整条件，且不会把项目排期误写成架构演进路线。
```

---

### Step 14. 风险与待确认事项

#### 本步目标
显式收纳尚未关闭的架构风险和待确认问题，防止作者或 Agent 为了填满文档而脑补确定性结论。

#### 本步输入
- 前面各步骤中尚未完全收稳的结论
- 已显式暴露的边界、依赖、数据、一致性、交互或选型问题

#### 本步输出
- 风险结论
- 影响范围结论
- 当前处理口径结论
- 待确认事项结论
- 当前状态结论

#### 应问的问题

```text
当前还有哪些尚未关闭的架构风险？
这些风险会影响哪一层架构结构？
当前还有哪些待确认事项？
哪些待确认项会影响前文结论是否成立？
哪些风险是当前阶段可接受的，哪些会阻塞后续推进？
```

#### 期望产出

```text
风险结论
影响范围结论
当前处理口径结论
待确认事项结论
当前状态结论
```

#### 回填位置
- `架构设计书写规范` 15 风险与待确认事项

#### 执行约束
- 具体输出形式、粒度要求和正反例，以 `架构设计书写规范` 4.15 为准。
- 风险和待确认事项必须拆开表达，不得混写。
- 风险的当前处理口径写“当前如何约束 / 暂存”，不写最终解决方案；待确认事项写“当前如何挂起”，不写空泛状态词。
- 不确定项必须留在本步，不得回填成前文确定结论。

#### 进入下一步的条件

```text
已明确拆分风险与待确认事项，已说明其影响范围与当前处理口径，且不会为了形成完整叙事而脑补确定性架构结论。
```

---

### Step 15. ADR 与需求追溯

#### 本步目标
把前文已经确定的关键架构决定与其需求来源、约束来源和风险来源显式连接起来，并沉淀需要长期保留的 ADR 索引。

#### 本步输入
- Step 1 ~ Step 14 已收敛的正式结构结论
- 已识别的关键架构决定、核心约束与重要取舍

#### 本步输出
- ADR 索引结论
- 需求追溯结论
- 漏项检查结论

#### 应问的问题

```text
哪些架构决定需要沉淀为 ADR？
每个关键架构决定对应哪些需求、约束或风险来源？
是否存在没有需求来源的架构设计？
是否存在没有架构承接的核心需求或关键约束？
哪些取舍和红线必须长期可追溯？
```

#### 期望产出

```text
ADR 索引结论
需求追溯结论
漏项检查结论
```

#### 回填位置
- `架构设计书写规范` 16 需求追溯矩阵
- `架构设计书写规范` 17 ADR 索引

#### 执行约束
- 具体输出形式、粒度要求和正反例，以 `架构设计书写规范` 4.16 和 4.17 为准。
- 不得在矩阵或 ADR 索引中新增前文未确认的新结论。
- 追溯要回答“为什么这样设计”，不是重复罗列章节标题。
- ADR 索引只记录需要长期保留的关键架构决定，不把普通实现选择全部塞入。

#### 进入下一步的条件

```text
已明确哪些架构决定需要 ADR，已建立关键需求与架构决定之间的追溯关系，且没有新增未确认项或孤儿结论。
```

---

### Step 16. 整理正式文档

#### 本步目标
把前面逐步确认的内容按正式结构整理为架构设计文档，只做重组、统一和回填，不新增未经讨论的新结论。

#### 本步输入
- Step 1 ~ Step 15 已确认并可回填的全部结论
- `架构设计书写规范.md` 已确定的正式章节主链

#### 本步输出
- 正式文档整理结论
- 章节回填结论
- 术语统一结论
- 交叉引用结论

#### 应问的问题

```text
哪些已确认结论应分别回填到哪些正式章节？
哪些结论需要拆分吸收到多个章节，而不是机械复制？
哪些术语、编号或交叉引用需要统一？
哪些内容仍应继续保留为风险或待确认，而不能润色成定论？
参考项应如何收口，不与 ADR 或追溯重复？
```

#### 期望产出

```text
正式文档重组结论
章节回填结论
术语统一结论
交叉引用结论
```

#### 回填位置
- `架构设计书写规范` 1 ~ 18 对应正式章节

#### 执行约束
- 只做重组、润色、统一术语、统一编号与补齐交叉引用。
- 不新增未经讨论的新结论，不在本步继续扩展分析。
- 必须显式保持：SOP 是生成流程，正式文档是结果结构；Step 与章节不是机械复制关系。
- `参考` 章节只收纳正式参考来源，不与 `ADR 索引`、`需求追溯矩阵` 混写。

#### 进入下一步的条件

```text
全部已确认结论都已按正式结构正确落位，术语与编号已统一，未确认项仍保留在风险或待确认中，且没有在整理阶段新增分析结论。
```

---
